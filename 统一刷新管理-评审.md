# 《统一刷新管理.md》评审报告（第十二版 · 第十一版复核）

> 视角：**元评审**——对第十一版（架构过度设计专项）做诚实复核。
> 第十一版提出 6 个问题（P1–P6），并对调用链、分层、模块关系做了一组判定。
> 本轮用 `wc`/`grep`/逐段实读回查证据，**逐条指出不准确、不清晰、切偏要点的地方**，
> 并给出**更精确的表述**。第十一版整体方向正确，但有 8 处需要修正，
> 其中 2 处会影响优先级判断、1 处会改变量化结论。
> 一句话结论：**P1/P4/P5 切中要点但表述过强、量化与"前两版维持"系无依据；P2/P3 切中要点；
> P6 把两条独立问题打包编号、应拆分；§1 `disposed` 计数口径模糊、§3 "11 跳"少算一倍、
> §4.1 "2 合理 1 可疑"误归类、§5.2(a) "运行期循环边"措辞不当、§8 与 §4.2 出现两个"最大"。**

---

## 0. 复核方法与判定原则

| 维度 | 判定原则 |
|---|---|
| **事实性** | 行号、计数、引用文本必须与 `wc -l`/`grep -n` 实测一致；推断型结论（"无人消费响应性""前两版评审判维持"）必须标注为推断 |
| **切题性** | 每条问题必须直指一个**有名字**的结构/接口/路径，且说明的"问题"是该实体的固有属性，而非外溢后果 |
| **影响** | 修法建议必须说明改的是契约、行为、还是结构；同一行的多个修法不能混为一谈 |
| **优先级** | 纯减法（不改任何外部行为）优先于结构调整（可能微调时序），优先于需求裁决（改契约） |

---

## 1. P1 `nextIdentity` 序号耗尽分支 —— **部分切中、关键事实误述**

### 1.1 第十一版原描述

> `manager.ts:564-571` 对「285 年才会耗尽」的安全整数上界做了完整的故障处理……
> 然后 `submit:118`、`query:164`、`resourceFor:376`、`enqueueTask:402` 四处各自消化 `null` 分支

### 1.2 实测纠偏

**行号偏移**（轻微，但全报告需统一核对）：

| 第十一版引用 | 实测行号 | 偏差 |
|---|---|---|
| `manager.ts:564-571` `nextIdentity` | `564-571` | ✓ 一致 |
| `submit:118` 消化 null | `117` | −1 |
| `query:164` 消化 null | `163` | −1 |
| `resourceFor:376` 消化 null | `375` | −1 |
| `enqueueTask:402` 消化 null | `401` | −1 |

实际后果几乎为零，但报告应避免「行号即证据」时的偏移。

**「各自消化 null 分支」这一表述不准确**：

```text
submit:118     if (id === null) return { status: 'cancelled', reason: CancelReason.Disposed }
query:164      if (id === null) return Promise.resolve({ status: 'cancelled', reason: CancelReason.Disposed })
resourceFor:376 if (id === null) return undefined
enqueueTask:402 if (version === null) return
```

四处都是「返回某个终止结果」的单行提前返回，**不是「消化 null 分支」**——
"消化" 暗示有 if/else 双路径，实际是单路径守卫。`enqueueTask` 的「return」等于「静默放弃」，
确实在正常路径里引入了一条早退，但 `resourceFor` 的 undefined 是被 `attach` 当作「资源缺失」处理
（继续走 fallback），与「失败」语义不同。这两种终止在调用链中**不是同类**，合并表述掩盖了差异。

**「前两版评审判「维持」，本轮架构视角改判」—— 无证据**：

第十一版附录把这一句作为 P1 的状态说明，但 `git log`/前文评审无任何线索表明前两版
讨论过 `nextIdentity`。这是**编造的引用**，本版删除该说法。

### 1.3 切中要点吗

**部分切中**。问题确实存在：4 处提前返回让 `resourceFor` 返回 `Resource | undefined`、
让 `enqueueTask` 出现静默 return。但第十一版把「守卫本身 + null 传播 + 类型带 undefined」
三者并列为问题，**实际上只有 null 传播和静态类型污染这两件事是真的问题**——
守卫本体（4 个 if、单行处理）规模极小，删除守卫并不能省行数。第十一版又说
"可测试性 −（删 4 个分支的用例）"，这反而承认守卫有覆盖成本，**自相矛盾**：
如果守卫本身成本可忽略、又确实不可达，为什么要改 throw 删掉它？

### 1.4 精确表述建议

```diff
- **P1（中）`nextIdentity` 序号耗尽的 null 传播。**
- `manager.ts:564-571` 对「285 年才会耗尽」的安全整数上界做了完整的故障处理：返回 `null`
- 并统一 `dispose`，然后 `submit:118`、`query:164`、`resourceFor:376`、`enqueueTask:402`
- 四处各自消化 `null` 分支（返回 cancelled / undefined / 静默放弃）。
- 前两版评审判「维持（有测试的安全网）」，本轮换架构视角**改判为轻度过度**：
- 问题不在守卫本身（4 个分支、有测试），而在 **null 沿调用链传播**——它让
- `resourceFor` 的返回类型带上 `undefined`，让 `enqueueTask` 出现「静默 return」路径，
- 这两处恰好都是正常逻辑的类型污染。建议：改为 `throw`（不可达异常，语义与
- 「耗尽不是产品行为」的文档声明一致），删掉 4 处 null 消化分支；
- 或至少收窄到 `resourceFor`/`enqueueTask` 不再返回可失败形状。
- 影响：可维护性 +（类型收窄）、可测试性 −（删 4 个分支的用例）、扩展性无影响。
+ **P1（中）`nextIdentity` 不可达路径污染正常返回类型。**
+ `manager.ts:564-571` 的耗尽分支本身只有 4 行（if + dispose + report + return），
+ 规模极小；真正的问题是它的返回值 `number | null` 沿调用链传染——
+ `resourceFor:375` 因此返回 `Resource | undefined`，`enqueueTask:401` 因此出现
+ 静默 return。两处都不是失败语义：前者代表「尚无资源」（attach 走 fallback），
+ 后者代表「资源已不再注册」（`registered()` 守卫外层已保护）。耗尽事件本身
+ 在 285 年尺度之外，但**类型污染是今天就付的成本**。
+ 修法（结构级，与守卫存在与否无关）：耗尽时改为 `throw`，调用方不再需要
+ 处理 `null` 分支，4 处守卫的早退可保留（语义清晰：null=已销毁守卫），
+ 或一并删除（依赖外层 `disposed`/`registered` 兜底）。**两条路都不动现有测试**
+ （耗尽分支本身没有验收覆盖—— grep 实测无 `'identity sequence exhausted'` 用例），
+ 影响：可维护性 +（类型收窄）、可测试性无变化、扩展性无影响。
```

---

## 2. P2 `createNamespace` 三级回退 —— **切中、表述准确**

### 2.1 第十一版原描述

> `app.ts:34-42`：`randomUUID` → `getRandomValues` 拼 16 字节 hex → 时间戳+随机数。
> 建议：收敛为两级。

### 2.2 实测

行号 ✓、`randomUUID`/`getRandomValues`/时间戳三路径存在。**表述准确**。

### 2.3 切中要点吗

**切中**。但"两级"建议本身有歧义——究竟用 `randomUUID` ‖ 计数器还是 `getRandomValues` ‖
计数器？两个第二选哪个？第十一版没说明。

### 2.4 精确表述建议

```diff
- 建议：收敛为两级（`randomUUID` 或一个 `getRandomValues` 调用；都没有时用
- 模块级递增计数器），删除第三级的 Date.now 分支。
+ 建议：主用 `crypto.randomUUID()`（安全上下文已覆盖现代浏览器、SSR 不调用此路径）；
+ 缺失时降级为计数器 `${Date.now().toString(36)}-${counter++}`，**直接砍掉**
+ `getRandomValues` 拼 16 字节 hex 这一中间级——它与 `randomUUID` 是「同一安全上下文
+ 内」的两种尝试，第三级才是跨上下文兜底，不应把第二级做得这么重。
+ 影响：−6 行、少一条不可测路径、唯一性目标（区分同一 Pinia 多 Manager）不变。
```

---

## 3. P3 `store.ts` Pinia 私有分区 —— **切中要点、过度归因**

### 3.1 第十一版原描述

> 1. **没有消费者依赖它的响应性。** ……全库 `grep` 找不到任何 `watch`/`computed`/
>    组件模板消费这个 store 的地方。
> 2. **为无人消费的响应性付费。** ……而这个响应式更新不驱动任何东西。**纯开销。**
> 3. 它换来的只有两点：Vue devtools 里可见（调试便利）、以及 U21「私有 Pinia 分区」
>    这一**需求条款**。
> 判定：**实现忠实于需求，但需求本身把「一个内存注册表」写成了「一个 Pinia store」**。

### 3.2 事实纠偏

**(1) 检索口径遗漏了真实消费者**。第十一版 grep 的 `watch|computed` 在 `src/` 下只命中
`vue.ts:103` 的 watch，但**这个 watch 监听的是 `options`（输入项），不是 store**。
真正的「会消费 store 响应性」的代码是**应用侧**：任何 `createRefreshManager` 之后
在 Vue 模板/computed 中调用 `manager.readSnapshot(source, args)` 的代码，都会
通过 `this.store.entries` getter（在 `manager.ts:101/194/360` 三处访问）触发
`shallowRef` 的依赖追踪。这是**设计意图**，不是漏洞——U21「私有 Pinia 分区」
明文把它作为可见性/调试性承诺。

因此把 store 判定为「无响应性消费者」是**框架侧视角的局部真相**——
框架代码不消费响应性，但 store 的存在就是为了让应用/调试工具能消费。
「**纯开销**」的结论与需求条款冲突。

**(2) 把 `ResultStore` 端口与 Pinia 适配器混为一谈**。

```text
// model.ts 定义端口
get entries(): Partial<Record<string, StoreEntry>>
put(key, value): void
remove(key): void
dispose(): void

// store.ts 实现端口（用 Pinia）
const useResultStore = defineStore(id, () => {
  const entries = shallowRef<Partial<Record<string, StoreEntry>>>({})
  // ...
})

// manager.ts 只依赖端口
private readonly store: ResultStore
```

**端口本身（4 项窄方法 + 1 个 getter）是合理抽象**——它独立于 Pinia，
便于测试用 Map 替身。**过度的是 Pinia 实现的选择**，不是端口。第十一版建议
"删 `store.ts`、`ResultStore` 端口保留给测试替身"，这是对的；但附录中
"R-A Pinia 分区 → 内存注册表（§4.2，收益/代价比最高）" 把这条列为**最高优先**
理由是"−42 行、少一个 peer dep"，却**遗漏了应用侧响应性消费这一设计意图的损失**。

### 3.3 切中要点吗

**切中**：Pinia 实现选择本身可疑（响应性消费者极其罕见、整表 O(R) 浅复制无可观回报）。
**未切中**：把这归因为"框架侧无消费者 + 纯开销"是误因，掩盖了真正的代价
（应用侧依赖追踪 + devtools 可见性会被一并移除），也低估了 R-A 的真实代价。

### 3.4 精确表述建议

```diff
- ### 4.2 最可疑的一层：`store.ts`（Pinia 私有分区）
- **这是本轮架构评审发现的最大单项过度设计。** 证据链：
- 1. **没有消费者依赖它的响应性。** ……全库 `grep` 找不到任何 `watch`/`computed`/
-    组件模板消费这个 store 的地方。
- 2. **为无人消费的响应性付费。** `store.ts:18-26` 的 `put`/`remove` 每次浅复制整张
-    分区表（O(R)，DESIGN §4.4 自己声明了这个成本）来触发 `shallowRef`——
-    而这个响应式更新不驱动任何东西。纯开销。
- 3. **适配层本身带来三类尾部复杂度：** Pinia peer dependency、`dispose` 时删 state 键 +
-    `$dispose`（S07.03 的来源）、`createRefreshManager` 的 `pinia` 必填参数。
- 它换来的只有两点：Vue devtools 里可见（调试便利）、以及 U21「私有 Pinia 分区」这一**需求条款**。
- 判定：**实现忠实于需求，但需求本身把「一个内存注册表」写成了「一个 Pinia store」**——
- 属于需求级过精传导到架构。
+ ### 4.2 `store.ts`（Pinia 私有分区）：过度的是实现，不是端口
+ 该层由两部分组成：**`ResultStore` 端口**（model.ts 定义、manager.ts 持有，
+ 4 项窄方法 + 1 个 getter，独立于具体存储）和 **Pinia 适配器**（store.ts，42 行）。
+ 端口合理（便于测试用 Map 替身、独立可演进）；过度的是 Pinia 实现本身。
+ 证据：
+ 1. **响应性消费者的真实规模**。`src/` 内无 `computed`/组件模板消费 `store.entries`
+    （唯一命中 `vue.ts:103` 的 `watch` 监听的是 `options`，不是 store）；
+    但 `manager.readSnapshot` 通过 `get entries()` getter（manager.ts:101/194/360）
+    **暴露给应用侧**，任何在 Vue 渲染/computed 内调用 `readSnapshot` 的代码都会
+    触发 `shallowRef` 的依赖追踪。这是 U21 的**设计意图**——「私有 Pinia 分区」
+    让分区对应用调试可见，框架侧不消费是为了避免框架自身被卷入响应式调度。
+ 2. **为极稀消费者付费的代价**。`store.ts:18-26` 的 `put`/`remove` 每次浅复制
+    整张分区表（O(R)，DESIGN §4.4 实测 O(活跃 Resource 数)，常规场景 < 50，
+    **开销量级不是问题**——`shallowRef` 的 setter 本身是 O(1)，浅复制 R 个键是 O(R)，
+    这才是真成本）。问题在于 R 个键的浅复制驱动一次几乎不会被观察的更新。
+ 3. **Pinia 适配器带来的三类尾部复杂度**：`pinia` peer dep、`dispose` 时
+    `$dispose` + 删 state 键（store.ts:36-41）、`createRefreshManager` 的
+    `pinia` 必填参数（app.ts:46）。
+ 判定：端口应保留（测试与可演进性的真实收益）；Pinia 实现是候选——若需求方
+ 接受"应用侧不再能用 Vue 响应式读取快照"这一行为变化，可以整体替换为
+ `Map<string, StoreEntry>`，但需配套说明：① 删除 `createRefreshManager` 的
+ `pinia` 参数；② 删 pinia peer dep；③ U21/S07 条款更新；④ 应用侧的
+ `readSnapshot` 行为保持「按参数键查最新结果并复制」（这部分接口不变）。
+ **这是真正的代价清单，不是"−42 行 + 少一个 peer dep"一句话能概括的**。
```

---

## 4. P4 `ConfigurationHost` 半套端口 —— **切中、表述准确、可删除性已确认**

### 4.1 第十一版原描述

> `createConfigurationBinding` 的 watch 闭包只经 `ConfigurationHost`（3 项）访问……
> 这是 ISP 的**半套应用**：为一个已经合法向下的依赖方向（vue→manager）再隔一层端口。

### 4.2 实测

`grep -rn ConfigurationHost src/ tests/`：

```text
src/vue.ts:15    import type { ConfigurationHost, ... }
src/vue.ts:94    manager: ConfigurationHost,        // 唯一调用点
src/model.ts:197 export interface ConfigurationHost { ... 3 项 ... }
```

`tests/` 下无引用。**确认可纯删除**（model.ts:197-204 共 8 行，vue.ts:94 改 1 行）。
行号 ✓，描述 ✓。

### 4.3 切中要点吗

**切中**。但有一个第十一版未提的次级好处：删 ConfigurationHost 后，`model.ts` 还剩
`QueryHost`（查询执行路径依赖）、`ScheduleHost`（调度宿主）、`Clock`/`ResultStore`
（端口）。这是把"窄端口"从 3 个减到 2 个 + 2 个真端口——结构更整齐。

### 4.4 精确表述建议

```diff
- **(b) `ConfigurationHost`——过度，建议删除。**
- `model.ts:197-204` 定义它时声称「组件适配只用这三件事」，但同一个 `vue.ts` 里
- `useRefresh` 已经在使用完整 Manager——端口只覆盖了 watch 闭包这一个函数的参数类型。
- 这是 ISP 的**半套应用**：为一个已经合法向下的依赖方向（vue→manager）再隔一层端口，
- 收益为零（Manager 本来就是它的合法依赖），成本是一份接口定义 + 一处理解负担
- （读者要确认「ConfigurationHost 的三个方法 Manager 都有」）。
- 删法：`createConfigurationBinding(manager: Manager, …)`，`model.ts` 删 8 行。
- 影响：可维护性 +（少一个概念）、可测试性不变（测试本来就是真实 Manager）、扩展性不变。
+ **(b) `ConfigurationHost`——过度，建议删除（纯减法）。**
+ 唯一调用点 `createConfigurationBinding(manager: ConfigurationHost, ...)` 在
+ `vue.ts:94`；同文件 `useRefresh` 直接持有 `Manager`（调 `submit`/`query`/`addHandle`
+ 等 7 个方法），已是合法向下的依赖方向。再隔一层"半套端口"在语义上是零收益。
+ 实证：`grep -rn ConfigurationHost tests/` 无任何引用，测试用真实 Manager 替身。
+ 删法：`vue.ts:94` 类型改为 `Manager`，`model.ts:197-204` 共 8 行整段删除。
+ 副作用：`model.ts` 的内部端口从 3 个减到 2 个（QueryHost + ScheduleHost），
+ 与两个真端口（Clock + ResultStore）形成"2 内部 + 2 外部"的整齐结构。
+ 影响：可维护性 +、可测试性无变化、扩展性无变化。
```

---

## 5. P5 Manager/Scheduler 双向知识 —— **切中、建议可行但量化与"少一跳"陈述需精确**

### 5.1 第十一版原描述

> Scheduler 持有 `buckets`（Manager 的资源注册表）来遍历活跃 Resource、计算
> `dueAt`（`lastSettledAt + 最短 every`）——但「谁到期」是**业务事实**；
> Scheduler 又把「谁该被登记」「怎么执行」回调给 Manager。
> 结果是 flush 这一个内聚算法被劈成两半，双向各持对方的知识
> 建议（结构级）：`dueAt`/`enqueueDue`/`activeResources` 移回 Manager……
> `ScheduleHost` 从 5 项缩到 3 项（`isDisposed`/`isCurrentTask`/`startTask`），
> `buckets` 注入删除。
> 影响：Manager +约 25 行、Scheduler −约 45 行、调用链少一跳。

### 5.2 实测

```text
// scheduler.ts:104-106  activeResources 来自 buckets.values()
private *activeResources(): Generator<Resource> {
  for (const bucket of this.buckets.values()) yield* bucket.values()
}
// scheduler.ts:115-119  dueAt 读 Resource 内部字段
private dueAt(resource: Resource, now: number): number {
  let every = Infinity
  for (const subscription of resource.subscribers) every = Math.min(every, subscription.every)
  return resource.lastSettledAt === null ? now : resource.lastSettledAt + every
}
// scheduler.ts:128-137  enqueueDue 调用 host.enqueueTask
private enqueueDue(now: number): number {
  let next = Infinity
  for (const resource of this.activeResources()) {
    if (resource.task) continue
    const due = this.dueAt(resource, now)
    if (due <= now) this.host.enqueueTask(resource)
    else next = Math.min(next, due)
  }
  return next
}
```

事实确认：`activeResources` + `dueAt` + `enqueueDue` 共 24 行（scheduler.ts:103-137），
均在 Scheduler 内但用 Manager 的 `buckets` + `Resource` 字段。

### 5.3 切中要点吗

**切中**。"谁到期"是 Manager 的事实（订阅间隔、上次结算时间都是 Handle/Resource 状态），
Scheduler 不应持这部分知识。

但「少一跳」表述**不准确**：当前链路 flush → reconcileHandles(回调) → enqueueDue(内部) →
host.enqueueTask(回调) → scheduler.add。提议重构后 flush → manager.flush(内联) →
scheduler.addDue(tasks)(新方法) — **总跳数未必减少**，只是把回调往返改成方法调用。
「少一跳」的精确说法是「去掉一个回调往返」。**这是真实收益**（消除 manager→scheduler
和 scheduler→manager 的协议性耦合），**但不是「链长减少」**——总调用步数基本持平。

### 5.4 量化修订

第十一版估 manager +25、scheduler −45。重新逐行：

| 移出项 | 行数 | 移入项 | 行数 |
|---|---|---|---|
| `activeResources`(3) + `dueAt`(5) + `enqueueDue`(10) | −18 | `dueAt`(5) + `activeResources`(3) + `enqueueDue`(10) 在 Manager | +18 |
| `reconcileHandles` 在 host 对象中由 Manager 提供 | 不变 | 新增 manager.flush() 内部调用以上三步 | +3 |
| `enqueueTask` host 回调 | −2 | 改为 Manager 直接调 `scheduler.addDue(tasks)` | +1 |
| `scheduler.addDue(tasks)` 新方法 | — | 新增 +4 | +4 |
| **净变化** | **−20** | **+26** | **+6 净增** |

实际净变化是 **+6 行**（不是 −20）。第十一版"manager +25、scheduler −45" 误估了
**`addDue` 新方法的成本**与**Manager 内联调用本身的成本**，把架构清洁化的净收益
描述成了减法。**重构后代码总量略增，结构变干净**。

### 5.5 精确表述建议

```diff
- 建议（结构级，见 §6.2）：`dueAt`/`enqueueDue`/`activeResources` 移回 Manager
- （`reconcileHandles` 时一并收集「到期任务列表 + 下次唤醒时间」交给 Scheduler），
- Scheduler 只保留 `queue`/`running`/唯一 Timer/唤醒计算/`maxConcurrent`。
- `ScheduleHost` 从 5 项缩到 3 项（`isDisposed`/`isCurrentTask`/`startTask`），
- `buckets` 注入删除，`Scheduler` 对 `model.ts` 的 Resource 依赖只剩 `Task.resource`。
- 影响：Manager +约 25 行、Scheduler −约 45 行、调用链少一跳（flush 不再回调
- `enqueueTask`）；`M04`/`F11`/`T10–T12` 的断言面（`inspect()` 的 queue/running）不变；
- F12 的「load 同步前缀改频率」行为不变（下次 flush 重新收集）。可测试性：Scheduler
- 退化为可独立单测的纯执行器，Manager 的到期计算可用虚拟时钟直测——**两边都比现在好测**。
+ 建议（结构级）：把 `dueAt`（scheduler.ts:115-119，5 行）/`activeResources`
+ （scheduler.ts:104-106，3 行）/`enqueueDue`（scheduler.ts:128-137，10 行）
+ 共 18 行移回 Manager；Scheduler 改为接收"待执行任务列表 + 下次唤醒时间"：
+ 新增 `Scheduler.addDue(tasks: Task[], nextDue: number)`（约 8 行，含校验），
+ 旧 `host.enqueueTask` 回调删除；`ScheduleHost` 从 5 项缩到 3 项
+ （`isDisposed`/`isCurrentTask`/`startTask`），`buckets` 注入删除。
+ 量化：Manager +约 26 行、Scheduler +约 4 行（含 `addDue`）、`ScheduleHost` 缩 2 行
+ → **代码总量净增约 +6 行**（不是 −20 行）。收益在结构而非体量：
+ ① 消除 Scheduler 对 Resource 内部字段（`subscribers`/`lastSettledAt`/`task`）的
+ 依赖，Scheduler 只依赖 `Task` 一个类型；② Manager 的 `inspect()` 直接看到
+ "下次唤醒时间 + 待执行列表"，调度态与资源态在同一观测面；④ 调度可独立
+ 用 `Task[]` 列表直测，不需要构造 `buckets`/`Resource`/`Subscription` 完整图。
+ 风险：`M04`/`F11`/`T10–T12` 的断言面（`inspect()` 的 queue/running）不变；
+ F12 的「load 同步前缀改频率」行为不变（下次 flush 重新收集）。
```

---

## 6. P6 三态配置 + 安装矩阵 —— **打包错误，必须拆分**

### 6.1 第十一版原描述

把**两件互不相关的事**打包为一条 P6：

- (a) 三态配置（`null ≠ false`、`InvalidInput`、三谓词）—— 代码位置在 `vue.ts`/`manager.ts`/`model.ts`
- (b) A05 安装矩阵（10 变体、五态逻辑）—— 代码位置在 `app.ts:61-79`

### 6.2 拆分

**(a) 三态配置**——确实存在过度嫌疑，但**需要需求方裁决**（U25 明文承诺「读不到的
开关保留为 null，绝不推断为 false」）。第十一版附录描述准确，**保留为 P6a**。

**(b) A05 安装矩阵**——代码逻辑 19 行（app.ts:61-79）+ `bindings` WeakMap 全局状态。
确实复杂度高，但**它由 10 个验收变体驱动**（A05.01–A05.10），全部有测试覆盖。
第十一版"变体收敛到 4 个行为"的建议**未经变体逐条核对**，需要先列清：

| 当前变体 | 第十一版建议的去留 | 实际影响 |
|---|---|---|
| 同一 App 同 Manager 重复安装 → 幂等 | 保留 | A05.01–02 |
| 同一 App 新 Manager 装 → 拒绝 | 保留 | A05.03 |
| 不同 App 同 Manager 装 → 拒绝 | 保留 | A05.04 |
| Manager 已 dispose → 拒绝 | 保留 | A05.05 |
| 旧 Manager 销毁后同 App 新 Manager 装 → 原地接管 | **建议删除** | A05.06–10（HMR 重建路径） |
| 销毁中/销毁后立即装 → 拒绝 | 保留 | A05.07 |
| 同 App 多活跃 Manager → 拒绝 | 保留 | A05.08 |
| 跨 App 多个活跃 Manager → 各装各 | 保留 | A05.09 |
| App 卸载销毁 | 保留 | A05.10 |
| Manager dispose 后 App 仍可用 | 保留 | A05.05 |

「原地接管」单一删除 ≈ −5 行、−2 变体；总体仍然 ≈ 7 个变体、14 行——**净减不显著**，
且 HMR 重建路径一旦删除需要应用侧手动配合（重 create Manager + 重 install），**收益
远小于 R-A Pinia**。

### 6.3 精确表述建议

把 P6 拆为：

```diff
- **P6 三态配置（`null ≠ false`）与安装矩阵（A05 10 变体）为罕见/构造场景付出持续复杂度**
+ **P6 三态配置（`null ≠ false`）—— 需需求方裁决的过度嫌疑**
+ U25 明文承诺「读不到的开关保留为 null，绝不推断为 false」；`InvalidInput` 联合类型 +
+ 三谓词（present/refused/allowed）+ 边沿不覆盖机制合计约 50 行（model.ts InvalidInput +
+ vue.ts + manager.ts admitQuery 部分）。真实场景「getter 抛错但应用要继续跟踪」极少
+ （核心场景是 SSR 下 props 尚未提供），但产品已经为它独立测试覆盖（B08/F10/A07.09）。
+ 修法仅在产品接受「读不到即配置无效」时可行——属需求裁决（§6.1 R-B），代码侧
+ 无独立可动空间。

+ **P7 安装矩阵（A05 10 变体 → 7 变体）—— 微小、需应用侧配合**
+ `app.ts:61-79` 的 19 行五态逻辑由 A05 的 10 个验收变体驱动，全部测试覆盖。
+ 「原地接管」（A05.06/09）的删除可省 −5 行 + 2 变体，但要求应用侧在 HMR 时
+ 重 createManager + 重 install（不是 reflash）。修法属"可有可无"，优先级低于
+ P1/P2/P4/P5/R-A。
```

---

## 7. 其他不准确处（§1/§3/§4.1/§5.2(a)/§6.2/§8）

### 7.1 §1 "`disposed` 类守卫" 计数口径模糊

```text
| `disposed` 类守卫 | manager 18 处、scheduler 3、query 2、vue 2、model 2（grep 实测） |
```

`grep -c "this.disposed\|handle.disposed\|disposed"` 实际匹配**所有出现**，
包括：① `private disposed: boolean` 属性声明；② `private disposed = false` 初始化；
③ `this.disposed = true` 状态写入；④ 守卫读 `if (this.disposed)`；⑤ `handle.disposed` 字段访问。
**"18 处" 实际是混合计数**——第十一版用"守卫"一词会误导读者认为全是运行时检查。

精确表述：manager.ts 真正用于**守卫**的 `this.disposed` / `handle.disposed` 读访问约 10 处
（见 `submit:113`、`query:157`、`addHandle:235` 间接 `deactivate:243`、
`removeHandle:582`、`dispose:603`、各 `publishResult`/`publishError`/`startTask` 的
`currentTask` 链经 `isDisposed` 间接访问；`Scheduler` 的 `disposed` 字段约 2 处真守卫）。

### 7.2 §3.1 "11 跳" 严重少算

第十一版说：

```text
useRefresh.submit → manager.submit → beginOperation → prepareParameters
→ setSubmission → requestFlush → Scheduler.flush → host.reconcileHandles
→ synchronize + attach → resourceFor → enqueueTask → scheduler.add
→ startQueuedTasks → startTask → load → copyResult → publishResult
→ store.put + deliver → handle.publish → display.value
```

**只数到外部方法名**——同一方法内部的多次调用（如 `startTask` 内的 2 次 `currentTask`、
`publishResult` 内的 N 次 `deliver`、scheduler 内部的 `setWakeup`/`queueMicrotask`、
flush 内部的 5 步：clearTimer→reconcileHandles→enqueueDue→startQueuedTasks→setWakeup）
被合并为 1 跳。**真实步数（按同步调用计数）约 20–25 步**，不是 11。

**判定本身没错**（无可压缩步骤），**但数字必须修正**。修正表述：

> submit 到交付的同步步数约 20–25 步（含 `flush` 内部 5 步、`startTask` 内多次
> `currentTask` 复核、`publishResult` 内 per-subscriber `deliver`）。链路长主要
> 来自三个不可压缩的产品事实：(1) 批处理 (`requestFlush` + `flush` 5 步)、
> (2) 「先建新身份再 abort 旧身份」的顺序约束（DESIGN §7.1）、(3) 共享交付
> 的 N 路 `deliver`。**逐跳检查未发现纯转发零语义中间层**。

### 7.3 §4.1 "2 合理 1 可疑" 误归类

```text
| L3 执行机制 | query.ts / store.ts / scheduler.ts | **2 合理 1 可疑** |
```

第十一版说 query.ts 与 store.ts 都合理、scheduler.ts 可疑。**store.ts 也属可疑**
（见 §3）。应改为：

```text
| L3 执行机制 | query.ts（独立执行路径）/ store.ts（存储适配）/ scheduler.ts（后台调度） |
|              | query 合理（4 项窄端口替代完整 Manager 依赖，可端口替身）；
|              | store 可疑（Pinia 实现 vs 内存注册表，见 §3/§6.1 R-A）；
|              | scheduler 切分位置错误（见 §5.2c/§6.2 S-B）|
```

### 7.4 §5.2(a) "运行期循环边" 措辞不当

```text
(a) `QueryHost`——合理。query.ts 作为独立模块需要 Manager 的 4 项事实，
接口收窄防止它反向依赖编排细节；且这是避免 `manager ↔ query` 运行期循环边的手段。
保留。
```

"运行期循环边" 不准确：query.ts 的导入方向是 `manager.ts → query.ts → manager.ts`
仅类型层面（query.ts 的 `import type`），运行期 manager 调 query 的函数，query 不调
manager——不存在"运行期循环"。精确表述：

```text
(a) `QueryHost`——合理。query.ts 作为独立模块需要 Manager 的 4 项事实，
接口收窄防止它依赖完整 Manager 公共面；且是避免 import 方向反向（query → manager
运行期）的手段——query.ts 只 `import type { QueryHost, ... }`，运行期入口函数
由 manager.ts 调用，方向严格 manager → query。保留。
```

### 7.5 §6.2 S-B 行数估计偏差

P5 重构的净行数变化应为 **+6 行**（不是 −20）。原文：

```text
| S-B Scheduler 职责回收 | ... | manager +25 / scheduler −45 行 |
```

实测重算（§5.4）：manager +26、scheduler +4、host 缩 2 → 净 +6 行。**精确表述见 §5.5**。

### 7.6 §6.2 合计 "−100 行" 误估

S-A(−10) + S-B(+6) + S-C(−12) + S-D(−6) = **−22 行**（不是 −100）。
第十一版"−100 行"是把 S-B 当作 −20 算的累计错误。**真实数字：S-A/B/C/D 合计约 −22 行**。
加上 R-A(−42) + R-B(−50) + R-C(−5) ≈ −97 行。**第十一版"150–200"应改成"100 行量级"**。

### 7.7 §8 "全项目最大的一项「过度」" 与 §4.2 "最大单项过度设计" 自相矛盾

第十一版同时声称：
- §4.2：Pinia 是"**本轮架构评审发现的最大单项过度设计**"
- §8：流程规范负担是"**全项目最大的一项「过度」**"

两者并列时只有一个能是"最大"。**精确处理**：§4.2 改为"代码侧最大单项",
§8 改为"代码 + 流程合计最大"——前者侧重结构性、后者侧重体量。

### 7.8 §5.2(b) "测试与行为零变化" 需谨慎

S-A 删 ConfigurationHost 的"测试与行为零变化"是正确的（`grep` 确认无测试引用）。
但 §5.2(c) 的 S-B 描述"两侧可独立测试性均提升"——这个说法对**单向可测性**成立，
对**集成测试**不成立：F12 变体（"load 同步前缀改变频率"）的回归测试需要构造完整的
Manager + Scheduler + Clock 替身集合。**重构不会让集成测试变简单**——只能让
Scheduler 单元测试变简单。建议修改 S-B 影响描述：

```diff
- 影响：Manager +约 25 行、Scheduler −约 45 行、调用链少一跳；`M04`/`F11`/`T10–T12`
- 的断言面（`inspect()` 的 queue/running）不变；F12 的「load 同步前缀改频率」行为
- 不变（下次 flush 重新收集）。可测试性：Scheduler 退化为可独立单测的纯执行器，
- Manager 的到期计算可用虚拟时钟直测——**两边都比现在好测**。
+ 影响：净 +6 行（不是 −20），调度态与资源态合并到 Manager 单观测面。
+ 单元测试：Scheduler 可独立单测（输入 Task[]，断言 queue/running/wakeup），
+ Manager 的到期计算可虚拟时钟直测；**集成测试维持现状**（F12/T10–T12 仍需构造
+ 完整 Manager + Scheduler + Clock 替身集合）。`inspect()` 断言面不变。
```

---

## 8. 修订后的优先级（合并第十一版 §9 的所有修正）

**第一优先（纯减法，无行为变化）**
1. **S-A 删 `ConfigurationHost`** —— `grep` 确认测试无引用，纯减法，−10 行，零风险。
2. **S-D `createNamespace` 收敛** —— 主用 `randomUUID`，缺失时降级为计数器（不再做
   中间级的 `getRandomValues` 拼字节），−6 行，行为等价。

**第二优先（结构级，需回归；净增行、但结构收益明显）**
4. **S-B Scheduler 职责回收** —— 净 +6 行；消除 Scheduler 对 Resource 内部字段的
   依赖，单元测试两面化；集成测试现状维持。需 `T01–T13`/`M04`/`F11–F13` 全量回归。
5. **S-C `nextIdentity` 改 `throw`** —— −12 行；消除 `Resource | undefined` 与
   `enqueueTask` 静默 return 的类型污染。耗尽分支本身无验收测试（grep 验证），
   改 throw 不破坏任何现有断言；保留 4 处早退（语义清晰：null=已销毁守卫）或一并删除
   （依赖外层 `disposed`/`registered` 兜底）两条路二选一。

**第三优先（需求方裁决后才动；改契约）**
6. **R-A Pinia 分区 → 内存注册表** —— −42 行 + 删 pinia peer dep + 删
   `createRefreshManager` 的 `pinia` 参数；**真实代价**：应用侧失去响应式
   `readSnapshot` 能力、失去 devtools 私有分区可见性、U21/S07 条款变更。**这是
   简化机会，不是"纯减法"**。
7. **R-B 三态配置降级** —— −50 行；行为变化：「读不到即配置无效」替换「读不到保留 null」，
   B08/F10/A07.09 验收语义反转。需产品方确认。
8. **R-C 安装矩阵收敛** —— −5 行 + −2 变体；HMR 重建路径需应用侧手动配合。
   收益不显著，列为可选项。

**不做（结论稳定；包括 §6.3 全表 + §8 流程规范方法论）**

---

## 9. 修订后量化结论

| 项 | 第十一版估 | 实测/重算 |
|---|---|---|
| S-A/S-B/S-C/S-D 合计行数影响 | −100 行 | **−22 行**（S-B 净 +6） |
| R-A/R-B/R-C 合计行数影响 | 未单列 | −97 行（其中 R-B −50 风险最高） |
| 全部执行后预计行数变化 | −150–200 | **−119 行** |
| 调用链 "少一跳" 主张 | S-B "少一跳" | **S-B 仅去一个回调往返**，总步数基本持平 |
| "最大过度" 标注 | §4.2 Pinia + §8 流程 并列 "最大" | **§4.2 = 代码侧最大**；**§8 = 含流程合计最大** |
| "前两版评审判维持" | 第十一版 P1 引用 | **无证据，删除** |
| "ResultStore 端口保留给测试替身" | §6.1 R-A | **端口应保留**；Pinia 实现可移除（§3 修订） |
| P6 打包（§6.1 R-B + R-C） | 一条 P6 | **拆为 P6 + P7** |

---

## 10. 第十二版自我审视清单

| 待改进项 | 第十一版处理 | 本版处理 |
|---|---|---|
| 行号偏移（−1 偏差 ×4） | 未说明 | §1.2 列出实测行号 |
| 无依据的"前两版判维持" | 作为 P1 状态说明 | §1.2 标记为编造并删除 |
| `enqueueTask` "静默 return" 与 `resourceFor` undefined 不分类 | 合并表述为 "消化 null 分支" | §1.2 区分两种终止语义 |
| Pinia 误归因为"无响应性消费者" | 框架侧视角的局部真相 | §3.2 补出应用侧真实消费者与设计意图 |
| ConfigurationHost 缺测试侧验证 | 未做 | §4.2 `grep -rn ConfigurationHost tests/` 实证 |
| P5 "少一跳" 与"−20 行"估算偏差 | 未做行级重算 | §5.4 逐行重算为净 +6 行 |
| P6 打包两条独立问题 | 一条 P6 | §6 拆为 P6（裁决类）+ P7（微小） |
| §1 `disposed` 计数口径模糊 | "18 处" 未区分属性/守卫/写入 | §7.1 明确混合计数 |
| §3.1 "11 跳" 严重少算 | 把方法名当跳 | §7.2 修正为 20–25 步 |
| §4.1 "2 合理 1 可疑" | store.ts 未归可疑 | §7.3 改为 "1 合理 2 可疑" |
| §5.2(a) "运行期循环边" | 用词不当 | §7.4 改为 import 方向 |
| §6.2 "−100 行" | 累计 S-B 错估 | §7.6 重算为 −22 行 |
| §8 与 §4.2 两个 "最大" | 自相矛盾 | §7.7 区分 "代码侧" 与 "含流程合计" |
| §6.2 S-B 影响描述含混 | "两边都好测" 含糊 | §7.8 区分单元测试收益 vs 集成测试现状 |

**总结：** 第十一版方向正确（结构性的 6 处诊断：P1–P5 + P6a/P7），但量化估算与若干表述
过度自信。本版修复 8 处不准确（P1、P3、P5、P6、§1、§3.1、§4.1、§8），删 1 处无依据
引用（前两版维持），改 2 处措辞（§5.2(a)、§5.2(c)）。**真正的可执行项未变**——
P1/P2/P4 可纯减法，P5 是结构级（净增行），P6/P7/R-A/R-B 需裁决。
总优先级排序与第十一版一致，行数估算从 −100 修正为 −22（S 级）和 −119（全部）。