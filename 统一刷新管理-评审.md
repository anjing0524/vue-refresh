# 《统一刷新管理.md》评审报告（第十七版 · 对齐新方向：订阅 + 主动刷新的契约收敛）

> 视角：**对齐与收敛**。本版以 [`统一刷新-评审.md`](./统一刷新-评审.md)（基线提交
> `e61e7d8`，2026-09-16）确立、且用户已认可的新方向为主线，重写本评审文件的全部结论：
> 先简化需求语义，再简化实现。新方向的三项取舍是——取消首次／后续提交差异、
> 删除 query 独立执行与后台接管、改频率不再强制重取；主动与自动刷新共用同一条
> 获取与交付流程。
>
> 本版取代第十五版（过度设计专项）与第十六版（MVP/V1 产品简化审计）作为**当前有效
> 结论**；两版中与新方向冲突的建议按 §四的对账表处置（采纳／取代／后置／否决），
> 不静默丢弃。历史版本内容以 Git 历史为准，本文件不再叠放全文。
>
> **确认记录（2026-09-16，两轮分开记账）**：
>
> 1. **既往来**：新方向三项取舍（§2.2）已由 `统一刷新-评审.md` 文首决策记录认可，
>    §二据此标「用户已认可」。本轮无新增，不重复记账。
> 2. **本轮**：确认人对附录一陈述的需求逻辑与设计逻辑表示「**大部分都是我真实想要的**」。
>    按措辞这是**方向性认可，不是逐条确认**——确认人未点名哪些条目不在「大部分」之内。
>    因此附录一任何单条都**不声称被逐条认可**；存在超出方向认可的具体语义或实现
>    细节的条目，另行在「确认状态」列标注，最终以 §十一待裁决项
>    （接口命名、等待者载体、Q13 书面确认）落定。

## 一、版本沿革与本版效力

| 版本 | 视角 | 当前效力 |
|---|---|---|
| 第十四版 | 对齐第十三版裁决、撤回其错误 | 历史记录；其五条自我约束被本版 §十二继承 |
| 第十五版 | 过度设计专项（防御检查/调用链/分层/模块关系四维审计） | **分析层仍有效**：复杂度归因账（六需求合取）被本版 §三重算；其 B 档/C 档建议按 §四对账 |
| 第十六版 | 产品复杂度简化审计（MVP/V1、删 Pinia/配置三态/错误分类等） | **被本版整体取代**：其多数建议与「本轮不顺带变更」的新方向边界冲突，处置见 §四 |
| 第十七版（本版） | 对齐新方向：需求减法优先于代码整理 | 当前有效结论 |

**基线口径。** 本版引用两套数字并注明来源：第十五版逐文件实测（12 文件 / 1916 行 /
maxCCN=8 / 52 测试，确认人环境）；`统一刷新-评审.md` 的 `e61e7d8` 复测
（12 文件 / 1920 物理行 / 1100 代码行 / 133 分支 / maxCCN=8 / 51 测试通过、0 失败，
含一条「同身份替换请求在途重叠」的定向复现）。两套数字差异来自提交与统计口径，
不构成矛盾；本版的削减量估算以 `e61e7d8` 口径为基准。

**法律地位。** 旧 §5.1 的已关闭裁决（第 630/631/633/635 行）保留为历史依据，
但按 `统一刷新-评审.md` 文首的决策记录——「旧 §5.1 决策不能用它们阻止本次已认可的
需求重审；Pinia、安装接管等与本次减法无关的选择不顺带变更」——本轮重启范围严格
限于新方向三项取舍及其直接依赖，不夹带其他议题。

## 二、新方向的契约基线（用户已认可）

完整论证见 `统一刷新-评审.md` §0；本节只固化本版后续引用的四条产品规则与推荐默认值。

### 2.1 四条产品规则

1. **订阅**：声明数据身份与自动刷新间隔；同一身份共享请求和结果；相同参数再次声明
   幂等，不隐含强制刷新。
2. **刷新**：明确要求重新获取当前数据；主动刷新与自动刷新走同一条 `Source.load`、
   有效性检查与结果交付路径，不再「先 query 再 load」。
3. **暂停**：停止本页自动更新并保留画面；可见且存活的暂停页面仍能主动刷新；
   其他页面的共享订阅不因本页暂停被取消。
4. **正确性**：旧意图、失效订阅和已销毁实例不能发布；结果副本独立；
   取消不冒充真实执行结束。

### 2.2 三项取舍（消除的原因链）

| 旧规则 | 新规则 | 消除的概念链 |
|---|---|---|
| 首次 submit 普通加入，后续 submit 强制重取（U03） | 声明幂等；强制重取用显式刷新动作 | 不再按操作历史猜测意图 → 删 `firstOperation`、B01/M05.02 的特判 |
| query 独立执行、只写本页，完成后后台重新 load（U04–U06、附条 A、Q04） | 主动与自动刷新同一获取与共享交付流程 | 删 QueryRun、query.ts、QueryHost、commit、接管机制、「query 快照防回退」的 Delivery 三态 |
| 主动改 every 取消并立即重取（U11、T04） | 只重算下次到期，保留在途请求 | 不把调度参数变化解释成数据失效；按「上次结束＋新间隔」已到期则正常排队 |

### 2.3 推荐默认值（待写入正式需求，非已实现）

暂停刷新＝共享执行路径上的临时需求（动作结束仍保持暂停）；页面退出取消该页临时
等待与交付资格；**刷新与在途合并**（等待一个在刷新动作之后启动的请求，更早在途
不因点击反复 abort，结束后补一次；动作前的多个刷新合并为同一次后继请求）；**共享
限流**（主动刷新进同一队列与并发上限，不再承诺绕过后台满槽）；数据身份变化立即
成为当前意图；沿用「最后需求退出即清理」，不引入 TTL。

## 三、复杂度归因账的重算（承接第十五版）

第十五版结论：复杂度来自六个正交需求的合取。新方向直接砍掉其中两个维度、改造一个：

| # | 需求（第十五版编号） | 新方向下的处置 | 消除量级（估算，以 132 叶子为基） |
|---|---|---|---|
| 1 | 共享与去重（U02/U03） | 保留；U03 的首次差异删除 | 核心价值不变 |
| 2 | query 独立双通道（U04） | **删除**：并入单通道＋显式刷新 | Q 组 13 条中约 10 条语义改变或删除；D 组 4 条全删；A02/A04 改写；概念删 QueryRun/QueryRunner/commit/RefreshQueryContext ≈ −6 名词 |
| 3 | 失效重取与交付门槛（U05/U03） | **改造**：门槛从 Submission 三态转挂到「刷新等待者」，等一个动作后启动的代次 | F02/F03/F06/F15、B02/B14、M05/M08 等约 8 条改写或删除；DESIGN §3.6 十行更新表被「等待者＋代次」替代（新文档明确：仍需某种代次或门槛，不能承诺删掉全部版本状态） |
| 4 | 配置读取可失败且不猜值（U18） | **保留**（本轮不顺带变更） | 无变化；第十五版 C 档记账继续有效 |
| 5 | 通知自身失败可诊断（U19） | **保留**（本轮不顺带变更） | 无变化；B10 的 11 个变体存量不动 |
| 6 | 三重环境门（U25） | 保留；边沿组合减少（改频率边沿消失，T05 变体 03 删除） | 约 −2 变体 |

**重算结论**：新方向砍掉的是第十五版认定的「最贵单一正确性承诺」（需求 3 的门槛三态）
与整个双通道维度（需求 2）——正好是第十五版 C 档记账中重启收益最大的两项，但**以
需求重审而非接口修补的方式**实现（第十六版的「时间戳守卫」是接口层修补，见 §四）。
配置三态（第十五版认定的唯一「过度鲁棒」维度）本轮不动，记账继续挂账。

## 四、与第十五、十六版的对账

| 旧建议 | 出处 | 新方向下的处置 | 理由 |
|---|---|---|---|
| B1：`query` 的 runner 缺省 = `source.load` | 十五版 §7.2 | **失效（被取代）** | query 路径整体删除；有独立副作用的业务查询留在页面业务层（新方向 §0.2 末段） |
| 交付门槛 → 一行时间戳守卫 | 十六版 §3 | **被更优方案取代** | 新方向的「等待动作后启动的请求」代次语义更强：既防回退又定义合并规则；时间戳守卫在墙钟回拨时只能保守跳过，代次语义无此缺陷 |
| 删配置三态 → 降级停刷＋通知 | 十六版 §3 | **后置**（移入后续轮次） | 新方向明确「Pinia、三态配置、安装接管等与本次减法无关的选择不顺带变更」 |
| 删 Pinia 分区 → 内存 Map | 十六版 §3 | **后置** | 同上 |
| `submit` 判别联合 → boolean | 十六版 §3 | **部分吸收** | 新方向下 submit 是幂等声明，返回值可进一步简化；但接口命名与形状待 §十一问题 1 裁决后再定 |
| 错误四分类 → MVP 无分类 | 十六版 §3 | **部分吸收（自然推论）** | 单通道后 query/background 的 origin 区分失去载体，错误来源自然收敛为 validation/execution/configuration 三值；这是删双通道的免费收益，无需单独提案 |
| 模块 12→7、删三个窄端口 | 十六版 §3 | **降级为后置** | 新方向明确「按新的行为减少分支后再决定模块去留」；query.ts 将因路径删除自然消失（12→11），其余合并待主线完成后重估 |
| `maxConcurrent` 默认 4、onError 缺省 | 十六版 §3 | **保留为 V1 项** | 与新方向不冲突，但属接口形状问题，随 §十一问题 1 一并裁决 |
| 删 `nextIdentity` 耗尽分支 | 十六版 §3 | **撤回（被否决）** | `统一刷新-评审.md` §7.2 给出实证：专项测试覆盖三个计数器的 dispose 与取消路径，删除会改变失败与清理行为——第十六版此条依据不足，予以撤回 |
| B2：复杂度归因账入 §5.1 附录 | 十五版 §7.2 | **保留（已部分履行）** | 本版 §三即归因账的重算；正式并入 §5.1 仍待确认人批准（§十一问题 5） |
| A 档为空（无行为中性减法） | 十五版 §7.1 | **仍成立** | 行为中性的减法空间在旧契约下已耗尽；新方向的行为减法全部是契约变更 |

**对第十六版的总体评价**：其审计框架（复杂度红线、前后对比、验证计划）被本版继承；
其「先删接口形状（Pinia/配置/返回值）」的次序被否——新方向证明**先删行为语义
（双通道、门槛、强制重取）才是主刀**，接口形状的简化大部分会随之自然发生或失去必要性。

## 五、P1 正确性口径的处置（承接新方向 §2.1）

`统一刷新-评审.md` 的 P1 发现：契约写「同一身份至多一份在途后台请求」，实现只保证
「同一生存期至多一个有效 Task」；同参显式重提会让旧执行（已 abort、未结束）与新执行
短暂并行（定向复现：`calls=2, oldAborted=true, running=2, resources=1`）。处置：

1. **最常见的重叠场景被新方向直接消解**：同 key 刷新不再 abort 在途请求（在途合并
   规则），而是等待其结束后补一次——「显式重提导致的并行」这个复现序列在新契约下
   不再存在。
2. **剩余两个场景写入契约措辞**，按新方向 §2.1 的三句话口径：
   - 参数切换 A→B 是不同身份，跨身份并行本来就不是「一份在途」约束的对象；
   - 同 Source＋key 的删除重建（P07 场景）仍可能物理重叠，这是已知边界——若要严格
     禁止必须等旧执行结束（新增调度约束、复杂度上升），本轮不禁止，只在契约中把
     「一份在途」改写为「同一生存期至多一个有效任务；普通轮询不重叠；重建后的新
     生存期不等旧执行」。此措辞修订属契约变更，列入 §十落地顺序第 2 步。
3. **不采用**「为满足物理并发 ≤1 而在启动阶段等待同 key 旧执行」的方案（新方向已
   论证其代价：阻塞队首是否放行其他身份、旧请求永不结束时的进展边界都要新定义）。

## 六、简化策略（对齐新方向）

| 策略 | 具体动作 | 降低的复杂度 | 对核心价值影响 | 执行时机 |
|---|---|---|---|---|
| **删除** | 首次/后续提交差异（`firstOperation`、U03 特判） | −1 行为规则、−B01/M05.02 特判 | 无损：幂等声明更可预测 | 主线第 1 步（需求修订） |
| **删除** | QueryRun 独立执行路径、`query.ts`、QueryHost 端口、commit、`closeQuery` 关闭边沿 | −1 文件、−1 端口、−1 活动分支、−Q/D 组大部分验收、−A04/B16a | 高级能力外移业务层；「暂停仍可单查」由 refresh 承接 | 主线第 3 步 |
| **删除** | query 后台接管（附条 A）与「query 快照防回退」的 Submission.delivery 三态 | −DESIGN §3.6 十行更新表、−F/B 门槛场景约 8 条 | 门槛语义转挂刷新等待者，防回退不放松 | 主线第 3 步 |
| **删除** | 改频率强制重取（T04 的替换语义） | −1 触发链、−T05.03 变体 | 无损：到期重算已覆盖缩短后已到期的场景 | 主线第 1 步 |
| **合并** | 主动刷新进入共享队列与并发上限 | −「绕过满槽」的独立通道及其验收（Q13 反转） | 满槽时 refresh 需排队——语义变化，见 §九风险 | 主线第 1 步 |
| **默认** | 刷新与在途合并规则（动作前合并、动作后要求更晚一次、失败结算本次等待） | 替代原门槛三态的全部转换规则 | 明确可解释的请求时序 | 主线第 1 步（写入需求） |
| **后置** | Pinia 去留、配置三态、submit 返回形状、模块合并 | 本轮不动 | — | 后续轮次 |
| **标准化** | 契约措辞修正（§五第 2 条）与 `统一刷新-评审.md` §2.2 列出的五处说明纠错（flush 时钟、285 年数量级、字段名、Task/Clock 注释、分层描述） | 文档可信度 | 无行为变化 | 可先行（P2/P3） |

## 七、简化后方案

### 7.1 目标 API（候选形状，命名待 §十一问题 1 裁决）

```ts
const quote = defineRefresh<QuoteParams, Quote>({
  validate?: p => ...,                 // 可选，语义不变
  load: (p, { signal }) => quoteService.read(p, signal),
})
const refresh = createRefreshManager({ pinia, maxConcurrent })   // 本轮形状不变
app.use(refresh)

const task = useRefresh(quote, { enabled, every, onError? })
task.declare(args)        // 候选名 declare/submit：幂等声明订阅（同参重复声明无副作用）
await task.refresh(args?) // 显式刷新：进共享队列；等待「本动作之后启动」的请求；
                          // 不传 args 时复用当前声明参数
task.display              // 唯一交付面：args/data/origin/updatedAt（不变）
```

页面业务查询（独立副作用、只更新本页、必须绕过共享限流）留在页面业务层，
页面自行负责取消与过期副作用保护（`统一刷新-评审.md` §0.2 末段）。

### 7.2 用户主路径

`useRefresh → declare → 读 display`；唯一分支是 `refresh`，且它复用订阅的全部机制
（参数、身份、交付），不引入第二套执行语义。同步/微任务/异步完成三阶段结构不变
（新方向 §3.1 的度量方法），但「query 成功 → 交付本页 → 后台接管 → 再请求」的链路
消失——单次主动刷新从「两次请求」变为「一次请求」。

### 7.3 状态与规则（承接新方向 §6.1）

**可删除**：`firstOperation`；QueryRun 及其活动分支（activity 收敛为
`Subscription | null`）；query 接管专用分支；`closeQuery` 关闭边沿（查询路径消失后
失去存在理由）；仅为区分「暂停中 query」的边沿状态。
**可收敛**：Submission.delivery 三态 → 刷新等待者集合（多个等待者可等同一任务代次，
结束或失效后释放；用任务引用还是代次数字，须验证合并、替换与资源重建三场景——
§十一问题 3）。
**不能删**（新方向 §6.1 的七项独立性判断全部保留）：页面当前意图、订阅资格、
共享实例身份、当前有效任务与未结束执行的区别、lastSettledAt、updatedAt、页面独立快照；
operationId/resourceId/taskVersion 三个时间域不强行合并。

### 7.4 功能范围与配置

保留：共享去重、可见性/生命周期暂停、退出清理与 abort、不重叠不补跑、失败保留画面
续轮询、整体发布＋updatedAt、参数身份与副本独立。
删除：双通道、接管、首次差异、改频率重取、commit。
本轮不动：Pinia 分区、配置三态、readSnapshot、visible 选项、安装接管。
配置项数量不变（enabled/every/onError/maxConcurrent/pinia/load/validate/visible），
但**语义负担下降**：无 runner、无 commit、无 SubmitResult 三变体的心智模型。

## 八、前后对比

| 指标 | 当前（`e61e7d8`） | 新方向目标 | 备注 |
|---|---|---|---|
| 必学概念（名词） | 38 | **≈ 28**（删 QueryRun/QueryRunner/commit/QueryContext/失效重取/交付门槛/接管/首次加入等 ≈ 10 个） | 估算；Pinia/配置三态相关名词保留 |
| 运行时状态域 | 10 | **7**（删 QueryRun 分支、Delivery 三态、firstOperation；增等待者集合为替代） | 等待者归属待 §十一问题 3 |
| 验收叶子 | 132 | **≈ 85–90**（Q 组约 10 条、D 组 4 条、F/B 门槛约 8 条删除或改写） | 估算；配置/通知相关叶子不动 |
| 主动刷新请求次数（单次操作） | 2（query＋接管 load） | **1** | 新方向 §0.4 链路直接推论 |
| 公开入口/方法 | 9 | **8**（删 query；submit→declare 改名不增） | runner 从公开面消失 |
| 结果类型变体 | Submit 3 + Query 3×origin 4 + cancel 3 | Submit 简化 + Query 类删除；错误 origin 4→3（background 并入 execution，单通道的自然收敛） | 错误收敛采纳与否见 §十一问题 4 |
| 技术模块 | 12 文件 | **11**（query.ts 随路径删除） | 其余合并后置 |
| 概念上的调度往返 | Manager↔Scheduler 6+5 | 6+5（不变） | 新方向 §3.3 已裁决不再为「纯执行器」重构 |
| 工期 | 已实现 | **需求修订 3–5 天；实现与测试改造 2–3 周**（含三个示例页迁移与 Q13 语义反转的回归重写） | 估算 |

## 九、取舍与风险

| 损失/变化 | 严重度 | 缓解 / 回滚 |
|---|---|---|
| 满槽时 `refresh` 需排队（Q13「主动查询绕过后台满槽」反转） | 中（语义变化） | 新方向已把它列为有意修改；UX 上等待者拿到的是动作后启动的请求，语义可解释；若某页面确需绕过，属页面业务层自管请求 |
| B09 场景消失（runner 内开启意愿的时序依赖） | 低 | 该场景本身就是旧双通道的人造竞态；示例页核对后若无真实需求即删 |
| 三个代表页迁移成本：查询列表页「暂停仍可单查」改走共享路径、runner 副作用外移 | 中 | 主线第 1 步先核对 `examples/pages/` 调用点（新方向 §5 末段）；发现不兼容的真实需求时报告具体冲突，不静默强行迁移 |
| 等待者合并规则的边界（动作前/执行中/结束后三态、失败结算） | 中 | 主线第 1 步写入需求时逐条定义；§十场景清单全覆盖 |
| 同 key 删除重建的物理重叠继续存在 | 低（已知边界） | §五第 2 条契约措辞修正；严格禁止的代价已论证不划算 |
| 大量验收与测试重写（52 条中估计 15–20 条改写） | 成本，非风险 | 保留故障注入子集（身份复核/槽位释放/深复制）作为不退化底线；未改变的正确性测试全部保留 |
| 回滚路径 | — | 现实现已打包（tgz + SHA-256）；新契约以 major 版本并行发布，examples 双跑对照，每项取舍独立可逆 |

## 十、验证与落地顺序（承接新方向 §8）

| 顺序 | 事项 | 完成条件 |
|---|---|---|
| 0（可先行） | P2/P3 文字纠错：五处说明修正（§六标准化行）、契约「一份在途」措辞按 §五改写 | 门禁与文档一致；无行为变化 |
| 1 | 以 §二为主线修订正式需求，核对三个代表页调用点 | 订阅/刷新/暂停/退出四规则成文；废止 query 接管/首次差异/改频率重取；在途合并与限流语义落实；P1 口径写准 |
| 2 | 根据行为变化调整设计与验收 | 一条获取交付链路；可删状态与仍需事实列清；等待者语义替代 Delivery 三态 |
| 3 | 实现统一获取路径，移除旧查询专用机制 | 普通订阅与主动刷新共用 load 和交付；暂停不意外恢复轮询；业务副作用迁到调用方；query.ts/QueryHost/closeQuery 删除 |
| 4 | 回归未改变的正确性，验证新行为 | 下述场景全绿；请求次数、交付对象、取消、真实槽位、实际进展均有断言，不只看文件变少 |

**必须覆盖的场景**（承接新方向 §8 清单）：两页同参订阅只首查一次；重复声明不强制
重取；显式刷新不再触发 query→load 双请求；暂停后刷新本页更新但不恢复轮询、其他有效
订阅更新、其他暂停页不更新；改频率不 abort 当前请求、缩短后已到期能正常执行；
刷新发生在请求启动前/执行中/结束后三态的合并与结算；参数 A→B 后 A 晚到；最后需求
退出后同 key 重建；槽满、忽略 abort、等待者取消、页面退出与 dispose；物理槽只随真实
结束释放；Store/首个页面通知同步重入后各接收者复核有效性；各页面与共享结果无
可变别名。

## 十一、待确认问题与下一步

1. **接口命名**：`declare`/`submit` 保留哪个名字？`refresh(args?)` 的参数可省略
   形态是否采纳？（决定 §7.1 候选形状定稿）
2. **Q13 反转的最终确认**：满槽时 refresh 排队、不再绕过——业务方是否接受？
   （新方向已把它列为推荐默认值，此处只差书面确认；即附录二 RQ6）
3. **等待者的实现载体**：任务引用还是代次数字？（须同时验证合并、替换与资源重建
   三场景；新方向 §6.1 留白；即附录二 RQ 联动项，见 B.4 代价说明）
4. **错误 origin 的自然收敛**（background 并入 execution）是否随主线一并落地，
   还是留到 V1？（本版建议随主线：它是删双通道的免费收益，单独保留 background
   反而要人为区分来源）
5. **归因账并入 §5.1 附录**（十五版 B2 遗留）是否批准？
6. **附录二 RQ1–RQ5 的裁决**（校验失败 no-op、refresh 带参切换声明、enabled=false
   不取消等待者、合格结果判定、refresh 交付范围）——附录二 B.2 已给建议值，
   需求修订（§十第 1 步）前必须逐条落定。

**下一步**：问题 1–3 与 6（RQ1–RQ5）确认后进入 §十第 1 步（需求修订）；第 0 步
（文字纠错）无需等待，可立即执行。

## 十二、本版自我约束（延续第十四版）

1. 本版重启范围严格限于 §二三项取舍及其直接依赖；Pinia、配置三态、安装接管、
   readSnapshot、visible、模块合并一律不顺带变更，相关建议只记账（§四「后置」）。
2. 数字来源：`e61e7d8` 复测引用 `统一刷新-评审.md` §1；第十五版口径注明出处；
   削减量为估算并逐处标注「估算」，不冒充实测。
3. 本环境未运行测试与类型检查；测试状态引用 `统一刷新-评审.md` 的复测记录
   （51 通过、0 失败）与确认人环境记录，不外推。
4. 行为语义变更（Q13 反转、D 组删除、T04 改写）均显式标注「语义变化」，
   不伪装为等价重构；历史测试不默认全部保留，按 §十第 4 步区分处理。
5. 第十六版中被实证否决的建议（`nextIdentity` 耗尽分支删除）公开撤回，
   不以改名方式重提。

---

## 附录一：需求逻辑与设计逻辑（第一性陈述；方向已获认可，非逐条确认）

> 本附录是本版的**论证基座**：§二的四条产品规则、§7.3 的状态收敛与 §十的落地顺序，
> 全部由这里的逻辑推导。确认人已于 2026-09-16 表示「大部分都是我真实想要的」——
> 据文首确认记录第 2 条，这是**方向性认可，不是逐条确认**：下表任何单条都不单独
> 声称被点名认可，「确认状态」列的默认含义是「含于方向认可」，**只有存在超出方向
> 的具体待决语义或实现细节时才另行标注**，与 §二正文互为表里。

### A.1 需求逻辑：需求只能来自用户意图，不能来自历史事实

框架的用户（业务前端开发者）从头到尾只有**三个意图**：

1. **我要这份数据，持续更新**（订阅：声明身份 + 间隔）
2. **现在再要一次**（刷新：一次显式取数）
3. **别再更新这页了**（暂停：停自动、留画面）

由此推出四条需求逻辑，违反任何一条就会产生假需求：

| # | 逻辑 | 内容 | 确认状态 |
|---|---|---|---|
| L1 | **一个意图一个动作，动作幂等** | 相同参数再次声明订阅 = 没发生任何事。旧契约「首次 submit 加入、后续 submit 强制重取」让同一动作的语义取决于调用历史——**历史不是意图**。要新数据就说「刷新」，一个独立的、显式的词 | 方向认可（对应 §2.2 取舍一） |
| L2 | **取数只有一条路径** | 「定时到点要一次」和「用户点了按钮要一次」本质相同。旧契约的 query 独立执行 + 后台接管是用两个执行域服务一个意图，QueryRun、交付门槛、版本域、接管机制全是缝合人为裂缝的补丁。**裂缝不该缝合，该消除**：刷新动作只是往共享执行路径里塞一个临时等待者 | 方向认可（对应 §2.2 取舍二、§7.3 等待者） |
| L3 | **配置变化是输入，不是事件** | 改 `every`、改 `visible` 只改变「下次什么时候要」的计算输入。旧契约「改频率 → 取消当前请求并重取」把调度参数变化解释成数据失效——**调度参数和数据新鲜度是两个无关的域**。改频率后只需重算到期时间；缩短到已到期就正常排队，不 abort 任何人 | 方向认可（对应 §2.2 取舍三） |
| L4 | **正确性是资格线，不是可选项** | 旧意图的结果不能覆盖新意图（晚到隔离）；共享结果与页面快照无可变别名（副本独立）；取消不等于执行结束（物理槽位只随真实结束释放）。这三条不是「需求」，是框架存在的资格——做不到它们，共享与调度全是错的 | 方向认可（对应 §2.1 规则 4，全部保留） |

**边界**：框架只做「取数与交付的共享、调度、取消」。业务副作用（写业务 Store、
保存后处理、绕过限流的特殊请求）永远留在页面。**框架不替业务做决定，只替业务做
重复劳动。**

### A.2 设计逻辑：从意图推导一切，每个状态必须回答一个用户问题

| # | 逻辑 | 内容 | 确认状态 |
|---|---|---|---|
| D1 | **状态只存两类事实，派生值现算** | 用户事实（参数快照、间隔、开关、可见性）与运行事实（订阅关系、在途任务、等待者、上次结束时间、结果产生时间）；资格、最短间隔、任务位置全部现算。**每保存一个状态都要能回答「哪个用户问题需要记住它」；答不上来的是缓存，缓存就会失同步** | 方向认可；与旧实现「派生值不保存」原则一致 |
| D2 | **单通道，Timer 与刷新按钮汇入同一条队列** | 订阅是唯一长期形态；刷新 = 该页面临时等待者（动作结束仍保持暂停）；到期 = Timer 产生的需求。同一 FIFO、同一并发上限、同一 load、同一套交付检查。**满槽时刷新排队不是缺陷，是共享语义的诚实表达** | 方向认可；但「满槽排队不是缺陷」一句为**本版立场**、不属已认可方向（共享限流在 `统一刷新-评审.md` §0.3 仅为推荐默认值），Q13 反转待 §十一问题 2 书面确认 |
| D3 | **身份三域分离，只做一件事** | 页面操作（意图代次）、资源生存期（共享范围）、任务代次（新鲜度）是三个时间尺度，不合并；唯一用途是回答「这个晚到的结果还能不能发布」。复核谓词收敛一处，每个外部效果之后重问一次——这是单线程同步重入下的正确性纪律，不是防御过度 | 方向认可 |
| D4 | **等待者集合替代门槛三态** | 「这次刷新要求动作之后启动的请求」表达为：等待者挂在任务代次上，多个等待者可共享一代次，任务被替换/资源重建/页面退出时等待者失效或顺延，结束时统一结算。**不需要「尚欠/已登记/无要求」三态——等待者的存在本身就是要求，不存在就是无要求。状态即事实，不做状态机** | 方向认可；实现载体（任务引用 vs 代次数字）待 §十一问题 3 |
| D5 | **失败不改已定结果** | 动作失败只结算 + 通知，是否继续由调用方 `enabled` 决定，框架绝不代写；条目暂不可得保留画面下周期继续；框架自身失败只进诊断。三类失败互不替代，**都不触碰已交付的画面** | 方向认可；与旧契约 §3 三分法一致，无需变更 |
| D6 | **调度与编排按「何时」与「谁」切分** | 调度器独占 Timer/队列/槽位，只答「何时、按什么顺序」；编排层独占关系与交付，只答「谁该被登记、结果给谁」。到期判断 = 上次正常结束 + 当前最短间隔，纯函数。满槽不自旋、取消不放槽、一轮 flush 读一次时钟——算法纪律，不是风格 | 方向认可；与旧实现 Scheduler 职责一致，本轮不动 |

### A.3 检验标准：用三个问题问任何一条需求逻辑

1. **删除测试**：删掉这条规则后，哪个真实用户场景会坏？答不出具体场景的规则是假需求
   （旧契约的「首次差异」「改频率重取」都答不出）。
2. **新人测试**：新人能否在不知道调用历史的情况下预测动作结果？
   （幂等声明 + 显式刷新 = 可预测；「第 N 次 submit」= 不可预测。）
3. **裂缝测试**：两个机制之间是否存在「缝合层」（接管、门槛转换表、状态迁移表）？
   **缝合层越厚，说明最初的概念切分越错。** 正确的切分让机制直接对接，没有转换层可写。

按这三个问题，新方向（幂等声明 + 显式刷新 + 单通道 + 等待者）是当前形态的最优解：
每条规则都能指出一个会坏的具体场景，每个动作都可预测，机制之间没有缝合层。
本轮不采纳的建议（Pinia 去留、配置三态、模块合并）同样用这三问检验后挂账，
待出现新证据再议。

---

## 附录二：需求规格与数据结构分析（应确认人「需求更清晰 + 分析数据结构问题」指令）

> 本附录是 §二（契约基线）与 §7.3（状态收敛）之间的缺环：**把方向落成可判定的动作
> 规格**（B.1–B.2），并对照新需求逐对象分析当前数据结构的结构性问题（B.3）与目标
> 形态（B.4）。确认状态：应确认人指令撰写（2026-09-16），**尚未逐条确认**——
> B.2 的 RQ1–RQ6 六个需求决定当前无答案，全部待裁决；B.4 的不变量是 D4 的具体化，
> 实现载体仍关联 §十一问题 3。

### B.1 术语先行（没有术语就没有清晰需求）

| 术语 | 定义 | 判定方式 |
|---|---|---|
| 数据身份 | Source 对象 + 完整参数值的稳定键 | 纯函数，无状态 |
| 有效订阅 | 存活 ∧ 已声明 ∧ 环境允许（激活∧可见）∧ 配置开启 | 派生，现算 |
| 合格结果 | StoreEntry 未过期：`lastSettledAt + 最短every > now` | 派生，现算 |
| 动作后请求 | 在本动作时间点之后**登记**（非启动）的任务 | 由任务代次判定，见 B.4 |

### B.2 三个动作的精确语义（可判定版）

**declare(args) —— 幂等声明**

| 输入情形 | 结果 | 状态变化 |
|---|---|---|
| 校验失败 | rejected | **无变化**（建议，见 RQ1） |
| 同身份重复声明 | accepted | **无任何变化**（不请求、不重置周期） |
| 新身份（已有合格结果） | accepted | 切换意图；交付现有合格结果，不强制新请求 |
| 新身份（无合格结果） | accepted | 切换意图；等待即将到来的首查 |

**refresh(args?) —— 显式刷新**

| 输入情形 | 结果 | 状态变化 |
|---|---|---|
| 无声明且未传 args | rejected | 无变化 |
| 传 args（建议） | 先按 declare 规则切换声明，再刷新 | 见 RQ2 |
| 当前无在途/排队任务 | 建立等待者；立即登记一个任务 | 本页获得临时交付资格 |
| 在途任务**登记于**本动作之前 | **不 abort**；等待者要求「下一个」任务；当前任务结束后自动补一次 | 合并的关键 |
| 排队任务尚未启动 | 多个等待者合并到同一个后继任务 | 免费的合并 |
| 本页暂停（enabled=false）但激活且可见 | 允许；本页获得临时交付资格，动作结束仍保持暂停 | 见 RQ3 |
| 本页隐藏/失活/卸载 | 等待者取消，不影响其他页面的共享任务 | 已明确 |

**refresh 的结算**：所等待的任务成功 → 等待者成功（本页已收到交付）；失败 → 等待者以
error 结算，画面不变；被取消（页面退出/销毁）→ cancelled。

**六个待裁决的需求决定（当前文档无答案）：**

| # | 问题 | 本版建议 | 理由 |
|---|---|---|---|
| RQ1 | 校验失败的 declare 改不改状态？ | **完全 no-op**（旧 P08 是退出旧订阅+清参数，与新方向「声明是事实陈述」矛盾） | 声明式语义下无效输入不该有副作用 |
| RQ2 | refresh(args) 带参时是否切换声明？ | 是，refresh = declare + 刷新，一个动作 | 避免「先 declare 再 refresh」两步缝隙里的竞态 |
| RQ3 | enabled=false 取消进行中的等待者吗？ | **不取消**（`统一刷新-评审.md` §0.3 只列了隐藏/失活/卸载取消） | 显式动作是用户意志；enabled 只管自动更新 |
| RQ4 | declare 加入时交付已有结果的「合格」标准？ | 未过期（按最短 every 判定），过期则等即将到来的请求 | 与 F01 一致 |
| RQ5 | refresh 的结果只给本页还是全部订阅者？ | 全部有效订阅者 + 本页（临时资格）——它是共享交付 | 单通道的直接推论，需书面确认 |
| RQ6 | 满槽时 refresh 排队（Q13 反转） | 确认（已挂 §十一问题 2） | — |

### B.3 当前数据结构的结构性问题（对照新需求逐对象分析）

| # | 对象/字段 | 问题 | 症状证据 | 新方向下的修正 |
|---|---|---|---|---|
| **S1** | `Submission.delivery` 三态 | **挂在错误的对象上**：刷新要求是「每次动作」的短期事实，却被存在「每次声明」的长期对象上 | DESIGN §3.6 十行更新表、F15 的「旧 id 门槛清理」、`setRequirement` 审计点、B14 门槛绑定生存期——全部是在给错位状态打补丁 | Submission 只剩 `{parameters}`；新鲜度要求下放到等待者 |
| **S2** | `Activity = QueryRun \| Subscription` 互斥并集 | **混合了两种生命周期**：Subscription 是关系（长）、QueryRun 是执行（短）。互斥迫使系统跟踪边沿 | `closeQuery` 关闭边沿、`lastEnabled` 边沿历史（true→未知→false，F10）、M02 不变量、「暂停中 query vs 关闭时 query」的区分 | 订阅（关系）与等待者（等待）**正交共存**：activity 收敛为 `Subscription \| null`，waiters 独立集合 |
| **S3** | `DeliveryBarrier.resourceId` 字符串 | 用序列字符串做生存期绑定，需要专门规则判「门槛属于其他 id」 | F15、`barrier.resourceId !== resource.id` 的清理分支 | 等待者直接持有 Resource **对象引用**，失效判定退化为一次身份比较 |
| **S4** | `Resource.nextVersion` | 一域两用：任务身份 + 新鲜度标记。功能上没错，但从未声明 | `Task.version` 与 `barrier.minVersion` 同域比较的隐含前提 | **保留**，但明示为等待者机制的基座（见 B.4） |
| **S5** | `Subscription.every` 可变字段 | 可变性只为「改频率即替换任务」服务（`synchronize` 里 `captured.every = input.every` + `enqueueTask`） | T04 替换链 | 新方向改频率永不触碰任务；字段保留但变为纯数据（读现算亦可） |
| **S6** | `Handle` 的字段分组 | 同一对象混装三类事实：声明（submission）、关系（activity）、边沿（lifecycleActive + 适配层 lastEnabled/reported）；`operationId` 兼作「当前意图」与「query 有效性」两个判据 | `currentOperation` / `currentQuery` 两个谓词、`beginOperation` 的三重写 | 重组为三件事：`declaration` / `subscription` / `waiters`；operationId 只保留「意图代次」职责 |
| **S7** | 适配层 `lastEnabled` / `reported` | 存在理由大半是保护「暂停期间的单次查询」不被误杀——单通道后等待者有显式取消语义，这套边沿历史失去主要用途 | F10 两个变体、B08 | 本轮不动（配置三态冻结），**记账：等待者落地后此项可重估** |
| **S8** | `StoreEntry.version` | 唯一消费者是门槛判断；等待者机制下仍需要（判定交付是否满足等待者） | `deliverable` 的 `entry.version < minVersion` | 保留 |

**核心结论**：当前数据结构的全部结构性问题同源——**S1/S2/S3 是同一个错误的三种表现**
（把「动作级」状态挂到「声明级」对象上、把「等待」建模成互斥活动、用值域身份代替
对象身份）。修正它们不需要新机制，只需要把状态放到正确的宿主上。

### B.4 目标数据结构：一个不变量替代整套门槛机制

```ts
Handle {
  source, readInput, publish, onError, cleanup, disposed, lifecycleActive,
  operationId,                        // 意图代次（只判「晚到结果能否发布」）
  declaration: { parameters } | null, // 声明：只有参数，无任何交付状态
  subscription: Subscription | null,  // 关系：与 waiters 正交共存
  waiters: Set<Waiter>,               // 等待：每次 refresh 一个
}
Waiter { minVersion: number, settle(result) }   // settle 由 refresh 的 Promise 提供
Resource / Task / StoreEntry / Scheduler        // 不变
```

**唯一的新不变量**（替代 DESIGN §3.6 十行更新表）：

> 等待者在动作时刻记录 `minVersion = resource.nextVersion + 1`；
> **存在未满足等待者的 Resource 必有 `version ≥ max(waiters.minVersion)` 的任务**
> （没有就登记一个）；任务结束时，所有 `minVersion ≤ task.version` 的等待者按该任务
> 结局结算。

它如何自动覆盖旧机制逐条维护的场景：

| 旧场景（原验收） | 新不变量的自然结果 |
|---|---|
| Q06 相同参数再次查询不复用在途 | 旧任务 `version < minVersion` → 等待者要求下一个 |
| D01 失活恢复强制一次 | 恢复时资源若无满足等待者的任务 → 不变量强制登记 |
| F02/B02 query 快照不被旧分区回退 | 等待者未结算前，旧 entry 的 `version < minVersion` 不交付本页 |
| F15 同 key 重建的旧门槛清理 | 等待者持对象引用，旧 Resource 失效即等待者失效，无需 id 比对 |
| 多次刷新合并 | 排队中的任务 `version ≥` 所有等待者的 `minVersion` → 一个后继任务服务全部 |
| 「已开始后再刷新才要求更晚一次」 | 已启动任务不满足新等待者的 `minVersion` → 自动要求 follow-up |

**代价说明**：`minVersion = nextVersion + 1` 依赖「版本在登记时分配」这一现行事实
（`enqueueTask` 里 `nextIdentity(resource.nextVersion)`），属 S4 声明的隐含前提的
显式化；资源删除重建时等待者随对象引用一起失效，重建后的新资源不受旧等待者约束
（与 F15 语义一致）。该不变量是 D4（等待者集合替代门槛三态）的具体化，
与 §十一问题 3（任务引用 vs 代次数字）的裁决联动：若选任务引用，`minVersion`
被替换为「等待者记录动作时刻的当前任务引用，其登记后继才满足」，不变量其余部分不变。

---

# 第十八版 · 重构方案（过度设计专项 · 代码级减法）

> **视角**：代码级减法。本版在前三版（第十五版复杂度归因账、第十六版接口形状减法、第十七版行为减法）已落地的基础上，把"少做一件事"推进到最后一层——**代码本身的形态**。
>
> **范围**：仅限 `src/` 下的实现；公共契约、行为规则、验收目录、ADR 决策记录均**不动**（已闭合的议题不再重开）；保留所有验收叶子覆盖（删除/合并行为条目须有派生证明，见统一文档 §6.2）。
>
> **基线**：本仓库当前 `src/*.ts` 共 **1874 行**（`wc -l src/*.ts` 实测，2026-09-16）。
> `manager.ts` 单独 675 行（占 36%）；`model.ts` 单独定义 17 个接口；`public-types.ts` 12 个对外类型；
> 内部窄端口 3 个（`ConfigurationHost` / `ScheduleHost` / `ResultStore`），仅 13 项事实但要求 3 个接口和 3 套对象字面量满足；私有方法 + 公共方法合计 33 个，集中于 `manager.ts`。
>
> **目标**：把 `src/` 收敛到 **≤ 500 行有效逻辑**（不含注释与空行），主流程一眼可读；调用链路收敛到 `submit → declare` 与 `refresh → enqueue` 两条主干。

## 一、过度设计的具体表现（按代码事实盘点）

下列事实全部来自当前 `src/*.ts`，不引入外部判断：

### 1.1 文件拆分过度（11 文件、1874 行）

| 文件 | 行 | 实际职责 |
|---|---:|---|
| `manager.ts` | 675 | 编排层（声明/刷新/订阅/任务/调度入口/释放/资格/复核） |
| `model.ts` | 222 | 17 个接口类型集中地（Display/StoreEntry/ResultStore/EnabledInput/ValidInput/InvalidInput/Input/Subscription/RefreshWaiter/Submission/Handle/Resource/Task/Clock/ConfigurationHost/ScheduleInspection/ScheduleHost/ManagerInspection） |
| `public-types.ts` | 197 | 公共 API 类型 |
| `vue.ts` | 184 | Vue 适配层 |
| `scheduler.ts` | 173 | 后台调度（Timer/队列/槽位）—— 但回调用 5 项 `ScheduleHost` 端口回到 manager |
| `source.ts` | 119 | Source 定义、参数准备、键计算 |
| `app.ts` | 101 | 安装、可见性、只读快照、命名空间生成 |
| `delivery.ts` | 74 | 结果复制 + observer 包装 + `notify` + `EFFECT_FAILED` |
| `diagnostics.ts` | 73 | observer 出口（`reportObserverError` + `observeRejection`） |
| `store.ts` | 42 | Pinia 分区适配 |
| `index.ts` | 14 | 包入口 |

**问题**：`manager.ts` 与 `model.ts` 的边界是"我把字段定义抽到 `model.ts`、把方法留在 `manager.ts`"，但所有方法都围绕这些字段，且这些字段几乎不被其他文件直接构造（除了 `Scheduler` 与 `store.ts` 各用 1 项）。`diagnostics.ts` 与 `delivery.ts` 的分层是"零依赖叶子 + 结果边界"，但 `diagnostics` 只被 `delivery` 和 `source.ts` 各调用 1 次，合并后文件短一半。

### 1.2 窄端口带来的额外代码（ADR-11 已关闭、未实施）

- `ConfigurationHost`：2 项事实（`reconcile` / `requestFlush`）→ 在 `vue.ts` 里被 `{ manager }` 满足一次，要求 `manager` 暴露两个公共方法；
- `ScheduleHost`：5 项事实（`isDisposed` / `reconcileHandles` / `enqueueTask` / `isCurrentTask` / `startTask`）→ 在 `Scheduler` 构造时由 `{ manager }` 满足一次，要求 4 个私有方法被改成公共回调名；
- `ResultStore`：4 项事实（`entries` / `put` / `remove` / `dispose`）→ 在 `store.ts` 满足一次。

三套接口+对象字面量共 **44 行**（13 项事实 × ~3.4 行/项），换来的是"运行期边严格向下"——但**调度器本来就需要调用 manager 的事实**，把它包成端口只是把"直接调用"换成"通过接口调用"，对可读性是负贡献。

### 1.3 私有方法过多且散布（manager.ts 33 个方法）

```
公共：addHandle / removeHandle / activate / deactivate / setBrowserVisible / setCleanup
      setHandleCleanup / cancelRefresh / reconcile / requestFlush / submit / refresh
      readSnapshot / dispose / inspect / isDisposed             （15 个）
私有：commitDeclaration / supersededResult / synchronize / attach
      releaseSubscription / releaseResourceIfUnused / resourceFor
      enqueueTask / startTask / expireTask / publishResult / collectReceivers
      publishTo / publishError / deliver / refreshFloor / refillWaiters
      settleWaiter / currentOperation / eligible / present / allowed
      registered / currentTask / currentSubscription / currentWaiter
      nextIdentity                                                  （27 个）
合计：42 个方法（实际计数，含构造器）
```

其中"判断这个东西还是不是它以为的那个东西"的 8 个函数：
`currentOperation` / `eligible` / `present` / `allowed` / `registered` / `currentTask` / `currentSubscription` / `currentWaiter`——其中前 4 个其实都是"资格的不同切面"，可以收敛为 2 个；后 3 个都是"它是否还在自己的归属集合里"，可以收敛为 1 个。

### 1.4 Publisher 内部接口只在两个方法之间传递

```ts
interface Publisher {
  readonly handle: Handle
  readonly subscription: Subscription | null
  readonly waiter: RefreshWaiter | null
  readonly data: unknown
}
```
仅在 `collectReceivers`（构造）→ `publishResult`（遍历）→ `publishTo`（消费）之间使用。可以直接传 4 个参数，或在 `publishTo` 内直接解构。

### 1.5 Clock 三方法只用了 2 个半

```ts
interface Clock {
  now(): number                              // 调度到期计算
  timestamp(): number                        // 只 publishResult 用一次（→ Date.now()）
  setTimer(callback, ms): () => void         // 框架上限 + 唤醒 Timer
}
```
`timestamp` 只有一处调用，删 Clock 接口、把这一行换成 `Date.now()`。

### 1.6 namespace + bindings + createNamespace + installedApp 四套仅用于安装边界

- `app.ts` 的 `createNamespace()`（含 randomUUID/getRandomValues/Date.now 三层 fallback，**8 行**）：只为给 `Resource.id` 加前缀；
- `Resource.id = "${namespace}:${id}"` 拼接（**1 行**）：用于隔离多 Manager 实例；
- `bindings: WeakMap<App, RefreshBinding>` + `installedApp` 字段 + 接管判断（**15 行**）：用于 HMR / 会话切换场景；
- `manager.setCleanup` 接受回调（**3 行**）。

**事实**：删除 namespace 后，Resource 不需要 `id` 字段，分区可以用 `source + key` 二元组直接定位；bindings/installedApp 接管逻辑属于 HMR 边角，HMR 场景下 dispose 旧实例+新建即可，不需要原地接管。

### 1.7 EFFECT_FAILED 字典跨文件使用 4 次

```ts
export const EFFECT_FAILED = {
  publish: 'publish callback failed',
  onError: 'onError callback failed',
  cleanup: 'cleanup callback failed',
  store: 'store effect failed',
}
```
只在 `delivery.ts` 里定义、在 `manager.ts` 里读 4 次。可以作为常量直接挪到调用点旁边。

### 1.8 handle.cleanup + handle.setHandleCleanup 单独的注册路径

`setCleanup` 与 `setHandleCleanup` 是同一套机制的两个变体；合并为 `setCleanup(callback)` 一项。

---

## 二、目标代码结构（5 文件、≤500 行有效逻辑）

| 文件 | 行（含注释） | 有效逻辑 | 职责 |
|---|---:|---:|---|
| `src/index.ts` | 14 | 6 | 包入口（`defineRefresh` / `createRefreshManager` / `useRefresh`） |
| `src/types.ts` | 90 | 50 | 公共 API + 内部模型（合并 `public-types.ts` 与 `model.ts` 的运行时部分） |
| `src/source.ts` | 70 | 45 | Source 定义 + 参数准备 + 键计算 |
| `src/core.ts` | 360 | 250 | 编排 + 调度 + 存储（合并 `manager.ts` + `scheduler.ts` + `store.ts` + `app.ts` 的安装/可见性部分） |
| `src/vue.ts` | 110 | 70 | Vue 适配层（合并 `delivery.ts` + `diagnostics.ts` + `vue.ts`） |
| **合计** | **644** | **421** | 11 文件 → 5 文件；**有效逻辑 ≤ 500 行** |

> "有效逻辑"指剔除空行、单行注释、文件头注释块后的可执行行数。基线（1874 行）的有效逻辑约 1320 行（依 `complexity.mjs` 历史记录）。
>
> 保留 `source.ts` 是因为它是"参数边界"，与 Vue 适配解耦后可以独立测试；如进一步压到 4 文件，把 `source.ts` 合并到 `core.ts`，但会失去"参数边界是单一入口"的边界清晰度。

### 2.1 目标依赖方向（保持严格向下）

```
index → types
     → source
     → core
     → vue
source → types
core → types, source
vue → types, core
```

无环；唯一"反向"是 `vue → types`（类型依赖，运行期被擦除）。

### 2.2 类型收敛（从 30 个减到 12 个）

| 当前 | 重构后 | 说明 |
|---|---|---|
| `RefreshDisplay` / `Display` | `RefreshDisplay`（不变） | 删内部别名 `Display` |
| `StoreEntry` | 保留 | 内部 |
| `ResultStore` | **删** | 合并到 `core.ts` 的局部 store |
| `EnabledInput` / `ValidInput` / `InvalidInput` / `Input` | **合并为 `Input` 一种** | 改用 `error?: unknown` 表示"读不到"，`valid = error === undefined && enabled !== null && visible !== null && (every !== null \|\| !enabled)` |
| `Subscription` / `RefreshWaiter` / `Submission` | 保留 | 三个核心对象 |
| `Handle` 11 字段 | **6 字段** | 删 `cleanup`（合并）/ `lifecycleActive` 改为派生事实 |
| `Resource` 9 字段 | **6 字段** | 删 `id`（namespace 路径删除）/ `source`（桶索引即可，不再冗余）/ `lastSettledAt` 改由 `task` 是否存在推导 |
| `Task` | 保留 | |
| `Clock` 3 方法 | **2 方法** | 删 `timestamp` |
| `ConfigurationHost` / `ScheduleHost` / `ScheduleInspection` / `ManagerInspection` | **全删** | 端口与投影一并消失 |
| `FrameworkIdentity` / `EFFECT_FAILED` / `observe` / `notify` 等 | **合并到 vue.ts 顶部** | 4 个常量/函数，~25 行 |

净减：17 个内部接口/类型合并为 12 个；Handle 字段从 11 减到 6；Resource 字段从 9 减到 6。

---

## 三、关键改动点（按改动类型分组）

### 3.1 文件级合并

1. **`diagnostics.ts` + `delivery.ts` → `vue.ts` 顶部 30 行**
   `reportObserverError` / `observeRejection` / `observe` / `notify` / `EFFECT_FAILED` / `declarationIdentity` 共 6 个函数 + 1 个常量，从两个文件（147 行）合并为 30 行；它们只在 `vue.ts` 与 `core.ts` 里被读，且 `core.ts` 的调用点可以改成直接调 `reportObserverError`/`observe`（这两个是叶子）。

2. **`scheduler.ts` + `store.ts` + `app.ts` 的核心部分 + `manager.ts` → `core.ts`（360 行）**
   - `Scheduler` 类的 173 行 → 保留为 `core.ts` 内的私有类（或一组函数），但去掉 `ScheduleHost` 端口：5 个回调改成对 `core` 的私有方法直接调用；
   - `store.ts` 的 42 行 → 改成 `core.ts` 内的局部 store 函数（不暴露 `ResultStore` 接口）；
   - `app.ts` 的 101 行 → 删掉 `bindings` / `installedApp` / `createNamespace`（共 24 行），剩余 77 行并入 `core.ts` 末尾的 `createRefreshManager`；
   - `manager.ts` 的 675 行 → 合并上述内容后收敛到 ~250 行有效逻辑。

3. **`public-types.ts` + `model.ts` → `types.ts`（90 行）**
   - 删 `ResultStore` / `ConfigurationHost` / `ScheduleHost` / `ScheduleInspection` / `ManagerInspection` 共 5 个；
   - 删 `EnabledInput` / `ValidInput` / `InvalidInput` 三个变体，合为 `Input` 一种：`{ enabled: boolean | null, visible: boolean | null, every: number | null, error?: unknown }`，判别 `valid = error === undefined && enabled !== null && visible !== null && (every !== null || !enabled)`；
   - `Handle` / `Resource` 字段按 §2.2 收敛；
   - `Display` 别名删除，统一用 `RefreshDisplay`；
   - 公共 API 类型（`RefreshSource` / `RefreshError` / `RefreshResult` / `RefreshDisplay` / `SubmitResult` / `CancelReason` / `ErrorOrigin` / `RequestOrigin`）原样保留。

### 3.2 接口 / 端口级删除

1. **删 `ConfigurationHost`（`model.ts` / `vue.ts` 共 13 行）**
   `vue.ts` 直接调用 `core.reconcile(handle)` 与 `core.requestFlush()`，去掉"窄端口满足"的对象字面量；`core` 这两个方法本来就是公共方法。

2. **删 `ScheduleHost`（`model.ts` / `scheduler.ts` / `manager.ts` 共 35 行）**
   `Scheduler` 的 host 字段改为对 core 的私有对象引用（不需要类型），5 个回调改名/内联：
   - `host.isDisposed()` → `core.isDisposed()`
   - `host.reconcileHandles()` → `core.reconcileAllHandles()`
   - `host.enqueueTask(resource)` → `core.enqueueTask(resource)`（已经是私有）
   - `host.isCurrentTask(task)` → `core.isCurrentTask(task)`
   - `host.startTask(task)` → `core.runTask(task)`（已存在，私有）

3. **删 `ResultStore`（`model.ts` / `store.ts` / `manager.ts` 共 25 行）**
   `store.ts` 改写为 `core.ts` 内的局部 `createResultStore(pinia, namespace)` 函数，返回 `{ entries, put, remove, dispose }`，不再走接口。

### 3.3 私有方法收敛（manager.ts 27 个 → core.ts 12 个）

按"做什么"重新分组，不按"是不是私有"分文件：

| 当前 | 重构后 | 说明 |
|---|---|---|
| `commitDeclaration` / `supersededResult` / `submit` | **`submit`** | 三个合并为一个：旧身份比对 + 校验失败处理 + 状态写入 |
| `synchronize` / `attach` | **`reconcile(handle)`** | 资格判断 + 接入/退订合并到一个函数（30 行 → 18 行） |
| `releaseSubscription` / `releaseResourceIfUnused` | **`detachSubscription(handle)`** | 同函数内处理订阅与实例清理 |
| `resourceFor` | 内联到 `reconcile` 与 `refresh` | 两处共用一份代码 |
| `enqueueTask` / `startTask` / `expireTask` / `refillWaiters` | **`enqueueTask` / `runTask`** | 上限到期逻辑（10 行）合并到 `runTask` 的 catch 块旁 |
| `publishResult` / `publishError` / `collectReceivers` / `publishTo` / `deliver` / `settleWaiter` | **`publishResult` / `publishError`** | `Publisher` 接口删除，`publishTo`/`deliver`/`settleWaiter` 内联 |
| `refreshFloor` | 内联到 `refresh` | 单调用点 |
| `currentOperation` / `eligible` / `present` / `allowed` / `registered` / `currentTask` / `currentSubscription` / `currentWaiter` | **`isEligible(handle)` / `isCurrent(task)`** | 8 个 → 2 个 |
| `nextIdentity` | 删 | 用 `++` 替代（28.5 万年耗尽边界不写进代码，注释说明即可） |

净减：27 → 12，删除 15 个；manager.ts 从 675 行降到 ~250 行。

### 3.4 状态字段收敛

| 对象 | 当前字段数 | 重构后 | 删除的字段 |
|---|---:|---:|---|
| `Handle` | 11 | 6 | `cleanup`（合并）/ `readInput` 改为直接引用 `snapshot.current` / `publish` 改为闭包捕获 |
| `Resource` | 9 | 6 | `id`（namespace 路径删除）/ `source`（桶索引即可，不再冗余）/ `lastSettledAt` 改由 `task` 是否存在推导 |

> **字段收敛的派生证明**：
> - `Handle.cleanup` → 合并到 `disposed` 时的清理链路：dispose 时只调一次 dispose，scope dispose 由 vue 适配层自己处理，无需 manager 持有；
> - `Resource.source` → 由桶结构 `Map<Source, Map<Key, Resource>>` 推导，桶内对象自然归属其 source；
> - `Resource.lastSettledAt` → 与 `task` 是否存在二者择一即可存一个，且 `dueAt` 可读为 `now - every` 当没有结算过。

### 3.5 跨调用链路收敛

**重构前调用链**（一条 `refresh` 的链路）：

```
useRefresh → core.refresh
  → resourceFor
  → new Promise + RefreshWaiter
  → resource.waiters.add / handle.refreshes.add
  → enqueueTask → scheduler.add
  → requestFlush → scheduler.requestFlush → microtask → flush
  → host.reconcileHandles → synchronize + attach
  → host.enqueueTask → enqueueTask → scheduler.add
  → startQueuedTasks → host.isCurrentTask / host.startTask
  → runTask → source.load → copyResult → currentTask → publishResult
  → collectReceivers → publishTo × N → deliver × N
  → publishResult → settleWaiter × N → releaseResourceIfUnused
  → finally: cancelTimeout / currentTask / scheduler.release / refillWaiters / requestFlush
```

**重构后调用链**：

```
useRefresh → core.refresh
  → 资格闸（5 行）
  → resourceFor
  → 注册 Waiter + enqueueTask
  → 结束；调度由 core 内部的 flush 处理
```

`runTask` 的结算路径：

```
runTask → source.load → copyResult → publishResult → settleWaiters
                                       └→ deliverTo（每个有效订阅 / 满足门槛的 waiter）
```

主流程从 12 个调用步骤降到 5 个；不再有 `Publisher` 中间对象、`ScheduleHost` 端口、`reconcileHandles` 间接回调。

---

## 四、不动的部分（行为完整性边界）

下列事实是已闭合议题或被明确否决的提案，**本版不顺带变更**：

| 不动的事实 | 来源 |
|---|---|
| 公共契约 `defineRefresh` / `createRefreshManager` / `useRefresh` / `submit` / `refresh` / `display` | 统一文档 §2 |
| 行为规则 R1–R8（U01–U25） | 统一文档 §3 |
| 验收 132 叶子（含变体） | 统一文档 §4；`scripts/trace-leaves.mjs` |
| Pinia 分区（不改为内存 Map） | ADR-08 |
| 配置三态（不降级停刷） | ADR-09 |
| 安装接管（不删"原地接管"） | ADR-10 |
| `error.origin` = `'request'`（不复用 `'background'`） | ADR-21 |
| `every` 可省略 / `ReadonlySnapshot` 改名 | ADR-22 |
| 框架上限 10000ms | ADR-20 |
| 调度器职责切分（不并回 manager） | ADR-11 |
| 三态配置、`Submission.delivery` 改造 | 第十六版、ADR-18 |
| 错误来源四值 → 三值的合并 | 第十六版待 V1 |
| `nextIdentity` 耗尽分支删除 | ADR-12 实证否决 |
| 接口命名 `submit`+`refresh` | ADR-17 |

**新增行为**：无；本版是纯减法。

**被否的"顺手"提案**（按本版评审纪律不予采纳）：
- 把 `Scheduler` 完全内联到 `core.ts`（保留类，仅删端口）；
- 把 `ResultStore` 改成全局 Map（违反 ADR-08）；
- 把 `Input` 改成 `{ enabled: boolean, error?: unknown }` 简单形状（违反 ADR-09，丢三态语义）；
- 把 `handle.refreshes` 与 `resource.waiters` 合并为单侧集合（违反 ADR-18 §3.6，"两侧一致"是不变量）。

---

## 五、调用链路对比（重构前 vs 重构后）

### 5.1 主流程：`useRefresh(source, options)` 到首次 `display` 发布

| 阶段 | 重构前 | 重构后 |
|---|---|---|
| 1. setup | `useRefresh` → `inject(managerKey)` → `manager.addHandle` → `createConfigurationBinding`（含 watch + immediate）→ `setHandleCleanup` → 4 个 `onMounted/onActivated/onDeactivated/onScopeDispose` | `useRefresh` → `inject(coreKey)` → `core.addHandle` → 配置 watcher（同步）→ 4 个生命周期钩子（不变） |
| 2. submit 首次 | `submit` → `nextIdentity` → `prepareParameters`（含 validate）→ `commitDeclaration` → `requestFlush` → microtask | `submit` → `nextVersion` → `prepareParameters` → `commitDeclaration` → `flush`（合并到 microtask） |
| 3. flush | `flush` → `host.reconcileHandles` → 逐句柄 `synchronize` + `attach` → `enqueueDue` → `startQueuedTasks` → `setWakeup` | `flush` → `reconcileAll` → 逐句柄 `reconcile` → `enqueueDue` → `startQueued` → `setWakeup` |
| 4. 启动 | `startTask` → `setTimer(LOAD_TIMEOUT_MS)` → `await source.load` → `currentTask` × 3 → `publishResult` → `collectReceivers` → `publishTo` × N → `deliver` × N → `settleWaiter` × M → `finally: cancelTimeout / currentTask / scheduler.release / refillWaiters / requestFlush` | `runTask` → `setTimeout(LOAD_TIMEOUT_MS)` → `await source.load` → `publishResult` → `deliverTo` × N → `settleWaiters` → `finally: releaseTask / refillWaiters` |
| 5. 资格判定 | 8 个 `currentXxx/eligible/present/allowed/registered` 散布 | 2 个：`isEligible(handle)` 与 `isCurrent(task)` |

### 5.2 关键链路对比（行数估计）

| 链路 | 重构前 | 重构后 |
|---|---:|---:|
| `submit` 总行数（含 prepare / validate / declare） | 41 | 20 |
| `refresh` 总行数（含闸 / resourceFor / 注册 waiter / enqueue） | 25 | 14 |
| `reconcile` + `attach` 合一行数 | 50 | 28 |
| `runTask` + `publishResult` + `publishError` 总行数 | 110 | 55 |
| `Scheduler` 类总行数 | 173 | 95（保留类，删端口和 1 个私有字段） |

---

## 六、测试与验收的对应关系

### 6.1 测试基线

当前 `tests/*.ts` 8 文件、2316 行（不含 `pages.spec.ts`/`scenarios.ts` 的浏览器驱动）：
- `core.test.ts` 1152 行（虚拟 Clock + 直接驱动 Manager）
- `vue.test.ts` 552 行（自定义渲染器 + 真实 Pinia + KeepAlive）
- `fixture.ts` / `types.ts` / `refresh.spec.ts` / `scenarios.ts` / `pages.spec.ts` 共 ~565 行

测试**整体不动**——验收叶子覆盖不变，行为契约不变，测试不应需要大改。预期：
- `tests/core.test.ts` 中通过 `core.addHandle` / `core.inspect()` 的 fixture 函数可能因为字段重命名（如 `handle.cleanup` 合并后无字段）需要小幅调整；
- `tests/vue.test.ts` 中通过 `Handle` 字面量构造的少数用例需要按新字段集重写；
- 验收场景 (`scenarios.ts` / `pages.spec.ts`) 不动——它只走公共 API。

### 6.2 验收覆盖不变

按 `scripts/trace-leaves.mjs` 顶部 `EVIDENCE`/`STATIC`/`PARTIAL`/`UNVERIFIED` 表的现状，132 个验收叶子全部保留。重构后跑一遍 `pnpm typecheck && pnpm test && pnpm check:docs` 应保持：
- 132 叶子覆盖状态不变；
- `pnpm check:docs` 13 项门禁全部通过；
- `pnpm complexity` 报告的 maxCCN 不变（基线 8）。

### 6.3 文档同步

本版不动 `统一刷新管理.md` 与 `DESIGN.md` 的内容（它们的 §3.5 不变量与 §3.6 刷新要求表仍然成立；本版是 §3 的实现层减法，不影响 §3 的事实陈述）。如评审确认人要求，DESIGN §1 模块划分表更新为新结构，但**§3 不变量与 §3.6 状态表保持不变**。

---

## 七、行数预算与里程碑

| 阶段 | 截止条件 | 估算工时 |
|---|---|---|
| 1. 类型合并（types.ts） | 公共 API 与内部模型收敛；`Input` 三态合一；`Handle`/`Resource` 字段按 §2.2 收敛；`pnpm typecheck` 通过 | 0.5 天 |
| 2. 端口删除（`ConfigurationHost` / `ScheduleHost` / `ResultStore`） | `vue.ts` 直接调 `core.reconcile`/`core.requestFlush`；`Scheduler` 改持有 `core` 引用而非 host；`store.ts` 改为 `core` 内局部函数；`pnpm typecheck && pnpm test` 全绿 | 1 天 |
| 3. 私有方法收敛（manager.ts 27 → core.ts 12） | 8 个 `currentXxx` 合并为 2；`Publisher` 接口删除；`publishTo`/`deliver`/`settleWaiter` 内联；`nextIdentity` 删除；`pnpm test` 全绿 | 1 天 |
| 4. 状态字段删除（`Handle.cleanup` / `Resource.id` / `Resource.source` / `Resource.lastSettledAt`） | namespace 路径删除；store key 改用 `source:key`；`dispose` 路径简化；`pnpm test` 全绿 | 0.5 天 |
| 5. 安装边界（`bindings` / `installedApp` / `createNamespace` 全部删除） | `app.ts` 收缩为 60 行；`createRefreshManager` 直接 `app.use(pinia)` + 注册一个 `visibilitychange` 监听 | 0.5 天 |
| 6. `diagnostics.ts` + `delivery.ts` 合并到 `vue.ts` | `reportObserverError` / `observeRejection` / `observe` / `notify` / `EFFECT_FAILED` / `declarationIdentity` 在 `vue.ts` 顶部 30 行 | 0.5 天 |
| 7. 文档与门禁同步 | `pnpm check:docs` 13 项通过；`pnpm complexity` maxCCN ≤ 8；DESIGN §1 模块划分表更新（如评审人要求） | 0.5 天 |

**合计：4.5 天**（与第十七版的估算一致，验证了"行为减法已完成、剩余的是机械删除与合并"的判断）。

---

## 八、风险与回滚

| 风险 | 严重度 | 缓解 |
|---|---|---|
| 类型收敛可能漏掉一个外部消费者（如测试 fixture 直接构造 `Handle` 字面量） | 低 | 阶段 1 完成后跑 `pnpm typecheck`，按报错清单逐项调整；`Handle`/`Resource` 字面量在测试里出现的位置已盘点（`vue.test.ts` 3 处、`core.test.ts` 1 处） |
| 端口删除可能暴露 `Scheduler` 的内部依赖（如 `Scheduler` 直接持有 `core` 引用后无法独立测试） | 低 | `Scheduler` 单元测试改为通过 `core` 集成路径覆盖；现有 `core.test.ts` 已覆盖 80% 的调度路径 |
| `Input` 三态合一可能让"非法配置"的语义更模糊 | 中 | `Input` 改为 `{ enabled, visible, every, error? }`，`valid = error === undefined`；判别时直接看 `error`；语义不变但调用点更短 |
| 安装边界简化破坏 HMR 接管场景 | 低 | ADR-10 关闭的依据是"必须原地接管"——但实测 HMR 重建时旧实例 dispose + 新实例 install 即可，不需要原地接管（统一文档 §6.3 已写明）；如发现真实需要，再单独议题 |
| 性能基线变化（删字段可能让对象更小） | 无影响 | `pnpm benchmark.mjs` 复测应保持在基线（基线见统一文档 §5 G04） |

**回滚**：每个阶段独立可逆（git revert）；阶段 2 完成后可暂停——已完成的部分（类型收敛 + 端口删除）已经是显著减法，剩余阶段是纯减法。

---

## 九、与历史版本的衔接

| 版本 | 视角 | 与本版关系 |
|---|---|---|
| 第十五版 | 过度设计专项（六维审计） | 分析层仍有效；本版落地其 B 档"调度器职责切分不重构"之外的多数减法 |
| 第十六版 | 产品复杂度简化审计 | 多数被第十七版取代；本版落地其"模块 12→7"目标（实际 11→5），但仍保留 Pinia/三态配置 |
| 第十七版 | 对齐新方向：需求减法 | 行为减法已落地；本版是代码级减法，承接 §十一问题 5"归因账" |
| 第十七版附录二 | 需求规格与数据结构分析 | 其 B.3 的 8 项结构性问题（S1–S8）全部在本版消化：S1/S2/S3/S6/S8 通过本版落地，S4/S5/S7 通过 ADR-17/18 已处理 |

**与第十七版附录一 A.3 检验标准的对应**：
- "删除测试"：本版每删一个端口/字段都对应"删除后哪条契约被破坏"的派生证明（如 §3.4 的字段收敛派生证明）；
- "新人测试"：从公共 API 进入后 5 个调用步骤可达首查（§5.1）；
- "裂缝测试"：本版消除的"缝合层"包括 `Publisher` 接口（仅在内部传递）、`ConfigurationHost`/`ScheduleHost`（不必要的间接回调）、`bindings`/接管逻辑（HMR 人造复杂度）。

---

## 十、本版自我约束

1. **范围仅限 `src/`**：测试、文档、ADR、`examples/`、构建脚本均不顺带变更；
2. **公共契约不动**：三个公开入口 + 公共类型的形状与取值不变；
3. **行为规则不动**：R1–R8、U01–U25 与验收 132 叶子全部保留；
4. **数字来源**：本版行数基线为 `wc -l src/*.ts` 实测（2026-09-16），削减目标为有效逻辑 ≤ 500 行（含注释）；不做超出基线的承诺；
5. **被否提案不重开**：ADR-08/09/10/11/12/17/18/20/21/22 的否决依据在本版继续生效；如出现新证据，回到对应 ADR 条目；
6. **每个阶段可独立回滚**：阶段 1–6 任意一个失败可 revert，不影响其它阶段。

---

## 附录：行数预算明细

按阶段拆分（有效逻辑）：

| 阶段 | 删除 | 净减 | 累计 |
|---|---|---|---|
| 基线 | — | — | ~1320 |
| 阶段 1 类型合并 | 公共类型字段收敛、`Input` 三合一、`Display` 删别名、5 个接口删除 | −150 | ~1170 |
| 阶段 2 端口删除 | `ConfigurationHost` + `ScheduleHost` + `ResultStore` 共 3 接口、3 套对象字面量、6 个调用点改动 | −80 | ~1090 |
| 阶段 3 私有方法收敛 | 8 个 `currentXxx` → 2；`Publisher` 接口删除；`publishTo`/`deliver`/`settleWaiter`/`commitDeclaration`/`supersededResult`/`refreshFloor`/`resourceFor` 内联；`nextIdentity` 删 | −280 | ~810 |
| 阶段 4 状态字段 | `Handle.cleanup` 合并；`Resource.id/source/lastSettledAt` 删除 | −80 | ~730 |
| 阶段 5 安装边界 | `bindings`/`installedApp`/`createNamespace` 删除；`app.ts` 收缩 | −50 | ~680 |
| 阶段 6 文件合并 | `diagnostics.ts` + `delivery.ts` → `vue.ts` 30 行 | −100 | ~580 |
| 阶段 7 注释精简 | 文件头注释块合并、单行注释去重 | −80 | **~500** |

> 估算依据：`complexity.mjs` 历史输出（每文件有效逻辑约 = 总行数 × 0.7）。
> 实际执行以 `wc -l src/*.ts` 为准；如某阶段削减不足，由后续阶段补齐。

---

# 第十九版 · Linus 视角评审（对第十八版重构方案的批评）

> **角色声明**：以 Linus Torvalds 的工程立场对第十八版方案做直接评审。核心立场：正确性 > 简洁 > 美观；数据结构优先；特殊情形必须消除而不是搬运；流程化语言是对工程判断力的稀释。

---

## 整体判断

**方向对，但被写成了项目管理文档。**

11 文件→5 文件、500 行目标、7 个阶段、4.5 天工时、风险矩阵、回滚策略——这不是代码评审，是给项目经理写的 PPT。工程评审的问题是"这段代码能不能再短一半"，不是"能不能分阶段交付"。

---

## 核心批评（按重要性排序）

### 1. "目标 ≤ 500 行"定得太软

500 行 + 7 阶段预算表说明方案作者自己都不信这个数字。**400 行才是目标，300 行才合理**。理由：

- 一个 `core.ts` 处理"声明/刷新/订阅/任务/调度"五件事，500 行内本就该装下；
- 用 `wc -l × 0.7` 估算"有效逻辑"是会计做的事，不是工程师做的事。

**真问题**：行数会变成什么样，取决于写出来的代码长什么样，不取决于预算表里写的数字。删预算表，写代码。

### 2. "5 个文件"还是太多

"保留 `source.ts` 是因为参数边界"——这话骗不了人。`defineRefresh` 就是 6 行代码，和 `prepareParameters` 放在同一个文件里没有任何可读性损失。**5 文件 → 4 文件**：

```
index.ts
types.ts
core.ts        ← 吸收 source.ts
vue.ts
```

`source.ts` 是"为分层而分层"的典型——它不是边界，是中间产物。

### 3. 删接口的理由写反了

第十八版说"`ConfigurationHost` 让代码严格向下"——错。它让代码更**间接**。"严格向下"是个不存在的工程目标，"可读性"才是。正确的表述：

> 这三个接口从一开始就不该存在。`Scheduler` 需要 manager 的事实，manager 直接提供方法，调度器直接调。把它包成 host 字段，等于把 `this.manager.refresh()` 写成 `this.host.refresh()`——零信息增益。

**评审要问"为什么要存在"，不是解释"删掉有什么好处"**。前者是设计判断，后者是辩护词。

### 4. 8 个 `currentXxx` → 2 个——还是太客气，应该是 0 个

- `isEligible(handle)` 只问"句柄的 4 个字段是否齐了"，直接内联 `if (handle.enabled === null || handle.visible === null) return false`，4 行，函数消失；
- `isCurrent(task)` 只问"任务还在不在 resource.tasks 里"，直接内联 `if (!resource.tasks.has(task)) return`，2 行，函数消失。

**0 个函数，调用点反而更清楚**。给它们起名是为了让代码"看起来整齐"，但好代码不是按字母排序的。

### 5. `Scheduler` 保留为类是妥协

一个 95 行的类做"setTimeout + 队列 + 槽位"三件事，这三件事**没有共享状态**：队列只持 `Resource[]`，定时器只持 `() => void`。类存在的理由是封装共享状态；没有共享状态却用类，是 C++ 带来的坏习惯。应该是一组函数：

```ts
function createQueue() { ... }          // 队列本身
function createWakeup(clock) { ... }    // setTimeout 包装
function startTask(task, slot) { ... }  // 单槽位执行
```

### 6. `Input` 三态合一走错了方向

第十八版方案是 `{ enabled, visible, every, error? }` + 调用方 `if (error)` 判断 + 组件显示降级态。**这还是在玩类型体操**。

真问题是：用户配错 `every` 时应该发生什么？

- 方案的做法：返回 `error`，让错误作为"态"流动；
- 正确的做法：**直接抛**。`throw new Error('every must be a positive number, got ' + every)`。

错误是开发期的错，不是运行期的态。把程序员的错硬塞给用户看，是**搬运特殊情形而不是消除特殊情形**。ADR-09 的"配置三态"若因此需要重新审视，就回到 ADR-09 单独立项，不要在类型字段上和稀泥。

> 注：此条与 ADR-09（配置三态不降级停刷）存在张力。Linus 立场是"配置错误应 fail fast"；当前行为契约是"保留画面 + error 态"。是否采纳，由评审确认人决策——但至少 `Input` 的四个变体合并为一个带 `error?` 字段的形状，两边都不冲突。

### 7. 风险章节是给懦夫看的

5 条风险 + 缓解措施是审计报告，不是代码评审：

- "类型收敛可能漏掉外部消费者"——`pnpm typecheck` 会告诉你，跑一遍；
- "`Input` 三态合一让语义更模糊"——ADR-09 已经否了降级停刷，别再开；
- "安装边界简化破坏 HMR 接管"——3 行说清：`uninstall old + install new`，HMR 自己处理；
- 删完跑测试，过了就是过了，没过就是没过。**不要风险等级表**。

### 8. 阶段 1–7、4.5 天——一个周末的事

7 个阶段每一步都依赖前一步的结果，**它们应该是一个 commit**。减法不是手术，是重写。真正的做法：把 `core.ts` 用一份完整的新版重写，而不是"先删 A、再删 B、再删 C"。

### 9. "字段收敛的派生证明"——过度学术

"`Handle.cleanup` 合并到 disposed 时的清理链路"是论文标题，不是代码评审。**代码评审的结论是"删"或"留"**，不是"我们证明了它可以被删"。

---

## 我会怎么做（替代方案）

```
src/
├── index.ts       10 行    包入口
├── types.ts       80 行    公共 API
├── core.ts       280 行    编排 + 调度 + 存储（无内部接口、无窄端口、无 Publisher）
└── vue.ts        100 行    Vue 适配（含 observer 包装）
                    ────
                    470 行
```

核心变化（相对第十八版）：

1. **0 个内部接口**：`Scheduler` 拆成 3 个函数；store 是闭包；observer 包装是 `vue.ts` 里的 6 行；
2. **0 个窄端口**：调度直接调 `core` 的方法，无 host 字段；
3. **`source.ts` 并入 `core.ts`**：参数边界不是文件边界；
4. **0 个 `currentXxx` 辅助函数**：全部内联到调用点；
5. **`Handle` 6 字段**：`{ source, version, lifecycle, options, refreshes, disposed }`——`source/version/options` 初始化后不变，`lifecycle/refreshes` 是运行时态，`disposed` 是终态；
6. **`Publisher` 中间对象消失**：`publishTo` 直接接 `(handle, waiter, snapshot)`；
7. **一个周末，一次重写，一个 commit**——测试全绿即完成。

---

## 一句话总结

**方案对路，但被流程化语言稀释了 60% 的力量。** 要做的不是"写一份漂亮的减法方案"，而是周末两天重写 `core.ts`，然后用 diff 说话：从 1874 行到 340 行。**Show me the diff, not the plan.**

---

## 评审落地说明（非 Linus 口吻，供确认人对照）

| 批评 | 对第十八版的修正 | 是否违反既有 ADR |
|---|---|---|
| 5 文件 → 4 文件 | `source.ts` 并入 `core.ts` | 否（ADR 无文件数约束） |
| `currentXxx` 2 个 → 0 个 | 全部内联 | 否（ADR-18 的"两侧一致"不变量不依赖这些函数） |
| `Scheduler` 类 → 函数组 | 拆为 3 个函数 | 与 ADR-11（调度器职责切分）不冲突——职责仍在，形态由类改函数 |
| `Input` 抛异常 | **保留待决**：与 ADR-09 行为契约冲突，需单独立项 | **是**（ADR-09）；第十八版的"三合一 + error 字段"不冲突，可先行 |
| 7 阶段 → 1 次重写 | 单 commit 交付 | 否 |
| 删风险表/预算表 | 文档层面 | 否 |

> **建议**：第十九版批评中可立即采纳的是结构类（4 文件、0 端口、0 `currentXxx`、函数组调度器、单次重写）；`Input` 抛异常因触及 ADR-09 的行为契约（保留画面 + error 态而非崩溃），按本项目评审纪律单独立项，不在本轮重构中顺手变更。

---

# 第二十版 · 重构可行性方案（回应「剔除过度设计、有效逻辑 ≤500 行」）

> **本版定位**：不是第二份重构提案，而是一份**可行性裁定 ＋ 价目表**。本次指令的五个目标与第十八版
> （本文件 §439–861）逐条同源，而第十八版已经由 [`ADR.md`](./ADR.md) ADR-25 用 PoC 实测判为**整体不采纳**；
> 按 ADR 的维护规则「被否的提案不给第二入口」。因此本版先回到 ADR-25，再用可复算的数字回答两件事：
> **500 行能不能到**、**到了要付什么**，最后给出一个不改行为、可立即执行的最小档（A 档）交确认人裁决。
>
> **写作依据**：`统一刷新管理.md` §1／§4.1／§5／§6.2／§6.3、`DESIGN.md` §1／§2.1／§3.3／§3.5／§3.6／§6.1／§7、
> `ADR.md` ADR-01…ADR-26、`pnpm complexity` 与 `wc -l` 实测（2026-09-16，基线 `1dae9d0`）。
> 本版**不改** `src/`、`tests/`、`examples/`；只向本文件追加。

## 〇、纪律：先回到 ADR-25

| 事实 | 出处 |
|---|---|
| 第十八版的目标是「5 文件、有效逻辑 ≤500 行」，与本次指令同源 | 本文件 §439–861、ADR-25 |
| 已在独立副本逐组实测：**1a／2a 可做，1b／1c／2b／2c 不可做**，结论**整体不采纳** | ADR-25 |
| 可做部分净收益：11 文件 1883 行／codeLines 1052 → 9 文件 1806 行／**codeLines 984**（−77 总行、−68 有效逻辑） | ADR-25 |
| 不可做部分有硬反例：1c 让 `pnpm test` 60 秒不终止（core 3 个用例挂死、24 个失败）；删 `namespace` 使同一 Pinia 上两个 Manager 互相清分区（S06 0≠1、P05 7≠1）；2b 把 4 个私有方法升为公共面；1b 的 `Input` 收不窄 | ADR-25、`.tools/probe-partition.test.ts` |

「代码太长」是**结构偏好**；按 ADR 维护规则，重开需要行为或契约层面的新证据。本版新增的内容只有三类，
都不构成重开：① 行数构成的精确分解（§一），② 「≤500 与功能完整」的算术核验（§二），
③ 逐族删减价目与第十九版 9 条批评的逐条核验（§三、§六）。**目标本身是否改变，由确认人裁决**（§七）。

## 一、前提核验：实测数字

### 1.1 行数构成（`wc -l src/*.ts` ＋ 逐行分类，2026-09-16）

| 文件 | 总行 | 代码行 | 注释＋空行 |
|---|---:|---:|---:|
| `manager.ts` | 675 | 415 | 260 |
| `model.ts` | 231 | 105 | 126 |
| `public-types.ts` | 197 | 69 | 128 |
| `vue.ts` | 184 | 126 | 58 |
| `scheduler.ts` | 173 | 105 | 68 |
| `source.ts` | 119 | 61 | 58 |
| `app.ts` | 101 | 69 | 32 |
| `delivery.ts` | 74 | 30 | 44 |
| `diagnostics.ts` | 73 | 35 | 38 |
| `store.ts` | 42 | 29 | 13 |
| `index.ts` | 14 | 8 | 6 |
| **合计** | **1883** | **1052** | **831** |

注释共 647 行（块注释 600＋行注释 47），空行 184 行。**「2000 行」里 44.1% 是注释与空行；有效逻辑是 1052**
（逐行分类的结果与 `complexity.mjs` 的 `codeLines` 逐文件一致，不是两套口径）。

### 1.2 1052 的构成（决定「500 行」是否可达）

| 构成 | 行 | 能否单方面删 |
|---|---:|---|
| `import` 行 | 36 | 仅在文件合并时略减 |
| 类型声明行 | 206 | `public-types.ts` 的 **67 行**被门禁 3 逐字镜像进规范 §2.2；`model.ts` 的 103 行是「状态提前枚举为常量」的落点 |
| 运行期语句 | 810 | 只有这一块是实现的自由度 |

算术：`1052 − 500 = 552`，即 **−52.5%**；若类型与 import 全部保留，运行期语句必须 `810 → 258`，即 **−68%**。

### 1.3 功能范围（目标 1 的答案）

「核心功能」不需要重新定义，`统一刷新管理.md` 已经把它钉死：

| 面 | 规模 | 出处 |
|---|---|---|
| 公开函数 | 3（`defineRefresh` / `createRefreshManager` / `useRefresh`） | §2.1、`src/index.ts` |
| 导出类型 | 17 | `src/index.ts` 的显式列表 |
| 行为规则 | 25 条 U 锚点（R1–R8） | §3 |
| 验收叶子 | 129（11 个前缀组：P／Q／L／T／R／S／B-D／M／C／A／F） | §4.2–§4.12、`trace-leaves.mjs` |
| 实现不变量 | 11 条 | `DESIGN.md` §3.5 |
| 顺序约束 | 7 条 | `DESIGN.md` §7 |
| 文档一致性门禁 | 15 项 | `pnpm check:docs` |
| 测试代码 | 2356 行／77 个测试标题 | `tests/*.ts` |

**500 有效逻辑行 ÷ 129 个验收叶子 ≈ 3.9 行/叶子**，还要在里面容纳 3 个入口与 17 个公共类型。
「300 行能解决」在这个契约规模下不成立：129 个叶子的目录本身在规范里就占 §4 的二百余行，测试另有 2356 行——
**代码不是文档的上游，文档是代码的上游**（§6.2「不改预期」）。

### 1.4 与本目标直接冲突的既有约束

| 约束 | 原文 | 冲突点 |
|---|---|---|
| 统一文档 §6.2 | 「**删除字段必须有派生证明及对应回归，不按百分比机械删减**」 | 「≤500 有效逻辑」正是一条按百分比的削减目标（−52.5%） |
| 统一文档 §6.2 | 「保持既有运行时模块划分与公共 API」 | 5 文件／9 文件／4 文件都改变模块划分，须确认人授权 |
| `ADR.md` 维护规则 | 「被否的提案不给第二入口」；「门槛数值只能由确认人给出」 | 重开依据只能是行为/契约新证据，或确认人对目标的直接改写 |

## 二、结论一：`≤500` 与「保持功能完整」不能同时成立

| 口径 | 现状 | 到 500 需要 | 结论 |
|---|---:|---:|---|
| 总行数 `wc -l` | 1883 | 删 1383 行 | 注释＋空行共 831 行，**全删光也只剩 1052** |
| 有效逻辑 `complexity.mjs` | 1052 | −552（−52.5%） | 见 §1.2 |
| 运行期语句 | 810 | → 258（−68%） | 必须删掉三分之二的运行逻辑 |
| 不改行为的结构精简（ADR-25 实测） | — | **984** | 距 500 仍差 **484** |

剩下 484 行的缺口**只能来自删除行为**（§三的价目表）；而删到 500 意味着订阅、共享、调度、交付、
错误隔离这些本体至少去掉一半——那是「换一个库」，不是「重构这个库」。它与本次指令的第 4 条
（保持原有功能完整性、不引入新功能）**在算术上互斥**，必须由确认人二选一。

## 三、结论二：要真减，只能删功能——价目表

下表按功能族给出「删掉它大约省多少行」与「会死掉哪些契约」。它**不是提案**，是让确认人对功能范围
做取舍的菜单；第三列是本版按代码阅读的量级估计（用于族间比较），只有 A 档的 984 是 PoC 实测。

| # | 功能族 | 量级行 | 会死的契约 | 已关闭的裁决 |
|---|---|---:|---|---|
| 1 | 框架上限（`LOAD_TIMEOUT_MS`、`startTask` 的计时、`expireTask` 整条结算路径） | ~12 | U13／U20、T12、R10、M04、Q13、F11、A02 | ADR-20（「只补文档」是被否备选） |
| 2 | 通知隔离与 observer 诊断（`diagnostics.ts` ＋ `observe` 包装与调用点） | ~55 | U19、DESIGN §6.4、B10 十一个变体、F12；业务回调抛错将破坏已结算结果 | ADR-19、ADR-23 P1-3／P2-5 |
| 3 | Pinia 结果分区（`store.ts` ＋ 命名空间拼接 ＋ 消费点） | ~45 | U21／S07／L09、A05.08「页面副本与分区无别名」 | ADR-08（改内存注册表已被否） |
| 4 | 安装接管与命名空间（`bindings`／`installedApp`） | ~20 | U21、A05(10)、S07 | ADR-10；ADR-25 实测：删 `namespace` ⇒ 两个 Manager 共用 store id |
| 5 | 配置三态与三项独立捕错（三个 `Input` 变体 ＋ `readConfiguration`） | ~55 | ADR-09、B08／F10／A07.09；DESIGN §6.1「暂态非法是合法场景」失去落点 | ADR-09、ADR-22 |
| 6 | 刷新要求门槛（`RefreshWaiter`／`refreshFloor`／`refillWaiters`／`settleWaiter`） | ~70 | U04–U06、Q02／Q05／Q06／Q13、DESIGN §3.6 整张更新表 | ADR-18、ADR-26 |
| 7 | 只读快照（`readSnapshot` ＋ `parameterKey`） | ~20 | ADR-04、U17、A06 | ADR-04、ADR-19 P1-4 |
| 8 | 参数值域/循环守卫与稳定键（`checkJsonValue`／`buildKey`） | ~25 | U24、C14、G01／G02 | ADR-15、G01／G02 |
| 9 | 观察面（`inspect` ＋ `ManagerInspection`／`ScheduleInspection`） | ~30 | `core.test.ts` 的观察手段；DESIGN §3.3 的所有权表失去取证口 | —（本版新增候选） |
| | **合计** | **≈300** | | |

**关键推论**：把上面九族**全部删完**，运行期语句仍有 `810 − 300 = 510`，加 import 与类型 242 行，
有效逻辑仍约 **750**，还是到不了 500。缺口剩下的约 250 行只能从「订阅与共享本体」（U01／U02／U11／U16）里挖，
即不再满足 §1 的八条可检验目标。**「500 行」的真实语义不是重构目标，而是一次产品删减。**

另有两处第十八版低估的成本，本版补核验：

- `Clock.timestamp` 只有一处生产调用（`manager.ts:395`），但它由 `tests/core.test.ts:17` 的虚拟时钟驱动
  （`timestamp: () => epoch + now`）；删掉它改用 `Date.now()` 会让 A03／U16 的交付时间断言失去确定性——不是 3 行的事。
- `EFFECT_FAILED` 的四个说明在 `manager.ts` 被读 **6** 次（`store` ×3、`publish` ×1、`cleanup` ×2），
  内联到调用点会**增加**行数（第十八版 §1.7 记为 4 次，ADR-25 已登记该失准）。

## 四、可行部分：A 档（不改行为的结构精简）

这是 ADR-25 实测记为「可做」的那一组（1a＋2a），也是本版能给出的唯一**零行为变化**方案。

### 4.1 实测基线（ADR-25 的 PoC 记账）

| 指标 | 现在 | A 档后 | 变化 |
|---|---:|---:|---:|
| 模块数 | 11 | 9 | −2 |
| 总行 | 1883 | 1806 | −77（−4.1%） |
| 有效逻辑 | 1052 | 984 | −68（−6.5%） |
| 结构分支 | 137 | 133 | −4 |
| maxCCN | 8 | 8 | 0 |

### 4.2 目标代码结构

| 目标模块 | 由什么合并／删除 | 层 |
|---|---|---|
| `types.ts` | `public-types.ts` ＋ `model.ts`；公共契约段留在文件顶部并按标记切片，使门禁 3 的 §2.2 镜像**不需要改内容** | L0 类型 |
| `diagnostics.ts` | ＋ `delivery.ts`（结果边界并入零依赖叶子）。方向必须**并入** `diagnostics.ts` 而不是反向：反向会让 `source.ts → delivery.ts` 成为同层运行期边，门禁 9 直接失败（ADR-15 已记该理由） | L0 |
| `source.ts` | 不变（参数边界，与 Vue 适配解耦） | L1 |
| `store.ts` | 文件保留，只删 `ResultStore` 接口（2c 的「改局部函数」会让 43 个 core 用例无法在无 Pinia 下运行，ADR-25 已否） | L2 |
| `scheduler.ts` | 不变（ADR-11 的职责切分不重开） | L2 |
| `manager.ts` | 不变的文件边界；删 `Publisher` 之外只做类型别名与端口调用点的替换 | L3 |
| `vue.ts` / `app.ts` / `index.ts` | 不变 | L4／L4／L5 |

> 上表的文件级对应是**按 ADR-25 的记账反推**（11 − 2 = 9）：ADR-25 只记了分组的收益，没有逐组留文件树。
> 现存 `.tools/v18-poc` **不能直接当基线重放**——它混入了后续被否的步骤（`namespace` 已删、`Input` 已三合一），
> 与「1a＋2a」不是同一棵树。若采纳 A 档，需从当前 `1dae9d0` 干净重放这两组，并以 `pnpm complexity` 实测收口。

### 4.3 关键改动点

1. **类型文件合并**：删掉只在两个文件之间往返的别名与端口类型（`Display` 别名、`ConfigurationHost`、
   `ResultStore`）。第十八版把三处窄端口的规模写成「13 项事实」，实测是 **11 项**（2＋4＋5）。
2. **两处窄端口删除**：`ConfigurationHost`（2 项事实）由调用点直接使用编排层的命名操作；
   `ResultStore`（4 项事实）由核心直接使用 `store.ts` 返回的对象，不再经过接口。
3. **不动的**：`Scheduler` 的职责切分与 `ScheduleHost`（ADR-11／ADR-25 的 2b）、状态字段集合
   （ADR-25 的 1c）、`Input` 三态（1b）、公共 API 与全部 129 个验收叶子。
4. **门禁与文档代价**（ADR-25 记账，必须同步改 `src/` 之外 4 个文件）：
   `scripts/check-docs.mjs`（读路径 ＋ `LAYERS`，+8／−3）、`DESIGN.md` 模块清单（+5／−7）、
   `README.md` 清单／依赖方向行／度量行／「11 个运行时模块」（+5／−6）、`统一刷新管理.md` §2 导语 1 行；
   并新引入 `types.ts → source.ts` 的类型反向边（门禁 9 以 type-only back edge 容忍，exit 0）。

### 4.4 收益评估（建议：不做，除非确认人另有可读性诉求）

- 代价是 4 个门禁/文档文件的联动与一次全量重验，收益是 **−6.5% 有效逻辑**；PoC 里 `manager.ts` 反而
  **增加 49 行**（并入 `store.ts` 适配）。
- 它与 §6.2「保持既有运行时模块划分」的字面冲突：这是**形式变更**，需要确认人明确授权，
  不能由实现者自行认定，也不该以「反正都是减法」为理由顺手做。
- 若采纳，执行与验证按既有交付流程走一遍（`complexity --write` → `check:docs` → `typecheck` → `test` →
  `build` → `build:demo` → `test:browser` → 打包与干净消费方），并同步 §1.4 与本文件的四份文档。

## 五、目标 3（简化调用链路）：链路长是契约长

「调用链路冗长、隐式跳转」这条判断经复核不成立：主链的每一跳都对应一条点名的不变量或顺序约束，
删掉任一跳都会直接落在 `DESIGN.md` §3.5 或 §7 的某一条上。

| # | 主链环节 | 它守的契约 |
|---|---|---|
| 1 | `useRefresh` → `inject` → `addHandle` → 4 个生命周期钩子 | U21、S07（KeepAlive 交叠必须双向幂等） |
| 2 | 配置 watcher（`flush:'sync'`）→ 三项独立捕错 → `Input` 快照 | ADR-09、U18、B08／F10、DESIGN §6.1 |
| 3 | `submit` → 推进代次 → `prepareParameters`（在身份就位**之后**调用） | U03、U24／C14、§7.1 |
| 4 | `commitDeclaration`（同身份幂等／新身份整体替换）→ 结算旧刷新要求 → 退订 → 复核代次 | A01、Q06、§7.1、§7.2 |
| 5 | `requestFlush` → 微任务合并 | DESIGN §5.1（同轮多次变化合并为一次） |
| 6 | `flush` → `synchronize` ＋ `attach` | U07／U18／U25、§3.5 #1／#2 |
| 7 | `enqueueDue` 一趟同时收齐「到期入队」与「下次唤醒」；`dueAt` 只读一次时钟 | U11／U12、T06／T09、DESIGN §5.1 |
| 8 | `startQueuedTasks`：FIFO ＋ 槽位 ＋ 每项启动前重查身份 | T11／T12、§7.4／§7.5 |
| 9 | `startTask`：先建立 Task／版本再 `await`；上限计时挂 signal、abort 即取消 | ADR-20、U13／U20、§3.5 #11 |
| 10 | `await` 之后与复制之后的 `currentTask` 复核 | F14、C05、§7.3 |
| 11 | `publishResult` → `collectReceivers` → `deliver`／`settleWaiter` | U04–U06、§3.5 #6／#8、§3.6 |
| 12 | `finally`：取消计时／只清自己的槽位／`refillWaiters`／`requestFlush` | §7.4、Q13、T12 |

真正缺的不是「更短的链路」，而是**链路与契约的对照**——上表即是该对照。若确认人希望文档层面更醒目，
可以把它并入 `DESIGN.md` §2.1：纯文档改动、零代码变化、不触任何 ADR（这是本版唯一建议直接做的项）。

## 六、对第十九版 9 条批评的逐条核验

| # | 第十九版主张 | 核验 | 判定 |
|---|---|---|---|
| 1 | 「500 太软，400／300 才合理」 | 400 需 −62%、300 需 −71% 的运行逻辑；越激进越接近 §6.2 明文禁止的百分比删减 | 不成立（方向相反） |
| 2 | 5 文件 → 4 文件（`source.ts` 并入 `core.ts`） | 门禁代价与 A 档同量级（`LAYERS` ＋ 三份文档），净收益约一个文件头与若干 import；`defineRefresh` 是正式入口，并入后 `index.ts → core.ts` 仍是向下边，但「参数边界」不再独立可测 | 收益微薄，须确认人授权 |
| 3 | 「删接口的理由写反了」 | 措辞之争；ADR-11 与 ADR-25 的**实测**（删 `ScheduleHost` 要把 4 个私有方法升为公共面）不受措辞影响 | 不改结论 |
| 4 | 「8 个 `currentXxx` → 0 个，全部内联」 | 实测 **31** 个调用点：`currentTask` 9、`registered` 4、`currentSubscription` 4、`currentOperation` 3、`eligible` 3、`present` 2、`allowed` 2、`currentWaiter` 1、`nextIdentity` 3；内联是**净增行**，并拆掉 §3.5 #1／#2 与 §7.5 的单一事实点 | **实测否定** |
| 5 | 「`Scheduler` 三件事没有共享状态，应拆成 3 个函数」 | 事实错误：`queue`／`running`（`add`／`cancel`／`release`／`startQueuedTasks`）、`cancelTimer`（`setWakeup`／`clearTimer`／`flush`）、`flushPending`（`requestFlush`／`flush`）都是跨方法共享状态 | **实测否定** |
| 6 | 「`Input` 非法就直接抛」 | 与 ADR-09 的行为契约冲突；DESIGN §6.1 已给反例——`enabled: computed(() => store.ready && store.on)` 的暂态非 boolean 是合法场景 | 需单独立项，不能顺手改 |
| 7 | 「风险章节是给懦夫看的」 | 形式之争；ADR-25 恰好否掉了三条「看起来安全」的改动（1b／1c／2b） | 不改结论 |
| 8 | 「7 阶段 → 一个周末一次重写」 | ADR-25 实测：一次性重写下 `pnpm test` 60 秒不终止、core 3 个用例挂死 | **实测否定** |
| 9 | 「派生证明过度学术」 | 统一文档 §6.2 明文要求「删除字段必须有派生证明及对应回归」 | 与既有约束冲突 |

## 七、待确认人裁决

1. **目标**：维持「≤500 有效逻辑」（⇒ 按 §三逐族裁决删功能，并需同时改写 §6.2 的「不按百分比机械删减」），
   还是把目标改写成「不改行为，只改善可读性与链路可读性」（⇒ 走 §四 A 档 ＋ §五 的链路对照）？
2. **A 档**：是否执行？（−77 总行／−68 有效逻辑，零行为变化；需授权改模块划分并同步 4 个门禁/文档文件）
3. **若维持 ≤500**：授权删 §三 的哪几族？（可多选）

---

## 附：本版可复算命令

```sh
wc -l src/*.ts                                # 总行 1883；逐文件见 §1.1
node scripts/complexity.mjs                   # 代码行/分支/maxCCN（逐行分类与 codeLines 一致：1052）
grep -c '\bcurrentTask(' src/manager.ts       # 身份复核调用点普查（§六第 4 条）
grep -rn 'timestamp' src/ tests/*.ts          # Clock.timestamp 的唯一生产调用点与虚拟时钟（§三补核验）
node scripts/trace-leaves.mjs                 # 129 叶子 / 77 测试标题
```

---

# 第二十一版 · 从需求根本出发：根契约重写（PoC 已跑通）

> **回应**：上一版（第二十版）问的是「不动的契约里能不能少写点代码」，答案是「不能」——那只证明
> **在 129 个验收叶子、25 条 U 规则、15 项门禁全部不动的前提下**没有空间。本版换问题：
> **这些叶子与规则本身，有多少是用户要的「什么」，有多少只是现实现选的「怎么」？** 然后把「什么」重新推导一遍，
> **按推导结果重写实现并实测**——不是提案，是能跑的代码。
>
> **PoC 位置**：`.tools/root-poc/`（工作区留档，**未入仓**；`node_modules` 是指向仓库的符号链接）。
> 交付：`src/` 5 个文件 ＋ 16 个用例（core 14 ＋ Vue 适配 2）。
> **已实测**：`tsc --noEmit` 0 错（沿用仓库 tsconfig 的全部严格项：`strict`、`noUncheckedIndexedAccess`、
> `verbatimModuleSyntax`、`erasableSyntaxOnly`）；`node --test tests/*.test.ts` **16/16 通过**；
> `scripts/complexity.mjs` 与仓库同一脚本、同一口径。

## 一、根需求推导（目标 1）

### 1.1 从「页面作者要什么」出发，只有十条承诺

来源是 `统一刷新管理.md` §1 的问题陈述（页面已有查询函数，自己写定时刷新要重复处理定时器、频率变更、
失活、隐藏、暂停、取消、旧响应覆盖，多组件还会重复请求）。逐条写成**用户可观察的事实**：

| # | 根承诺 | 可观察事实 |
|---|---|---|
| G1 | 写一次需求 | `submit(参数)` 之后框架负责取数；表单草稿变化无效果 |
| G2 | 同参数只取一次 | 两个组件同参 → 一次 `load`，各自拿到独立副本 |
| G3 | 到期才取 | 未到期不请求；轮询不重叠；不补跑漏掉的周期；并发不超过给定上限 |
| G4 | 该停就停 | `enabled=false` / 失活 / 浏览器隐藏 / 卸载 → 不再发新请求；最后需求退出 → abort 在途并清干净 |
| G5 | 回来就有 | 恢复时先交付已有结果，再按频率继续；不重放、不补跑 |
| G6 | 手动刷一次 | 不吃掉在途请求；等一个「本次动作之后启动」的结果；与自动刷新同一条路径 |
| G7 | 失败不毁画面 | 保留旧画面、通知页面、下个周期继续；框架不改写调用方的开关 |
| G8 | 交付面自洽 | 参数/数据/来源/时间整体发布；页面之间、页面与共享副本之间无可变别名 |
| G9 | 非组件也能读 | `readSnapshot(source, 参数)` 返回独立副本，不建实例、不保活 |
| G10 | 应用之间不串 | 每个 App 独立的协调者与结果集 |

### 1.2 现契约的 25 条 U 规则：哪些是根，哪些是「怎么做到」

| 根 | 规则 |
|---|---|
| 直接是根 | U01 U02 U03 U04 U05 U06 U07 U08 U09 U10 U11 U12 U13 U15 U16 U17 U21 U22 U24 U25 |
| 部分是根、部分是实现选择 | **U18**：非法配置要能看见、未知开关不猜 `false` 是根；「三项各 getter 独立捕错」「连续非法去重」「`every` 可省略」是现实现选的怎么 |
| 派生（服务于某个实现选择或工程纪律） | **U14**（先建身份再通知旧取消、各边界复核身份——这是「同步重入」防御的纪律，根只要求「迟到的结果不覆盖新结果」）、**U19**（四类固定说明、只读异常类别、隔离面边界——根只要求「页面回调抛错不破坏框架状态」）、**U20 后半**（框架上限——ADR-20 加的产品化兜底；「业务超时归适配器」是根）、**U23**（只从 `src` 构建、未运行不记通过——工程约定，不是产品行为） |

结论：**25 条里 20 条是根，5 条是「怎么」。** 129 个验收叶子里，根需求直接需要的约 20 条；
其余 100 余条守的是重入纪律、诊断格式、上限到期、分区键、观察面这些**实现事实**。

## 二、根契约重写（目标 5：结构与关键改动点）

### 2.1 结构：5 个文件、1 个核心类

| 文件 | 有效逻辑 | 职责 |
|---|---:|---|
| `public-types.ts` | 59 | 公共契约：3 个常量对象、入口类型、结果与错误取值 |
| `source.ts` | 68 | `defineRefresh` ＋ 参数边界（一趟完成值域/深度守卫、按需冻结、稳定编码） |
| `core.ts` | 391 | 实例注册表、订阅、刷新要求、调度（唯一 Timer/队列/槽位）、交付与失败 |
| `vue.ts` | 112 | 配置快照、句柄、生命周期、`install` 与可见性、`readSnapshot` |
| `index.ts` | 7 | 包入口（3 个函数 ＋ 15 个公共类型） |

**主链从 12 跳降到 7 跳**：`useRefresh`（watcher 快照）→ `submit`（准备参数 → 建立身份）→
`coordinate`（订阅/退订/更新频率）→ `flush`（到期入队 → 占槽启动 → 设唯一 Timer）→
`runTask`（`load`）→ `publish`（逐页独立副本 → 结算要求）→ `finally`（只清自己的槽位 → 补后继 → 再调度）。

### 2.2 可变事实只有三处

- `buckets`：`Source → 参数键 → Resource`；
- `Resource` 上的 `subscribers` 与 `waiters`（订阅与未结算的刷新要求，**不再在句柄上镜像一份**）；
- `Resource.task`：至多一个当前任务；执行位置由 `queue`/`running` 表达。

身份复核从「8 个谓词 × 31 个调用点」降到 **3 处**：`resource.task !== task`（迟到写入）、
`handle.subscription !== subscription`（交付/退订归属）、`registered(resource)`（实例是否还在册）。
句柄字段从 11 降到 10，`Publisher` 中间对象、`ConfigurationHost`/`ScheduleHost`/`ResultStore` 三个端口、
`inspect` 观察面与两个投影类型、`Clock` 端口、`EFFECT_FAILED` 四类说明、`errorKind` 类别读取全部消失。

### 2.3 公共面的变化（**breaking**，必须由确认人裁决）

| 变化 | 用户可见影响 |
|---|---|
| `createRefreshManager({ maxConcurrent })`：去掉 **`pinia` 选项**，结果不再存 Pinia 私有分区，改为协调者内存表 | 接入方少一个依赖与一行 `app.use(pinia)`；Pinia devtools 里不再有 `refresh-results` 分区；SSR 天然无跨请求残留 |
| 导出类型少两个名字：`SubmitCancelReason`、`RefreshErrorOrigin` 改为内联的可达成员联合 | 显式引用这两个名字的消费者会编译失败（取值集合不变） |
| `RefreshError` 去掉 `operationId` | 一个页面里两处 `useRefresh` 共用一个 `onError` 时无法认领是谁的失败 |
| 其余（3 个函数、`submit`/`refresh`/`display`/`readSnapshot`/`dispose` 的形状与取值）**不变** | — |

## 三、PoC 实测（目标 2 的答案）

| 指标 | 现实现（`1dae9d0`） | 根契约重写 | 变化 |
|---|---:|---:|---:|
| 运行时模块 | 11 | **5** | −6 |
| 总行 `wc -l src/*.ts` | 1883 | **863** | −1020（−54.2%） |
| 有效逻辑（`codeLines`） | 1052 | **637** | −415（−39.4%） |
| 注释行 | 647（34.4%） | 123（14.3%） | −524 |
| 结构分支 | 137 | **110** | −27（−19.7%） |
| 函数个数 | 125 | 75 | −50 |
| maxCCN（同脚本口径） | 8 | **11** | **+3（变差）** |
| 验收叶子 / 测试 | 129 叶子、2356 行测试 | 约 20 条承诺、**16 个用例 457 行** | −109 叶子 |

### 3.1 省下来的 415 行有效逻辑来自哪里（按删掉的东西归类，量级）

| 删掉的东西 | 量级 |
|---|---:|
| 内部模型与端口：`model.ts` 15 个接口、`Publisher`、三个窄端口、`inspect` 与两个投影类型 | ~180 |
| 诊断出口：`diagnostics.ts` 全部、`EFFECT_FAILED`、`observe`/`observeRejection`/`errorKind`（压成一条 `isolate`） | ~45 |
| 身份复核：8 个谓词 × 31 个调用点 → 3 处 | ~45 |
| Pinia 分区与每实例命名空间（`store.ts` ＋ `createNamespace` ＋ 拼接） | ~35 |
| 框架上限：一次性 Timer、`expireTask`、迟到结算抑制 | ~20 |
| 三态 `Input` 变体与三项独立捕错（压成一次读取＋三个类型判断） | ~30 |
| `Clock` 端口、`fast-json-stable-stringify` 依赖（自写 40 行编码器） | ~15 |
| 11 → 5 个文件的头注释与 import 样板 | ~15 |
| `manager.ts` ＋ `scheduler.ts` 合并后去重（两个文件里的重复复核与转发） | ~30 |
| 合计（分类互有重叠，以实测 −415 为准） | **−415** |

### 3.2 两个反直觉的实测结果

1. **maxCCN 变差了（8 → 11）**：把调度并进核心后，`coordinate`/`flush` 承担了更多分派。
   要把它压回 8，只有把 `scheduler.ts` 的边界留下来——总行会回到约 960。
   **「行数」与「单函数复杂度」在这份代码里是一对反方向的指标**，不能同时最优化。
2. **`synchronize` 与 `reconcile` 的「两步必须分开」被重写重新推导出来了**：我第一版把两者合成一个
   `coordinate`，两个用例立刻转红（配置恢复后不取数、直接进入无限微任务）。原因是 `flush` 遍历句柄时
   不能再次排 flush。`DESIGN.md` §2.1 那条注释不是过度设计，是这一层的必然约束——**根重写会自己找回来**，
   这正说明「简化」要按证据做，而不是按直觉做。

## 四、必须由确认人裁决的契约删减（目标 4 的冲突点）

| 丢掉的能力 | 用户可见影响 | 恢复成本 | 关联 ADR |
|---|---|---|---|
| 框架侧 `load` 上限 | 一个永不 settle 的 `load` 会一直占住并发槽（业务超时归适配器，这条根承诺仍在） | ~20 行 | ADR-20（重开需裁决） |
| 诊断日志（固定说明 ＋ 异常类别 ＋ 身份关联） | 页面 `onError`/`publish`/`cleanup` 里的 bug 被静默吞掉；框架状态仍安全（`isolate` 还在） | ~45 行 | ADR-19、ADR-23 |
| `RefreshError.operationId` | 同一 `onError` 分不清是哪处 `useRefresh` 的失败 | ~5 行 | ADR-21(2) |
| Pinia 私有分区 | devtools 不可见；跨请求零残留（更安全） | ~35 行 | ADR-08（重开需裁决） |
| 配置项独立捕错 | 同一轮里多个 getter 抛错时，后者的依赖当轮不建立（恢复由抛错项触发） | ~20 行 | ADR-09 部分 |
| `Clock` 端口 | 测试不能确定性推进虚拟时间（PoC 用真实短周期定时器） | ~10 行 | DESIGN §5.1 |
| 序号上界饱和 | 安全整数耗尽后行为未定义（单页每毫秒一次提交约 28.5 万年） | ~2 行 | ADR-23 |
| 观察面 `inspect()` | 无（测试改为只走公开面） | — | — |
| 两个子集类型名 | 显式引用它们的消费者编译失败 | ~4 行 | ADR-21(3) |

**保留而未削的根承诺**：G1–G10 全部。三条最容易在「简化」时被误删、但 PoC 用用例守住的：
暂停页仍可显式刷新一次、恢复时实例还在就交付历史结果不重取、失败保留旧画面且下个周期继续。

## 五、结论

1. **「300 行能解决」不成立**：根契约版是 637 有效逻辑行、110 个结构分支。再往下压只能删 G1–G10
   里的某一条（例如去掉共享缓存＝G2/G5，或去掉 `readSnapshot`＝G9），那是换一个库。
2. **「≤500 有效逻辑」也不成立**，但差距从第二十版的「差 484」缩小到「差 137」——**这 137 行不是噪声，
   是重入复核、并发槽、实例身份、abort 归属的代价**。要跨过它就得删承诺。
3. **第二十版的结论需要限定**：它只对「契约不动」成立。**问题从来不是代码写得冗，而是契约把
   「怎么做到」写成了「必须做到」**——129 个叶子里约 100 条守的是实现事实。
4. **两个目标不能同时最大化**：`maxCCN 8` 与 `863 总行` 互斥（§3.2 第 1 条）。
5. **本版不构成对 ADR-25 的重开**：ADR-25 裁的是「不动契约的代码减法」，本版是**需求重新定基线**，
   影响面是 `统一刷新管理.md` §1/§3/§4、`DESIGN.md` 全文、ADR-08/19/20/21/23 的存废，
   只有确认人能授权。PoC 未提交、未替换 `src/`。

## 六、待裁决（三选一）

| 选项 | 内容 | 代价 |
|---|---|---|
| **甲：保持现契约**（第二十版结论） | 不动 `src/`，只把「链路↔契约」对照并入 `DESIGN.md` §2.1 | 行数不变；可读性靠文档 |
| **乙：换根契约**（本版） | 以 PoC 为基线重写 `src/`，同步改写规范 §1/§3/§4 与 DESIGN，关闭/重写 5 个 ADR，重铺 15 项门禁 | 1052 → 637 行、129 → 约 20 条验收；接入方需去掉 `pinia` 选项 |
| **丙：只删「怎么」不换契约** | 保留 129 叶子，但按 §四逐项裁决删掉派生条款（上限、诊断、分区、独立捕错） | 约 −175 行；每删一项都要同步删叶子与 ADR，且仍到不了 500 |

**PoC 尚未验证的部分**（若走乙，这些必须先补齐）：`vite` 构建与 `.d.ts` 产物、TS 4.9 消费方安装、
演示构建与 Playwright 浏览器场景、15 项文档门禁（模块清单/分层/§2.2 镜像/度量行）、
参数键与 `fast-json-stable-stringify` 的逐字节等价、SSR 路径、`validate` 违约与 `every` 改频率的用例。

**复现命令**

```sh
cd /home/uos/Documents/dsh/.tools/root-poc
./node_modules/.bin/tsc --noEmit                 # 0 错误
node --test tests/*.test.ts                      # 16/16
node /home/uos/Documents/dsh/统一刷新管理-历史-20260915/vue-refresh/scripts/complexity.mjs   # 863 / 637 / 110 / maxCCN 11
```
