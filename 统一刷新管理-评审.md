# 《统一刷新管理.md》评审报告（第十七版 · 对齐新方向：订阅 + 主动刷新的契约收敛）

> 视角：**对齐与收敛**。本版以 [`统一刷新-评审.md`](./统一刷新-评审.md)（基线提交
> `e61e7d8`，2026-09-16）确立、且用户已认可的新方向为主线，重写本评审文件的全部结论：
> 先简化需求语义，再简化实现。新方向的三项取舍是——取消首次／后续提交差异、
> 删除 query 独立执行与后台接管、改频率不再强制重取；主动与自动刷新共用同一条
> 获取与交付流程。
>
> 本版取代第十五版（过度设计专项）与第十六版（MVP/V1 产品简化审计）作为**当前有效
> 结论**；两版中与新方向冲突的建议按 §四的对账表处置（采纳／取代／后置／否决），
> 不静默丢弃。历史版本内容以 Git 历史为准，本文件不再叠放全文。
>
> **确认记录（2026-09-16，两轮分开记账）**：
>
> 1. **既往来**：新方向三项取舍（§2.2）已由 `统一刷新-评审.md` 文首决策记录认可，
>    §二据此标「用户已认可」。本轮无新增，不重复记账。
> 2. **本轮**：确认人对附录一陈述的需求逻辑与设计逻辑表示「**大部分都是我真实想要的**」。
>    按措辞这是**方向性认可，不是逐条确认**——确认人未点名哪些条目不在「大部分」之内。
>    因此附录一任何单条都**不声称被逐条认可**；存在超出方向认可的具体语义或实现
>    细节的条目，另行在「确认状态」列标注，最终以 §十一待裁决项
>    （接口命名、等待者载体、Q13 书面确认）落定。

## 一、版本沿革与本版效力

| 版本 | 视角 | 当前效力 |
|---|---|---|
| 第十四版 | 对齐第十三版裁决、撤回其错误 | 历史记录；其五条自我约束被本版 §十二继承 |
| 第十五版 | 过度设计专项（防御检查/调用链/分层/模块关系四维审计） | **分析层仍有效**：复杂度归因账（六需求合取）被本版 §三重算；其 B 档/C 档建议按 §四对账 |
| 第十六版 | 产品复杂度简化审计（MVP/V1、删 Pinia/配置三态/错误分类等） | **被本版整体取代**：其多数建议与「本轮不顺带变更」的新方向边界冲突，处置见 §四 |
| 第十七版（本版） | 对齐新方向：需求减法优先于代码整理 | 当前有效结论 |

**基线口径。** 本版引用两套数字并注明来源：第十五版逐文件实测（12 文件 / 1916 行 /
maxCCN=8 / 52 测试，确认人环境）；`统一刷新-评审.md` 的 `e61e7d8` 复测
（12 文件 / 1920 物理行 / 1100 代码行 / 133 分支 / maxCCN=8 / 51 测试通过、0 失败，
含一条「同身份替换请求在途重叠」的定向复现）。两套数字差异来自提交与统计口径，
不构成矛盾；本版的削减量估算以 `e61e7d8` 口径为基准。

**法律地位。** 旧 §5.1 的已关闭裁决（第 630/631/633/635 行）保留为历史依据，
但按 `统一刷新-评审.md` 文首的决策记录——「旧 §5.1 决策不能用它们阻止本次已认可的
需求重审；Pinia、安装接管等与本次减法无关的选择不顺带变更」——本轮重启范围严格
限于新方向三项取舍及其直接依赖，不夹带其他议题。

## 二、新方向的契约基线（用户已认可）

完整论证见 `统一刷新-评审.md` §0；本节只固化本版后续引用的四条产品规则与推荐默认值。

### 2.1 四条产品规则

1. **订阅**：声明数据身份与自动刷新间隔；同一身份共享请求和结果；相同参数再次声明
   幂等，不隐含强制刷新。
2. **刷新**：明确要求重新获取当前数据；主动刷新与自动刷新走同一条 `Source.load`、
   有效性检查与结果交付路径，不再「先 query 再 load」。
3. **暂停**：停止本页自动更新并保留画面；可见且存活的暂停页面仍能主动刷新；
   其他页面的共享订阅不因本页暂停被取消。
4. **正确性**：旧意图、失效订阅和已销毁实例不能发布；结果副本独立；
   取消不冒充真实执行结束。

### 2.2 三项取舍（消除的原因链）

| 旧规则 | 新规则 | 消除的概念链 |
|---|---|---|
| 首次 submit 普通加入，后续 submit 强制重取（U03） | 声明幂等；强制重取用显式刷新动作 | 不再按操作历史猜测意图 → 删 `firstOperation`、B01/M05.02 的特判 |
| query 独立执行、只写本页，完成后后台重新 load（U04–U06、附条 A、Q04） | 主动与自动刷新同一获取与共享交付流程 | 删 QueryRun、query.ts、QueryHost、commit、接管机制、「query 快照防回退」的 Delivery 三态 |
| 主动改 every 取消并立即重取（U11、T04） | 只重算下次到期，保留在途请求 | 不把调度参数变化解释成数据失效；按「上次结束＋新间隔」已到期则正常排队 |

### 2.3 推荐默认值（待写入正式需求，非已实现）

暂停刷新＝共享执行路径上的临时需求（动作结束仍保持暂停）；页面退出取消该页临时
等待与交付资格；**刷新与在途合并**（等待一个在刷新动作之后启动的请求，更早在途
不因点击反复 abort，结束后补一次；动作前的多个刷新合并为同一次后继请求）；**共享
限流**（主动刷新进同一队列与并发上限，不再承诺绕过后台满槽）；数据身份变化立即
成为当前意图；沿用「最后需求退出即清理」，不引入 TTL。

## 三、复杂度归因账的重算（承接第十五版）

第十五版结论：复杂度来自六个正交需求的合取。新方向直接砍掉其中两个维度、改造一个：

| # | 需求（第十五版编号） | 新方向下的处置 | 消除量级（估算，以 132 叶子为基） |
|---|---|---|---|
| 1 | 共享与去重（U02/U03） | 保留；U03 的首次差异删除 | 核心价值不变 |
| 2 | query 独立双通道（U04） | **删除**：并入单通道＋显式刷新 | Q 组 13 条中约 10 条语义改变或删除；D 组 4 条全删；A02/A04 改写；概念删 QueryRun/QueryRunner/commit/RefreshQueryContext ≈ −6 名词 |
| 3 | 失效重取与交付门槛（U05/U03） | **改造**：门槛从 Submission 三态转挂到「刷新等待者」，等一个动作后启动的代次 | F02/F03/F06/F15、B02/B14、M05/M08 等约 8 条改写或删除；DESIGN §3.6 十行更新表被「等待者＋代次」替代（新文档明确：仍需某种代次或门槛，不能承诺删掉全部版本状态） |
| 4 | 配置读取可失败且不猜值（U18） | **保留**（本轮不顺带变更） | 无变化；第十五版 C 档记账继续有效 |
| 5 | 通知自身失败可诊断（U19） | **保留**（本轮不顺带变更） | 无变化；B10 的 11 个变体存量不动 |
| 6 | 三重环境门（U25） | 保留；边沿组合减少（改频率边沿消失，T05 变体 03 删除） | 约 −2 变体 |

**重算结论**：新方向砍掉的是第十五版认定的「最贵单一正确性承诺」（需求 3 的门槛三态）
与整个双通道维度（需求 2）——正好是第十五版 C 档记账中重启收益最大的两项，但**以
需求重审而非接口修补的方式**实现（第十六版的「时间戳守卫」是接口层修补，见 §四）。
配置三态（第十五版认定的唯一「过度鲁棒」维度）本轮不动，记账继续挂账。

## 四、与第十五、十六版的对账

| 旧建议 | 出处 | 新方向下的处置 | 理由 |
|---|---|---|---|
| B1：`query` 的 runner 缺省 = `source.load` | 十五版 §7.2 | **失效（被取代）** | query 路径整体删除；有独立副作用的业务查询留在页面业务层（新方向 §0.2 末段） |
| 交付门槛 → 一行时间戳守卫 | 十六版 §3 | **被更优方案取代** | 新方向的「等待动作后启动的请求」代次语义更强：既防回退又定义合并规则；时间戳守卫在墙钟回拨时只能保守跳过，代次语义无此缺陷 |
| 删配置三态 → 降级停刷＋通知 | 十六版 §3 | **后置**（移入后续轮次） | 新方向明确「Pinia、三态配置、安装接管等与本次减法无关的选择不顺带变更」 |
| 删 Pinia 分区 → 内存 Map | 十六版 §3 | **后置** | 同上 |
| `submit` 判别联合 → boolean | 十六版 §3 | **部分吸收** | 新方向下 submit 是幂等声明，返回值可进一步简化；但接口命名与形状待 §十一问题 1 裁决后再定 |
| 错误四分类 → MVP 无分类 | 十六版 §3 | **部分吸收（自然推论）** | 单通道后 query/background 的 origin 区分失去载体，错误来源自然收敛为 validation/execution/configuration 三值；这是删双通道的免费收益，无需单独提案 |
| 模块 12→7、删三个窄端口 | 十六版 §3 | **降级为后置** | 新方向明确「按新的行为减少分支后再决定模块去留」；query.ts 将因路径删除自然消失（12→11），其余合并待主线完成后重估 |
| `maxConcurrent` 默认 4、onError 缺省 | 十六版 §3 | **保留为 V1 项** | 与新方向不冲突，但属接口形状问题，随 §十一问题 1 一并裁决 |
| 删 `nextIdentity` 耗尽分支 | 十六版 §3 | **撤回（被否决）** | `统一刷新-评审.md` §7.2 给出实证：专项测试覆盖三个计数器的 dispose 与取消路径，删除会改变失败与清理行为——第十六版此条依据不足，予以撤回 |
| B2：复杂度归因账入 §5.1 附录 | 十五版 §7.2 | **保留（已部分履行）** | 本版 §三即归因账的重算；正式并入 §5.1 仍待确认人批准（§十一问题 5） |
| A 档为空（无行为中性减法） | 十五版 §7.1 | **仍成立** | 行为中性的减法空间在旧契约下已耗尽；新方向的行为减法全部是契约变更 |

**对第十六版的总体评价**：其审计框架（复杂度红线、前后对比、验证计划）被本版继承；
其「先删接口形状（Pinia/配置/返回值）」的次序被否——新方向证明**先删行为语义
（双通道、门槛、强制重取）才是主刀**，接口形状的简化大部分会随之自然发生或失去必要性。

## 五、P1 正确性口径的处置（承接新方向 §2.1）

`统一刷新-评审.md` 的 P1 发现：契约写「同一身份至多一份在途后台请求」，实现只保证
「同一生存期至多一个有效 Task」；同参显式重提会让旧执行（已 abort、未结束）与新执行
短暂并行（定向复现：`calls=2, oldAborted=true, running=2, resources=1`）。处置：

1. **最常见的重叠场景被新方向直接消解**：同 key 刷新不再 abort 在途请求（在途合并
   规则），而是等待其结束后补一次——「显式重提导致的并行」这个复现序列在新契约下
   不再存在。
2. **剩余两个场景写入契约措辞**，按新方向 §2.1 的三句话口径：
   - 参数切换 A→B 是不同身份，跨身份并行本来就不是「一份在途」约束的对象；
   - 同 Source＋key 的删除重建（P07 场景）仍可能物理重叠，这是已知边界——若要严格
     禁止必须等旧执行结束（新增调度约束、复杂度上升），本轮不禁止，只在契约中把
     「一份在途」改写为「同一生存期至多一个有效任务；普通轮询不重叠；重建后的新
     生存期不等旧执行」。此措辞修订属契约变更，列入 §十落地顺序第 2 步。
3. **不采用**「为满足物理并发 ≤1 而在启动阶段等待同 key 旧执行」的方案（新方向已
   论证其代价：阻塞队首是否放行其他身份、旧请求永不结束时的进展边界都要新定义）。

## 六、简化策略（对齐新方向）

| 策略 | 具体动作 | 降低的复杂度 | 对核心价值影响 | 执行时机 |
|---|---|---|---|---|
| **删除** | 首次/后续提交差异（`firstOperation`、U03 特判） | −1 行为规则、−B01/M05.02 特判 | 无损：幂等声明更可预测 | 主线第 1 步（需求修订） |
| **删除** | QueryRun 独立执行路径、`query.ts`、QueryHost 端口、commit、`closeQuery` 关闭边沿 | −1 文件、−1 端口、−1 活动分支、−Q/D 组大部分验收、−A04/B16a | 高级能力外移业务层；「暂停仍可单查」由 refresh 承接 | 主线第 3 步 |
| **删除** | query 后台接管（附条 A）与「query 快照防回退」的 Submission.delivery 三态 | −DESIGN §3.6 十行更新表、−F/B 门槛场景约 8 条 | 门槛语义转挂刷新等待者，防回退不放松 | 主线第 3 步 |
| **删除** | 改频率强制重取（T04 的替换语义） | −1 触发链、−T05.03 变体 | 无损：到期重算已覆盖缩短后已到期的场景 | 主线第 1 步 |
| **合并** | 主动刷新进入共享队列与并发上限 | −「绕过满槽」的独立通道及其验收（Q13 反转） | 满槽时 refresh 需排队——语义变化，见 §九风险 | 主线第 1 步 |
| **默认** | 刷新与在途合并规则（动作前合并、动作后要求更晚一次、失败结算本次等待） | 替代原门槛三态的全部转换规则 | 明确可解释的请求时序 | 主线第 1 步（写入需求） |
| **后置** | Pinia 去留、配置三态、submit 返回形状、模块合并 | 本轮不动 | — | 后续轮次 |
| **标准化** | 契约措辞修正（§五第 2 条）与 `统一刷新-评审.md` §2.2 列出的五处说明纠错（flush 时钟、285 年数量级、字段名、Task/Clock 注释、分层描述） | 文档可信度 | 无行为变化 | 可先行（P2/P3） |

## 七、简化后方案

### 7.1 目标 API（候选形状，命名待 §十一问题 1 裁决）

```ts
const quote = defineRefresh<QuoteParams, Quote>({
  validate?: p => ...,                 // 可选，语义不变
  load: (p, { signal }) => quoteService.read(p, signal),
})
const refresh = createRefreshManager({ pinia, maxConcurrent })   // 本轮形状不变
app.use(refresh)

const task = useRefresh(quote, { enabled, every, onError? })
task.declare(args)        // 候选名 declare/submit：幂等声明订阅（同参重复声明无副作用）
await task.refresh(args?) // 显式刷新：进共享队列；等待「本动作之后启动」的请求；
                          // 不传 args 时复用当前声明参数
task.display              // 唯一交付面：args/data/origin/updatedAt（不变）
```

页面业务查询（独立副作用、只更新本页、必须绕过共享限流）留在页面业务层，
页面自行负责取消与过期副作用保护（`统一刷新-评审.md` §0.2 末段）。

### 7.2 用户主路径

`useRefresh → declare → 读 display`；唯一分支是 `refresh`，且它复用订阅的全部机制
（参数、身份、交付），不引入第二套执行语义。同步/微任务/异步完成三阶段结构不变
（新方向 §3.1 的度量方法），但「query 成功 → 交付本页 → 后台接管 → 再请求」的链路
消失——单次主动刷新从「两次请求」变为「一次请求」。

### 7.3 状态与规则（承接新方向 §6.1）

**可删除**：`firstOperation`；QueryRun 及其活动分支（activity 收敛为
`Subscription | null`）；query 接管专用分支；`closeQuery` 关闭边沿（查询路径消失后
失去存在理由）；仅为区分「暂停中 query」的边沿状态。
**可收敛**：Submission.delivery 三态 → 刷新等待者集合（多个等待者可等同一任务代次，
结束或失效后释放；用任务引用还是代次数字，须验证合并、替换与资源重建三场景——
§十一问题 3）。
**不能删**（新方向 §6.1 的七项独立性判断全部保留）：页面当前意图、订阅资格、
共享实例身份、当前有效任务与未结束执行的区别、lastSettledAt、updatedAt、页面独立快照；
operationId/resourceId/taskVersion 三个时间域不强行合并。

### 7.4 功能范围与配置

保留：共享去重、可见性/生命周期暂停、退出清理与 abort、不重叠不补跑、失败保留画面
续轮询、整体发布＋updatedAt、参数身份与副本独立。
删除：双通道、接管、首次差异、改频率重取、commit。
本轮不动：Pinia 分区、配置三态、readSnapshot、visible 选项、安装接管。
配置项数量不变（enabled/every/onError/maxConcurrent/pinia/load/validate/visible），
但**语义负担下降**：无 runner、无 commit、无 SubmitResult 三变体的心智模型。

## 八、前后对比

| 指标 | 当前（`e61e7d8`） | 新方向目标 | 备注 |
|---|---|---|---|
| 必学概念（名词） | 38 | **≈ 28**（删 QueryRun/QueryRunner/commit/QueryContext/失效重取/交付门槛/接管/首次加入等 ≈ 10 个） | 估算；Pinia/配置三态相关名词保留 |
| 运行时状态域 | 10 | **7**（删 QueryRun 分支、Delivery 三态、firstOperation；增等待者集合为替代） | 等待者归属待 §十一问题 3 |
| 验收叶子 | 132 | **≈ 85–90**（Q 组约 10 条、D 组 4 条、F/B 门槛约 8 条删除或改写） | 估算；配置/通知相关叶子不动 |
| 主动刷新请求次数（单次操作） | 2（query＋接管 load） | **1** | 新方向 §0.4 链路直接推论 |
| 公开入口/方法 | 9 | **8**（删 query；submit→declare 改名不增） | runner 从公开面消失 |
| 结果类型变体 | Submit 3 + Query 3×origin 4 + cancel 3 | Submit 简化 + Query 类删除；错误 origin 4→3（background 并入 execution，单通道的自然收敛） | 错误收敛采纳与否见 §十一问题 4 |
| 技术模块 | 12 文件 | **11**（query.ts 随路径删除） | 其余合并后置 |
| 概念上的调度往返 | Manager↔Scheduler 6+5 | 6+5（不变） | 新方向 §3.3 已裁决不再为「纯执行器」重构 |
| 工期 | 已实现 | **需求修订 3–5 天；实现与测试改造 2–3 周**（含三个示例页迁移与 Q13 语义反转的回归重写） | 估算 |

## 九、取舍与风险

| 损失/变化 | 严重度 | 缓解 / 回滚 |
|---|---|---|
| 满槽时 `refresh` 需排队（Q13「主动查询绕过后台满槽」反转） | 中（语义变化） | 新方向已把它列为有意修改；UX 上等待者拿到的是动作后启动的请求，语义可解释；若某页面确需绕过，属页面业务层自管请求 |
| B09 场景消失（runner 内开启意愿的时序依赖） | 低 | 该场景本身就是旧双通道的人造竞态；示例页核对后若无真实需求即删 |
| 三个代表页迁移成本：查询列表页「暂停仍可单查」改走共享路径、runner 副作用外移 | 中 | 主线第 1 步先核对 `examples/pages/` 调用点（新方向 §5 末段）；发现不兼容的真实需求时报告具体冲突，不静默强行迁移 |
| 等待者合并规则的边界（动作前/执行中/结束后三态、失败结算） | 中 | 主线第 1 步写入需求时逐条定义；§十场景清单全覆盖 |
| 同 key 删除重建的物理重叠继续存在 | 低（已知边界） | §五第 2 条契约措辞修正；严格禁止的代价已论证不划算 |
| 大量验收与测试重写（52 条中估计 15–20 条改写） | 成本，非风险 | 保留故障注入子集（身份复核/槽位释放/深复制）作为不退化底线；未改变的正确性测试全部保留 |
| 回滚路径 | — | 现实现已打包（tgz + SHA-256）；新契约以 major 版本并行发布，examples 双跑对照，每项取舍独立可逆 |

## 十、验证与落地顺序（承接新方向 §8）

| 顺序 | 事项 | 完成条件 |
|---|---|---|
| 0（可先行） | P2/P3 文字纠错：五处说明修正（§六标准化行）、契约「一份在途」措辞按 §五改写 | 门禁与文档一致；无行为变化 |
| 1 | 以 §二为主线修订正式需求，核对三个代表页调用点 | 订阅/刷新/暂停/退出四规则成文；废止 query 接管/首次差异/改频率重取；在途合并与限流语义落实；P1 口径写准 |
| 2 | 根据行为变化调整设计与验收 | 一条获取交付链路；可删状态与仍需事实列清；等待者语义替代 Delivery 三态 |
| 3 | 实现统一获取路径，移除旧查询专用机制 | 普通订阅与主动刷新共用 load 和交付；暂停不意外恢复轮询；业务副作用迁到调用方；query.ts/QueryHost/closeQuery 删除 |
| 4 | 回归未改变的正确性，验证新行为 | 下述场景全绿；请求次数、交付对象、取消、真实槽位、实际进展均有断言，不只看文件变少 |

**必须覆盖的场景**（承接新方向 §8 清单）：两页同参订阅只首查一次；重复声明不强制
重取；显式刷新不再触发 query→load 双请求；暂停后刷新本页更新但不恢复轮询、其他有效
订阅更新、其他暂停页不更新；改频率不 abort 当前请求、缩短后已到期能正常执行；
刷新发生在请求启动前/执行中/结束后三态的合并与结算；参数 A→B 后 A 晚到；最后需求
退出后同 key 重建；槽满、忽略 abort、等待者取消、页面退出与 dispose；物理槽只随真实
结束释放；Store/首个页面通知同步重入后各接收者复核有效性；各页面与共享结果无
可变别名。

## 十一、待确认问题与下一步

1. **接口命名**：`declare`/`submit` 保留哪个名字？`refresh(args?)` 的参数可省略
   形态是否采纳？（决定 §7.1 候选形状定稿）
2. **Q13 反转的最终确认**：满槽时 refresh 排队、不再绕过——业务方是否接受？
   （新方向已把它列为推荐默认值，此处只差书面确认；即附录二 RQ6）
3. **等待者的实现载体**：任务引用还是代次数字？（须同时验证合并、替换与资源重建
   三场景；新方向 §6.1 留白；即附录二 RQ 联动项，见 B.4 代价说明）
4. **错误 origin 的自然收敛**（background 并入 execution）是否随主线一并落地，
   还是留到 V1？（本版建议随主线：它是删双通道的免费收益，单独保留 background
   反而要人为区分来源）
5. **归因账并入 §5.1 附录**（十五版 B2 遗留）是否批准？
6. **附录二 RQ1–RQ5 的裁决**（校验失败 no-op、refresh 带参切换声明、enabled=false
   不取消等待者、合格结果判定、refresh 交付范围）——附录二 B.2 已给建议值，
   需求修订（§十第 1 步）前必须逐条落定。

**下一步**：问题 1–3 与 6（RQ1–RQ5）确认后进入 §十第 1 步（需求修订）；第 0 步
（文字纠错）无需等待，可立即执行。

## 十二、本版自我约束（延续第十四版）

1. 本版重启范围严格限于 §二三项取舍及其直接依赖；Pinia、配置三态、安装接管、
   readSnapshot、visible、模块合并一律不顺带变更，相关建议只记账（§四「后置」）。
2. 数字来源：`e61e7d8` 复测引用 `统一刷新-评审.md` §1；第十五版口径注明出处；
   削减量为估算并逐处标注「估算」，不冒充实测。
3. 本环境未运行测试与类型检查；测试状态引用 `统一刷新-评审.md` 的复测记录
   （51 通过、0 失败）与确认人环境记录，不外推。
4. 行为语义变更（Q13 反转、D 组删除、T04 改写）均显式标注「语义变化」，
   不伪装为等价重构；历史测试不默认全部保留，按 §十第 4 步区分处理。
5. 第十六版中被实证否决的建议（`nextIdentity` 耗尽分支删除）公开撤回，
   不以改名方式重提。

---

## 附录一：需求逻辑与设计逻辑（第一性陈述；方向已获认可，非逐条确认）

> 本附录是本版的**论证基座**：§二的四条产品规则、§7.3 的状态收敛与 §十的落地顺序，
> 全部由这里的逻辑推导。确认人已于 2026-09-16 表示「大部分都是我真实想要的」——
> 据文首确认记录第 2 条，这是**方向性认可，不是逐条确认**：下表任何单条都不单独
> 声称被点名认可，「确认状态」列的默认含义是「含于方向认可」，**只有存在超出方向
> 的具体待决语义或实现细节时才另行标注**，与 §二正文互为表里。

### A.1 需求逻辑：需求只能来自用户意图，不能来自历史事实

框架的用户（业务前端开发者）从头到尾只有**三个意图**：

1. **我要这份数据，持续更新**（订阅：声明身份 + 间隔）
2. **现在再要一次**（刷新：一次显式取数）
3. **别再更新这页了**（暂停：停自动、留画面）

由此推出四条需求逻辑，违反任何一条就会产生假需求：

| # | 逻辑 | 内容 | 确认状态 |
|---|---|---|---|
| L1 | **一个意图一个动作，动作幂等** | 相同参数再次声明订阅 = 没发生任何事。旧契约「首次 submit 加入、后续 submit 强制重取」让同一动作的语义取决于调用历史——**历史不是意图**。要新数据就说「刷新」，一个独立的、显式的词 | 方向认可（对应 §2.2 取舍一） |
| L2 | **取数只有一条路径** | 「定时到点要一次」和「用户点了按钮要一次」本质相同。旧契约的 query 独立执行 + 后台接管是用两个执行域服务一个意图，QueryRun、交付门槛、版本域、接管机制全是缝合人为裂缝的补丁。**裂缝不该缝合，该消除**：刷新动作只是往共享执行路径里塞一个临时等待者 | 方向认可（对应 §2.2 取舍二、§7.3 等待者） |
| L3 | **配置变化是输入，不是事件** | 改 `every`、改 `visible` 只改变「下次什么时候要」的计算输入。旧契约「改频率 → 取消当前请求并重取」把调度参数变化解释成数据失效——**调度参数和数据新鲜度是两个无关的域**。改频率后只需重算到期时间；缩短到已到期就正常排队，不 abort 任何人 | 方向认可（对应 §2.2 取舍三） |
| L4 | **正确性是资格线，不是可选项** | 旧意图的结果不能覆盖新意图（晚到隔离）；共享结果与页面快照无可变别名（副本独立）；取消不等于执行结束（物理槽位只随真实结束释放）。这三条不是「需求」，是框架存在的资格——做不到它们，共享与调度全是错的 | 方向认可（对应 §2.1 规则 4，全部保留） |

**边界**：框架只做「取数与交付的共享、调度、取消」。业务副作用（写业务 Store、
保存后处理、绕过限流的特殊请求）永远留在页面。**框架不替业务做决定，只替业务做
重复劳动。**

### A.2 设计逻辑：从意图推导一切，每个状态必须回答一个用户问题

| # | 逻辑 | 内容 | 确认状态 |
|---|---|---|---|
| D1 | **状态只存两类事实，派生值现算** | 用户事实（参数快照、间隔、开关、可见性）与运行事实（订阅关系、在途任务、等待者、上次结束时间、结果产生时间）；资格、最短间隔、任务位置全部现算。**每保存一个状态都要能回答「哪个用户问题需要记住它」；答不上来的是缓存，缓存就会失同步** | 方向认可；与旧实现「派生值不保存」原则一致 |
| D2 | **单通道，Timer 与刷新按钮汇入同一条队列** | 订阅是唯一长期形态；刷新 = 该页面临时等待者（动作结束仍保持暂停）；到期 = Timer 产生的需求。同一 FIFO、同一并发上限、同一 load、同一套交付检查。**满槽时刷新排队不是缺陷，是共享语义的诚实表达** | 方向认可；但「满槽排队不是缺陷」一句为**本版立场**、不属已认可方向（共享限流在 `统一刷新-评审.md` §0.3 仅为推荐默认值），Q13 反转待 §十一问题 2 书面确认 |
| D3 | **身份三域分离，只做一件事** | 页面操作（意图代次）、资源生存期（共享范围）、任务代次（新鲜度）是三个时间尺度，不合并；唯一用途是回答「这个晚到的结果还能不能发布」。复核谓词收敛一处，每个外部效果之后重问一次——这是单线程同步重入下的正确性纪律，不是防御过度 | 方向认可 |
| D4 | **等待者集合替代门槛三态** | 「这次刷新要求动作之后启动的请求」表达为：等待者挂在任务代次上，多个等待者可共享一代次，任务被替换/资源重建/页面退出时等待者失效或顺延，结束时统一结算。**不需要「尚欠/已登记/无要求」三态——等待者的存在本身就是要求，不存在就是无要求。状态即事实，不做状态机** | 方向认可；实现载体（任务引用 vs 代次数字）待 §十一问题 3 |
| D5 | **失败不改已定结果** | 动作失败只结算 + 通知，是否继续由调用方 `enabled` 决定，框架绝不代写；条目暂不可得保留画面下周期继续；框架自身失败只进诊断。三类失败互不替代，**都不触碰已交付的画面** | 方向认可；与旧契约 §3 三分法一致，无需变更 |
| D6 | **调度与编排按「何时」与「谁」切分** | 调度器独占 Timer/队列/槽位，只答「何时、按什么顺序」；编排层独占关系与交付，只答「谁该被登记、结果给谁」。到期判断 = 上次正常结束 + 当前最短间隔，纯函数。满槽不自旋、取消不放槽、一轮 flush 读一次时钟——算法纪律，不是风格 | 方向认可；与旧实现 Scheduler 职责一致，本轮不动 |

### A.3 检验标准：用三个问题问任何一条需求逻辑

1. **删除测试**：删掉这条规则后，哪个真实用户场景会坏？答不出具体场景的规则是假需求
   （旧契约的「首次差异」「改频率重取」都答不出）。
2. **新人测试**：新人能否在不知道调用历史的情况下预测动作结果？
   （幂等声明 + 显式刷新 = 可预测；「第 N 次 submit」= 不可预测。）
3. **裂缝测试**：两个机制之间是否存在「缝合层」（接管、门槛转换表、状态迁移表）？
   **缝合层越厚，说明最初的概念切分越错。** 正确的切分让机制直接对接，没有转换层可写。

按这三个问题，新方向（幂等声明 + 显式刷新 + 单通道 + 等待者）是当前形态的最优解：
每条规则都能指出一个会坏的具体场景，每个动作都可预测，机制之间没有缝合层。
本轮不采纳的建议（Pinia 去留、配置三态、模块合并）同样用这三问检验后挂账，
待出现新证据再议。

---

## 附录二：需求规格与数据结构分析（应确认人「需求更清晰 + 分析数据结构问题」指令）

> 本附录是 §二（契约基线）与 §7.3（状态收敛）之间的缺环：**把方向落成可判定的动作
> 规格**（B.1–B.2），并对照新需求逐对象分析当前数据结构的结构性问题（B.3）与目标
> 形态（B.4）。确认状态：应确认人指令撰写（2026-09-16），**尚未逐条确认**——
> B.2 的 RQ1–RQ6 六个需求决定当前无答案，全部待裁决；B.4 的不变量是 D4 的具体化，
> 实现载体仍关联 §十一问题 3。

### B.1 术语先行（没有术语就没有清晰需求）

| 术语 | 定义 | 判定方式 |
|---|---|---|
| 数据身份 | Source 对象 + 完整参数值的稳定键 | 纯函数，无状态 |
| 有效订阅 | 存活 ∧ 已声明 ∧ 环境允许（激活∧可见）∧ 配置开启 | 派生，现算 |
| 合格结果 | StoreEntry 未过期：`lastSettledAt + 最短every > now` | 派生，现算 |
| 动作后请求 | 在本动作时间点之后**登记**（非启动）的任务 | 由任务代次判定，见 B.4 |

### B.2 三个动作的精确语义（可判定版）

**declare(args) —— 幂等声明**

| 输入情形 | 结果 | 状态变化 |
|---|---|---|
| 校验失败 | rejected | **无变化**（建议，见 RQ1） |
| 同身份重复声明 | accepted | **无任何变化**（不请求、不重置周期） |
| 新身份（已有合格结果） | accepted | 切换意图；交付现有合格结果，不强制新请求 |
| 新身份（无合格结果） | accepted | 切换意图；等待即将到来的首查 |

**refresh(args?) —— 显式刷新**

| 输入情形 | 结果 | 状态变化 |
|---|---|---|
| 无声明且未传 args | rejected | 无变化 |
| 传 args（建议） | 先按 declare 规则切换声明，再刷新 | 见 RQ2 |
| 当前无在途/排队任务 | 建立等待者；立即登记一个任务 | 本页获得临时交付资格 |
| 在途任务**登记于**本动作之前 | **不 abort**；等待者要求「下一个」任务；当前任务结束后自动补一次 | 合并的关键 |
| 排队任务尚未启动 | 多个等待者合并到同一个后继任务 | 免费的合并 |
| 本页暂停（enabled=false）但激活且可见 | 允许；本页获得临时交付资格，动作结束仍保持暂停 | 见 RQ3 |
| 本页隐藏/失活/卸载 | 等待者取消，不影响其他页面的共享任务 | 已明确 |

**refresh 的结算**：所等待的任务成功 → 等待者成功（本页已收到交付）；失败 → 等待者以
error 结算，画面不变；被取消（页面退出/销毁）→ cancelled。

**六个待裁决的需求决定（当前文档无答案）：**

| # | 问题 | 本版建议 | 理由 |
|---|---|---|---|
| RQ1 | 校验失败的 declare 改不改状态？ | **完全 no-op**（旧 P08 是退出旧订阅+清参数，与新方向「声明是事实陈述」矛盾） | 声明式语义下无效输入不该有副作用 |
| RQ2 | refresh(args) 带参时是否切换声明？ | 是，refresh = declare + 刷新，一个动作 | 避免「先 declare 再 refresh」两步缝隙里的竞态 |
| RQ3 | enabled=false 取消进行中的等待者吗？ | **不取消**（`统一刷新-评审.md` §0.3 只列了隐藏/失活/卸载取消） | 显式动作是用户意志；enabled 只管自动更新 |
| RQ4 | declare 加入时交付已有结果的「合格」标准？ | 未过期（按最短 every 判定），过期则等即将到来的请求 | 与 F01 一致 |
| RQ5 | refresh 的结果只给本页还是全部订阅者？ | 全部有效订阅者 + 本页（临时资格）——它是共享交付 | 单通道的直接推论，需书面确认 |
| RQ6 | 满槽时 refresh 排队（Q13 反转） | 确认（已挂 §十一问题 2） | — |

### B.3 当前数据结构的结构性问题（对照新需求逐对象分析）

| # | 对象/字段 | 问题 | 症状证据 | 新方向下的修正 |
|---|---|---|---|---|
| **S1** | `Submission.delivery` 三态 | **挂在错误的对象上**：刷新要求是「每次动作」的短期事实，却被存在「每次声明」的长期对象上 | DESIGN §3.6 十行更新表、F15 的「旧 id 门槛清理」、`setRequirement` 审计点、B14 门槛绑定生存期——全部是在给错位状态打补丁 | Submission 只剩 `{parameters}`；新鲜度要求下放到等待者 |
| **S2** | `Activity = QueryRun \| Subscription` 互斥并集 | **混合了两种生命周期**：Subscription 是关系（长）、QueryRun 是执行（短）。互斥迫使系统跟踪边沿 | `closeQuery` 关闭边沿、`lastEnabled` 边沿历史（true→未知→false，F10）、M02 不变量、「暂停中 query vs 关闭时 query」的区分 | 订阅（关系）与等待者（等待）**正交共存**：activity 收敛为 `Subscription \| null`，waiters 独立集合 |
| **S3** | `DeliveryBarrier.resourceId` 字符串 | 用序列字符串做生存期绑定，需要专门规则判「门槛属于其他 id」 | F15、`barrier.resourceId !== resource.id` 的清理分支 | 等待者直接持有 Resource **对象引用**，失效判定退化为一次身份比较 |
| **S4** | `Resource.nextVersion` | 一域两用：任务身份 + 新鲜度标记。功能上没错，但从未声明 | `Task.version` 与 `barrier.minVersion` 同域比较的隐含前提 | **保留**，但明示为等待者机制的基座（见 B.4） |
| **S5** | `Subscription.every` 可变字段 | 可变性只为「改频率即替换任务」服务（`synchronize` 里 `captured.every = input.every` + `enqueueTask`） | T04 替换链 | 新方向改频率永不触碰任务；字段保留但变为纯数据（读现算亦可） |
| **S6** | `Handle` 的字段分组 | 同一对象混装三类事实：声明（submission）、关系（activity）、边沿（lifecycleActive + 适配层 lastEnabled/reported）；`operationId` 兼作「当前意图」与「query 有效性」两个判据 | `currentOperation` / `currentQuery` 两个谓词、`beginOperation` 的三重写 | 重组为三件事：`declaration` / `subscription` / `waiters`；operationId 只保留「意图代次」职责 |
| **S7** | 适配层 `lastEnabled` / `reported` | 存在理由大半是保护「暂停期间的单次查询」不被误杀——单通道后等待者有显式取消语义，这套边沿历史失去主要用途 | F10 两个变体、B08 | 本轮不动（配置三态冻结），**记账：等待者落地后此项可重估** |
| **S8** | `StoreEntry.version` | 唯一消费者是门槛判断；等待者机制下仍需要（判定交付是否满足等待者） | `deliverable` 的 `entry.version < minVersion` | 保留 |

**核心结论**：当前数据结构的全部结构性问题同源——**S1/S2/S3 是同一个错误的三种表现**
（把「动作级」状态挂到「声明级」对象上、把「等待」建模成互斥活动、用值域身份代替
对象身份）。修正它们不需要新机制，只需要把状态放到正确的宿主上。

### B.4 目标数据结构：一个不变量替代整套门槛机制

```ts
Handle {
  source, readInput, publish, onError, cleanup, disposed, lifecycleActive,
  operationId,                        // 意图代次（只判「晚到结果能否发布」）
  declaration: { parameters } | null, // 声明：只有参数，无任何交付状态
  subscription: Subscription | null,  // 关系：与 waiters 正交共存
  waiters: Set<Waiter>,               // 等待：每次 refresh 一个
}
Waiter { minVersion: number, settle(result) }   // settle 由 refresh 的 Promise 提供
Resource / Task / StoreEntry / Scheduler        // 不变
```

**唯一的新不变量**（替代 DESIGN §3.6 十行更新表）：

> 等待者在动作时刻记录 `minVersion = resource.nextVersion + 1`；
> **存在未满足等待者的 Resource 必有 `version ≥ max(waiters.minVersion)` 的任务**
> （没有就登记一个）；任务结束时，所有 `minVersion ≤ task.version` 的等待者按该任务
> 结局结算。

它如何自动覆盖旧机制逐条维护的场景：

| 旧场景（原验收） | 新不变量的自然结果 |
|---|---|
| Q06 相同参数再次查询不复用在途 | 旧任务 `version < minVersion` → 等待者要求下一个 |
| D01 失活恢复强制一次 | 恢复时资源若无满足等待者的任务 → 不变量强制登记 |
| F02/B02 query 快照不被旧分区回退 | 等待者未结算前，旧 entry 的 `version < minVersion` 不交付本页 |
| F15 同 key 重建的旧门槛清理 | 等待者持对象引用，旧 Resource 失效即等待者失效，无需 id 比对 |
| 多次刷新合并 | 排队中的任务 `version ≥` 所有等待者的 `minVersion` → 一个后继任务服务全部 |
| 「已开始后再刷新才要求更晚一次」 | 已启动任务不满足新等待者的 `minVersion` → 自动要求 follow-up |

**代价说明**：`minVersion = nextVersion + 1` 依赖「版本在登记时分配」这一现行事实
（`enqueueTask` 里 `nextIdentity(resource.nextVersion)`），属 S4 声明的隐含前提的
显式化；资源删除重建时等待者随对象引用一起失效，重建后的新资源不受旧等待者约束
（与 F15 语义一致）。该不变量是 D4（等待者集合替代门槛三态）的具体化，
与 §十一问题 3（任务引用 vs 代次数字）的裁决联动：若选任务引用，`minVersion`
被替换为「等待者记录动作时刻的当前任务引用，其登记后继才满足」，不变量其余部分不变。
