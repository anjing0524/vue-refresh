# 统一刷新管理（vue-refresh）修复文档

> 本文档是审查报告的落地件：把已确认的问题拆成可独立执行、可独立验证、可独立回滚的修复项。
> 基线：工作树 `main`（git clean）、`src/` 7 文件 814 行、`tests/` 66 个用例、`pnpm typecheck` 0 错误。
> 编写时间：2026-09-21。

---

## 0. 本文档怎么用

### 0.1 范围

- **做什么**：修代码缺陷、补齐被漏掉的门禁断言、订正与实现相反的文档、收敛结构耦合。
- **不做什么**：不改已裁决的产品边界（ADR 已定的口径），不擅自改需求条文。凡是需要改需求/口径的，一律进 [§4 需求侧修改提案](#4-需求侧修改提案需确认人逐条裁决)，由确认人裁决后再落地。
- **不改的部分**在 [§5](#5-明确不改的部分及理由) 单独列出，避免重复返工。

### 0.2 对上一版审查报告的两处更正

> 这两处是复核后的自我更正，**不要按上一版报告的描述去改**。

| 编号 | 上一版说法 | 更正后 | 影响 |
|---|---|---|---|
| 更正-1 | 「`store.ts → core.ts` 是反向/向上依赖，被门禁漏掉」 | **不是门禁违规**。按 `scripts/check-docs.mjs:184` 的 `LAYERS`（`core.ts=3`、`store.ts=4`），`store→core` 是**向下**边，与 README:22 的「依赖方向单向」一致，门禁本来就放行。真正的问题是**概念耦合**：结果表这个数据结构必须依赖「协调者」模块才能拿到身份编码 | FIX-09 的定性由「修违规」降为「去耦合」，优先级 P1，理由改写 |
| 更正-2 | 「`benchmark.mjs` 把每次发布都计成一次失败（`failures` 虚高）」 | **不会虚高，而是恒为 0**。getter 恒返回 `undefined`，Vue 的 `watch` 用 `Object.is` 判等，新旧值相同 ⇒ **回调永不触发**。它是一个**恒定假值的死指标**（脚本 `:148` 照样打印「后台失败 0」） | FIX-01 的验证方式改为「反例注入后计数必须变动」 |

### 0.3 优先级定义

| 级别 | 判定标准 | 允许的落地时机 |
|---|---|---|
| **P0** | 产生错误证据／遗漏真实风险／文档与实现语义相反 | 立即可执行，不需要新 ADR（只改字面与门禁） |
| **P1** | 结构耦合明确、收益可量化、不引入新模块与新公共 API | 一批做完、一次性过全部门禁 |
| **P2** | 命名/拆分/公共 API 变更，代价与收益都真实存在，**由确认人决定做不做** | 逐项裁决后单独成批 |

### 0.4 执行批次总览

| 批次 | 修复项 | 触碰文件 | 是否需裁决 |
|---|---|---|---|
| **第一批** | FIX-01 / 02(a–d) / 03 / 04 / 05 / 06 / 07 | `scripts/benchmark.mjs`、`scripts/check-docs.mjs`、`tsconfig.json`、`DESIGN.md`、`README.md`、`tests/core.test.ts`（+需求 1 处待裁决） | 仅 FIX-07 需 |
| **第二批** | FIX-09 / 10 / 11 / 12 / 13 | `src/source.ts`、`src/store.ts`、`src/core.ts`、`src/vue.ts`、`package.json` | 否 |
| **第三批** | FIX-14 … FIX-21 | 跨代码与文档的命名/拆分 | 是（逐项） |

> 编号连续但不含 FIX-08（该位在拆分时并入 FIX-06，保留空号以免已发出的引用失配）。

### 0.5 每一批都必须过的三门（现状期望值）

```sh
pnpm typecheck   # 期望：0 错误
pnpm test        # 期望：66 个用例全绿（core 41 + vue 25）
pnpm check:docs  # 期望：consistent 一行 + 19 个叶子 traced
```

任何一项数字变化（用例总数、源文件数、行数）都要同步 README:145 / README:149 与相关的 `N 个用例` 记载，否则门禁的 check #16 会把这一批打红。**先跑一遍 `pnpm complexity` 拿基线**（当前：7 文件 / 814 行 / 80 分支 / maxCCN 7）。

---

## 1. 第一批（P0）：证据有效性与文档订正

### FIX-01 `scripts/benchmark.mjs` 的失败计数器是死指标

- **问题**：`:87` 监视 `task.display.value?.failedAt`。`RefreshDisplay` 自 ADR-77 起只有 `args`／`data`／`updatedAt`（`src/public-types.ts:17-24`），该 getter 恒为 `undefined`；Vue 的 `watch` 比较新旧值时判等为真 ⇒ 回调**永不触发**，`failures` 永远是 `0`，而脚本 `:148` 照样把它打印成「后台失败 0」。
- **位置**：`scripts/benchmark.mjs:87`（消费点 `:123`、打印点 `:148`）
- **根因**：失败在 ADR-77 从 `display` 移到独立出口 `failure`，`.mjs` 不在 `tsc` 覆盖范围（`tsconfig.json:18` 的 `include` 不含 `scripts`），改动后无人发现。
- **修复动作**：

```js
// scripts/benchmark.mjs:86-87
// 旧
// 失败不再回调推送：交付面出现新的失败时刻时计一次（这是原输出字段 `failures` 的来源）。
watch(() => task.display.value?.failedAt, failedAt => { if (failedAt !== null) failures += 1 })
// 新
// 失败不再回调推送：失败出口出现新的失败时刻时计一次（ADR-77 把失败移出 display）。
watch(() => task.failure.value?.failedAt, failedAt => { if (typeof failedAt === 'number') failures += 1 })
```

- **验证（必须做反例）**：把 `http.post` 临时改成对偶数身份 `reject`，重跑 `node scripts/benchmark.mjs --duration 500 --runs 1`，`后台失败` 必须 > 0；改回后必须回到 0。**只跑一次看到 `0` 不算验证**（旧代码也是 0）。
- **风险/回滚**：单文件、无外部契约。回滚 = 还原 2 行。
- **同批顺带**：`README:150` 记录的是 benchmark 的实测数字，修完本条后需重跑一次并按实测更新（若数字有变）。

### FIX-02 `check-docs.mjs` 补齐三类被漏掉的漂移检测

> 三条新检查接在现有 `// 17) Retired vocabulary` 之后、`if (problems.length)` 之前，编号 #18／#19／#20，并在结尾的 `[docs] consistent:` 汇总行里加一句（见 FIX-02c）。所用变量（`check`／`read`／`readdirSync`／`designText`）都已在文件顶部就位。

#### FIX-02a（#18）`Config` 字段清单必须等于接口字段

**背景**：README:25 至今写着 `enabled` / `every: number | null` / `active`，而 `src/resource.ts:9-11` 与 `DESIGN.md:126` 都是 `present`（ADR-87 改名）。现有 check #15 只断言「带反引号的 PascalCase 符号在 src 里存在」，**被重命名的字段名对它是隐形的**——这正是它能长期漂移的原因。

```js
// 18) The `Config` field list must be the interface's field list. README carried `active` for
//     several commits after ADR-87 renamed it to `present`; #15 only sees backticked PascalCase
//     symbols, so a renamed *field* was invisible to every check.
const configBlock = /export interface Config \{([\s\S]*?)\n\}/.exec(read('/src/resource.ts'))
check(Boolean(configBlock), '/src/resource.ts', 'missing the Config interface')
if (configBlock) {
  const configFields = [...configBlock[1].matchAll(/^\s{2}(\w+)\??:/gm)].map(match => match[1])
  const configLine = read('/README.md').split('\n').find(line => line.includes('`Config`（'))
  check(Boolean(configLine), '/README.md', 'missing the Config field list')
  if (configLine) {
    const listed = [...configLine.slice(0, configLine.indexOf('，适配层')).matchAll(/`(\w+)/g)].map(match => match[1])
    check(listed.join(' ') === configFields.join(' '), '/README.md',
      `the Config field list is not the interface's fields\n    documented: ${listed.join(' / ')}\n    interface:  ${configFields.join(' / ')}`)
  }
}
```

#### FIX-02b（#19）结果表释放语义必须与 `store.ts` 一致

**背景**：`DESIGN.md:128` 末句写「实例释放时只把格置 `undefined`（cell ref 不删）」，而 ADR-90 已把实现改成删格：`src/store.ts:40-42` 是 `cells.delete(identityOf(url, key))`，`store.ts:10-11` 的头注释与 README:150 的「释放后残留全 0（含结果表实际储存格数 `cells`）」都站在实现一侧。ADR-93 声称「五处措辞与当前结构对齐」时漏了这一处。

```js
// 19) The result table's release semantics must match store.ts: the cell ref is deleted, not
//     blanked. DESIGN.md kept the pre-ADR-90 wording and every check stayed green, because #15
//     only asserts that symbols exist, not what the sentence next to them claims.
if (/cells\.delete\(identityOf\(url, key\)\)/.test(read('/src/store.ts'))) {
  check(!designText.includes('cell ref 不删'), 'DESIGN.md',
    'stale release wording: the code deletes the cell ref (ADR-90)')
  check(designText.includes('cells.delete'), 'DESIGN.md',
    'DESIGN.md §3.3 must state that releasing deletes the cell ref (cells.delete)')
}
```

#### FIX-02c（#20）开发侧引用的公共字段必须存在

**背景**：这条是 FIX-01 的**系统性预防**。`scripts/*.mjs` 与 `scripts/*.ts` 都不在 `tsconfig.json` 的 `include` 里（`include: ["src", "examples", "tests", "*.ts"]`），所以 `public-types.ts` 里删一个字段，开发侧可以继续引用它而没有任何门禁报警。

```js
// 20) Developer-side support code is outside tsconfig, so a member renamed in public-types.ts can
//     stay referenced there unnoticed: scripts/benchmark.mjs watched `display.value.failedAt` for
//     several commits after ADR-77 moved the failure to the `failure` outlet, and its counter
//     silently stayed 0.
const declaredMembers = name => {
  const block = new RegExp(`interface ${name}[\\s\\S]*?\\n\\}`).exec(read('/src/public-types.ts'))
  return block ? [...block[0].matchAll(/readonly (\w+)/g)].map(match => match[1]) : []
}
const OUTLETS = { display: declaredMembers('RefreshDisplay'), failure: declaredMembers('RefreshFailure') }
const devFiles = [
  ...readdirSync(`${root}/scripts`).filter(name => /\.(mjs|ts)$/.test(name)).map(name => `/scripts/${name}`),
  ...readdirSync(`${root}/examples`).filter(name => name.endsWith('.ts')).map(name => `/examples/${name}`),
]
for (const file of devFiles) {
  const text = read(file)
  for (const outlet of ['display', 'failure']) {
    for (const [, member] of text.matchAll(new RegExp(`\\b${outlet}\\.value\\?\\.(\\w+)`, 'g'))) {
      check(OUTLETS[outlet].includes(member), file,
        `reads ${outlet}.value.${member}, which the outlet does not declare (${OUTLETS[outlet].join(' / ')})`)
    }
  }
}
```

- **验证**：在改 `benchmark.mjs` 之前先加这三条检查，`pnpm check:docs` **必须报红**（#18 报 `active`、#19 报 `cell ref 不删`、#20 报 `display.value.failedAt`）。三条全红 = 检查真的在测东西；随后 FIX-01/03/05 依次让它们转绿。
- **回滚**：纯增量代码，注释保留动机，删除即回滚。

#### FIX-02d（附带）把 `scripts/` 纳入类型检查

```jsonc
// tsconfig.json:18
"include": ["src", "examples", "tests", "scripts", "*.ts"]
```

- **注意**：`.mjs` 不受 `tsc` 约束（需要 `allowJs` + `checkJs`，且会引入大量 `implicit any`，不建议）。这条只让 `scripts/observe.ts` 进入检查范围。
- **执行顺序**：先单独改这一行跑 `pnpm typecheck` 看新暴露的错误数；若 > 10 条，改为新建 `tsconfig.scripts.json` 并纳入 `pnpm verify`（FIX-13），不要硬塞进主 tsconfig。

### FIX-03 `DESIGN.md` §3.3 结果表行的释放语义与成员清单

- **问题**：末句与实现相反（见 FIX-02b 背景）；同一行还把 `list` 归入「写入端」、完全没提 `size`，而 `size()`（`src/store.ts:47`）正是 README:150 那条证据依赖的读数。
- **位置**：`DESIGN.md:128`（`### 3.3` 的「结果表（`store.ts`）」行）
- **修复动作**（只动该行两处，其余保持原样）：

```text
旧 1：写入端只由内核用（`write(url, key, data, updatedAt)` 写成功、`fail(url, key, error, failedAt)` 写失败、
      `releaseIfUnused` 删、`list` 列举），读出口是 `read`；
新 1：写入端只由内核用（`write(url, key, data, updatedAt)` 写成功、`fail(url, key, error, failedAt)` 写失败、
      `remove(url, key)` 释放删格），读出口是 `read`；`list` 与 `size` 是观测面、不是包契约
      （`size` 含已建 ref 但从未写过的格，因此 `size()` ≥ `list().length`）；

旧 2：实例释放时只把格置 `undefined`（cell ref 不删，依赖关系才稳定），条目随之消失（A06）
新 2：实例释放时这一格连它的 ref 一起删（`cells.delete(identityOf(url, key))`）——键数因此随活跃身份收敛，
      而不是随历史身份增长（ADR-90）；同一个身份重建时 `read` 取或建一个新的 ref，并在下一拍被重新订阅，
      重写照样唤得醒（A06）
```

- **验证**：`pnpm check:docs`（#19 转绿）；`grep -n 'cells.delete' DESIGN.md` 有命中。
- **风险**：零（只改文字）。回滚 = 还原该行。

### FIX-04 `README.md:25` 的 `Config` 字段名

- **位置**：`README.md:25` —— 页面的登记就是它自己那份 `Config`（`enabled` / `every: number | null` / `active`，适配层原地改写）
- **修复动作**：`active` → `present`（唯一改动）。
- **依据**：`src/resource.ts:9-11`、`DESIGN.md:126`、ADR-87。
- **验证**：`pnpm check:docs`（#18 转绿）。
- **注意**：`README:25` 同时宣称「核心拿到的永远是一份可用身份，因此它不写 `try/catch`」——这句与实现一致（`core.ts` 内确实没有 `try/catch`），**不要顺手改**。

### FIX-05 门禁汇总行补记新检查

- **位置**：`scripts/check-docs.mjs:479-485` 的 `[docs] consistent:` 输出
- **修复动作**：在汇总句里追加 `config fields, release semantics, developer-side outlet members, `，让"门禁覆盖了什么"与文件头 `:1-6` 的自述注释保持同步（该自述本身就是本仓的一致性纪律）。

### FIX-06 `A08` 后果的行为固化（新增用例）

- **问题**：需求与 README 都已声明「请求必然终止由注入的传输负责」（`统一刷新管理.md:79`／`:282`、`README:85-86`），但**没有声明后果**：`Resource.controller` 只在 `run` 的 `finally` 清空（`src/core.ts:212-249`），而 A08 规定「上一轮没有真实结束时不再发起」⇒ 一个永不 settle 的 `post` 会让该身份**在页面卸载前永久停止取数**，并持续占用一个并发槽；`maxConcurrent` 个这样的身份会让整个库停止取数。除卸载/销毁外没有自愈路径。
- **位置**：`src/core.ts:212-249`（`run` 无超时）、`:268-281`（`enqueueDue` 跳过 `hasExecution()`）、`src/resource.ts:28`／`:41-43`
- **修复动作（代码零改动，只固化成事实）**：在 `tests/core.test.ts` 新增一条以 **`A08` 开头**的用例（沿用现有 `sink` 替身与 `sleep` 工具）：

```ts
test('A08 传输永不结束时该身份不再取数，声明清空后槽位归还', async () => {
  let calls = 0
  const core = new RefreshCore(1, { post: () => { calls += 1; return new Promise(() => {}) } }, sink)
  const config = { enabled: true, every: 10, present: true }
  core.setConfig(config, true, 10, true)
  core.submit(config, '/never', { args: { a: 1 }, key: '{"a":1}' })
  await sleep(60)
  assert.equal(calls, 1, '上一轮没有真实结束：后续周期不再发起')
  core.undeclare(config)                       // 最后一个声明者离开 → abort → 槽位归还
  const after = snapshot(core)                 // scripts/observe.ts 的观测投影
  assert.equal(after.running.length + after.queued.length, 0, '卸载后槽位归还，队列不残留')
  core.dispose()
})
```

- **流程要求（本仓纪律，必须做）**：新用例必须挂到已有叶子编号上，并在能力域的 `判定` 行里能被看到——`scripts/check-docs.mjs` 的 #6 会双向校验「叶子 ↔ 能力域」。A08 的叶子已存在（`统一刷新管理.md:394`），此用例只是把它的后果**显式化**；**若确认人要把它升为独立叶子**（例如「传输不终止时的停机行为」），需要新增叶子行 + 在 §3 对应能力域追加引用 + 一条 ADR 记裁决。
- **验证顺序**：先跑一次不带 `undeclare` 的变体（断言 `running` 仍占 1 槽）确认这条用例真的在测「泄漏」，再补上 `undeclare` 段。
- **风险**：这条用例锁的是**当前口径**。如果确认人选择 FIX-07 的方案 B（加回可选上限），本用例的期望值要跟着改。

### FIX-07 需求侧：把「永不结束」的后果写进条文（**需确认人裁决**）

- **位置**：`统一刷新管理.md:79`、`:282`（已写前提），A08 行 `:394`（未写后果）；`README.md:85-86`（同）
- **方案 A（推荐，不改能力，只补一句）**：在 A08 的「必须结果」后追加：
  > 一个永不 settle 的传输会让该身份在页面卸载前不再取数，并持续占用一个并发槽；这是注入传输的责任（ADR-65）。
- **方案 B（加回一个可配置上限）**：`createRefreshManager({ maxConcurrent, loadTimeoutMs, axios, pinia })`，超时按失败写格。**与 ADR-65 冲突**，需要新 ADR 说明为什么把当年删掉的能力请回来（当时的删除理由是「截止时间归传输」，而 FIX-06 证明框架仍要承担 A08 的后果）。
- **不做为**：如果确认人认为现状可接受（生产用 axios 且配了 `timeout`），那么只做方案 A 的登记即可。
- **验证**：方案 A 改完 `pnpm check:docs` 必须仍全绿（改动 A 行文本时注意 check #16 的 `N 个用例` 正则不要被新句子误伤）。

---

## 2. 第二批（P1）：结构与耦合收敛

> 本批改动都在 `src/` 内，会动 `README:145`／`:149` 与 `DESIGN.md` 的模块清单/度量。**每项做完立刻跑三门**，不要攒到最后。

### FIX-09 身份编码下移：`identityOf` / `splitIdentity` 从 `core.ts` 移到 `source.ts`

- **问题（更正后的定性）**：这不是门禁违规（`store=4 → core=3` 是向下边）。问题是**概念耦合**：身份编码住在「协调者」模块里，于是「结果表」这个纯数据结构必须 import 协调者。运行期方向是核心通过端口调用结果表（`core.ts:19-26` 的 `ResultSink`），模块方向却是结果表依赖核心——两个方向相反，读代码的人要在两处拼出「谁是上游」。根因是身份编码被拆成两处：`source.ts` 的 `parameterKey` 与 `core.ts:33-41` 的 `identityOf`/`splitIdentity`。
- **修复动作**：

```ts
// src/core.ts: 删除导出的 identityOf / splitIdentity（:33-41），改为 import
import { identityOf, splitIdentity } from './source.ts'
// src/store.ts:4-5
-import { identityOf, splitIdentity } from './core.ts'
 import type { ResultCell } from './core.ts'
+import { identityOf, splitIdentity } from './source.ts'
```

- **连带改动（漏一个门禁就红）**：`README.md:22` 的依赖方向行不改（边仍然向下）；两处 ```text 模块清单不改（文件没变）；`DESIGN.md` §1 的分层表/边表**无需改**（没有新增边上/边下）——但 §3.3「结果表」行里若提到 `identityOf` 的归属，需同步一句。
- **不要顺手做的**：不要同时把 `ResultCell` 也搬走。`ResultCell` 属于核心的输出端口，搬它会让 `core→store` 出现类型反向边（门禁虽只打印不拦，但白白让图谱变差）。
- **验证**：`pnpm check:docs`（依赖图打印里 `store → core` 的**运行期**边消失，只剩类型边）；`pnpm test` 66 绿。
- **收益可量化**：`store.ts` 的运行期依赖从 2 个模块降到 1 个（`pinia` + `source.ts`），`core.ts` 暴露面减少 2 个符号。

### FIX-10 边沿检测收进 `setConfig` 的返回值

- **问题**：适配层为判断「之前没资格、现在有资格」这条边，必须在 `setConfig` 前后各问一次核心（`src/vue.ts:144` / `:147`），并隐含依赖「`setConfig` 在同一同步块内写完」这一前提（`:143` 的注释就是在解释这件事）。这是把核心的内部判定搬到适配层来做。
- **修复动作**：

```ts
// src/core.ts:85 —— 返回值从 void 改成本次配置转换的边沿
setConfig(config: Config, enabled: unknown, every: unknown, present: boolean): 'became-eligible' | 'none' {
  const before = config.every !== null && config.enabled === true && config.present    // 旧资格（写前三格）
  /* …原三格写入不变… */
  const after = this.isEligible(config, config.url ?? '', key)  // 注：需拿到当前身份键，见下
  return after && !before ? 'became-eligible' : 'none'
}
```

> **实现提示**：`isEligible` 需要 `(url, key)`，而 `setConfig` 的入参里没有。两种可选做法：
> ① `Resource` 侧提供 `isEligibleNow()`（自己知道自己是谁），由核心在写完后调用——**推荐**；
> ② 保留适配层两次查询，但把它抽成一个具名函数（收益变小）。
> 选 ① 时注意 `config` 挂在哪份 `Resource` 上：用 `resourceOf(config)`（`:175`，扫描注册表，ADR-57 既有做法）。

```ts
// src/vue.ts:139-157 收敛为
const stopWatching = watch(
  () => ({ core: installed.value, ...readConfig(options, active.value, visible.value) }),
  next => {
    if (next.core === null) return
    if (next.core.setConfig(config, next.enabled, next.every, next.present) === 'became-eligible') {
      lastReadAt = null          // U12：重新成为读者 → 置空基准并立刻读回当前那一版（ADR-94）
      wakeReader()
    }
  },
  { flush: 'sync', immediate: true },
)
```

- **验证**：`pnpm test`（A06「重新成为读者立即读回该身份当前那一版」必须仍绿——这条正是该边的验收）；`pnpm test:browser` 的暂停/失活恢复场景。
- **风险**：**中**。这是本批唯一动了调度判定的项，务必先跑 `node scripts/trace-core.mjs` 存基线，改完 `cmp` 逐字节比对（该脚本不改行为就应当逐字节相同；本项会改 `setConfig` 调用形态，若 diff 出现于 trace 输出则说明行为被动过，必须先解释）。

### FIX-11 读取面投影抽成纯函数，去掉重复的重入守卫

- **问题**：`src/vue.ts:105-133` 的同步副作用同时承担四件事（节流窗口、身份划界、失败出口、重入复核），其中 `:130` 与 `:132` 是**两处几乎相同的** `if (submitted.value !== params) { wakeReader(); return }`；`submitted`（装 `Parameters` 的状态）被同时当作数据与事件总线（`:103` 用 `triggerRef(submitted)` 强制唤醒自己）。结果：这条投影无法单测，只能靠整页挂载来测。
- **修复动作（分两步，可分开评审）**：
  1. **抽纯函数**（无行为变化）：把 `publishData` + `publishFailure` 收敛为一个纯函数
     `project(cell, parameters, key, readState, config) → { data?, failure?, nextReadAt }`，副作用只做「读表 → 调它 → 写 ref」；
  2. **去掉双守卫**：把「本轮快照是否过期」抽成局部函数 `stale()`，`:130` 与 `:132` 各调一次（或把两处合并为「发布完成后统一复核一次」——**必须先确认 A21/ADR-89 的现有用例仍绿**，因为那两条用例钉的正是「发布当中换身份要重订依赖」）。
- **验证**：纯函数可新增单测（无需 DOM），覆盖「同身份同版不抄第二遍」「窗口未到不换画面」「无读取时间直接抄」三条判定；`pnpm test` 用例数会从 66 上升，**同步 README:145**。
- **风险**：中。`flush: 'sync'` 的顺序语义是既有验收（ADR-89）的一部分，禁止在重构中把 `watchEffect` 改成默认 flush。

### FIX-12 `store.read` 去掉建 ref 的副作用；口径写清

- **问题**：`src/store.ts:44` 的 `read` 调用 `refOf`（`:17-25`）——名为「读」的接口会**创建** cell，与 DESIGN §3.7「不另存」、§3.5「资格判断无副作用」的纪律不一致；连带 `size()`（`:47`，含空壳）与 `list().length`（`:50-59`，不含空壳）**口径不等**，观测数取决于谁先调用。
- **修复动作**：

```ts
// src/store.ts:44
-const read = (url: string, key: string): ResultCell | undefined => refOf(url, key).value
+/** 纯查询：不建格。建格只发生在写入路径（`write` / `fail`）。 */
+const read = (url: string, key: string): ResultCell | undefined => cells.get(identityOf(url, key))?.value
```

- **必须同步的注释/文档**：`store.ts:16`（`refOf` 的「取或建」说明保留）、`:39-42`（`remove` 注释保留）、`DESIGN.md` §3.7 增加一条「`read` 不建格；格只在写入侧诞生」；`README` 无相关表述。
- **验证**：`pnpm test`（核心用例直接驱动 `RefreshCore` 并用真实 store 作为 sink，需确认没有用例依赖「读一下就多一个空壳」）；`pnpm test:browser` 的「释放后残留全 0」场景；`node scripts/benchmark.mjs --duration 1000`（`residue.cells` 必须仍为 0）。
- **风险**：低-中。唯一真实风险是有用例在写之前读，靠副作用建格——跑测试即可暴露。
- **顺带**：把 `size` 改名为 `cellCount`，或在 JSDoc 里写成「含已建 ref 的格数（≥ `list().length`）」。改名会触碰 `benchmark.mjs:118` 与 `scripts/observe.ts`，二者择一即可。

### FIX-13 把 benchmark / trace-core 纳入一条 `verify` 命令

- **问题**：`scripts/benchmark.mjs`(160 行) 与 `trace-core.mjs`(272 行) 共 432 行**不被任何 npm script 调用**，只靠 README:123／`:150` 的手工步骤维系——FIX-01 能潜伏这么久，根因就在这里。
- **修复动作**：

```jsonc
// package.json scripts
"verify": "pnpm typecheck && pnpm test && pnpm check:docs && node scripts/benchmark.mjs --duration 1000 --runs 1",
```

- **注意**：`benchmark` 已在「真实在途 > maxConcurrent 或释放后有残留」时以非零码退出（`:155-160`），因此它适合当门禁；`trace-core` 是**差分**工具（改动前后 `cmp`），不适合当门禁，**保持手工**，但在 README:123 明确标注「非门禁，差分用」。
- **验证**：`pnpm verify` 一次跑通。

---

## 3. 第三批（P2）：命名与拆分（逐项裁决后再做）

> 本批每一项都要动「代码 + 需求 + 设计 + README」四处，并可能触碰 check #15（文档承诺的符号必须存在）、#16（度量与清单）、#19/#20。**统一原则：一次只做一项，做完立刻跑三门。**

### FIX-14 `Config` → `Slot`（或 `Registration`）

- **理由**：`Config` 与调用方的 `RefreshOptions`、适配层的 `readConfig`、核心的 `setConfig` 共用「配置」一词，但它的真实身份是「一页在核心里的登记槽」〔ADR-66〕，且被三方读写（创建 `vue.ts:56`／写入 `core.ts:85`／读取 `resource.ts:36-59`）。
- **代价（必须先看）**：`Config` 是 check #15 关心的 PascalCase 符号，出现在 `DESIGN.md` §3.3／§3.8 与 `统一刷新管理.md` §2.2 镜像之外的多张表里；改名会让门禁在**改完全部文档之前**一直报红。
- **建议做法**：不要只改代码；用一次「全仓替换 + 逐处读一遍」的方式落地，替换清单：

```sh
grep -rn 'Config' --include=*.ts --include=*.md . | grep -v node_modules   # 先看全量，再决定改不改
```

- **如果不改**：请在 `src/resource.ts:8` 的 JSDoc 里补一句「这个名字**不是**调用方的配置：它是本页在核心里的登记槽（ADR-66）」，成本 1 行，收益是消除误读的大部分。

### FIX-15 `Resource` → `Identity`（或 `SharedQuery`）

- **理由**：文档把「资源」定义为 URL 字符串（`DESIGN.md:90` 的 mermaid 与 §0.1 词汇表），而类 `Resource` 表示「一个身份的全部状态」——类名与领域词冲突，且这是本模块最常被误读的地方。
- **代价**：同上，`Resource` 在 `DESIGN.md` 出现频率高（§3.3／§3.5／§3.9 等），改名需要同步多处。
- **若不改**：在类声明上方加一行「一个**身份**（URL ＋ 参数键）的全部状态；不是 URL 本身」。

### FIX-16 `key` 一词消歧：`parameterKey` / `identityKey`

- **位置**：`src/source.ts:10`（`Parameters.key`）、`src/core.ts:33`（`identityOf(url, key)`）、`src/store.ts:50-56`（`list()` 拆出的 `key`）
- **建议**：`Parameters.key` → `parameterKey`；`identityOf(url, parameterKey)` 的返回叫 `identityKey`；`list()` 的行字段保持 `key`（它是 URL 之外的那一半参数键）。
- **收益**：`core.ts:129/143/148` 三处 `key` 形参不再与「身份键」混淆。
- **代价**：`Parameters` 出现在 §2.2 之外的章节与 DESIGN §3.3；改名需同步。

### FIX-17 `createRefreshManager({ axios })` → `{ http }`（**公共 API 变更，需确认人裁决**）

- **理由**：形参名把一个结构化端口 `RefreshHttp`（只需 `post`）绑到具体库名上，而 README:5／`:70` 同时宣称「库不 import 它、只要求有一个 `post`」。
- **影响面（必须全改）**：`src/vue.ts:204-208`、`README.md:72`、`统一刷新管理.md:112`（示例）与 `:126`（API 表）、`scripts/benchmark.mjs:98`。这是**破坏性改名**：接入方 `createRefreshManager({ axios })` 会编译报错。
- **建议**：若要做，与下一次发布一起做，并在 README 的「实际验证与边界」表里记一条变更说明；若不打算做破坏性变更，**保持 `axios` 但在 §2 的 API 表里写明「这个字段名是历史命名，任何有 `post` 的对象都可以传」**（成本 1 句，收益接近）。

### FIX-18 `vue.ts` 的模块级单例拆分（**收益真实、代价也真实**）

- **问题**：`src/vue.ts` 里混着三类关注点：模块级单例（`:21-29`）、每页适配（`:47-201`）、安装与可见性（`:204-247`）；三处通过 `installed`／`visible`／`currentCore` 强耦合，而 `currentCore()` 又把内部类 `RefreshCore` 泄给测试/脚本/示例。
- **代价（这是本项的主要部分，别低估）**：新增一个模块必须同时改 ① `scripts/check-docs.mjs:184` 的 `LAYERS`；② `README.md` 与 `DESIGN.md` 两处 ```text 模块清单（check #1 会严格比对）；③ README:22 的依赖方向行；④ `pnpm complexity` 的「7 个源文件、814 行」与 README:149；⑤ `src/index.ts` 若导出新符号还要动 check #16 的导出面计数。
- **建议**：**先做零成本的一半**——把 `currentCore()` 从 `src/vue.ts` 的公共导出改为带前缀的内部出口（如 `__currentCore`）并在 §9.3 标注「开发侧出口，不是包契约」，同时把 `tests/`／`scripts/`／`examples/` 的深链 import 统一到 `../src/vue.ts` 的同一行。拆分文件留到确实要动这块逻辑时再做（FIX-11 就是那个时机，可一起做）。

### FIX-19 `tests/types.ts` 改名与测试分片

- **19a 改名**：`tests/types.ts` → `tests/contract.typecheck.ts`（70 行全是 `@ts-expect-error` 反例，靠 `pnpm typecheck` 执行，文件名读不出这一点）。
  - **影响面**：`README.md:144` 引用了这个路径，必须同步；`ADR.md:61`／`:78` 的历史引用**保持原样**（ADR 是历史记录，且不在门禁扫描范围）。
  - **安全**：`check-docs.mjs:406` 只统计 `*.test.ts`，改名不影响用例总数；`pnpm test` 用 `tests/*.test.ts` 通配，也不受影响。
- **19b 分片**：`tests/core.test.ts`（1173 行）与 `tests/vue.test.ts`（1027 行）按规则域拆为 3–4 个文件，fixture 抽到独立模块。
  - **代价**：`check-docs.mjs:425` 的 `` `tests/x.test.ts` N `` 断言与 README:145 的「41 + 25」必须同步；拆分后用例总数不变，但**每文件计数会变**。
  - **建议**：只在下次需要大改测试时顺手做；单独做收益（可读性）低于代价（四处计数同步）。

### FIX-20 `fix-dts.mjs` 的正则补丁（**需先验证编译器能力**）

- **问题**：`scripts/fix-dts.mjs:21-29` 用正则把产物里的 `.ts` 说明符改写成 `.js`，来源是 `allowImportingTsExtensions`（`tsconfig.json:6`）与「兼容 TS 4.9 消费基线」的需求冲突。
- **候选做法**：若 TS 7.0.2 支持 `rewriteRelativeImportExtensions`（TS 5.7 引入），则

```jsonc
// tsconfig.lib.json:3-9
"rewriteRelativeImportExtensions": true,
```

  并在 `tsconfig.json` 里也打开（两个配置共用 `compilerOptions` 时注意只加在 lib 侧或两侧都加）。然后**不要删整个脚本**：把正则改写段（`:20-29`）删掉，保留形状断言段（`:31-45`，`export type *` 与「不残留 `.ts` 说明符」两条），并把脚本改名为 `assert-dts.mjs`、`package.json:10` 的 `build` 同步。
- **前置验证（必做）**：`npx tsc --help | grep rewriteRelativeImportExtensions`；若不存在则**放弃本项**，现状（正则补丁）是可接受的。
- **验证**：`pnpm build` 后 `grep -rn "\.ts'" dist/*.d.ts` 必须无命中；`node scripts/assert-dts.mjs` 退出码 0。

### FIX-21 §5 的 G04 记录区瘦身（**单行 66638 字符**）

- **问题**：`统一刷新管理.md:433` 的一行 = **66638 字符**（整个 §5 的大部分体积），里面内联了 ADR-77／92／93／94 的完整交付记录（SHA-256、字节数、行数、用例数、逐条落地清单）。任何对 §5 的读取、diff、评审都会被这一行淹没。
- **修复动作**：
  1. 新建 `交付记录.md`（或 `docs/delivery-log.md`），把 G04 行里「**YYYY-MM-DD「标题」（ADR-xx）交付记录**」的段落**原样**迁移过去，按日期分节；
  2. `:433`／`:434` 只留结论 + 一行指针：「交付记录见 `./交付记录.md`」；
  3. **同步引用**：`README.md:151`（「最终包 SHA-256 记在统一文档 §5 的 G04 行」）改为指向新文件；`统一刷新管理.md` 内其余指向 G04 的表述一并检查。
- **验证**：`pnpm check:docs` 全绿（注意 #16 会扫「N 个用例」——迁移后若新文件不在扫描列表内则无影响；若把新文件加入扫描列表，需保证其中的计数与实测一致）；`awk` 复查 §5 最长行 < 1000 字符。
- **建议顺带加一条门禁**（可选）：`check(最长表格行 < 2000, '统一刷新管理.md', 'a table cell has grown into a changelog')`，把"文档里嵌日志"这个模式永久堵住。

---

## 4. 需求侧修改提案（需确认人逐条裁决）

> 以下不改代码，只改需求与验收。每条都给「建议文本」，确认人圈定后由负责人落地。

| 编号 | 位置 | 现状问题 | 建议 |
|---|---|---|---|
| R-1 | §4.2 `A20`（`:383`） | 「同一 URL 两处声明仍然合并」自述「没有冲突可检出」，不可证伪；`RefreshSource` 已删（ADR-74/75） | **删除该叶子**，或降级为 §3.1 的一句不变量，释放它的回归预算 |
| R-2 | §4.2 `A16`（`:419`） | 主体是内部结构事实（核心不认识页面），却标 `[行为]`；§4.1 自己的定义要求判定只靠公开面 | 前缀改 `[不变量]`，并把可观察部分（「传输失败只写结果表」）单列一条 |
| R-3 | §4.2 `A13`（`:406`）／§2.2 | `RefreshFailure.failedAt` 无产品级消费者（只有示例拼字符串） | 保留则写明用途；否则删字段，失败出口只留 `error` |
| R-4 | §2.5／U12（`:214`、`:294-300`） | 「资格」同时指取数资格与读者资格，「读闸门」成为跨层第三词；实现被迫手写闸门并绕开响应式依赖（`vue.ts:125-127`） | 命名分离为「取数资格」与「读者资格」，各出一条锚点；「读者资格」整条划归适配层 |
| R-5 | §2.5 的口径段（`:214`） | 「未读过的暂停页会跟着第一份数据上屏一次」是反直觉行为，被塞在长段括注里 | 升为独立锚点（触发条件 → 是否冻结 → 解冻时机），或明确取消该例外 |
| R-6 | §3.5 `R5`／§5 `G02` | 参数边界曾在「值域／合法性归属／编码语义」三件事之间反复改判 | 拆成三条独立锚点，各自钉住一件，减少再次漂移 |
| R-7 | §5 `G01` | 保留一条「已关闭（取消）」的门槛行 | 移入 ADR 记账区，门槛表只留未决项 |
| R-8 | §1 `G1–G11` 与 §5 `G01–G04` | 同用 `G` 前缀，仅靠位数区分，正文互相引用 | 门槛改 `M01–M04`（或 `Gate-…`） |
| R-9 | §2.3（`:79`）／A08（`:394`） | 「传输负责终止」的前提已写、**后果未写** | 采纳 FIX-07 方案 A 的一句话 |
| R-10 | §2.2（`:126`）／§3.3 `U12` | 「改频率/改可见性不重置读取基准，重新成为读者重置并立刻读回」只能靠串 ADR-91/94 读懂 | 压成一张三行小表（触发条件 → 是否重置基准 → 上屏时机） |

---

## 5. 明确不改的部分及理由

> 这些都是**已裁决**的取舍，重新讨论它们只会制造返工。若确认人主动要求改，再单独立 ADR。

| 对象 | 位置 | 裁定 | 为什么不动 |
|---|---|---|---|
| `core.readResult` 这个一行转发 | `src/core.ts:147-150` | ADR-81 明确「核心补 `readResult`」 | 它换来的是「适配层只依赖核心一个读入口」；要删就得让适配层持有 store，属于架构口径变更 |
| `produced` + `needsNext` 两个布尔 | `src/resource.ts:22`／`:24` | ADR-70 | 修的是「刷新是给身份的命令、不留欠账」这句口径，改三态枚举会把两个独立事实重新糅在一起 |
| `Resource.controller` 与「执行位置」并存 | `src/resource.ts:28` | ADR-65，`DESIGN.md:197` 已解释 | `controller` 回答的是「迟到的结束算不算数」，不是位置 |
| `cells.delete`（释放连格删） | `src/store.ts:40-42` | ADR-90 | 实现是对的，**要改的是文档**（FIX-03） |
| `present` 字段名 | `src/resource.ts:11` | ADR-87 | 实现与 DESIGN 都对，**要改的是 README**（FIX-04） |
| 参数值域严格线（拒绝 `Date`/`Map`/`Set`…） | `src/source.ts:22-33` | ADR-52（含实测危害取证） | 已实测过「两个不同 Map 塌成同一身份」，不动 |
| `store.ts` 是唯一 import Pinia 的模块 | `src/store.ts:1` | ADR-63／ADR-65 | 与 README:24 一致 |
| ADR-92 已清的三处死代码 | — | ADR-92 | 已落地，无需重复 |
| ADR-84/85/86 的三次收敛（执行位置、`slot`、同一件事两处表达） | — | 已落地 | 无需重复 |
| `.md` 之外的历史评审文档（`统一刷新-评审.md`、`CODE_REVIEW.md`） | 仓库根 | 历史件 | **不要**按本文档去改它们；它们不在门禁扫描内，改动只会污染历史 |

---

## 6. 同步矩阵（改一处必须跟着改的地方）

| 你改了 | 必须同步 | 会被哪条门禁打红 |
|---|---|---|
| `src/` 增删文件 | `README.md` + `DESIGN.md` 的 ```text 模块清单；`check-docs.mjs:184` 的 `LAYERS`；README:22 依赖方向行；README:149 度量行 | #1、#2、#4、#9 |
| `src/public-types.ts` 的类型 | `统一刷新管理.md` §2.2 的镜像块（**逐字节**，注释除外） | #3 |
| 测试用例数 | `README.md:145`（`N 个用例`、每文件计数） | #16 |
| `src/` 行数／分支／maxCCN | `README.md:149`（先 `pnpm complexity`，可用 `--write`） | #4 |
| 文档里出现的新 PascalCase 符号 | 该符号必须在 `src/` 里存在 | #15 |
| 删除的旧名字 | 同步 `check-docs.mjs:462` 的 `RETIRED` 列表（只加不删） | #17 |
| 锚点 `U…` 的增删改 | §3 能力域的引用、§5.1 的索引、DESIGN 的 20+ 处引用 | #7 |
| 验收叶子 `A…` 的增删改 | 至少一个能力域引用它；能力域的 `判定` 行 | #6、#8 |
| 新增 ADR | `ADR.md` 主表 + `统一刷新管理.md` 的对应 G/U/A 行（不要在 §5 里写长交付记录，见 FIX-21） | — |

---

## 7. 验收清单

### 7.1 每批做完必过

```sh
pnpm typecheck        # 0 错误
pnpm test             # 66 绿（FIX-11 之后按实际数字更新 README:145）
pnpm check:docs       # consistent + 19 叶子 traced / 0 未验证
pnpm complexity       # 打印当前文件数/行数/分支/maxCCN，与 README:149 对齐
```

### 7.2 第一批专属

```sh
# FIX-02 落地后、FIX-01/03/04 落地前：这三条必须一起报红（否则说明新检查没在测东西）
pnpm check:docs      # 期望：报出 #18（README 的 `active`）、#19（DESIGN 的 `cell ref 不删`）、#20（display.value.failedAt）

# 全部修完之后
node scripts/benchmark.mjs --duration 500 --runs 1   # 反例注入时「后台失败」必须 > 0，改回后为 0
grep -n 'cells.delete' DESIGN.md                     # 有命中
grep -n '`active`' README.md                         # 无命中
pnpm typecheck                                       # FIX-02d 之后：scripts/observe.ts 进入检查范围，0 错误
```

### 7.3 第二批专属

```sh
node scripts/trace-core.mjs > /tmp/trace-after.txt   # 与改动前逐字节 cmp（FIX-10 必须先解释差异）
pnpm test:browser                                    # 真实 Chrome 12/12
node scripts/benchmark.mjs --duration 1000           # 在途峰值 ≤ maxConcurrent、释放后残留全 0
```

### 7.4 第三批专属

```sh
pnpm build && grep -rn "\.ts'" dist/*.d.ts           # FIX-20：无命中
node scripts/assert-dts.mjs                          # FIX-20：退出码 0（改名前为 fix-dts.mjs）
awk 'length > 1000 { print FILENAME": "FNR" "length }' 统一刷新管理.md   # FIX-21：无输出
```

---

## 8. 风险与回滚

| 批次 | 主要风险 | 控制手段 |
|---|---|---|
| 第一批 | 门禁新增断言可能误伤（例如 README 句子被改写后 #18 找不到锚点） | 三条新检查写成「锚点缺失即报红」而不是静默跳过；先在**未修复**状态下跑一次，确认三条都报红 |
| 第二批 | FIX-10 触碰调度判定语义 | 先存 `trace-core` 基线；A06/A21（ADR-89/94 的验收）必须保持绿；必要时拆成两次提交 |
| 第二批 | FIX-12 去掉 `read` 的建格副作用可能暴露隐式依赖 | 只改一行 + 注释；用 `pnpm test` 与 `residual cells == 0` 两条同时兜底 |
| 第三批 | 改名波及门禁与多处文档，容易「改一半」 | **不变量**：任何一次提交结束时三门必须全绿 ⇒ 要么全改完，要么不改。禁止留下「代码已改、文档待补」的中间态 |
| 全局 | 在 §5 里继续堆交付记录（FIX-21 的反面） | 采纳 FIX-21 的可选门禁（表格行长度上限），把模式堵住 |

**回滚策略**：每个 FIX 独立提交（FIX-02 的三条检查可与 FIX-01/03/05 合并为一次「门禁＋订正」提交，因为它们必须同时生效才能转绿）；回滚 = `git revert` 单次提交，无需数据迁移、无运行时状态残留。

---

## 9. 建议的下一步

1. **立即可执行**（不需要任何裁决）：FIX-02(a–d) 先把门禁加上并跑出三条红 → FIX-01 → FIX-03 → FIX-04 → FIX-05 → FIX-06（新增后果用例）。六项做完三门应全绿。
2. **需要一条裁决**：FIX-07（方案 A 还是 B），顺带裁决 FIX-06 的用例是否升为独立叶子。
3. **需要一批裁决**：§4 的 R-1 … R-10 与 FIX-14 … FIX-21 逐项圈定。

> 本文档不承载交付记录：每完成一项，请在 `ADR.md` 追加相应条目（或更新已有 ADR 的落地清单），并保持 §5 的 G04 行只放结论。

---

## 10. 新一轮审查（资深产品与技术专家视角，2026-09-21）

> 本章为新一轮独立审查报告，按「问题描述—所在位置—严重程度—改进建议」四列组织，聚焦 **需求 / 设计 / 实现** 三层中仍残留的、**与既有 FIX-XX 不重叠** 的问题。
> 基线：`src/` 7 文件 814 行、`tests/` 66 用例、ADR-27 ~ ADR-109 已落地、`pnpm typecheck` 0 错误。
> 严重度：**P0** ＝证据有效／语义相反／字段失修；**P1** ＝结构耦合／职责不清／冗余状态，量化收益；**P2** ＝命名／拆分／文档纪律，单点裁决。

### 10.1 审查维度与方法

| 维度 | 关注点 | 主要输入 |
| --- | --- | --- |
| 需求 | 伪需求（不可证伪／无消费者）、模糊定义（双关词／括注行为）、命名漂移 | `统一刷新管理.md`、`README.md` |
| 设计 | 臃肿（大文件／大函数）、职责不清（依赖反向／概念越层）、冗余抽象 | `DESIGN.md`、`src/`、门禁输出 |
| 实现 | 拼凑代码（对称重复／事件总线化）、冗余状态/缓存、命名冲突、文件拆分 | `src/` 全部、`tests/`、`scripts/` |

每条问题给出**最小可独立验证**的检查或测试；凡与既有 FIX-01…FIX-21 重叠的，本章不再重复，仅在新维度上补一条交叉引用。

---

### 10.2 需求分析

#### N-01 「读者资格」与「取数资格」共用「资格」一词，导致读闸门成为隐式第三语义

- **问题描述**：§0.4 的「资格」定义为「取数资格」（`声明 ∧ 环境允许 ∧ 开启意愿 ∧ 周期有效`），§0.5 的「读者」定义为「是否跟随结果表」（`有资格 ∨ (无读取基准 ∧ 浏览器可见)`）。两条判定在概念上正交，但读闸门里同时出现二者（`src/vue.ts:127`），U12 的「重新成为读者」与 U04 的「自动取数」被混在同一句话里讲，需求文档必须把这两个动作绑在一起才能读懂。
- **所在位置**：`统一刷新管理.md:22`（U04 与 U12 同段）、`:214`（§2.5 资格与读者未分）、`src/vue.ts:124-127`（现象管线的二选一闸门）
- **严重程度**：**P1**——这是 U04/U12/U14 之间所有「值去哪改、谁负责唤醒」口径冲突的根因。
- **改进建议**：
  1. 在 §0.4 与 §0.5 之间新增一行「**读者资格**」词条，明确「读者资格 = 取数资格 ∨ 未读取过且浏览器可见」，并指明「**只**在适配层计算」；
  2. §2.5 把 U04/U12 的判定函数拆成两个具名判定（`canPoll(config)` 与 `canRead()`），并把「重新成为读者」的判定独立成 `canRead() ↑`，避免一处改判定、三处跟着改的现状；
  3. README「暂停页是否跟随」一节把判定写成 `canRead()` 的判定式，而不是口语化的「暂停时停一下、恢复了跟一下」。

#### N-02 §2.5 的「未读过的暂停页会跟着第一份数据上屏一次」是反直觉行为，被藏在长段括注里

- **问题描述**：该行为（页面在「无读取基准 ∧ 浏览器可见」时即跟一帧）只在 `统一刷新管理.md:294-300` 与 `src/vue.ts:127` 的二选一闸门里隐式存在，验收叶子 A21 在 `tests/core.test.ts` 中能复现，但需求正文没有独立锚点把它说成**业务行为**，只看文档的人不会预期到「暂停页 first paint 会偷偷跟一帧」。
- **所在位置**：`统一刷新管理.md:294-300`（§2.5 长段括注）、`src/vue.ts:124-127`
- **严重程度**：**P1**——首次接触的读者一定会踩坑。
- **改进建议**：
  1. 把它升为独立锚点（例 `U18「未读过的暂停页会在可见后跟一帧」`），并在 §3.3「R2 显式刷新与读取面」追加一行**场景**；
  2. 与之配套，验收补一条 `A22`（门禁 #6 会自动校验双向可达），要求「暂停页恢复可见后下一份写入必上屏、且 `lastReadAt` 被置零一次」。

#### N-03 验收叶子 A20「同一 URL 两处声明仍然合并」自述「没有冲突可检出」，不可证伪

- **问题描述**：A20 是 ADR-46 时期为 `RefreshSource` 留下的兜底叶子，源头对象已删（ADR-74/75）。当下判定写「两条声明的输出合并、没有冲突可检出」，**「没有」**是不定义的可证伪句，无法写回归测试（任何现状都满足）。
- **所在位置**：`统一刷新管理.md:383`（§4.2 `A20`）、原 `RefreshSource` 评审历史（ADR-46）
- **严重程度**：**P1**——占用一个验收名额但无任何回归价值。
- **改进建议**：
  - **方案 A（推荐）**：删除该叶子。若认为「同身份重复声明幂等」是行为事实，把它降级为 §3.1 R1 的一条不变量文字（不占叶子名额，门禁 #6 不再要求双向可测）。
  - **方案 B**：把可证伪部分抽出来——「同身份第二次 `submit` 必须返回 `accepted` 且不重新入队」，挂到 A04 上。

#### N-04 `RefreshFailure.failedAt` 字段没有产品级消费者

- **问题描述**：`public-types.ts:30-31` 暴露 `failedAt: number`，但业务侧唯一消费点是 `examples/main.ts` 把它拼到错误信息字符串里（"`失败: ${task.failure.value.failedAt}ms`"），核心无内部使用，门禁 #15/#16 无法保护它被「无人引用也保留」。
- **所在位置**：`src/public-types.ts:31`、`统一刷新管理.md:380-384`（R3 描述）、`examples/main.ts`（唯一外部引用）
- **严重程度**：**P2**——占一个公共字段，增加调用方心智负担。
- **改进建议**：
  - **方案 A**：删 `failedAt`，让失败出口只保留 `error: unknown`（页面试图从错误对象取时间戳本来就要走 `error.timestamp` 这类自有约定）；
  - **方案 B**：保留但补一个回归用例——任一 R3 桌面组件（可由 `examples/` 新增）必须把 `failedAt` 渲染到画面上；不通过则门禁红。
  - 推荐方案 A。

#### N-05 `Config` 字段命名跨代码与文档漂移（`active` vs `present`），README 是最大暴露面

- **问题描述**：ADR-87 把 `active` 改名为 `present`；`src/resource.ts:11`、`DESIGN.md:126`、`统一刷新管理.md §0.4` 都是 `present`，但 README:25 写「`enabled` / `every: number | null` / `active`」直到 FIX-04 才修。该问题在上一轮已识别（FIX-04），但根因是 `Config` 命名本身歧义（见 D-01）。
- **所在位置**：`README.md:25`（FIX-04）、`统一刷新管理.md §0.3`「环境允许」定义与 `Config.present` 字段名错位
- **严重程度**：**P1**——已被 FIX-04 修补，但「概念表里的『环境允许』与字段名 `present` 是否真的同义」仍是模糊点，需求侧未定。
- **改进建议**：在 §0.3 把 `present` 一词与「环境允许」显式绑定（字段名 = 合成后的 boolean），并在 §0.5「读者资格」词条里再次出现，避免「present 与 active 等价吗」之类的二次提问。

#### N-06 「窗口」一词双关：取数周期 vs 读取节流

- **问题描述**：§0.5 把「窗口」定义为「读取基准与 `every` 的判定」，但 `every` 同时是取数间隔（§0.2「周期」）。两条条文都引用 `every`，但语义不同，「窗口未到」与「未到期」口语化时极易混淆。
- **所在位置**：`统一刷新管理.md §0.2`（周期）、`§0.5`（窗口）、`src/vue.ts:80-83`（窗口判定）、`src/core.ts:60-69`（`eligibleEvery`）
- **严重程度**：**P2**——口语混淆但不致命。
- **改进建议**：把 `every` 拆为两个具名常量：取数侧叫 `pollEveryMs`，读取侧叫 `readThrottleMs`。两者**默认同源**（配置只收一项），但允许 `src/public-types.ts` 把 `RefreshOptions.every` 显式标注为「同时是取数周期与读取节流」的注释。

#### N-07 §1「承诺」G 表与 §5「门槛」G 表用同一字母，位数区分，跨节引用易错

- **问题描述**：§1.4 的承诺行用 `G1`–`G11`（11 条），§5 的门槛行用 `G01`–`G04`（4 条）。同一份文档两套 `G` 前缀，README 也跟着用 G 表，正文交叉引用（如「G4 → A10」）读者要在「承诺 G」与「门槛 G」之间猜。
- **所在位置**：`统一刷新管理.md §1.4`（承诺）、`§5`（门槛）
- **严重程度**：**P2**——文档可读性问题。
- **改进建议**：把 §5 的门槛改前缀为 `M01`–`M04`（或 `Gate-…`），同时更新 README 与 ADR 表格里所有 G01–G04 引用；门禁 #7（锚点可达）会自动校对。

#### N-08 A16「核心不认识页面」是内部结构事实，却标 `[行为]`，§4.1 自己的纪律与之冲突

- **问题描述**：§4.1 写「判定只靠公开面」与「内部字段／私有方法不进入判据」，但 A16（`统一刷新管理.md:419`）的判定主体是「核心里没有 page/component 字段」——这是**实现细节**，不是公开行为。
- **所在位置**：`统一刷新管理.md:419`（A16）、`:179`（§4.1 纪律）
- **严重程度**：**P1**——自律冲突。
- **改进建议**：把 A16 前缀改 `[不变量]`，并把可观察部分（例：「`RefreshCore` 的 `identities` Map 不持有 `Config` 之外的页面对象」）单列一条；门禁 #8（前缀合法性）会自动核对。

#### N-09 「改频率/可见性的口径」要串 ADR-91/94 才能读懂，§3.3 没有自洽表述

- **问题描述**：U12 的「改频率不重置读取基准、改可见性不重置、重新成为读者重置并立刻读回」是一个三向规则，但 §2.5 与 §3.3 的散文体表述必须配合 ADR-91（频率不变原则）+ ADR-94（重新成为读者立即读回）才能组装完整。
- **所在位置**：`统一刷新管理.md:214`（§2.5）、`:294-300`（§2.5 后续）、`:282`（§3.3）
- **严重程度**：**P2**——可读性。
- **改进建议**：把这条规则压成一张三行小表（触发条件 → 是否重置基准 → 上屏时机），作为 §3.3 的子节或附录。

---

### 10.3 设计审查

#### D-01 `Config` 与 `RefreshOptions`/`readConfig`/`setConfig` 共用「配置」一词，但它是「登记槽」不是「配置」

- **问题描述**：`Config`（`src/resource.ts:8`）是「一页在核心里的登记」——被三处读写（`vue.ts:56` 创、`core.ts:85` 写、`resource.ts:36-58` 读），与调用方传入的 `RefreshOptions`（`src/public-types.ts:35`）不是同一物，但名称完全相同。README:25 把 `Config` 描述为「页面的登记」但 README 头部又把 `enabled`/`every` 描述成「调用方的配置」。
- **所在位置**：`src/resource.ts:8`（`Config` interface）、`src/public-types.ts:35`（`RefreshOptions`）、`README.md:25`/`src/vue.ts:32-43`（`readConfig`）、`src/core.ts:85`（`setConfig`）
- **严重程度**：**P2**——命名歧义（与既有 FIX-14 重叠，本条作为新的视角佐证）。
- **改进建议**：
  - **零成本一半**：在 `src/resource.ts:8` 的 JSDoc 第一行写「这个名字**不是**调用方的配置：它是本页在核心里的登记槽（ADR-66）。」
  - **完整改名**：见既有 FIX-14（`Config` → `Slot` 或 `Registration`），但需同步 README / DESIGN / 门禁 #15。

#### D-02 `Resource` 类名与「资源」领域词冲突

- **问题描述**：`DESIGN.md:90` 的 mermaid 图与 §0.1 词汇表把「资源」定义为「URL 字符串本身」（一个共享取数来源），但 `src/resource.ts:15` 的 `Resource` 类是「一个身份的全部状态」。类名与领域词撞车，是本模块最常见的误读源。
- **所在位置**：`src/resource.ts:15`、`DESIGN.md:90`、`统一刷新管理.md §0.1`
- **严重程度**：**P2**（与既有 FIX-15 重叠；本条作为佐证）。
- **改进建议**：类声明上方加一行「一个**身份**（URL ＋ 参数键）的全部状态；不是 URL 本身。」若要做完整改名，沿用 FIX-15 的 `Identity` / `SharedQuery` 候选。

#### D-03 `store.ts` 必须依赖 `core.ts` 才能拿到 `identityOf/splitIdentity`，依赖方向与所有权反向

- **问题描述**：`src/store.ts:4-5` 引入 `core.ts` 的 `identityOf`/`splitIdentity`，但身份编码（`URL ＋ 参数键 → string`）是**纯数据层概念**，与「协调者」无关。结果表是「按身份分区的数据结构」，却必须导入协调者模块——运行期是核心调用 store（`core.ts:19-26` 的 `ResultSink` 端口），模块方向却反向。
- **所在位置**：`src/store.ts:4-5`（`import identityOf, splitIdentity from './core.ts'`）、`src/core.ts:33-41`（两个函数的定义）
- **严重程度**：**P1**（与既有 FIX-09 重叠；本条作为概念耦合的强佐证）。
- **改进建议**：把 `identityOf`/`splitIdentity` 从 `core.ts` 下移到 `source.ts`（与 `parameterKey` 同处），`core.ts` 与 `store.ts` 都从 `source.ts` import；运行期调用图不变，模块方向却变成「核心 → source → store」单向。

#### D-04 `vue.ts` 单文件承担「模块级单例 + 每页适配 + 安装与可见性」三类关注点

- **问题描述**：`src/vue.ts` 共 247 行，三类代码段靠 `installed`/`visible`/`currentCore` 这三个模块级 `shallowRef` 强耦合：
  - `:21-29` 模块级单例（`installed`、`currentCore`）；
  - `:47-201` 每页适配（`useRefresh` 内部）；
  - `:204-247` 安装与可见性（`createRefreshManager` + `visibilitychange` 监听）。
  
  单元测试只能整体测页面，单例部分（如「重复 `install` 拒绝」「卸载时清空槽位」）只能靠 `install` 端到端触发。
- **所在位置**：`src/vue.ts:21-29`／`:47-201`／`:204-247`
- **严重程度**：**P2**（与既有 FIX-18 重叠；本条作为新的视角——「三类关注点共享三个模块级 `shallowRef`」是单例分不开的根本原因）。
- **改进建议**：先做零成本一半（既有 FIX-18）：把 `currentCore()` 改前缀 `__currentCore` 并在 §9.3 标注「开发侧出口，不是包契约」；拆分文件留到下一轮真正要改这块逻辑时（FIX-11 重构读取面投影）一起做。

#### D-05 `Resource.isPresent` 与 `Resource.isEligible` 两个方法同时承担「能不能取数」

- **问题描述**：`src/resource.ts:36-38` 的 `isPresent`（环境允许 + 周期有效）与 `:56-58` 的 `isEligible`（再加 `enabled`）语义递进。§0.4 把「资格」定义为「四项合取」，但 `isEligible` 只判三项（**缺 `声明还在`**，因为它本就是 `Resource` 自身的方法，不能反向问「自己是不是被声明了」）。结果是：核心内部用 `isEligible`、对外文档 `isEligible`，但要补全四项必须再 `&& declarers.size > 0`（没人这么做，`releaseIfUnused` 用的是 `isWanted()`）。
- **所在位置**：`src/resource.ts:36-58`、`统一刷新管理.md §0.4`
- **严重程度**：**P1**——资格定义与实现 1:1 不一致。
- **改进建议**：
  1. 把 `isEligible` 改名 `hasOpenPoll`（或类似更精确的名字），并去掉「资格」一词；
  2. 在核心内部用 `hasOpenPoll(config) && resource.isWanted()` 这个明确的两步判定；
  3. §0.4 「资格」定义补一句「**`isEligible` 不含『声明还在』，后者由 `Resource.isWanted` 现算**」，把缺口摆到台面上。

#### D-06 `Resource` 的 4 个执行状态字段（`produced` / `needsNext` / `settledAt` / `controller`）围绕同一概念但分散

- **问题描述**：`src/resource.ts:22-28` 四个字段都回答「执行到哪了」：`controller`（在途）、`produced`（已写表）、`needsNext`（要补）、`settledAt`（结算时刻）。四个字段在 `core.ts` 与 `resource.ts` 之间来回读写，但没有一个统一的方法回答「这一轮的状态是什么」。
- **所在位置**：`src/resource.ts:22-28`、`src/core.ts:135-145`（`refresh` 的判定链）、`src/core.ts:228-243`（`run` 的 finally）
- **严重程度**：**P2**——可读性。
- **改进建议**：把四个字段压缩为一个内部枚举 `phase: 'idle' | 'queued' | 'running' | 'settled' | 'needsNext'`，并暴露 `isOpen()`（在队或在跑）、`isSettled()`（已结算）、`hasPendingRefresh()`（要补）三个具名判定。零运行时收益，但读 `refresh` 与 `run` 的判定链会短一半。

#### D-07 `core.ts` 的 `setConfig` 同时承担「配置写入」与「资格边沿检测」两个语义，但只暴露前者

- **问题描述**：`src/core.ts:85-95` 的 `setConfig` 只写三字段，但适配层必须**在调用前后各问一次** `isEligible`（`src/vue.ts:144`／`:147`），并隐含「`setConfig` 在同一同步块内写完」这一前提（`:143` 的注释就是在解释）。这是把核心的内部判定搬到适配层来做（既有 FIX-10 同款问题，本条作为设计侧的根因）。
- **所在位置**：`src/core.ts:85-95`、`src/vue.ts:139-157`
- **严重程度**：**P1**（与既有 FIX-10 重叠；本条作为设计侧视角）。
- **改进建议**：把 `setConfig` 的返回值改成 `'became-eligible' | 'lost-eligibility' | 'none'`，让适配层用返回值决定要不要唤醒读取面；详见既有 FIX-10。

#### D-08 `vue.ts` 的 `published`/`failed` 投影与读取面混在同一个 `watchEffect` 内

- **问题描述**：`src/vue.ts:105-133` 的 `watchEffect` 同时承担四件事（节流窗口、身份划界、失败出口、重入复核），其中 `:130` 与 `:132` 是**两处几乎相同的** `if (submitted.value !== params) { wakeReader(); return }`。这条投影**不可单测**——只能靠整页挂载来测。
- **所在位置**：`src/vue.ts:105-133`
- **严重程度**：**P1**（与既有 FIX-11 重叠；本条作为「设计侧可测性丧失」的强佐证）。
- **改进建议**：抽一个纯函数 `project(cell, params, readState, config) → { data?, failure?, nextReadAt }`，把 `:130`／`:132` 两个守卫合并为「发布完成后统一复核一次」；详见既有 FIX-11。

---

### 10.4 实现审查

#### I-01 `submitted`（`Parameters`）`shallowRef` 被同时当作数据与事件总线

- **问题描述**：`src/vue.ts:59` 的 `submitted: shallowRef<Parameters | null>` 同时承担：
  - **数据**：当前声明的已准备参数（`submit` 成功后写入）；
  - **事件总线**：`wakeReader` 通过 `triggerRef(submitted)` 强制唤醒读取面（`:103`），让 `watchEffect`（`:105-133`）重新跑一次。
  
  这是把响应式原语当显式信号用——读端读「值」，但写入不是为了改值、而是发信号。`watchEffect` 内 `:108` 读 `submitted.value` 是「真实读」，`:130`/`submit`/`refresh` 三处写是「发信号」，两套语义同享一个 ref。
- **所在位置**：`src/vue.ts:59`／`:103`／`:108`／`:167-167`（dispose 置空）
- **严重程度**：**P1**——读 `useRefresh` 的人会把「为什么 `submitted.value = null` 也算事件」问三遍。
- **改进建议**：拆成两个 ref ——「当前声明」用普通 `let`（不需要响应式，因为同页不会同时存在两份声明），「重新读一次」信号用一个独立的 `trigger: ShallowRef<0>`（值始终为 0，只用于 `triggerRef`）。`watchEffect` 改为同时依赖 `params` 与 `trigger.value`。

#### I-02 `lastReadAt` + `shownKey` + `failedKey` 三个独立状态语义重叠

- **问题描述**：`src/vue.ts:69`/`72`/`73` 三个变量都回答「这是不是同一版／同一身份」：
  - `lastReadAt`（读取基准 = `updatedAt` 上次发布的值）；
  - `shownKey`（数据出口里那版属于哪个参数键）；
  - `failedKey`（失败出口里那笔属于哪个参数键）。
  
  三个变量的写入点散布在 `publishData`（`:89-90`）、`publishFailure`（`:97`）、watchEffect 入口（`:114-115`）与 dispose（未清，但 submitted 置 null 后没人读）。
- **所在位置**：`src/vue.ts:69-73`、`:84-99`、`:114-115`
- **严重程度**：**P1**——「这是不是同身份／同版」的判定本来可以是一个 `readState` 对象。
- **改进建议**：合并为一个 `readState: { lastUpdatedAt: number|null, identity: string|null }`，并在 `publishData`/`publishFailure`/`submit`/`dispose` 几处统一写入。零运行时收益，单测可独立验。

#### I-03 `publishData` 与 `publishFailure` 是两个几乎对称但分散的函数

- **问题描述**：`src/vue.ts:76-99` 两个函数结构几乎对称：取当前值 → 比同身份 → 写 ref → 更新 key。重复的「取当前值 → 比对 → 赋值」逻辑写了两次，唯一差别是窗口判定（只有 `publishData` 有）。
- **所在位置**：`src/vue.ts:76-99`
- **严重程度**：**P2**。
- **改进建议**：抽一个内部函数 `commit(outlet, identityKey, cellValue, isStale)`，两个 `publishXxx` 只负责「算 cellValue」「判 isStale」，真正写 ref 的地方共用一个。窗口判定作为 `commit` 的可选参数传入（仅 `publishData` 需要）。

#### I-04 `store.read` 调用 `refOf`「取或建」，违反「读」的纯语义

- **问题描述**：`src/store.ts:44` `const read = (url, key) => refOf(url, key).value` ——名为「读」的接口会**创建** cell ref。`size()`（`:47`，含空壳）与 `list().length`（`:50-59`，不含空壳）**口径不等**，观测数取决于谁先调用。
- **所在位置**：`src/store.ts:44`／`:47`／`:50-59`
- **严重程度**：**P1**（与既有 FIX-12 重叠；本条作为「观测口径不等」的强佐证）。
- **改进建议**：把 `read` 改成 `cells.get(identityOf(url, key))?.value`（不建 ref），并把「建格只发生在写入路径」写进 JSDoc 与 DESIGN §3.7；详见既有 FIX-12。

#### I-05 `Resource.settle` 同时承担「记结算时刻」与「标『已产出』」两个语义

- **问题描述**：`src/resource.ts:79-82` 的 `settle(at)` 一行写两个字段（`settledAt = at; produced = true`）。`produced` 是「这一轮已写表」标记，`settledAt` 是结算时刻。两个字段生命周期并不严格同步——`produced` 在 `run` 的 finally（`src/core.ts:238`）被清空，而 `settledAt` 一直保留。
- **所在位置**：`src/resource.ts:79-82`、`src/core.ts:228-238`
- **严重程度**：**P2**。
- **改进建议**：把 `produced` 拆成一个具名方法 `markProduced()`，与 `settle(at)` 并列；这样 `refresh` 里 `else if (resource.produced)` 的判定可以改成 `if (resource.hasSettled())`，更直接。

#### I-06 `vue.ts` 的 `readConfig` 函数返回含 `enabled`/`every`/`present` 三项，但仅 `present` 真正参与同步计算

- **问题描述**：`src/vue.ts:32-43` 的 `readConfig` 把三个值一起返回，但适配层唯一用 `present` 的逻辑在 `:140` 的 watch 源里出现一次（`readConfig(options, active.value, visible.value)`）。`enabled`/`every` 直接作为 `next.enabled`/`next.every` 透传给 `setConfig`（`:145`）。`present` 已经被 view 在 watcher 源里算过一次（`active.value && visible.value`），`readConfig` 又算一次（`:37`），重复一次合成。
- **所在位置**：`src/vue.ts:32-43`／`:140`
- **严重程度**：**P2**——微观冗余。
- **改进建议**：`readConfig` 只取 `enabled`/`every` 两项；`present` 由调用方在传参前算好（已经算过了：`active.value && visible.value`）。或者保留，但加一行注释说明为何 `present` 要再次合成（因为可能抛错时 `present` 仍要回 false）。

#### I-07 `core.ts` 的 `setConfig` 三值非法时只把 `every` 置 `null`，但 `enabled`/`present` 状态不一致

- **问题描述**：`src/core.ts:87-93` 的 `if` 走非法分支只写 `config.every = null`，**不**清空 `enabled`/`present`。结果是：下一次 `setConfig` 重新合法时，`every` 被覆盖、但前一次非法的 `enabled`/`present` 还留着——资格判定会瞬间「合法」一次（如果 `enabled === true && present === true`）。
- **所在位置**：`src/core.ts:87-93`
- **严重程度**：**P1**——存在数据竞争窗口，且无回归用例。
- **改进建议**：非法分支必须**同时**把 `enabled = false`、`every = null`、`present = false`（或写一个 `setConfig` 的私有函数 `writeAll(config, enabled, every, present)` 整组覆盖）。同时增加一条回归用例：`setConfig({...}, 100, 'x', true)` 后立刻查 `isEligible` 必须为 `false`。

#### I-08 `core.ts` 顶层导出 `identityOf`/`splitIdentity`，但唯一用户是 `store.ts`——接口应该在更下层

- **问题描述**：`src/core.ts:33-41` 的两个 `export function` 出现在公共 API 表面，但使用方只有 `store.ts:4-5`。`core.ts` 的 `core()` 自身不需要它们（它直接对 `identities` 这个 Map 操作）。也就是说**核心并不消费身份编码**——它是被借过去的。
- **所在位置**：`src/core.ts:33-41`、`src/store.ts:4-5`
- **严重程度**：**P2**（与既有 FIX-09 重叠）。
- **改进建议**：与既有 FIX-09 合并执行——把两个函数搬到 `source.ts`。

#### I-09 `tests/types.ts` 文件名读不出它是 `@ts-expect-error` 反例集

- **问题描述**：`tests/types.ts` 共 70 行（参见 `read_file` 输出），全部是编译期类型断言（`@ts-expect-error` 反例），靠 `pnpm typecheck` 执行，**不**在 `pnpm test` 范围。但文件名读不出这一点——新人会以为它是测试工具。
- **所在位置**：`tests/types.ts`
- **严重程度**：**P2**（与既有 FIX-19 重叠）。
- **改进建议**：与既有 FIX-19 一致——`tests/types.ts` → `tests/contract.typecheck.ts`（仅改名，不分片）。

#### I-10 `tests/core.test.ts`（1202 行）与 `tests/vue.test.ts`（1054 行）单文件过大，按规则域分片可读性更高

- **问题描述**：两个测试文件都按时间顺序累加用例，未按规则域分组。`tests/core.test.ts` 同时承载 A01…A21 共 40+ 用例（资源声明、调度、执行、释放、并发限制都在同一文件），找某条规则的用例要全文搜。
- **所在位置**：`tests/core.test.ts:1-1202`、`tests/vue.test.ts:1-1054`
- **严重程度**：**P2**（与既有 FIX-19b 重叠）。
- **改进建议**：与既有 FIX-19b 一致——按规则域（声明与资格 / 调度 / 执行 / 释放 / 失败 / 共享）拆 3–4 个文件，fixture 抽到 `tests/fixtures.ts`；同步更新门禁 #16 的计数。

#### I-11 `examples/main.ts` 单文件 330 行承担 4 个示例页 + `HarnessBridge` + `mountHarness`/`mountShell`

- **问题描述**：`examples/main.ts` 同时定义 `mountHarness`、`mountShell`、`HarnessBridge`（带 `snapshot`/`refresh`/`visibility`/`mutate` 四个动作）与四个示例组件（ListPage/QuotePage/DetailPage/KeepAlivePage）。每个示例组件只在该文件被读一次，公共 harness 的演化与示例组件的演化互相干扰。
- **所在位置**：`examples/main.ts:1-330`
- **严重程度**：**P2**。
- **改进建议**：把 `mountHarness`/`mountShell`/`HarnessBridge` 抽到 `examples/harness.ts`，四个示例组件各占一个文件（`examples/pages/*.vue` 或 `.ts`）；`main.ts` 只剩「挂载哪个示例」的路由代码。

#### I-12 `scripts/observe.ts` 用 `as unknown as` 突破 `private` 字段

- **问题描述**：`scripts/observe.ts`（约 50 行）通过 `as unknown as` 把 `RefreshCore` 强转到内部结构形状，读取 `resources`/`results`/`queued`/`running` 四个字段。这是合法的开发侧观测，但**门禁没有禁止这种访问模式**——意味着任何后续 PR 都可以「为了调试」加新的 `as unknown as` 读私有字段。
- **所在位置**：`scripts/observe.ts:1-50`
- **严重程度**：**P2**。
- **改进建议**：在 `core.ts` 的类注释里显式标「**`scripts/observe.ts` 的快照访问是开发侧契约，更改字段名时必须同步 `scripts/observe.ts`**」，或者把要观测的字段升格为 `@internal` 公开字段（带 JSDoc `@internal` 标记，门禁可以 grep）。

#### I-13 `scripts/check-docs.mjs` 第 1 组「`RefreshCore` 所有权行」只能文字校验，无法应对字段名漂移

- **问题描述**：门禁 #1 断言「README 里的 `RefreshCore` 所有权表」与 `src/core.ts` 字段一致，但只比对**符号存在**，不比对**字段名**。这正是 FIX-02a/#18（README `active`）能漂移多年的根因。
- **所在位置**：`scripts/check-docs.mjs:184-?`、`README.md:25`
- **严重程度**：**P1**（与既有 FIX-02 重叠；本条作为「门禁漏检漂移」的系统性根因）。
- **改进建议**：与既有 FIX-02 合并执行；本条额外加一条纪律——**所有 `[docs] consistent:` 汇总行必须列出本轮新增的检查项**（FIX-05），让门禁覆盖范围与文件头注释自述对齐。

#### I-14 `Resource.controller` 在 `run` 的 `finally` 才清空，但 `releaseIfUnused` 同时改 `controller`

- **问题描述**：`src/core.ts:191-193`（`releaseIfUnused`）先把 `resource.controller` 读出来再 `controller.abort()`，并立刻把 `resource.controller = null`；而 `run` 的 `finally`（`:236-237`）又写一次 `resource.controller = null`。两条路径在同一时刻都可能写这个字段（虽然同步代码不会真撞车），但**两次写同一个字段**的语义不清晰。
- **所在位置**：`src/core.ts:189-194`、`src/core.ts:236-237`
- **严重程度**：**P2**。
- **改进建议**：把 `releaseIfUnused` 改成「先 abort，再让 `run` 的 `finally` 自己清字段」——`releaseIfUnused` 只调 `controller.abort()`，不写 `resource.controller = null`。`run` 的 `finally` 是 controller 字段的唯一写入处。

#### I-15 `vue.ts` 的 `install` 在 `app.onUnmount` 里调 `core.dispose()`，但 `dispose` 已经被 `dispose()` 暴露

- **问题描述**：`src/vue.ts:239` 的 `app.onUnmount(() => { uninstall(); core.dispose() })` 与 `:242` 的 `dispose: () => { uninstall(); core.dispose() }` 重复了 `dispose` 逻辑。如果用户在 `onUnmount` 之前手动调 `dispose()`，`core.dispose()` 第二次调用是幂等的（`src/core.ts:98-100`），但 `uninstall` 第二次调用会把 `installed.value` 置 null（前提是 `installed.value === core`，否则不置）——这个保护又通过 `installed.value === core` 的判等做了。
- **所在位置**：`src/vue.ts:218-247`
- **严重程度**：**P2**——保护路径正确但不直观。
- **改进建议**：把 `uninstall` 与 `core.dispose` 的调用关系写进 JSDoc；把「`installed.value === core` 才允许置 null」这条规则显式化（已有，:230）。

---

### 10.5 总结

#### 10.5.1 维度统计

| 维度 | 问题数 | P0 | P1 | P2 |
| --- | --- | --- | --- | --- |
| 需求分析 | 9 | 0 | 5 | 4 |
| 设计审查 | 8 | 0 | 4 | 4 |
| 实现审查 | 15 | 0 | 4 | 11 |
| **合计** | **32** | **0** | **13** | **19** |

> P0 已清零：FIX-01（`benchmark.mjs` 死指标）已经在上一轮处理；本轮未发现新的 P0。
> P1 集中在三个根因：**资格/读者资格双关（N-01）**、**读取面投影不可单测（D-08 / I-01 / I-02 / I-04）**、**门禁对字段名漂移无感（I-13）**——三者都是系统性问题，需要一次性补足。

#### 10.5.2 与既有 FIX-XX 的关系

| 本章编号 | 重叠的既有项 | 增量 |
| --- | --- | --- |
| N-01 | — | 全新（既有 FIX-10/11 触及但未拆资格词） |
| N-02 | — | 全新 |
| N-03 | — | 全新（既有 R-1 类似但未提） |
| N-04 | — | 全新（与既有 R-3 类似但更具体） |
| N-05 | FIX-04 | 增量：根因是 Config 命名歧义（D-01） |
| N-06 | — | 全新 |
| N-07 | — | 全新（与 R-8 类似但更聚焦 G 字母冲突） |
| N-08 | — | 全新（与 R-2 类似但更具体） |
| N-09 | — | 全新 |
| D-01 | FIX-14 | 增量：佐证「Config」命名是根源 |
| D-02 | FIX-15 | 增量：佐证「Resource」命名冲突 |
| D-03 | FIX-09 | 增量：强佐证 |
| D-04 | FIX-18 | 增量：明确三个模块级 `shallowRef` 是分不开的根因 |
| D-05 | — | 全新（资格定义缺口） |
| D-06 | — | 全新 |
| D-07 | FIX-10 | 增量：设计侧视角 |
| D-08 | FIX-11 | 增量：可测性视角 |
| I-01 | — | 全新（事件总线化） |
| I-02 | — | 全新（语义重叠） |
| I-03 | — | 全新 |
| I-04 | FIX-12 | 增量：观测口径不等的强佐证 |
| I-05 | — | 全新 |
| I-06 | — | 全新 |
| I-07 | — | 全新（数据竞争窗口） |
| I-08 | FIX-09 | 增量：核心并不消费身份编码 |
| I-09 | FIX-19a | 增量：佐证 |
| I-10 | FIX-19b | 增量：佐证 |
| I-11 | — | 全新 |
| I-12 | — | 全新 |
| I-13 | FIX-02 | 增量：系统性根因视角 |
| I-14 | — | 全新 |
| I-15 | — | 全新 |

#### 10.5.3 建议落地顺序

| 批次 | 内容 | 风险 | 门禁收益 |
| --- | --- | --- | --- |
| **P0 收尾** | I-07（`setConfig` 三值非法分支必须整组写） | 低 | 锁住数据竞争窗口；需新增回归用例 |
| **P1 第一批** | N-01、N-05、D-03、D-05、D-07、D-08、I-01、I-02、I-04、I-07、I-13 | 中 | 解决资格双关、读取面可单测、门禁覆盖字段漂移三件事 |
| **P1 第二批** | N-03、N-08、I-05、I-06、I-08、I-12、I-14、I-15 | 低-中 | 清理冗余与小补丁 |
| **P2 第一批** | N-02、N-04、N-06、N-07、N-09、D-01、D-02、D-04、D-06、I-03、I-09、I-10、I-11 | 中 | 命名/拆分/文档纪律，需逐项裁决 |

> 所有 P1 项做完期望：`src/` 行数与分支数变化应 < 10%，`maxCCN` 维持 ≤ 7；用例总数预计从 66 上升 3–5 条（新增 N-02 的 A22、I-07 的 `setConfig` 回归）；README:145 与 README:149 必须同步。

#### 10.5.4 与现有门禁的兼容性

本章新增的检查项若要做成自动化，应在 `scripts/check-docs.mjs` 加：

1. **#21** `Parameters` 字段名 `key` 与 `core.ts` 的 `identityOf` 参数名 `key` 是否一致（解决 I-08 与既有 FIX-09 的残留隐患）；
2. **#22** `Config` 接口字段名在所有 Markdown 文件中必须等于 `enabled` / `every` / `present`（解决 N-05 的再发生）；
3. **#23** `Resource.isEligible` 词条必须不在 §0.4「资格」定义里被独立引用（解决 N-01 的再发生）；
4. **#24** `store.read` 的实现不得调用 `refOf`（解决 I-04 的再发生）。

> 这四条都是已有 #18/#19/#20 思路的延展，写作风格与 FIX-02a/b/c 保持一致；可与 FIX-02 一并提交。

---

## 11. 重审与落地（资深前端开发者视角，2026-09-21）

> 本章是对第 10 章 32 条问题**重新审视**后的可执行版本。三件事：
> 1. **自我修正**：标出原标 P1 但实际无 bug 的 3 条，避免按错判去改代码；
> 2. **收敛**：把 32 条里重叠 / 佐证类的合并，留下 12 条独立问题；
> 3. **可贴 patch**：每条真 P1/P2 给出**修改前 → 修改后**的具体代码、改动**触发**原因、验证命令与回滚点。
> 
> 阅读顺序：11.1（结论）→ 11.2（自我修正，**先读这一节避免按错判动手**）→ 11.3（4 个真 P1 patch）→ 11.4（5 个 P2 patch）→ 11.5（依赖图与执行顺序）→ 11.6（一次性验证脚本）。

### 11.1 重审结论

| 类别 | 上一轮数 | 本轮数 | 差异说明 |
| --- | --- | --- | --- |
| 需求分析 | 9 | 4 | N-03、N-05、N-08 合并到 1 条；N-02、N-06、N-07、N-09 降级 |
| 设计审查 | 8 | 4 | D-05 降级（实际无 bug）；D-03 / D-04 / D-07 保留 |
| 实现审查 | 15 | 4 | I-01+I-02+D-08 合并；I-04+D-03 合并；I-07 降级；I-05/I-06/I-14/I-15 降级 |
| **合计** | **32** | **12** | 20 条合并或降级 |

**关键判断修正**（见 11.2 详述）：

| 原编号 | 原判 | 重审 | 原因 |
| --- | --- | --- | --- |
| I-07 | P1 | **P3**（无 bug，仅注释不清） | `every` 是资格判定的关键变量，非法分支已正确清空；enabled/present 不清是设计选择 |
| D-05 | P1 | **P3**（无 bug，词条名与实现名错位） | `isEligible` 实际不缺「声明还在」——Resource 存在 ⇒ 必有声明者 |
| N-02 | P1 | **P2**（设计可商榷但行为正确） | 暂停页 first paint 偷偷跟一帧**不会渲染**（KeepAlive 失活），是「避免缓存过期」的有意行为 |

**真 P1 收敛为 4 个 patch**（见 11.3）：

| Patch | 一次性解决 | 涉及文件 |
| --- | --- | --- |
| **P1-A** | I-01 + I-02 + D-08（部分） + N-01 + D-05 | `src/vue.ts` |
| **P1-B** | I-04 + D-03 + I-08 | `src/store.ts`、`src/source.ts`、`src/core.ts`、`src/public-types.ts` |
| **P1-C** | I-07（防御性） + I-05（顺带） | `src/core.ts`、`src/resource.ts` |
| **P1-D** | I-13（系统性） | `scripts/check-docs.mjs` |

---

### 11.2 自我修正（**动手前必读**）

#### 11.2.1 【降级】I-07 `setConfig` 非法分支不会造成竞争窗口

- **原标**：P1，「存在数据竞争窗口」——「非法分支只清 `every`，`enabled`/`present` 旧值留着，下一次 setConfig 重新合法时资格会瞬间『合法』一次」。
- **重审**：**不会**。`isEligible(config) = isPresent(config) && config.enabled = (config.every !== null && config.present) && config.enabled`——`every` 是 AND 的关键变量，非法分支把它清为 `null` 后 `isEligible` **必然为 `false`**，与 `enabled`/`present` 旧值无关。
- **代码证据**（src/core.ts:87-93 + src/resource.ts:36-38）：
  ```ts
  // setConfig 非法分支：仅清 every
  config.every = null
  // isPresent 立刻返回 false（every 是 AND 第一项）
  return config.every !== null && config.present
  ```
- **结论**：行为正确，无 bug。**降级为 P3**——只补一行注释说明「只清 `every` 是因为它是资格判定的关键变量」即可。
- **如果做 P1-C 顺手补**：把非法分支也清 `enabled`/`present` 是**防御性**而非纠错——见 11.3.3。

#### 11.2.2 【降级】D-05 `isEligible` 实际不缺口——「声明还在」由 Resource 存在性蕴含

- **原标**：P1，`isEligible` 只判三项，缺「声明还在」。
- **重审**：**不缺**。`isEligible` 是 `Resource` 实例方法，而 `Resource` 实例**只在**有声明者时被 `resourceFor` 创建（src/core.ts:164-172），**且**当最后一个声明者离开时 `releaseIfUnused` 立刻 `identities.delete`（src/core.ts:184-194）——**Resource 存在 ⇒ 必然仍有声明者**。
- **代码证据**（src/core.ts:143-145）：
  ```ts
  isEligible(config: Config, url: string, key: string): boolean {
    return this.identities.get(identityOf(url, key))?.isEligible(config) ?? false
    //              └─ undefined 时短路 false（身份不存在）─┘  └─ Resource 必有 declarers┘
  }
  ```
- **结论**：行为正确，但**词条名与「资格」字面不一致**是真实的可读性问题。**降级为 P3**，但应通过**改名**（见 11.3.1 patch 中的 `isEligible` → `hasOpenPoll`）解决。

#### 11.2.3 【降级】N-02 暂停页 first paint 跟一帧是有意行为

- **原标**：P1，「反直觉行为塞在括注里」。
- **重审**：KeepAlive 失活时**组件实例仍存活**，watchEffect 仍运行。但**失活的组件不渲染**——「偷偷跟一帧」是「写进 `display.value`，但用户看不到」。等用户切回（activated）时看到的是已经更新过的数据。
- **意图**：避免「缓存内容过期」——如果保留最后一帧（不跟），用户切回看到的是失活前的旧数据，反而更糟。
- **结论**：行为正确，但**应该文档化**让首次接触的人不会误解。**降级为 P2**——在 §2.5 或 §3.3 加一句「KeepAlive 失活时若有后续写入，`display` 静默更新但不渲染；切回激活时直接呈现新数据」。

#### 11.2.4 【新增】其它 17 条佐证 / 重叠 类目的的去向

| 原编号 | 去向 | 说明 |
| --- | --- | --- |
| N-01、N-02、N-06、N-07、N-09、D-01、D-02、D-04、D-06、I-03、I-06、I-09、I-10、I-11、I-12、I-14、I-15 | 保留进 11.4 P2 | 命名 / 拆分 / 文档纪律，**不阻塞 P1** |
| N-03、N-04、N-05、N-08、D-03、D-07、D-08、I-01、I-02、I-04、I-05、I-07、I-08、I-13 | 进入 11.3 P1 收敛 patch | 与其它真问题一起解 |
| 其它佐证条目 | 已在 10.5.2 标注「与 FIX-XX 重叠」 | 不重复 |

---

### 11.3 真 P1 收敛 patch

> **执行纪律**：每个 patch 单独一个 commit；改完跑 `pnpm typecheck && pnpm test && pnpm check:docs`，**三门全绿才进下一个**。

#### 11.3.1 P1-A：vue.ts 读取面投影重构（一次性解决 I-01 + I-02 + D-08 部分 + N-01 + D-05）

**根因**：vue.ts 的 watchEffect 把 4 件事（节流窗口、身份划界、失败出口、重入复核）混在一起，且 `submitted` 兼当数据与事件总线、3 个独立 key（`lastReadAt` / `shownKey` / `failedKey`）语义重叠。

**目标**：
1. `submitted` 只装数据；新增 `readTick: ShallowRef<0>` 显式信号；
2. 3 个独立 key 合并为 `readState: { lastUpdatedAt, identity }`；
3. watchEffect 抽成纯函数 `project()`，可单测；
4. 适配层暴露两个具名判定：`canPoll()`（取数资格）与 `canRead()`（读者资格），核心方法 `isEligible` 改名 `hasOpenPoll` 与词条对齐。

**改动 1：`src/resource.ts:56-58`** — `isEligible` 改名 `hasOpenPoll`：

```diff
- /** 一个声明者此刻是否有资格取数：环境允许 ＋ 开启意愿为真。 */
- isEligible(config: Config): boolean {
-   return this.isPresent(config) && config.enabled
- }
+ /** 这一份配置此刻是否对外有取数资格：环境允许 ＋ 开启意愿为真。
+  *  注意：本方法不判「声明还在」——Resource 实例存在 ⇒ 必有声明者。 */
+ hasOpenPoll(config: Config): boolean {
+   return this.isPresent(config) && config.enabled
+ }
```

**改动 2：`src/core.ts:144`** — 调用点改名：

```diff
- return this.identities.get(identityOf(url, key))?.isEligible(config) ?? false
+ return this.identities.get(identityOf(url, key))?.hasOpenPoll(config) ?? false
```

**改动 3：`src/core.ts:64`** — `eligibleEvery` 内调用点改名：

```diff
- if (!this.isEligible(config)) continue
+ if (!this.hasOpenPoll(config)) continue
```

**改动 4：`src/core.ts:33-41`** — `identityOf`/`splitIdentity` 下移到 `source.ts`（与 P1-B 合并，见 11.3.2）。

**改动 5：`src/vue.ts:55-100`** — 状态合并 + 命名清晰化（核心 patch）：

```ts
// 修改前（src/vue.ts:55-100，约 50 行）
const submitted = shallowRef<Parameters | null>(null)
let lastReadAt: number | null = null
let shownKey: string | null = null
let failedKey: string | null = null

const publishData = (cell, parameters) => { /* ... */ }
const publishFailure = (cell, key) => { /* ... */ }
const wakeReader = () => { queueMicrotask(() => { triggerRef(submitted) }) }

// 修改后（约 35 行）
/** 本页当前声明；`null` ＝ 还没提交过。仅数据，不作事件总线用。 */
let declared: Parameters | null = null
/** 显式信号：让 watchEffect 下一拍再读一次。 */
const readTick = shallowRef(0)
/** 「上一帧」是哪一版：合并自原 lastReadAt / shownKey / failedKey。 */
const readState: { lastUpdatedAt: number | null, identity: string | null } = {
  lastUpdatedAt: null,
  identity: null,
}

/** 纯函数：把一格结果投影到两个出口。判定全部由此处显式写出，可单测。 */
function project(cell: ResultCell, params: Parameters, state: typeof readState, every: number | null): {
  data: RefreshDisplay<P, object> | null
  failure: RefreshFailure | null
  nextState: typeof state
} {
  // 数据：同身份同 updatedAt 不抄；窗口未到不抄；never → 还没读过，直接读
  let data: RefreshDisplay<P, object> | null = null
  if (!(state.identity === params.key && cell.updatedAt === state.lastUpdatedAt)
    && (state.lastUpdatedAt === null || every === null
        || cell.updatedAt === null
        || state.lastUpdatedAt + every <= cell.updatedAt)) {
    data = {
      args: structuredClone(params.args) as unknown as ReadonlySnapshot<P>,
      data: cell.updatedAt === null ? null : cell.data as ReadonlySnapshot<T>,
      updatedAt: cell.updatedAt,
    }
  }
  // 失败：换一笔就发，成功清 null
  const failure: RefreshFailure | null = cell.failedAt === null
    ? null
    : { error: cell.error, failedAt: cell.failedAt }
  const nextState = data !== null
    ? { lastUpdatedAt: cell.updatedAt, identity: params.key }
    : state
  return { data, failure, nextState }
}

/** 适配层具名判定：取数资格——与核心 §0.4 「资格」对齐。 */
function canPoll(core: RefreshCore, config: Config, url: string, key: string): boolean {
  return core.readOpenPoll(config, url, key)
}
/** 适配层具名判定：读者资格——满足其一即跟。 */
function canRead(core: RefreshCore, config: Config, url: string, key: string, hasRead: boolean): boolean {
  return canPoll(core, config, url, key) || (!hasRead && !document.hidden)
}

const display = shallowRef<RefreshDisplay<P, T> | null>(null)
const failure = shallowRef<RefreshFailure | null>(null)

watchEffect(() => {
  const tick = readTick.value  // 触发依赖收集（信号通道）
  const params = declared
  const bound = installed.value
  if (bound === null) {
    display.value = null
    failure.value = null
    readState.lastUpdatedAt = null
    readState.identity = null
    return
  }
  if (params === null) return
  const cell = bound.readResult(url, params.key)
  if (cell === undefined) return
  if (!canRead(bound, config, url, params.key, readState.lastUpdatedAt !== null)) return
  const next = project(cell, params, readState, config.every)
  failure.value = next.failure
  if (declared !== params) { readTick.value = tick + 1; return }
  if (next.data !== null) {
    display.value = next.data
    readState.lastUpdatedAt = next.nextState.lastUpdatedAt
    readState.identity = next.nextState.identity
  }
  if (declared !== params) readTick.value = tick + 1
}, { flush: 'sync' })
```

**改动 6：`src/vue.ts:139-157`** — 配置 watcher 用具名判定：

```diff
- const before = key !== null && next.core?.isEligible(config, url, key) === true
- if (next.core !== null) next.core.setConfig(config, next.enabled, next.every, next.present)
- const viewer = currentCore()
- const after = key !== null && viewer !== null && viewer.isEligible(config, url, key)
+ const before = key !== null && next.core !== null && canPoll(next.core, config, url, key)
+ next.core?.setConfig(config, next.enabled, next.every, next.present)
+ const viewer = currentCore()
+ const after = key !== null && viewer !== null && canPoll(viewer, config, url, key)
```

**改动 7：`src/vue.ts:170-200`** — `submit`/`refresh` 改用 `declared`（普通变量）与 `readTick`：

```diff
- submit: args => {
+ submit: args => {
    const bound = currentCore()
    if (released || bound === null) return { status: 'cancelled' }
    let parameters: Parameters
    try {
      parameters = prepareParameters(args)
    } catch (error) {
      return { status: 'rejected', error }
    }
    const result = bound.submit(config, url, parameters)
    if (result.status === 'accepted') {
-     const previous = submitted.value
+     const previous = declared
-     if (previous === null || previous.key !== parameters.key) lastReadAt = null
+     if (previous === null || previous.key !== parameters.key) {
+       readState.lastUpdatedAt = null
+       readState.identity = null
+     }
-     submitted.value = parameters
+     declared = parameters
+     readTick.value += 1
    }
    return result
  },
  refresh: () => {
    const bound = currentCore()
    if (released || bound === null) return
-   const params = submitted.value
+   const params = declared
    if (params === null) return
    if (bound.refresh(config, url, params.key)) {
-     lastReadAt = null
+     readState.lastUpdatedAt = null
+     readState.identity = null
+     readTick.value += 1
    }
  }
```

**改动 8：`src/vue.ts:163-168`** — dispose 清状态：

```diff
  onScopeDispose(() => {
    released = true
    stopWatching()
    currentCore()?.undeclare(config)
-   submitted.value = null
+   declared = null
+   readTick.value += 1
  })
```

**改动 9：`src/core.ts:147`** — `readResult` 保留；新增 `readOpenPoll` 给适配层用：

```ts
/** 适配层用：取数资格（不含「读者资格」）。 */
readOpenPoll(config: Config, url: string, key: string): boolean {
  return this.identities.get(identityOf(url, key))?.hasOpenPoll(config) ?? false
}
```

**验证脚本**：

```sh
pnpm typecheck        # 期望 0 错误；isEligible 改名后所有调用点已同步
pnpm test             # 期望 66 用例全绿（A21/A06 等读取面用例必须仍绿）
pnpm check:docs       # 期望全绿；#15 会扫到 hasOpenPoll 必须在某 .md 里出现，否则门禁红
```

**回滚点**：单文件 / 单 patch，git revert 即可。

**风险**：`flush: 'sync'` 的语义不能改（ADR-89）。`project()` 抽离后必须保持输入输出严格对应。

#### 11.3.2 P1-B：`identityOf`/`splitIdentity` 下移 + `store.read` 改纯读（一次性解决 I-04 + D-03 + I-08）

**根因**：`src/core.ts` 顶层导出身份编码函数但自己不用；`src/store.ts` 必须反向依赖 `core.ts`；`store.read` 含建格副作用。

**改动 1：`src/source.ts` 末尾追加**：

```ts
/** 身份键：`URL ＋ 参数值稳定键` 的字面形式（NUL 分隔）。注册表与结果表共用这一个键。 */
export function identityOf(url: string, key: string): string {
  return `${url}\u0000${key}`
}

/** 身份键拆回两级：分隔符取最后一个 NUL——键里的 NUL 一定被 JSON 编码转义，URL 里可能有。 */
export function splitIdentity(identity: string): { readonly url: string; readonly key: string } {
  const sep = identity.lastIndexOf('\u0000')
  return { url: identity.slice(0, sep), key: identity.slice(sep + 1) }
}
```

**改动 2：`src/core.ts`** — 删除原 `:33-41`、加 import：

```diff
- /** 身份键 `URL ＋ 参数值稳定键` 的字面形式（NUL 分隔）。注册表与结果表共用这一个键。 */
- export function identityOf(url: string, key: string): string {
-   return `${url}\u0000${key}`
- }
- /** 身份键拆回两级（`store.list()` 用）：分隔符取最后一个 NUL——键里的 NUL 一定被 JSON 编码转义，URL 里可能有。 */
- export function splitIdentity(identity: string): { readonly url: string; readonly key: string } {
-   const sep = identity.lastIndexOf('\u0000')
-   return { url: identity.slice(0, sep), key: identity.slice(sep + 1) }
- }
+ import { identityOf, splitIdentity } from './source.ts'
```

**改动 3：`src/store.ts:4-5`** — 改 import 源：

```diff
- import { identityOf, splitIdentity } from './core.ts'
+ import { identityOf, splitIdentity } from './source.ts'
```

**改动 4：`src/store.ts:44`** — `read` 改纯读：

```diff
- const read = (url: string, key: string): ResultCell | undefined => refOf(url, key).value
+ /** 纯查询：取已存在格的当前值；不存在的格返回 `undefined` 且不建 ref。
+  *  建格只发生在写入路径（`write` / `fail`）。 */
+ const read = (url: string, key: string): ResultCell | undefined => {
+   const ref = cells.get(identityOf(url, key))
+   return ref?.value
+ }
```

**验证脚本**：

```sh
pnpm typecheck        # 0 错误；identityOf/splitIdentity 迁移后所有 import 路径正确
pnpm test             # 66 用例全绿；A02/A11（共享取数）必须仍绿
pnpm check:docs       # #1 依赖图打印：store.ts 只 import source.ts，不 import core.ts
node scripts/benchmark.mjs --duration 1000 --runs 1   # cells 终态必须仍为 0
```

**风险**：低。`identityOf` 是纯字符串操作；`read` 改纯读后**唯一可能的回归**是「watchEffect 第一次跑时不再建空壳」——但 `write`/`fail` 路径仍会建格，所以 `display` 仍能被唤醒。

#### 11.3.3 P1-C：`setConfig` 整组写（防御性）+ `Resource.settle` 拆分（一次性解决 I-07 + I-05）

**根因**：I-07 经 11.2.1 重审无 bug，但**意图不清**（注释只说「非法时 every 置 null」，不说为什么 enabled/present 不动）。I-05 是 `settle(at)` 同时写两个字段，语义混杂。

**改动 1：`src/core.ts:85-95`** — `setConfig` 整组写：

```diff
- /** 把这一页的配置写进它自己那份槽并重排调度；三个值非法时只把 `every` 置 `null`（＝这一拍配置非法）。 */
- setConfig(config: Config, enabled: unknown, every: unknown, present: boolean): void {
-   if (this.disposed) return
-   if (typeof enabled !== 'boolean' || typeof every !== 'number' || !Number.isSafeInteger(every) || every < 1) {
-     config.every = null
-   } else {
-     config.enabled = enabled
-     config.every = every
-     config.present = present
-   }
-   this.flushSoon()
- }
+ /** 把这一页的配置写进它自己那份槽并重排调度。
+  *  三个值同时非法时整组置无效（`enabled=false`／`every=null`／`present=false`），防止下一次 setConfig 重新合法时残留旧值触发瞬间「合法」窗口；
+  *  ——尽管 `every` 是资格判定的关键变量（`isEligible` 第一项），清掉它已使资格为 false，但整组写更不容易让读者误解。 */
+ setConfig(config: Config, enabled: unknown, every: unknown, present: boolean): void {
+   if (this.disposed) return
+   if (typeof enabled !== 'boolean' || typeof every !== 'number' || !Number.isSafeInteger(every) || every < 1) {
+     config.enabled = false
+     config.every = null
+     config.present = false
+   } else {
+     config.enabled = enabled
+     config.every = every
+     config.present = present
+   }
+   this.flushSoon()
+ }
```

**改动 2：`src/resource.ts:79-82`** — 拆 `settle` 为 `markSettled` + `markProduced`：

```diff
- /** 结算一次执行：记下结算时刻并标上「这一轮的结果已经产出」（成功与失败都走它）。 */
- settle(at: number): void {
-   this.settledAt = at
-   this.produced = true
- }
+ /** 结算：记下结算时刻（成功与失败都走它）。 */
+ markSettled(at: number): void {
+   this.settledAt = at
+ }
+ /** 标记本轮结果已写表（成功才走；失败另走 `fail`，不调本方法）。 */
+ markProduced(): void {
+   this.produced = true
+ }
```

**改动 3：`src/core.ts:228-243`** — 调用点同步：

```diff
  // 先记结算时刻，再写这一格。
- resource.settle(at)
+ resource.markSettled(at)
  this.sink.write(resource.url, resource.parameters.key, data, at)
+ resource.markProduced()
  // ...
  } catch (error) {
    if (!resource.isCurrent(controller)) return
    const at = Date.now()
-   resource.settle(at)
+   resource.markSettled(at)
    this.sink.fail(resource.url, resource.parameters.key, error, failedAt)
  }
```

**改动 4：`src/core.ts:135-137`** — `refresh` 用具名判定：

```diff
- if (!resource.hasExecution()) this.enqueue(resource, true)
- // 结果已经产出了才需要「再来一轮」；还没产出的话本轮结果就够。
- else if (resource.produced) resource.needsNext = true
+ if (!resource.hasOpenExecution()) this.enqueue(resource, true)
+ // 结果已经产出了才需要「再来一轮」；还没产出的话本轮结果就够。
+ else if (resource.hasProduced()) resource.needsNext = true
```

**改动 5：`src/resource.ts:41-43`** — `hasExecution` 改名：

```diff
- /** 这个身份此刻有没有执行（在队或在跑）。 */
- hasExecution(): boolean {
-   return this.controller !== null
- }
+ /** 这个身份此刻有没有执行（在队或在跑）。 */
+ hasOpenExecution(): boolean {
+   return this.controller !== null
+ }
+ /** 这一轮的结果是否已经写表（成功才标；失败不会标）。 */
+ hasProduced(): boolean {
+   return this.produced
+ }
```

**改动 6：`src/core.ts:273`** — `enqueueDue` 调用点改名：

```diff
- if (resource.hasExecution()) continue
+ if (resource.hasOpenExecution()) continue
```

**验证脚本**：

```sh
pnpm typecheck        # 0 错误；所有 hasExecution/settle/produced 调用点同步
pnpm test             # 66 用例全绿；A01（提交后入队）、A14（显式刷新）必须仍绿
pnpm check:docs       # #15 会扫到 hasOpenExecution/markSettled/markProduced 必须在某 .md 出现
```

**风险**：中。`Resource` 内部接口改名波及 `core.ts` 多处调用点；务必跑全量测试。

#### 11.3.4 P1-D：门禁覆盖字段名漂移（解决 I-13 + N-05）

**改动 1：`scripts/check-docs.mjs`** — 在 #20 之后追加 #21/#22/#23：

```js
// 21) The Config interface's field names must be the canonical three in every Markdown that mentions it.
//     FIX-02a/#18 caught `active`; this catches any other drift (e.g. a future rename to `ready`).
const configMdFiles = ['README.md', '统一刷新管理.md', 'DESIGN.md']
const configFields = [...read('/src/resource.ts').matchAll(/^\s{2}(\w+)\??:/gm)]
  .map(m => m[1])
  .filter(name => name !== 'url' && name !== 'parameters')  // 排除 Resource 的 url/parameters
for (const file of configMdFiles) {
  const text = read(`/${file}`)
  const mentioned = [...text.matchAll(/`Config`[^.]*?`(\w+)`/g)].map(m => m[1])
  for (const field of mentioned) {
    check(configFields.includes(field), `/${file}`,
      `Config field \`${field}\` is not declared in src/resource.ts (declared: ${configFields.join(' / ')})`)
  }
}

// 22) The two identity-key helpers must live in source.ts, not core.ts.
//     FIX-09 已把这两个函数从 core.ts 搬走；此检查防止再次被搬回。
check(!/export function identityOf|export function splitIdentity/.test(read('/src/core.ts')),
  '/src/core.ts', 'identityOf/splitIdentity must live in source.ts, not core.ts (FIX-09)')
check(/export function identityOf/.test(read('/src/source.ts')),
  '/src/source.ts', 'source.ts must export identityOf (consumed by store.ts and core.ts)')

// 23) Resource.isEligible was renamed to hasOpenPoll (11.3.1); documents must reflect the rename.
for (const file of configMdFiles) {
  const text = read(`/${file}`)
  check(!/\.isEligible\(/.test(text), `/${file}`, '.isEligible() was renamed to .hasOpenPoll() (11.3.1)')
}
```

**改动 2：README / DESIGN / 统一刷新管理.md** — 把出现的 `isEligible(...)` 改为 `hasOpenPoll(...)`（门禁 #23 会自动找位置）。

**验证脚本**：

```sh
pnpm check:docs       # 期望：#21/#22/#23 全绿
grep -rn '\.isEligible(' --include='*.md' .    # 期望：无命中
grep -rn 'isEligible' src/                    # 期望：无命中（已全部改名）
```

**风险**：低——纯文档 + 门禁，与代码 patch 同步落地即可。

---

### 11.4 P2 patch（命名 / 拆分 / 文档纪律）

> 这些是命名/拆分/文档纪律，**不阻塞 P1**。每个 patch 单独决策：是否要做。

#### 11.4.1 P2-A：暂停页 first paint 跟一帧的语义文档化（解决 N-02 降级）

**改动**：`统一刷新管理.md §2.5`（约 `:294-300`）追加一段：

```text
边界：KeepAlive 失活的页面，若失活期间结果表被其它页面更新（且 lastReadAt 仍为 null），
`display.value` 会被静默写入新数据；切回激活时直接呈现新数据。这是「避免缓存过期」的有意行为，
不是 bug——失活期间页面不渲染，写入对用户不可见。
```

**验证**：`grep -n 'KeepAlive 失活' 统一刷新管理.md` 有命中。

#### 11.4.2 P2-B：`window` 与 `every` 双关解耦（解决 N-06）

**改动 1**：`src/public-types.ts:39` `every` 字段注释补：

```diff
  /** 刷新间隔（毫秒），只接受正安全整数。它同时是取数间隔与本页读结果表的节流间隔。 */
  readonly every: Ref<number>
+ // 注：与核心侧 `pollEveryMs` / 读取侧 `readThrottleMs` 同源；调用方只看到一项。
```

**改动 2**：`统一刷新管理.md §0.5`「窗口」词条补一句：

```text
| 窗口 | 读取基准与 `every` 的判定：…… | 不是取数间隔（取数与读取各取自同一 `every` 但不同用途） | 适配层 | U12 |
```

#### 11.4.3 P2-C：G 表与门槛表字母冲突（解决 N-07）

**改动**：`统一刷新管理.md §5` 4 个门槛行改前缀 `G01`–`G04` → `M01`–`M04`；同步 README 与 ADR 表格。

**验证**：`pnpm check:docs`（#7 会自动校验所有 M01–M04 引用可达）。

#### 11.4.4 P2-D：`Config` 命名澄清注释（解决 D-01 零成本一半）

**改动**：`src/resource.ts:8` 第一行 JSDoc：

```diff
+ /** 注意：本接口**不是**调用方的配置 `RefreshOptions`——它是本页在核心里的登记槽（ADR-66）。
+  *  命名沿用历史（FIX-14 评估改名代价大于收益）；读者请把 `Config` 当作「Registration」读。 */
  export interface Config {
    enabled: boolean
    every: number | null
    present: boolean
  }
```

#### 11.4.5 P2-E：`Resource` 类名 JSDoc 澄清（解决 D-02 零成本一半）

**改动**：`src/resource.ts:15` 第一行：

```diff
- /** 一个「URL ＋ 参数值」的共享实例：这个身份的全部状态与判定。 */
+ /** 一个**身份**（URL ＋ 参数键）的全部状态与判定；不是 URL 本身，也不是网络资源。
+  *  命名沿用历史（FIX-15 评估改名代价大于收益）。 */
  export class Resource {
```

#### 11.4.6 P2-F：`tests/types.ts` 改名（解决 I-09）

**改动**：

```sh
mv tests/types.ts tests/contract.typecheck.ts
```

**同步**：`README.md:144` 引用路径；ADR 历史行**保持原样**（ADR 是历史记录）。

**验证**：`grep -n 'tests/types.ts' README.md` 无命中；`ls tests/` 见 `contract.typecheck.ts`。

#### 11.4.7 P2-G：测试文件按规则域分片（解决 I-10）

**改动**：把 `tests/core.test.ts`（1202 行）拆为：

```
tests/
├── core.declaration.test.ts    # A01-A07：声明 / 身份 / 资格
├── core.scheduler.test.ts      # A08-A11：调度 / 队列 / 并发槽
├── core.execution.test.ts      # A12-A14：执行 / 结算 / 刷新命令
├── core.release.test.ts        # A15-A19：释放 / 销毁 / 不可达响应
└── core.shared.test.ts         # A20-A22：共享 / 多页 / 跨身份
```

`vue.test.ts` 同样按规则域拆。

**同步**：门禁 #16 的 `tests/x.test.ts N` 断言要重新对应；README:145 的「41 + 25」按新文件分布。

---

### 11.5 依赖图与执行顺序

#### 11.5.1 Patch 依赖关系

```text
P1-A (vue.ts 重构)
   │
   ├── P1-D #23 文档同步（isEligible → hasOpenPoll 文档侧）
   │
   └── 触发 P2-A（watchEffect 重构后 N-02 的语义变化更显眼）

P1-B (identityOf 下移 + read 纯读)
   │
   └── P1-D #22（identityOf 必须在 source.ts）

P1-C (Resource 接口改名)
   │
   └── P1-D 同步 #21 字段名

P1-D 依赖 P1-A / P1-B / P1-C 全部完成后再统一加门禁
```

#### 11.5.2 推荐执行顺序

| 顺序 | patch | 触发的测试 | 触发的门禁 |
| --- | --- | --- | --- |
| 1 | **P1-B** | A01/A02/A11（共享）必须仍绿 | #22 必须绿 |
| 2 | **P1-C** | A14/A21 必须仍绿 | #15 扫新符号 |
| 3 | **P1-A** | A06/A11/A12/A21 必须仍绿 | — |
| 4 | **P1-D** | — | #21/#22/#23 全绿 |
| 5 | **P2-A** / **P2-B** / **P2-C** / **P2-D** / **P2-E** | — | #7 锚点可达 |
| 6 | **P2-F** | — | — |
| 7 | **P2-G** | 用例数变化 | #16 重新对齐 |

#### 11.5.3 风险评估

| Patch | 主要风险 | 控制手段 |
| --- | --- | --- |
| P1-A | watchEffect 同步副作用顺序不能变 | 改前存 `node scripts/trace-core.mjs` 基线，改后逐字节 cmp（vue.ts 改动不会影响 trace-core 输出） |
| P1-B | `read` 改纯读可能暴露「读一下就建格」的隐式依赖 | 全量测试 + benchmark 残留为 0 双兜底 |
| P1-C | `Resource` 接口改名波及 4 处调用点 | 全量测试兜底 |
| P1-D | 门禁可能因文档旧措辞报红 | 文档改全 + grep 兜底 |

#### 11.5.4 度量预期

| 项 | 现状 | P1-A 后 | P1-A+B+C 后 | P2 全做后 |
| --- | --- | --- | --- | --- |
| `src/` 行数 | 814 | 800–820 | 790–810 | 750–790 |
| `src/` 文件数 | 7 | 7 | 7 | 7（P2-F 仅改测试） |
| 结构分支 | 80 | 80–85 | 78–84 | 75–82 |
| `maxCCN` | 7 | 7 | 7 | 7 |
| 用例数 | 66 | 66 | 66 | 64–68（P2-G 拆分后数字按文件分布变） |
| 公共 API | 2 fn + 5 types | 不变 | 不变 | 不变 |
| 模块依赖方向 | 单向 | 单向 | 单向 | 单向 |

---

### 11.6 一次性验证脚本

执行所有 P1 后跑这条：

```sh
#!/usr/bin/env bash
# scripts/verify-p1.sh —— 一键验证 P1-A/B/C/D 全绿
set -e
cd "$(dirname "$0")/.."

echo "[1/5] typecheck"
pnpm typecheck

echo "[2/5] unit test"
pnpm test

echo "[3/5] docs gate"
pnpm check:docs

echo "[4/5] benchmark contract"
# 在途峰值 ≤ maxConcurrent、释放后残留全 0（含 cells）
node scripts/benchmark.mjs --duration 1000 --runs 1

echo "[5/5] no renamed-symbol leftover"
test -z "$(grep -rn '\.isEligible(' --include='*.ts' --include='*.md' src/ tests/ scripts/ examples/ DESIGN.md README.md 2>/dev/null)"
test -z "$(grep -rn 'export function identityOf' src/core.ts 2>/dev/null)"

echo "OK — P1 all green"
```

---

### 11.7 与原 32 条的对照（已合并 / 降级 / 保留）

| 原编号 | 本章位置 | 状态 |
| --- | --- | --- |
| N-01 | P1-A + 11.3.1 改动 5（`canRead` / `canPoll` 具名判定） | **合并** |
| N-02 | 11.4.1 P2-A 文档化 | **降级** |
| N-03 | 见 11.2.4 → 合并到 P2-A 周边裁决 | **降级** |
| N-04 | 11.4 系列已涵盖（`failedAt` 字段保留，加门禁） | **合并** |
| N-05 | 11.3.4 P1-D #21 | **合并** |
| N-06 | 11.4.2 P2-B | **保留** |
| N-07 | 11.4.3 P2-C | **保留** |
| N-08 | 见 11.2.4 → 与 P1-A 同批裁决 | **合并** |
| N-09 | 11.4 系列已涵盖（统一文档压表） | **合并** |
| D-01 | 11.4.4 P2-D | **保留** |
| D-02 | 11.4.5 P2-E | **保留** |
| D-03 | P1-B 改动 2 | **合并** |
| D-04 | 见 11.2.4 → 留到 P2-G 一起做（不动单例拆文件） | **合并** |
| D-05 | 11.3.1 改动 1 + P1-D #23 | **合并** |
| D-06 | 见 11.2.4 → 留到 P1-C 改名时一起做 | **合并** |
| D-07 | 见 11.2.4 → 留到 P1-A 一起做（具名判定） | **合并** |
| D-08 | P1-A 改动 5（`project` 纯函数） | **合并** |
| I-01 | P1-A 改动 5（`declared` + `readTick`） | **合并** |
| I-02 | P1-A 改动 5（`readState` 合并三个 key） | **合并** |
| I-03 | P1-A 改动 5（`project` 合并对称函数） | **合并** |
| I-04 | P1-B 改动 4 | **合并** |
| I-05 | P1-C 改动 2-4 | **合并** |
| I-06 | 见 11.2.4 → 保留作为观察项（`present` 已在 P1-A 涉及） | **降级** |
| I-07 | P1-C 改动 1（防御性整组写） | **降级** |
| I-08 | P1-B 改动 1-2（identityOf 下移） | **合并** |
| I-09 | 11.4.6 P2-F | **保留** |
| I-10 | 11.4.7 P2-G | **保留** |
| I-11 | 见 11.2.4 → 与 P2-G 一起做（examples 分片） | **合并** |
| I-12 | 见 11.2.4 → 留作未来 P3（`@internal` 标记） | **降级** |
| I-13 | P1-D | **合并** |
| I-14 | 见 11.2.4 → 与 P1-C 一起做（`controller` 单一写入） | **合并** |
| I-15 | 见 11.2.4 → 与 P1-A 一起做（dispose 清状态） | **合并** |

> 原 32 条 → 本章 12 条（4 P1 patch + 7 P2 patch + 1 汇总结论）；20 条被合并 / 降级。

---

## 12. 给「下一步执行」的最终建议

按以下顺序执行，每步一个 commit，每步三门全绿：

1. **P1-B**：最干净、最独立，先做可以立刻验证 `store.ts` 不再依赖 `core.ts` 的目标。
2. **P1-C**：`Resource` 接口改名（`hasOpenPoll` / `markSettled` / `markProduced` / `hasOpenExecution` / `hasProduced`），牵动 core.ts 多处但语义清晰。
3. **P1-A**：vue.ts 读取面投影重构——本批最大、最有价值的 patch，先存 trace-core 基线后再做。
4. **P1-D**：门禁 #21/#22/#23 加上去，**让这次的所有命名改动永久不再漂移**。
5. **P2-A ~ G**：命名/拆分/文档纪律，每个单独 commit，每个单独验收。

执行完预期：
- `src/` 行数 790 ± 20；结构分支 ≤ 84；`maxCCN` 维持 7；
- 用例 66 全绿 + 新增 0–2 条（视 11.3 顺手补的回归用例而定）；
- 公共 API 0 变化；
- 模块依赖方向仍单向；
- README:145（用例数）与 README:149（度量）按实测同步。

> **核心交付**：4 个 P1 patch 一次性解决「读取面可单测」「核心依赖方向单调」「资格词条与实现对齐」「门禁防止命名再漂」四件事。
> **保留为 P2**：7 项命名 / 拆分 / 文档纪律，按治理节奏逐项裁决。
> **不做的**：17 项已合并 / 降级的条目——原标过严的（I-07 / D-05 / N-02）按自我修正保留观察；其余已被其它 patch 吸收。

---

## 13. 执行记录与恢复清单（2026-09-21 14:30）

> 本章是第 11 章修复计划的**实际执行账**。P1-B / P1-C / P1-A 已落地并验证；
> P1-D（门禁）因工作区被并发任务改写而暂停，恢复步骤见 13.3。

### 13.1 已落地的代码改动（git diff 可核）

| Patch | 文件 | 改动 | 验证 |
| --- | --- | --- | --- |
| **P1-B**（FIX-09/D-03/I-08） | `src/source.ts` | 追加 `identityOf` / `splitIdentity`（+11 行，与 `parameterKey` 同处数据层） | typecheck 0 错 |
| | `src/core.ts` | 删除两个函数定义，改 `import { identityOf } from './source.ts'`（运行期边 3→1 向下） | 68 用例全绿（当时） |
| | `src/store.ts` | import 源从 `core.ts` 改 `source.ts`——**运行期反向依赖消除**，只余 `import type { ResultCell }` 类型边 | 门禁 PASS（当时） |
| | `tests/core.test.ts` | `:3` 拆 import 路径同步 | |
| **P1-C**（I-07 防御性） | `src/core.ts` | `setConfig` 非法分支**整组置无效**（`enabled=false`／`every=null`／`present=false`）+ 注释说明动机 | typecheck 0 错 |
| | `tests/core.test.ts` | 新增用例「A04/A05 配置由合法转非法时整组失效：资格立即为假，槽内不残留合法旧值」（锁行为防回归） | **69 用例全绿** |
| **P1-A**（I-01/I-02/N-01 适配层半） | `src/vue.ts` | ① `readTick = shallowRef(0)` 独立信号通道，`wakeReader` 改 `queueMicrotask(() => { readTick.value++ })`，删除 `triggerRef` import——`submitted` 回归纯数据职责；② `lastReadAt`/`shownKey`/`failedKey` 三散落状态收拢为 `readState` 账本（`dataIdentity`/`dataUpdatedAt`/`failureIdentity` 保留独立语义，不互相覆盖）；③ 新增 `canPoll`（§0.4 资格）与 `canRead`（§0.5 读者，含 DOM 读 `document.hidden` 不入依赖的原因注释）具名判定；④ `watchEffect` 闸门改 `canRead`、配置边沿检测改 `canPoll` | typecheck 0 错 / **69 用例全绿**（含 KeepAlive 失活、冻结最后帧、节流窗口、重新成为读者等全部读取面行为用例——重构行为等价） |

**已同步的文档**：`README.md:148`（度量行 814→834）、`工程约定.md:116`（68→69 用例、core 42→43）。

**已丢失的文档改动**：`DESIGN.md` §1.3 两处模块清单与 §1.4 模块职责表的 `source.ts` 行补 `identityOf`/`splitIdentity` 描述——随并发任务清空 DESIGN.md 而丢失，恢复时需重做（见 13.3）。

### 13.2 执行中的范围修正（对第 11 章计划的三处收敛）

1. **`store.read` 不改纯读**（推翻 11.3.2 改动 4）：ADR-90 明确「`read` 会在被声明的身份上建空格——这是『第一次写入唤得醒订阅』的前提」。改纯读会让 `watchEffect` 首跑登记不上对空格 ref 的依赖，**后台首写永远唤不醒读取面**（首声明→首写→上屏链路断裂）。保留建格行为。
2. **`Resource` 接口不改名**（推翻 11.3.3 改动 2-6 的 `markSettled`/`markProduced`/`hasOpenExecution`/`hasProduced`）：修复文档 §5「明确不改」清单 + ADR-70 已裁决 `produced`+`needsNext` 两布尔不并三态；`settle` 同属已裁决口径。
3. **`isEligible` 不改名**（收敛 11.3.1 改动 1-3）：改名波及 DESIGN 10+ 处 + 统一文档词条，收益是可读性；本轮改为**适配层内部 `canPoll`/`canRead` 包装**（已落地），核心公共面不动。词条级澄清留 P3。

### 13.3 恢复清单（并发任务结束后执行）

> **已于 2026-09-21 下午执行完毕**，结果见 13.5。

### 13.4 当前工作区状态快照（暂停时点）

```text
git 未暂存：DESIGN.md（外部清空，非我方）、src/core.ts、src/source.ts、src/store.ts、src/vue.ts、tests/core.test.ts
未跟踪：    修复文档.md（本文件）
外部改写：  统一刷新管理.md、ADR.md、交付记录.md、README.md、工程约定.md（14:19–14:23，并发任务）
被删脚本：  scripts/check-docs.mjs、trace-leaves.mjs、check-docs.selftest.mjs（未跟踪、无 git 备份）
度量基线：  7 文件 / 834 行 / 80 分支 / maxCCN 7；69 用例（core 43 + vue 26）
```

### 13.5 最终验证与完成状态（2026-09-21 收尾）

**确认人裁决**：门禁脚本（`check-docs.mjs`／`trace-leaves.mjs`／`check-docs.selftest.mjs`／`benchmark.mjs`）为**故意删除**，不再需要；`DESIGN.md` 已由确认人恢复（610 行，§1.3 两处与 §1.4 的 `source.ts` 行已含 `identityOf`／`splitIdentity` 职责描述，比本计划原稿更完整）；单元测试与集成测试是必须的。

**验证结果**（三门中保留的两门 + 集成）：

| 验证 | 命令 | 结果 |
| --- | --- | --- |
| 类型安全 | `tsc --noEmit` | **0 错误** |
| 单元测试 | `node --test tests/*.test.ts` | **69/69 全绿**（core 43 + vue 26，含 P1-C 新增的「合法转非法」回归用例） |
| 集成测试 | `playwright test`（真 Chrome） | **12/12 全过**（40.4s） |

集成用例覆盖的关键行为（全部通过，证明 P1-A 读取面重构在真实浏览器下行为等价）：
查询列表／行情面板／双组件共享（1s/5s 同参共享、单页暂停冻结、重新进入读回、全部退订）／A05+A14 失败后同步块声明新身份／A14 暂停后显式刷新／A09 满槽排队不绕上限／A02+A06 真实 HTTP 共享与恢复／A13 传输超时槽位释放／A11 换身份互不覆盖／A06 卸载晚到执行不复活／A04+A06 KeepAlive 失活与受控 visibilitychange。

**P2 中顺手落地的两项**（零风险代码注释）：
- D-01：`src/resource.ts` 的 `Config` JSDoc 补「不是调用方的 `RefreshOptions`，是核心持有的登记槽（ADR-66）」；
- D-02：`Resource` 类 JSDoc 补「不是 URL 本身（URL 只是身份的一半），也不是网络资源」。

**代码问题修复完成度**（对照第 11 章 12 条）：

| 项 | 状态 |
| --- | --- |
| P1-A 读取面重构（I-01 事件总线化 / I-02 三状态散落 / N-01 适配层具名判定） | ✅ 已落地 |
| P1-B 身份编码下移（D-03 概念耦合 / I-08 接口错层） | ✅ 已落地 |
| P1-C `setConfig` 整组写（I-07 防御性）＋回归用例 | ✅ 已落地 |
| P1-D 门禁 #21/#22/#23 | ⛔ 按确认人裁决跳过（门禁已删） |
| D-01／D-02 JSDoc 澄清 | ✅ 顺手落地 |
| N-02 暂停页跟帧语义文档化 | 📄 文档项，留 P2 裁决 |
| N-06 窗口双关注释、N-07 G 前缀、I-09 `tests/types.ts` 改名、I-10 测试分片 | 📄 P2 提案，未做（不阻塞） |

**最终改动清单**（`git status`）：

```text
修改： src/core.ts      （P1-B import + P1-C setConfig 整组写）
修改： src/source.ts    （P1-B identityOf/splitIdentity）
修改： src/store.ts     （P1-B import 源）
修改： src/vue.ts       （P1-A 读取面重构）
修改： src/resource.ts  （D-01/D-02 JSDoc）
修改： tests/core.test.ts（P1-B import + P1-C 回归用例）
```

公共 API（2 函数 + 6 类型）、导出面、模块数（7）、`maxCCN`（7）均未变化；`triggerRef` import 已随 P1-A 移除。

### 13.6 P2 执行记录（2026-09-21 下午，确认人圈定全部 10 项）

> 确认人裁决（对话记录）：A 组 N-02／N-06／N-07／N-05、B 组 N-03／N-08／N-04、C 组 I-09／I-10／I-11 全部执行。

| 项 | 落地内容 |
| --- | --- |
| **N-02** | `统一刷新管理.md` §2.5 追加「边界（KeepAlive 失活期间的静默跟随）」段：失活期间 `display` 静默更新不渲染、切回直接呈现新数据、已冻结页面不受影响 |
| **N-06** | §0.5「窗口」词条补「取数周期与读取窗口取自同一个 `every`，但各计各的（到期＝结算时刻＋周期，窗口＝上一版 `updatedAt`＋周期）」；`src/public-types.ts` 的 `every` 字段注释同步双用途说明 |
| **N-07** | 门槛 `G01–G04` → `M01–M04`：统一文档（§3.5 引用、§5 四行门槛、结尾说明）；`DESIGN.md`（§1.1 需求行、硬约束表、两个「> 需求：」行、指引行）；`README.md`（两处）；`工程约定.md`（三行门槛＋正文）；`交付记录.md`（指路行）。ADR 历史行不动。 |
| **N-05** | §0.3「环境允许」词条补「合成后的布尔值就是配置槽字段 `present`，字段名与词条一一对应」 |
| **N-03** | 删除 A20 叶子（不可证伪的「没有冲突可检出」）；`§1.4` G2 行与 `§3.1` 判定行去掉 A20 引用；`tests/core.test.ts` 两条 A20 用例锚点改挂 **A02**（其判定内容「两处声明合并只发一次」与「身份键拼拆」正是 A02 的可证伪部分） |
| **N-08** | A16 前缀 `[行为]` → `[不变量]`，并补「判定主体是内部结构事实，故标不变量而非行为」说明 |
| **N-04** | `examples/pages/quote-panel.ts` 新增 `qp-last-failure` 行：渲染 `task.failure.value.failedAt`（相对时间＋原始毫秒）——`RefreshFailure.failedAt` 从此有画面级消费者；公共 API 未变（方案 B） |
| **I-09** | `tests/types.ts` → `tests/contract.typecheck.ts`；`README.md:141` 路径引用同步 |
| **I-10** | `tests/core.test.ts`（1222 行，43 用例）按规则域拆为 5 片＋共享夹具：`core.helpers.ts`（239 行，`newCore`／`page`／`declared`／`eligible`／`assertQueueConsistent`／`settle`／`sleep`＋`disposeAllCores` 供各片 `afterEach`）、`core.identity.test.ts`（10 例）、`core.refresh.test.ts`（10 例）、`core.core.lifecycle.test.ts`（14 例）、`core.failure.test.ts`（5 例）、`core.schedule.test.ts`（4 例）。用例总数不变（69）。 |
| **I-11** | `examples/main.ts`（330 行）拆为 `examples/harness.ts`（253 行，集成验证台：受控传输＋测试桥）、`examples/shell.ts`（82 行，代表页面外壳）、`main.ts`（17 行，纯入口：`declare global`＋分支装配）；两文件各自持有 `params`，互不依赖 |

**最终验证**（P2 全部落地后）：

| 验证 | 结果 |
| --- | --- |
| `tsc --noEmit` | **0 错误** |
| `node --test tests/*.test.ts` | **69/69 全绿**（5 个 core 分片 + vue 26） |
| `playwright test`（真 Chrome） | **12/12 全过**（41.4s，含渲染 `failedAt` 的行情面板页与 harness/shell 拆分后的全部页面） |

---

## 14. 第四轮执行记录：数据结构设计评估的落地（2026-09-21 下午）

> 问题来源：同一会话的「数据结构设计合理性评估」报告——总判定「不是拼凑、单一真值成立」，
> 列出 10 处语义重载／双份存储／文档冲突。本节记录其中 8 项的落地与 2 项的显式缓办。
> 沿用的既有裁决：ADR-90（`read` 建格保留，不改纯读）、ADR-70（`produced`＋`needsNext` 两布尔不并三态）、
> ADR-65（不设独立执行对象）。**公共 API 改名 `axios` → `http`**（即原 FIX-17）依据用户 2026-09-21 指令
> 「规划并修复上面的问题」执行：包未发布（`version 0.0.0`、`private`），破坏面为零；统一文档 §2 已同步。

### 14.1 落地清单

| 项 | 动作 | 触碰文件 |
| --- | --- | --- |
| R1 `read` 建格口径 | `refOf`／`read` 注释写清「读取即建格＝订阅前提（ADR-90）」；DESIGN §1.4／§2.5 同步 | `src/store.ts`、`DESIGN.md` |
| R2 观测面表述冲突 | DESIGN §2.8 改为「投影在 `scripts/observe.ts`；观测数据出口（`list`/`size`）留 `store` 但不进包出口」，消除与「观测面不住 src/」的自相矛盾 | `DESIGN.md` |
| R3 `every` 不可拆 | 统一文档 §2.1 写死「两个用途取自同一个 `every`、不可拆分，不提供独立的读取节流配置」 | `统一刷新管理.md` |
| R4 执行状态机注释 | `Resource` 三位（`controller`/`produced`/`needsNext`）的合法组合与迁移点写成注释块（对齐 ADR-65/70 的裁定形态） | `src/resource.ts` |
| R5 `isPresent` 改名 | → `isPresentAndValid`：方法额外含「配置有效」，与字段 `present` 同名不同义 | `src/resource.ts`、`src/core.ts`、`DESIGN.md` 附录 B |
| R6 `axios` → `http` | 公共参数名与端口类型 `RefreshHttp` 对齐；示例、测试、README、统一文档 §2、DESIGN 同步 | `src/vue.ts`、`tests/vue.test.ts`、`examples/harness.ts`、`examples/shell.ts`、`README.md`、`统一刷新管理.md`、`DESIGN.md` |
| R7 `readState` 收口 | `dataUpdatedAt` → `dataBaseline`（对齐 §0.5 词条「读取基准」）；新增 `resetReadState`／`resetBaseline` 两个具名重置入口，原四处散落赋值收拢；`publishData` 写清「第一守卫比画面那一版（U12 同一版不重发）、第二守卫比基准（窗口）」与「基线 vs 画面」双份存储的分叉语义 | `src/vue.ts`、`DESIGN.md` §1.5#13／§2.7／A.2 |
| R8 投影关系注释 | `ResultCell` ↔ `RefreshDisplay`/`RefreshFailure` 的投影关系写进两侧 JSDoc | `src/core.ts`、`src/public-types.ts` |
| R9 文档漂移 | controller 迁移点修正：`dequeue`／`startQueued` 只动 `queue`／`running`，不碰把手（DESIGN §1.5#4／§2.6／A.2 三处原文把它们列为把手迁移点，与代码相反） | `DESIGN.md` |
| R10 `visible` 双角色 | 模块级 `visible` 注释写清「watcher 依赖源 vs `canRead` 直读 DOM」，改一处要两处一起想 | `src/vue.ts` |

### 14.2 缓办（留待确认人裁决）

| 项 | 理由 |
| --- | --- |
| `Config` → `PageSlot` 改名 | 词条表「字段名与词条一一对应」（§0.3）＋ ADR-66 锚定；先例 FIX-14 本就留裁决。已由 D-01 的 JSDoc 澄清补偿 |
| `controller` → `execution` 改名 | `tests/core.helpers.ts` 的队列不变量探针与 DESIGN／ADR 多处引用；纯命名收益，与上一项同批裁决更划算 |

> **2026-09-21 补记**：两项已由确认人裁决落地（连同 `needsNext` → `refreshRequested`），见 [ADR.md](./ADR.md) 的 ADR-123 与本文档 §16。

### 14.3 顺带订正

- `工程约定.md` §5 单元验收行按 I-10 的分片结果与 `core.boundaries.test.ts` 的实际数字订正（69 → 78，文件名列表更新）。

### 14.4 验证（2026-09-21，本轮全部落地后）

| 验证 | 命令 | 结果 |
| --- | --- | --- |
| 类型安全 | `pnpm typecheck` | **0 错误** |
| 单元测试 | `pnpm test` | **78/78 全绿**（core 六片 49 ＋ vue 29） |
| 库构建 | `pnpm build` | 通过（7 个声明文件检查、5 个改写、产物形态断言过） |
| 浏览器集成 | `pnpm test:browser` | **12/12 全过**（40.3s，真 Chrome） |

行为等价性依据：78 条单元用例（含读取面节流、冻结、重新成为读者、同一版不重发、发布期间换身份）与 12 条浏览器场景
全部按原预期通过，未改任何断言；唯一对外可见的变化是公共参数名（`axios` → `http`）。

---

## 15. 后续变更补记（ADR-122，2026-09-21）

> 本节只记录**本文档正文与建议被后续裁决覆盖**的部分，正文不回改（它记录的是当轮基线：`src/` 7 文件、66／69／78 用例）。

- **N-04 ／ R-3 的裁决被反向覆盖**：11.4 系列当时选「方案 B：保留 `RefreshFailure.failedAt`、给画面加消费者（`qp-last-failure`）」。ADR-122 改成**方案 A 的加强版**：`failedAt` 字段与 `RefreshFailure` 类型一并删除，失败并入唯一出口 `display`——格子的四个平字段改为 `data` / `updatedAt` / `failed` / `error`，失败也推进 `updatedAt`（＝这一格最近一次被请求更新的时刻）。因此正文里所有「失败出口」「`RefreshFailure`」「`cell.failedAt`」「`display.value.failedAt`」「失败不参与数据窗口」「换一笔就发布」都是**历史快照**，不是现行口径。
- **I-03 的改进方向以另一种方式落地**：I-03 建议「抽纯函数 `project(cell, parameters, …)`」。ADR-122 选了更小的改动——把 `publishData` / `publishFailure` **合并成一个 `publish(cell, parameters)`**（发布返回 `rollback`，供发布中途换身份时撤回这一帧），副作用仍写在 `vue.ts`，没有抽新模块；`project` 纯函数仍是未做的建议。
- **读取面账本简化**：I-02 建议的 `readState = { dataIdentity, dataUpdatedAt, failureIdentity }` 在本次落地时删掉 `failureIdentity`，只剩「画面那一版的身份与基准」两项。
- **门禁编号失效**：`scripts/check-docs.mjs` 已在 §13.5 确认故意删除，本文档里的门禁编号（#15／#16／#18–#20 等）与「反例注入后 `pnpm check:docs` 必须报红」这类验证方式都只是历史描述；现行验证只有 `pnpm typecheck` / `pnpm test` / `pnpm build` / `pnpm test:browser`。
- **`src/source.ts` 已改名为 `src/parameters.ts`**（`prepareParameters` ＋ `identityOf` / `splitIdentity` 职责不变），本文档多处 `source.ts` 均指这一文件。
- 本轮（ADR-122）验证：`tsc --noEmit` 0 错误、`node --test tests/*.test.ts` **89/89 全绿**（core 六片 52 ＋ vue 37）、`pnpm build` 通过、`playwright test` **12/12 全过**（`--repeat-each=2` 无抖动）。

---

## 16. 第五轮执行记录：缓办改名落地（ADR-123，2026-09-21）

> 确认人裁决「需要做」：§14.2 两项缓办改名，连同同轮会话议定的 `needsNext` → `refreshRequested`。
> 纯改名、零行为变化；动机与裁决全文见 [ADR.md](./ADR.md) 的 ADR-123 行。

| 改名 | 波及 |
| --- | --- |
| `Config` → `PageSlot`（接口与全部引用；局部变量 `config` → `slot`；`DEFAULT_CONFIG` → `DEFAULT_SLOT`、`PartialConfig` → `PartialSlot`、`Page.config` → `Page.slot`） | `src/resource.ts`、`src/core.ts`、`src/vue.ts`、`tests/core.helpers.ts`、`tests/core.boundaries.test.ts`、`tests/core.failure.test.ts`、`tests/core.lifecycle.test.ts`、`DESIGN.md`、`README.md` |
| `Resource.controller` → `Resource.execution`（`isCurrent` 参数改 `handle`；DESIGN mermaid 节点同步） | 同上；`tests/core.helpers.ts` 的队列不变量探针同步改引用 |
| `Resource.needsNext` → `refreshRequested`（JSDoc 改写为「忙时到达的刷新命令（容量 1，重复命令合并）」） | 同上；`Resource` 三位状态表注释同步 |

**不动（明说）**：`RefreshCore.setConfig`／适配层 `readConfig`／`RefreshHttp.post` 的 `config` 参数——它们说的是「配置值」，不是登记槽类型；ADR 历史行、本文档历史章节、`交付记录.md`——旧名在历史行文里是事实，不回改。

**验证**：`pnpm typecheck` 0 错误；`pnpm test` **89/89**（core 六片 52 ＋ vue 37）全绿，未改任何断言；`pnpm build` 通过；`playwright test` **12/12**。
