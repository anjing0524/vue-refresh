# 统一刷新需求与设计评审

评审日期：2026-09-16。初次取证基线：[`e61e7d8`](https://github.com/anjing0524/vue-refresh/tree/e61e7d855f416a00480810ce4df834c84652ea7a)。

提交前已对照远端 `9f1f214` 的增量：源码有五项小幅整理，新增一条销毁调度测试；查询接管、首次提交差异及主动改频率重取语义仍在。下文旧基线的行数、测试数和永久链接保留为历史证据，不冒充最新全量复测。Scheduler 重复 disposed 字段已删除，相关条目已更新。

本次任务提到的《统一刷新.md》，仓库中的对应文件实际是[《统一刷新管理.md》](./统一刷新管理.md)。本报告结合该需求、[DESIGN.md](./DESIGN.md)、全部 12 个 src 模块、相关测试、示例及文档检查脚本评审。现有《统一刷新管理-评审.md》是历史评审，本报告重新取证，不继承其未经验证的结论。

**修订结论：先简化需求，再简化实现。推荐将框架收敛为“订阅数据＋主动刷新”，主动与自动刷新共用获取和交付流程。** 原报告“整体架构没有明显过深”的结论只适用于旧契约；它不能证明旧契约本身足够简单。主要减法应来自取消首次／再次提交差异、query 后再 load、改频率即强制重取等特殊语义，而非先减少文件数量。

**本次决策记录：** 用户在本轮对话中认可上述简化方向，并要求更新评审、提交。本报告据此将其列为推荐目标，不再仅作为遥远的可选项。当前《统一刷新管理.md》、DESIGN.md 与源码仍是旧契约，本次只更新评审；接口命名、在途刷新合并规则及迁移验收尚未实现验证。旧 §5.1 决策保留为历史依据，不能用它们阻止本次已认可的需求重审；Pinia、安装接管等与本次减法无关的选择不顺带变更。

## 0. 从根本需求出发：推荐的简单方案

### 0.1 用户真正需要什么

> 多个页面需要同一份数据时，按需持续更新或主动重取，避免重复请求，并防止旧结果覆盖当前意图。

由此保留四条产品规则：

1. **订阅：** 声明数据身份与自动刷新间隔；同一身份共享请求和结果。相同参数再次声明保持幂等，不隐含强制刷新。
2. **刷新：** 明确要求重新获取当前数据；主动刷新和自动刷新走同一条 Source.load、有效性检查与结果交付路径，不再先 query 再 load。
3. **暂停：** 停止该页面的自动更新并保留画面；可见且存活的暂停页面仍能主动刷新。其他页面继续订阅时，不能因这一页暂停而取消共享请求。
4. **正确性：** 旧意图、失效订阅和已销毁实例不能发布；结果副本独立；取消不冒充真实执行结束。

“旧”指相对于页面意图或客户端执行顺序已失效，不承诺服务端强一致或按墙钟判定结果先后。

### 0.2 三项真正减少复杂度的取舍

| 旧规则 | 推荐目标 | 消除的原因链 |
|---|---|---|
| 第一次 submit 普通加入，后续 submit 强制重取 | 订阅声明始终幂等；强制重取使用明确刷新动作 | 不再根据操作历史猜测用户意图 |
| query 独立执行、只写本页，完成后后台重新 load | 主动与自动刷新使用同一获取与共享交付流程 | 删除查询结果转后台的接管过程及紧随其后的第二次请求 |
| 主动改 every 取消并立即重取 | 只重算下次到期时间，保留当前在途请求 | 不再把调度参数变化解释成数据失效 |

改频率后，若按“上次正常结束＋新间隔”已经到期，可以正常排队；“不强制刷新”不等于无论如何都要再等一个完整周期。在途执行完成后按新间隔继续。

有独立副作用、只更新本页或必须绕过共享限流的业务查询，留在页面业务层处理；页面仍须负责取消和过期副作用保护。只有证明这些例外有广泛真实需求，才重新考虑纳入框架。搬出能力会转移这部分责任，不能把文件删除当作业务复杂度自动消失。

### 0.3 将必要边界说清，不再增加一套机制

以下是实现前应落实到正式需求的**推荐默认值**，不是宣称用户已逐项确认或当前代码已经支持：

- **暂停刷新：** 主动动作临时加入同一资源执行路径，允许更新发起页；动作结束后仍保持暂停，不恢复周期刷新。其他有效订阅也接收共享结果，其他暂停页面不接收。临时需求结束且无人订阅时，沿用当前清理策略。
- **页面退出：** 隐藏、失活或卸载取消该页面的临时等待与交付资格；不能取消其他仍有效需求。恢复时按订阅规则处理，不自动重放页面业务副作用。
- **刷新与在途合并：** 建议等待一个在刷新动作之后启动的请求；已有更早在途请求不因点击而反复 abort，在其结束后补一次。开始前的多个刷新动作合并到同一次后继请求；它已开始后再刷新，才要求更晚的一次。失败结算本次等待，保留旧画面；有自动订阅时下周期继续。
- **共享限流：** 主动与自动刷新使用同一队列与并发上限；不再承诺主动动作绕过后台满槽。页面可获得刷新动作完成结果，但等待者取消不等于共享任务取消。
- **数据身份变化：** 新身份立即成为页面当前意图，旧身份结果不能写回；草稿变化不触发操作。新身份已有共享数据时可先交付；若明确要求新取数，再调用刷新动作。
- **无需求后的数据：** 本次沿用最后需求退出即清理，不引入 TTL 或历史缓存。停止执行与删除缓存并非同一根本需求，但当前没有证据值得新增缓存策略。

这些默认值强调共享与可解释的请求时序。尤其“动作后启动”仍需某种代次或门槛，不能承诺删掉全部版本状态。同 Source＋key 跨资源重建的物理重叠问题继续见 §2.1；若要严格禁止，必须等旧执行结束，不能只检查当前 Resource。

### 0.4 目标链路与正确性底线

```text
页面声明订阅／发起刷新
  → 定位 Source＋参数身份
  → 合并需求、按统一队列启动 load
  → 复核执行与接收者身份
  → 写共享结果、复制并交付有效页面
```

Timer 只产生到期需求；主动动作产生一次刷新需求。二者汇入同一执行流程，不再存在“查询成功 → 交付本页 → 后台接管 → 再请求”的链路。

保留参数快照、共享资源生命周期、任务有效性、页面意图隔离、独立结果副本与真实并发计数。按新的行为减少分支后再决定模块去留，不建设通用任务引擎、事件总线或可配置状态机。

下文 §1–§4 是对旧基线的取证，§5–§8 将这些证据转化为新目标下的删减和验证计划。

## 1. 评审问题与证据口径

将任务收敛为以下问题：每个概念是否代表独立的生命周期或正确性事实？每层是否有真实职责？一个用户事件需要多少次跨模块协调？哪些状态可以推导？删除某个状态、回调或步骤后，哪个具体场景会改变？

结论分三类：**事实**来自当前代码、需求或实测；**判断**是对成本与收益的解释；**候选方案**尚未实现，不承诺节省行数或已经保持时序等价。下文 P1 表示优先处理的契约／正确性问题，P2 表示维护问题，P3 表示可选的小幅整理。

下表记录前次基线复测，本次仅修订评审，未重跑或验证新方案。复测使用 Node v24.19.0；读取当前基线源码，借用本机已有依赖目录运行，没有重新安装依赖，不构成干净安装或发布验收。

| 检查 | 本轮结果 | 能证明什么 |
|---|---|---|
| `node --test tests/*.test.ts` | 51 个测试通过，0 失败 | 当前核心与 Vue 测试通过，不代表全部验收变体已覆盖 |
| `node scripts/check-docs.mjs` | 通过 | 文档结构、引用和声明镜像一致；不证明自然语言语义正确 |
| `node scripts/complexity.mjs` | 12 文件、1920 物理行、1100 代码行、133 结构分支；最大函数 CCN=8、最大控制嵌套=3 | 代码行含类型和导入，不含注释；指标沿用仓库脚本，不能直接等同架构复杂度 |
| 同身份替换请求定向复现 | `calls=2, oldAborted=true, running=2, resources=1` | 全局并发允许时，旧请求未结束，新请求已经启动 |
| 浏览器、构建、类型检查、真实业务接入 | 本轮未执行 | 不将历史通过记录当成本轮验证 |

文档检查输出的 132 个叶子是证据账：120 已验证、12 部分验证；其中 1 项是静态证据。它不是本轮执行了 132 项完整行为测试。

## 2. 优先问题：先统一正确性口径

### 2.1 [P1] “一份在途”与“一个有效任务”混用了

**证据。** 需求 §0.1／§1 写同一身份至多一份在途后台请求，并把目标量化成 `load` 并发数 ≤ 1。实现却允许旧的失效任务留在 running，同时启动替换任务：

- [manager.ts 的 enqueueTask](https://github.com/anjing0524/vue-refresh/blob/e61e7d855f416a00480810ce4df834c84652ea7a/src/manager.ts#L399)：先登记新任务，最后 abort 旧任务。
- [scheduler.ts 的 startQueuedTasks](https://github.com/anjing0524/vue-refresh/blob/e61e7d855f416a00480810ce4df834c84652ea7a/src/scheduler.ts#L140)：只检查任务有效性与全局 `running.size`，没有检查同 Source＋key 的旧执行是否结束。
- `startTask.finally` 在真实结束后释放槽位；这部分符合 T12，不能为了满足“一份在途”而提前删除 running。

**复现序列。** 使用 `tests/core.test.ts` 现有 fixture，`maxConcurrent=2`：甲、乙首次 submit 同参，得到一个 Resource 和一个挂起 load；甲再次 submit 同参，乙维持该 Resource；旧 load 忽略 abort、不结算；推进微任务后出现两个 running，仍只有一个 Resource。以下片段可放在该 fixture 定义后的独立临时脚本中运行：

```ts
const f = fixture(2), a = f.page(), b = f.page()
try {
  a.submit({ x: 1 }); b.submit({ x: 1 }); await tick()
  const resource = f.manager.inspect().resources[0]
  a.submit({ x: 1 }); await tick()
  assert.equal(f.calls[0]!.signal.aborted, true)
  assert.equal(f.calls.length, 2)
  assert.equal(f.manager.inspect().running.length, 2)
  assert.equal(f.manager.inspect().resources[0], resource)
} finally {
  f.manager.dispose()
  for (const call of f.calls) call.resolve(null)
  await tick()
}
```

**影响。** 当前实现保证“唯一有效 Task”以及“后台真实未结束数不超过全局上限”，没有保证“同一身份物理执行不重叠”。普通周期轮询不会主动重叠；显式重提、改频率及同 key 重建是不同情形。这里确认的是文档／实现冲突，并非证明旧结果能覆盖新结果。

**旧实现的准确口径。** 可将契约拆成三句话：同一生存期至多一个有效 Task；普通轮询不重叠；显式替换后已取消但未结束的 load 仍占全局槽，并可与新任务短暂并行。这与当前实现一致，但会收窄 §1 原有目标，必须作为契约修订记录，不能称为无行为变化的需求整理。

如果“一身份物理并发 ≤ 1”确是必须目标，则需要在启动阶段等待同 Source＋key 的旧执行结束，并覆盖 Resource 删除重建。仅检查 `task.resource === resource` 不够。还必须定义被阻塞队首是否允许其他身份越过，以及旧请求永久不结束时的进展边界。这是新增调度约束，复杂度会增加，应在正式需求中明确后再实现。

### 2.2 [P2] 几项设计说明比代码实际保证更强

| 位置 | 核查结果 | 建议 |
|---|---|---|
| DESIGN §5.1“一次 flush 只读一次时钟” | `flush` 将一次 now 传给 enqueueDue／dueAt；有下一到期项时，`setWakeup` 还会再读 now；同步交付重入也可能执行其他读时钟路径 | 改为“本轮到期判断共用一次 now；设置 Timer 时重新计算剩余延迟” |
| DESIGN §3.2“每毫秒一次也要 285 年” | `Number.MAX_SAFE_INTEGER / 1000 / 86400 / 365.25 ≈ 285421 年` | 修正数量级；耗尽仍可视为非正常业务场景，不能据此否认已有测试 |
| DESIGN §3.3 配置状态字段 | 表里写 `configuration`、`reportedConfigurationError`，代码为 `snapshot.current`、`reported` | 使用真实字段名，或明确这些是概念名称，减少追查成本 |
| model.ts 的 Task／Clock 注释 | 仍指向 `Manager.queue/running`、`Manager.scheduleNextWakeup`，当前归 Scheduler | 更新所有者与方法名，避免旧设计残留 |
| DESIGN §1“运行时分层” | 文件清单包含类型；model.ts 因 ActivityKind 也有运行时值，不能一概称为纯类型层 | 分开描述静态依赖、状态所有权和事件调用路径 |

这些修订不需要变更业务行为。文档门禁通过仍能出现这些问题，说明继续增加行数、标题或编号检查并不能替代语义核对。

## 3. 链路是否过长、分层是否过深

### 3.1 以业务阶段度量链路，不把辅助函数数当延迟

首次 submit 的主路径是：

```text
同步：组件 submit → Manager 接纳操作 → 参数准备 → 保存 Submission
微任务：Scheduler.flush → 协调 Handle／attach → 到期登记 → FIFO 启动 load
异步完成：身份复核 → 复制结果 → 写共享 Store → 逐订阅复核与发布 Display
```

独立 query 是：

```text
同步：入口闸 → 接纳新操作并退出旧活动 → 准备参数 → 立即执行 runner
异步完成：身份复核 → 复制／发布本页 → 结算并清活动 → 请求 flush
后续：资格成立时，由共享后台路径发起新 load
```

这里有同步、微任务、异步完成三个执行阶段；单次阶段内部的辅助函数调用不应被描述为额外异步“跳数”。共享交付又随订阅数量变化，所以没有一个适用于所有场景的“11 跳”或“20–25 步”常数。

**判断：主链条不短，但大部分步骤有不可省略的语义。** 参数快照、并发调度、异步有效性检查和独立交付各解决不同问题；压成一条 Promise 链不会消除这些责任。真正容易增加业务时延和请求量的是 query 后再 load，这来自 Q04／附条 A 的明确要求，不是文件多导致。

### 3.2 分层是适配与机制分工，不是层层转发

| 模块组 | 当前收益 | 判断 |
|---|---|---|
| vue／app／store | Vue 生命周期、安装环境、Pinia 存储与核心隔离 | 保留；分别有不同变化来源 |
| manager | 操作接纳、资源关系、后台有效性与交付顺序的单一编排者 | 401 代码行、最大 CCN=8；不能仅凭 642 物理行认定 God Object |
| scheduler | Timer、FIFO、真实并发槽唯一所有者 | 保留；当前是领域调度器，不是通用任务池 |
| query | 旧契约下负责独立执行、绕过队列和 commit | 旧契约下合理；采用 §0 后业务查询外移，评估删除此路径及 QueryHost |
| source／delivery／diagnostics | 参数身份边界、结果所有权、通知异常隔离 | 保留小型内聚函数，无须再增加服务层 |
| model／public-types／index | 内部模型、公共契约、导出边界 | 主要是类型与模块边界，不是额外运行层级 |

**没有整体分层过深的证据。** 建议在说明上收敛成“适配层—刷新核心—边界与端口”三个责任区域，但不为凑三层移动文件。保留核心不依赖 Vue／Pinia／HTTP 的实质约束。

### 3.3 模块调用最复杂的地方是调度往返

```text
Manager.requestFlush → Scheduler.flush
  → host.reconcileHandles → Manager.synchronize／attach
  → Scheduler.enqueueDue → host.enqueueTask → Manager.enqueueTask → Scheduler.add
  → Scheduler.startQueuedTasks → host.startTask → Manager.startTask
  → 异步 finally → Scheduler.release／requestFlush
```

这不是 import 循环；是控制流双向协作。窄端口控制依赖面，但没有消除往返。

当前切分确有理解成本：Scheduler 除了队列和 Timer，还读取 Resource／Subscription 决定到期；Manager 又负责创建它要执行的任务。不过只存在一个具体调度器，且 §5.1 已裁决保留边界，**不建议本轮为“纯执行器”目标再引入任务列表快照、下一唤醒计划对象或通用调度协议**。它们会增加失效与重入校验面。

若未来再评估切分，应先比较两个可运行方案的事件轨迹，而非预计净减行数：① 原边界；② 单个 flush 编排归属，执行池只负责槽位。必须证明 F08／F11／F12、取消即时结算及正常进展都不变。仅搬迁 `dueAt` 不会自动改善这些性质。

## 4. 使用了哪些设计原则，是否用过头

以下是从结构识别出的原则，不声称作者明确采纳了某套方法论。

| 原则 | 实际落点 | 评审 |
|---|---|---|
| 单一事实来源 | enabled 由业务持有；Display 由 Vue 持有；queue/running 只在 Scheduler；最短间隔和资格派生 | 主要收益明确，继续保持 |
| 依赖倒置／端口适配 | Clock、ResultStore、QueryHost、ScheduleHost；核心不导入 Vue／Pinia | 合理；接口本身不增加运行跳数 |
| 接口隔离 | QueryHost 4 项、ConfigurationHost 3 项、ScheduleHost 5 项 | 无额外转发类；删除几个类型行的收益很小，不应称“零风险架构优化” |
| 让非法状态难以表达 | activity 为 QueryRun／Subscription／null，避免 query 和订阅双活 | 合理；不再改成几个互相约束的布尔值 |
| 信息隐藏／所有权 | 参数冻结、结果复制；生命周期结束后对象身份失效 | 属于正确性要求，不能用浅拷贝或共享 DTO 替代 |
| 最新操作生效／失效隔离 | operationId、Resource 生命周期、Task 版本；外部效果后复核 | 三个时间域不同，不能为减少计数器强行合并 |
| DRY／KISS／YAGNI | 参数 prepare 一次、接管机制共用、不增加查询库或第二身份维度 | 基本克制；可减少重复解释和无实质保护的极薄方法，而不是再建通用状态机 |

过度倾向主要体现在**将合理原则机械化为固定实现形状**：文档规定唯一 Timer、固定文件层级、Manager 八段标题顺序、完整类型镜像等。部分是可测试的工程选择，部分仅是维护格式。应分别标明，不把换函数位置、合并标题视作行为变更。

现有检查脚本已经允许登记同层边，并非完全僵化；建议保留公共契约一致性、禁止核心依赖 UI 和真正的循环依赖检查。标题顺序、模块枚举等低价值维护项可在实际改动触及时重新衡量，不另建一套更大的门禁系统。

## 5. 需求重审：哪些改变语义，哪些保持不变

旧设计的主要原则是单一事实来源、接口隔离和失效身份保护；这些仍然有价值。新的减法更直接依据 KISS／YAGNI：一个真实用户意图对应一个概念，没有证据的特殊语义不进入公共工具。DRY 应统一取数流程，不应强行统一页面的业务副作用。

| 项目 | 处理 | 关联旧契约／验收 |
|---|---|---|
| 首次与后续提交区别 | 改为幂等声明与显式刷新两个意图 | U03、P07、R06／R07：按新动作重新定义预期 |
| query 成功／失败／恢复后再 load | 删除接管机制；成功统一交付，失败保留画面 | U04–U06、U10、附条 A、Q04、S02、D 组及相关 F／B 门槛场景 |
| 主动改频率强制替换 | 改成仅重算到期，不替换当前请求 | U11、T04；继续区分到期与强制重取 |
| 暂停后主动刷新 | 保留能力，改成共享执行路径中的临时需求 | Q01、Q13：暂停不改变，但“不写共享／不占槽”的旧预期改变 |
| 隐藏、失活、卸载后的停止交付 | 保留；覆盖临时需求与长期订阅 | L 组、U08／U09／U21 |
| 参数身份、晚到隔离、副本独立 | 保留正确性，不因统一流程放宽 | U01／U02／U14／U16、P02／P03、R05、L09 |
| Pinia、三态配置、安装接管 | 本次不顺带更换；仅查询相关状态在查询路径删除后再核对是否仍需要 | 现行 §5.1 及 U18／U21 |

这是有意修改旧产品语义，不是对旧实现做等价重构。历史测试不能不加区分地要求全部保持：未改变的正确性测试保留，明确改变的行为预期先在需求中修订，再更新实现与测试，记录依据。

目前没有调查证据证明每个业务页面都能迁移。正式改实现前，应核对查询列表、行情面板、双组件共享三个现有示例的调用点，重点识别 runner／commit 的额外副作用以及“只有本页更新”的真实用途。用户已认可简化方向，不需要为重复确认方向暂停；发现与该目标不兼容的真实需求时，再报告具体冲突。

## 6. 状态能否合并收敛

**先删除产生状态的旧语义，再判断剩余状态是否独立。** 下表前半是旧契约下的独立性判断，不表示新目标必须保留全部类型。 不建议引入一个总 `status`，把页面意图、共享任务、物理请求、交付资格全塞进同一个枚举。

| 状态／对象 | 判断与反例 |
|---|---|
| Submission 与 activity | 保持分离：失活后没有活动，但必须保留已准备参数和恢复要求；合并会迫使 inactive 分支再携带同样信息 |
| QueryRun 与 Subscription | 旧契约下不可硬合并；新目标移出独立 runner 后，可删除 QueryRun／QueryHost／activity 的查询分支，以订阅和临时等待者表达需求 |
| Resource.task 与 running | 保持分离：前者是当前有效任务，后者包括已失效但仍未结束的执行；本轮复现正好证明二者不等价 |
| StoreEntry 与 Display | 保持分离：query 只改 Display；退出页面冻结，其他页面仍更新共享结果 |
| operationId／resourceId／taskVersion | 保留页面意图、资源生存期和执行代次三个不同概念；新实现可用对象身份替代部分计数，但不能将三个域误当成一个 |
| lastSettledAt 与 updatedAt | 保持分离：失败也推进周期，却不产生新结果；单调时间与墙钟也不能混用 |
| configuration.enabled 与 lastEnabled | 当前值和最近一次已知值不同；true→未知→false 需要历史事实 |
| flushPending 与 cancelTimer | 一个表示待处理微任务，一个表示将来到期唤醒；新 flush 排入后旧 Timer 尚未清理时可同时存在 |
| Manager.disposed 与 Scheduler.disposed | 最新 `9f1f214` 已删除后者，调度入口统一读 host.isDisposed()；这是已完成的状态收敛，不再列为待办 |

### 6.1 新目标下可删除和仍需保留的状态

**可评估删除：** firstOperation；独立 QueryRun 及其 commit／结算路径；query 后后台接管专用分支；仅为区分“暂停中 query”与“关闭时 query”而存在的历史边沿状态。每项都应检查所有读写点，不按名字直接删字段。

**可收敛：** 原 Submission 中用于“查询快照防后台旧结果回退”的 Delivery 状态，在不再产生私有 query 快照后不应原样保留。主动刷新仍要求合格的新请求，应把该要求归属于一次刷新等待者；多个等待者可以等待同一任务代次，结束或失效后释放。是否能用任务引用替代版本门槛，须同时验证合并、替换与资源重建。

**不能删：** 页面当前意图、订阅资格、共享实例身份、当前有效任务与尚未真正结束执行的区别、最近正常结束时间、结果产生时间、页面独立快照。暂停页需要保留旧画面，不能直接绑定共享 Store。

不承诺状态数一定降到某个数字。目标是消除独立查询与后台接管的第二套转换，同时用最少事实表达主动刷新仍需要的新鲜度约束。

## 7. 实施建议：需求减法优先于代码整理

### 7.1 主线完成后再做的小幅整理

1. **先修说明。** 按 §2.2 更新不准确描述；按 §3 给出三阶段主流程；把复杂度指标与架构判断分开。保留 §7 覆盖清单的位置，符合既有裁决。
2. **消除没有额外语义的包装候选。** `setSubmission` 仅两处调用、内部只有 `handle.submission = {parameters,delivery}`；可内联以减少阅读跳转。`setRequirement` 也是赋值，但集中定位所有门槛变更有审计价值，建议保留。不要把 beginOperation／releaseActivity／forgetActivity 一并内联，它们有不同顺序和作用。
3. **把“事实／派生／历史”写清。** 现有所有权表与 §6 对照即可，不另建状态总表、状态机框架或更多 getter。
4. **限制评审文档的重复。** 需求讲可观察行为，设计讲所有权、执行顺序和不变量；示例与完整声明可引用既有入口。若调整现有类型镜像门禁，要作为独立文档维护变更，不在行为重构时顺手删除。

这些是候选整理，不以估算“减少 100 行”作为收益证据。除文字纠错外，即使只有两处赋值内联，也应检查时序与现有回归结果。

### 7.2 当前不推荐的“简化”

- **不把 nextIdentity 直接改成 throw。** 当前三个计数器耗尽都走 dispose，并及时取消 pending query。`tests/core.test.ts` 的 `sequence exhaustion: all three counters dispose the manager once and cancel pending work` 明确覆盖。改 throw 会改变失败与清理路径，绝非“没有测试影响”。
- **不为少几行改命名空间生成。** `createRefreshManager` 构造时就调用 createNamespace，早于 install 的 SSR 分支；“SSR 不调用”与源码不符。改模块计数器还需处理多份包或热重载使用同一 Pinia 的碰撞，当前收益不足。
- **不删 await／abort／Store 写入／Display 发布后的身份复核。** 回调可以同步重入；单看同一函数里判断重复，会误删正确性边界。
- **不要只改并发计数而保留旧 query 契约。** 新目标明确统一限流与交付之后，再移除旧绕过路径；需要绕过的业务操作由页面承担。
- **不把深复制改成浅复制，不缓存第二份 enabled／eligible／interval。** 前者损失隔离，后者增加同步点。
- **不引入通用事件总线、插件管线或可配置调度策略。** 当前固定需求没有证明需要这些扩展轴。

## 8. 验证与落地顺序

| 顺序 | 事项 | 完成条件 |
|---|---|---|
| 1 | 以 §0 为主线修订正式需求，核对三个代表页面 | 明确订阅、主动刷新、暂停与退出；标记废止的 query 接管／首次差异／改频率重取规则；落实在途合并和限流语义 |
| 2 | 根据行为变化调整设计和验收 | 给出一条获取交付链路；列清可删状态与仍需事实；将 §2.1 的物理并发口径写准确 |
| 3 | 实现公共获取路径，再移除旧查询专用机制 | 普通订阅与主动刷新共用 load 和交付；暂停不意外恢复轮询；业务副作用迁移到明确调用方 |
| 4 | 回归未改变的正确性，并验证新的行为 | 新预期有契约依据；检查请求次数、交付对象、取消、真实槽位和实际进展，不能只看文件变少 |

至少覆盖以下场景：

- 两页同参订阅只首查一次；重复声明不强制重取；明确刷新不再触发 query→load 双请求。
- 暂停后刷新一次，本页更新但不恢复轮询；其他有效订阅更新，其他暂停页不更新。
- 改频率不 abort 当前请求；按新间隔计算下一次，缩短后已到期能正常执行。
- 刷新发生在请求启动前、执行中、结束后；多个刷新合并；失败／取消后的等待者全部正确结算。
- 参数 A→B 后 A 晚到；最后需求退出后同 key 重建；旧成功、旧失败、旧 finally 都不影响新实例。
- 槽满、请求忽略 abort、等待者取消、页面退出和 dispose；物理槽只随真实结束释放，有效任务最终能启动。
- Store／首个页面通知同步重入；后续接收者复核有效性；各页面与共享结果之间没有可变别名。

本次交付的是更新后的评审。上一轮 51 个基线测试通过只能说明旧实现的已测行为，新目标尚未实现或验证。下一步应从正式需求与验收的减法开始，不先重排模块，也不将旧特殊语义换个名字保留下来。
