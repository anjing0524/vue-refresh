# ADR：已裁决的产品边界（决策记录）

本文件是**决策记录**：每条记录一次已关闭的取舍——评审或结构提案的采纳与不采纳、结论与依据、
关联的规则与验收。它不构成对外承诺（对外承诺在 [`统一刷新管理.md`](./统一刷新管理.md) §1–§3），
也不承载行为细节（内部字段与所有权见 [`DESIGN.md`](./DESIGN.md) §3）。

**维护规则**

- 每条决议一经登记即**已关闭**：不再作为未决门槛，也不新增 §7 覆盖子项；其验收落在既有条目。
- **被否的提案不给第二入口**：需要重启议题时先回到本文件的对应条目，用它的依据判断是否成立；
  只有出现行为或契约层面的新证据才重开，措辞与结构偏好的重提不构成新证据。
- 编号 `ADR-01`… 一经发布**不复用、不重排**；新增决议追加在末尾。
- 「结论与依据」就是该条的判定说明；关联列指向 [`统一刷新管理.md`](./统一刷新管理.md) 的 §1–§4
  与 [`DESIGN.md`](./DESIGN.md) 的对应章节。
- 历史行文中的「本文件」「本节」指本文件；「§5.1」是搬迁前在统一文档中的位置。
- 门槛数值只能由确认人给出，实现者不得静默填入或声称已达标（与统一文档 §5 同规则）。
- **外部评审必须声明所读的提交**（或明确说明「基于工作树」）：本仓库的收缩很快，一份停在旧提交的
  评审会把已经删除的结构当成现状。2026-09-17 那份审查读的是落后两个提交的 `1cbb761`，
  它列出的 10 个取值里有 6 个当时已不存在，建议里还包含把 ADR-46 已经删掉的刷新回执请回来。
  2026-09-17 之后这一条由门禁接管：`scripts/check-docs.mjs` 第 17 组扫规范正文里的**退役术语**
  （`caller`／`ErrorOrigin`／`CancelReason`／`RefreshError`／`readSnapshot` 等），历史只在 ADR.md 里引用。

## 已裁决清单

| ID | 议题 | 结论与依据 | 关联 |
|---|---|---|---|
| ADR-01 | 交付面是否给出结果新旧 | **给出**：这是产品取舍——页面要显示「数据多旧」，而结果时间是框架手上唯一可靠的依据。字段记法、发布时机与后加入者读到的取值以 §3（U16）为准，验收落 A03。 | U16、A03、§2.5 |
| ADR-02 | 交付面是否给出实时「正在刷新」 | **不给出**：这是产品取舍——框架的在途事实是「某个实例正在取数」，共享下它并不等于「这一页的数据正在更新」，做成公开布尔值会让页面显示的状态与它手里的事实不同步。页面若确实要显示「刷新中」，只能自管一个本地标记：在发起 `refresh` 或 `submit` 后置位，按 `refresh` 返回的 Promise 结算或 `submit` 的同步 `SubmitResult` 清除，`onError` 到来时也清除。**不能只等 `display.updatedAt` 前进**：失败或取消的刷新不会产生新结果，此时时间不会前进。这个标记不代表真实在途，框架不代管、也不保证一致。 | U15、U16、U19 |
| ADR-03 | 多租户/会话隔离是否新增 `scope` / `namespace` | **不新增**：身份必须只有一个函数（Source 身份＋完整参数值，取值规则见 §3（U02）），否则 Store 键、`readSnapshot`、交付门槛与实例重建都要引入第二个维度；边界由独立 Source 或参数表达，框架不做认证语义猜测。 | U02、§1 不纳入 |
| ADR-04 | `readSnapshot` 是否保留 | **保留**，按「不保证新鲜度」交付（用户已确认不交付时间）：典型场景是另一个组件或刚挂载的页面在没有订阅时直读共享结果做首屏预填，不创建 Resource、不延长生存期。它是不经交付门槛的读取入口，取值规则见 §3（U17），验收落 A06。 | U17、A06 |
| ADR-05 | 资格（U25）失败原因是否对外暴露 | **不暴露**：这是产品取舍——资格是内部推导事实，暴露它就要为「部分成立」定义对外语义并长期维护，而它不改变调用方能做的任何事。排查靠调用方自己手里的事实：`enabled` 由它写入、失活与隐藏是页面事实、是否已提交它自己清楚，框架不再回报一遍。若将来确实需要只读诊断，应作为独立开发者特性单独裁决。 | U25 |
| ADR-06 | 覆盖清单与文首状态行是否移出规范文件 | **不移出**：§8.1 已把 §7 标为非规范性，对外承诺只由 §1–§3 承载；移出要跨文件查覆盖项，并因 §8 顺移改写全部 §8 引用。文首状态行改为与实现事实一致，不再重复进度明细。§7 在原位置压缩为条目式（只留工作单元与判定范围），压缩不等于移出，本裁决不受影响。 | §8.1、README |
| ADR-07 | 不纳入哪些能力（§1 的边界清单） | **不纳入**：本地时钟与动画、历史回放推进、下载副作用、通用保存封装、自动保存重试、跨标签页共享、通用缓存失效、分页封装、业务权限判断、认证语义猜测、服务端强一致承诺。本条是这份清单的唯一维护入口，§1 只留指针与三例。 | §1 |
| ADR-08 | 私有 Pinia 分区是否改为内存注册表 | **不改为**：U21/S07 要求每个 App 独立的私有 Store；且分区表必须浅持有——被 Pinia 深响应式化成 Proxy 后，交付副本的原生复制会抛错，「Store、各页面与 `readSnapshot` 之间没有可变别名」随之失效。收益（少一个 peer 依赖、少一个适配模块）小于删除该验证面与依赖契约变更的代价。 | U21、S07、L09 |
| ADR-09 | 配置 getter 读不到时是否降级为「配置无效」 | **不降级**：读不到保留 `null`、未知开关不猜为 `false`、未知不覆盖开启边沿是已声明承诺，验收落 B08/F10/A07.09；降级会把「getter 抛错」变成静默停刷，或误杀暂停期间的单次查询。 | U18、B08、F10 |
| ADR-10 | 安装矩阵是否删除「原地接管」 | **不删除**：同 App 重建（HMR、会话切换）既要只 `provide` 一次，又不能留下指向旧实例的第二份归属，靠的就是已 `disposed` 的原实例让位；删除要求应用侧重写重建路径，并反转既有变体。 | U21、A05（10）、S07 |
| ADR-11 | 内部窄端口与调度职责切分是否重构（删 `ConfigurationHost`／把到期计算搬回 `Manager`） | **不重构**：窄端口是各适配面对外的最小事实集；把到期计算搬回编排层会让 flush 的批处理与交付时机重新暴露在同步栈上，端口也缩不到 3 项，净收益为 0 或负。 | F08、T11、M04 |
| ADR-12 | 第十二版评审（元评审）提出的结构级提案 | **维持（不新增改动）**：S-A／S-B／R-A／R-B／R-C 五项已在本文件关闭，按「被否的提案不给第二入口」不重开；两项新提案实测后亦不采纳——`nextIdentity` 改 `throw` 实际只减 5 行（不是 −12），且会把 §2.4 的 `submit` 返回契约改成同步抛错；`createNamespace` 收敛两级会把非安全上下文里唯一的 Crypto 随机源降级为时间戳／计数器（`getRandomValues` 是 `Crypto` 中唯一可用于非安全上下文的成员）。第十二版对「少一跳」（应表述为「去掉一个回调往返」）与 Pinia 响应性消费者的措辞纠正成立，不改变结论。 | 本次复核；`manager.ts:564-571`、`app.ts:34-42`、§2.4 |
| ADR-13 | 代码审查 v5（`CODE_REVIEW.md`）提出的结构级提案 | **不采纳（不改 src）**：该报告未指出任何行为或契约缺陷，20 项均为结构偏好。ARCH-11／ARCH-12／API-01／API-02 属ADR-11 已关闭的窄端口与调度职责议题；ARCH-10 与 §1.3 六协作者蓝图、MOD-02／MOD-03／ARCH-13／MOD-04 属 §6.2 禁止的新增模块与架构层，需先裁决再议；STATE-01／FLOW-05 与 DESIGN §3.6「两种要求＋没有要求、不建立额外枚举副本」冲突（外层 `null` 与内层 `null` 不在同一层级）；STATE-02／API-04 会新增 `Handle` 持久字段。按原样实现会失败的四处硬反例：`requestFlush` 改 private 会破坏 `QueryHost`（`query.ts:50/99`）；`instanceof QueryRun` 对 `interface` 不可实现；删循环内 `currentTask` 复核会丢掉 F14／C05 的同步重入防线；`setBrowserVisible` 只同步独立查询会让隐藏页继续后台刷新。其度量基线另有 9 处与代码不符（方法数 12／16／34、分支 56／94、测试行数 510／298、`Handle` 8 字段、闭包 42 行等），实测 maxCCN 为 8 而非 ~12。 | 本次复核；§6.2、DESIGN §3.6、F14、C05、U25。其中 `QueryHost`／`QueryRun`／`commit` 已随ADR-16 的需求重写删除，该行的否决理由按当时的契约成立 |
| ADR-14 | 第十三版评审（根本简化问题清单）提出的结构级提案 | **整体不采纳，只收下 5 项行为中性小改**：A1／A2+A3／C1／C2(1) 都是本文件已关闭议题的重开（窄端口见 ADR-11、`Delivery` 三态见 ADR-13、配置降级见 ADR-09），本版给的新证据（测试零引用、补出第 4 个消费点）不触及各行的依据。D1（`nextIdentity` 改 `throw`）另有实测反例：`tests/core.test.ts` 的"sequence exhaustion"用例已覆盖三个计数器并断言观察者日志 3 条，改 `throw` 后 `submit` 变成同步抛错（违反 §2.4「`disposed` 返回 `cancelled`」，与ADR-12 已否决的是同一修法），该用例直接转红；其"耗尽分支无测试（按日志字符串 grep 零命中）"是取证方法错误。第一档声称合计约 −48 行，逐项实测 −23（含 D1）／−18（不含 D1）；A4／A5 按纯减法不可实施（要让编排层直接持有 `queue`／`running`，或读调度器私有字段）。C3 与「提取所有状态为提前枚举的常量」的既有指令冲突（且所举 5 处"判别读取点"有 2 处是构造）；C4 把"整体替换 `Submission`"误读成"要求也整体替换"——`setRequirement` 是 `delivery` 的唯一写入点。C2(2) 的 `errors` 单元素数组成立，但改后为 0 行而非 −4。**已收下**（src 1920 → 1916 行，测试 52 条全绿）：删 `Scheduler.disposed`，存活以 `ScheduleHost.isDisposed()` 为唯一事实（`flush` 已在用，派生证明即此；回归为新增用例"disposed manager: a flush request from any port is inert and schedules nothing"）、删 `removeHandle` 中被 `forgetActivity` 身份守卫保证为 no-op 的置空、删 `dispose()` 末尾防御性 `resources.clear()`（零订阅者实例不存续，U08）、`readConfiguration` 的 `errors` 数组改单变量、`reportObserverError` 直写三个已知字段。其备选"删 `removeHandle` 里的 `releaseActivity` 调用"不可采纳：订阅将永不释放，页面卸载后实例仍按 `every` 永久后台空转。该报告另有 11 处基线与环境陈述与代码不符，其中四例：`node -v` 实测 v24.20.0 且三项闸门当场全绿（不是"无可用 Node、闸门无法执行"）、验收叶子 136（实测 132）、本文件 630 行（实测 1015）、`requestFlush` 调用点 6 处（实测 5 处，`626` 不是调用行）。 | 本次复核；§6.2、§2.4、ADR-12、U08 |
| ADR-15 | 第十五版评审（过度设计专项评估与需求归因）提出的两项提案 | **整体不采纳（不改 src）**：B1「`query` 的 runner 可选化」是公共契约**增量**而非减法——复核时按缺省复用 `source.load` 实现，实测 **0 行、结构分支 134 → 135**，并强制同步 §2.2 契约镜像、§2.1 契约表、README「参数怎样定义」示例与 README 度量行，另需 1–2 条测试覆盖缺省分支（`pnpm check:docs` 在镜像与度量行两处当场转红）；缺省还会同时落在 `vue.ts` 与 `manager.ts` 两处，需指定唯一归属。其新依据成立（统一文档 §2.1 与 README 的正式示例里 runner 与 `load` 是同一调用体），但按「不新增第二入口、裁剪不必要的分支」不采纳：纯增量改动等接入方提出再做，成本不变。B2「把复杂度归因账作为 §5.1 附录」不采纳：本文件每条已含结论、依据与关联，附录的「重启前提」列等于给被否提案第二入口，与本节「被否的提案不给第二入口」及 §8.1「取舍理由只记 §5.1」的收敛方向相反。C 档是记账，不构成提案。其 §5.3 的唯一合并候选「`delivery.ts`(49)+`diagnostics.ts`(58)，合并不改变依赖方向」不成立：保留结果边界的形状会让 `source.ts → delivery.ts` 成为同层运行期边，门禁 9 直接失败，DESIGN §1 第 28–29 行「`diagnostics.ts` 是零依赖叶子」正是这条理由；只有并进 L0 的 `diagnostics.ts` 才方向中立，但要同时改 `LAYERS`、两份模块清单与职责表。另有 10 处基线与表述与代码或公开仓库不符：逐文件行数把 `model.ts`/`public-types.ts` 写成 257/190（实测 256/189，其表合计 1918 而非 1916，「与 `complexity.mjs` 的差异是末行空行计数」不成立——`complexity.mjs` 用 `trimEnd()`，12 个文件都没有末行空行）、规范侧 812+401（实测 811+400）、身份复核谓词「27 处调用点、manager 18」（实测 28 处标识符、20 处真实调用点，manager 19）、「状态表 18 个域中 2 个标注推导」（实测 19 行、3 处 `**推导**`）、「Manager 约 39 个方法」（实测 41 个方法 + 构造器）、「§7 顺序约束第 3 条声明的六个边界」（该条只声明五个，abort 之后来自第 1/2 条）、「本环境 Node v16.18.0」（宿主确有 `/opt/证联讯/node/node` v16.18.0，但仓库环境 `node -v` = v24.20.0、`pnpm -v` = 11.24.0，三项闸门当场全绿，未运行测试是自选限制而非环境阻塞）、「285 年不可达」（照抄 DESIGN §3.2，毫秒口径应为约 28.5 万年，本轮已修）、「`vue-query` 核心约 3-4 个模块」（`@tanstack/query-core@5.102.8` 的 `src/` 有 23 个模块，3–4 个文件是 `@tanstack/vue-query` 适配包的形状）、版本号（本版把上一轮 5 项删除记在「第十四版」名下，ADR-14 记为「第十三版评审」）。其机器读数与本次复测全部一致：12 文件 / 1916 行 / 134 分支 / maxCCN 8、132 叶子、25 锚点、52 条测试、observer 包装 17 处（manager 7／delivery 4／diagnostics 3／query 2／source 1）、34 处 `disposed` 检查、B10 十一个变体、A07 九个变体、四个窄端口 4/3/5/4 项、`Scheduler` 6 个下行方法 + 5 个 host 回调、`Handle` 10 字段＝4 固定端口＋6 可变，且每处身份复核都落在 DESIGN §7 的边界上。 | 本次复核；§6.2、§2.2、§8.1、DESIGN §1/§3.2/§3.8 |
| ADR-16 | 外部专家评审（`统一刷新-评审.md`）提出的需求重审 | **采纳方向，进入需求重写**：主线是「订阅数据＋主动刷新」——首次/后续提交差异改为幂等声明＋显式刷新动作（`refresh`，命名按 §6.2 的普通命名授权）；删除 `query` 独立通道（`QueryRun`／`QueryHost`／`commit`／只写本页）与「query 成功后后台接管」（附条 A）；主动改频率只重算下次到期、保留在途请求。落地按该评审 §8 的顺序：先改正式需求与验收（§0–§4、§7），再改 DESIGN，最后改实现与测试，每阶段保持 `pnpm check:docs`／`typecheck`／`test` 全绿。其 §0.3 的边界（暂停刷新走同一执行路径、页面退出取消临时等待、刷新等待「动作之后启动」的请求、主动与自动共用队列与并发上限、身份变化立即成为当前意图、无需求后仍即清理）按其自述的「推荐默认值」落入规范，实现方不另填。同时登记两件事：(1) 其 §2.1 的契约口径冲突已按本条修正 §0.1／§1——原文「至多一份在途请求」「同 key `load` 并发数 ≤ 1」与实现不符，实测同身份可有两个物理在途 `load`（一个已取消未结束、一个有效），`Resource.task` 只指向有效的那个，这与 T12／F11「abort 不释放物理槽」一致；(2) 其 §6.1 触及ADR-13 关闭的 `Delivery` 三态——该行依据含「query 后不回退旧数据」这一产品承诺，本次需求变更移除了该承诺的前提，因此属**前提变更后的重新裁决**，不是第二入口。本次不顺带变更 Pinia 分区、安装接管、三态配置、参数身份、副本独立与真实并发计数。**已落地**：规范、DESIGN、实现与测试已按本条改写（`src` 12 → 11 个模块，`query.ts` 删除；公共契约改为 `submit` 幂等声明＋`refresh`；验收 132 → 129 叶子）。该评审自身声明：新方向尚未实现验证，§0.3 的边界是推荐默认值而非逐项确认；其 §2.2 的五处文档失准经复核四处属实（`DESIGN` §3.3 配置字段名、§5.1 时钟措辞、`model.ts` 两条过时注释，以及上一行已修的 §3.2 序号口径），已随本条一并订正。 | 本次复核；§0.1、§1、§2.1/§2.2/§2.4、§3（U03–U06、U10、U11、附条 A）、§4（Q 组、S02、T04、D 组）、ADR-13 |
| ADR-17 | 第十七版评审（对齐新方向：订阅＋主动刷新的契约收敛） | **主线已落地，只收下记账与四处勘误**：该版 §二／§六／§七 主张的三项取舍（删首次差异、删 query 独立通道与接管、改频率不重取）与 §十 第 0–4 步，已由 `0fabb00`／`6e6a144`／`ef216a3`／`e927cb2` 全部完成——它的基线停在重写前的 `e61e7d8`（12 文件／51 测试／132 叶子），因此其「待写入正式需求、非已实现」的表述对本仓库已过期。本次据其 §十一 的待裁项裁决：**接口命名维持 `submit`＋`refresh()`**（不采纳 `declare`，也不采纳 `refresh(args?)` 可选参——那等于把「声明新身份并立刻取一次」变成一条捷径，重新模糊声明与刷新的边界）；**错误 origin 维持 `background`／`validation`／`configuration`**（它主张把 `background` 并入 `execution`，实际相反：`execution` 已随独立查询路径删除，保留的是描述失败步骤的那个名字）；**归因账不入 §5.1 附录**（第十五版 B2 已裁决不采纳，「被否的提案不给第二入口」继续适用）；**验收叶子保留 129 条**（不按其估算 85–90 再删一轮——Q／D 组叶子是改写而非删除，新语义的合并、结算、退出取消与「恢复不重放」需要自己的验收）。同时登记它与实测不符或需更正的三处：(1)「Q 组约 10 条删除、D 组 4 条全删」实测为 Q 组保留 11 条（仅 Q12 移出）＋ D 组 4 条全部改写保留；(2)「运行时状态域 10 → 7」未发生——`Delivery` 三态是被「等待者集合＋版本下限」等价替换，DESIGN §3.8 仍为 19 行、3 处推导；(3)「删 `closeQuery` 关闭边沿」不成立——关闭边沿仍须结算当时未完成的刷新要求（B05／F10／Q07），该命名操作按新语义改名为 `cancelRefresh`，`ConfigurationHost` 仍是 3 项事实。它的 §五（P1 口径）已按其三句话改写（`f9c8032`），§六「标准化」列的五处说明纠错已修四处（第五处「分层描述」经复核判定为措辞澄清而非失准），§十一问题 3（等待者载体）由实现回答：**版本下限＝代次数字**，配 `Scheduler.isRunning(task)` 区分「排队」与「在执行」，合并／替换／重建三场景分别由 Q06／B16a／B14 断言，无需再裁。版本号链条：该版自称承接「第十五、十六版」，但工作区仅存在第十五版与外部评审 `统一刷新-评审.md`（已归档），第十六版仅见于该版转述，故本条按转述记账、不声称其原文存在。 | 本次复核；§0.1、§1、§2.1／§2.4、§3（U04–U06、U11）、§4（Q 组、D 组、B14、M05）、DESIGN §3.6／§3.8 |
| ADR-18 | 第十七版附录二（需求规格与数据结构分析）提出的 RQ1–RQ6 | **收下 RQ1、RQ3，拒绝 RQ4，确认 RQ5；RQ2／RQ6 已由本节既有裁决覆盖**：RQ1 校验失败的声明改为**完全 no-op**（声明是事实陈述，无效输入不产生副作用；旧 P08／B07 的「退出旧订阅＋清参数」删除，实现同时删掉一个恒真分支与三个小助手，`src` −22 行）；RQ3 `enabled=false` **不取消**进行中的刷新要求（适配层删 `lastEnabled` 与关闭边沿分支、`ConfigurationHost` 由 3 项事实缩为 2 项、`Manager` 公共方法 17 → 16；依据是上游 `统一刷新-评审.md` §0.3 的原文边界「隐藏、失活或卸载取消」——旧 U07 的「关闭边沿取消」是本节上一行落地时多带的）；RQ4「未过期才交付」**不采纳**——与 F01「立即交付独立快照；未到期不请求」冲突，且会重新引入一个当前不存在的「新鲜度闸门」（唯一的闸门是等待者的版本下限），先给旧数据再后台更新本身就是本库的存在前提；RQ5「刷新的结果交付全部有效订阅者＋本页临时资格」**确认**，已实现并有浏览器场景断言。其 B.3 的 S1／S2／S3／S5／S6 在上一行落地时已修，S7 的清账条件随 RQ3 成立，S4／S8 判断正确；**新增一条它未指出的**：`Resource.nextVersion`／`Manager.nextResourceId` 名不符实（存的是「最后一个已分配」的值），已改名为 `issuedVersion`／`issuedResourceId`。其 B.4 的「唯一不变量」与实现一致，两处字面缺口按修正写入 DESIGN §3.6：①「登记一个」以「该实例没有当前任务」为前提；②失败结算该实例全部未结算要求（否则按字面会失败即自动重试）。其 B.1 把「动作后请求」定义为「之后**登记**」，与上游 §0.3 的「之后**启动**」相反：按「启动」语义，排队未启动的任务可以满足本次刷新（不追加请求）——实现取后者，并由 Q13 场景断言。其 B.2 把 refresh 在无声明时的结果写作 `rejected`，而契约里没有这个取值（`rejected` 属于 `SubmitResult`）：实现返回 `cancelled`／`unavailable`（A07／Q08 断言）；其 B.1 的「合格结果（按过期判定）」是实现里不存在的术语，属 RQ4 的产物。**补记（ADR-43）**：本行为论证 RQ4 写的那句「唯一的闸门是等待者的版本下限」已经不存在——ADR-40 把下限降成「登记时那个请求的版本」，ADR-43 证明它恒真并删掉 `Waiter.min` 与那次比较。RQ4 的结论本身（不采纳「未过期才交付」、不引入新鲜度闸门）不受影响，反而更直接：现在的交付与结算都不看版本。 | 本次复核；§2.4、§3.1（U03、U07）、§3.6、§4（P08、B07、Q07、B05、F10、M06、A01、Q13、F01）、DESIGN §2／§2.1／§3.3／§3.6 |
| ADR-19 | 第 6 版代码审查（`CODE_REVIEW.md`，Linus 视角的对抗性审查）的逐条复核裁决 | **收下 9 项、否 12 项、5 项不动**。复核先对齐基线：该报告写于 `75f1caa`（§0 的 11 个文件行数与 `core.test.ts` 1110／`vue.test.ts` 503 逐格一致；本行上一版曾记为 `2929b24`，与现行报告不符，此处订正）。报告 P1-3 引用的 `OBSERVER_FAILED` 属于更早的 `2929b24`，而它引用的行号属于 `75f1caa`（那一版实参已改为按类别区分的 `reason`）——两版混用，但不影响该条实质成立。**另订正一处结论**：该报告 v6.1 的 §4「P0-1 撤回」与 §10「评审事实错误」不成立——`LOAD_TIMEOUT_MS`／`expireTask` 由 `1a1f05b`（2026-09-16 16:55）引入，报告文件 16:59 才写入该段；在它声明的基线上框架没有任何 `load` 上限，U13／U20 也没有「框架上限」字样（`git log -S'LOAD_TIMEOUT_MS' -- src/` 只有该提交，且逐提交重放确认）。P0-1 因此恢复为「成立、已采纳、已修复」（见 ADR-20）。**收下**：P0-1（框架上限，见ADR-20）、P2-9／P2-10／P2-11（见ADR-21）、P2-12（`observe` 注释点明「同步抛错与返回的 Promise 拒绝都覆盖，只观察已产生的返回值用 `observeRejection`」）、P1-4 的一半（`readSnapshot` 补 `@throws`，**不换稳定 id**——换 id 会改公共 API 并推翻 U17／B13b）、P2-7 的 two halves（`every` 可省略、`ReadonlySnapshot` 改名，见ADR-22）、D-1／S-2／S-3／P2-6 的文档补写（见ADR-23）。**否**：S-1「`Handle` 收进 `Manager` 作私有 class、复核函数减半」——私有化只降低误写概率，而这 8 个复核函数守的是**同步重入后的身份**（abort 监听、publish、onError、Store 通知都能改身份），与字段可见性无关，一个都删不掉；S-4「两个端口是绕开 ESM 环的类型体操」——端口同时满足本仓第 9 项门禁的「运行期边只向下」规则（`scheduler.ts` 若直接依赖 `Manager` 就是 L2→L3 向上边），且它给的建议 3「让 `Scheduler` 直接持有 `Manager` 的窄引用」就是它正在批评的 `ScheduleHost`；S-5／S-6／S-7／S-8 与 §6.2 冲突或经复核不成立（`bindings` 槽对象是给已 setup 组件原地换 Manager 用的，「`provide` 支持替换」推不出它多余；Pinia 文档只写 "you should define each store in a different file"，且 `pinia.js:1311` 按 id 缓存、本代码 id 每 Manager 唯一，不存在重复定义；`$dispose()` 确实不删 `pinia.state.value[id]`，但公开 API 没有这个能力，「用公开 API 释放」不可执行）；P1-2（溢出分支）本轮未列入裁决，保持现状；P2-5（`abort` 未在 `observe` 内）未列入裁决，本轮不动，且实测证明它给的修法无效——监听器抛错时 `abort()` 正常返回、后续监听器照常执行、异常走 `uncaughtException`／`window.onerror`，`observe` 永远 catch 不到，所以「把 `abort()` 纳入隔离」是无效修法，真正要动的是隔离承诺的措辞；P2-8 总体保持（4 处 `as` 中两处是品牌擦除的必然代价，另两处是异构注册表的边界还原）。该报告另有 14 处失准，其中五处影响判断：把 P1-4 的「渲染路径每次渲染两趟遍历」写成 `examples/pages/shared-pair.ts:119` 的用法（该行只渲染一个缓存字符串，`readShared` 的唯一调用点在按钮 `onClick`）、P2-6 称「文档没提」（DESIGN §3.6 三处写明规则与理由）、P2-5 的修法无效、S-7 两处依据不成立、`Handle`「9 个可变字段」实为 7 个（另有 4 个只读字段），以及 §7.1 的「8 处 `observe` + 2 处 `notify`」实为 7 处 `observe(...)` 调用（manager 6 处 + `notify` 内 1 处；另有 `observeRejection` 2 处）与 3 处 `notify`（漏了 `vue.ts:110`）。现行文件已把 `Handle` 的可变字段数改为 7，并新增 §10 与附录 B；本行列举的失准按当时文件计，`Handle` 那一条对现行文件不再适用。正面清单 R-1–R-10 逐条复核全部成立。 | 本次复核；`CODE_REVIEW.md`、§2.4／§2.5／§3（U13／U17／U18／U19／U20）、DESIGN §3.5／§5.2 |
| ADR-20 | P0-1：一个永不 settle 的 `load` 会让实例不再轮询、并发槽永久泄漏 | **采纳「框架侧上限」，数值由确认人给定：`LOAD_TIMEOUT_MS = 10000`（10 秒）**。机制：从任务**真正开始执行**起算（排队等待不计入），用注入的 `Clock.setTimer` 登记一次性 Timer（测试可确定性推进），到期按**共享请求失败**结算——通知订阅者 `onError`（`origin=request`，带声明代次）、结算该实例全部未结算的刷新要求为 `error`、更新 `lastSettledAt`、**立即出册**（`resource.task = null` + `scheduler.release`）并交还槽位，紧接着的一轮 flush 即可启动排队任务；`abort` 会取消该计时，因此被释放的任务不留下悬挂计时。出册后这次执行迟到的结束在 `currentTask` 处被判无效：不写 Store、不交付、不二次通知。它覆盖三类输入（挂死的传输、忘了拒绝的适配器、把长连接当一次 `load` 的封装），代价是 `maxConcurrent` 由「真实在途请求上限」变成「**在册任务数**上限」（物理在途可暂时超过上限），DESIGN §3.5 第 5／9 条、§5.2、§7 第 6 条与 §1 的观测指标按此改写，U13／U20 与 T12／R10／M04／Q13／F11／A02 的措辞按「上限内／上限到期」区分。**被否的备选**：(a)「保持现状只补文档」——确认人认为静默停摆不可接受；(b)「只 abort 不摘牌」——经复核无效，`finally` 只在 Promise settle 时执行，无视 signal 的适配器永远不会释放槽位，且会把健康的在途任务逐个 abort；(c)「槽位满且仍有排队就无条件抢占」——经推演会把正常负载变成空转（基线 8 个身份／every=25ms／上限 4 时每个周期都有 8 个任务同时到期，无条件抢占会让每个在途任务在完成前被 abort，基线 624 次 load 与收敛比 0.97 全部作废），故必须有时间判据。**未经确认人给出的数值不得由实现者静默填入**：本条的 10000 由确认人拍定，另有「按资源自身 `every` 出册」的备选未被采纳。 | 本次复核；U13、U20、T12、M04、Q13、F11、A02、§2.4、DESIGN §3.4／§3.5（第 11 条）／§5.1／§5.2／§7 |
| ADR-21 | 错误处理契约的三项收敛：错误来源独立取值、公开错误带声明代次、可达子集不用 `Exclude` | **三项都采纳**。(1) `ErrorOrigin.Background` 不再复用 `RequestOrigin.Background` 的字面量，改为自有取值 `request`：这是**ADR-17 已关闭条目的重开**，重开依据是契约层面的新证据——同一个字符串在两个语义域里表达两件不相关的事，而 `display.origin` 与 `error.origin` 会同时出现在一个页面上，页面写下 `origin === 'background'` 时必须先确认自己站在哪条通道上；ADR-17 当时的理由（`execution` 已随查询通道删除、`background` 描述的是失败步骤）成立，但它没有评估这个复用造成的误读面。取值域与 `RefreshErrorOrigin`／`RefreshResult` 的镜像、DESIGN §3.8 的状态表与 C11／Q03／F05／R01／B10.04 的措辞一并改写。(2) `RefreshError` 增加 `operationId?: number`（声明代次，框架生成）：一个页面两处 `useRefresh` 且共用同一个 `onError` 时据此认领；沿用既有规则「尚无有效代次时字段缺席，`0` 不是有效操作号」，`resourceId`／`taskVersion` 仍只在 observer 诊断里。它**推翻**了 `public-types.ts` 原来「通知不携带任何框架身份」的声明，理由是原先把「可用于比较或续传」当成唯一判据，漏掉了「这是谁的失败」这条调用方能做也必须做的判断。(3) `RefreshErrorOrigin` 与 `SubmitCancelReason` 改为**逐个写全可达成员**（`typeof ErrorOrigin.Request \| typeof ErrorOrigin.Configuration`），不再用 `Exclude` 求差集：读者不必心算补集，同时保留「成员改名时类型一起报错」的推导链。测试侧的按对象全等断言同步更新（`vue.test.ts` 的「尚无操作时不带身份」用例保持原样，它正是缺席规则的证据）。 | 本次复核；§2.2／§2.4、`public-types.ts`、T12／A02／A07、DESIGN §3.8、ADR-17（重开） |
| ADR-22 | `every` 可省略、`DeepReadonly` 改名 `ReadonlySnapshot` | **采纳**。(1) `RefreshOptions.every` 改为可选：**只在 `enabled` 为真时必需**——只手动刷新的页面可以整个省略它，不必写 `{ enabled: false, every: 1 }` 这种用假数字表达「没有周期」的写法；未开启且省略合法且不通知，开启而省略按配置非法拒绝（`TypeError('every is required when enabled is true')`），给了但非法同样拒绝（「省略」与「非法」不是同一件事）。实现上 `readEvery` 接受 `undefined` 并读作 `null`，`ValidInput` 因此分成两支（`EnabledInput` 的 `every` 必为正安全整数，未开启分支的 `every` 可为 `null`），`eligible` 的类型谓词改为 `input is EnabledInput`，`attach` 取周期时不再需要断言或分支。省略是 **setup 期形状**（对象上没有该字段）：运行期读到 `undefined` 仍按配置非法拒绝。`vue.ts` 的「三项各自独立捕错」保持不动（R-1 的依据是 watcher 取值函数必须让三项各自完成依赖收集）。(2) `DeepReadonly` → `ReadonlySnapshot`：该类型是**编译期**只读视图，`cloneSnapshot` 用 `structuredClone` 产出独立副本但**不做运行期冻结**，`pages.spec.ts` 那条「篡改页面副本不影响共享分区」的验收正是靠这份独立所有权成立；因此选择改名而不是冻结（冻结会给每次交付多一趟全量深遍历，并让那条验收在严格模式下抛错失效）。注释与 DESIGN／统一文档同步写明「拿到只读视图不等于共享对象不可写」。 | 本次复核；U18、A07、A05.08、§2.1／§2.2／§2.5、DESIGN §3.8／§6.1 |
| ADR-23 | 第 6 版审查报告的第二次逐条复核（基线校准 + 收下 P1-2／P1-3／P2-5／P2-7、拒 P2-8） | **先校准再裁决**：(1) 订正 ADR-19 的基线归属（`75f1caa`，不是 `2929b24`）与「P0-1 撤回」的定性——报告 v6.1 把 `1a1f05b` 引入的框架上限当成基线既有能力，是评审自己新增的事实错误，P0-1 恢复为「成立、已采纳、已修复」。(2) 补上报告提出、上一轮漏做的 `统一刷新管理.md` §6.4.3 第 6 个故障族（「拿掉框架上限到期的结算与出册」→ T12 三段断言）；DESIGN §5.2 的上限数值补 ADR-20 锚点；顺手修三处仓库自身漂移（DESIGN 的幻影类型 `DeliveryBarrier` → `RefreshWaiter`、幻影方法 `cancelRefresh` 从 Manager 命名操作列表删除，§7 状态行 132／120／12 → 129／128／1）。(3) **收下 P1-2**：`nextIdentity` 到达安全整数上界后停在原地——删掉「耗尽即 `dispose()` + 日志 + `null`」与调用方的 `null` 协议（`submit`／`resourceFor`／`enqueueTask` 三处分支与 `refresh`／`attach` 的两处死分支一并删除），上界因此不再是恐慌式失败；Q06 用例改为断言三条计数路径都照常工作且不写诊断。(4) **收下 P1-3**：`reportObserverError` 收下原始 error，事件形状 `{origin:'observer', reason, errorKind, …identity}`（`error` 字段名改为 `reason`，新增 `errorKind` = `constructor.name`，原始值取 `typeof`）；这**推翻**「诊断不读取原异常」的原承诺——新承诺是「只读类别，不读 message/stack/cause、不序列化」，类别读取本身抛错时整条诊断被吞掉（B10 增加该断言）；U19、§0.1 词表、B10.10／B10.11、DESIGN §6.4 同步改写。(5) **收下 P2-5**：只改隔离承诺的措辞——隔离面恰好是框架**调用**的四类回调，`load` 的 `AbortSignal` 监听器由宿主在 `abort()` 时调用、抛错由宿主上报，框架 catch 不到，因此不在承诺内（delivery 头注释、U19、DESIGN §6.4）；不采纳「把 `abort()` 塞进 `observe`」——该修法经实测无效。(6) **收下 P2-7 的第三项**：`useRefresh` 的 `options` 参数类型放宽为 `RefreshInput<RefreshOptions>`，整个对象可以是值、Ref 或 getter，替换 `options.value` 即按新对象重新协调；原「整体替换调用方手里的对象不会生效」的使用须知改为「给字面量时框架持有它本身，要整体替换就把 Ref／getter 交给框架」，并补一条 vue 用例（含「被 abort 的执行仍占物理槽直到真实结束」的断言）。(7) **P2-8 未实施**：按裁决尝试删除 `vue.ts` 与 `app.ts` 的两处 `as`，`pnpm typecheck` 当场报 `Type 'Display' is not assignable to 'RefreshDisplay<P,T>'`（`args` 逆变）与 `Type 'unknown' is not assignable to 'ReadonlySnapshot<T> \| undefined'`——前者是异构注册表的边界还原、后者是类型擦除存储的还原；裁决里的「交付值冻结」是运行期操作，改不了这两处赋值错误，且与 ADR-22「改名而不是冻结」冲突，「`Manager` 泛型化」会让异构注册表退化成 `any`。故保持现状，等新的证据或裁决（**后续**：确认人看到双变 PoC 后裁决改为方法声明，见 ADR-24）。**本次契约翻转**（都已同步文档）：诊断事件字段名 `error`→`reason` 并新增 `errorKind`；「诊断不读原异常」→「只读类别」；序号上界不再销毁 Manager；`options` 参数类型放宽。测试 68 → 69。 | 本次复核；`CODE_REVIEW.md`、§0.1／§2.1／§2.2／§2.4／§3（U13／U19／U20）／§4（A05、B10、Q06）／§6.4.3、DESIGN §3.2／§5.2／§6.1／§6.4／§7、README、ADR-19／ADR-22 |
| ADR-24 | P2-8 收尾：用「方法声明 + 参数双变」消掉 `vue.ts` 创建点的类型断言 | **采纳**。ADR-23 曾以「泛型化消不掉断言」为由保持现状；确认人在看到最小 PoC（`/home/uos/Documents/dsh/.tools/handle-poc/bivariance.ts`：属性式泛型化复现 `TS2322 Property 'a' is missing in type '{}'`，方法式双变则赋值通过且 `erasedHandle.publish({ args: {}, data: undefined })` 也通过）后裁决取方法式。落地：`Handle<P extends object = object, T = unknown>`，`publish(display: RefreshDisplay<P, T>): void` 由函数类型属性改为**方法签名**，`vue.ts` 因此写成 `const handle: Handle<P, T>` 并删掉 `value as RefreshDisplay<P, T>`（`src/` 的类型断言 4 → 3 处）。**已接受的代价**：方法参数按双变比较，注册表侧（`Handle`＝`Handle<object, unknown>`）投递形状不匹配的 Display 时编译器不再拦；安全靠不变量「每个句柄的 Display 只经它自己的 publish 投递」——`deliver` 的 `args` 取自该句柄的 Submission、`data` 取自该实例的 StoreEntry，两者都经过句柄身份复核。`app.ts` 那处断言与 Handle 无关（`unknown` → `ReadonlySnapshot<T>` 的类型擦除还原），保持不变。零行为变化：`pnpm typecheck` 0 错误、`pnpm test` 69/69、门禁全绿。 | 本次复核；`CODE_REVIEW.md` P2-8、ADR-23、`model.ts`、`vue.ts`、DESIGN §3.3 |
| ADR-25 | 第十八版评审（`统一刷新管理-评审.md` §439–861「重构方案：5 文件、有效逻辑 ≤500 行」）阶段 1–2 的实测复核 | **整体不采纳**（不改 `src/`）。复核方式：在独立副本 `.tools/v18-poc` 里按提案字面实施 1a／1b／1c／2a／2b／2c，逐子步跑三项门禁；主仓未被写入（`git status` 只余评审文件）。**实测**：11 文件 1883 行／137 分支／maxCCN 8／codeLines 1052 → 9 文件 **1806 行**／133 分支／**codeLines 984**，净 −77 行、有效逻辑 −68 行（`manager.ts` 反增 49 行，因并入 `store.ts`），距「有效逻辑 ≤500」仍差 484 行。**四条否决依据**：(1) 1c 删 `Resource.lastSettledAt` ⇒ `dueAt` 退化为「立即到期」，结算后立刻再入队：`pnpm test` 60 秒不终止（partial 29／21／7／1），逐用例隔离后 core 16 pass／24 fail／**3 hang**、vue 16 pass／6 fail、`typecheck` 8 错全在 tests——违反 U11／U12 与 T 组验收，且标题含「不忙循环」的 T14 仍 PASS（现有验收拦不住这类忙循环）；(2) 1c 删 `namespace`／`Resource.id` ⇒ 同一 Pinia 上两个 Manager 共用固定 store id `refresh-results`，互相读到并清掉对方分区（S06 实测 0≠1、P05 7≠1、探针 `.tools/probe-partition.test.ts`），而分区键退化为纯参数键后同一 Manager 内不同 Source 同键互相覆盖；删 `Resource.id` 同时打断 observer 诊断身份（B10／B10.06／B10.08 因缺 `resourceId` 全红）；(3) 2b「删 `ScheduleHost`」不是减法——`Scheduler` 直接持有 `Manager` 后必须把 `reconcileAllHandles`／`enqueueTask`／`isCurrentTask`／`runTask` 提到公共面（否则 4 条 TS2339／TS2341），端口从接口变成 Manager 的公共 API；2c 的「局部函数」会让 43 个 core 用例无法在无 Pinia 下运行；(4) 1b `Input` 三态合一后 `every` 收不窄（`manager.ts:228／324` TS2322），只能补交叉类型谓词 `input is Input & { enabled: true; every: number }`，并把判据压成 `error === undefined`（`throw undefined` 会被读成配置有效）。另有伪减法两处：`Resource.source` 删除只换成 O(全部桶) 反查 + 一个原本不存在的失败分支；`Handle` 11→6 按提案三条只到 9。**门禁代价**：为让合并后的 `check:docs` 变绿必须改 `src/` 之外 4 个文件——`scripts/check-docs.mjs`（读路径改 `types.ts` + `LAYERS` 增删，+8／−3）、DESIGN manifest（+5／−7）、README manifest／依赖方向行／度量行／「11 个运行时模块」（+5／−6）、`统一刷新管理.md` §2 导语 1 行——并新引入 `types.ts → source.ts` 的类型反向边（门禁以 type-only back edge 容忍，exit 0）；**§2.2 契约镜像本身不需要改**（公共契约段留在 `types.ts` 顶部、按 `// ==== 内部模型` 标记切片即可）。**结论**：阶段 1c 整体否决；2b／2c 不是减法；1a／2a 虽可做但只减 77 行且要动门禁脚本与三份文档，与「≤500 有效逻辑」目标不匹配——该目标只能在改行为的前提下达成，与提案自述的「纯减法、不动验收」冲突。该版另有 10 处数字／引用失准（基线写 1874 实测 1883、验收叶子写 132 实测 129、方法数标题 33／正文 42 且公共清单含不存在的 `cancelRefresh`、`model.ts` 写「17 个 interface」实测 15、窄端口「13 项事实」实为 11、`EFFECT_FAILED` 读 4 次实测 6、tests「2316 行（不含 pages/scenarios）」实际含这两个文件（剔除后 1850）、Handle 字面量位置写反（实测 core 2 处、vue 0 处）、有效逻辑「~1320」实测 1052、`Submission.delivery` 字段不存在）与三处自相矛盾（安装接管删／不删、`nextIdentity` 删／不动、`Input` 形状改／不改）及两处错误引用（「统一文档 §6.3 已写明」实为 DESIGN §6.3，且该节写的正是原地接管）。证据（工作区留档，未入仓）：副本 `.tools/v18-poc`、`.tools/sweep-core-final.log`、`.tools/sweep-vue-final.log`、`.tools/final-pnpm-test.log`、`.tools/probe-partition.test.ts`。 | 本次复核；`统一刷新管理-评审.md` §439–861、U11／U12／U21、§4（P03／A07／S05／S06／P05／B10／T14）、DESIGN §1／§3.3、ADR-08／ADR-10／ADR-11／ADR-23／ADR-24 |
| ADR-26 | P2-6：`publishError` 是否按 `minVersion`（刷新下限）过滤 | **维持现设计**：失败结算该实例**全部**未结算要求（含下限高于本次失败任务的那一个）。依据：DESIGN §3.6 的表格行与专门段落已写明理由——一次失败的含义是「这次刷新没拿到新结果」，若把更高的下限留在集合里，`refillWaiters` 会立刻补一次后继请求，等于「失败即自动重试」，与「失败保留画面、下个周期继续」冲突；因此 `refresh()` 的下限语义是「**启动后**」而不是「成功」，这条推论已写进 DESIGN §3.6。**被否的备选**：「按下限过滤、把更高门槛的要求留给后继任务（配合显式重试节流）」——它把一次自动重试藏进一次刷新调用，并要为「点击后失败」引入一个用户可观察的新等待状态，属行为与验收扩张；v6 评审未给出现场反例，不予采纳。**同批落地**（同轮确认人裁决，非产品边界变更故不单列）：`scripts/check-docs.mjs` 增加第 14／15 项门禁（README 记录的 `dist/index.js` 尺寸、两份规范文档承诺的代码符号存在性；13 → 15 项），`examples/pages/shared-pair.ts` 的「暂停本页」改为换掉整个 `options` 对象，演示 `RefreshInput<RefreshOptions>`。 | 本次复核；`CODE_REVIEW.md` P2-6、DESIGN §3.6、U15／Q05／R01、README、`scripts/check-docs.mjs` |
| ADR-27 | 确认人指令「按方案乙重写」：以根契约重写实现与全部规范文档 | **采纳并已落地**。本条不是 ADR-25 的重开——ADR-25 裁的是「不动契约的代码减法」，本条是**需求重新定基线**，由确认人 2026-09-16 直接指令，其质量要求原文是「分层过度、分层防御过度、一个概念分成多个数据结构解释、强行解耦导致代码无法维护」，并补充「不要把猜测当需求」。**已落地**：`src/` 由 11 个模块 1883 行（有效逻辑 1052）重写为 5 个模块（`public-types` / `source` / `core` / `vue` / `index`）863 行／有效逻辑 637；删掉三个窄端口（`ConfigurationHost`／`ScheduleHost`／`ResultStore`）、`Publisher` 中间对象、`Clock` 端口、观察面投影、`EFFECT_FAILED` 与 `errorKind` 诊断、Pinia 私有分区与每实例命名空间、`fast-json-stable-stringify` 依赖；一个概念只留一份事实（订阅只在句柄上，实例侧持有同一批句柄；结果只在实例上，无分区镜像）；参数准备由三趟遍历压成一趟。**规范同步**：`统一刷新管理.md` 按十条根承诺（G1–G10）重写，18 条触发锚点（`U01`–`U18`）与 18 个验收叶子（`A01`–`A18`）取代旧版 25 锚点／129 叶子；`DESIGN.md`、`README.md`、`scripts/check-docs.mjs`（分层表、分段清单、词汇表、门禁对象）、`scripts/trace-leaves.mjs` 同步重定基线。**被本条取代的裁决**：ADR-08（Pinia 私有分区）、ADR-09 的类型三态部分（行为保留为「配置非法即不订阅并通知一次」）、ADR-10 的 `bindings` 形态（改为注入槽位原地替换）、ADR-11 的窄端口切分、ADR-14/ADR-15/ADR-19 的观察面与诊断条目、ADR-22 的子集类型名（`SubmitCancelReason`／`RefreshErrorOrigin` 改为内联联合）、ADR-24 的 `Handle` 泛型在公共面保留、ADR-26 的 P2-6（`publishError` 门槛）随「失败结算全部未结算要求」保留结论。**继续生效的裁决**：ADR-01（交付时间）、ADR-02（不交付实时在途）、ADR-04（`readSnapshot`）、ADR-06、ADR-07、ADR-12/ADR-13 的实证否决、ADR-17（`submit`＋`refresh` 命名）、ADR-18（校验失败不改状态）、ADR-20（框架上限，数值 10000 保留）、ADR-21(1)(2)（错误来源独立取值、公开错误带声明代次）、ADR-23（序号上界停在原地）。**两处与第十八版方案乙 §四 的偏离**（按确认人本轮的质量要求判断，可被否决）：① 保留一个约 10 行的框架上限（ADR-20 的数值是确认人给的，且去掉它会让挂死的 `load` 永久占住槽位、直接破坏 G3 的可观察事实）；② 保留 `RefreshError.operationId`（ADR-21(2) 已确认，5 行，错误归属闭环）。**证据**：`pnpm typecheck` 0 错误；`pnpm test` 35/35（A01–A18）；`pnpm test:browser` 12/12（三条代表页面与八条受控场景未改一行）；`pnpm check:docs` 全项通过；包与消费方复测记录见 README。 | 本次落地；`统一刷新管理.md` §1–§5、`DESIGN.md` 全文、`scripts/check-docs.mjs`、`scripts/trace-leaves.mjs`、ADR-08／09／10／11／19／22／24／25／26 |
| ADR-28 | 状态常量对象是否作为值导出（确认人 2026-09-16 指令） | **作为值导出**：`RequestOrigin` / `ErrorOrigin` / `CancelReason` 由 `src/index.ts` 用 `export { … }` 导出（值涵义与类型涵义一次导出，因此这三个名字从 `export type { … }` 列表里移除）。依据：这三个对象是状态取值的唯一来源（§2.2），此前只导出类型涵义——消费方实测 `error.origin === ErrorOrigin.Request` 报 `TS1362: cannot be used as a value because it was exported using 'export type'`，只能写裸字面量，与「提取所有状态、提前枚举为常量」的既有指令不符。**契约面变化**：包导出从「3 函数 ＋ 15 类型」变为「3 函数 ＋ 3 个常量对象（同时是类型）＋ 12 类型」，取值域与类型形状不变。同步统一文档 §1／§2 导语与 §6.1、DESIGN §2、README，并在干净消费方加一条值与类型两用的反例。 | 本次落地；`src/index.ts`、统一文档 §1／§2／§6.1、DESIGN §2、README |
| ADR-29 | 包导出面收窄与 `source.ts` 的参数边界重排（确认人 2026-09-16：「类型一多，调用方更不好区分，调用方需要这些吗，你需要问自己」） | **按调用方视角收窄**。逐个类型问「调用方会不会持有或接收它」：`RefreshSource`（包装函数签名）、`RefreshOptions`（单独构造配置）、`RefreshHandle`（保存/传递句柄）、`RefreshDisplay`（渲染组件入参）、`RefreshError`（命名的 `onError`）、`SubmitResult` / `RefreshResult`（保存结算结果）、`RefreshManager`（保存协调者）——保留；`ReadonlySnapshot`（深层只读工具类型）、`RefreshInput`（三个字段的形状）、`RefreshLoadContext`（单字段包装）、`RefreshManagerOptions`（单字段包装）——**不再从包入口导出**，它们仍是上述类型的组成部分，调用方按推断使用。包导出从「3 函数 ＋ 3 常量 ＋ 12 类型」变为「3 函数 ＋ 3 常量 ＋ 8 类型」。示例同步改为调用方写法（不命名工具型别名，`readShared` 直接由包签名推断返回类型）。**同批内部重排**：`source.ts` 原先把守卫、冻结、编码压在一趟里并用 `freeze` 布尔开关控制副作用；现拆成 `canonical`（纯编码，守卫与深度在其中）与 `deepFreeze`（唯一副作用），多一趟遍历、少一个 flag，契约（值域/深度规则、冻结的仍是框架副本、键的内容）不变。**验收**：`pnpm typecheck` 0 错误、`pnpm test` 35/35、`pnpm check:docs` 通过；干净消费方 TS 4.9.5 零错误、SSR 与客户端通过。 | 本次落地；`src/index.ts`、`src/source.ts`、`examples/`、统一文档 §1／§2／§6.1、DESIGN §2／§4.1／§4.4、README、ADR-28 |
| ADR-30 | 参数身份为什么是「完整稳定编码」而不是「稳定排序 ＋ 哈希」，以及哈希映射方案的复核（确认人 2026-09-16：「稳定排序不是有 npm 包吗，hash 完在 store 或者内存里存一下 key→参数 的 map 映射，需要的时候 get 一下不是更简单吗，为啥要在复杂的参数上做这么多加工」） | **不采纳哈希映射方案；同时补回一个被重写丢掉的守卫**。实测 `fast-json-stable-stringify@2.1.0`（该包只做排序那一半）：`-0` 与 `0` 撞键、`NaN` 与 `null` 撞键、`Infinity` 与 `null` 撞键、`undefined` 字段与缺字段撞键、函数字段与缺字段撞键、`Date` 与它的 ISO 字符串撞键（循环引用它会抛）。本框架 `U15`／G02 要求这些值**显式拒绝**而不是静默合并，因此守卫那一趟无论如何都要走：守卫与编码合成一趟（`canonical`）＝ 1 趟遍历 ／ 0 依赖；换成「包 ＋ 守卫」＝ 2 趟遍历 ／ ＋1 运行时依赖 ／ −约 14 行。**哈希 ＋ `key→参数` 反向表**额外代价：① 代码里没有任何一处需要从键反查参数——`load` 用 `resource.parameters.args`、`refresh` 用 `handle.parameters`、`readSnapshot` 手里就有 `args`，反向表是一份新状态（要随实例释放一起清理）；② 哈希带来碰撞，碰撞在这里等于「A 账户的结果被当成 B 账户的」，是静默错数据；键的收益只是把 Map 键从 O(K) 压到常数，而 `args` 本来就整份持有（20 个身份、每个约 2 KB ≈ 40 KB）。**本轮修复**：重写时丢掉了「根容器必须是普通记录」这一条（旧实现的 `buildKey` 有），导致 `readSnapshot(source, [1,2]／null／7／'x')` 不抛而是当作合法身份；现已补回 `requireRecord`（两处入口共用，＋6 行）并把四类非法根容器纳入 A15／A18 的断言。 | 本次落地；`src/source.ts`、`tests/core.test.ts`、`DESIGN.md` §4.1、ADR-29、G02／`U15` |
| ADR-31 | 参数边界再收敛：去掉内部深度守卫、一个身份只保留一份参数对象（确认人 2026-09-16：「深度守卫直接去掉，但是你说的参数被各个持有这合理吗」） | **两件都采纳**。① 删除 `MAX_PARAMETER_DEPTH` 与 `canonical*` 的 `depth` 形参：循环引用改为让递归耗尽调用栈、由引擎抛 `RangeError`，与其它非法参数同样处理（`prepareParameters` 异常经 `submit` 按 `validation` 拒绝、`readSnapshot` 抛给调用者），契约里「不设内部深度上限」写进 U15／`DESIGN` §4.1，共 −4 行、少一条规则与一个形参。② 「多个模块都持有参数」的复核成立一半：此前每个句柄持有**自己**的 `Parameters` 对象、实例持有首个声明者的那一份，同键时内容等值但不是同一个对象（24 订阅 / 8 身份即 24 份冻结副本）。现在订阅成立时后加入者改用实例已持有的那一份，**一个身份只有一份参数对象**（句柄、实例、`load` 与 `deliverTo` 都用它）。实测：两份同键声明 → `a.parameters === b.parameters === resource.parameters`。**未采纳**「处处只持 key ＋ 全局 `key→参数` 表」：那张表的生命周期必须覆盖「仍在声明身份、实例却已被释放」的暂停页，需要额外引用计数或接受泄漏，等于把刚删掉的第二份归属请回来。**碰撞复核**：`-0`／`0` 业务上是同一个数（拒绝它属于契约的额外严格，要放开须改 G02）；`undefined` 字段与缺字段含义接近，但契约要求显式省略；其余四类（`NaN`／`Infinity` 与 `null`、`Date` 与 ISO 字符串、函数字段与缺字段、非 JSON 值）是**错误输入**，静默合并会让坏值共享另一条查询的数据，故保留拒绝。 | 本次落地；`src/source.ts`、`src/core.ts`、`tests/core.test.ts`（A15／A18）、统一文档 §3／§4／§5、`DESIGN.md` §3.3／§4.1、README |
| ADR-32 | 「坏值归调用方」与「暂停不等于取消在途」两处澄清（确认人 2026-09-16：「暂停页只是不订阅了，关后台刷新啥关系，它页面里面不是有数据副本吗」「坏值是调用方应该做的事情，调用方不会这么傻逼」） | **两条都采纳**。① **编码不再做合法性判断**：删掉 `canonicalNumber`（`-0`／非有限数检查）、删掉「非普通记录即抛」（改为带构造器名前缀，既不拒绝也不与空记录合并）、删掉 `requireRecord`（数组／`null`／原始值根容器照常按值编码）。现在 `canonical` 只做一件事——稳定编码；实测键行为：`-0` 与 `0` 同键（业务上同一个数），`NaN`／`Infinity`／`undefined`／函数／`BigInt` 各自成键，`Date` 等非普通记录得 `Date:{}`、不与 `{}` 合并，字段顺序不影响键、数组顺序影响键。仍然会失败的只有平台自己拒绝的输入：函数／Proxy 在 `structuredClone` 抛 `DataCloneError`、循环引用让递归耗尽调用栈抛 `RangeError`，两者都按 `validation` 回报调用方。G02 与 U15 的「保留 `-0` 拒绝」随之删除（这是对已确认条款的**反向修订**，理由是本条指令）。② **暂停的语义澄清（代码本来就是对的，错的是我上一轮的解释）**：`enabled=false` 只做一件事——退订；`present` 仍为真时**不结算**本页未完成的刷新要求（U05）。实例的释放只由「订阅与刷新要求都空」决定，与「暂停」不是同一件事。页面自己的 `display` 是独立副本，释放共享实例不动它。新增 A05 用例把这三件事分开断言：暂停后订阅为空、刷新要求仍在、实例不释放、在途任务结束后后继请求照常发出、暂停页仍拿到结果、要求结算后实例才释放而页面副本仍在。**被否的备选**：把「最后一个需求退出就 abort 在途」改成「让在途跑完再释放」——那会让一个没人要的慢请求继续占着并发槽（60 秒的报表就是 60 秒），与「该停就停」冲突，故不改；halt 的边界仍是「订阅与刷新要求都空」。 | 本次落地；`src/source.ts`、`src/public-types.ts`、`tests/core.test.ts`（A05／A15／A18）、统一文档 §2／§3／§4／§5、`DESIGN.md` §4.1／§4.2、README、`scripts/check-docs.mjs`（主题引用表） |
| ADR-33 | 参数键的稳定编码交回 npm 包（确认人 2026-09-16：「canonical 整个都可以删掉吧？毫无意义啊，我们有 npm 包处理啊」） | **采纳**。ADR-32 去掉合法性判断之后，`canonical` 只剩「对象键排序 ＋ 数组保序」，而这正是 `fast-json-stable-stringify` 的全部职责，继续自己写只是重复实现。落地：删掉 `canonical` / `canonicalArray` / `canonicalRecord`（3 个函数、约 25 行），`parameterKey` 与 `prepareParameters` 直接调用该包；`prepareParameters` 现在是四步：复制 → `stringify` → `deepFreeze` → 可选 `validate`。**本条改判 ADR-30**：ADR-30 以「守卫无论如何要自己走一趟，所以自带编码更省一趟遍历」为由拒绝该包；ADR-32 删掉守卫后该理由不成立。编码语义随之回到 `JSON.stringify`：`-0` 与 `0` 同键，`NaN` / `Infinity` 按 `null`，`undefined` 与函数字段按省略，`Date` 按其 ISO 字符串，循环引用抛 `TypeError`。打包：`vite.lib.config.ts` 把它与 `vue` 并列列为 external，产物不重复打包，`dependencies` 声明它。源码 1038 → 1007 行、结构分支 117 → 105、函数 80 → 76。 | 本次落地；`src/source.ts`、`vite.lib.config.ts`、`package.json`、`tests/core.test.ts`（A15／A18）、统一文档 §1／§2／§3／§4／§5／§6.1、DESIGN §1／§2／§4.1／§4.2、README、ADR-30（改判） |
| ADR-34 | 交付面是否交付「这次结果由谁触发」（`RefreshDisplay.origin` 与 `RequestOrigin`）（确认人 2026-09-16：「这两个解决的是什么需求」） | **删除**。逐条核验：① 框架自己**从不**按它分支——`origin` 只是被写进 `display`，`src/` 里没有任何 `origin ===` 判断；② 它的语义站不住：`publish` 按接收者判定，甲点了刷新，共享同一实例的乙拿到的是 `background`，而这次请求明明是甲手刷触发的——要么它是一个**资源级**事实（该记在任务上），要么只是「本次交付是否满足了我的刷新要求」（调用方自己就有）；③ 调用方要的东西可以自己得到：`await refresh()` 返回 `success` 时本页 `display` 已经是那次结果（结算在交付之后），记下它的 `updatedAt` 即可判断「这次是不是我手刷的」；④ 实际消费者只有示例页的两处文案（`ql-origin`、共享页「来源」）。落地：删掉 `RequestOrigin`（常量对象 ＋ 类型）与 `RefreshDisplay.origin`，`deliverTo` 少一个形参，`publish` 少一次判定；公共状态常量从 3 个降到 2 个（`ErrorOrigin`、`CancelReason`）；示例改成页面侧判定（手刷成功后记住当时的 `updatedAt`，与当前 `updatedAt` 比较）——`tests/pages.spec.ts` 对「本页刷新／共享刷新」的断言在真实浏览器里**未改一行**仍然通过，说明调用方确实能自己得出这个事实。源码 1008 → 约 990 行；**被否的备选**：把它改成资源级事实（每次任务记一个触发者）——那要新增一个持久字段与一处所有权，而收益只有示例文案。 | 本次落地；`src/public-types.ts`、`src/core.ts`、`src/index.ts`、`tests/`、`examples/`、统一文档 §0／§1／§2（含 §2.2 镜像）／§3／§4、`DESIGN.md` §2／§3.8、README、消费方探针 |
| ADR-35 | 固定资源定义是否保留「类型品牌 ＋ 品牌擦除」（`sourceRuntime` / `SourceRuntime`）（确认人 2026-09-16：「sourceRuntime 又是啥东西」） | **删除品牌与擦除辅助**。核验：① 品牌是纯类型装置（`declare const sourceBrand` 运行时不存在），它只买到两件事——调用方不能直接 `source.load(...)`（但那个函数本来就是调用方自己写的，藏起来不是安全边界）、以及 `P`/`T` 不变型；② 代价是一个只用于被断言的内部接口 `SourceRuntime`、一个 2 行的断言函数 `sourceRuntime`、以及 `defineRefresh` 里一次 `as unknown as`（类型上的一次谎）；③ 替代方案与 ADR-24 对 `publish` 的裁决一致：把 `RefreshSource` 的 `load` / `validate` 写成**方法**，方法参数双变，具体 Source 可以直接进入框架的擦除视图 `RefreshSource<object, unknown>`，注册表与 `readSnapshot` 都不再需要在接收点断言。落地：删掉 `sourceBrand` 与两个内部类型/函数，`Handle.source`、`Resource.source`、`buckets`、`readSnapshot`、`resourceFor`、`prepareParameters` 一律用擦除视图；`prepareParameters` 改为收「来源」而不是单独的 `validate`（顺路执行 `source.validate`）；`defineRefresh` 去掉断言。**代价**：擦除视图反向当具体 Source 用时，`T` 仍被 `unknown` 挡住（`tests/types.ts` 有反例），但 `P` 可被放宽——类型精度上让一步，换掉一个概念、一个断言函数与一次类型谎言。 | 本次落地；`src/public-types.ts`、`src/source.ts`、`src/core.ts`、`src/vue.ts`、`tests/`、`examples/`、统一文档 §2.2 镜像、DESIGN §3.3、ADR-24 |
| ADR-36 | 单字段 interface 是否值得单独起名（`RefreshLoadContext { signal }` / `RefreshManagerOptions { maxConcurrent }`）（确认人 2026-09-16：「为啥一个字段的也要封装 interface，这不是纯增加抽象负担吗」） | **两个都内联，不再起名**。核验：两者都只有一个字段，且都不在包导出面里（ADR-29 已把它们移出 `index.ts`）——调用方从来不需要写它们的名字：`load` 的第二个参数一直是解构用法，`createRefreshManager` 的参数一直是对象字面量。落地：删掉这两个 interface，签名直接写形状（`load(args, context: { readonly signal: AbortSignal })`、`createRefreshManager(options: { readonly maxConcurrent: number })`），`vue.ts`、`tests/vue.test.ts` 同步。公共类型从 10 个降到 8 个，§2.2 契约镜像随之重新生成。**保留判断标准**：多字段的语义载体（`RefreshError` / `RefreshDisplay` / `RefreshOptions` / `RefreshHandle` / `RefreshManager` / `Parameters`）继续单独起名——它们承载「这是什么事实」，而不是「一个参数的形状」。 | 本次落地；`src/public-types.ts`、`src/vue.ts`、`src/index.ts`、`tests/vue.test.ts`、统一文档 §2.2 镜像、ADR-29 |
| ADR-37 | 配置格式固定为两项 `Ref`、去掉调用方可见条件、去掉 SSR 分支、HMR 改为模块级单例（确认人 2026-09-16：「静态页和动态页保持一致 都是用 ref 允许用户修改值自动切换，其他一起修订」） | **四项都采纳**。① `RefreshOptions` 固定为 `{ enabled: Ref<boolean>; every: Ref<number>; onError? }`：两项都必需、都是 `Ref`，改 `.value` 即按新配置重新协调；`RefreshInput<T>`（值 / Ref / getter 三态）与「整个 options 可以是 Ref」一并退役（改判 ADR-22 与 ADR-23 P2-7）。`computed` 也是 `Ref`，所以响应式组合能力不丢，丢掉的只是传字面量与裸 getter 两条便捷路径。② 删掉 `options.visible`：浏览器可见性本来就由框架自己监听（`visibilitychange` → `setVisible`），这一项是「调用方额外可见条件」，全仓零调用点（示例与测试都没有用它），因此不再保留第二层可见判断。③ 删掉 SSR 分支：本库只服务 SPA，`install` 直接注册监听；消费方检查里那条 SSR 场景随之删除。④ HMR 改为模块级单例：删掉 `slots: WeakMap<App, 槽位>`、`managerKey` 与 `inject`，`useRefresh` 直接读模块内的当前协调者；安装时「现有实例还活着就拒绝，已销毁就替换」（改判 ADR-08 与 ADR-10 的形态）。连带简化：`Config` 只剩 `enabled` / `every` 两个字段，`core.coordinate` 少一项判据与两处 `null` 分支。 | 本次落地；`src/public-types.ts`、`src/core.ts`、`src/vue.ts`、`src/index.ts`、`examples/`、`tests/`、统一文档 §2／§3／§4，`DESIGN.md` §1／§6.1／§6.3，README，ADR-22／ADR-23／ADR-08／ADR-10 |
| ADR-38 | 读取点是否还抛错：`readSnapshot` 遇到编码不出的参数（确认人 2026-09-16：「调用方忘记 trycatch 或者完全不用 trycatch，毕竟这是 SPA，出现运行错误页面直接就渲染错误了」） | **读取点不再抛错：编码不出参数返回 `undefined`**。核验：全仓 9 处 `throw` 里只有这一处能被**运行期数据**触发，其余是装配误用（`useRefresh` 不在 setup、没有协调者、`maxConcurrent` 非法、安装冲突），条件静态、第一次运行就能看见，正是应当响亮失败的位置。**实测**（真 Chrome ＋ Node 探针，跑完即删）：① 事件处理器里未捕获的抛错——dev 构建 Vue 先 `warn` 再**重抛**（冒到 `window.onerror`），生产构建只 `console.error`（`logError` 的 `throwInDev` 只在 dev 分支，`throwUnhandledErrorInProduction` 默认 false），且 DOM 已更新（实测计数照常 0→1→2）；② `computed`／渲染里抛错让该子树停在上一次的值（实测文本保持原样），其余组件照常；③ `setup` 里抛错在 dev 会让 `app.mount()` 同步抛出，生产只 `console.error`。即「页面直接渲染错误」只发生在 `setup` 与模块顶层，而读取点一旦被放进 `computed`／渲染就会踩到；读取又没有通知通道，逼每个读取点写 `try/catch` 与「坏参数只有一个响亮出口」自相矛盾。**落地**：`parameterKey` 由「抛 `TypeError`」改为「编码不出返回 `null`」（这是事实值，不是异常）；`prepareParameters` 在这个 `null` 上抛中文 `TypeError`（提交点成为坏参数的唯一出口）；`readSnapshot` 拿到 `null` 直接返回 `undefined`。删掉 `public-types.ts` 的 `@throws`；A15 由「断言抛 `TypeError`」改为「断言返回 `undefined`」；`core.test.ts` 新增边界总账（三类入口都不抛错，含 `readSnapshot` 与 `refresh` 不 reject），`pages.spec.ts` 的行情面板失败用例由 1 次改为**连续 3 次**（每轮各通知一次、页面不崩、恢复后照常取数，`pageerror` 守卫全程在）。**代价**：`parameterKey` 不再把上游包的英文错误挂在 `cause` 上（中文消息本身已写明「存在循环引用或无法序列化的值」）。**反向修订**：ADR-19 的 P1-4 一半（给 `readSnapshot` 补 `@throws`）与 ADR-31／ADR-32／ADR-33 中「读取点抛给调用者」的表述。 | 本次落地；`src/source.ts`、`src/core.ts`、`src/public-types.ts`、`tests/core.test.ts`、`tests/pages.spec.ts`、统一文档 §3.5／§4.6／§6、`DESIGN.md` §4.1／§4.2、README、ADR-19／ADR-31／ADR-32／ADR-33 |
| ADR-39 | 外部评审指出的三处「零消费者／类型层不可达／文档残留」（确认人 2026-09-16：「OK 修订」） | **三项都采纳**。① **删掉只读投影的两个出口** `snapshot().disposed` / `snapshot().visible`（`core.ts` 4 行）：全仓读取点 0 处——`.visible` 的命中全是 `this.visible` 私有字段本体、投影产出与文档，`.disposed` 的命中全是 `this.disposed` 守卫、`handle.disposed`、`isDisposed()` 方法与测试字面量；消费者逐处取证：`examples/main.ts:183-198` 读 `entries/running/queued/resources/scheduled/flushing`、`examples/main.ts:271-278` 读 `resources/handles/entries/running/queued/scheduled`、`scripts/benchmark.mjs:103/108-112` 读 `resources/handles/running/queued/entries`、`tests/core.test.ts` 读 `resources/queued/running/handles/scheduled`；全仓没有任何 `assert.deepEqual(core.snapshot(), …)` 整对象比较，且最近的一次整组断言 `tests/core.test.ts:641-642` 只比 `[handles, resources, queued, scheduled]`。删的是**投影上多开的出口**，不是事实：`this.visible` / `this.disposed` 仍在用（`coordinate`、`refresh`、各守卫），存活已有唯一公开出口 `isDisposed()`，可见性只有写入口（ADR-37 ② 已删调用方可见条件）。门禁无依赖：`check-docs.mjs` 的 `INTERNAL_FIELDS` 只断言这些名字不出现在 spec §0 词汇表；`DESIGN.md` §3.3 的表描述的是核心私有状态与命名操作，没有列投影字段。② **`SubmitResult` 的取消原因收窄到唯一可达成员 `disposed`**：`submit` 全文只有一条取消路径（`core.ts:220`），`Superseded` 只出现在 `settleRefreshes`（`core.ts:235`），它结算的是**本页已发出的刷新要求**、经 `refresh()` 的 `RefreshResult` 交付，因此原来「可达子集：`superseded` / `disposed`」的类型、`public-types.ts` 注释与 `DESIGN.md` §3.3 那一行三处都在说谎。`RefreshResult` 的三值**不动**：`superseded`（换身份结算）、`unavailable`（失去存在结算）、`disposed`（销毁结算）都真可达。同步改了 spec §2.2 的镜像行——它是 `check-docs.mjs:55-63` 对源文件逐行比对的产物，不同步会直接让 `check:docs` 变红。③ **README 测试覆盖清单按实况重写**：`tests/vue.test.ts` 对 `visible`/`setVisible` 零引用（可见性由 `tests/core.test.ts` 的 `setVisible` 用例与浏览器场景「A04/A06 祖先 KeepAlive 失活与受控 `visibilitychange`」覆盖），同一句里的「整段 `options` 换对象」也是 ADR-37 的残留（现在只读 `options.enabled.value` / `options.every.value`，测试全部是改 `.value`）。**订正外审的两处定位**：DESIGN 的状态域表是 §3.3 而非 §3.8；spec §2.2 是机器镜像，改公共类型必须同步，性质比「顺手同步」硬。**遗留待确认**：无。 | 本次落地；`src/core.ts`、`src/public-types.ts`、`tests/`（无改动，仅复跑）、统一文档 §2.2／§2.4、`DESIGN.md` §3.3、README、ADR-37 |
| ADR-40 | 显式刷新用哪个请求的结果：等一个「本次动作之后启动」的请求，还是直接用已有的（确认人 2026-09-17：「有请求就直接使用呗，没启动就直接启动」） | **改为直接用已有请求**。原语义由 `floor` 表达：排队未启动的任务算「动作之后启动」可直接满足；已在执行的算「之前启动」，因此新建要求时 `min = 该任务版本 + 1`，等它结束后由 `refill` 补一次后继请求。本条推翻后半：现在只登记一个要求，`min` 取 `resource.issued`——有请求时它就等于那个请求（排队或已在执行）的版本（`enqueue` 每次都把 `issued` 写成新任务的版本，`DESIGN.md` §3.2 已在用这条等价关系），没有请求时由本次要求当场登记的任务满足。落地：删掉 `floor`（方法 ＋ JSDoc），`refresh` 直接写 `min: resource.issued`。**`refill` 保留，但它现在只有一个来源**：`publish` 先算好这一批要结算的要求再交付（`core.ts:503-509`），交付回调里重入登记的刷新要求不在那一批里，只能由任务结束后的后继请求结算；删掉它会让这种要求永不结算。**三条连带结论**：① DESIGN §3.6 原先「空闲／排队／在执行」三行合并成一行，三种状态不再有区别；② 失败路径不再存在「下限高于本次失败任务」的要求，ADR-26 的前提消失（结论「失败不自动重试」仍成立且更强），`CODE_REVIEW.md` P2-6 抱怨的「点击被早于点击的请求结算」正是本条要的行为，它建议的「按 floor 过滤、留给后继任务」被否；③ 天花板上界问题随之消失——`floor` 的那个 `+ 1` 是全仓唯一会算出高于饱和上界（`MAX_SAFE_INTEGER`，ADR-23）的版本号的地方，实例序号一旦到达上界，等待者就永远不满足而 `refill` 会无限补真实请求；改用 `issued` 后不再有越过上界的算术（一次性探针复跑：`min = 9007199254740990`、任务版本 `9007199254740991`，两个要求都由它结算为 `success`，后继请求 0 次，探针跑完即删）。**代价（明说）**：点击时若已有请求在途，拿到的可能是**点击之前就上路**的那份结果，`refresh` 不再承诺「结果来自我这次动作之后启动的请求」——§0.1、G6、U14、A14 的措辞按此重写。**收尾（ADR-43）**：本条落地时把 `min` 留在要求上、把 `min ≤ 本次版本` 留在 `publish` 里当防御，但按本条的语义这两个判断恒真——ADR-43 已把它删掉，本条欠的那次收尸结清。 | 本次落地；`src/core.ts`、`tests/core.test.ts`、统一文档 §0.1／§1／§2.4／§3.4／§4.5、`DESIGN.md` §2（链路与符号映射）／§3.3／§3.4／§3.6／§3.7、ADR-23／ADR-26、`CODE_REVIEW.md` P2-6 |
| ADR-41 | 「为什么这份代码读不懂」的两条修法（确认人 2026-09-17：「我感觉需要 A + B」；前一轮诊断指出难读在**函数之间**：36 个具名状态槽、9 个入口、2 个汇点、4 条时间线） | **A（文档）与 B（结构）都做**。**A 的复核先更正我上一轮的过宽说法**：DESIGN 早已有 §3.4 事件表（含「重入边界」列）、§3.5 九条不变量、§5.1–§5.3 竞态三节，「没有任何一处列出这些不变量」不成立。真正的缺口是三件：①不变量只写「是什么」，不写**由谁保证**；②三条最反直觉路径的前提散在函数体外（交付回调可同步重入、`abort` 后可晚到结束、到期要先撤销身份）；③没有读法导航。**落地**：§3.5 每条不变量后注明保证者符号名，并补两条真缺失的（`task` 非空时 `task.version === issued`；订阅成立后 `handle.parameters === resource.parameters`）；新增 §3.9「三条路径的走读」（交付重入／最后退出与迟到结束／上限到期），**刻意不写行号**——行号会随编辑失效，符号名不会；DESIGN 开头加「读代码的顺序建议」（§2.1 → §3.1 → §3.9 → §3.3／§3.5）。**B**：`Resource` 由 `interface` 变为 `class`，一个身份的状态与操作都落到类里（`shortestEvery` / `dueAt` / `deliverLatest` / `publish` / `fail` / `settleWaiter` / `refill`，`deliverTo` 私有），跨实例只经新增的 `ResourceHost` 三件动作（`enqueue` / `registered` / `releaseIfUnused`）；核心用三个箭头委托，因此这些方法仍保持 `private`（与 §3.3「全部 private」的承诺一致）。顺带把 `settledAt = Date.now()` 从 `runTask` 移进 `Resource.publish` 首行（等价：原本就在 `publish` 调用前一行），于是**核心对 instance 字段的写只剩入队与注销两处**。**零行为变化，实测**：`typecheck` 0 错；`pnpm test` 37/37；`pnpm test:browser` 真 Chrome 12/12；`check:docs` 全绿；`build:demo` 通过。**代价（明说）**：总行数 973 → **1010**（+37），`core.ts` 621 → 658、有效逻辑 421 → 438、函数 49 → 54（+3 是 host 的三个箭头、+1 构造函数、+1 `deliverLatest`），结构分支 100 与 `maxCCN` 11 不变；产物 `dist/index.js` 12259 → **12599 字节**（+340，class 语法与箭头的代价）。**被否的备选**：① 新开 `src/resource.ts`——要动 `check-docs.mjs` 的 `LAYERS`、DESIGN §1 与 README 的模块数与依赖行，换来的是同文件内的同样内容；② 让 `Resource` 直接持有核心——必须把 `enqueue` / `releaseIfUnused` / `registered` 改成 public，与 §3.3 的承诺冲突；③ 让方法返回「是否已无人要」、由调用方回收——`Resource.fail` 一次结算多个要求，漏判一次就泄漏实例，错误面比一个 `host` 指针大。**遗留**：`snapshot().entries` 仍按参数键跨 Source 合并（非契约的观察面，潜伏，未改）。 | 本次落地；`src/core.ts`、`DESIGN.md` §1／§2／§2.1／§3.1／§3.3／§3.5／§3.9／§5.1、README、ADR-24／ADR-31／ADR-39 |
| ADR-42 | ADR-41 的 B 用什么边界形态：保留 `ResourceHost` 接口 ＋ 三个箭头包装，还是让 `Resource` 直接持有核心（确认人 2026-09-17：「ResourceHost 又是干啥的，还有很多一层函数包装，真的是被调用了很多次吗」→ 裁决「B」） | **采纳「直接持有核心」：删掉 `ResourceHost`，`Resource` 持有 `RefreshCore`，三个入口改 public**。**触发复核的实测**：`this.host.` 全文件只有 3 处调用——`Resource.settleWaiter` → `releaseIfUnused`，`Resource.refill` → `registered` / `enqueue`；热路径全部绕过它（`refresh` 与每轮 `flush` 的到期遍历直调 `this.enqueue`，`unsubscribe` 直调 `this.releaseIfUnused`，`refill` 本身虽每次任务结束都被调，但第一行 `waiters.size === 0` 就返回，只有交付回调重入那一次才走到 `host.`）。也就是说那 8 行（字段 ＋ 3 个箭头 ＋ 注释 ＋ 构造块）不为调用频率存在，只为「把三个方法保持 `private`」这一条；而 `RefreshCore` 并不在包导出里（`index.ts` 只导出三个函数、两个常量对象与 8 个公共类型），这条收益换不来任何对外安全，只换来读 `refill` 时多一跳。**落地**：删 `ResourceHost` 接口与 `RefreshCore.host` 字段／构造块；`Resource` 增加 `core` 字段，调用点改为 `this.core.*`；`enqueue` / `registered` / `releaseIfUnused` 去掉 `private`，JSDoc 标记「**实例入口**，不属于包契约」；DESIGN §2 ／§2.1／§3.1／§3.3 按新形态改写，`RefreshCore` 所有权行不再声称「全部 private」而是「字段全部 private ＋ 三个实例入口 public ＋ 其余内部转换 private」。**实测代价（对比 ADR-41 落地前的基线）**：总行数 973 → **987**（+14，ADR-41 的 host 形态是 1010／+37）、`core.ts` 621 → 635（有效逻辑 421 → 427）、函数 49 → **51**（+1 `deliverLatest`、+1 构造函数，host 形态是 54）、结构分支 100 与 `maxCCN` 11 不变；产物 `dist/index.js` 12259 → **12415 字节**（+156，host 形态是 +340）。门禁 `typecheck` 0 错误、`pnpm test` 37/37、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12、`build:demo` 通过。**被否的备选 C**：把 `publish` / `fail` / `settleWaiter` / `refill` 退回核心、类只留状态与 `dueAt` / `shortestEvery` / `deliverLatest`——零包装，但 ADR-41 想要的「一个身份的一生只读一个类」还回去一半。 | 本次落地；`src/core.ts`、`DESIGN.md` §2／§2.1／§3.1／§3.3、README、ADR-41 |
| ADR-43 | ADR-40 的收尾：`Waiter.min` 与 `publish` 里那次版本比较（`min > entry.version` → `continue`）是恒真的门槛，删不删 | **删掉，行为零变化**。**证据链**（三条合起来才叫恒真）：① `min` 只在登记时取一次 `resource.issued`（`refresh`），而 `enqueue` 的三个调用点都先确认没有当前任务——`refresh` 的 `if (!resource.task)`、`flush` 到期遍历的 `if (resource.task || …) continue`、`Resource.refill` 的 `this.task !== null` 返回，因此一个实例同时至多一个任务；登记时若有请求，它就是结果来源，其版本等于登记时的 `issued`。② `issued` 只在 `enqueue` 写，`nextSequence` 单调不减（`previous + 1`，到 `MAX_SAFE_INTEGER` 停在原地），所以「登记时的 `issued`」不会超过「发布时的 `issued`」。③ 发布的 `entry.version` 就是当前任务的版本，也就是发布时的 `issued`（`runTask` 在 `await` 之后与复制结果之后各复核一次 `resource.task !== task`，只有当前任务能 `publish`）。于是 `min ≤ entry.version` 恒成立、`continue` 永不执行，`satisfied` 恒等于 `[...this.waiters]` 的全量快照——「交付期间重入登记的要求不进这一批」本来就靠快照，不靠版本。**落地**：`Waiter` 收成 `{handle, settle}`，`publish` 改为取快照 ＋ `new Set(satisfied.map(…))`，`refresh` 少传一个参数。**行为零变化的实测**：`typecheck` 0 错；`pnpm test` 37/37（全仓测试与示例对 `waiters` / `version` 零读取，故无断言需要改）；`test:browser` 真 Chrome 12/12；`check:docs` 全绿；`build` / `build:demo` 通过。**实测代价**：总行数 987 → **984**（−3）、结构分支 100 → **98**（−2）、`maxCCN` 11 不变；产物 `dist/index.js` 12415 → **12326 字节**（−89）；包 37299 → **37404 字节**（gzip 重新分块，包体积不随源码行数单调；两次打包逐字节相同）。**代价（明说）**：这一层「防御」在 ADR-40 之后就没人读过，删掉它不减少任何保护；反过来，留着它的代价是每个读代码的人都要先自己反推一遍「为什么不会跳过」——这正是「看不懂」的来源。**遗留（未决，等确认人）**：`Entry.version` / `Task.version` / `Resource.issued` 现在已无任何读者（`snapshot().entries` 的 `version` 在 `tests/` 与 `examples/` 里也无人读，只有 `Object.keys(entries)` 与 `.data`），删掉它们要重写 U18 与 `DESIGN.md` §3.2／§3.3／§3.5 第 8 条，属另一次改动（已由 ADR-45 落地）。 | 本次落地；`src/core.ts`、`DESIGN.md` §3.2／§3.3／§3.4／§3.6／§3.7／§3.8／§3.9、README、统一文档 §3.3（U12）／§4.4（A12）／§5（G04）、ADR-18／ADR-40 |
| ADR-44 | 上一轮审查列出的「恒不触发／冗余」条件删不删（确认人 2026-09-17：「一起都修改 无效代码留着都是负债」） | **全删，并连带删掉因此失去调用点的 `registered` 入口**。清单与依据（都是「按代码可证」，不是感觉）：① `Resource.refill` 的 `if (!this.core.registered(this)) return`；② `RefreshCore.releaseIfUnused` 的 `if (!this.registered(resource)) return`；③ 于是 `RefreshCore.registered` 一个调用点都不剩，方法本身一并删除——实例与核心的边界从三个入口收成**两个**（`enqueue` / `releaseIfUnused`）；④ `addHandle` 的 `if (this.disposed) return`（唯一调用点在 `useRefresh` 里，前面刚查过 `current.isDisposed()`，中间没有用户代码）；⑤ `readSnapshot` 的 `if (this.disposed) return undefined`（`dispose` 末行清空注册表，之后没有任何写入口，返回值等价）；⑥ `Resource.shortestEvery` 的 `&& subscription.resource === this`（`subscribers` 与句柄的 `subscription` 成对写入）；⑦ `vue.ts` 里 `handle.disposed \|\| core.isDisposed()` 的前半句（句柄刚建好、外部还拿不到它，能发生的只有 `onError` 里把协调者 dispose）。**顶替它们的不变量**：DESIGN §3.5 新增第 12 条——注销只在订阅与要求都空时发生，且空实例再也拿不到新的边（新订阅与新要求都只能落在 `resourceFor` 当前返回的实例上）。**风险（明说）**：删掉 ② 之后，这条不变量成为唯一屏障；若将来有路径让已注销实例重新获得订阅或要求，`releaseIfUnused` 会把这个键上的**新**实例误删（静默损坏）。这是换回 3 个结构分支所付的价钱。**复核中确认「不是死分支」因而不动的**（有触发证据）：`settleWaiter` 的删除守卫（交付回调里同步 `submit` 会先结算这一批要求）、`activate` / `deactivate` 的 `handle.disposed`（Vue 3.5.42 `runtime-core`：KeepAlive 的 `onBeforeUnmount` 对当前子节点 `resetShapeFlag` 后把 `onDeactivated` 排进 `queuePostRenderEffect`，而真正的 unmount 先 `scope.stop()`＝`removeHandle`，因此 `deactivate` 确实在句柄释放之后才到）、`flush` 丢弃过期任务（`load` 是同步起手的，它内部的重入可能释放队列后段的实例）、`expire` / `runTask` 的身份复核、`publish` / `fail` 的订阅者复核；另外 `readConfig` 的 `typeof every !== 'number' \|\|` **不是**冗余——`Number.isSafeInteger` 的签名是 `(number: unknown) => boolean`，没有类型谓词，删掉它 `every` 就不再收窄成 `number`（差点误删）。**唯一的行为变化**：`publish` 的 `refreshing` 循环补上与订阅者同口径的复核——要求已在交付期间被结算（换身份、失活、卸载）的句柄不再收这一次结果；这不是新语义，而是让代码回到 U12 的字面（「本页**未结算**的刷新要求」）。反向验证：临时去掉该复核，新测试 `A12 交付期间被结算的刷新要求不再收这一次结果` 变红（`1 !== 0`），加回后 38/38。**实测**：总行数 984 → **985**（+1：注释换掉了守卫）、结构分支 98 → **95**（−3）、`maxCCN` 11 不变；`dist/index.js` 12326 → **12158 字节**（−168）；包 37404 → **37705 字节**（gzip 重新分块，两次打包逐字节相同）。门禁 `typecheck` 0 错误、`pnpm test` 38/38、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12、`build` / `build:demo` 通过。 | 本次落地；`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`DESIGN.md` §2／§2.1／§3.1／§3.3／§3.4／§3.5／§3.9、README、统一文档 §5（G04）、ADR-41／ADR-42／ADR-43 |
| ADR-45 | 任务版本编号（`Resource.issued` / `Task.version` / `Entry.version`）删不删（确认人 2026-09-17：「需要做」，承接 ADR-43 的遗留） | **删掉**。**取证**：这三个字段在 ADR-43 删掉 `Waiter.min` 与 `publish` 那次比较之后已经**零读者**——`version` 的全部读取点就是那一次比较；此后 `Entry.version` 只被写、从不被读，`Task.version` 只被复制进 `Entry`，`Resource.issued` 只被 `enqueue` 自增后再无人看。全仓（`src` / `tests` / `examples` / `scripts`）除 `examples/main.ts` 的两处**类型声明**外没有任何读取；`snapshot().entries` 的 `version` 在测试与演示里同样无人读（只有 `Object.keys(entries)` 与 `entries[key].data`）。**落地**：`Task` 收成 `{resource, controller}`，`Entry` 收成 `{data, updatedAt}`，删掉 `Resource.issued`，`enqueue` 不再分配版本，`runTask` 不再写版本；`nextSequence` **保留**——它现在只服务 `Handle.operationId`（声明代次），ADR-23 的结论（安全整数上界、停在原地、不给调用方第三种结果）随之收窄到代次一处，U18 与 A18 的措辞照此重写。文档同步：§0.2「任务」行去掉版本说明、U18、A18、`DESIGN.md` §3.2（标题「身份与版本域」→「身份与代次域」）／§3.3（Resource / Task / Entry 三行）／§3.5（删掉原第 8 条，9–12 顺次上移，§3.9 与 `src/core.ts` 里对「第 12 条」的引用改为「第 11 条」）／§7 第一条；并订正 ADR-44 漏掉的三处文档残留（`DESIGN.md` §1 / §2 / §2.1 与 mermaid 里的「三个入口」与 `registered` 字样）。**零行为变化**：`typecheck` 0 错、`pnpm test` 38/38、`check:docs` 全项通过、`test:browser` 12/12、`build` / `build:demo` 通过。**实测**：总行数 985 → **979**（−6）、结构分支 95 → **95**（不变——`nextSequence` 的饱和分支属于 `operationId`，仍在）、`maxCCN` 11 不变；`dist/index.js` 12158 → **12067 字节**（−91）；包 37705 → **37485 字节**（两次打包逐字节相同）。**这一步真正省下的不是行数**：它是 ADR-40 那条时间线最后的影子——「结果属于哪个版本」这个问句在代码里彻底消失，`Entry` 只剩「一份结果 ＋ 它产生的时刻」。 | 本次落地；`src/core.ts`、`examples/main.ts`、`tests/core.test.ts`、`scripts/check-docs.mjs`、`DESIGN.md` §1／§2／§2.1／§3.2／§3.3／§3.5／§3.9／§7、README、统一文档 §0.2／§3.6（U18）／§4.7（A18）／§5（G04）、ADR-23／ADR-43／ADR-44 |
| ADR-46 | 包是否保留「读取共享结果」这条出口、显式刷新是否保留回执（确认人 2026-09-17：「snapshot() 本身有作用吗…pinia 里面不就是存的最新数据吗，数据不是和resource参数绑定吗…refresh 不需要返回，它就是通知我要新数据」→ 裁决「直接删，refresh 改 void」） | **删掉两条读取出口 ＋ 刷新改 `void`**。① 删 `RefreshManager.readSnapshot`（公开入口）与 `snapshot().entries`（非契约投影里按参数键拍平的结果字典）：身份本来就是（Source ＋ 完整参数值），而 `entries` 只按参数键，两个 Source 用同一个键会互相覆盖——删掉之后这个潜伏问题不是被修好，而是**不存在了**。② 删 `RefreshResult` 类型，`refresh()` 改为 `void`。**失败通道同时拓宽**（必须连做，否则是净功能损失）：`fail` 原来只把 `onError` 发给订阅者，而暂停页刷新失败的唯一信号是回执；现在通知面是「仍有效的订阅者 ∪ 本次有未完成要求的页面」，去重后各通知一次。**随之收敛的**：`Waiter` 接口没有 `settle` 可写了，`Resource.waiters` 收成 `Set<Handle>`（一个句柄此刻想不想要一次取数——标志而不是队列）；`CancelReason` 的 `Superseded`／`Unavailable` 失去唯一消费者，常量对象收成单成员（只剩 `submit` 用的 `disposed`）；入口条件不成立（已销毁／未声明身份／环境不允许）时 `refresh` 直接返回、不通知，只有配置非法按 `configuration` 通知一次。**语义变化（明说）**：a) 未订阅的页面不再有任何读取通道——结果只经页面自己的 `display` 交付，要落到业务 Store 由页面适配层写；b) 失败通知不再区分「我手刷失败」与「后台周期失败」（`RefreshDisplay.origin` 已按 ADR-34 删除，回执是最后的分辨来源，现在也没了）；c) 交付回调里**同一个句柄**再次 `refresh()` 会被这一批吸收（它刚收到这一批的结果），**另一个句柄**的重入登记仍由 `refill` 补一次后继请求。**实测**：`typecheck` 0 错；`pnpm test` **39/39**（删掉 A15 的旧测试，新增「暂停页刷新失败经 `onError` 通知」与「交付回调里新登记的要求由后继请求满足」两条；反向验证：把 `fail` 的通知面收回订阅者，前者立刻变红）；`check:docs` 全项通过（叶子 18 → **17**、测试标题 50 → 51）；`test:browser` 真 Chrome 12/12；`build`／`build:demo` 通过。**度量**：总行数 979 → **935**、结构分支 95 → **91**、`maxCCN` 11 不变；公共类型 8 → **7**；`dist/index.js` 12067 → **11120 字节**。**代价**：包不再承担「读共享结果」这个能力（§1 的 G9 撤销、U15 收窄为提交侧参数边界、A15 删除、§4.6 随之并入 §4.6 原 §4.7）；`refresh()` 变成单向通知，调用方要判断「这次结果是不是我的动作之后产生的」得自己记点击时刻再比较 `updatedAt`（演示与测试都照这个口径改了：`manual` 由「结果时间相等」改成「结果时间 ≥ 点击时刻」）。 | 本次落地；`src/public-types.ts`、`src/core.ts`、`src/vue.ts`、`src/index.ts`、`examples/main.ts`、`examples/sources.ts`、`examples/pages/query-list.ts`、`examples/pages/shared-pair.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tests/scenarios.ts`、`tests/pages.spec.ts`、`scripts/benchmark.mjs`、`DESIGN.md` §1／§2／§2.1／§3.2–§3.6／§3.8／§3.9／§4.1–§4.3、README、统一文档 §0.2／§1／§2／§2.1／§2.2／§2.4／§3.3／§3.4／§3.5／§4.3–§4.6／§5（G04）、ADR-34／ADR-40／ADR-44 |
| ADR-47 | 清完读取出口与刷新回执之后，剩下哪些结构真的没有消费者（确认人 2026-09-17：「都需要清理」） | **四项全清，行为零变化**。① `Handle.subscription` 从 `{resource, every}` 收成 `Resource \| null`：那个 `every` 是配置快照的镜像（唯一读者 `Resource.shortestEvery`、唯一写入者 `coordinate`，每次配置变化抄一遍），而 DESIGN §3.7 本来就写着「派生值：不重复保存」；现在 `shortestEvery` 直接从 `handle.config()` 现算，并新增 §3.5 第 12 条不变量顶替那次复核——订阅成立 ⟹ 配置快照存在、`enabled` 为真、`every` 是正安全整数（这正是 `coordinate` 的入口条件）。② `parameterKey` 去掉 `export`：`readSnapshot` 是它最后一个外部调用者（ADR-46 删除），现在只剩 `source.ts` 内的 `prepareParameters` 自用。③ 验证台的 `HarnessBridge.submit` 删除：`tests/` 里零调用（用到的只有 `snapshot` / `refresh` / `enable` / `resolve` / `mutatePage` / `nestedOuter` / `visibility` / `unmount`）。④ **删掉整条声明代次轴**：`Handle.operationId`、`RefreshError.operationId`、`identity()`、`nextSequence()`、`submit` 里的推进、以及 ADR-23 的饱和上界。**为什么敢删**（取证）：`src/` 里没有真读者——`operationId` 的唯一去处就是它自己的通知字段，`examples/` 一处不用，只有 4 条测试断言与文档在支撑；删掉那些断言之后 A03／A13／A18 的行为断言一条不少。U18 收成「参数只在提交边界准备一次」，A18 去掉上界句，§0.2／§2.4／README 不再提「据此认领」。**语义变化（明说）**：`onError` 不再能分辨「这条失败是哪个 `useRefresh` 的」；一个页面里两处 `useRefresh` 共用同一个 `onError` 时，用自己的闭包标记或各自的 `display` 分辨（README 与 DESIGN 已按此改写）。**顺带订正** ADR-46 遗留的四处文档残留：§2.4 的 `refresh` 增删结转表还写着 `cancelled` / `unavailable`、取消原因段还在讲 `superseded` / `unavailable`、§2.5 标题还带「只读快照」、U05／U06／DESIGN §3.4 还写「结算为 `unavailable`」；同时把已失效的 `operationId` 从 `check-docs.mjs` 的 `INTERNAL_FIELDS` 里清掉。**实测**：`typecheck` 0 错；`pnpm test` 39/39；`check:docs` 全项通过（叶子 17、测试标题 51）；`test:browser` 真 Chrome 12/12；`build`／`build:demo` 通过。**度量**：总行数 935 → **917**、结构分支 91 → **89**、`maxCCN` 11 不变；`dist/index.js` 11120 → **10801 字节**；公共类型仍是 7 个（`RefreshError` 少一个字段）。 | 本次落地；`src/core.ts`、`src/source.ts`、`src/public-types.ts`、`src/vue.ts`、`examples/main.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`scripts/check-docs.mjs`、`DESIGN.md` §1／§2.1／§3.2／§3.3／§3.4／§3.5／§3.8、README、统一文档 §2.2／§2.4／§2.5／§3.3–§3.5／§4.3／§4.6／§5（G04）、ADR-23／ADR-43／ADR-45／ADR-46 |
| ADR-48 | 错误来源该不该按「哪一步失败」分类，以及 `CancelReason` 是否留在公共面（确认人 2026-09-17：「删除，修订文档 取值命名统一」；依据本轮外部审查「调用方不关心『哪里出错』」） | **采纳并落地**。① `ErrorOrigin` 由三值收成两值 `request` / `caller`：按**调用方能采取的动作**分两侧（`request` 可重试；`caller` 要改的是自己的参数或配置），不再按「哪一步失败」分。② `CancelReason` **连同代码内部的常量对象一起删除**，`SubmitResult` 的取消分支不再带 `reason`。③ `RefreshError.operationId` **维持 ADR-47 的删除**。**取证（三条）**：(a) `src/` 对 `origin` / `reason` / `status` 的比较运算 **0 处**（四个出现全是构造点：`core.ts:151` / `308` / `333`、`vue.ts:79`），框架自己不为这套分类写任何分支，唯一读者是调用方那个 `if`；(b) 旧契约在 `RefreshResult.error.origin` 上**已经**把同一维度收成 2 值（`1cbb761:src/public-types.ts:78` = `Request | Configuration`），「三步」从来不是世界的事实，而是某条通知通道的形状；(c) 仓库里那个调用方的 `if` 就是 `examples/` 四处，**四处全部只与 `'request'` 比较**，没有一处区分 `validation` 与 `configuration`（唯一把 origin 当诊断文本打印的 `query-list.ts:50` 随本条改为固定文案）。**重开记录**：本条**重开 ADR-17 的 origin 项**（原裁决「错误 origin 维持 `background`／`validation`／`configuration`」，理由是「保留的是描述失败步骤的那个名字」——正是本条推翻的口径），并**收尾 ADR-21(1)**（那次把 `Background` 改名 `request` 只修了「同一个字符串在两条通道上撞车」，没有问这个维度要不要存）。**`operationId` 的正面冲突与结论**：本轮审查把它判为「留（唯一例外）：两处 `useRefresh` 共用 `onError` 时认领归属」，而 ADR-47 刚以「仓库内零读者」删掉它。按**本轮审查自己的判据**（调用方能不能为它写出一个别的取值写不出的动作）它也站不住：那个动作调用方用闭包包装就写出来了（`onError: e => shared(e, 'A')`），不需要框架发号；仓内也没有任何一处共用同一个 `onError` 函数对象。故维持删除。**契约变化（breaking）**：`ErrorOrigin` 取值域收窄、`SubmitResult` 的取消分支少一个字段、包导出少了 `CancelReason`（现为三个函数 ＋ 一个常量对象 ＋ 7 个类型）。**零行为变化**：两处调用方失败的分类只改标签，`submit` 仍返回 `rejected`、配置非法仍只通知一次。**实测**：`typecheck` 0 错误；`pnpm test` 39/39；`check:docs` 全项通过（叶子 17、测试标题 51）；`test:browser` 真 Chrome 12/12；`build` / `build:demo` 通过；干净消费方 `typescript@4.9.5` 零错误（新增两条反例断言：`ErrorOrigin.Caller` 的值／类型两用、取消分支不得读 `reason`）＋ 客户端场景与「销毁后 `submit` → `cancelled` 不带 `reason`」通过。**度量**：总行数 917 → **906**、结构分支 89 → **89**、`maxCCN` 11 不变；`dist/index.js` 10801 → **10665 字节**；包 34946 → **34792 字节**。**顺带订正三处门禁抓不到的文档漂移**（ADR-46/47 遗留）：`统一刷新管理.md` §7「8 个公共类型」→ 7；`DESIGN.md` 两处「三个状态常量对象与 8 个公共类型」（还与同文件 §3.8 的「两个」自相矛盾）→「一个状态常量对象与 7 个公共类型」；README 的「37 个用例（`core` 30）」→「39 个用例（`core` 32）」。 | 本次落地；`src/public-types.ts`、`src/core.ts`、`src/vue.ts`、`src/index.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`examples/pages/query-list.ts`、`统一刷新管理.md` §1／§2.1／§2.2／§2.4／§2.5／§3／§4／§5／§7、`DESIGN.md` §1／§2／§3.8／§4.1、`README.md`、ADR-17／ADR-21／ADR-47 |
| ADR-49 | 导出面计数与用例数没有任何门禁（确认人 2026-09-17：「需要做」；先回答「为啥要思考错误来源于哪里」——见 ADR-48 的动作分侧，以及统一文档 §2.4 新增的这一段） | **采纳：`check-docs` 新增第 16 组断言，计数与名单全部从代码现算，不维护任何清单。****触发证据**：ADR-46/47/48 三次收缩（类型 8 → 7、常量对象 2 → 1）之后，四处数字在**五次全绿交付**里一直是错的——`统一刷新管理.md` §7「8 个公共类型」、`DESIGN.md` 两处「三个状态常量对象」（还与它自己 §3.8 的「两个」互相矛盾）、README「37 个用例（`core` 30）」（ADR-44 起就没跟上）。**落地**：脚本读取 `src/index.ts` 的两条 `export { … }` / `export type { … }` 现算「函数 / 状态常量对象 / 公共类型」三个名单，并读 `tests/*.test.ts` 现算用例总数与每文件数；断言分两半——(a) 三份文档里每条「N 个函数 / 状态常量对象 / 公共类型」与「N 个用例」、「`` `tests/x.test.ts` N ``」都等于实测值；(b) 凡是列出这些符号的地方（§2 的三行名单、以及 §1 与 README 的 `N 个X（…）` 括号清单）都必须**恰好**是导出的那批符号。§4.1 的「一个函数」刻意不在 (a) 的取数范围（(a) 只读同时提到公共类型或常量对象计数的句子）。**反向验证（7 条探针，逐条改坏→必须变红→复原）**：README 公共类型计数 7→8、用例总数 39→37、`core` 用例数 32→30、DESIGN 常量对象 1→2、§2 类型行删一个名字、§2 常量行加回已删的 `CancelReason`、§1 类型括号清单删一个名字——七条全部以预期消息失败，复原后全绿。**顺带**：第 15 组注释里残留的模块名 `src/manager.ts` 改为 `src/core.ts`。**本条不改代码，也不改包内容**（`scripts/` 不进 tarball）。**订正 ADR-48 的交付记录**：它记的 34792 字节 / `89aefd6e…` 是**在 README 最后一处编辑之前**打出来的包（那次编辑改了消费方 `@ts-expect-error` 清单，而 README 被 npm 强制打进包内），与提交树不符；本条按当前树重打包（34815 字节 / `47fbfeb0…`），逐条目比对确认 9 个条目里 8 个与 ADR-48 的 tarball 逐字节相同、只有 `package/README.md` 不同（`ed7b04e6981a…` → `b1d28ef58a10…`），连跑两次可复现，并用这份 tarball 重跑了干净消费方检查。**纪律（写进 G04）**：README 在包内，任何 README 编辑都必须发生在打包之前。 | 本次落地；`scripts/check-docs.mjs`、`统一刷新管理.md` §2.4／§5（G04）、`README.md`（门禁清单以脚本自述为准，无需改）、ADR-48（交付记录订正） |
| ADR-50 | 本轮三处收尾：G04 包记录要不要门禁、手抄的 `INTERNAL_FIELDS` 漏项、外部评审的基线（确认人 2026-09-17：「A ，origin 有必要存在吗  其他问题修复」） | **三条都按最小面落地**。① **G04 的包尺寸/哈希不设门禁（裁决 A）**：`check:docs` 保持「只读、不引入 npm 依赖」，代价由纪律承担——G04 已写明「README 在包内，任何 README 编辑都必须发生在打包之前」，且每次交付都逐条目比对包内 9 个条目与工作树一致（ADR-49 就是这样发现 ADR-48 记录的包比提交树旧了一个 README 编辑）。被否的备选：B（门禁里跑 `npm pack`——门禁不再只读、依赖 npm、每次 +1~2 秒）、B′（把包内 `package/README.md` 的 sha256 记进 G04 做纯读比对——每改一次 README 就要重算一个数）。② **`INTERNAL_FIELDS` 补 `active`**：这张手抄清单是第 13 组检查 (c) 的输入，脚本自述「漏一项只弱化、不失败」，所以漏了 `active`（`Handle` 的内部字段，DESIGN §3.3 里有）谁也不会知道。被否的备选「从 DESIGN §3.3 机械派生」：派生出的字段名还得减掉公开交付面里同名的 `args` / `data` / `updatedAt`，那是拿一份清单换另一份清单，不是消掉清单。③ **外部评审必须声明所读提交**写进本文件「维护规则」：本轮那份审查停在 `1cbb761`（落后两个提交），它列的 10 个取值里 6 个当时已不存在。**验证**：把 `active` 临时写进 §0，第 13 组 (c) 按预期变红并报 `names the internal field active`，复原后全绿。**本条不改代码、不改包内容**（`scripts/` 与 `ADR.md` 都不进 tarball），G04 里 ADR-49 的 34815 字节 / `47fbfeb0…` 继续有效。 | 本次落地；`scripts/check-docs.mjs`、`ADR.md` 维护规则、ADR-46／ADR-48／ADR-49 |
| ADR-51 | `onError` 是否只报共享请求失败：删掉 `RefreshError` 信封与最后一个常量对象 `ErrorOrigin`（确认人 2026-09-17：「按照O2」，触发于「请求都合并了，为啥 onError 还要区分到底是谁发起的，如果同一个 resource 他们的动作应该一样吧」） | **采纳 O2**。① `onError?: (error: unknown) => void`，**只报共享请求失败**（`Resource.fail`，含框架上限到期），参数就是原始异常。② 删 `RefreshError` 与 `ErrorOrigin`（包内最后一个常量对象）。③ 输入类问题只在它发生的位置报：参数不可用＝`submit` 同步返回 `rejected`；配置非法＝**不通知**（框架只负责不订阅、不请求，修正后自动恢复）。④ 连带删掉 `vue.ts` 的 `reported` 去重标志与 `core.ts` 的 `INVALID_CONFIG_MESSAGE`（三处引用一起消失）。**依据（四条取证）**：(a) ADR-48 只论证到「请求失败必须能广播」，本条补上另一半——两类事件**产生在不同位置**（输入类在页面自己的调用点或自己的 ref 上），按位置分账比在一条共享通道上贴标签便宜；(b) `RefreshError.error` 在全仓**零读者**：`src/` / `tests/` / `examples/` 里 `.error` 的命中全是 `console.error` 的同名巧合；(c) `origin` 仓内四处消费者里三处是反向排除（`!== 'request'` 提前返回），一处（`query-list`）是提示文案，而那条分支在演示里走不到（配置是常量 ref、参数由页面构造）——这一位支撑的动作只是「别把输入错误当取数失败」，那正是按位置分账后自然成立的事；(d) 字段删除后 `ErrorOrigin` 无写入者也无线读者，`reported` 的唯一理由（「配置非法只报一次」）消失，`INVALID_CONFIG_MESSAGE` 的两处引用一起消失——按 ADR-44/45/46/47 的口径，三者都是负债。**回退的既有裁决（明说）**：ADR-28（状态常量对象作为值导出——它导出的三个值里 `RequestOrigin` 已由 ADR-34 删除、`CancelReason` 由 ADR-48 删除，本条的 `ErrorOrigin` 是最后一个）、ADR-09（配置非法的电平通知：连续非法去重报一次）；ADR-48 的「按动作分两侧」随值本身一起被取代。**契约变化（breaking）**：公共面从「三个函数 ＋ 一个常量对象 ＋ 7 类型」变成「三个函数 ＋ **0** 常量对象 ＋ **6** 类型」；`onError` 的参数类型由 `RefreshError` 变成 `unknown`，语义收成「这次取数没拿到」。**语义变化（明说）**：(a) 页面若忽略 `submit()` 的同步返回值，参数被拒就没有任何信号（那是它自己输入的回声，位置足够近）；(b) 配置非法期间只是安静地不订阅、不请求，页面要显示「自动刷新已暂停」得自己读 refs；(c) `onError` 不再需要来源判断——它的每一次调用都意味着一次取数失败。**实测**：`typecheck` 0 错误；`pnpm test` 39/39；`check:docs` 全项通过（叶子 17、测试标题 51；第 16 组断言新增「导出类别为 0 时不许留行」，3 条 O2 专属反向探针各自变红后复原）；`test:browser` 真 Chrome 12/12；`build` / `build:demo` 通过；干净消费方 `typescript@4.9.5` 零错误（含取消分支不得读 `reason` 的反例）＋ 运行期新增「导出面恰好是三个函数」的断言并通过。**度量**：总行数 906 → **872**、结构分支 89 → **87**、`maxCCN` 11 不变；`dist/index.js` 10665 → **10211 字节**；包 34815 → **31737 字节**（两次打包逐字节相同，包内 README 与工作树哈希一致）。 | 本次落地；`src/public-types.ts`、`src/core.ts`、`src/vue.ts`、`src/index.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`examples/pages/*`、`scripts/check-docs.mjs`、`统一刷新管理.md` §0.2／§1／§2／§2.1／§2.2／§2.4／§2.5／§3／§4／§7／§5（G04）、`DESIGN.md` §1／§2／§3.3／§3.4／§3.8／§6.1、`README.md`、ADR-09／ADR-28／ADR-34／ADR-46／ADR-48／ADR-49 |
| ADR-52 | 参数值域与参数隔离：外部评审主张「把值域从沉默改成显式拒绝」（确认人 2026-09-17：「严格线，类型约束加运行时兜底，并入，保持rejected，消费者副本推荐做」） | **五项全采纳**。**① 危害先取证（实测，真 Chrome／Node 探针跑完即删）**：`Map`／`Set`／`RegExp`／`Error`／`ArrayBuffer`／`DataView` 的内容对编码不可见，全部编码成 `{}`——两个内容不同的 `Map` 参数得到同一个身份，端到端实测「只发一次请求、B 页拿到 A 页的数据」；`Object.freeze` 后 `Map.set`／`Set.add`／`Date.setTime` 全部成功（冻结写的是属性描述符、它们写的是内部槽），`Object.values(new Map(...))` 长度为 0，因此 Map/Set 里的**普通对象**也没被冻住；`U11`「页面既改不动参数」对 Date/Map/Set 为假，而全仓冻结断言为 0 条。**② 严格线**：对象型参数只能是普通对象或数组；其余（`Date`／`Map`／`Set`／`RegExp`／`ArrayBuffer`／TypedArray…）在提交边界按非法参数拒绝，`Date` 请传 ISO 字符串。检查放在**复制之后**——`structuredClone` 已把类实例原型剥成普通对象，所以不误伤类实例；标量沿用 G02 不动（`-0`≡`0`、`NaN`／`Infinity`→`null`、`undefined` 字段省略）。**③ 参数隔离改为「每个消费者各一份副本」**：删掉 `deepFreeze`，框架私有那份不外发——`validate`、每一轮 `load`、每个接收者的 `display` 各拿一份副本；隔离从此不依赖「冻得住」这个规范事实，`U11` 那句话对**任何**值成立（ADR-31 ② 的「框架内部一个身份一份参数对象」原样保留）。**④ 类型约束做在主闸门**：`defineRefresh<P extends JsonParameters<P>, T>`，`JsonParameters<P> = { [K in keyof P]: JsonValue }`（同态映射，保留可选与只读修饰符；换成 `Record<keyof P, …>` 会把可选字段变必填），只加在声明入口，内部 `RefreshSource<object, unknown>` 擦除视图不变；实测边界：扁平 interface／type 别名／内联嵌套／数组／字面量联合／`page?: number`／显式 `number \| undefined` 全部通过，`from: Date`／`box: Map<…>` 编译期报错，**嵌套字段用具名 interface 会误伤**（无隐式索引签名，文档写明用 `type` 或内联）；`tests/types.ts` 与消费方固定装置各加 `@ts-expect-error` 反向探针，把约束改宽（如值域换 `unknown`）时它们变成「未使用的指令」而变红（已实测）。**⑤ 违约通道保持 ADR-51**：只经 `submit` 同步返回的 `rejected`，不通知、不抛错；运行期检查只是 JS 调用方／`any`／`as`／跨包旧数据的兜底，正常 TS 调用方唯一能撞到 `rejected` 的路径仍是他自己写的 `validate` 返回假值。**评审基线的订正**：该评审的 `SubmitResult` 引的是 **L54-L57**，这只在 `85f1f32`（HEAD~1）成立（HEAD 是 L32-L35），它引作依据的 `ErrorOrigin`／`caller`／「U03 通知一次 caller」都已被 ADR-51 删除——这是 ADR-50 写下「外部评审必须声明所读的提交」后的第一个案例；它的实质建议（拒绝而非转换、不收窄 `P`、不动 `display`）经核验成立，只有依据过期。**改判**：ADR-32 的「框架不判断值是否合法、非普通记录照收」收窄为「对象型值必须是普通对象或数组」——只改**框架机制前提**那一半，业务字段合法性仍归调用方与 `validate`；**不改判 ADR-33**（编码仍交给 `fast-json-stable-stringify`），也不动 `display` 的形状与键的用法。**附带（同条并入）**：门禁新增第 17 组「退役术语」并入维护规则；修掉 ADR-51 遗留的两处 `caller`（`DESIGN.md` §4.1、`U15`）；订正「函数字段按省略」（4 处）——函数在 `structuredClone` 就被拒绝，走不到编码。**代价（明说）**：源码 872 → **905** 行、结构分支 87 → **89**、`dist/index.js` 10211 → **10552** 字节（值域检查、值域类型与反向探针）；每份交付多一次 `O(K)` 参数复制，换来的是不需要任何「冻得住」的假设。**被否的备选**：① 转换（`Date`→ISO、`Map`→entries）——`P` 会撒谎且 `display.args` 不再是页面传进去的东西；② 收窄 `P` 到 `Record<string, JsonValue>`——会把本仓最常见的 `interface` 参数声明全打红；③ 入参改传 JSON 字符串——`JSON.parse` 自身仍会塌（`1e999`→`Infinity`→`null`、大整数精度丢失），且会失去「字段拼错＝编译错误」；④ 手搓单趟复制＋编码——要自己实现键排序（改判 ADR-33）并踩 `__proto__` 赋值这类语义坑，而性能不是本仓目标（G04 已关闭）。 | 本次落地；`src/source.ts`、`src/core.ts`、`src/public-types.ts`、`tests/core.test.ts`（A18／A19）、`tests/types.ts`、`scripts/check-docs.mjs`、`README.md`、`DESIGN.md` §1／§3.3／§4.1／§4.3／§4.4、`统一刷新管理.md` §0.2／§2.2／§3.1／§3.2／§3.5／§4.2／§4.6／§5、ADR-31／ADR-32／ADR-33／ADR-46／ADR-48／ADR-50／ADR-51 |
| ADR-53 | 值域约束的**声明形式**没进规范、README 对显式 `undefined` 的说法与 `U15` 相反（确认人 2026-09-17：「修正文档」；触发于「你整体思考一下 这些角度都站的住吗 是否有更合适的数据结构」→「也就是说没有问题？」） | **只改文档；两条证据缺口留待裁决**。**① 审计口径先订正**：本轮把「类型擦除与数据结构是否服务需求」按 `G1–G10`／`U01–U18`／`A01–A19` 复核（上一轮是先证类型机制、再找需求背书，因果讲反了）。结论两条：擦除是**需求后果**——§0.1「协调者：一个 App 一个实例」＋ `U17` ＋ `U02`「身份由 Source 身份 ＋ 完整参数值决定」三者共同要求同一协调者内多 Source、多参数形状并存，需求若改成「每 Source 一个协调者」，`RefreshCore<P, T>` 立刻成立；四处数据结构各能一对一翻译成需求句子（`buckets` ↔ `U02` 两段身份、`queue` ↔ `U09` FIFO、`running` ↔ `U09`「真实尚未结束」、`subscribers`／`waiters` ↔ `U07`／`U12`），没有更合适的替代。24 处 `unknown` **在需求层中性**（全部换成 `any`，`G`／`U`／`A` 一条都不变），它服务的是自定工程门槛（0 `any`／1 处 `as`／TS 4.9 消费基线）。**② 两处文档缺陷（本轮修正）**：(a) `U15` 只约束「值」，没约束「声明形式」——嵌套字段用具名 `interface` 会被 `JsonParameters` 打红（TS2344，实测），而 ADR-52 那句「文档写明用 `type` 或内联」在三份文档里**零命中**（只命中 `ADR.md` 自己），即它写作当时与工作树不符；(b) README 第 53 行写「不能显式传 `undefined`」，与 `U15`「`undefined` 字段按省略」及 A18 的同名断言相反——实测 `{account,symbol,page:undefined}` 被**接纳**，键与不传 `page` 相同（`{"account":"a","symbol":"b"}`）；消费基线（TS 4.9 固定装置）也没开 `exactOptionalPropertyTypes`，故它不是编译期规则。**落地**：`U15` 的值域段补「**声明形式**：这条值域在声明点就由 `defineRefresh` 的类型约束先报错；字段嵌套时请用 `type` 别名或内联对象字面量——具名 `interface` 没有隐式索引签名，会被同一条约束打红（TS2344），这是类型写法的限制」；README 那条改为「显式传 `undefined` 会被按省略处理（与不传是同一个身份），推荐直接省略」并补上同一条嵌套写法说明。补上之后 ADR-52 那句「文档写明」才成立，故不改写 ADR-52（历史记录保留），由本条登记这次订正。**③ 未落地（等裁决）**：A02 的必须结果只覆盖 `U02` 的参数半边——「两个不同 Source ＋ 同一个参数键」全仓零用例（只有 3 个用例出现 ≥2 个 `defineRefresh`，都不是这个用途），临时探针实测行为**正确**（两实例、两次取数、各拿自己 Source 的副本）；A09 的必须结果写了「槽位空出后按 FIFO 继续」，用例只覆盖同一次 flush（入队序＝创建序），跨 flush 的临时探针实测启动序 `[1,3,2]`（创建最晚的 B 先入队故先启动，行为**正确**），若把队列换成注册表派生序会变成 `[1,3,1]`。按 §4.1「不得为通过测试而删除或放宽条目」，这两条应补证据而非改条目字面。第三条证据缺口由本轮**新增的文档句子**产生：`U15` 现在写明「嵌套用具名 `interface` 会被打红（TS2344）」，而 `tests/types.ts` 只钉了 `Date`／`Map` 两条反向探针，没有这一条——约束一旦被换成能接纳具名 `interface` 的写法（例如递归映射，实测会连 `Map` 一起放过），这句话会悄悄过期而门禁全绿。**④ 附带的自我订正**：此前把「`T` 无值域约束」归因给类型擦除（次生理由）——需求层理由是 `U11` 只承诺「`null` 有效、`undefined` 不是」，没有任何一条要求对 `T` 做值域检查（业务合法性归调用方）；并撤回「`DESIGN.md` §4.3 与 ADR-52 轻微打架」的定性（上下文明确是结果侧）。**范围（明说）**：本轮审计只覆盖「类型/结构与需求对齐」一条轴，调度与竞态、生命周期、真 Chrome 场景、其余用例的充分性与性能未重审，因此不能概括为「本库没有问题」。**度量**：源码与 `dist/` 未改（`src/` 905 行／89 分支／maxCCN 11 不变，`dist/index.js` 仍 10552 字节）；改的是包内 README，故按 ADR-49 纪律重打包，记录见 §5 G04。 | 本次落地；`统一刷新管理.md` §3.5（`U15`）／§5（G04）、`README.md`、`ADR.md`；ADR-24／ADR-49／ADR-50／ADR-52 |
| ADR-55 | 「代码已经复杂到看不懂、注释在解释而不是写作用、太多 `if` 防范这那」的本轮修法（确认人 2026-09-17：「先做1吧」——在上一轮给出的三个选项里选 ①＝只做丙（注释分流）＋丁（读代码导读），**不碰逻辑**） | **痛点成立，但「`if` 可以删」被证否；本轮只做文档与非行为面的注释整理**。**① 先把感觉变成数（HEAD `1d979d6`）**：`src/core.ts` 584 行里 50 个 `if`，其中 **20 个防的只是三个不变量**——10 处「已销毁或不在册」、6 处「任务是否仍是当前任务」、4 处「交付／通知点复核归属」；注释 158 行（占该文件 43%），其中 11 行在引 ADR；`flush` 用 28 行做四件事、`ccn` 11；71 个函数里 `ccn ≥ 6` 的 5 个全在 `core.ts`。**② 诊断（与 ADR-41 同源，粒度更细）**：难读不是 `if` 多，而是**这些 `if` 没有名字**——`this.disposed \|\| !this.handles.has(handle)` 被逐字拼了 6 遍、`resource.task !== task` 拼了 6 遍，「在册」与「仍是当前任务」这两个概念在代码里根本不存在，每遇一次都要现场重建；注释又把「做什么」埋在「为什么」后面，读者拿不到「这个函数干什么」。**③ 但「`if` 是冗余、可以删」这一步不成立**（用一个可自查的例子证否）：`activate` 先查一次在册，再写 `handle.active = true`，最后调 `coordinate`——删掉第一次查询，已释放的句柄就会被写上「已激活」；`coordinate` 里那道**看起来一样**的查询防的是另一次重入。两处各自防的东西不同，这是承重设计，不是重复代码（这次复核写进 `DESIGN.md` §9.4）。**④ 落地两件（都不碰行为）**：(a) `src/` 五个文件的注释按「**第一句＝做什么，理由下沉到 DESIGN／ADR**」重排——注释行 289 → **232**（−57，全部减在注释上），代码行 532、空行与函数数一行未动，`ADR` 引用 26 → 16；(b) `DESIGN.md` 新增 §9「读这个库的顺序」（三条进入路线／读前先记住的四个词／每个符号一句话作用／「遇到 `if` 时它在防什么」的三组守卫对照表），文首的顺序建议与 §3.3 原先那句「`src/core.ts` 的接口注释是权威」一并改为指向 §9 与 §3.3 本表（**原话自相矛盾**：同一行自己就带着完整所有权表，却把权威判给代码注释）。**⑤ 零行为变化的证据（两条互相独立）**：按 `check-docs.mjs` 的 `stripComments` 口径逐文件比对，五个源文件的**非注释行逐行相同**（5／31／44／365／87 行）；`dist/index.js` **10577 字节、SHA-256 `f1710b19e206cc25…` 与 ADR-54 逐字节相同**，函数数 71、结构分支 89、`maxCCN` 11 全部不变。**⑥ 本轮发现（已记录，未改）**：三个公开函数之一的 `useRefresh` **此前零 JSDoc**（已补）；`createRefreshManager` 上压着**两条 JSDoc**，前一条（「注册可见性监听与卸载释放」）永远读不到（已合并）；门禁第 15 组的观测面**过宽**——`RegExp`／`ArrayBuffer`／`NaN` 是 JS 内置名而不是本仓库的代码符号，它们此前能过闸只因为在 `source.ts` 的注释里恰好出现过，注释分流后当场报红三条，本轮**不改门禁**，改为把这三个名字放回 `assertJsonValue` / `parameterKey` 的一句话作用里（那本来就是它们的契约：拒绝这些容器、这些标量的键语义）。**⑦ 遗留（等裁决，均不在本轮范围）**：(a) 选项 ② 的两项——`src/core.ts` 的 `report` 仍带**零消费者**的 `export`（唯一调用点在同文件 `Resource.fail` 里，`dist/index.js` 里 0 次出现），`DESIGN.md` §3.3 的 Resource 行「核心只在跨实例边界读写 `task` / `entry`」与代码不符（核心还读写 `subscribers`／`waiters`／`parameters`／`source`）；(b) 本轮新发现的文档漂移——`DESIGN.md` §6.1 两处写「三项都会读完／三项各自完成依赖收集」，而 `readConfig` 只读 `enabled` 与 `every` 两项（`options.visible` 已由 ADR-37 删除），`readConfig` 自己的旧注释也写着「读三项配置」（本次改写去掉了计数以免与 DESIGN 那句互相矛盾）；(c) 甲（把三组守卫收成具名判定，净减阅读面而不减 `if` 条数）与乙（`flush` 类内拆四步，`maxCCN` 11 → ≤4）**未做**。 | 本次落地；`src/core.ts`、`src/vue.ts`、`src/source.ts`、`src/public-types.ts`、`src/index.ts`、`DESIGN.md` §9／§3.3、`README.md`（度量行 913 → 856，由 `pnpm complexity --write` 回写）、`统一刷新管理.md` §5（G04）、ADR-41（同一个「读不懂」议题的上一次修法）、ADR-54、`scripts/check-docs.mjs` 第 15 组 |
| ADR-54 | 架构评审「`Handle` 是一个混了三种角色的状态袋，该瘦身到只留端口」是否成立（确认人 2026-09-17：「1 2」——先裁「深入系统架构考虑了 真真不是个多余的数据结构吗」，再裁「注释与 DESIGN 按三组角色重写 ＋ 连带删 `Handle.disposed`」） | **不拆接口，收下一条真冗余，并把所有权那句话改成真的**。**① 评审描述经复核基本准确**（逐格对 `core.ts` / `vue.ts`，行号全对）：组 A 四个端口（`source` / `config` / `publish` / `onError`）是类型擦除边界，`Resource` 侧只用这一组；组 B（`parameters` / `subscription` / `active` / `disposed`）适配层零读取（全仓检索：`vue.ts` 只写一次 `cleanup`，不读任何核心写的字段）；`cleanup` 是唯一双向字段（`vue.ts:78` 写、`core.ts:260` 读/清/调）。**② 但「整包多余」不成立，拆接口不是减法**：组 B 搬进核心私有表只是把 4 个字段换成 4 张 `Map<Handle, …>`，结构数 1 → 5，还多出一条「名册与各表增删同步」的新不变量——正是 ADR-27 禁止的「一个概念分成多个数据结构解释」；ADR-19 早已用同一理由否掉同类提案（S-1「`Handle` 收进 `Manager` 作私有 class」，结论「私有化只降低误写概率」），ADR-25 又整体否掉含「删 `cleanup` 合并」的方案乙。**③ 但它问对了粒度更小的一件事**：`Handle` 里确有两处「同一事实的第二份存放」，且不对称——`subscription` 与 `Resource.subscribers` 的成员资格同源，是**付过钱的反向索引**（删了 `unsubscribe` / `resourceOf` 要扫全部桶，ADR-25 已把同型反查实测为伪减法），并且 §3.5 第 1 条不变量认领了它；`disposed` 与 `RefreshCore.handles` 的成员资格同源，却是**纯缓存、零算法收益**（7 个读取点全在 `RefreshCore` 内，`this.handles.has(handle)` 同样 O(1)；写入点 `handle.disposed = true` 的正下方就是 `this.handles.delete(handle)`；全仓除 `core.ts` 零读取），而 §3.7 写着「派生值不重复保存」、§3.8 却把它列成存储事实。**④ 落地两件**：(a) `Handle` 的接口注释与 `DESIGN.md` §3.3 按组 A／B／C 重写——原文「字段都由本文件写，适配层只读」是**假的**（`cleanup` 由适配层写、状态字段的初值也由适配层的对象字面量给出）；(b) 删掉 `Handle.disposed`，五个入口守卫（`removeHandle` / `activate` / `deactivate` / `submit` / `refresh` / `coordinate`）改用 `!this.handles.has(handle)`，`removeHandle` 的「先失效再清理」由「先出名册再清理」承担，重入安全不变。**唯一语义收紧（明说）**：未登记的句柄从「入口照常工作」变成「按已释放处理」——这正是「已释放 ⟺ 不在名册」的必然结果；无任何现存调用点依赖旧行为（`useRefresh` 一律先 `addHandle`；KeepAlive 的 `deactivate` 晚于 `removeHandle` 那条路径两边都早返）。**被否的备选**：① 完整拆分（组 A 接口 ＋ 组 B 私有表 ＋ `cleanup` 并入 `addHandle`）——字段只是搬家，另需新增一类观测出口才能让 18 处测试断言重写，换来的是「注释变成类型」；② 只做组 C——`cleanup` 离了字段就得进一张新的 `Map<Handle, () => void>`，单做是净增结构。**度量**：`src/core.ts` 575 → 584（−2 行代码 ＋ 11 行所有权注释）、`src/` 905 → **913** 行、结构分支 89 → **89** 不变、`maxCCN` 11 不变；`dist/index.js` 10552 → **10577 字节**；包 34066 → **34868 字节**。 | 本次落地；`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`DESIGN.md` §3.3／§3.7／§3.8、`README.md`（度量行与产物尺寸行）、`统一刷新管理.md` §5（G04）、ADR-19／ADR-25／ADR-44／ADR-47／ADR-52。**副本实测（裁决前主仓未被写入）**：`.tools/probe-arch/probe.mjs`（三条断言探针：先 `reconcile` 后 `addHandle` 的等价性、销毁后 `addHandle` 仍入册、`Resource` 侧只用组 A）、`.tools/probe-arch/disposed.patch`（删 `disposed` 的完整 diff）、`.tools/probe-arch/disposed.result.txt`（`pnpm test` 40/40 ＋ `tsc` 0 错误） |
| ADR-56 | 「函数设计与数据结构设计随心所欲、到处提取函数、到处函数调用」是否成立（确认人要求以 Linus 标准评判；裁决「② → ③ 分两次走」） | **两条函数层面的指控实测不成立；「数据结构」的指控成立，但问题不在容器而在归属。本轮只做 ②——把任务位置的三处状态收成一个写入点。**（1）度量（用仓库自己的 `@babel/parser` 解析 5 个源文件）：71 个函数 = 47 具名 ＋ 24 个回调；函数体合计 528 行、平均 7.4 行；函数出度中位数 2，出度 ≥5 的 14 个全是编排型（`useRefresh` 18、`runTask` 12、`flush` 11、`coordinate` 7）；`core.ts` 最深调用链 5 跳（`removeHandle` → `clearRefreshes` → `resourceOf` → `Resource.clearRequest` → `releaseIfUnused`），其中 3 跳各有 ≥3 个调用者。47 个具名函数里 28 个在 `src/` 内 ≤1 个调用点，但**调用点为 0 的恰好是公开入口**（`useRefresh`／`createRefreshManager`／`defineRefresh`／`currentCore`／`snapshot`／两个构造函数），它们的调用者在 `examples/` 与 `tests/`；真正空转的只有两处：`resourceOf`（唯一调用者 `clearRefreshes`）与 `report` 的零消费者 `export`。（2）容器选择保守且正确（Source → 参数键的两层 `Map`、`Set` 当 FIFO、`Set` 当标志、全库唯一唤醒 Timer），问题在归属：`resource.task` 的字段声明在 `Resource`，4 个写入者却全在 `RefreshCore`（`enqueue`／`runTask` 的 `finally`／`expire`／`releaseIfUnused`），同一件事实另由 `queue`／`running` 两处编码。（3）**本轮做法**：新增私有 `placeTask(task, position)`，`position ∈ queued／running／detached／settled`，成为 `Resource.task`、`queue`、`running` 三处的唯一迁移点；`detached` 专为 `releaseIfUnused` 而设——实例已回收、当前执行已撤销，但在跑的那次仍必须占着并发槽位（当场交还就会让在途 `load` 与后来者并发），这也是三处不能直接并成一个指针的原因。（4）**零行为变化的两组证据**：差分轨迹 `.tools/trace-core.mjs` 用同一套脚本化场景分别驱动 HEAD 与新 `core.ts`，43 行可观测轨迹（load／abort／`snapshot()` 投影／交付／错误）**逐字节相同**，覆盖 `placeTask` 的全部 5 个调用点与 `detached`／`settled` 两条丢弃分支（S3「卸载在跑的页」实测实例 0 而在跑 1；S6 覆盖回收排队任务）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 40/40、真 Chrome 12/12、干净消费方 TS 4.9.5 零错误 ＋ 三条运行期场景通过。（5）**度量**：`src/` 856 → **869** 行、函数 71 → 72、结构分支 89 → **93**（新增的 4 个都是 `placeTask` 自己的判定）、`maxCCN` **11 不变**；`dist/index.js` 10577 → **10832 字节**（＋255，代价明记）；包 32234 → **33094 字节**（两次 pack 逐字节相同）。（6）**顺手订正**：DESIGN §3.3 的 Resource 行原写「核心只在跨实例边界读写 `task` / `entry`」，与代码不符（核心还增删 `subscribers`／`waiters`），已改为核心实际越界的四个字段。（7）**自纠**：给确认人的评估里曾把 `resource.task !== null ⟺ task ∈ queue ∪ running` 说成严格 1:1——反方向不成立：`releaseIfUnused` 对在跑的任务会清 `resource.task` 而任务仍留在 `running`（即新的 `detached`）。（8）**未做**：`flush` 丢弃过期项的分支在 `placeTask` 之后已不可达（静态论证：`queue` 的写入者只剩 `placeTask`），但本轮保留未删；`report` 的零消费者 `export`、DESIGN §6.1 两处与代码不符的「三项」、甲（三组守卫改具名判定）与乙（`flush` 拆四步）均未动，等确认人裁决。 | 本次落地；`src/core.ts`、`DESIGN.md` §3.3／§3.5 第 4 条／§3.7／§3.8／§3.9 第二·三条／§9.2／§9.3／§9.4、`README.md`（度量行与产物尺寸行）、`统一刷新管理.md` §5（G04）、ADR-42／ADR-44／ADR-55 |
| ADR-57 | ③「删掉 `Handle.subscription`、把 `handle.config` 从闭包改成普通字段」是否做（承接 ADR-56 的裁决「② → ③ 分两次走」） | **做，两项都做。**（1）**先纠正我自己上一轮的一句错话**：评估里说「没有任何读者需要这个冗余」——实测有 12 处读 `handle.subscription`，其中 8 处是 `tests/core.test.ts` 的断言。它们读的都是**副本而不是事实**；删掉字段后这些断言改读实例侧 `subscribers`（新增 4 行探针 `subscribed(core, view)`），探针因此变强而不是变弱：`assert.equal(subscribed(core, view), undefined, '隐藏即退订')` 直接证「集合里没有它」，而旧断言只证「句柄上的副本是 null」。（2）**删掉字段同时消掉一处文档自相矛盾**：DESIGN §3.1 写着「`subscription` 只出现在句柄上……没有第二个对象描述同一条关系，因此不存在「两侧一致」这类需要维护的不变量」，而 §3.5 第 1 条列的恰恰就是一条「两侧一致」的不变量（`subscription` 非空 ⟹ `subscribers` 含该句柄，退订先清字段再删集合成员）。原句把「有一处冗余」说成了「没有冗余」，两句并存时读者只能二选一信。（3）**删反向索引暴露出一个隐藏的顺序依赖**：`removeHandle` 原先先执行 `handle.parameters = null` 再 `unsubscribe`，旧的反向索引让退订**不依赖身份**，所以这条顺序一直是安全的；字段一删，`unsubscribe` 必须靠身份查桶，就会查不到实例——句柄留在 `subscribers` 里，实例永不回收、`abort` 不发生、**已卸载的页仍会收到迟到的结果**。修法是把 `unsubscribe` 提到清空身份之前（`clearRefreshes` 与它都读身份，两者都必须在清空之前）。这个缺陷由两条独立探测器同时抓到：差分轨迹的「旧顺序」变体与正确版有 4 处差异（无 abort／实例 1 vs 0／已释放的页 `published=[[7]]`／销毁后仍交付），且既有用例 3 条转红（A06、A06/A11、A17）。（4）**字段形态统一**：`config` 从闭包 `() => snapshot` 改成普通字段，与它的四个同级字段（`parameters` / `active` / `cleanup`）同形，`vue.ts` 少一个 `let snapshot` 变量；`Handle` 因此成为纯数据记录，8 个字段里没有一个是另一份事实的副本。（5）**门禁未动**：`scripts/check-docs.mjs` 手抄的 `INTERNAL_FIELDS` 里 `subscription` 保留——那张表只用于禁止规范 §0 命名内部字段，删条目只会削弱它，不会修好任何东西。**零行为变化的证据**：差分轨迹 `.tools/trace-core.mjs`（旧 43 行 ＋ 新增 S7 配置变化、S8 卸载后迟到结果，共 **55 行**）在新旧 `core.ts` 下**逐字节相同**；`pnpm typecheck` 0 错误、`pnpm test` **40/40**、`pnpm check:docs` 全项通过、真 Chrome **12/12**、`build` 与 `build:demo` 通过；干净消费方 TS 4.9.5 零错误 ＋ 三条运行期场景通过。**度量**：`src/` 869 → **863** 行、函数 72 → 71、结构分支 93 → **92**、`maxCCN` **11 不变**、`tests/core.test.ts` 817 → 825 行；`dist/index.js` 10832 → **10752 字节**（−80；②＋③ 合计 10577 → 10752，＋175）、`dist/core.d.ts` 11629 → 11623；包 33094 → **33080 字节**（两次 pack 逐字节相同，SHA-256 `23808ce6…`）。**未做**：`report` 的零消费者 `export`（core.ts:207 一带）、DESIGN §6.1 两处与代码不符的「三项」、甲（三组守卫改具名判定）、乙（`flush` 拆四步）、`flush` 里已不可达的过期项丢弃分支——都仍等确认人裁决。 | 本次落地；`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`DESIGN.md` §3.1／§3.3／§3.5 第 1 条／§3.7／§3.8、`README.md`（度量行与产物尺寸行）、`统一刷新管理.md` §5（G04）、ADR-24／ADR-44／ADR-54／ADR-56 |
| ADR-58 | 取数的身份与发起方式：`defineRefresh` 收 URL ＋ 可选 `validate`，框架用注入的 axios 实例 POST 参数值 | **采纳并已落地（阶段一：身份与契约）**。确认人给出现实形态与目标：「各个页面都定义了自己的 load 函数，但实际上访问的是同一个 REST API」、「想实现的就是固定 url 相同参数直接合并」、接口「都是 POST body，同参数就是 body 的稳定排序」，并确认没有非 HTTP 来源。**裁决**：① 身份从「Source 对象 ＋ `Parameters.key`」改成「**URL（`RefreshSource.name`）＋ `Parameters.key`**」——同一个 URL 在几处各写一份定义也照样合并，原来那种「同一个接口写两遍就静默不合并、且没有任何检出」的缺口随之消失；URL 必须非空（空 URL 会把所有资源并成一条），在 `defineRefresh` 定义点直接拒绝。② 取数不再由定义提供函数：`RefreshSource` 只留 `name` 与可选 `validate`，框架对 URL 发 `post(url, 参数值的框架私有克隆, { signal })`，传输由 `createRefreshManager({ maxConcurrent, axios })` 注入——**库里不 import axios**（`RefreshHttp` 只要求一个 `post`，真实 axios 实例结构上满足），因此 `core.ts` 保持零运行时依赖，接入方配好的 baseURL／拦截器／鉴权头照旧生效。③ `validate` 留在定义里且**跟参数走**：每次 `submit` 由提交方自己那份定义执行，因此不参与身份、也不存在「谁先接入谁生效」。④ `T` 从「`load` 的返回类型（证明）」变成「定义点的声明」——框架不做响应转换，响应结构校验归传输侧（示例是 `demoHttp`，接入方通常是 axios 响应拦截器）。**明说的行为变化**：(a) 同名重复定义由「静默分裂成两个实例」变成「合并成一个实例」；(b) 2xx 但形状不对的响应在框架看来是一次**成功**，会覆盖旧结果（原来 `load` 里的校验把它转成取数失败）——要不要挡住由传输侧决定；(c) `T` 不再是编译期证明，只是声明。**不采纳的相邻提案**：不加 `admit()` 式的同名冲突检出（`load`／`transform` 都不在定义里，已无「谁先接入谁生效」的东西可比）；不为「未订阅也能读」新开公开读入口（保持三个函数 ＋ 6 个类型的导出面）。**被本条触动的历史裁决**：ADR-24 的「方法双变」论据此后只由 `validate` 承担；ADR-52 的参数复制纪律不变（请求体仍是每轮一份框架私有克隆）。 | 本次落地；`src/public-types.ts`、`src/source.ts`、`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tests/types.ts`、`tests/pages.spec.ts`、`tests/scenarios.ts`、`examples/sources.ts`、`examples/main.ts`、`examples/http-fixture.ts`、`scripts/benchmark.mjs`、`DESIGN.md` §1／§2.1／§3.2／§3.3／§3.5／§4.1／§4.3／§5.2／§7／§9.3、`README.md`、`统一刷新管理.md` §0.2／§2.1／§2.2／§3.1／§4.2（A02 改写、新增 A20）／§5（G04）、ADR-24／ADR-52 |
| ADR-59 | 结果表切到 Pinia：结果唯一真值、按 URL → 参数键分组、随实例释放即删，删除整条推送路径 | **采纳并已落地（阶段二：结果表）**。确认人裁决：「结果随实例清」「出口使用 `shallowRef`；如果调用方要改，让用户自己复制——也就是说同一对象」「axios 返回的结果直接写入 pinia，按照 url 分组，相同 url 按照稳定后的 key 分组，pinia 提供数据出口」。**落地**：① 新增 `src/store.ts`——模块级 `defineStore('vue-refresh')`，内部一个 `shallowRef<Record<URL, Record<参数键, Entry>>>`，**整条替换**（内层永远是普通对象，不会被 Vue 深代理，`structuredClone` 照旧可用），写入端只有内核（`write` / `remove` / `list`），读出口是 `read`；`pinia` 因此从无用的 devDependency 升为 **peer 依赖**。② `Resource.entry` 删除：结果只住结果表，`Resource.publish` → `Resource.settle`（记结算时刻 → **先取这一批要求快照** → 写结果表 → 结算要求），`deliverTo` / `deliverLatest` / `Handle.publish` 一并删除——**框架不再调用任何页面交付回调**；内核新开第三个实例入口 `writeResult`，`releaseIfUnused` 改为删结果表条目（A06「随实例清」在存储层一字不改）。③ `display` 不再是「推过来的一份副本」，而是适配层按**已声明身份**从结果表读出来的一个 `shallowRef` 视图（`watchEffect`，`flush: 'sync'`）：`args` 每次复制、`data` 就是表里那**同一个对象**；`RefreshHandle.display` 的类型随之从 `Readonly<ShallowRef<…>>` 放宽为 `Readonly<Ref<…>>`。④ `snapshot()` 多一项 `results`（`url` / `key` / `entry` 扁平行），两个 URL 同参数键不再互相覆盖；`createRefreshManager({ maxConcurrent, axios, pinia })`。**明说的行为变化**：(a) **暂停／失活只停「由这一页驱动取数」，不再冻结画面**——它读的是共享结果表，因此别人刷新的结果它也会显示；(b) **画面保留（keep-last）**：条目随实例释放即删，但已挂载页面保留最后一次读到的画面（与旧推送模型下「页面自己那份副本」表现一致），新加入的页面读回空并重新取数；(c) **数据是共享对象**：就地改它对同一身份的所有读者可见，而且**不会触发响应式更新**——要改自己复制；(d) A12 的「接收者」概念消失，改成「属于该身份的任何页面都读到它」；A11/A16 的口径同步改写（§4.1 允许「行为随契约移除而消失时同步改写并记录理由」，本条即该理由）。**过程中抓到并修掉的三个真缺陷**（都由用例/浏览器场景抓到，不是事后推理）：① `settle` 初版把 `waiters` 快照放在写结果**之后**，写结果时页面的同步重入（`flush: 'sync'` watcher）登记的要求被同一轮清掉，`refill` 不触发，A12/A14 用例 `1 !== 2`；② `display` 初版用 `computed`，首次求值时还没有身份就 `return null`，`store.read` 从未执行 → 这个计算属性**没有任何依赖、永久缓存 `null`**（结果表里已有条目而页面一直显示「等待首查」，浏览器 12 条场景里 10 条转红），改成「无条件先读结果表」＋把身份做成 `Ref`；③ 「释放即删条目 ＋ 页面没有副本」会让暂停页在结算那一刻**画面变空**，与 A05／G6 冲突，补丁是适配层 keep-last（存储层仍旧随实例清）。**未采纳的相邻提案**：不新增公开读入口（导出面仍是 3 函数 ＋ 6 类型；`useRefreshStore` 与 `snapshot()` 留在库内／演示／测试）；不给暂停页做「冻结」（那要把订阅状态再喂给视图层，等于把推送模型的需求搬回来）。**被本条触动的历史裁决**：ADR-27（删 Pinia 分区）与 ADR-46（删读取出口）的结果存储结论被取代，但 ADR-46 的**理由**仍成立——本条同样不给「按参数键拍平的字典」，分组键是「URL ＋ 参数键」；ADR-52 的参数复制纪律不变、结果那一半改为共享同一对象。 | 本次落地；`src/store.ts`（新增）、`src/core.ts`、`src/vue.ts`、`src/public-types.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tests/pages.spec.ts`、`tests/scenarios.ts`、`examples/main.ts`、`examples/pages/shared-pair.ts`、`vite.lib.config.ts`、`package.json`（peer 加 `pinia`）、`scripts/check-docs.mjs`（分层表加 `store.ts`）、`DESIGN.md` §1／§2／§2.1／§3.1–§3.9／§4.2–§4.4／§5.2／§5.3／§7／§9.1–§9.4、`README.md`、`统一刷新管理.md` §0.2／§1（G8）／§2.1／§2.2／§2.5／§3.3／§4.2／§4.3／§4.4／§4.6／§6.4（G04）、ADR-27／ADR-46／ADR-52 |
| ADR-60 | 暂停／失活后画面是否跟随共享结果表 | **改为冻结（修正 ADR-59 的行为项 (a)）**。确认人指出：「暂停、失活了为啥还在刷新，画面应该冻结了」。ADR-59 落地时 `display` 是无条件按身份读结果表，于是暂停页在**别人**取回新结果时也跟着变——那一条我在交付记录里列为行为变化，现在按确认人裁决改掉。**裁决**：视图不是无条件读，而是**读者才跟随**——「读者」＝本页此刻订阅着该身份，**或**它上面有未撤销的刷新要求（`RefreshCore.isReader`，就是 ADR-46 之前 A12 那个「接收者」集合）。读者成立时整条替换画面，不成立时**冻结在最后一帧**（也不清空）。为什么不干脆按「订阅」一个条件：A05／G6 要求**暂停页仍可刷新一次**，那一次它只有刷新要求、没有订阅；若按订阅判定，暂停页自己刷出来的结果会被自己的闸门挡住。**落地**：`src/core.ts` 新增只读判定 `RefreshCore.isReader`；`src/vue.ts` 的 `display` 副作用在写入前加这道闸门，并在「适配层自己发起的资格边沿」（`activate` 与配置 watcher）上 +1 个计数器让它重新判定。**边界（明说）**：浏览器可见性是全局的，隐藏会让所有页面同时失去订阅，实例通常当场释放、条目删除 → 由「没有条目就保留上一帧」兜住；若隐藏期间仍有未撤销的刷新要求（实例仍活着），回到可见后画面在下一次结果到达时更新，最多滞后一个周期。**与 ADR-59 的关系**：结果表仍是唯一真值、仍随实例释放即删、数据仍是共享同一个对象（要改自己复制）——本条只改「视图要不要跟」。 | 本次落地；`src/core.ts`、`src/vue.ts`、`tests/pages.spec.ts`、`tests/scenarios.ts`、`DESIGN.md` §3.3／§3.7／§9.2／§9.3、`README.md`、`统一刷新管理.md` §2.5／§3.3（U12、A12）／§5（G04）、ADR-59 |
| ADR-61 | 声明与资格两分：`active` 并进配置快照；暂停／失活／隐藏不再撤销声明（P1＋P2） | **采纳并已落地**。确认人裁决：「同一个身份肯定按照最小的间隔」（甲，保留共享最小间隔）；「已发出的请求永远不取消，但是 `setTimeout` 定时了 100 秒才发的，肯定要被短间隔取消掉」；「最后需求退出那肯定要 abort」；「页面失活恢复不就是 keepalive 吗」；「config 里加个 visible，取最小 every 的时候过滤 enabled && visible」；「先一起做吧」。**P1（`active` 并进配置快照）**：`Config` 变成 `{ enabled, every, active }`，`Handle.active` 与 `RefreshCore.activate` / `deactivate` 删除；适配层把两个 `Ref` 与这一页的激活状态合成一份快照写进 `Handle.config`（唯一写入口是一个 `flush: 'sync'` 的 watcher），于是**资格只有一处来源**：`config.enabled && config.active && RefreshCore.visible`。**P2（声明与资格两分）**：`Resource.subscribers` → **`declarers`**，成员资格＝**声明了该身份**（页面挂载期间一直算；暂停、失活、隐藏都不撤销），登记在 `coordinate`、撤销只发生在卸载／换身份／销毁（`dropDeclaration`）；**资格不再维护第二份集合**，由 `Resource.isEligible` 现算，`eligibleEvery(visible)` / `dueAt(now, visible)` 据此取最小间隔，没有有资格的声明者就直接不取数、不安排唤醒；`releaseIfUnused` 的判据换成「`declarers` 与 `waiters` 都空」，因此**暂停／失活／隐藏不再释放实例、不再删结果表条目、不再 abort 在途**；`isReader` = 声明着它 且（有资格 或 有未撤销要求），画面跟随／冻结（ADR-60）判据不变；`fail` 只通知此刻的读者（不再通知暂停页）。**行为变化（明说）**：① 失活／暂停／隐藏后恢复**直接读回已有结果、不重复取数**（不需要 TTL／宽限期：「保留期」就是页面的挂载期）；② 需求退出后的 abort 只发生在**卸载、换身份、销毁**与框架上限到期，不再发生在「暂停／失活／隐藏」；③ 代价：暂停或隐藏的页面会钉住一个实例与一条结果（上限＝挂载的页面数），今天隐藏会把它们全放掉；④ 失败通知面收窄到读者（暂停页不会收到它没要求过的失败）。**与本文件其它条的关系**：ADR-57 的「需求关系只有一处事实」继续成立，只是那份事实从「有资格的订阅集合」变成「声明集合」；ADR-59／ADR-60 的结果表与画面冻结不变。 | 本次落地；`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tests/scenarios.ts`、`DESIGN.md` §2／§2.1／§3.1／§3.3／§3.4／§3.5 第 1／3／11／12 条／§3.7／§3.8／§3.9／§6.2／§7／§9.2／§9.3／§9.4、`README.md`、`统一刷新管理.md` §0.2／§1（G4）／§3.2（U04–U06）／§4.3（A04–A06）／§4.4（A12）／§5（G04）、ADR-57／ADR-59／ADR-60 |
| ADR-62 | 清理：拆 `flush` 四步、删掉队列里已不可达的归属复核、`report` 收成模块私有、DESIGN §6.1 的「三项」订正 | **采纳并已落地**（确认人「先清理干净」）。① **拆 `flush`**：原来一个 27 行、`maxCCN` 11（全库最高）的函数串了四件事，现在拆成四个私有方法——`coordinateAll`（协调全部句柄）→ `enqueueDue`（到期入队并收齐最早到期时刻）→ `startQueued`（按 FIFO 占槽启动）→ `scheduleWakeup`（安排唯一唤醒 Timer），`flush` 只剩这四行调用；全库 `maxCCN` **11 → 6**。② **删掉 `startQueued` 里「队列里的任务不再是它实例的当前执行就丢弃」那个分支**：`queue` 的唯一写入者是 `placeTask`，而它只在「这个任务就是当前执行」时才把任务放进队列（§3.5 第 4 条），因此那个分支已不可达。删掉的前提用一条**不变量探针**钉住（`assertQueueConsistent`：队列里的任务必须是它实例的当前执行，`tests/core.test.ts`），一旦有人新增第二个入队路径就会红。③ **`report` 收成模块私有**：它唯一的调用点是 `Resource.fail`（同文件），不在包导出面里，`export` 是残留。④ **DESIGN §6.1 的「三项」订正为「两项」**：那段讲的是 `options` 的两个 `Ref` 在 getter 抛错时的依赖收集边界，而 ADR-61 之后快照里多出来的第三项 `active` 是适配层自己的 `shallowRef`（在 `readConfig` 之外读取），不参与那段边界——原文的「三项」是 ADR-37 删掉 `visible` 之前的残留。 **验证**：`pnpm typecheck` 0 错误；`pnpm test` **41/41**；`pnpm check:docs` 全项通过（6 模块、叶子 19、测试标题 53）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 pinia ＋ 真 axios ＋ 真实 HTTP）五条场景通过。**度量**：`src/` 1076 → **1094** 行（拆函数带来的签名与注释）、函数 79 → **84**、结构分支 94 → **93**、`maxCCN` **11 → 6**；`dist/index.js` 12151 → **12263 字节**（12.26 kB，拆函数使产物略增）；包 41192 → **41748 字节**（10 个条目，两次 pack 逐字节相同，SHA-256 `9070ea25c85cc6c06aa9fd6c8417effa46c45f43052a6136655609e6fd53fbf6`，10 个条目哈希与工作树逐个一致）。**没有做的两项（等你点名）**：`LOAD_TIMEOUT_MS` ＋ `expire`（框架上限）与 `detached` 任务位置——砍掉它们才会让调度只剩一个 `setTimeout`，但代价是挂死的请求永久占住一个并发槽（G3 的「应用不停摆」没了），这是产品取舍，ADR-20 的数值也是确认人给的，因此不擅自砍；**甲（三组守卫具名化）**也先不做：三组守卫是全库最承重的 `if`，要动它们得先按 ADR-61 之后的新契约更新差分轨迹工具（`.tools/trace-core.mjs` 还停在 `load` ＋ `publish` 的时代）再动。 | 本次落地；`src/core.ts`、`tests/core.test.ts`、`DESIGN.md` §2.1／§3.5 第 4 条／§5.1／§6.1／§9.3、`README.md`（度量与产物尺寸行）、`统一刷新管理.md` §5（G04）、ADR-56 |
| ADR-63 | 读取面按每页 `every` 采样；失败作为身份那一格的事实（删掉 `onError`） | **采纳并已落地**。确认人裁决：「有的页面不需要那么快，它需要依据页面的频率去读取已经存在 pinia 的数据，这只是读取，不破坏 resource 身份，也不破坏 resource 对应的最新数据，是为每个页面定时存一个副本」；「如果出错……失败是绑定在 resouce 上的……需要在 resource 上写个 error 字段，让组件读取的时候取处理也就是读取面……如果有新数据刷新正常了 那 error 就应该删掉……所以调度完全是围绕 resouce 的」；「适配层也不用知道调度，它就是读取 pinia 数据对吧」。**① 读取面按拍**：`display` 从「结果表一变就跟着变」变成**本页自己的副本**——适配层装一条采样脉搏（`setInterval`，周期＝本页 `every`），一拍最多发布一次；两拍之间结果表里的新版本不改变画面（比较整格引用即知变没变，不引版本号）。**三处不等拍**：身份落定、重新成为读者（配置／生命周期边沿）、显式 `refresh()`——于是首查、失活恢复、手刷都不会被自己的周期拖后。**取数仍按同一身份所有页面里最小的 `every`**：慢页面白嫖快页面，不额外发请求（ADR-61 语义不变）。**可见性不参与采样**：它只挡有网络代价的取数，挡不住也不该挡读内存——所以适配层连 `document` 都不碰，可见性监听继续由核心独占。**② 失败进结果表**：结果表每格从「一条 `Entry`」变成 `ResultCell`（`entry` 与 `failure` 各自可空）；失败**不动**最后一次成功的数据，只把 `failure = { cause, at }` 换上，成功一到就清回 `null`。`RefreshOptions.onError` 删除（连带 `Handle.onError` 与 `Resource.fail` 里的通知循环），`ResultSink` 加 `fail`、`RefreshCore` 加第四个实例入口 `writeFailure`，`Resource.fail` 与 `settle` 同序（先定下这批要求、再写表、再结算）。**首查就失败**时 `display` 不是 `null` 而是 `data: null` 加 `failure`——否则首查失败的页面永远读不到失败；**暂停页**显式刷新的那一次也读得到（它是要求里的读者）。**代价（明说）**：① 慢页面看到的数据可能滞后最多一个 `every`（这正是它要的）；② **两次采样之间「先失败、后成功」的那一段看不见**——按频率读取的固有代价，框架不引失败计数或时间戳（ADR-43／45／47 删过版本与代次计数）；③ 失败不再有推送通道，页面必须在 `display` 上处理它。**顺带**：`data` 与 `updatedAt` 变成可空，导出面从 3 函数加 6 类型变成 3 函数加 **7 类型**（新增 `RefreshFailure`）；`Handle` 只剩三组角色（端口与配置／状态／接驳）。 | 本次落地；`src/core.ts`、`src/store.ts`、`src/vue.ts`、`src/public-types.ts`、`src/index.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tests/types.ts`、`tests/scenarios.ts`、`tests/pages.spec.ts`、`examples/`、`scripts/benchmark.mjs`、`DESIGN.md` §2.1／§3.3／§3.4／§3.5 第 13 条／§3.6／§4.2／§4.3／§4.4／§6.1／§6.2／§6.4／§9.2／§9.3／§9.4、`README.md`、`统一刷新管理.md` §0.2／§1（G7、G11）／§2.1／§2.4／§2.5／§3.x／§4.x（A21）／§5（G04）／§6.4／§7、ADR-46／ADR-51／ADR-59／ADR-60／ADR-61 |
| ADR-64 | 核心不再认识页面：`Handle` 收成纯数据的 `Demand`、参数准备上移到适配层、删掉 `setCleanup` 与 `isolate` | **采纳并已落地**。确认人指出「根本问题核心不再认识页面好像还没做啊」——ADR-63 删掉 `onError` 之后，核心这边还剩三根线：句柄上的 `source`（定义对象，含 `validate`）、`cleanup`（适配层的释放回调）、以及 `submit` 收的那个 `prepare` 闭包（核心替调用方跑参数准备与校验）。**① `Handle` → `Demand`**：`{url, config, parameters}`，纯数据、零回调；核心不再 import `RefreshSource`（定义与校验完全留在适配层）。名册 `RefreshCore.handles` → `demands`，`addHandle`／`removeHandle` → `addDemand`／`removeDemand`，`snapshot().handles` → `snapshot().demands`。**② `submit(demand, parameters)`**：参数准备（复制、值域、身份键编码、`validate`）搬进适配层的提交边界，抛错就地变成 `rejected`（公开契约不变，取值域仍是 accepted／rejected／cancelled），核心没有 `try/catch`、也不产生 `rejected`。**③ `setCleanup` 删除**：可见性监听由适配层自己收尾（`install` 的 `app.onUnmount` 与返回的 `dispose()` 都摘监听）；模块私有的 `isolate()` 随之删除（它唯一的用途就是隔离页面回调的抛错）。**④ `Resource.source` → `Resource.url`**：一个字符串，不再是定义对象。**结果**：`core.ts` 拨出去的外部调用只剩两个**注入端口**——传输 `http.post` 与结果表 `sink.write／fail／remove／list`；**核心零回调**，它不执行任何调用方代码。**代价（明说）**：① `rejected` 现在由适配层构造（类型仍是包契约，语义不变）；② 拆卸责任从核心移回适配层（谁装监听谁摘、谁开脉搏谁停表；脉搏 tick 里加了一句「核心已销毁就自己停表」，兜住 HMR 那种「协调者先没、组件还在」的窗口）；③ `removeDemand`／`dispose` 不再能替适配层清理外部资源——这本就是适配层的事，过去是核心代劳的。**没做的（如果还要更彻底）**：把身份也从需求上拿掉、让实例只持有一组 `Config`（那样核心连 `parameters` 都不认识）。不做的理由：换身份的次序（先撤要求 → 再撤声明 → 再登记新身份 → 协调）是 ADR-56／ADR-57 抓过真缺陷的地方，把它拆到适配层、或让核心维护「`Config` → 身份」的反向索引，都会把刚钉住的顺序不变量重新变成调用方的责任；现在的 `Demand.parameters` 只是**核心自己写的那一半身份**（DESIGN §3.5 第 8 条），不是页面对象。 | 本次落地；`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`examples/main.ts`、`tests/pages.spec.ts`、`DESIGN.md` §1／§2.1／§3.1／§3.3／§3.4／§3.5／§3.6／§3.7／§6.3／§6.4／§9.2／§9.3／§9.4、`README.md`、`统一刷新管理.md` §3.6（U16）／§4.6（A16）／§6.4、ADR-57／ADR-59／ADR-63 |
| ADR-65 | 拆环、砍框架取数上限与 `Task`、结果表拍平（`Entry`／`ResultRow`／`RefreshFailure` 三个类型消失） | **采纳并已落地**。确认人对「数据结构太多、职责不对、互相引用」的整体分析给出裁决：「挂死请求可以给 axios 配置超时啊，或者 nginx 你说的永久不存在的吧，根据分析全部执行吧 概念合并，拆环 砍收 task」。**① 拆环**：`Resource` 不再持有 `RefreshCore`（构造只剩 `(url, parameters)`）。它的 `settle(at)`／`fail(at)` 只记结算时刻并把**这一批要求**交回核心；写表、回收、排队全部由核心做，`clearRequest`／`refill` 两个方法删除，核心新增私有 `settleRequest`。`releaseIfUnused`／`writeResult`／`writeFailure`／`enqueue`／`place`／`run` 一并收成私有——**核心公开面 14 → 10**（`isDisposed`／`setVisible`／`addDemand`／`removeDemand`／`reconcile`／`submit`／`refresh`／`snapshot`／`dispose`／`isReader`），**全库再无环**：实例不调核心、核心不调页面。**② 砍上限与 `Task`**：删除 `LOAD_TIMEOUT_MS` 与 `expire`，`runTask(task)` → `run(resource)`；`Task` 类型删除，`queue`／`running` 直接装 `Resource`；「这次执行」的身份与取消把手收到 `Resource.controller`（`null` ＝ 没有执行，或这次结束已不算数），位置迁移 `placeTask` 四态 → `place(resource, queued／running／abandoned／idle)`。依据（明说）：**请求必然终止由注入的传输负责**（axios 的 `timeout`、反向代理或宿主自己的截止），框架不再自带截止——代价是一个永不结束的请求会占着它的并发槽直到传输自己结束；ADR-20 的 `10000` 毫秒与 A10 叶子随之作废。**③ 概念合并**：`ResultCell` 从「`entry` ＋ `failure` 两层」拍平成四个平字段 `data`／`updatedAt`（`null` ⟺ 从未成功过）／`error`／`failedAt`（`null` ⟺ 自最后一次成功以来没失败过）；`Entry`、`ResultRow`（改为内联的行形状）与 `RefreshFailure`（内联进 `RefreshDisplay`）三个类型删除，**公共类型 7 → 6**；`ResultSink.write/fail` 改成平参数。`core.ts` 的导出类型从 10 个降到 7 个，读者要记的概念少了三个。**为什么不做更彻底的形态**（把身份也从需求上拿掉、让实例只持有一组 `Config`）：换身份的次序是 ADR-56／57 抓过真缺陷的地方，拆到适配层或让核心维护反向索引都会把顺序不变量重新变成调用方的责任——这一条在 ADR-64 里已经说明，本轮不变。 | 本次落地；`src/core.ts`、`src/store.ts`、`src/vue.ts`、`src/public-types.ts`、`src/index.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tests/types.ts`、`tests/scenarios.ts`、`examples/`、`scripts/benchmark.mjs`、`DESIGN.md` §2.1／§3.1／§3.3／§3.4／§3.5／§3.6／§3.7／§3.9／§5.1／§5.2／§5.3／§7／§9、`README.md`、`统一刷新管理.md` §0.2／§1（G3）／§2／§3.3／§4.3（删 A10 叶子）／§5（G04）／§6.4、ADR-20／ADR-42／ADR-44／ADR-56／ADR-57／ADR-63／ADR-64 |
| ADR-66 | 一个身份一本账：`Config` 就是页面的登记（删掉 `Demand` 与全局名册，读闸门搬适配层） | **采纳并已落地**。确认人裁决：「身份 resource 才是合并请求的标准，终止也是终止这个，排队也是派（排）对这个」，并且「需求不是读取直接读取 pinia 吗……组件读新鲜数据不就足够了吗」。**落地**：① `Config` 变成 `{ enabled, every: number \| null, active }`，**每页一个对象、适配层原地改写**，它同时就是这一页在核心里的登记（加入实例的 `declarers`）——`Demand` 类型、`RefreshCore.demands` 全局名册、`addDemand`／`removeDemand` 一并删除；`every === null` 表示这一拍配置非法，与「暂停」可区分（A04/A05）。② `RefreshCore` 公开面：`isDisposed`／`isVisible`（新的一比特出口）／`setVisible`／`reconcile()`（无参：重算到期与唤醒）／`submit(config, url, parameters)`／`refresh(config, url, key): boolean`（「这句命令收下了没有」，不是取数回执）／`isEligible(config, url, key)`／`undeclare(config)`／`snapshot()`／`dispose()`。③ **读闸门搬到适配层**：核心不再回答「谁该跟随结果表」（`isReader` 删除），适配层自己算 `core.isEligible(...) || (pending && core.isVisible())`——`pending` 是这一页「点过刷新、还没看到那一拍」的标志；「读者」因此不再是核心的概念，冻结语义不变，只是判定点搬家。④ `submit` 换身份改成扫描注册表找到旧位置（不存反向字段，ADR-57 的纪律不变）。⑤ `dispose` 先清每个实例的 `declarers` 与 `waiters` 再回收（只清声明不够：要求还在就回收不了，迟到的结果还会写进表——A17 钉住了）。**试过并否掉的更激进版本（记下来免得重提）**：把 `waiters` 也收成「身份级一个布尔（`needsNext`）」——「核心不需要知道是谁要的」在这一层不成立：写表会同步触发页面 watcher，一个布尔会在每次产出之后被重新置起，补发因此没有上界（`A12/A14 结果写入期间新登记的刷新要求由后继请求满足` 这条用例当场死循环），而且「另一个页面在别人等待期间点刷新」会被吞掉。`Set<Config>` 记的是「**哪个页面**已经有未满足的要求」，正是 §0.2「刷新要求……只结算一次」「重复刷新只留一份」的实现；它换来的是补发有上界、要求按页公平。 | 本次落地；`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tests/pages.spec.ts`、`examples/main.ts`、`scripts/benchmark.mjs`、`DESIGN.md` §1／§2／§2.1／§3.1／§3.3／§3.4／§3.5／§3.6／§3.7／§3.9／§4／§5／§6.3／§6.4／§7／§9、`README.md`、`统一刷新管理.md` §0.2／§1／§2／§3.x／§4.x／§6.4／§7、ADR-54／ADR-57／ADR-60／ADR-64／ADR-65 |
| ADR-67 | 读取面从采样脉搏改为写入驱动的 `updatedAt` 时间差节流（删掉每页常驻 `setInterval`） | **采纳并已落地**。确认人裁决：「读端记录上一次读取到时间不就行了，数据变化了触发 watch，watch 判断 config 到期没有，到期就复制一份」；细化判据为「计算数据获取时间＋every 延迟是否小于等于 updatedAt」，并确认「不存在时钟回拨」。**落地**：① 删除适配层的采样脉搏（`pulse`／`sampledPulse`／`restartPulse`／`stopPulse`／`pulseTimer`／`MAX_PULSE_DELAY`），读取面完全由写入事件驱动——`store.write`／`fail` 替换 cell 后，`flush: 'sync'` 的副作用同步醒来判定。② 判据锚定**数据的时间戳**而非 watch 触发时刻：`sampled.updatedAt + every <= cell.updatedAt` 才换画面，否则跳过；跳过后 `sampled` 仍指旧格，判据自持，无需记「上次跳过时刻」。任一份从未成功过（`updatedAt === null`）不受节流——失败与恢复的事实立刻可见；配置非法（`every === null`）不抄。③ 三个不等节流的边沿保留：身份落定、重新成为读者、显式 `refresh()`（`immediate`／`pending`／`eligible` 机制不变）。**语义变化（明说）**：更新时刻落在写入事件上，「读者在自己的拍上跟随」降级为「每 `every` 最多变一次＋变时取最新」；写端稀于本页 `every` 时**更及时**（写入即抄，原方案要等下一拍）；写端密且 `every` 非整数倍时实际更新周期被**量化到写入网格**（最坏 `every`＋写入间隔）——确认人接受。前提：运行环境不存在时钟回拨（确认人确认），判据不做回拨防护。取数按最小 `every`、失败写格、读者冻结、显式刷新不等节流等 ADR-63／ADR-61／ADR-60／ADR-66 语义均不变。**收益**：每页少一个常驻 `setInterval`，适配层净减约 20 行。 | 本次落地；`src/vue.ts`、`src/public-types.ts`、`src/store.ts`（注释）、`tests/vue.test.ts`、`tests/pages.spec.ts`（注释）、`examples/pages/shared-pair.ts`（注释）、`DESIGN.md` §2.1／§3.5／§6.4、`README.md`、`统一刷新管理.md` §2／§3.x／§4.x，关联 ADR-63 |
| ADR-68 | 身份注册表拍成一层：`buckets`（`Map<url, Map<key, Resource>>`）→ `identities`（`Map<身份键, Resource>`） | **采纳并已落地**。确认人看出「核心不需要多个字段持有 resource」：注册表真正多余的只是**第二层**。**落地**：`RefreshCore.buckets` → `identities: Map<string, Resource>`，键由新导出的 `identityOf(url, key)` 构造（`url\u0000key`，两个字符串里都不会出现 NUL）——**结果表用的就是同一个键**，所以 `src/store.ts` 里的 `keyOf` 删除、改为复用 `identityOf`（一处格式，不再有两处各拼一遍的重复）。收益：① 少一层嵌套；② `releaseIfUnused` 少一段「桶空了就删桶」的清理（现在只删一个键）；③ `all()` 就是 `[...identities.values()]`。三处查找（`resourceFor`／`refresh`／`isEligible`）仍是**一次命中**，读路径不变。**没有做的**：把注册表换成 `Set<Resource>`——功能上等价（`Resource` 自带身份），但三处查找会变成扫描，其中 `isEligible` 在**读路径**上（每个页面每次结果写入各一次）；`queue`／`running` 也不能并进去：它们回答的是「入队次序」与「占了几格」两件别的事（`Set` 只保留首次加入的顺序，FIFO 会错位；槽位要计数）。 | 本次落地；`src/core.ts`、`src/store.ts`、`DESIGN.md` §3.3／§3.4／§3.7／§9.4、`README.md`、ADR-66 |
| ADR-69 | 守卫具名化（甲）：给「身份不成立 / 这不是当前执行 / 还有没有人要」三类守卫起大白话名字，并补上「零行为变化」的取证工具 | **采纳并已落地**。确认人裁决：「需要守卫具象化，但是单词要易懂、不要专有名词」（§9.4 那张表里原本还写着「尚未裁决」）。**落地**：`Resource` 新增三个判定——`hasExecution()`（这个身份还有没有执行：在队或在跑）、`isCurrent(controller)`（这次执行还是不是当前执行，迟到的结果与迟到的异常都靠它判「还算不算数」）、`isWanted()`（还有声明者或未结算的要求吗，没有就该回收）；`RefreshCore` 新增 `find(url, key)`（按身份键找实例，找不到＝这个身份不成立），`refresh`／`isEligible` 两处裸查表改走它；`run` 里三处裸比较改走 `isCurrent`，`enqueueDue`／`refresh`／`refill` 改走 `hasExecution`，`releaseIfUnused` 改走 `isWanted`。名字都是常用词，没有 `stale`／`generation`／`guard` 这类行话。**取证工具（此前一直缺的前置）**：新增 `scripts/trace-core.mjs`——用 8 个脚本化场景驱动核心，打印**只看结构、不看时间戳**的可观测轨迹（资源数／声明者数／在队／在跑／唤醒 Timer／结果表格子状态／结果表写入次数）。改动前后各跑一遍 **21 行轨迹逐字节相同**（1889 字节），这是单测之外更强的「零行为变化」证据；工具本身也进了仓库，以后任何改名／提取都能用它对比。 | 本次落地；`src/core.ts`、`scripts/trace-core.mjs`（新增）、`DESIGN.md` §3.3／§9.4、`README.md`、ADR-62／ADR-66 |
| ADR-70 | 删掉 `waiters`：刷新是**给身份的命令**（队头插入 ＋ `produced`／`needsNext` 两个位），核心不再记「谁要的」 | **采纳并已落地**。确认人裁决：「这个 waiter 怎么还没干掉啊，refresh 手动不管是谁最后反应的都是 resource 需要重新刷新吧，那不是只需要给 core 里面 queue 头部插入进入一个 resource 就行了吗，resource 以及 core 不关心是谁要 refresh，调用方根据数据新鲜度 超过了就加载，哦对了 这里手动刷新可以直接把上次读取时间置 null 不就行了」。**① 核心侧**：`Resource.waiters: Set<Config>` 删除，换成两个位——`produced`（**这一轮的结果已经产出**：写表之前置起、一轮结束时清掉）与 `needsNext`（产出之后又有人点过刷新：本轮结束再排一次）。`refresh` 只剩三条分支：**没有执行 → 插到队头**；**有执行、结果还没产出 → 什么都不做**（A14：有在途就直接用它的结果，不追发第二次）；**有执行、结果已经产出 → 记一笔**（这一轮已经写完了新结果，它要的必然是下一轮）。`settle`／`fail` 的返回值删除（不再交出「这一批要求」），私有 `settleRequest` 删除，`undeclare`／`submit`／`dispose`／`releaseIfUnused` 里的要求清理全部删除，`isWanted()` 收成一句话「还有声明者吗」。**② 插队头怎么实现**：`queue` 是 `Set<Resource>`，只有「加入顺序」——新增私有 `enqueueAtHead`：先 `place(resource, 'queued')`，再按「它最前、其余保持原序」重建一次（队列规模＝同时待取的身份数，几十个以内）。手动刷新是「人正等着」的取数，排在周期取数前面。**③ 适配层（读了、试了两次、都被实测打回，如实记录）**：确认人给的读取面主意是「手动刷新把上次读取时间置 null」。它首先**被采纳了一部分**：`refresh()` 不再需要单独一个「不等节流」的位——但落地时发现读取面那个位**删不掉**，两次尝试都留下了可复现的红：**(i) 用 `immediate` 顶替读闸门**（`immediate` 在身份落定与配置／生命周期边沿也会置起）→「从未上过屏的暂停页」在下一份结果到达时当场显示，真 Chrome 的 `A11 换身份建立新实例`／`A02/A06 真实HTTP`／`双组件共享` 三条转红（`shown(乙) === null, 'paused page stays frozen'`）。**(ii) 把基线本身置 `null` 当那个位**（`sampled === null` ⇒ 放行读闸门）→ 一次**不产出**的副作用运行会把它用掉：失活那一拍 `eligible` 变化唤醒副作用，它把当前这一格（还是旧版本）抄进去，`null` 当场消费掉；于是「**点过刷新之后才失活**、结果随后到达」的那一帧再也抄不进来——本轮新增的 `A04/A06 点过刷新之后失活：缓存里那一帧仍会更新；失活期间点刷新不产生事实` 用例当场转红（确认人 2026-09-17 对这条角落的裁决是「允许它更新一帧呗」，ADR-67 记录在案）。**结论**：`sampled`（已抄到的那一版：引用比较 ＋ 节流基线）与「这一页还欠一次抄写」是**两件事实，不能压成一个**；适配层保留 `pending`（点过一次刷新、还没看到那一拍）与 `immediate`（边沿不等节流），读闸门＝`core.isEligible(...) || (pending && core.isVisible())`——与 ADR-66／ADR-67 逐字相同。核心侧删掉「谁在等」不受影响：适配层只对**自己的**那一页记账，不再有任何页面清单进入核心。**语义变化（明说）**：**「一个页面同时只能有一次未满足的刷新请求」这个上界没了**——产出之后点的每一次刷新都会补一轮（同一轮内点几次仍合并成一次，因为 `needsNext` 是一个位）。旧口径下「每写一次结果就点一次刷新」的页面最多多取一轮（第二句被吃掉，因为那句话属于同一个未满足要求），新口径下它会一直取下去。这不是理论顾虑：那条用例（`A12/A14 结果写入期间新登记的刷新要求由后继请求满足`）在改动后**当场死循环**（`onWrite` 里 `refresh` 不断置位，全微任务链把事件循环饿死，`pnpm test` 60 秒超时），所以它按新语义改写为「写表期间只点一次」并保留原有断言（后继请求满足它、同轮内两次合并成一次），另加一条钉住新语义的用例（页面每收一次结果就点一次 → 连取 4 轮，停手就不再取）。这正是确认人点明的口径：**刷新是命令，页面自己循环就是页面自己的循环**，框架只保证同一轮内合并。读闸门那一侧的两次尝试被实测打回的过程见上条 ③，不重复。**取证口径**：本轮确认人点名的语义变化只有一条（这条补发上界），读取面**零语义变化**；被否掉的两次改动连同复现方式都写在 ③，免得重提。**改判 ADR-66 的一处记录**：那条把「身份级一个布尔（`needsNext`）」记为「试过并否掉」；本轮确认人点名要它，于是采纳——保留一个位的**用途**（区分「结果还没出来」与「结果已经写进表」），但把「谁在等」从核心整个拿掉。当时那次实验的真正教训是「**一个位不够回答『哪个页面还没拿到』**」，而新口径下这个问题**不再需要回答**：调用方按自己的数据新鲜度读结果表（ADR-67 的时间差节流）。**试过并否掉的**：把「补一轮」也一起拿掉（有执行时刷新一律不排队）——不记 `produced` 就无法区分 A14（结果没出来：本轮就够，不追发）与 A12（结果已写进表：要下一轮），两条验收必坏其一。**取证**：旧 8 幕轨迹在新旧核心下**逐字节相同**（21 行 / 1889 字节）——旧行为面一行没动；随后给轨迹工具补上**队列顺序**（`queued=n[键序]`）与两幕新语义：S9「刷新插队头」（`queued=2[{"id":3},{"id":2}]`，a 结束后起跑的是刷新的 c，实测 `c=30`、`b=2`）、S10「产出之后点两次刷新只补一轮」；新基线 `.tools/trace-after.txt`（28 行 / 2747 字节）。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **49/49**（core 36 ＋ vue 13；新增三条：补发上界的钉法、暂停页「不点刷新就不上屏／点一次才上屏」、失活角落，并改写一条既有场景）；`pnpm check:docs` 全项通过；`pnpm test:browser` 真 Chrome **12/12**（其中 3 条在改动中途抓到过 ③ 里那处读闸门回归，还原后复原）；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过。**度量**：`src/` 1139 → **1155** 行、函数 80 → **81**、结构分支 82 → **81**、`maxCCN` 8 → **7**；`dist/index.js` 11600 → **11557 字节**（11.60 → **11.56** kB）；测试文件 `tests/core.test.ts` 的行数 1036 → **1061**、`tests/vue.test.ts` 的行数 439 → **550**。 | 本次落地；`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`scripts/trace-core.mjs`、`DESIGN.md` §1／§3.3／§3.5／§3.6／§3.7／§9.3／§9.4、`README.md`、`统一刷新管理.md` §0.1／§1／§2.1／§2.4／§2.5／§3.x／§4.x／§6.x／§7／§5（G04）、ADR-60／ADR-66／ADR-67／ADR-69 |
| ADR-71 | 读取面收成「一个读取基准 ＋ 一次比较」：删掉 `immediate`，三处不等节流各自回到「没有读取时间就直接读」 | **采纳并已落地**。确认人第二次追问读取面：「读取面第一读取没有时间所以直接读取，然后记录读取时间，和watch的最新数据时间比较」——即判定应当只有**一个状态**（上次读取的那一份，它的时间就是这一页的读取基准）＋ **一次比较**。**① 删掉 `immediate`**：它原来只回答「这一份不等节流」，三个来源是身份落定、重新成为读者、显式刷新。前两者本来就是**没有读取基准**的状态——把基准清掉即等价（「没有时间就直接读」）；显式刷新改由 `pending` 在判定里直接跳过窗口（`!pending && …`）。于是判定只剩：`cell === sampled` 不动；否则 `sampled === undefined || every === null || sampled.updatedAt + every <= cell.updatedAt` 就换画面，最后 `sampled = cell`——**「记录读取时间，和 watch 的最新数据时间比较」**。适配层的标志 3 → **2**（`released`、`pending`）。**② 一处必须保留的条件（明说）**：配置／生命周期边沿只在**此刻有资格**时才清基准。失去资格那一侧（暂停、失活、隐藏）**不动基准**——否则那一拍会把当前这一版重抄一次、顺手把 `pending` 清掉，于是「失活之前点过刷新、结果随后到达」的那一帧上不了屏（ADR-67 的角落、确认人 2026-09-17 的裁决「允许它更新一帧呗」；用例 `A04/A06 点过刷新之后失活：缓存里那一帧仍会更新；失活期间点刷新不产生事实` 把这条钉住，本轮第一版没加这个条件时它当场转红）。**③ 代价（明说）**：(a) 重新成为读者那一拍即使结果表里的版本没变，也会把同一份内容再发布一次（数据不变、`display` 换新对象——引用去重随基准一起失效）；(b) 这一轮减的是**概念**不是行数：结构分支 81 → **82**、`src/` 1155 → **1160** 行、`dist/index.js` 11557 → **11608 字节**。**④ 仍然删不掉的**：读闸门的第二项只认 `pending`（这一页点过刷新、还没看到那一拍）。ADR-70 记了两次实测：换成 `immediate` 会让未上过屏的暂停页乱显示（真 Chrome 3 条红），换成「读取基准为空」会被一次不产出的副作用运行用掉。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **49/49**（core 36 ＋ vue 13，含那条失活角落用例）；`pnpm check:docs` 全项通过（叶子 19、测试标题 61）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过。**度量**：`src/` 1155 → **1160** 行、函数 81（不变）、结构分支 81 → **82**、`maxCCN` 7（不变）；`dist/index.js` 11557 → **11608 字节**（11.56 → 11.61 kB）；测试文件 `tests/core.test.ts` 的行数 1061（不变）、`tests/vue.test.ts` 的行数 550（不变）。 | 本次落地；`src/vue.ts`、`DESIGN.md` §6.2／§6.4、`统一刷新管理.md` §2.5／§5（G04）、`README.md`（度量与产物尺寸行）、ADR-67／ADR-70 |
| ADR-72 | 读取面收成一个数：`sampled`／`pending`／`immediate` 三样东西合成「上次读取时间 `lastReadAt`」＋ 两次比较 | **采纳并已落地**。确认人第三次点名读取面：「为啥要区分 效果上不是一样的吗 都需要我需要一次数据，判定是 `lastReadAt + every >= update` 啊……读取面肯定是想读数据关 error 啥关系」。**① 状态**：适配层只剩**一个标志** `released`（释放）＋ 读取面**一个数** `lastReadAt: number | null`（上一次抄进画面的那一版数据的 `updatedAt`；`null` ＝ 还没读取过）＋ 一个纯 Vue 触发计数 `eligible`（配置/生命周期边沿让副作用重跑）。`sampled`（存整格）、`pending`、`immediate` 全部删除。**② 判定三行**：读闸门＝`core.isEligible(...) || (lastReadAt === null && core.isVisible())`；**同一版不抄第二遍**＝格子的 `updatedAt` 与失败位都跟画面里那份一样就直接返回（画面本身就是「已经抄到的那一版」，因此不需要另存状态）；窗口＝`lastReadAt !== null && every !== null && cell.updatedAt !== null && lastReadAt + every > cell.updatedAt` ⇒ 不换画面；发布后 `lastReadAt = cell.updatedAt`。刷新只把 `lastReadAt` 置 `null`（数据一到自然就换），换身份与「重新成为读者」两处边沿同样只置 `null`。**③ 「同一版」那一行是承重的（真 Chrome 抓到的）**：失败那一笔**不改 `updatedAt`**、只换 `error`/`failedAt`（ADR-63），所以只有它能看见「只失败、没新数据」的写入。少了它会红两条：`A13`（失败要能被读取面取到）与演示页 `查询列表` 的「失败关闭」（页面重新开启自动刷新后那一拍会把旧版抄一次、用掉置空的读取时间，随后那笔失败被窗口挡住，页面永远等不到失败而关不掉自动刷新）。**④ 唯一变掉的口径（明说）**：暂停页**还没读到过**时会跟着第一份到达的数据上屏一次（此后它有了上次读取时间就冻住）——原来那条「未上过屏的暂停页什么都不显示」的断言（真 Chrome `shown(乙) === null`）已改写为 `shown(乙) === 111`。这正是确认人「效果上不是一样的吗」的落地：没有读取时间就直接读，不分「第一次」与「点过刷新」。「点过刷新之后**才**失活」的那一帧**仍会更新**（「同一版不抄第二遍」保住了置空的读取时间），那条角落用例与 ADR-67 的裁决都原样保留。**⑤ 必须保留的一处**：失去资格那一侧（暂停、失活、隐藏）**不清** `lastReadAt`——若清了，读闸门第二项会把整个失活期放行，画面在后台一路跟下去，「失活冻结」就没了。**⑥ 顺序约束（用例抓到过）**：配置/生命周期 watcher 里必须**先**清 `lastReadAt`、**后** `eligible.value += 1`（后者是 `flush: 'sync'` 副作用的触发点），顺序颠倒会让那一拍带着旧的读取时间去判定而白跑。**订正我前两轮的两处判断（记下来免得重提）**：(a) 我曾说「失败可见性要求存整格」——**错**：按这个模型写，失败靠的是一条「同一版不抄第二遍」的比较，而它可以从**画面本身**推出来，不需要另存状态（我那次「实测」是把现有引用比较换成时间比较，并不是这个模型，方法有错）。(b) 我两次说「`pending` 删不掉」——那是在「未上过屏的暂停页什么都不显示」这条口径下才成立；本轮确认人明确「两者效果一样」，于是把那条口径改成「还没读到过就上屏第一份」，位就不需要了。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **49/49**（core 36 ＋ vue 13）；`pnpm check:docs` 全项通过（叶子 19、测试标题 61）；`pnpm test:browser` 真 Chrome **12/12**（A11 场景那一处断言按新口径改写，另有一处页面用例在过程中抓到了 ③ 的回归）；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过。**度量**：`src/` 1160 → **1152** 行、函数 81（不变）、结构分支 82（不变）、`maxCCN` 7（不变）；`dist/index.js` 11608 → **11668 字节**（11.61 → **11.67** kB）；测试文件 `tests/core.test.ts` 的行数 1061（不变）、`tests/vue.test.ts` 的行数 550 → **549**。这一轮减的是**状态**（读取面 3 件 → 1 件），不是行数。 | 本次落地；`src/vue.ts`、`tests/vue.test.ts`、`tests/scenarios.ts`、`DESIGN.md` §3.5／§3.7／§3.9／§6.2／§6.4／§9、`统一刷新管理.md` §1／§2.4／§2.5／§3.x／§4.x／§6.4.3／§7／§5（G04）、`README.md`、ADR-63／ADR-67／ADR-70／ADR-71 |
| ADR-73 | `eligible` 触发计数也删掉：读取面只剩一个数，副作用只由「这一格被写入」与「换了身份」唤醒 | **采纳并已落地**。确认人一句「`eligible` 也没啥用啊」。它原本唯一的作用是：配置/生命周期变化（`config` 是原地改写的普通对象、不响应式）时，让 `flush: 'sync'` 的读副作用重跑一次。**落地**：删掉 `const eligible = shallowRef(0)`、副作用里那行依赖读取、watcher 里那记 `+= 1`。副作用此后只由两件事唤醒——**这一格被写入**（结果表 cell ref）与**这一页换了身份**（`identity`）；watcher 只做「把 `lastReadAt` 清掉（于是下一份写入不受窗口限制）＋ `core.reconcile()`」。**ADR-72 那条顺序约束（先清 `lastReadAt`、后 `eligible += 1`）随之消失**：没有触发点了，状态与唤醒不再有先后关系。**语义变化（明说，一处小的）**：「重新成为读者」这一拍**不再自己把表里已有的那一版补抄进来**——它只清掉上次读取时间，因此**下一份写入**到达时立即上屏（不等窗口）。差别只在「表里已有一版更新、而此刻恰好没有新写入」这一种情形：页面先显示自己冻结的那一帧，直到下一份写入（≤ 一个共享窗口；若这个身份没有别的读者，页面恢复资格本身就会触发一次取数，取数回来即上屏）。ADR-72 记的那条代价（「重新成为读者那一拍会把同一份内容再发布一次」）随触发点一起消失。首查与显式刷新这两处本身就是写入触发的，不受影响。已加用例钉住新口径（`A06 重新成为读者不会自己补抄：下一份写入到达时才上屏，而且不等窗口`）。**验证**：删掉后 `pnpm typecheck` 0 错误；`pnpm test` **50/50**（core 36 ＋ vue 14）；`pnpm check:docs` 全项通过（叶子 19、测试标题 62）；`pnpm test:browser` 真 Chrome **12/12**（含 KeepAlive 失活/激活、配置非法修正、暂停页三条边沿用例与演示页「失败关闭」）；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；核心未动（轨迹与 ADR-70 那次逐字节相同）。**度量**：`src/` 1152 → **1147** 行、函数 81（不变）、结构分支 82（不变）、`maxCCN` 7（不变）；`dist/index.js` 11668 → **11635 字节**（11.67 → **11.63** kB）；测试文件 `tests/core.test.ts` 的行数 1061（不变）、`tests/vue.test.ts` 的行数 549 → **597**（新增那条用例与说明）。 | 本次落地；`src/vue.ts`、`tests/vue.test.ts`、`DESIGN.md` §6.2／§6.4、`统一刷新管理.md` §2.5／§7／§5（G04）、`README.md`、ADR-72 |
| ADR-74 | 结构里不留没人读的字段：`RefreshSource` 从对象收成带类型的 URL 字符串、`validate` 整条删除（框架零调用方回调）、`snapshot()` 去掉派生的 `declarers` | **采纳并已落地**。确认人两次点名：「你有没有发现有很多类似的问题」「有些结构里面的很多字段都没有（存在的必要），比如 `useRefresh` 为啥需要 `source.name`」，并追问「`validate` 在 define 和 use 里面都有吗」。**① `RefreshSource` 不再是对象**：`export type RefreshSource<P, T> = string & { readonly __params?: P; readonly __result?: T }`——**没有字段、没有运行期结构、不再冻结对象**；`defineRefresh<P, T>(url)` 只收一个参数（原来收 `(name, { validate })`）、只做类型声明并返回那个 URL 字符串；空 URL 的检查移到 `useRefresh`（它一定会被每一页调用）。带 `P`／`T` 只为**在定义点声明一次**，`useRefresh` 从它推出句柄类型。**② `validate` 整条删除**：业务准入由调用方在 `submit` 之前自己判（输入本来就是它自己的东西）；框架只做值域与编码（`Date`／`Map` 这类容器、循环引用、编码不出身份），`rejected` 仍由它们产生。参数准备只剩三步（复制 → 值域 → 编码），`prepareParameters(input)` 不再收 `source`。这完成了 ADR-64「核心零回调」的最后一步：**全库零调用方回调**，框架不再运行任何调用方代码。`tests/types.ts` 里那条把 `validate` 写进 `useRefresh` 的探针（注释「validation belongs to the fixed source definition」）随能力一起删除——确认人在源码里看到的「两处 `validate`」正是它。**③ `snapshot()` 去掉 `declarers`**：它是 `resources[].declarers` 的派生物，调用方一行就能推出来（`resources.flatMap(r => [...r.declarers])`）。**④ 一次「删过头」当场纠正（记下来）**：我最初把 `scheduled`／`flushing` 也并进「只有测试与演示读的投影」删掉了——它们**不是派生物**：`scheduled` 观测「队列已空但唤醒 Timer 已排」、`flushing` 观测「正在一轮 flush 中」，两者都推不出来，而且是我的差分轨迹工具与演示面板的唯一窗口。发现后立即还原，并把子代理因此删掉的三条断言补回（`A17` 销毁后无残留 Timer、`A06` 卸载清理后无残留 Timer、`A09` 满槽不自旋）。**契约变化（明说，破坏性）**：`defineRefresh(url, { validate })` → `defineRefresh(url)`；`RefreshSource` 不再有 `.name`（它本身就是那个 URL 字符串）；`snapshot().declarers` 消失。**导出面计数不变**：仍是 3 个函数（`createRefreshManager`／`defineRefresh`／`useRefresh`）与 6 个类型。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **50/50**（core 36 ＋ vue 14；validate 那几处断言换成了值域拒绝与复制失败两个触发点，用例与断言没有减少）；`pnpm check:docs` 全项通过（叶子 19、测试标题 62；§2.2 镜像用 `node .tools/sync-mirror.mjs` 重新生成）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；`node scripts/benchmark.mjs` 重跑：取数中位 632 次、收敛比 0.99、在途峰值 4、释放后残留全 0。**度量**：`src/` 1147 → **1130** 行、函数 81 → **80**、结构分支 82 → **77**、`maxCCN` 7（不变）；`dist/index.js` 11635 → **11178 字节**（11.63 → **11.18** kB）；测试文件 `tests/core.test.ts` 的行数 1061 → **1059**、`tests/vue.test.ts` 597（不变）。 | 本次落地；`src/public-types.ts`、`src/source.ts`、`src/vue.ts`、`src/core.ts`、`tests/core.test.ts`、`tests/types.ts`、`tests/scenarios.ts`、`examples/sources.ts`、`examples/main.ts`、`scripts/trace-core.mjs`、`scripts/benchmark.mjs`、`DESIGN.md`、`README.md`、`统一刷新管理.md`（含 §2.2 镜像）、ADR-52／ADR-64 |
| ADR-75 | `defineRefresh` 并进 `useRefresh`：入口只剩一个，声明＝「URL 字符串 ＋ 那一行上的 `P`／`T`」，`RefreshSource` 随之消失 | **采纳并已落地**。确认人的问题就是判断：「有没有可能 `defineRefresh` 其实是 `useRefresh` 的一部分……参数也不用到处传递了」——ADR-74 之后 `defineRefresh` 已经只剩「类型声明」这一件事（收一个 URL、原样返回），运行期什么都不做，而它存在的唯一理由是**把 `P`／`T` 从一个函数搬到另一个函数**。**① 合并**：`useRefresh<P extends JsonParameters<P>, T>(url: string, options)` 成为**唯一入口**（URL 就直接写在调用行上，类型参数也写在同一行）；`defineRefresh` 删除、`RefreshSource` 类型删除；空 URL 的 `TypeError` 由 `useRefresh` 抛（原来在 `defineRefresh`）。**导出面 3 函数 ＋ 6 类型 → 2 函数 ＋ 5 类型**（`createRefreshManager`／`useRefresh`；`RefreshDisplay`／`RefreshHandle`／`RefreshManager`／`RefreshOptions`／`SubmitResult`）。`src/source.ts` 只剩参数边界（`Parameters` 与 `prepareParameters`）。**② 「一处声明、多处复用」改由调用方自己承担**：多个页面用同一个 URL 时，应用侧导出一个类型别名 ＋ 一个 URL 常量即可（`type QuoteArgs = {…}` ＋ `const QUOTE_URL = '/api/quote'`）——那是 TS 本来的做法，不需要框架提供一个载体对象。**改判（明说）**：早先那条「`T` 在定义点声明」的裁决被本条取代——声明点从框架的 `defineRefresh` 移到**调用方自己的类型别名**；代价是同 URL 的多个页面不再由框架保证 `T` 一致（要靠应用侧共享类型），换来的是入口少一个、类型少一个、每次调用少一层对象。**③ 门禁**：`defineRefresh` 与 `RefreshSource` 进退役词表（名字从 `src/` 消失就进表，仓里既有做法）。**④ 过程中修掉两条既有的时序 flake**：`A05/A11 暂停页还没有读取时间时…` 与 `A06 重新成为读者不会自己补抄…` 都用固定次数的 `tick()` 等真实调度（约每 5～12 次红一次，`1 !== 2`）；按仓库既有约定改成 `until(条件, …)`，断言强度与用例数不变。另补一条用例：空 URL 被 `useRefresh` 直接拒绝（原来是 `defineRefresh` 的运行期检查，从未被测过）。**契约变化（破坏性）**：`defineRefresh<A, B>('/api/x')` ＋ `useRefresh(quote, options)` → `useRefresh<A, B>('/api/x', options)`。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **51/51**（core 36 ＋ vue 15；连跑 15 次无 flake）；`pnpm check:docs` 全项通过（叶子 19、测试标题 63；§2.2 镜像重生）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过（导出面断言改成「恰好两个函数」）；`node scripts/benchmark.mjs` 重跑：取数中位 632 次、收敛比 0.99、在途峰值 4、释放后残留全 0。**度量**：`src/` 1130 → **1106** 行、函数 80 → **79**、结构分支 77（不变）、`maxCCN` 7（不变）；`dist/index.js` 11178 → **11169 字节**（11.18 → **11.17** kB）；测试文件 `tests/core.test.ts` 的行数 1059 → **1060**、`tests/vue.test.ts` 的行数 595 → **598**。 | 本次落地；`src/source.ts`、`src/public-types.ts`、`src/vue.ts`、`src/index.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tests/types.ts`、`examples/sources.ts`、`examples/main.ts`、`examples/pages/*`、`scripts/trace-core.mjs`、`scripts/benchmark.mjs`、`scripts/check-docs.mjs`、`DESIGN.md`、`README.md`、`统一刷新管理.md`（含 §2.2 镜像）、ADR-24／ADR-58／ADR-74 |
| ADR-76 | 数据结构与函数的逐项复核：删掉 8 处没人读的机械、订正 5 处文档与 3 处会话不符（未新增字段、未改口径） | **采纳并已落地**。确认人：「开始整体重构代码，梳理数据结构，分清数据结构……每个数据结构，数据结构的每个字段，你都需要问自己 需要我持有吗 有没有更好的结构，包括每个函数，需要抽取吗 需要调用链传参，需要外部调用吗等内容 检查代码问题……不要从门禁出发，门禁是保护但不是规则」。**逐项复核后保留的结构（每条都有人读、都有唯一写入点）**：`Config{enabled, every, active}` 三个字段各自承重——`active` 是环境、`enabled` 是意愿、`every === null` 是这一拍非法，资格（`isEligible`）与手动刷新的入口闸分别读它们，删任何一个都会把两条口径塌成一条；`Parameters{args, key}` 是身份键与请求体各一份；`Resource` 的 `declarers`（声明）、`produced`／`needsNext`（这一轮产出没有／还欠一轮）、`settledAt`（到期锚点）、`controller`（取消 ＋ 认人，ADR-65 明确不要独立执行对象）；`RefreshCore` 的 `identities`／`queue`／`running`／`wakeup`／`flushing`／`visible`／`disposed` 与 `snapshot()` 的六项投影；`ResultSink` 的四个动作——`list` 只为观测面存在，留着它 `snapshot()` 才不依赖 Pinia、测试也才能注入假表；`parameterKey`／`copyResult`／`assertJsonValue` 都是被锚点或 DESIGN 点名的**规则**，不是一次调用的别名，故保留（ADR-56 的度量已判定「≤1 个调用点」本身不是缺陷）。**三处「更好的结构」复核后维持原判（明说）**：① 结果格从四个平字段改成「最后一次成功 ＋ 最近一次失败」两个成对对象（`{data, at}` / `{error, at}`）——那正是 ADR-65 拍平之前的 `entry` ＋ `failure`，拍平换来的就是少三个类型，代价（`data === null` 不区分「从未成功」与「成功返回 null」）由「用 `updatedAt`／`failedAt` 判有没有」这一条规则承担；② 注册表与结果表改回 `Map<url, Map<key, …>>` 两层——ADR-68 刚从两层拍成一层；③ 把 `settle`／`fail`、`produced`／`needsNext`、`controller` 各自拆成两个字段——都是「一个字段回答两件事」的既定取舍。**落地（8 处，零行为变化）**：① 删除 `Resource.fail(at)`——与 `settle(at)` 逐字节相同，失败路径改调 `settle`（失败也算结算这条口径不变）；② 删除核心私有的 `writeResult`／`writeFailure` 两个一行转发，`run` 里直接 `sink.write`／`sink.fail`；③ 删除私有 `enqueue`（`place(resource, 'queued')` 的一行别名，调用点直接写 `place`）；④ 适配层不再拿假键 `key ?? ''` 查结果表——`store.read` 是「取或建」，那会给每个用过的 URL 留下一格永远不会被写、也永远不会被删的 `(url, '')`，而这个 `Map` 按设计不缩；⑤ 删掉 `if (core.isDisposed()) stopWatching()` 死分支（入口第一行已经拒绝已销毁的协调者，中间没有任何东西能让 `core` 变成已销毁）；⑥ 模块级 `current` ＋ `currentStore` 合成一个 `installed` 绑定（两者永远同源，合成后 `useRefresh` 只剩一次检查、也少一条重复错误）；⑦ 删除 `createRefreshManager` 的 4 个 sink 转发 lambda，直接把 Pinia store 交给核心当端口（签名本来就同形，核心仍只认识 `ResultSink`）；⑧ `readConfig` 的元组元素加名字（`[enabled, every, active]`），`applyConfig` 的形参改名 `values`。**顺手订正 3 处会话与代码不符**（都在 `src/`／`examples/` 的注释里）：`display` 的注释还在描述 ADR-72 之前的 `pending` 位，并且写着「没点过刷新的暂停页什么都不抄」——现行口径是「没有读取时间就直接读」，真 Chrome 的 A11 断言（暂停页跟着第一份数据上屏一次）恰好相反；`RefreshCore.submit` 的注释还写着已删除的 `validate`；`examples/pages/` 两处注释还在描述 ADR-65 之前的 `failure` 对象。**文档订正 5 处（都是文档错、代码对）**：① `every: 2000` 这种示例不成立（`RefreshOptions.every` 是 `Ref<number>`），统一文档 §2.1 与 README 的示例改成 `const every = ref(2000)`；② `await task.refresh()` 暗示有回执，改成 `task.refresh()`；③ 「具名 `interface` 没有隐式索引签名、会被值域约束打红（TS2344）」不成立——实测 TS 4.9.5（干净消费方的 `interface Params`）与 7.0.2（`tests/types.ts`）都通过，因为那条约束是同态映射类型、不要求索引签名；④ DESIGN §6.1 的「`every` 只在 `enabled` 为真时必需」与代码不符：配置有效是刷新入口闸的一部分，暂停页要能手动刷新也必须给一个有效周期；⑤ DESIGN §3.8 写调度的 `settledAt` 是「单调时间」，代码里它与 `updatedAt` 用的是同一个墙钟（一次取数里那个 `at` 同时是两者），已按代码订正，并明说墙钟回拨会把下一次到期推到时钟追平之后。**零行为变化的证据**：`tests/` 一行未改，51 个用例全绿；`node scripts/trace-core.mjs` 与 ADR-70 之后的基线 `.tools/trace-after.txt` **逐字节相同**（2747 字节，覆盖队列次序与「产出之后再点刷新只补一轮」）。**度量**：`src/` 1106 → **1089** 行、函数 79 → **71**、结构分支 77 → **76**、`maxCCN` 7 → **8**（把一处 `key === null || cell === undefined` 拆成两个守卫，换掉假键查表；代价明说）；`dist/index.js` 11169 → **10738 字节**（11.17 → **10.74** kB）；包 45099 → **45081 字节**、SHA-256 `d6695492f85a1e9cae19cecc96c3be7c0119f9864a7b3f0f7024e4d134a4d87e`（两次 pack 逐字节相同，10 个条目哈希与工作树逐个一致）。 | 本次落地；`src/core.ts`、`src/vue.ts`、`examples/pages/query-list.ts`、`examples/pages/quote-panel.ts`、`DESIGN.md` §2.1／§3.3／§3.4／§3.5／§3.6／§3.8／§3.9／§6.1／§9.3／§9.4、`README.md`、`统一刷新管理.md` §2.1／§3.5（U15）；关联 ADR-56／ADR-65／ADR-68／ADR-72／ADR-74 |
| ADR-77 | 失败有独立的读出口：`display` 只装数据，失败走不参与数据窗口的 `failure`（`RefreshFailure` 类型回来） | **采纳并已落地**。确认人先否掉「让失败挤进窗口」这条思路：「窗口display 要的就是数据 为啥要考虑 失败，失败是通过onerror 处理的啊」；核对事实后我报告：`onError` 已在 ADR-63 删除（`tests/types.ts` 有反向探针钉着），今天页面拿失败的唯一通道就是 `display.error` / `display.failedAt`；确认人给出做法：「没毛病啊 是绑定在resource上啊，display 读取数据 创造个新的 读取error 不就行了」。**落地**：① `RefreshHandle` 增第二个读出口 `failure: Readonly<Ref<RefreshFailure | null>>`，`RefreshFailure = { error, failedAt }`（ADR-65 删过的同名概念按新口径请回来）；② `RefreshDisplay` 收成 `args` / `data` / `updatedAt` 三格——它只管数据；③ 适配层把那个读取副作用拆成 `publishData`（同一版不抄第二遍 ＋ 窗口比较，仍只记一个数 `lastReadAt`）与 `publishFailure`（这一格最近一次失败换了就发布，成功是 `null`）；两者共用同一个读者闸门；④ 导出面 2 函数 ＋ 5 → **6 个公共类型**（`RefreshFailure` 逐个列出）。**口径变化（明说）**：**放宽 U13**——失败不再「按自己的节流窗口读」，它不参与数据窗口、换一笔就发布，因此「成功过的身份上持续失败完全静默」这个缺口被关掉（ADR-76 报的那条）；**收窄 U11**——数据出口只剩三格，失败移到 `failure`，`display` 上不再有 `error` / `failedAt`（`tests/types.ts` 与干净消费方各有一条反向探针钉住）。**不变**：失败仍是那一格的事实（ADR-63）、成功后清回 `null`、失败也算结算且不自动重试、不推送、也**不是回调**——ADR-64「框架不执行任何调用方代码」照旧；所谓「独立出口」是第二个只读 `Ref`，不是 `onError` 回来。**一个设计选择（明说）**：`failure` 给的是「这一格**此刻**的失败态」，不是「某一版的失败」——它没有版本、也不需要窗口；`null` 与「非 `null`」就是有没有失败（`error` 本身可以是 `undefined`，不承担存在性）。因此「已经显示的旧数据」与「最近一次失败」可以来自不同的两轮取数，这正是「数据按本页节流、失败立刻」的语义；两个出口共用同一个读者闸门，所以暂停／失活／隐藏仍然冻结（还没读取过的页面照旧跟着第一份上屏一次，A11）。**反向对照（真证据）**：把老口径的窗口加回 `publishFailure`，新增用例 `A13/A11 失败与数据是两个出口：周期性失败不等数据窗口就上屏，成功一到又清回 null` 立刻以「超时：周期性失败立刻上屏，不等数据窗口」失败；去掉那三行后 52/52 全绿——这条用例真的在探这个行为，不是复述现状。**另外**：顺手清掉 `tests/pages.spec.ts` 里两处 ADR-63 的残影（注释还写着「关闭由页面在 `onError` 里做」）与 README 里「没点过刷新的暂停页什么都不上屏」这句已被 A11 反向钉住的旧口径。**证据**：`pnpm test` **52 个用例**全绿（`tests/core.test.ts` 36、`tests/vue.test.ts` 16）、`pnpm typecheck` 0 错误、`pnpm check:docs` 全项（叶子 19、测试标题 64、§2.2 镜像重生）、`pnpm build` / `build:demo` 通过、真 Chrome **12/12**（浏览器场景从 harness 投影读失败，行为不变）；`node scripts/trace-core.mjs` 与基线逐字节相同（本条不动 `core.ts`）；干净消费方（真实 axios／HTTP／pinia ＋ TS 4.9.5）类型 0 错误、**8 条**运行期场景通过（新增「成功之后的周期失败立刻上屏」）。**度量**：`src/` 1089 → **1129** 行、函数 71 → **73**（两个发布函数）、结构分支 76 → **79**、`maxCCN` 8 → **6**（读取面那一个副作用拆开之后，最深的只剩 `run`）；`dist/index.js` 10738 → **10920 字节**（10.74 → **10.92** kB）；包 45081 → **46130 字节**、SHA-256 `2c106e59bb802050e9c7b681ecaddc9cce79371ddfaf7a2b5bd06a3f7681337c`（两次 pack 逐字节相同，10 个条目哈希与工作树逐个一致）；测试文件 `tests/vue.test.ts` 的行数 598 → **639**。 | 本次落地；`src/public-types.ts`、`src/vue.ts`、`src/index.ts`、`tests/types.ts`、`tests/vue.test.ts`、`tests/pages.spec.ts`、`examples/main.ts`、`examples/pages/*`、`README.md`、`DESIGN.md` §2.1／§2.2／§3.3／§3.4／§3.5／§4.2／§6.2／§6.4／§9.2／§9.3、`统一刷新管理.md` §0.1／§0.2／§1／§2（含 §2.2 镜像）／§2.4／§2.5／§3.3（U11–U13）／§4.4／§6.1／§7；关联 ADR-63／ADR-64／ADR-65／ADR-67／ADR-72／ADR-76 |
| ADR-78 | 读取面随协调者退场：协调者销毁时两个出口一起清空，页面里的入口一律现读「此刻活着的那个」 | **采纳并已落地**。确认人先核事实：「协调者理论上只有 app 整个销毁才会出现吧，所以为啥要考虑销毁但是页面还活着」——规格上成立（`src/` 里 `core.dispose()` 的写入点只有两处：`install` 的 `app.onUnmount` 与 `manager.dispose()`，见 ADR-79 之后的 `src/vue.ts:339`／`:344`），所以在纯 SPA 生产路径里「销毁了但页面还活着」看不见；它只有三个真实来源：**HMR**（`examples/main.ts` 的两处 `import.meta.hot.dispose`，热更新销毁协调者而页面是热替换）、**调用方主动 `dispose()`**（公开方法：多 App、会话切换、测试）、以及**测试与基准脚本**（`tests/vue.test.ts` 的 afterEach 与两条用例、`scripts/benchmark.mjs`）。确认人随后裁决按 B 收（本条与观测面外移分两条 ADR、都落）。**缺陷（探针实测，不是推断）**：`manager.dispose()` 之后结果表条目已随 `releaseIfUnused` 清空（`results` 为 `[]`），而页面读到的 `display` 仍是销毁前那一帧 `{"args":{"symbol":"A"},"data":7,...}`——读取副作用只依赖「那一格」与「身份」，而销毁两者都不动，于是没有任何东西叫醒它。更硬的一条：销毁之后**同一次重跑里 `failure` 会清成 `null`，`display` 却冻住**——两个出口对同一个「协调者没了」的事实给出相反答案，这是自相矛盾的状态，不是「没影响」。**① 现象口径**：HMR 下句柄被模块级变量举着越过销毁点（模块级绑定**刻意不置空**，读取面也不读它），于是页面读一份已经不存在的协调者留下的数据，而且再也没有东西能叫醒它；同步窗口内（`app.unmount()` 的 `onUnmount` 钩子先跑、组件 `onScopeDispose` 后跑）也短暂如此，只是页面正在拆、不渲染，用户看不见。**② 修法（一处依赖，不新增状态）**：读取副作用**第一件事**读模块级 `installed`，它因此成为这个副作用的依赖；协调者退场时把它置空，副作用重跑一次，走「没有协调者」分支把两个出口一起清回 `null`（内联：`display`／`failure` 清空 ＋ `lastReadAt` 归零）。仍然**没有**第二个名册、没有版本号、没有回调：`installed` 本来就是「当前协调者＋结果表」那一个同源绑定。**③ 页面不再捕获会变死的协调者**：`useRefresh` 原先在 `setup` 里 `const { core, store } = installed` 捕获一次，销毁后 `submit` 查的是新实例／读取面问的是「没有协调者」，**同一个页面会分两半**；现在 `submit`／`refresh`／配置 watcher／`onScopeDispose`／读闸门一律走 `currentCore()` 现读，并且一个回调里只读一次（读两遍可能拿到两个不同实例），`const { store }` 只留结果表。**语义变化（明说，一条）**：`currentCore()`（不在包导出面，给演示面板与基准脚本用）在销毁后回 **`null`**、不再回那个已销毁的实例。仓库里所有调用点都在「协调者活着」时读它（`scripts/benchmark.mjs:83` 在 `install` 之后、`dispose` 之前；`examples/main.ts:123`／`:289` 同理），所以没有破坏；若将来要保留「翻旧实例诊断」，要么再加一个只给观测用的绑定（ADR-66 明确不想要的第二份账），要么撤销本条——已报给确认人备案。**④ 顺手清掉一处同源重复**：身份键的**拼**在 `core.ts` 的 `identityOf`，而**拆**原先在 `store.ts` 的 `list()` 里自己认 NUL（`indexOf('\0')` ＋ 两次 `slice`）——`identityOf` 自己的注释骂过这类重复（「两处各写一遍拼接就是一处会悄悄走样的重复」），走样时又没有任何断言拦得住：整条键会被当成 `key` 返回，观测面的 `results[].key` 悄悄变形。新增 `splitIdentity`（与 `identityOf` 紧挨着，一对函数独占分隔符这一个事实），`store.ts` 只调它；用例钉住「往返还原两级」与「key 里出现 NUL 也不串级」，不存在的输入不补分支（`core.ts` 的注释写明前提）。**取证**：`manager.dispose()` 前后各打印一次——修前 `display={"args":{"symbol":"A"},"data":7,...}`／`results=[]`，修后 `display=null`／`failure=null`；新增用例 `A17 协调者销毁后读取面一起清空：结果表已空，两个出口不该把最后一帧冻在画面上（ADR-78）` 同时钉住「销毁后 `submit` 回 `cancelled`、`refresh` 不产生事实」与「原地接管（`app.use` 新 manager）后画面读新协调者的结果」，`tests/vue.test.ts` 另加 `snapshotOf()` 助手把「没有协调者」与「协调者里没有东西」分开（原来 4 处 `currentCore()?.snapshot()` 的 `?.` 把两者合成一个 `undefined`）。**门禁**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **54** 全绿（`tests/core.test.ts` 37、`tests/vue.test.ts` 17；本条加两条：身份键往返、读取面清空）；`pnpm check:docs` 全项通过（19 个叶子、66 条测试标题）；`pnpm test:browser` 真 Chrome **12/12**；`pnpm build`／`build:demo` 通过；`node scripts/benchmark.mjs`：取数中位 632、收敛比 0.99、在途峰值 4、释放后残留全 0、事件循环延迟 p99 1.77ms／max 7.02ms。**没有用差分轨迹**（`.tools/trace-after.txt`）：本条的改动面全在适配层，核心的可观测行为一行未动，而轨迹工具读的正是核心账本——它对这个改动是恒等的，跑它不构成证据；能给核心零变化背书的仍是「`src/core.ts` 只多了一个不参与调度的 `splitIdentity`」。**度量**：`src/` 1155 → **1193** 行（`slot` ＋ `reportConfig()` 单一写入口、`splitIdentity` 与各自注释；净额被删掉的 `core` 解构、`core.isDisposed()` 检查与捕获抵掉一部分）、函数 76 → **77**、结构分支 82 → **84**、`maxCCN` 6（不变；新增的都是 CCN 1～2 的浅分支）；`dist/index.js` 10970 → **11259 字节**（10.97 → **11.26** kB）；包 **48807 字节**、SHA-256 `7a51cf53db7331ec13a04694ab58bb7c0692e36c24a5b1a71a10dcda694262b3`（两次 pack 逐字节相同，10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，哈希与工作树逐个一致）。 | 本次落地；`src/vue.ts`、`src/core.ts`（`splitIdentity`）、`src/store.ts`、`tests/vue.test.ts`、`tests/core.test.ts`、`README.md`（度量／产物尺寸／用例数行）、`统一刷新管理.md` §2.5／§7／§5（G04）；关联 ADR-63／ADR-64／ADR-66／ADR-70／ADR-72／ADR-77 |
| ADR-79 | 观测面搬出核心：只读计数投影从 `RefreshCore.snapshot()` 移到测试侧支撑模块，核心的公开面只剩真正的外部动作 | **采纳并已落地**（与 ADR-78 同一次收尾，确认人裁决「B」：两个会话的改动分两条 ADR 一起落）。**① 决定**：删掉 `RefreshCore` 的 `snapshot()` 成员，只读计数投影改成 `tests/support/observe.ts` 里的一个纯函数 `snapshot(core)`，它返回同样的四个字段（`resources`／`results`／`queued`／`running`）。**② 为什么**：投影**不是包契约**（`src/index.ts` 从不导出它），却是核心唯一一个「只为别人看状态」的公开成员——`core.ts` 因此多背一个不是动作的成员、多一个 `sink.list()` 的读取理由；而它的四个消费者（演示面板、差分轨迹、基准脚本、集成测试）本来就在 `src/` 之外。搬走之后核心的公开面只剩外部动作：`isDisposed`／`isVisible`／`setVisible`／`reconcile`／`submit`／`refresh`／`isEligible`／`undeclare`／`dispose`（公开成员 **10 → 9**），`core.ts` 的职责分段 **6 → 5**（「观测面」整段消失，`README.md` 与 `DESIGN.md` 的分段列表同步改成五个，`check-docs.mjs` 的「核心分段与文档一致」那条会核对）。**③ 越界读私有账本这件事被显式化并加了形状校验**：支撑模块用一次 `as unknown as Partial<CoreLedger>` 读三项私有集合（`identities`／`queue`／`running`）与 `sink.list()` 这一条结果表缝；**读之前先验形状**（不是 `Map`／不是 `Set`／`list` 不可用就当场抛 `TypeError` 并提示「要同步形状视图」），而不是静默返回空数组——观测面最容易出的错就是内部改名后断言假通过，这里把它变成当场红。**④ 四个使用方改名接入**：`tests/core.test.ts`（`core.snapshot()` 全部改走 `snapshot(core)`，并新增 `tests/support/observe.ts`）、`tests/vue.test.ts`（改用 `snapshotOf()` 助手，见 ADR-78）、`scripts/trace-core.mjs`、`scripts/benchmark.mjs`、`examples/main.ts`（演示面板与 harness 桥）。**⑤ 顺手删掉两个推得出来／已无人读的投影字段**：`scheduled`／`flushing`（`snapshot().scheduled`／`flushing`）与演示面板 `inspect()` 的 `scheduled`／`timer`／`pending` 字段——它们消费的是核心的 `wakeup`／`flushing` 私有状态，随投影一起搬走会给支撑模块再加两条越界缝，而差分轨迹与基准脚本对它们的断言已经不再需要（`timer`／`pending` 在轨迹里的镜像字段也一并去掉）。**代价（明说，两处）**：其一，**生产侧的示例面板与基准脚本现在从 `tests/support/` 导入**（`examples/main.ts` 的 `import { snapshot } from '../tests/support/observe'`）——「示例反向依赖测试目录」在同一个仓里可接受（示例与文档本来就不进包），但如果将来示例要独立分发，就得把这个投影搬回一个明确的开发用出口；其二，核心内部一改名，`tests/support/observe.ts` 的形状视图要跟着改（形状校验会在下次跑测试时当场抛错，不会静默）。**没有做的（明说，留作裁决）**：把投影放回 `src/` 作为一个公开的「开发用出口」——那会让它重新变成包契约（本库的立场是核心只留动作、观测留给测试侧），在确认人未点名之前不做。**验证**：`pnpm typecheck` 0 错误（含形状视图的类型）；`pnpm test` 用例总数 **54** 全绿（`tests/core.test.ts` 37、`tests/vue.test.ts` 17；`tests/core.test.ts` 里的投影读点全部改走支撑模块，断言强度与用例数不变）；`pnpm check:docs` 全项通过（19 个叶子、66 条测试标题、核心分段与文档一致）；`pnpm test:browser` 真 Chrome **12/12**（harness 桥的投影照旧喂给 `tests/scenarios.ts`）；`pnpm build`／`build:demo` 通过；`node scripts/benchmark.mjs` 正常（投影来自支撑模块）；`dist/core.d.ts` 里不再有 `snapshot`（`grep -rn snapshot dist/*.d.ts` 无命中），`dist/core.d.ts` 多了 `splitIdentity`（ADR-78）。**度量**：`src/core.ts` 560 → **570** 行（删掉「观测面」整段与 `snapshot()`，再加 ADR-78 的 `splitIdentity` 与其注释）；`src/` 合计 1155 → **1193** 行（并入 ADR-78 的改动）；函数 76、结构分支 82、`maxCCN` **6** 均不变；`dist/index.js` **11259 字节**（11.26 kB）；包 **48807 字节**、SHA-256 `7a51cf53db7331ec13a04694ab58bb7c0692e36c24a5b1a71a10dcda694262b3`（两次 pack 逐字节相同）。**订正**：另一个会话在 `DESIGN.md` §3.3／§4.2／§9.3 与 `统一刷新管理.md` §2.4 里把这次外移记成了「随 ADR-74」——ADR-74 那次删的是 `snapshot()` 的派生字段 `declarers` 与 `RefreshSource`／`validate`，外移发生在本条，四处已改成 ADR-79。 | 本次落地；`src/core.ts`、`tests/support/observe.ts`（新增）、`tests/core.test.ts`、`tests/vue.test.ts`、`scripts/trace-core.mjs`、`scripts/benchmark.mjs`、`examples/main.ts`、`README.md`（分段数与度量行）、`DESIGN.md` §1／§3.3／§4.2／§9.3、`统一刷新管理.md` §2.4／§7／§5（G04）；关联 ADR-46／ADR-74／ADR-78 |
| ADR-80 | 注释只留一句「做什么」：删掉 359 行无效解释，代码零改动 | **采纳并已落地**。确认人：「检查需求是否现在都是我后来给你明确的，把注释里面大量无效解释全删了」。**改动**：`src/` 六个文件的注释按「第一句＝做什么」重排，删掉设计理由、ADR／DESIGN 引用、用例编号与举例。注释行 **516 → 157**、`src/` 总行 **1193 → 834**。**代码零改动**（按 `check-docs.mjs` 的 `stripComments` 口径比对：六个文件的非注释行逐行相同），函数与结构分支不变，`dist/index.js` 仍是 **11259 字节**（打包时注释已被剥离）；变了的是 6 个 `.d.ts`（随注释瘦身，包 48807 → **33430 字节**）。**门禁**：`pnpm typecheck` 0 错误、`pnpm test` **54** 全绿、`pnpm check:docs` 全项通过（`RegExp`／`ArrayBuffer`／`NaN` 三个被文档反引号的符号按门禁保留在 `src/source.ts` 的值域说明里）。**度量**：`src/` 1193 → **834** 行、结构分支 84（不变）、`maxCCN` 6（不变）；包 **33430 字节**（10 个条目，SHA-256 `91d7066189dd33f813f5c66c257e63c10478212c6681444b4f5615a85edcf692`）。无（已关闭）。 |
| ADR-81 | 代码排查后的八处收敛：删掉死变量／死条件／端口错位／反向依赖，核心补 `readResult`，按 SPA 单例定口径 | **采纳并已落地**。确认人：「排查代码 里面无效变量，假设场景非真实需求场景 仅测试使用代码 多余的数据结构 错误的持有结构」，随后给出定性：「全部修复，就是spa单例，所有不是单例都是因为hmr热更新，不要假设需求」。**逐条落地（八处）**：① 删掉 `useRefresh` 里解构后零读取的 `const { store } = installed.value`（`noUnusedLocals` 未开，门禁抓不到）；② 安装检查从 `installed.value === null || installed.value.core.isDisposed()` 收成 `installed.value === null`——后者恒为 false（install 先挡已销毁实例，`uninstall()` 又在 `core.dispose()` 之前把槽置空）；③ `ResultSink` 去掉核心**从不调用**的 `list`，换成核心真正用的 `read`，核心新增 `readResult(url, key)` 供读取面用（端口形状不再由观测面决定，核心也不再有不用到的端口方法）；④ 观测面从 `tests/support/observe.ts` 搬到 `scripts/observe.ts`（`package.json` 的 `files` 只有 `dist`，scripts 不进包），示例与工具不再反向依赖测试目录，`DESIGN.md` 三处同步改成「开发侧支撑模块」；⑤ 删掉 `boundApp` 与「同一实例装到第二个 App」的冲突分支——SPA 单例、非单例只有 HMR 的原地接管，install 只按 `installed.value` 判「同一个实例重复安装」与「已有存活实例」；⑥ `identity`（`Ref`）与 `declared`（`let`）两处持有同一次提交，合成一个 `submitted`（`shallowRef<Parameters | null>`），顺带删掉 `publishData` 里那条只为类型收窄的不可达分支；⑦ `installed` 只持有协调者（原先 `{ core, store }` 与核心内部的 sink 重复持有同一个 store），页面读取改走 `core.readResult`；⑧ `Resource.isPresent`／`eligibleEvery` 收成 `private`。**没有改（明说）**：观测面仍用一次带形状校验的 `as unknown as` 越界读核心三个私有集合（ADR-79 的既定设计，未点名不动）。**口径不变**：SPA 单例；非单例只发生在 HMR 原地接管。**订正**：ADR-79 记的支撑模块位置 `tests/support/observe.ts` 由本条改为 `scripts/observe.ts`。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用**例总数 54** 全绿；`pnpm check:docs` 全项通过；`build` 与 `build:demo` 通过。**度量**：`src/` 834 → **824** 行、结构分支 84 → **83**、`maxCCN` 6（不变）；`dist/index.js` 11259 → **11024 字节**（11.02 kB）；包 33062 字节、SHA-256 `0f28e36809ad36196c0d43740de0f7f7fc3207e52919f8465ecd328ab1fd69b7`。无（已关闭）。 | 本次落地；`src/core.ts`／`src/vue.ts`（八处收敛）、`scripts/observe.ts`（自 `tests/support/` 迁入）、`tests/core.test.ts`（假表补 `read`）、`scripts/trace-core.mjs`／`scripts/benchmark.mjs`／`examples/main.ts`（改导入）、`tests/vue.test.ts`（改导入）、`README.md`（度量行与产物尺寸）、`DESIGN.md`（观测面位置）；关联 ADR-63／ADR-74／ADR-76／ADR-79 |
| ADR-82 | 模块拆分：`Resource`（含 `Config`）独立成 `src/resource.ts`，`RefreshCore` 留在 `src/core.ts` | **采纳并已落地**。确认人：「resource和refrescore 拆成俩文件」。**落地**：新增 `src/resource.ts`（`Config` 配置槽 ＋ `Resource` 类），`src/core.ts` 只留 `RefreshCore` 与结果表类型（`ResultCell`／`ResultSink`）、身份键（`identityOf`／`splitIdentity`）、传输接口（`RefreshHttp`）与调度私有件。**没有环**：`Resource` 不持有核心、也不 import 核心，只 import `source.ts` 的 `Parameters` 类型，因此运行期边只有 `core → resource`，严格向下。**层表与文档同步**：模块层插入 `source` 与 `core` 之间（`public-types ← source ← resource ← core ← store ← vue ← index`），`scripts/check-docs.mjs` 的 `LAYERS` 表加上 `resource.ts: 2` 并顺移其后各层，`README.md` 与 `DESIGN.md` 的 ```text 模块清单、依赖方向行、模块职责表同步；顺带订正 `DESIGN.md` 里 `RefreshCore` 的公开面计数（补 ADR-81 新增的 `readResult`，9 → 10）与 `store.ts` 的端口动作名。**导入点改指**：`src/vue.ts`、`tests/core.test.ts`、`scripts/observe.ts` 的 `Config`／`Resource` 改从 `resource.ts` 取。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用**例总数 54** 全绿；`pnpm check:docs` 全项通过（**7 modules**）；`build`／`build:demo` 通过；真 Chrome **12/12**；`node scripts/benchmark.mjs`：取数中位 632、收敛比 0.99、在途峰值 4、释放后残留全 0。**度量**：源文件 6 → **7**、`src/` 824 → **830** 行、结构分支 **83**（不变）、`maxCCN` **6**（不变）；声明文件 6 → **7**；`dist/index.js` 11024 → **11028 字节**（11.03 kB）；包 **33184 字节**（11 个条目，SHA-256 `53363e4a104fa1198629f059e1e4febbcd175f1161c61a5237b59e7b18420919`）。无（已关闭）。 | 本次落地；`src/resource.ts`（新增）、`src/core.ts`、`src/vue.ts`、`tests/core.test.ts`、`scripts/observe.ts`、`scripts/check-docs.mjs`（层表）、`README.md`（模块清单／依赖方向行／度量与产物尺寸）、`DESIGN.md`（模块清单／依赖方向行／模块表／公开面计数）；关联 ADR-56／ADR-65／ADR-66／ADR-81 |
| ADR-83 | `maxConcurrent` 就是真实 HTTP 并行度（实测复核）：补一条数传输层在途峰值的用例，结构不改 | **复核后不改结构**。确认人：「refreshcore 出现了重大设计失误，http根本没有并行，我们设计的 maxConcurrent 是 http 并行度，结果高出了个 fifo quene 和 running，串行在这调用……我们需要维护的就是一个并行 http set 执行完就从队列移动一个过去，而不是现在虽然有两个 但是根本没有并行啊」。**先取证**：写探针直接数**传输层**的在途调用（不是框架自己的投影）——8 个身份、`maxConcurrent=4`、假传输可控放行：按下后**在途 = 4、峰值 = 4**；放行 1 个之后**在途仍是 4**（队列里立刻补上一个）、峰值不变；稳定期 400ms（every=50ms、单请求真实耗时 20ms）每 5ms 采样，**在途峰值恒为 4**。**机制**：`startQueued` 用 `void this.run(resource)` 起（**不 await**），`run` 在 `await this.http.post(...)` 处挂起，循环因此继续把下一个也发出去——同一轮最多发出 `maxConcurrent` 个，是真的同时在途；`run` 的 `finally` → `flushSoon` → `flush` → `startQueued` 从队列补下一个，所以在途在稳定期一直顶满上限。**两个结构不是重复**：`running` 就是「并行 http set」（同时兼并发计数），`queue` 是**待取 FIFO**，只负责顺序（手动刷新经 `enqueueAtHead` 插到队头）。**结论：没有串行，结构不改**。**但证据面有缺口，本轮补掉**：仓里此前唯一「并行」的证据是框架自己的 `running` 投影（循环论证），因此新增用例 `A09 maxConcurrent 就是真实 HTTP 并行度：在途顶满上限，一个结束立刻从队列补上一个`——直接数传输层调用，断言 4 个同时在途、峰值恒等于上限、放行一个立刻补一个、最终 8 个身份各发起一次；用例总数 54 → **55**（core 37 → 38）。**顺带查明**：`examples/main.ts` 的演示默认并发是 **2**（`?slots=1` 时为 1），所以在演示里最多看到 2 个在途——那是示例配置，不是核心。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **55/55**；`pnpm check:docs` 全项通过（67 条测试标题）；`pnpm build` 通过（**`dist/index.js` 不变，11028 字节**——本轮只动测试与文档）。**度量**：`src/` 830 行、结构分支 83、`maxCCN` 6 均不变；包 **33186 字节**（SHA-256 `ec760846cc1649cbbbbd0cf34e30c9fe74aa37da5398300405170f761609a162`）。无（已关闭）。 | 本次落地；`tests/core.test.ts`（新增并行度用例）、`README.md` 与 `统一刷新管理.md` §7（用例数 54 → 55、core 37 → 38）；关联 ADR-65／ADR-70／ADR-81 |
| ADR-84 | RefreshCore 收一遍：删死调用与一行包装，执行位置从四态字符串改成「数组队列 ＋ 在途集合」，并修掉一个复活缺陷 | **采纳并已落地**。确认人：「refrescore的代码能否继续优化了 看着很多调用 实现的很奇怪」→「全部做 不要出现reconcile 这种意义不明的函数」。**① 修掉一个真缺陷（探针实测）**：`refill` 会把**已释放的身份复活重取**并把结果写回表——路径是 `settle` → `sink.write` 的同步扇出里页面 `refresh()`（此时 `produced` 为真，于是 `needsNext = true`）并当场卸载（`releaseIfUnused`），回到收尾时 `refill` 见到 `needsNext` 仍就把这个已不在注册表里的实例排进队列，`startQueued` 照样发请求、把已删掉的格子写回来（探针：`calls` 1 → 2，且写回 `{"data":999}`）。修法：`releaseIfUnused` 里把这一份账一并作废（`needsNext = false` ＋ 摘出队 ＋ 清 `controller` ＋ abort）——符合「刷新是给身份的命令，不留账」。复验：`calls` 回到 1、结果表不再被写回；新增回归用例 `A06/A17 写表时的同步重入里刷新并卸载：释放之后不再补一轮，身份不会被复活重取`。**② 删掉两处死调用**：`dispose` 里的 `queue.clear()` 与 `identities.clear()`——在队的实例必带 `controller`，而回收会把实例从注册表与队列一起摘掉，循环之后两个集合必然已空。**③ 删掉四处一行包装**：`all()` / `find()` / `refill()` / `scheduleWakeup()` 全部内联消失。**④ 消掉 `reconcile` 这个意义不明的公开入口**：配置写入与重排合成一个方法 `setConfig(config, enabled, every, active)`——适配层把三个值交给核心，核心在同一个同步块里写槽并重排；`Config` 的唯一写入口从「适配层的 `applyConfig` ＋ 另一次 `reconcile()`」收成「核心的 `setConfig`」，适配层的 `applyConfig` 随之删除。**⑤ 执行位置不再用四态字符串**：`place(resource, 'queued'|'running'|'abandoned'|'idle')` 删除，改成两个各说一件事的结构——`queue`（**数组**，只表达待取顺序：`enqueue` 排到队尾或插到队头、`dequeue` 摘出）与 `running`（**在途集合**，并发槽的唯一事实）；「这次执行还算不算数」仍由 `Resource.controller` 单独表达（回收时清空它，请求仍占着 `running` 里那一格直到真实结束）。观测面的形状视图同步改成数组校验。**口径不变**：SPA 单例；`maxConcurrent` 是真实 HTTP 并行度。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **56**（core 38 → 39）全绿；`pnpm check:docs` 全项通过（68 条测试标题）；`build` 与 `build:demo` 通过；真 Chrome **12/12**；`node scripts/benchmark.mjs`：取数中位 632、收敛比 0.99、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 正常。**度量**：`src/` 830 → **802** 行、结构分支 83 → **80**、`maxCCN` 6 → **7**（`run` 的收尾内联了「补一轮」那一支）；`dist/index.js` 11028 → **10585 字节**（10.59 kB）；包 **32842 字节**（SHA-256 `2acc774e2633fe27e2e9c669632df5502fe3bb5d5ce2ab641318ebec0a244c73`）。无（已关闭）。 | 本次落地；`src/core.ts`、`src/vue.ts`（删 `applyConfig`，改调 `setConfig`）、`tests/core.test.ts`（harness 走 `setConfig` ＋ 新增回归用例）、`scripts/observe.ts`（queue 形状改数组）、`DESIGN.md`（链路图／模块表／私有动作表／时间线全部改名）、`README.md`（度量行与产物尺寸）；关联 ADR-56／ADR-62／ADR-65／ADR-66／ADR-70／ADR-79 |
| ADR-85 | 接管后的配置以当前 refs 为准（删掉不承重的 `slot`）、身份键拆分认最后一个 NUL、`settledAt` 复核保留，并做一次全文档清理 | **采纳并已落地**。确认人四条：版本口径「毫秒的两个数据来源默认是一样的 足够业务使用了」（保持，不引版本号）、① 按推荐 A 收、「清理文档 文档腐朽度太高了 从头查看所有文档全部清理」、「`settledAt` 好像没啥用你在分析」＋「url 不会含 nul 可以换成 `lastIndexOf` 更严格」。**① 删掉 `slot`**：它三处用途没有一处承重——普通 `let` 进 watch 取值函数**不登记依赖**，所以代码注释与 ADR-78 记录里「`slot` 在依赖里，所以换了协调者会重新报一次」不成立；取值函数改读 `installed.value`、`onScopeDispose` 改走 `currentCore()`，接管后新协调者因此拿到这一页**当前**的配置（探针：空窗期关掉这一页，接管后取数 1 → 0）。**② 身份键拆分改认最后一个 NUL**：键里不可能有裸 NUL（JSON 编码把控制字符转义成六个字符），URL 里可能有，所以 `splitIdentity` 的前提从「URL 里没有 NUL」（调用方假设）换成「键里没有裸 NUL」（编码保证）；A20 用例按新前提改写。**③ `settledAt` 复核后保留**：它是「最近一次执行有结局」的唯一记录（成功与失败都写），`dueAt` 用它现算；删掉就得让调度去读结果表、把 `max(updatedAt, failedAt)` 当结算时刻——一处事实拆成两个字段加一个取最大值的地方。**④ 全文档清理**：DESIGN 里点着三个已不存在的名字（`find`×3、`place`×3、`declared`）与一处已删的死调用（`identities.clear`×2），另有端口的 `list` → `read`、`validate` 引用、§9.4 的重复词、§3.7 引错 ADR 号、§6.1 的 watch 源（补上安装槽）、`maxConcurrent` 的「在册的执行数」→「在途请求数」；README 的「6 个运行时模块」→「7 个源文件」、`A01`–`A20` → A21、benchmark 行重测、「本轮新增的」这类相对时间说法、产物尺寸；规范两处「框架不依赖应用的状态库」（`pinia` 是 peer 依赖、结果表就挂在它上）、§2.1 示例缺 `pinia`、五处「成功与失败都只经 `display`」（失败走 `failure`）、§2.5 的「只有一个读出口」与 `snapshot()` 的位置（测试侧 → 开发侧 `scripts/observe.ts`）、U07 的「正常结束」、「取数上限」→「单次取数的截止」（四处）、§7 的模块清单漏 `resource`、条目数 10 → 11、测试标题 64 → 69、浏览器场景「三条代表页面」→四条。**门禁补退役词**：`check-docs` 第 17 组加 `place`／`find`／`declared`／`identities.clear`——这类小写退役名此前只有管 PascalCase 的第 15 组看得见。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **57**（core 39 ＋ vue 18）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、69 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**；benchmark 中位 637、收敛比 1、在途峰值 4、释放后残留全 0；`trace-core` 输出与 HEAD **逐字节相同**（核心只改了 `splitIdentity` 一行，轨迹不覆盖适配层）。**度量**：`src/` 802 → **795** 行、结构分支 80 → **79**、函数 70、`maxCCN` 7；`dist/index.js` 10585 → **10535 字节**（10.53 kB）；包 **32696 字节**（SHA-256 `d2226de3b460fcb10f59ebc13abe97ca6cc3d41cb9d0d20a6b6074d5884d810e`）。**口径（明说）**：① 把 ADR-78 已经写下的承诺从「名义」变成「事实」（放开）；② 换的是拆分前提，只影响「URL 里含裸 NUL」这类输入（原先切错键、现在切对）。 | 本次落地；`src/vue.ts`、`src/core.ts`、`src/resource.ts`、`tests/vue.test.ts`、`tests/core.test.ts`、`scripts/check-docs.mjs`、`DESIGN.md`、`README.md`、`统一刷新管理.md`；关联 ADR-78／ADR-79／ADR-80／ADR-81／ADR-84 |
| ADR-86 | 三处「同一件事两处表达」收成一处：删掉 `enqueue` 的死去重（前提交给探针）、安装槽不再现场判死活、presence 判定改问实例 | **采纳并已落地**。确认人：「看起来冗余那就是真冗余 说明数据结构设计的可能有问题，死调用是一定要删除的」。**① 删死调用**：`enqueue` 开头的 `this.dequeue(resource)` 一次也找不到东西——三个调用点都以「实例此刻不在队里」为前提（`refresh` 与 `enqueueDue` 拿 `hasExecution()` 当门，`run` 的收尾时实例在 `running`），而在队实例恒握着 `controller`；删掉后 57 条全绿。前提改由探针接管：`assertQueueConsistent` 补一条「队列里没有重复项」，`enqueue` 的注释写明这条前提。**② 安装槽不再现场判死活**：`installed` 里只会有活着的协调者（`uninstall` 在 `core.dispose()` 之前把槽置空），所以 `current !== null && !current.isDisposed()` 的那半句恒真——收成 `if (current !== null) throw`。「已销毁就能原地接管」的事实是**槽空着**，不是安装时现场判死活；A17 的接管用例仍绿。**③ presence 判定单一化**：`refresh` 的入口闸不再自己重写「激活 ＋ 可见 ＋ 配置有效」，改问实例（`resource.isPresent(config, this.visible)`，该方法随之放开为 public）；核心从此只问实例，规则只有一处表述（ADR-81 ⑧ 把 `isPresent` 收成 `private` 的半条随之翻转，`eligibleEvery` 仍私有）。**④ 顺带**：`createRefreshManager` 里只用一次的 `const store` 内联。**口径（明说）**：三处都是结构收敛，不是语义变更——`refresh` 问的是同一个合取，删掉的判空在既定结构里不可达。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **57**（core 39 ＋ vue 18）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、69 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**；benchmark 中位 640、收敛比 1、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 与 HEAD **逐字节相同**（核心可观测行为零变化）。**度量**：`src/` 795 → **792** 行、结构分支 **79**、函数 70、`maxCCN` **7**；`dist/index.js` 10535 → **10479 字节**（10.48 kB）；包 **32743 字节**（SHA-256 `c44374ab5d9afae9fd56179fbd3f718840139af2cf88e40712d2851f1bd1156`）。 | 本次落地；`src/core.ts`、`src/resource.ts`、`src/vue.ts`、`tests/core.test.ts`、`DESIGN.md`、`README.md`、`统一刷新管理.md`；关联 ADR-44／ADR-62／ADR-81 |
| ADR-87 | 浏览器可见性的持有者从核心移到适配层：核心不再有 `visible`／`setVisible`／`isVisible`，「环境允许」并进每页配置槽 | **采纳并已落地**。确认人：「我们在 vue 适配层已经处理了如 visible 之类的，为啥要在调度里面还存个变量 visible，去更新调度，直接调用调度需要执行的函数不行吗」→ 裁决「改」。**① 事实归学到它的那一方**：`visibilitychange` 的监听一直装在适配层（`createRefreshManager.install`），核心只是被推入一份快照、再被适配层用 `isVisible()` 读回去供自己的读闸门用——DESIGN §6.4 那句「这一项只有核心知道，所以有一个一比特出口」本来就不成立（规范 U04 里「全局可见性由核心持有」一并改写）。现在可见性与「这一页是否激活」由适配层在 `readConfig` 里合成快照的一个位 `present`（环境允许），核心只读快照；核心的三个读点（`refresh` 的入口闸、`isEligible`、`enqueueDue` 的到期判定）问的都是某一页的环境，快照本来就带着。**② 调度边沿不再新开入口**：隐藏／恢复让每页的配置 watcher 醒一次、各自重报（Vue 的响应式天然扇出，`setConfig` 里的 `flushSoon` 把它们合并成一拍）——这就是「直接调用调度要执行的函数」的既有入口；核心因此少了 `setVisible()` 这个只为重排调度而存在的公开方法，`Resource` 的 `isPresent`／`isEligible`／`eligibleEvery`／`dueAt` 各少一个参数。**③ 读闸门第二项读 DOM**：适配层的读闸门是 `isEligible(...) || (lastReadAt === null && !document.hidden)`——这里读 DOM 而不读那个 ref，因为读 ref 会把它登记成读取副作用的依赖，于是「恢复可见那一刻就把表里已有的新版本上屏」会顶掉既有口径（重新成为读者要等下一份写入，ADR-73）。**代价（明说）**：核心内部不再区分「浏览器隐藏」与「这一页失活」——两者的调度与刷新闸行为本来就一致（§3.2 U06），真正需要区分的那一处（读闸门第二项只问可见、不问激活）在适配层，语义不丢；核心级的「隐藏」夹具随之改成「环境不允许」（`tests/core.test.ts`、`scripts/trace-core.mjs` S4、`scripts/benchmark.mjs` 各一处），真 Chrome 的受控 `visibilitychange` 场景照旧覆盖真实路径。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **57**（core 39 ＋ vue 18）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、69 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**（含受控 `visibilitychange`）；benchmark 中位 640、收敛比 1、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 与 HEAD **逐字节相同**（可见性持有者换了，核心可观测行为零变化）。**度量**：`src/` 792 → **785** 行、结构分支 79 → **78**、函数 70、`maxCCN` 7；核心公开面 10 → **8**；`dist/index.js` 10479 → **10319 字节**（10.32 kB）；包 **32820 字节**（SHA-256 `41365e5a7d129001ccb940d79fdedf69db7b9c15a836d647ccc6af1bf2005dc1`）。**口径（明说）**：可见性的持有者从核心移到适配层（同一事实只留一处：适配层的 `visible` ref，并进每页快照的 `present`）。 | 本次落地；`src/core.ts`、`src/resource.ts`、`src/vue.ts`、`tests/core.test.ts`、`scripts/trace-core.mjs`、`scripts/benchmark.mjs`、`scripts/check-docs.mjs`、`examples/main.ts`、`DESIGN.md`、`README.md`、`统一刷新管理.md`；关联 ADR-61／ADR-66／ADR-73 |
| ADR-88 | 补两条用例钉住 `run` 的两处迟到复核（迟到的失败、复制结果期间的重入） | **采纳并已落地**。确认人：「可以补充用例」——回应上一轮报的「两处守卫注入删除后 57 条全绿，守卫必要但没有用例钉住」。**落地**：新增两条核心用例。① `A06/A13 释放之后那次请求才失败：迟到的失败不写进已释放身份的那一格`——释放身份后那次在途请求才 `reject`，断言结果表仍为空、槽位照常交还；**注入验证**：去掉 `catch` 里那次 `!resource.isCurrent(controller)`，这条用例转红（迟到的失败会把已经随实例删掉的那一格写回来）。② `A12/A06 复制结果期间换了执行：旧执行的结果不写进结果表`——响应对象的取值器在 `copyResult`（`structuredClone`）里被读到，那一刻同步 `undeclare` 本页；**注入验证**：去掉复制结果之后那次复核，这条用例转红。规范 §6.4.3 的故障注入表登记这两条。**没写的第三处（明说）**：`await` 之后那次复核在**正确性**上与「复制结果之后」那次不可区分（都在写表之前挡住过期执行），它的作用是「所有权已经丢了就省掉一次复制」（复制会执行调用方响应对象的取值器）；为它写一条「取值器没有被调用」的用例会把实现细节升成契约，故不写，只在代码注释与本节留下记录。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **59**（core 41 ＋ vue 18）全绿；两条新用例各自做过定向注入（去掉对应守卫即转红，恢复即绿）；`pnpm check:docs` 的测试计数与叶子追溯两项同步（19 个叶子、71 条测试标题）；`dist/` 未变（本次只动测试与文档）。**过程说明（明说）**：取证期间工作树里出现另一份**未提交**的 `src/` 改动（`src/index.ts`、`src/vue.ts`），它让 README 度量行的实测值变成 786 行／79 分支——本次提交里的度量行配的是**提交自己的** `src/`（785 行／78 分支），那份改动落定后需自带度量更新；本次交付没有触碰这两份文件。**度量**：`src/` 785 行、78 结构分支、函数 68、`maxCCN` 7（均不变）；测试文件 `tests/core.test.ts` 的行数 1134 → **1178**；`dist/index.js` **10319 字节**（不变）；包 **32872 字节**（SHA-256 `734ea476cbf62541aa97a5abad69d01615d16c0fc19064ef778afe3321544703`）。 | 本次落地；`tests/core.test.ts`、`README.md`、`统一刷新管理.md`；关联 ADR-84／ADR-87 |
| ADR-89 | 读取面的身份边界：同一版去重按身份划界、发布当中换身份必须重订依赖（外部审查 F2／F4） | **采纳并已落地**。外部审查（基线 `e14d0ff`）报出 F2／F4 两个 P1，我在当前 HEAD 用独立探针复现后确认，确认人裁决「1-4 直接做」。**① F2**：两个从未成功的格 `updatedAt` 都是 `null`，旧的「同一版」只比时间戳，于是换身份后新身份的 `args` 被挡在画面外（实测：提交 A 首查失败→提交 B 首查失败，`display.args.id` 仍停在 `A`）。落地：数据出口与失败出口各记「这一版属于哪个身份键」（`shownKey`／`failedKey`），**同一身份同一版**才不抄第二遍；ADR-85 的毫秒口径不动（同一身份同一毫秒仍然合并）。**② F4**：`watchEffect` 正在运行时，它自己触发的变更不会唤醒它——页面若在同步 watcher（失败出口或数据出口）里换身份，读取面就**永久**订在旧身份那一格（实测：`every=1` 排除窗口干扰后，请求体早已是 B、连发 50+ 次，画面永远停在 `A`）。落地：发布路径上复核「本轮快照是否仍然有效」（失效即不再把旧身份的结果写进画面），并用一次微任务 `triggerRef(submitted)` 强制唤醒读取面，让新身份那一格的依赖重新订上。**验收**：新增 4 条用例（换身份后首查又失败、failure watcher 里换身份、display watcher 里换身份、发布当中卸载），并逐条做过定向注入——去掉身份划界，F2 那条红；去掉复核与重订，三条 F4 用例红。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **64**（core 41 ＋ vue 23）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、76 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**；benchmark 中位 636、收敛比 0.99、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 与本批之前**逐字节相同**（只动适配层，核心未动）。**度量**：`src/` 785 → **802** 行、结构分支 78 → **82**、函数 70、`maxCCN` 7；`dist/index.js` 10410 → **10658 字节**（10.66 kB）；包 **33652 字节**（SHA-256 `4ed823f30b7529fcf2a134022a945724989a7d7bdb601efa2afdfc07435e39ee`）。 | 本次落地；`src/vue.ts`、`tests/vue.test.ts`、`DESIGN.md`、`README.md`、`统一刷新管理.md`；关联 ADR-67／ADR-72／ADR-73／ADR-85 |
| ADR-90 | 结果表的储存随活跃身份收敛：释放连格一起删、观测面补「实际格数」（外部审查 F6） | **采纳并已落地**。外部审查（基线 `e14d0ff`）报 F6：`read` 会经 `refOf` 建格、`remove` 只把 `ref.value` 置 `undefined`，于是 `cells` 里的键与 ref 只增不删——我在当前 HEAD 复现：1000 轮 `read`+`remove` 后内部 Map **写入 1000 个身份键、删除 0 个**，而 `list()` 跳过 undefined 格所以看起来是 0。**落地**：① `remove` 改成 `cells.delete(identityOf(url, key))`（连 ref 一起删）；② store 增 `size()`，基准的残留行加 `cells`（它因此自动进入「释放后残留必须为 0」的契约检查）；③ 新增用例「换身份不留格 + 同一个身份重建仍唤得醒 + 最后声明者离开后格数收敛到 0」，并做定向注入（`remove` 退回置 `undefined` → 该用例红）。**边界（明说）**：删格意味着「释放之后还订阅着旧 ref 的人不再被唤醒」——可达路径只有「这一页已释放或已换身份」，读取面要么已经重订、要么随作用域停止，「同 key 重建」用例正是钉这一点的；`read` 仍会在被声明的身份上建空格（这是「第一次写入唤得醒订阅」的前提），所以格数的稳态是**活跃身份数**而不是 0。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **65**（core 41 ＋ vue 24）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、77 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**；benchmark 中位 633、收敛比 0.99、在途峰值 4、释放后残留全 0（含 `cells: 0`）；`node scripts/trace-core.mjs` 与本批之前**逐字节相同**（核心未动）。**度量**：`src/` 802 → **805** 行、结构分支 82 → **81**（`remove` 的 `if (ref)` 随之消失）、函数 70 → **71**、`maxCCN` 7；`dist/index.js` 10658 → **10647 字节**（10.65 kB）；包 **33891 字节**（SHA-256 `4dc07d1ed058db3334afdafe79b75cbaf3f76bb32edab5bf7ff029b6383c9009`）。 | 本次落地；`src/store.ts`、`tests/vue.test.ts`、`scripts/benchmark.mjs`、`DESIGN.md`、`README.md`；关联 ADR-59／ADR-79 |
| ADR-91 | 读取基准只在「资格从无到有」那一条边上重置：改频率、改可见性只重排调度（外部审查 F5／C1） | **采纳并已落地**。外部审查报 F5（改 `every` 被当成「重新成为读者」，读取窗口被白白放行一次）与 C1（G5／U06／A06 说恢复时读回已有结果，ADR-73 说等下一份写入）。确认人的口径：「失活后恢复画面本来就有值（keep-last），不做主动读回；同一份配置内的变化只是重排调度，读取基准要保留」。**落地**：配置/生命周期 watcher 里先按**旧**配置问一次资格（本页槽的唯一写入口就在下一行，核心此刻手里还是旧值），写完再问一次；只有 `false → true` 那一条边才把 `lastReadAt` 置空。同一份配置内改频率、改可见性因此不再放行窗口；重新成为读者（暂停→开启、失活→激活、隐藏→显示）仍按 U12 放行一次。**C1 按 ADR-73 收口**：不做主动读回（画面本来就保持着最后一帧），并把 G5／U06／A06／§2.5 的措辞改成「画面保持已有结果 + 表里已有更新时下一份写入才上屏」。**验收**：新增用例「改频率只重排调度、不放行窗口」，并做定向注入（把判定退回「只要有资格就清基准」→ 该用例红；既有「重新成为读者」用例仍绿）。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **66**（core 41 ＋ vue 25）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、78 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**；benchmark 中位 633、收敛比 0.99、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 与本批之前**逐字节相同**（核心未动）。**度量**：`src/` 805 → **808** 行、结构分支 81 → **80**、函数 71、`maxCCN` 7；`dist/index.js` 10647 → **10701 字节**（10.70 kB）；包 **34175 字节**（SHA-256 `69d4f2a48e3cb8476892868dd8c6d0148b3588579928780d6a5180828af139c1`）。 | 本次落地；`src/vue.ts`、`tests/vue.test.ts`、`DESIGN.md`、`README.md`、`统一刷新管理.md`；关联 ADR-72／ADR-73／ADR-87／ADR-89 |
| ADR-92 | 低风险精简：三处死代码、配置 watcher 不再重读一次 refs、到期扫描不再复制 Map 视图，并把 `noUnusedLocals` 纳入 `pnpm typecheck`（外部审查 §7／§8） | **采纳并已落地**。① 删 `src/source.ts` 未使用的类型导入 `ReadonlySnapshot`（编译器确认）；② 删 tests 里同样由编译器确认的两处：`tests/core.test.ts` 的 `micro`、`tests/vue.test.ts` 的 `RefreshOptions`；③ `tsconfig.json` 打开 `noUnusedLocals`／`noUnusedParameters`——这类死代码此后是 `pnpm typecheck` 的失败，而不是等外部审查来发现；④ 删掉 `reportConfig` 这一层：watch 的取值函数改成返回具名对象（`core` ＋ `enabled`／`every`／`present`），回调直接用当轮新值——少一个函数、少一次重复读取 `options` 的 refs，两个 `unknown` 的顺序仍然写在名字里而不是位置里；⑤ `enqueueDue` 直接迭代 `identities.values()`（那一趟扫描不跑外部代码，Map 迭代容忍删除），`dispose` 那趟**保留**快照（它的 `sink.remove` 会同步唤醒页面 watcher，评审这一点是对的）。**不做**（评审列为可选、只是表达风格）：合并 `parameterKey` 的异常转换、内联 `isWanted`／`hasExecution`、把 `wakeup` 的取消闭包换成裸句柄、把 `splitIdentity`／`list` 再往外迁。**验证**：`pnpm typecheck` 0（且此后开着 `noUnusedLocals`／`noUnusedParameters`）；`pnpm test` 用例总数 **66**（core 41 ＋ vue 25）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、78 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**；benchmark 中位 640、收敛比 1、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 与本批之前**逐字节相同**。**度量**：`src/` 808 → **809** 行、结构分支 **80**（不变）、函数 71 → **70**、`maxCCN` 7；`dist/index.js` 10701 → **10705 字节**（10.71 kB）；包 **34311 字节**（SHA-256 `259efa06093cef053ff5f3fbe0b6c1ad3d4a241cdca18bba0792e43a6904c2de`）。 | 本次落地；`src/source.ts`、`src/vue.ts`、`src/core.ts`、`tests/core.test.ts`、`tests/vue.test.ts`、`tsconfig.json`、`DESIGN.md`、`README.md`；关联 ADR-80／ADR-81／ADR-84 |
| ADR-93 | 文档按最新口径修订：F3 收口为单活跃 SPA、五处措辞与当前结构对齐 | **采纳并已落地**。确认人对 F3 的口径是「单 spa」，并要求「文档必须修订为最新」。**① F3 按单活跃 SPA 收口**：不恢复「安装所属 App」字段（ADR-81 ⑤ 的删除保持），改的是**规范表述**——G10／§2.3／U17／A17／U02 与 DESIGN §6.3 不再声称「每个 App 独立、拒绝跨 App」，改成「一个进程只允许一个活跃协调者；**不记录安装所属 App**，跨 App 复用同一个实例不在检出范围」。**② 五处措辞对齐当前结构**：G2／§2.2／A02 与 `public-types.ts` 的「框架交付的每个值都是独立副本」→「`args` 是每页一份副本、`data` 是结果表里那一份共享对象」（U11 的口径）；U16 与 DESIGN §6.4 的「框架不运行任何调用方代码」→「**没有业务回调接口**」，并写明写结果表会同步唤醒页面副作用、取值器与传输本身也是调用方代码；DESIGN 里残留的「两级结果表」描述（模块清单／职责表／对象关系图／§9.2）与两处已删的 `isVisible` 引用改成当前结构（一张复合键 Map、核心只回答 `isEligible`）；`RefreshDisplay` 的「实例被释放时读回 `null`」→「本页释放后不再更新（保留最后一帧）、协调者退场时清回 `null`」。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **66**（core 41 ＋ vue 25）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、78 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**；benchmark 复测中位 633、收敛比 0.99、在途峰值 4、释放后残留全 0（本批不改行为，数字只是复测）；`trace-core` 正常。**度量**：`src/` 809 → **810** 行（只动 `public-types.ts` 的注释）、结构分支 **80**、函数 70、`maxCCN` 7；`dist/index.js` **10705 字节**（注释被剥离，产物不变）；包 **34345 字节**（SHA-256 `96b758c51c650d0a0341cdc987bc731d6642ae3e99ff3d24ce6f56b98b257ac4`）。 | 本次落地；`统一刷新管理.md`、`DESIGN.md`、`src/public-types.ts`；关联 ADR-46／ADR-59／ADR-81／ADR-87／ADR-90 |
| ADR-94 | 口径订正：重新成为读者**立刻读回该身份当前那一版**（不是失活前那一帧） | **采纳并已落地，订正 ADR-91／ADR-93 对 C1 的读法**。确认人澄清：「读取面恢复，不管是可见性恢复还是失活恢复，读取回来的都是最新后台更新后的数据，也就是 resource 对应的最新数据，不是失活前的数据」。**落地**：配置/生命周期 watcher 在「资格从无到有」那一条边上，除把 `lastReadAt` 置空，还**唤醒读取面一次**（微任务 ＋ `triggerRef(submitted)`，与 ADR-89 的「换身份重订」同一机制）——于是它在那一拍就按当前格发布**最新那一版**（不等窗口、不重新取数）；同一份配置内的变化（改频率、改可见性）仍然只重排调度（ADR-91 的 F5 修复保持）。**C1 因此按 U06／G5／A06 的原文口径收口（恢复读回当前那一版）**，ADR-91／93 里「等下一份写入」的表述作废并已改回。**验收**：改写既有用例为 `A06 重新成为读者立即读回该身份当前那一版：不重新取数，此后按窗口跟随`（断言恢复到最新版、请求数不增、此后按窗口跟随），并做定向注入（去掉那条边上的唤醒 → 该用例红）。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **66**（core 41 ＋ vue 25）全绿（连跑三次稳定）；`pnpm check:docs` 全项通过（7 模块、19 叶子、78 条测试标题）；`build`／`build:demo` 通过；真 Chrome **12/12**；benchmark 中位 633、收敛比 0.99、在途峰值 4、释放后残留全 0；`trace-core` 正常（核心未动）。**度量**：`src/` 810 → **814** 行、结构分支 **80**、函数 70、`maxCCN` 7；`dist/index.js` 10705 → **10710 字节**（10.71 kB）；包 **34414 字节**（SHA-256 `b6d490f34cfbcd4e02d991c5bac9aa160382a10d30f6fdd00725506eebc8d20b`）。 | 本次落地；`src/vue.ts`、`tests/vue.test.ts`、`DESIGN.md`、`README.md`、`统一刷新管理.md`；关联 ADR-73／ADR-89／ADR-91／ADR-93 |
| ADR-95 | 需求文本按最新口径对齐（14 处）＋补两条判据证据：独立审查抓到我漏掉的 4 条真残留 | **采纳并已落地**。确认人：「做一个产品经理改做的事情，重新梳理需求 找到冲突点」。做法：① 我按 PM 口径重排需求结构（L0 根承诺 G → L1 行为条文 U → L2 验收叶子 A → L3 设计 → L4 决策）并逐条找冲突；② **并行开一个独立只读审查**（同一份规范、独立上下文）对冲我的盲区——它抓到我漏掉的 4 条真残留，我逐条复核后合并。**本轮落地（全是文本对齐，除注明者不改行为）**：**(a) 我上一轮自己改坏/漏改的**：§2.5 里残留的「重新成为读者要等下一份写入」（ADR-94 只改了同段另一处）→ 改成「watcher 主动唤醒读取面一次，读的是表里当前那一版」；DESIGN「三处「没有读取时间就直接读」」整段（被我改成半句＋旧尾巴并存）→ 重写为自洽一段；DESIGN §9.3 的 `useRefresh` 行补「重新成为读者那处还唤醒一次」、`Config` 行 `{enabled,every,active}` → `present`（ADR-87 漏改）。**(b) 真冲突**：U06「失去资格期间才点的 refresh 不放行」→ 限定「**失活或隐藏**期间」（暂停不属于：U05／U14／§2.4／A06 与实现都允许暂停页刷新一次）；U12「失去读者身份后不再跟随新值」补两处已裁定的例外；G4「不再发新请求」→「不再**新增**取数（已入队的那次照常走完，U06）」，**口径不变**；§0.2「一个 App 一个实例」→「一个进程一个活跃实例」；§2.3「同实例同 App」→「同实例」；§2.2「别名安全由每个消费者各一份副本保证」→「`args` 每页一份副本、`data` 共享」；§6.4.3 那条故障注入的错误理由（说失败靠数据出口进画面）→ 改成失败有自己的出口。**(c) README**：「有执行就用这一轮的结果」补回第三支；删掉「靠同一版判定进画面」的旧说法；「只监听这两项配置」补「另外跟随安装槽、生命周期与可见性」。**(d) 术语与追溯**：「失去存在」→「本页已释放」；§0.2 增「读者」词条；A21 判据补「重新成为读者立即读回」；U17 补「未安装协调者时 `useRefresh` 抛错」（A17 的行为此前没有 §3 落点）；G10 标题手滑错词修正。**(e) 两条新用例**：`A11/A12 同一身份同一版不抄第二遍`（注入：去掉去重 → 红）、`A06/A09 失去资格只挡新入队：已经排上的那次取数照常走完`（注入：把失去资格的排队项丢掉 → 红，那正是「收窄」那条备选路）。**没有动（留给你）**：① §1／§2／§4／README 里的行为细节是否收敛成指针（B1：这正是 C1 类冲突的根因，规则在 ~10 处各写一遍）；② G9／U10 两个编号的去向（全库无交代，我不编）。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **68**（core 42 ＋ vue 26）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、80 条测试标题、20 行词汇）；真 Chrome **12/12**；benchmark 复测中位 640、收敛比 1、在途峰值 4、释放后残留全 0（本次不改行为）。**度量**：`src/` **814** 行、结构分支 **80**、函数 70、`maxCCN` 7（源码未动）；`dist/index.js` **10710 字节**（不变）；包 **34472 字节**（SHA-256 `dee9450cbf18ef35c9c4fc85fa4c244346a2a0c2f0126f96c6fd484757fc6dfb`）。 | 本次落地；`统一刷新管理.md`、`DESIGN.md`、`README.md`、`tests/vue.test.ts`、`tests/core.test.ts`；关联 ADR-77／ADR-91／ADR-93／ADR-94 |
| ADR-96 | 概念统一定死：§0 重写成唯一词表（31 个概念）、退役四个旧词、§3 关键条文改正向表述，并加两条门禁 | **采纳并已落地**。确认人：「到处都写 概念没有理清楚 也没有固定清楚，大部分都是不要怎样而不是应该怎样……把必须的概念先统一定死」。**① §0 从两张「名称｜含义」表改成七节唯一词表**：0.1 怎么用（一个概念一个词）／0.2 调用方给的三项输入（声明、开启意愿、周期）／0.3 环境给的两个事实（激活、浏览器可见）与合成（环境允许）／0.4 归属与取数（身份、已准备参数、共享实例、已声明身份、资格、一次取数、排队、在途、结算、到期、成功、失败）／0.5 结果与读取面（结果表、格、出口、发布、读取基准、窗口、同一版、读者、冻结）／0.6 命令与生命周期（刷新命令、释放、回收、销毁）／0.7 用词纪律。每行四列：**定义（正向）｜它不是什么｜持有者｜条文**；31 行由门禁逐行核对（词必须在 §0 之外出现且不重复）。**② 退役四个旧词**：需求→声明；订阅→按语境写声明／资格／读者；交付→发布／读取面；失去存在→已释放（资格语境里的「存活」一并删除）。规范 §0–§4 的正文与 README 已清空这些词（现在只在 §0 的「它不是什么」列、§4 引用的测试标题、§5–§7 的历史与非规范区、以及 DESIGN 里两处明确讲「已删除的交付回调」的地方出现）。**③ §3 关键条文改正向**：U05（暂停＝意愿为假：声明保留、画面按读者规则冻结、刷新命令仍受理）、U06（环境不允许：声明保留、按读者退下、期间命令不产生事实、重新成为读者立即读回、资格只在入队时判定、最后声明者离开才回收）、U12（读者按窗口跟随、同一身份同一版不发布第二遍、三处不等窗口）；R2／R3 标题改成「声明与调度」「读取面与失败」，§2.5／§4.4 同步。**④ 门禁加两条**：第 17 组退役词表补这四个词（消息改成中性措辞，因为它现在也管用词退役）；**新增第 18 组：§3 的正文不得出现任何退役词**（带不带反引号都算）——「一个概念一个词」从此有机器守着。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **68**（core 42 ＋ vue 26）全绿（本次未改用例）；`pnpm check:docs` 全项通过（**31 行词汇**、7 模块、19 叶子、80 条测试标题）；`pnpm build` 通过且 `dist/index.js` 不变（源码未动）。**度量**：`src/` 814 行、结构分支 80、函数 70、`maxCCN` 7（均不变）；`dist/index.js` **10710 字节**；包 **34471 字节**（SHA-256 `b2778d347b58b662acabf01d883fbcc0b8988c19fc40d7abada761d02880035b`）。**下一批（未做）**：§1／§2／§4／README 里剩下的「不…」式表述改成正向；B1 的「复述→指针」收敛。 | 本次落地；`统一刷新管理.md`、`README.md`、`DESIGN.md`、`scripts/check-docs.mjs`；关联 ADR-61／ADR-77／ADR-93／ADR-94／ADR-95 |
| ADR-97 | 需求正文的否定式表述改正向（§1 承诺表、§2.4 契约表、README 规则句），并把 §0.7 的表述纪律改成可执行标准 | **采纳并已落地**（承接 ADR-96 的用词纪律；确认人：「大部分都是不要怎样而不是应该怎样」）。**做法**：把**规则本身**写成「什么条件下发生什么」，禁止性说法只保留在**上界与不可逆**两处（超限、重复、复活、迟到写入），并且与它成立的条件成对出现；§4 的判据与证据可以只写可观察结果（判据本来就是「没有发生什么」的断言）。**落地**：① §1 承诺表 7 行改正向——G1（表单草稿不进框架）、G3（只在到期时取数／在途不超上限）、G4（取数只发生在有资格时，失去资格只挡住新的入队）、G5（重新成为读者立即读回当前那一版）、G7（失败写进那一格、成功数据与时间原样保留、开启意愿由调用方掌握）、G10（安装以槽位为界，跨 App 复用不在检出范围）、G11（同一身份同一版只发布一次）；② §2.4 的 `submit` 与 `refresh` 入口两行改成条件式（入口闸「放行四件事同时成立的命令」）；③ README 四处规则句改正向（关闭意愿＝仍然是声明者；失败有自己的出口；失活或隐藏期间刷新不产生事实）；④ §0.7 的表述纪律改成上面的可执行标准。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **68**（core 42 ＋ vue 26）全绿（未改用例）；`pnpm check:docs` 全项通过（31 行词汇、19 叶子、80 条测试标题）。**度量**：源码未动（`src/` 814 行、结构分支 80、`maxCCN` 7）；`dist/index.js` **10710 字节**（不变）；包 **34512 字节**（SHA-256 `00e5c65d4b16e420e81d2575909dcd9071c713d68789cd6796be68c7b69a9b6d`）。**下一批（未做）**：§3 的 22 行、§4 的 9 行「禁止性说法是否与条件成对」逐个巡视；B1 的「§1／§2／§4 复述 → 指针」收敛。 | 本次落地；`统一刷新管理.md`、`README.md`；关联 ADR-96 |
| ADR-98 | 文档瘦身：历次交付记录从 §5 G04 行迁出为「交付记录.md」、§2.5 的机制复述收成指针 | **采纳并已落地**。确认人：「一次性修订 把概念统一 口径一致 整体整理文档 现在的需求太长太臃肿并且内部很多地方不一致」。**落地**：① §5 G04 行里累积的历次交付记录（6 字符级）逐字迁到新增的 `交付记录.md`，§5 只留决策与指针——统一文档 **184263 → 61765 字节（−66%）**；② 所有「包 SHA-256 见 §5 G04」的引用改指 `交付记录.md`；③ §2.5 的两处机制复述（读者/窗口判定、三处不等窗口）收成指向 §3 U12 的指针，失败句改正向。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **68**（core 42 ＋ vue 26）全绿；`pnpm check:docs` 全项通过（31 行词汇、19 叶子、80 条测试标题）。**度量**：源码未动（`src/` 814 行、结构分支 80、`maxCCN` 7）；`dist/index.js` **10710 字节**；包 **34566 字节**（SHA-256 `4c25c0bf8def4f67a9ec6e713013c0042d88a0a4f769abcb05c8c972562c8b36`）。**下一批**：§2／§4 剩余机制复述继续收指针（B1）。 | 本次落地；`统一刷新管理.md`、`交付记录.md`（新）、`README.md`；关联 ADR-95／ADR-96／ADR-97 |
| ADR-99 | 文档整理收尾：§2.1 机制复述收指针、§3 孤立否定补齐正向条件、§4 四行「必须结果」收成判据 | **采纳并已落地**（ADR-96→99 一次性修订的最后一批）。① §2.1 的「改任一项都按新配置重新协调（暂停页仍可刷新一次；改频率立刻生效…）」收成指向 §3 U05／U07／U12 的指针；② §3 唯一的**孤立否定**（U09「不绕过并发上限；满槽不自旋」）补成正向「在途数始终不超过上限；满槽时等真实结束再继续」；其余禁止性说法逐行核对后确认属于 §0.7 允许的**上界/不可逆**（幂等、至多一个执行、资格只在入队时判定、下个周期继续、用这一轮结果）且与正向条件成对，因此保留；③ §4 的 A05／A06／A12／A21 四行把「必须结果」里的机制复述收成判据 ＋ 指向 U05／U06／U12 的指针（证据列原样保留）。**结果**：统一文档 **184263 → 60941 字节（−67%）**——规则只住在 §3（由第 18 组门禁守着），§2 只写契约，§4 只写判据，历史在 `交付记录.md`。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **68**（core 42 ＋ vue 26）全绿；`pnpm check:docs` 全项通过（31 行词汇、7 模块、19 叶子、80 条测试标题）。**度量**：源码未动（`src/` 814 行、结构分支 80、`maxCCN` 7）；`dist/index.js` **10710 字节**；包 **34566 字节**（SHA-256 `4c25c0bf8def4f67a9ec6e713013c0042d88a0a4f769abcb05c8c972562c8b36`）。 | 本次落地；`统一刷新管理.md`、`交付记录.md`；关联 ADR-96／ADR-97／ADR-98 |
| ADR-100 | 交代两个退役编号（G9、U10）的去向，并让门禁认「退役锚点」 | **采纳并已落地**。确认人：「你补充以下」。**取证（git 历史 ＋ ADR 行）**：`G9`＝「非组件也能读：readSnapshot 返回独立副本、不创建实例、不保活（A15）」，由 5606957（ADR-46，确认人裁决「直接删，refresh 改 void」）删掉；`U10`＝「框架上限：从任务真正开始执行起算（排队不计入）的一次性上限（A10）」，由 6ee1bba（ADR-65，确认人裁决「砍框架取数上限」）删掉——两条都有叶子一起退役，只是本文档从没写过它们的去向。**落地**：① §1 承诺表下补四条说明（编号是稳定标签、不复用不重排；G9／U10 各自的原文、对应叶子与退役依据）；② `scripts/check-docs.mjs` 第 7 组新增 `RETIRED_ANCHORS = new Set(["U10"])`：**退役锚点可以在正文里被提及**（正文必须说明它去哪了），但不作为落点——与 `RETIRED` 词表同一纪律（都写在一处、加一个就要同时交代退役）。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **68**（core 42 ＋ vue 26）全绿；`pnpm check:docs` 全项通过（31 行词汇、19 叶子、80 条测试标题）。**度量**：源码未动；`dist/index.js` **10710 字节**；统一文档 **61718 字节**；包 **34566 字节**（SHA-256 `4c25c0bf8def4f67a9ec6e713013c0042d88a0a4f769abcb05c8c972562c8b36`）。 | 本次落地；`统一刷新管理.md`、`scripts/check-docs.mjs`、`交付记录.md` |
| ADR-101 | 需求文档按标准形态重排：§1 改成「背景与范围」（场景／现状与问题／目标／承诺）、§0 补对象链、§3 六个能力域各补「场景」，门禁不再写死章名 | **采纳并已落地**。确认人：「你是个标准的需求文档，你是高级产品经理，你描述一个需求应该描述 背景 概念 具体需求这些」。**落地**：① **§1 背景与范围**——1.1 场景（报表/行情类 SPA：同一 URL ＋ 同一组参数被多个组件同时需要；四个业务要求：按页面自己的间隔自动更新／随时手刷／切页签或隐藏或暂停时不取数也不清画面／失败不毁画面并按周期再试）、1.2 现状与问题（P1 重复请求、P2 停不干净、P3 旧响应覆盖、P4 失败毁画面、P5 隐藏仍取数、P6 各处副本不一致，逐条给框架的答案）、1.3 目标、1.4 承诺（原 G 表保留）；章首加阅读路径；② §0.1 补对象链一句话（组件→配置槽→身份→共享实例→一次取数→结算→结果表格→两个出口；三个判定：资格／读者／窗口）；③ §3 的 R1–R6 各补一行**场景**（业务语言，先讲为什么存在，再进机制与条文与判定）；④ 门禁第 13/14 组的 §0 与正文边界从写死 `## 1. 目标与范围` 改成按编号 `\n## 1. ` 找——**改章名不会再误判**。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **68** 全绿；`pnpm check:docs` 全项通过（31 行词汇、19 叶子、80 条测试标题）。**度量**：统一文档 **64598 字节**（+2658）；源码未动（`src/` 814 行、结构分支 80、`maxCCN` 7）；`dist/index.js` **10710 字节**；包 **34575 字节**（SHA-256 `12e792eba0c2290213e1ec745017ca63ce33cca171cd7cdeb9a268ecae5839b9`）。 | 本次落地；`统一刷新管理.md`、`README.md`、`scripts/check-docs.mjs`、`交付记录.md` |
| ADR-102 | §2 收成纯接口形态：契约表只留「接口形状／取值域／返回值／错误」，行为一律指回 §3 | **采纳并已落地**（承接 ADR-101）。① §2.4 的五行「返回值与错误契约」重写——`submit` 只列 `accepted`／`rejected`／`cancelled` 各自意味着什么，`refresh` 三行只写「无返回值／无回执」与两个外发出口，共享请求失败只写它写进哪一格，规则一律指向 §3（U01–U03／U11–U14／U13）；② §2.5 的两处「什么都不发生」复述收成指针（入口闸 → U14）。**目的**：把三件事分家——**§2 只讲接口**、**§3 只讲行为（唯一有规范效力）**、**§4 只讲判据**，这样「到处都写」的复述不再产生新的口径冲突。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **68** 全绿；`pnpm check:docs` 全项通过（§2.2 镜像与源码一致、导出面清单一致、2 条主题引用仍在、31 行词汇、19 叶子、80 条测试标题）。**度量**：§2 6984 → **6689 字符**；统一文档 **63699 字节**；源码未动（`src/` 814 行、结构分支 80、`maxCCN` 7）；`dist/index.js` **10710 字节**；包 **34575 字节**（SHA-256 `12e792eba0c2290213e1ec745017ca63ce33cca171cd7cdeb9a268ecae5839b9`）。 | 本次落地；`统一刷新管理.md`、`交付记录.md`；关联 ADR-101 |
| ADR-103 | 删掉两份外部评审文档；DESIGN 逐节追平当前 §0／§3 口径（字段 `present`、结果表删格、资格四组输入、认人次数、读表链路） | **采纳并已落地**。确认人：「评审文档直接删除 design重新检查」。**① 删文档**：`CODE_REVIEW.md`（总结构审查）与 `统一刷新管理-评审.md`（逐符号清单）是外部一次性审查的产物（基线 `e14d0ff`），它们提出的每一条都已有裁决与落地记录（F2／F4／F5／F6 → ADR-89／ADR-90／ADR-91，F3 → ADR-93，C1 → ADR-94）；留在仓库里就是**第二份无人维护、且与当前口径冲突的「事实来源」**。删除不丢决策——判断在 ADR 行、行为在 §3、判据在 §4；ADR 历史行里对这两份文件的引用**不重写**（历史照旧），全仓检索确认除这 10 处历史引用外无人再引用。**② DESIGN 复核共 33 行**（只是写法跟不上改名与改判，没有新设计）：(a) 配置槽第三个字段 `active` → `present`（ADR-87 的改名在 §2.1 调用链、§3.4、§6.1（2 处）、§6.2、§9.2 没跟上；`active` 从此只指适配层自己的 `shallowRef`）；(b) 结果表释放语义按 ADR-90 更正：旧文写「只把格置 `undefined`、cell ref 不删，依赖关系才稳定」→「**连 ref 一起删**（`cells.delete`），键数随活跃身份收敛；同身份重建时 `read` 建新 ref、重写照样唤得醒」；(c) 已删符号：§9.1 的 `RefreshCore.setVisible` → 适配层的 `visible`，`scripts/observe.ts` 的注释把「可见性」从核心的外部动作里删掉（核心 8 个公开面里没有它）；(d) 资格一律按 §0 的**四组输入**写——§3.1／§3.7／§3.8／§9.2 原先把「环境允许」拆成「这一页激活 ＋ 浏览器可见」、又把「开启意愿」与「周期有效」并成一项，分组与 §0 对不上；§3.5 第 1 条的资格公式补上缺失的一项（原文只写 `config.enabled && config.present`，漏了 `isPresent` 里的 `every !== null`，即「周期有效」）；(e) 读表链路写清：适配层读取面 → 核心 `readResult` → `sink.read`（模块表与 §2.1 原来写 `store.read`，跳过了核心那一跳）；(f) 去含糊措辞：「存活状态的唯一公开出口」→「**协调者**存活状态的唯一公开出口」，§5.3「回来就有」→「重新激活或恢复可见能读回旧结果」；全篇两处「内核」统一成规格用的「核心」；(g) §3.4／§3.6／§6.2 的「失去资格（隐藏／失活／暂停）」等写法改成 §0 的定名「**意愿关闭或环境不允许**」（「失去资格」不是 §0 的词，而这三处的宾语本来就只是环境那一侧）；(h) §4.1 的过度断言「**框架不跑任何调用方代码**」→「**框架没有业务回调接口**」——ADR-93 只改了 §6.4 与 `U16`，这一节漏了（写结果表会同步唤醒页面副作用，取值器与传输本身也是调用方代码）。**②′ 第二遍独立只读复核（子代理，只读、逐条给证据）又坐实 9 处**，全部按证据改掉：(i) §9.3 的 `Resource` 一排写「`isPresent` 与 `isEligible` 由核心问它、另两个留在实例内部」——`dueAt` 也是核心调的（`enqueueDue` 里的 `resource.dueAt(now)`），四个里只有 `eligibleEvery` 是 `private`；(j) §9.3 的 `useRefreshStore` 一行「`read`（页面读出口）」→「核心读取面 `readResult` 的底层；页面的读出口是 `display` / `failure`」（与刚刚改好的模块表／§2.1 自相矛盾）；(k) `run` 的认人次数：成功路径是**复制结果前后各一次**（复制要读属性、取值器可能同步重入），失败路径进 `catch` 时再一次——§3.9 第三条与 §5.3 原来把「复制结果之后」与「写表之前」数成两个时刻（同一个检查数了两遍）；(l) 「结果时间与调度同一个墙钟」那段写「`dueAt` 与 `setWakeup` 都按 `Date.now()` 现算」——`dueAt` 收外部读数、不自己读钟（§5.1 已明说）；(m) 结果表那一行补上 `size`（ADR-90 加的观测出口，§9.3 已列、这里漏了）；(n) §6.1 写 `present` 是适配层的 `shallowRef`——它是每页 `active` 与模块级 `visible` 两个 `shallowRef` 现算出的布尔值；(o) §6.3 写 `dispose` 清理「配置槽」——配置槽归页面，核心只清队列／Timer 与各实例的 `declarers`；(p) §6.2 删掉「实例已销毁则重建并首查」——没有这条路径（重新成为读者只读回结果表那一版并重排调度，身份登记只能由调用方 `submit`）；(q) 两处「§2.4」标明是[统一刷新管理.md](./统一刷新管理.md) 的 §2.4（DESIGN 自己的 §2 只有 §2.1）。**没改的（明说）**：ADR-94 的三处「不等窗口」清单、ADR-77 的用词「不参与数据窗口」（§3 `U13` 原文即此）、以及讲「已删除」的历史句（`交付`／`setCleanup`／交付回调）照旧保留。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **68** 全绿（core 42 ＋ vue 26）；`pnpm check:docs` 全项通过（7 模块、31 行词汇、19 叶子、80 条测试标题）；`pnpm build`／`build:demo` 通过；真 Chrome **12/12**；`node scripts/trace-core.mjs` 与基线逐字节相同（本条不动 `src/`）；benchmark 中位 634 次、收敛比 0.99、在途峰值 4、释放后残留全 0（含 `cells: 0`）。**度量（逐字节不变，明说）**：`src/` **814** 行、`maxCCN` 7；`dist/index.js` **10710 字节**；包 **34575 字节**、SHA-256 `12e792eba0c2290213e1ec745017ca63ce33cca171cd7cdeb9a268ecae5839b9`——与 ADR-102 之后**逐字节相同**（只动文档与一处注释，`files` 只含 `dist`）；`DESIGN.md` 530 行。 | 本次落地；`CODE_REVIEW.md`（删除）、`统一刷新管理-评审.md`（删除）、`DESIGN.md`、`scripts/observe.ts`、`ADR.md`、`交付记录.md` |
| ADR-104 | 补删最后一份评审文档；README 三处口径对齐（漏改的 `active`、资格四组输入） | **采纳并已落地**。确认人：「怎么还有评审文档没有删除」。**① 补删 `统一刷新-评审.md`**（28792 字节，2026-09-16，基线 `e61e7d8`）：ADR-103 只删了 `CODE_REVIEW.md` 与 `统一刷新管理-评审.md`，漏掉同一类的第三份——它本身就是「重新取证、不继承前一份结论」的又一轮外部评审，方向（订阅＋主动刷新的收敛）已由 ADR-16／17／18 与后续架构收口记录在案；删掉后 ADR-16／17／18 的历史引用照旧（历史不重写）。**② README 三处口径**（ADR-103 只查了 DESIGN，README 漏在外面）：`README.md:25` 的配置快照字段仍写 `active` —— ADR-87 早已把它改名 `present`（＝环境允许），这门禁拦不住（见下）；`README.md:27` 的「资格（开启 ＋ 激活 ＋ 浏览器可见）」与 `:101` 的「声明着它、配置有效、开启、激活且浏览器可见」与 §0 的四组输入分组对不上，改成 §0 的定名「声明还在 ＋ 环境允许 ＋ 开启意愿 ＋ 周期有效」。**门禁为什么没拦住（明说）**：`check-docs` 的符号检查只认反引号包着的 PascalCase，小写字段名（`active`）不在覆盖内，所以文档里的字段名漂移只能靠人／评审发现；本轮**不新增**门禁规则——那是独立的一件事，要单独裁决。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **68** 全绿（core 42 ＋ vue 26）；`pnpm check:docs` 全项通过；`pnpm build`／`build:demo` 通过；真 Chrome **12/12**；`src/` 未动（`git diff HEAD -- src/ scripts/ tests/` 为空，`trace-core` 因此与基线逐字节相同）；两次 pack 逐字节相同，11 个条目里包内 `README.md` 与 `dist/index.js` 的 SHA-256 与工作树逐个一致。**度量**：包 34575 → **34593 字节**、SHA-256 `12e792eba0c2290213e1ec745017ca63ce33cca171cd7cdeb9a268ecae5839b9` → **`9d5ee8e8f909aef494cc4a632098cbb88a14e838a537c90f86f77d2348a40ed8`**（README 由 npm 自动打进包，所以只改文档也会变）；`dist/index.js` **10710 字节**、`src/` **814** 行（都不变）。 | 本次落地；`统一刷新-评审.md`（删除）、`README.md`、`ADR.md`、`交付记录.md` |
