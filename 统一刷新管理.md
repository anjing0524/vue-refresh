# Vue SPA 统一刷新管理

**统一文档** · 2026-09-16 根契约重写版 · 状态：实现完成；全量验收、业务接入与发布未完成

本文是唯一维护入口：目标、公共契约、行为规则、验收与门槛都在这里；设计与实现细节见 [DESIGN.md](./DESIGN.md)。
文档修订版本不代表软件成熟度。

**阅读路径：** §0 名词解释 → §1 目标与范围 → §2 公共契约 → §3 行为规则 → §4 验收目录 →
§5 门槛 → §6 工程与记录约束 → §7 覆盖状态 → §8 维护约定。
本版是「根契约重写」（依据见 [ADR.md](./ADR.md) ADR-27）：目标与规则按**页面要什么**重新推导，
不再是上一版按实现事实逐条累加的 25 条触发锚点与 129 个验收叶子。旧编号整体退役，不复用。

---

## 0. 名词解释

### 0.1 核心概念

这八个词是产品的抽象层，不依赖任何具体类型或字段；先读它们，再看后面的条目。

| 名称 | 含义 |
|---|---|
| 声明 | 一个组件对「要哪份数据、多久一次」的声明。声明本身不是请求；相同身份重复声明幂等（U03）。**挂载期间一直有效**：暂停、失活、隐藏只让它失去资格，卸载或换身份才撤销；声明的多寡决定实例与结果的生死（U06、ADR-61）。这一页的声明不另存名册——它就是这一页那份配置槽的归属（ADR-66）。 |
| 身份 | URL ＋ 完整参数值。身份相同即共享；字段顺序不影响身份，数组顺序影响身份（U02）。 |
| 共享实例 | 一个身份在框架内的一次共享生存期：第一个需求创建它，最后一个需求退出即销毁。 |
| 需求 | 一个句柄对某个共享实例的要求的总称：周期订阅，以及一次显式刷新带来的那次取数（U06）。 |
| 资格 | 一个订阅当前是否应该驱动自动刷新的唯一判据：存活、已声明身份、环境允许、配置开启且有周期（U04）。它是推导值，不单独保存。 |
| 暂停 | 调用方通过 `enabled` 表达的「不要自动刷新」。框架只读、从不改写；暂停页仍可刷新一次（U05）。 |
| 刷新 | 明确要求重新获取当前身份的数据。显式刷新与自动刷新走同一条取数、复核与交付路径（U14）。 |
| 刷新命令 | 一次显式刷新对身份下的一句「现在再取一次」：没有执行就排到最前，有执行就用这一轮的结果，结果已经写进表就再补一轮。**框架不记是谁点的**，同一轮内点几次合并成一次，没有回执（U14）。 |

### 0.2 领域名词

本节只保留**对外含义**；内部字段与所有权由 [DESIGN.md](./DESIGN.md) §3 维护。

| 名称 | 含义 |
|---|---|
| 资源（URL） | 一种稳定的取数目标：**它的身份就是一个 URL 字符串**（加上参数值）。声明一个资源不需要框架里的对象或函数——`useRefresh<P, T>(url, { enabled, every })` 的第一个参数就是它，`P`／`T` 就写在同一行的类型参数上；建议 URL 放应用级常量（U01、U02、ADR-75）。 |
| 已准备参数 | 提交边界复制的框架私有参数副本（不外发），以及完整参数值的稳定编码；编码表示数据范围，不是共享实例的生存期。 |
| 协调者（`RefreshManager`） | 应用级对象：一个 App 一个实例，负责共享、调度、取消与交付（U17）。 |
| 句柄（`RefreshHandle`） | 一个组件的一份需求：`display`（数据）／`failure`（失败）／`submit`／`refresh`。 |
| 已声明身份 | 句柄当前声明的参数身份；临时退出保留，新身份整体替换，**参数被拒不改动它**（U03）。 |
| 订阅 | 声明与资格同时成立时的说法：这一页此刻真的在驱动（或被纳入）自动取数。它**不是一条持久连接**，而是由声明与资格现算出来的状态（U04、ADR-61）。 |
| 共享执行 | 一次共享后台取数：框架按资源的 URL 发一次 POST，请求体是已准备参数的一份副本；一个实例同时至多一个（U08）。 |
| 页面快照（`RefreshDisplay`） | 本页按**已声明身份**从结果表抄出来的一个只读视图（**按本页 `every` 节流**）：`args`（每次抄写复制）、`data`（结果表里那同一个对象）、`updatedAt`（U11）。**它只装数据**——失败在另一个出口（U13）。 |
| 节流 | 读取面按本页 `every` 从结果表抄一份到 `display` 的判定：写入触发，只记**一个数**「上次读取时间」，比较 `updatedAt` 时间差——新格距那一版满一个本页 `every` 才换画面，否则保留；**同一身份同一版**（身份没换、数据时间没变）不抄第二遍，一次写入最多抄一次；**失败不进这个窗口**，它走自己的出口（U13）。写端稀于本页 `every` 时写入即抄。新身份的第一份内容、显式刷新那一次、以及重新成为读者后的第一份内容**没有读取时间，直接读**（U12、A21、ADR-67、ADR-72）。 |
| 失败 | 共享请求失败在结果表该身份那一格上留下的 `error` 与 `failedAt`（原始异常与发生时刻）：**不动**最后一次成功的数据，成功一到 `failedAt` 清回 `null`。页面从**失败出口** `handle.failure` 读它（`{ error, failedAt }`，没有失败时是 `null`）：**它不参与数据窗口**，换一笔就发布，所以首查就失败（此时 `data` 为 `null`）与成功过的身份上的失败都立刻可见（U13、ADR-77）。 |
| SubmitResult | `submit` 的判别返回值；取值与形状见 §2.4。 |

---

## 1. 目标与范围

页面已有查询函数与组件数据，但自行编写定时刷新要重复处理定时器、频率变更、页面失活、浏览器隐藏、
手动暂停、取消请求与旧响应覆盖；多个组件需要相同数据时还会重复请求。

**目标：** 页面表达刷新需求，公共工具负责订阅、共享请求、调度、取消与有效结果交付。
框架按身份保存共享结果的内部副本，只通过每个页面自己的 `display` 交付；它不做第二个数据出口，
也不承担「业务最新数据」这个角色（需要落到业务 Store 的页面在自己的适配层写，见 ADR-46）。

**十条根承诺**（每条都由 §4 的叶子判定）：

| # | 承诺 | 可观察事实 | 判定 |
|---|---|---|---|
| G1 | 写一次需求 | `submit(参数)` 之后框架负责取数；表单草稿变化无效果 | A01、A03 |
| G2 | 同参数只取一次 | 两个组件同 URL 同参 → 一次取数，各自拿到独立副本 | A02、A11、A20 |
| G3 | 到期才取 | 未到期不请求；轮询不重叠；不补跑漏掉的周期；不超过给定并发上限 | A07–A09 |
| G4 | 该停就停 | 暂停／失活／隐藏后不再发新请求（但声明与结果都留着）；**最后一个声明者离开**（卸载／换身份／销毁）才 abort 在途并清干净 | A04–A06 |
| G5 | 回来就有 | 恢复时画面**保持**已有结果再按频率继续（不重新取数）；表里已有更新时等下一份写入才上屏；不重放、不补跑 | A06、A07 |
| G6 | 手动刷一次 | 已有执行（在途或排队）就直接用它的结果，没有就排到最前，这一轮已经产出就再补一轮；与自动刷新同一条路径 | A14 |
| G7 | 失败不毁画面 | 失败写进该身份那一格、旧数据保留、下个周期继续；框架不改写调用方的开关 | A13 |
| G8 | 结果面自洽 | 一条结果对应一个身份；参数每次读取复制一份、数据是结果表里那一份（要改自己复制），时间随结果走 | A11、A12 |
| G10 | 应用之间不串 | 每个 App 独立的协调者与结果集 | A17 |
| G11 | 读取面按 `every` 节流 | 画面按本页 `every` 节流：读取面只记「上次读取时间」一个数，**同一身份同一版**不抄第二遍；新格距那一版满一个 `every` 才换画面，窗口内结果表的新版本不改变画面；新身份首份、显式刷新、重新成为读者后的首份**没有读取时间，直接读** | A21 |

**职责划分：** 调用方负责完整参数、业务校验、页面交互与展示；框架负责订阅、共享、调度、取消与交付。
框架不是查询库、缓存层或事务层。

**截止时间由传输负责：** 框架不给单次取数设自己的截止（ADR-65）——请求必然终止由注入的传输承担
（axios 的 `timeout`、反向代理或宿主自己的截止），一个永不结束的请求会一直占着并发槽。

**不纳入：** 产品边界以 [ADR.md](./ADR.md) 的裁决表为唯一清单。最常被问的三例——本地时钟与动画、
跨标签页共享、通用保存封装——都不在范围内。保存后需要重读时，由页面提交一次新的主动刷新，不增加保存事务 API。

**工程边界：** 包只导出两个函数与 6 个公共类型（`RefreshOptions` / `RefreshHandle` / `RefreshDisplay` / `RefreshFailure` / `SubmitResult` / `RefreshManager`）；其余类型只是上面这些类型的组成部分，不单独导出——调用方按推断使用即可，不必再记一个名字；公共入口形态、模块划分与依赖方向由 `pnpm check:docs` 断言。
结果表挂在调用方传入的 `pinia` 上（`pinia` 是 peer 依赖）；框架不把结果再落到第二个状态库，也不做第二个数据出口。

---

## 2. 公共 API 契约

正式导出只有两个函数与 6 个公共类型；**没有常量对象**——取值域直接写在判别联合里（ADR-51）。

| 类别 | 名单 |
|---|---|
| 函数 | `createRefreshManager`、`useRefresh` |
| 类型 | `RefreshOptions` / `RefreshHandle` / `RefreshDisplay` / `RefreshFailure` / `SubmitResult` / `RefreshManager` |

**不导出的类型**（`ReadonlySnapshot`）只是上表类型的组成部分，调用方从函数签名与 `display` 推断即可；少一个名字，调用方就少一处「该用哪个」的判断。
公共类型的唯一代码入口是工程内 `src/public-types.ts`。

### 2.1 两个入口

```ts
interface QuoteParams { account: string; symbol: string }
interface Quote { price: number }

const QUOTE_URL = '/api/quote'   // 应用侧自己组织「一处声明、多处复用」：类型别名 ＋ URL 常量

// 框架对 URL 发 `post(url, 参数值, { signal })`；这是你配好 baseURL／拦截器／鉴权头的 axios 实例。
const refresh = createRefreshManager({ maxConcurrent: applicationConfig.refreshConcurrency, axios, pinia })
app.use(refresh)

const task = useRefresh<QuoteParams, Quote>(QUOTE_URL, { enabled, every })  // 两项都是 `Ref`，都必需
task.submit({ account: 'demo', symbol: 'DEMO' })  // 声明订阅；同参重复声明是幂等的
task.refresh()                                    // 显式刷新：与自动刷新同一条获取与交付路径；没有回执
```

`useRefresh` 的第二个参数是 `RefreshOptions`，**格式固定**：`enabled` 与 `every` 都是 `Ref`，且都必需。
框架只读它们的 `.value`，改任一项都按新配置重新协调（暂停页仍可刷新一次；改频率立刻生效——它同时是
这个身份取数的间隔与本页读取结果表的节流间隔）。浏览器可见性由框架自己监听，调用方不再声明第二层可见条件。

| 接口 | 输入 | 返回 |
|---|---|---|
| `createRefreshManager` | `{ maxConcurrent, axios, pinia }`（`axios` 只需要有 `post`；结果表挂在 `pinia` 上） | `install` / `dispose` |
| `useRefresh` | `(url, { enabled: Ref<boolean>, every: Ref<number> })`；`url` 是取数目标的 URL 字符串（非空，空 URL 抛 `TypeError`），`P`／`T` 写在这一行的类型参数上 | `RefreshHandle<P,T>` |
| `handle.submit` | `(params: P)` | 同步 `SubmitResult`；`accepted` 表示身份已记录，不代表请求成功；同身份重复声明幂等 |
| `handle.refresh` | 无 | 无返回值；只给身份下一条刷新命令（没有回执），**成功经 `display`、失败经 `failure`**；不恢复自动刷新 |
| `manager.dispose` | 无 | 幂等释放 |

### 2.2 完整类型与状态取值

取值域直接写在判别联合里，**没有常量对象**（ADR-51）：调用方比较的是裸字面量（`result.status === 'cancelled'`）。
不使用 TS enum —— 工程的 `erasableSyntaxOnly` 与 Node 的类型擦除都不接受该语法。

```ts
import type { App, Ref } from 'vue'
export type ReadonlySnapshot<T> =
  T extends (...args: never[]) => unknown ? T :
  T extends object ? { readonly [K in keyof T]: ReadonlySnapshot<T[K]> } : T
export type SubmitResult =
  | { readonly status: 'accepted' }
  | { readonly status: 'rejected'; readonly error: unknown }
  | { readonly status: 'cancelled' }
export interface RefreshDisplay<P extends object, T> {
  readonly args: ReadonlySnapshot<P>
  readonly data: ReadonlySnapshot<T> | null
  readonly updatedAt: number | null
}
export interface RefreshFailure {
  readonly error: unknown
  readonly failedAt: number
}
export interface RefreshOptions {
  readonly enabled: Ref<boolean>
  readonly every: Ref<number>
}
export interface RefreshHandle<P extends object, T> {
  readonly display: Readonly<Ref<RefreshDisplay<P, T> | null>>
  readonly failure: Readonly<Ref<RefreshFailure | null>>
  submit(args: P): SubmitResult
  refresh(): void
}
export interface RefreshManager {
  install(app: App): void
  dispose(): void
}
```

`ReadonlySnapshot` 是**编译期**视图：框架交付的每个值都是独立副本，运行期不做拦截，
因此只读视图不承诺运行期不可写——别名安全由「每个消费者各一份副本」保证（U11）。

### 2.3 安装与销毁

`createRefreshManager` 要求正安全整数 `maxConcurrent`。`app.use` 安装；同实例同 App 重复安装无副作用，
同一 App 上的另一个活跃实例被拒，被拒的安装不改动已有绑定。
本库是 **SPA 单例**：当前协调者放在模块级变量里，组件适配不经过 `provide` / `inject`；
HMR 或会话切换时只判断槽里有没有占位者（槽里只会有活着的协调者）——空着就直接接管（ADR-37）。
应用卸载或显式 `dispose` 释放自身监听、句柄、实例、Timer 与唤醒调度。已销毁实例不复用（U17）。

### 2.4 返回值与错误契约

| 操作 | 结果与副作用 |
|---|---|
| `submit` | 同步 `accepted` 表示身份已记录，**不是**请求成功；相同身份重复声明幂等（不写状态、不产生新请求）；参数被拒返回 `rejected` 且**不改动任何状态**（无效输入不产生副作用），保留画面；已销毁返回 `cancelled` |
| `refresh` 入口 | 刷新命令在入口闸被挡下时直接返回：未声明身份、失活或隐藏、已销毁、配置非法都不产生请求、不写结果、不改动已声明身份 |
| `refresh` 合并 | 直接用该实例已有的执行：无论它还在排队还是已在跑，都由这一轮的结果满足，不追发第二次；没有执行时就排到最前。这一轮已经写进结果表之后再点，就补一轮（一个位，同一轮内点几次合并成一次）；多个页面的要求由同一个结果一起满足 |
| `refresh` 交付 | **没有回执**：成功经 `display`、失败经 `failure`（两者都取结果表同一格：成功在写进之后，失败写在同一格上），保留旧数据与开启意愿，下个周期继续；**框架不改写调用方 `enabled`**；入口条件不成立或已销毁时静默返回，不写结果 |
| 共享请求失败 | **写进该身份那一格的事实**：`error` 与 `failedAt` 换上本次异常与发生时刻、最后一次成功的数据与其产生时间原样保留；失败也算这一轮的结算，**不自动重试**；需求与开启意愿保留，下个周期继续。框架不向页面推送失败——页面从**失败出口** `failure` 读到它，**不参与数据窗口**（U13、ADR-77） |

`submit` 的取消分支不携带原因：取消只有一个来源（句柄或协调者已销毁），单成员取值没有信息量。
失败是**那个身份那一格的事实**（ADR-63）：失败只把该格的 `error` 与 `failedAt` 换上（原始异常原样带出），
**不动**最后一次成功的数据与它的产生时间；之后成功一到，失败出口清回 `null`。请求可能是别的页面发起的，
因此这是页面**无法从别处知道**的一类失败——它只能从**失败出口**读到，没有推送通道。
**失败出口**（`handle.failure`）给的是「这一格此刻的失败态」：`{ error, failedAt }` 或 `null`。
**它不参与数据窗口**（ADR-77）：数据窗口比的是 `updatedAt`，而失败那一笔不动 `updatedAt`——要是让失败也走窗口，
它按定义永远进不了画面，成功过的身份就会对持续失败完全静默。所以失败换一笔就发布，**有没有失败看这个对象在不在**，
不看 `error` 是不是 `undefined`（页面 `throw undefined` 那种病态情况也如实带出）。
**首查就失败**时 `display` 不是 `null`，而是 `data: null`，同时失败出口上有那一笔；暂停页自己刷新的那一次同样写进这一格。
框架不引失败计数或时间戳版本（ADR-43／45／47 删过版本与代次计数）。
**调用方自己的输入问题不在这格上**——参数不可用由 `submit` 同步返回 `rejected`；
配置非法不写失败（框架只负责不订阅、不请求，修正后自动恢复），页面读自己的 refs 就知道。
**交付面不交付「这次结果由谁触发」**：页面自己知道它何时调用过 `refresh()`，
而共享请求的触发者可能是别人——这个字段要么是资源级事实（得另存一份），要么只是「本次交付是不是我点的那一次」（调用方自己就有），
因此在根契约重写时删除（见 ADR-34）。

### 2.5 交付面与刷新动作

页面有**两个读出口**，都按**已声明身份**（URL ＋ 参数值）从结果表抄当前那一格：`display` 给**数据**（`args` / `data` / `updatedAt`
整格一起给出，不出现半新半旧的画面），`failure` 给**失败**（`{ error, failedAt }`，没有失败时是 `null`）。**数据是结果表里那同一个对象**——同一身份的所有读者拿到同一份，要改自己复制
（编译期只读视图由 `ReadonlySnapshot` 约束）；`args` 每次抄写复制一份，所以页面改不到下一轮请求的参数。
**从未成功过**（首查就失败）时 `data` 与 `updatedAt` 为 `null`，而失败出口上有那一笔失败；成功一到，失败出口清回 `null`。
失败**不参与数据窗口**：它换一笔就发布，所以持续失败的接口不会被节流窗口挡住（ADR-77）；两个出口共用同一个读者闸门。
**只有读者才跟随，而且按本页 `every` 节流**（ADR-67、ADR-72）：本页仍是该身份的读者——**有资格**（声明着它、开启、激活且浏览器可见），或它**还没有读取过**、此刻浏览器可见（读闸门在适配层：取数资格，或「还没有读取时间」这一个事实）——时，读取面由**写入事件驱动**：结果表该格被替换后立即判定，状态只有一个数「上次读取时间」；**同一身份同一版**（身份没换、数据时间没变）不抄第二遍；新格的 `updatedAt` 距那一版满一个本页 `every` 才换画面，否则保留（**节流窗口内结果表里的新版本不改变画面**；没有可比的时间——本页还没读到过、或这一格从未成功过——就不受窗口限制）。写端稀于本页 `every` 时**写入即抄**（比固定窗口更及时）；写端密且 `every` 非整数倍时实际更新周期被量化到写入网格（最坏 `every`＋写入间隔，确认人接受的代价）。新身份的第一份内容、本页显式 `refresh()` 那一次、以及**重新成为读者**（资格从无到有那一条边）之后的**下一份写入不等窗口**——前两处把这一页的上次读取时间置空，第三处由配置/生命周期 watcher 在那条边上置空（同一份配置内改频率、改可见性不动它，ADR-91），都一样是「没有时间就直接读」；其中「重新成为读者」这一处要等下一份写入到达才上屏（配置变化不叫醒读取面，ADR-73）；快页面（`every` 小）因此比慢页面跳得密，取数仍按同一身份所有页面里**最小的 `every`**（慢页面白嫖快页面）。浏览器可见性只挡有网络代价的取数，**不参与**读取面节流。暂停、失活、卸载中都不是读者，画面**冻结在最后一次读到的值**（ADR-60）；暂停页自己 `refresh()` 那一次照样更新画面——框架不留「欠一次」的账，放行靠这一页自己的「还没有读取时间」（A05、G6）。**口径（明说）**：暂停页如果**还没有读取过**，会跟着第一份到达的数据上屏一次（此后它有了上次读取时间，就冻住）——这正是「没有读取时间就直接读」不再区分「第一次」与「点过刷新」的代价；**点过刷新之后才失活**、浏览器仍可见的缓存页，那一帧**仍会更新**（确认人 2026-09-17 的裁决「允许它更新一帧呗」），而**失活或隐藏期间才点 `refresh` 的什么都不产生**（入口闸要求这一页激活、配置有效且浏览器可见）；A04／A06 的验收（失活不产生请求、画面保留、激活读回同一份结果）两条都照旧成立。
`updatedAt` 是产生本条结果的墙钟 epoch 毫秒；实例仍在时后加入的页面读到的仍是原结果时间，
因此页面可以据此显示数据多旧。它是墙钟读数，不保证单调、也不承诺 `updatedAt ≤ Date.now()`，
页面计算相对时间时应自行把差值钳制到 0。框架不提供「正在刷新」这类实时状态。

配置非法的通知行为规则见 §3（U05）。
参数编码的行为规则见 §3（U15）。

`refresh()` **没有回执**：成功经 `display`、失败经 `failure`——与自动刷新同一条通道，**包括未订阅的暂停页**
（它自己那次刷新的结果或失败同样写进这一格，并按「没有读取时间就直接读」那一次不等窗口的抄写立即上屏，因此拿到新画面或新失败就是这个动作的结果）。
它不携带 DTO、不恢复自动刷新、不改写调用方的开关；入口条件不成立（已销毁、未声明身份、环境不允许）时什么都不发生，
配置非法时也什么都不发生——页面读自己的 refs 就知道（ADR-51）。

结果表本身**不是包契约**：页面侧只有 `display` 与 `failure` 两个读出口（`useRefreshStore` 留给库内，只读计数投影
`snapshot()` 随 ADR-79 移出 `src/`、住在开发侧支撑模块 `scripts/observe.ts`，两者都只给演示与测试看）。
结果随实例释放即删，所以「没有需求就没有结果」这条仍然成立（A06）。

---

## 3. 行为规则（唯一维护入口）

规则以**能力域**为阅读单位：R1–R6 是全部行为承诺，每条能力域由「机制 ＋ 触发清单 ＋ 判定」组成。
`U01`–`U09`、`U11`–`U18` 是**锚点**：跨文档引用一律指向锚点；本版是根契约重写，**旧版锚点与全部旧叶子编号整体退役、不复用**（ADR-27）。`pnpm check:docs` 断言锚点不重不漏，且本文档与 DESIGN、README 里出现的每个 `U` 引用都有落点。

失败只有三个产品含义，三者互不替代：**动作失败**——页面按下的那一次没有成功（`U14`）；
**本条暂不可得**——共享数据这一次没取到，失败写进该身份那一格、旧数据保留，需求与开启意愿都保留，下个周期继续（`U13`）；
**输入被拒**——参数复制失败、值域不合格或编码不出身份（循环引用），或配置非法，都直接回报调用方且不改动任何已定事实（`U03`、`U05`）。

### 3.1 R1 共享结果与身份

**机制：** 已准备参数相同的调用共享同一次请求与同一份结果，参数变化立即换身份。

- `U01` `submit` 接纳完整参数；接纳时复制成框架私有副本、编码为身份键。表单草稿与结果永不进入跟踪范围。
- `U02` 身份由取数 URL（一个字符串）＋ 完整参数值决定：字段顺序不同仍共享，数组顺序影响身份；不同身份各有一次取数。
  **URL 就是身份的一半，因此同一个 URL 在几处各写一份定义也照样合并**；反过来，同一个 URL 配不同参数值是不同身份。
  账户与请求上下文必须纳入参数，或由独立 URL 表达；应用级隔离由每个 App 独立的协调者保证（`U17`）。
- `U03` 相同身份的 `submit` 幂等：保留现有订阅，不产生新请求、不重建订阅；
  身份变化立即成为当前意图并整体替换。**参数被拒时不改动任何状态**——旧身份与订阅原样保留，
  只回报 `rejected`（输入问题只走同步返回值，不写结果表那一格）。

判定：A01、A02、A03、A20

### 3.2 R2 订阅生命周期与调度

**机制：** **声明决定归属，资格决定取数**：页面挂载期间一直声明着身份（暂停、失活、隐藏都不撤销），只要有有资格的声明者就按其中最急的间隔取数；只有卸载、换身份、销毁才撤销声明并回收实例与结果。

- `U04` **资格**只决定要不要取数，由这一个合取唯一判定——声明还在、环境允许（组件激活且浏览器可见）、
  配置明确开启且有正安全整数周期（`enabled` 与「这一页是否激活」同在一份配置槽里；周期读不出或不是正安全整数时整槽按非法处理；**全局可见性由适配层持有**，并与「这一页是否激活」合成同一份快照里的 `present`（ADR-87））。
  资格现算、不产生副作用，取消与恢复都由它推导；刷新的命令不参与资格，**失去资格也不撤销声明**。
- `U05` `enabled` 是唯一的开启意愿，**只由调用方写入**，框架只读。关闭只是失去资格：不再自动取数、
  画面冻结（读闸门在适配层判定它不再是读者；**若这一页还没有读取过，它会跟着第一份到达的数据上屏一次**，
  此后有了上次读取时间就冻住），但**不撤销声明、不删结果**；暂停页仍可刷新一次
  （刷新命令的入口闸不看 `enabled`），刷新不把暂停页变回自动订阅。
  配置槽读不出或值非法时：不订阅、不自动刷新、**不写失败**（框架没有推送通道；修正后按当前资格恢复），刷新入口也直接返回；
  读不到的开关不猜成 `false`——配置是页面自己的输入，它读自己的 refs 就知道（ADR-51）。
- `U06` 失去资格（失活、隐藏、暂停）不再取数、画面冻结，但**不撤销声明**：
  实例、结果表条目与在途执行都留着，**框架不留任何「欠一次」的账**——读取面只有一个数「上次读取时间」；
  失去资格**之前**点过的那次刷新把读取时间置空，因此那一帧仍会更新（没有回执），而失去资格期间才点的
  `refresh` 连入口闸都不放行，什么都不产生；重新取得资格时画面保持在最后一帧、不重复取数，表里已有更新时**下一份写入**才上屏（ADR-73）。
  失去资格那一侧的边沿**不清**上次读取时间——清了读闸门第二项就会把整个失去资格的期间放行、画面在后台
  一路跟下去，「冻结」就没了（ADR-72）。
  **最后一个声明者离开**（卸载、换身份、销毁）时才删除实例、结果表条目与排队执行，并 abort 在途执行。
  资格只在入队时判定，因此已经排上的取数照常走完。
- `U07` 到期时刻由「最近一次执行有结局的时刻（成功与失败都算）＋ 当前最短间隔」现算，因此改频率立刻生效；
  从未结算过的实例立即到期。取消不计时、不补跑挂起期间漏掉的周期；恢复后只取一次。
- `U08` 同一实例同时至多一个共享执行：上一轮没有真实结束时不再发起，普通轮询不重叠。
- `U09` 后台执行按排队次序执行（**手动刷新的命令排到队首**：它是「人正等着」的取数），并发槽统计真实尚未结束的请求；显式刷新与自动刷新共用这些槽位，
  不绕过并发上限；满槽不自旋，执行真实结束后继续。**框架不给单次取数设自己的截止**（ADR-65）：
  请求必然终止由注入的传输负责，一个永不结束的请求会一直占着并发槽。

判定：A04、A05、A06、A07、A08、A09

### 3.3 R3 交付面与失败

**机制：** 页面按已声明身份、**按本页 `every` 节流**（写入事件驱动、比较 `updatedAt` 时间差，ADR-67）读结果表；失败写在同一格上，不改变已定结果。

- `U11` 两个读出口都按**同一格**整份发布、不做深响应式：`display` 给数据（`args` / `data` / `updatedAt`），`failure` 给失败（`{ error, failedAt }` 或 `null`）。
  **数据的读出口与参数不同**（ADR-59）：`data` 是结果表里**同一个对象**，同一身份的所有读者共享它——要改自己复制，
  编译期只读视图不算运行期隔离；`args` 仍然每次抄写复制一份，因此页面改不到下一轮请求的参数或身份键所描述的值（ADR-52）。
  `data` 与 `updatedAt` 在从未成功过时为 `null`（此时失败在 `failure` 出口上）；`null` 是有效结果，`undefined` 不是。
- `U12` 一条结果写进结果表后，**属于该身份的任何读者都读到它**（有资格的页面，或还没有读取过且此刻浏览器可见的页面；读闸门在适配层），
  同一次取数对每个身份只写一条；**失去读者身份（暂停、失活、卸载）后画面冻结在最后一帧，不再跟随新值**。
  读者按**本页 `every` 节流**跟随（ADR-67）：写入触发判定，新格的 `updatedAt` 距展示中那份满一个本页 `every` 才换画面，
  节流窗口内结果表里的新版本**不改变**画面（快页面跳得密，慢页面主动要的就是这个）；写端稀于本页 `every` 时写入即抄，
  写端密且 `every` 非整数倍时更新周期被量化到写入网格；**同一身份同一版**（身份没换、数据时间没变）不抄第二遍。**失败不进这个窗口**——它有自己的出口，换一笔就发布。
  新身份的第一份内容、显式刷新那一次、以及**重新成为读者**（资格从无到有那一条边）后的第一份内容**不等窗口**（读取时间被置空）；
  同一份配置内的变化（改频率、改可见性）不算这一条边，窗口照常（ADR-91）。
  页面若要知道「这次是不是我手刷的结果」，记下点击时刻、读 `updatedAt` 自行判断即可。
- `U13` 共享请求失败写成**该身份那一格的事实**：`error` 与 `failedAt` 换上本次原始异常与发生时刻，
  最后一次成功的数据与其产生时间原样保留（旧数据不动、不毁画面）；失败也算这一轮的结算，框架**不自动重试**，
  需求与开启意愿保留，下个周期继续；框架不改写 `enabled`、也不向任何页面推送失败——页面从**失败出口**
  `handle.failure` 读到它：`{ error, failedAt }`，没有失败时是 `null`。**它不参与数据窗口**（数据窗口比的是
  `updatedAt`，失败那一笔不动它），换一笔就发布——**包括首查就失败**（此时 `display.data` 为 `null`）与暂停页自己刷新的那一次；
  之后成功一到，`failure` 清回 `null`。`error` 原样带出原始异常（页面 `throw undefined` 也如实带出），
  所以判断有没有失败看 `failure` 是不是 `null`，而不是看 `error` 是不是 `undefined`。

判定：A11、A12、A13、A21

### 3.4 R4 显式刷新与共享取数

**机制：** 页面显式刷新与自动刷新共用同一条获取、有效性复核与交付路径；框架只有这一条取数通道。

- `U14` `refresh` 是给身份下的一句刷新命令：「现在再取一次」，走同一条取数通道。
  **没有执行**就排到最前；**有执行、结果还没产出**就用这一轮的结果（不追发第二次，A14）；
  **有执行、结果已经写进表**就再补一轮——**同一轮内点几次合并成一次**，跨轮则每点一次补一轮（框架不再给
  「一个页面同时只有一次未满足请求」这个上界）。
  **没有回执**：成功经 `display`、失败经 `failure`（U11／U12／U13），包括未订阅的暂停页；它自己那一次把这一页的读取时间置空（**没有时间就直接读**），结果一到就抄。
  入口检查用取数资格里除 `enabled` 以外的三组事实（已声明身份、这一页激活、浏览器可见）＋ 配置有效；配置非法时直接返回、不写事实；
  失去存在、未声明身份或销毁时直接返回、不产生副作用。刷新也不恢复自动刷新、不改写调用方的开关。

判定：A14

### 3.5 R5 参数边界

**机制：** 参数只在提交边界复制、查值域、编码一次；框架不判断业务字段是否合法，也不设深度或体量上限。

- `U15` 参数在提交边界**复制 → 值域检查 → 按参数键排序的稳定 JSON 编码（`fast-json-stable-stringify`）**；
  这份是框架私有副本，不外发——每一轮请求体、每个接收者的 `display` 各拿一份副本（ADR-52）。
  **值域**：对象型参数只能是普通对象或数组；`Date`／`Map`／`Set`／`RegExp`／`ArrayBuffer` 等容器的内容对编码
  不可见（全部编码成 `{}`），内容不同的参数会塌成同一个身份、共享另一条查询的数据，故一律拒绝（`Date` 请传它的
  ISO 字符串）。**声明形式**：这条值域在调用 `useRefresh` 时就由它的类型参数约束（`P extends JsonParameters<P>`）先报错；`type` 别名、
  `interface` 与内联对象字面量都满足这条约束——它是同态映射类型，不要求索引签名（`interface Params` 的写法在 TS 4.9.5 与 7.0.2 下都实测通过）。
  标量沿用 JSON 语义：`-0` 与 `0` 同键，字段顺序无关，数组顺序有关，`NaN` / `Infinity` 按 `null`，
  `undefined` 字段按省略；函数与 Proxy 这类复制不了的值在复制这一步就被拒绝，走不到编码。
  值域不合格、复制不了（函数、Proxy）与编码不出身份（循环引用）都按非法参数拒绝（只经 `rejected`，不写结果表那一格），
  且不改动任何状态、不产生实例或请求。不设内部深度上限，也不因参数大而拒绝（G01 已关闭）。
  这一份参数只准备一次：轮询、恢复与显式刷新都复用同一个对象；**框架不跑任何调用方回调**，业务准入由调用方在 `submit` 之前自己判（ADR-74）。

判定：A18、A19

### 3.6 R6 隔离与工程边界

**机制：** 页面回调的失败不影响框架状态；每个 App 独立；参数规模由应用决定。

- `U16` **框架不运行任何调用方代码**：核心不调用页面代码，适配层也不再跑任何回调（业务准入归调用方）；
  提交边界参数准备的抛错就地变成同步 `rejected`，不改变已定结果、声明、调度与其他读者的读取。传输拿到的
  `AbortSignal` 上的监听器由宿主在 `abort()` 时同步调用，不是框架调用的回调，其抛错由宿主上报，不在隔离承诺内。
- `U17` 每个 App 独立协调者与结果集；同实例同 App 安装幂等，非法跨 App 或多活跃绑定被拒且不破坏已有绑定；
  `dispose` 先失效再清理自身监听、句柄、实例与调度，未结束的执行到真实结束才释放槽位。
- `U18` 参数只在提交边界准备一次：复制、值域检查、编码各一次，之后轮询、恢复与显式刷新都复用同一份。

判定：A16、A17、A18

---

## 4. 验收目录

### 4.1 判定记法与归属

**归属前缀。** 每个叶子项自带一个前缀，判据是**判定它是否需要调用方观察不到的内部事实**：

- `[行为]` 产品可观察行为：判定只靠公开面，逐条可回答「这条对外承诺了什么」。
- `[不变量]` 实现不变量：判定依赖内部结构与顺序纪律，用于回归与定向故障注入，不构成对外承诺。
- `[混合]` 混合条目：外层结果可由公开面断言，但「必须结果」里另含一条内部一致性要求。

前缀与叶子同格；`pnpm check:docs` 断言每个叶子恰好有一个前缀、前缀取值封闭。
不得为通过测试而删除或放宽条目；行为随契约移除而消失时，同步删除对应条目并记录理由。

**变体记法。** 需要分别判断的输入直接写在条目「必须结果」列尾：`变体：01 …；02 …`。
编号是稳定标签，不保证连续：一旦分配即不复用、不重排。

**记号：** 甲 / 乙 指两个独立组件句柄；A / B / C 指不同参数值（因而通常是不同共享实例）。

### 4.2 共享结果与身份

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A01 | 首次声明后立即取一次并把结果写进结果表；已准备参数是框架私有副本，页面改原对象不影响身份 | `G1/G8 首次订阅…`、`A11 交付面…` |
| [行为] A02 | 同 URL 同参数的两个句柄共享一次取数且各得独立副本；字段顺序不同仍共享，数组顺序不同不共享 | `A11/A02 同参数…`、`A02 身份是…` |
| [行为] A20 | 同一 URL 在两处各声明一份定义（两份声明就是同一个 URL 字符串）仍然合并：只发一次取数、各自交付；定义里既没有取数函数也没有准入规则，因此没有「谁先接入谁生效」的冲突可检出 | `A20 同一个 URL 就是同一个身份…` |
| [行为] A03 | 同身份重复声明不新增请求、不重建订阅；参数被拒返回 `rejected`、旧身份与订阅原样保留，输入问题只走同步返回值（不写结果表那一格） | `A03 相同参数重复声明幂等`、`A03 参数被拒时…` |

### 4.3 订阅生命周期与调度

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A04 | 资格只决定取数，三组事实缺一不可：声明还在、激活且浏览器可见、配置明确开启（读到的 `enabled` 必须是布尔、`every` 必须是正安全整数，否则按配置非法处理，不取数） | `A04/A05 配置非法时不取数、不刷新、不通知…`、`A04 运行期读到非布尔时…` |
| [行为] A05 | 关闭开启意愿只失去资格（不再自动取数、画面冻结），不撤销声明、不删结果，**框架不留「欠一次」的账**（刷新是当下那一句命令）；暂停页仍可刷新一次且不恢复自动订阅（**还没读取过的暂停页会跟着第一份到达的数据上屏一次**，此后冻结）；配置非法不写失败并可恢复 | `A04/A05 关闭开启意愿后…`、`A04/A05 配置非法：不取数并停止订阅` |
| [行为] A06 | 隐藏与失活不再取数、画面冻结（不再跟随新值；**还没读取过的那一页会跟着第一份到达的数据上屏一次**，此后冻结），但不撤销声明：实例、结果表条目与在途执行都留着，**框架不留任何「欠一次」的账**（失去资格**之前**点过的那次刷新把读取时间置空，因此那一帧仍会更新、没有回执；失活或隐藏**期间**连 `refresh` 的入口闸也不放行）；**最后一个声明者离开**时三者一并消失且 abort 在途；重新成为读者时画面保持在最后一帧、不重复取数（表里已有更新时下一份写入才上屏，ADR-73） | `A06 最后一个声明者退出（卸载）…`、`A06/A11 恢复…` |
| [混合] A07 | 到期由「结算时刻 ＋ 当前最短间隔」现算（改频率立刻生效）；长时间挂起后恢复只取一次，不补跑漏掉的周期 | `A07 有效间隔取…`、`A07 长时间挂起后恢复…` |
| [行为] A08 | 上一轮没有真实结束时不再发起新的取数 | `A08 轮询不重叠…` |
| [不变量] A09 | 并发槽统计真实未结束的请求，满槽排队不自旋；槽位空出后按排队次序继续（手动刷新在队首） | `A09 并发上限约束…` |

原 `A10`（上限从开始执行起算、到期强制结算并交还槽位）整条已删除：框架自带取数上限的能力按确认人
2026-09-18 的裁决随 ADR-65 移除——请求必然终止改由注入的传输负责，不是为通过测试而删。

### 4.4 交付面与失败

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A11 | 一条结果对应一个身份：`args` 每次抄写复制一份，`data` 是结果表里**同一个对象**（同一身份的读者共享它，要改自己复制），`updatedAt` 与它同来；从未成功过时 `data`／`updatedAt` 为 `null`（失败在失败出口上）；`null` 是有效结果 | `A11 交付面…`、`A11/A13 空结果…` |
| [行为] A12 | 结果写进结果表后，属于该身份的任何读者都能读到它（有资格的页面，与还没有读取过且此刻浏览器可见的页面一样；读闸门在适配层）；同一次取数对每个身份只写一条；读者按本页 `every` 节流跟随（ADR-67、ADR-72），同一版不抄第二遍、一次写入最多一次、窗口内新版本不改变画面；失去读者身份后画面冻结在最后一帧 | `A06/A11/A12 适配层…` |
| [行为] A13 | 失败写进该身份那一格、旧数据保留、失败也算这一轮的结算、不自动重试、下个周期继续；从未成功过（首查就失败）时 `data` 为 `null`，失败从**独立出口** `failure` 读得到（**不参与数据窗口**，所以成功过的身份上的失败也立刻可见），之后成功一到 `failure` 清回 `null` | `A13 失败写进结果表那一格：首查失败也读得到，成功后失败被清掉，数据保留旧址`、`A13 共享请求失败…`、`A13/A14 在途时点的刷新由这一轮的结果回答…`、`A13/A14 暂停页显式刷新失败…` |
| [行为] A21 | 读取面按本页 `every` 节流（ADR-67、ADR-72）：慢页面不跟着快页面跳、窗口内结果表里的新版本不改变画面；写端稀于本页 `every` 时写入即抄；新身份的第一份内容与显式刷新那一次**不等窗口**（此时没有上次读取时间）、立即上屏；取数仍按同身份所有页面里最小的 `every` | `A21 节流：慢页面不跟着快页面跳，节流窗口内的新版本不换画面；显式刷新不等节流`、`A21 写端稀于本页 every 时写入即抄：节流不丢数据，新格一到就上屏` |

### 4.5 显式刷新

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A14 | 已有执行（在途或排队）直接满足本次刷新、不追发第二次；没有执行时排到最前；结果已经写进表时再补一轮，同一轮内点几次合并成一次；没有回执（成功经 `display`、失败经 `failure`）、不恢复自动刷新 | `A14 在途任务直接满足…`、`A14 排队未启动的任务同样直接满足…`、`A12/A14 结果写入期间点的刷新由后继请求满足…`、`A12 产出之后的刷新是下一轮的命令…` |

### 4.6 隔离与工程边界

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A16 | 零回调：传输失败只写结果表，核心不认识页面、也不调用任何页面代码 | `A16 零回调：传输失败只写结果表，核心不认识页面也不调用任何页面代码` |
| [行为] A17 | 同实例同 App 安装幂等；另一个活跃实例被拒；销毁后可原地接管；未安装时 `useRefresh` 抛错；销毁后所有入口拒绝且未结束执行到真实结束才释放槽位 | `A17 安装…`、`A17 未安装协调者时…` |
| [混合] A18 | 键按 JSON 语义稳定排序（字段顺序无关、数组顺序有关）；值域不合格的容器（`Date`／`Map`／`Set`／`RegExp`／`ArrayBuffer`）、复制不了的值（函数、Proxy）与循环引用都按非法参数拒绝且不改动状态；编码得出身份的参数照常接纳并取数 | `A18 参数编码与值域…` |
| [行为] A19 | 参数隔离靠每个消费者各一份副本：框架私有的一份、每一轮请求体、每个接收者的 `display` 各拿一份，改自己的不影响身份键、也影响不到别人 | `A19 参数副本…` |

---

## 5. 决策与门槛

| 决策 ID | 内容与状态 | 提案责任与依据 | 确认人 | 阻断节点 |
|---|---|---|---|---|
| G01 | **已关闭（取消）**：参数不设业务侧深度或字节上限——业务无法预知参数规模，框架不因为键大而拒绝合法查询；实现不做业务合法性判断（参数只做稳定排序与值域检查——对象型限普通对象或数组，ADR-52；循环引用由编码包抛 `TypeError`，见 `U15`） | 用户 2026-09-15 决定：不要限制参数 | 用户已确认 | 无（已关闭） |
| G02 | 参数为可信 JSON 记录，全字段按值共享；**对象型参数只能是普通对象或数组**（严格线，ADR-52；`Date` 传 ISO 字符串），标量沿用 JSON 语义（`-0` 与 `0` 同键、`NaN` 按 `null`，见 `U15`）；值域由 `useRefresh` 的类型参数约束保证，提交边界再由运行期检查兜底；业务字段的合法性由调用方在 `submit` 之前自己判（框架不跑任何回调，ADR-74）；DTO 由业务边界校验、框架原生复制 | 用户确认固定资源定义与一次边界校验 | 用户已确认 | 只核对 A02 / A15 的现行边界测试 |
| G03 | **已确认**：**本包自身的类型自 TypeScript 4.9 起可用于消费（已实测）**；Vue 的 TypeScript 下限以它自己的 peer 声明为准，本包不替它承诺；最低运行环境需原生 `structuredClone` 与应用卸载钩子 | 用户 2026-09-15 指定「TS 4.9，其余最新」；仓库自身用 TS 7 构建 | 用户已确认 | 发布前核验：干净消费方安装、TS 4.9 类型消费与运行时能力；证据见工程 README |
| G04 | **已关闭**：目标设备性能充足；典型规模约 20+ 订阅、在途请求很少 | 用户 2026-09-15 给出的设备与规模结论 | 用户已确认 | 无（已关闭）。**2026-09-18「失败有自己的读出口」（ADR-77）交付记录**：确认人：「窗口display 要的就是数据 为啥要考虑 失败，失败是通过onerror 处理的啊」→「没毛病啊 是绑定在resource上啊，display 读取数据 创造个新的 读取error 不就行了」。**落地**：`RefreshHandle` 增 `failure`（`RefreshFailure = { error, failedAt }` 或 `null`），`RefreshDisplay` 收成 `args` / `data` / `updatedAt`；适配层拆成 `publishData`（窗口）与 `publishFailure`（不等窗口）；导出面：函数仍是 2 个、公共类型 5 → **6**。**口径**：放宽 U13（失败不参与数据窗口、换一笔就发布）、收窄 U11（数据出口只剩三格）；失败仍是那一格的事实、成功后清、不自动重试、也不是回调（ADR-64）。**反向对照**：把窗口加回失败一侧，新用例立刻超时失败；去掉后全绿。**证据**：用例总数 53（ADR-77 交付时 52；`tests/core.test.ts` 当时 37、`tests/vue.test.ts` 当时 16）、真 Chrome 12/12、轨迹与基线逐字节相同（`core.ts` 未动）、干净消费方类型 0 错误 ＋ **8 条**运行期场景通过。**度量**：`src/` 1089 → **1129** 行、函数 71 → **73**、结构分支 76 → **79**、`maxCCN` 8 → **6**；`dist/index.js` 10738 → **10920 字节**（10.92 kB）；包 **46130 字节**、SHA-256 `2c106e59bb802050e9c7b681ecaddc9cce79371ddfaf7a2b5bd06a3f7681337c`（两次 pack 逐字节相同）。**2026-09-18「按需求逐个复核数据结构与函数」（ADR-76）交付记录**：确认人：「开始整体重构代码，梳理数据结构，分清数据结构，你需要从需求出发，易用性 清晰性 不要从门禁出发……每个数据结构，数据结构的每个字段，你都需要问自己 需要我持有吗 有没有更好的结构，包括每个函数，需要抽取吗 需要调用链传参，需要外部调用吗等内容 检查代码问题」。**结论**：逐项复核后**没有新增字段、没有改口径**——三处看起来「更好的结构」（结果格成对、注册表两层、`settle`／`fail` 拆开）都是 ADR-65／ADR-68 拍过的东西，维持原判；落地的是 8 处没人读的机械（`Resource.fail` 与 `settle` 逐字节相同、`writeResult`／`writeFailure`／`enqueue` 三个一行转发、适配层的假键查表与死分支、两个永远同源的模块级变量、四个 sink 转发 lambda、未命名的元组元素），外加 3 处会话与 5 处文档订正（`every: 2000` 的示例、`await task.refresh()`、不成立的「`interface` 会被 TS2344 打红」、`every` 的必需性、`settledAt` 的时钟域）。**证据**：`tests/` 一行未改、用例总数 53（ADR-76 交付时 51，ADR-77 加了一条失败出口用例，ADR-78 又加了一条读取面清空用例）；`.tools/trace-after.txt`（ADR-70 之后的基线）与本次轨迹逐字节相同；真 Chrome 12/12；干净消费方 TS 4.9.5 类型 0 错误、7 条运行期场景通过；`build`／`build:demo` 通过；包 **45081 字节**、SHA-256 `d6695492f85a1e9cae19cecc96c3be7c0119f9864a7b3f0f7024e4d134a4d87e`（两次 pack 逐字节相同）。**度量**：`src/` 1106 → **1089** 行、函数 79 → **71**、结构分支 77 → **76**、`maxCCN` 7 → **8**；`dist/index.js` 11169 → **10738 字节**（10.74 kB）。**2026-09-18「`defineRefresh` 并进 `useRefresh`：入口只剩一个」（ADR-75）交付记录**：确认人：「有没有可能 `defineRefresh` 其实是 `useRefresh` 的一部分……参数也不用到处传递了」——ADR-74 之后它确实只剩「类型声明」这一件事，存在理由只是把 `P`／`T` 从一个函数搬到另一个函数。**落地**：`useRefresh<P extends JsonParameters<P>, T>(url: string, options)` 成为**唯一入口**（URL 与类型参数写在同一行）；`defineRefresh` 与 `RefreshSource` 类型删除；空 URL 的 `TypeError` 移到 `useRefresh`；`src/source.ts` 只剩参数边界（`Parameters`、`prepareParameters`）。**导出面 3 函数 ＋ 6 类型 → 2 函数 ＋ 5 类型**。**改判（明说）**：早先「`T` 在定义点声明」的裁决由本条取代——声明点从框架的 `defineRefresh` 移到**调用方自己的类型别名与 URL 常量**（`type QuoteArgs = {…}` ＋ `const QUOTE_URL = '/api/quote'`）；代价是同 URL 的多个页面不再由框架保证 `T` 一致（靠应用侧共享类型），换来入口少一个、类型少一个、每次调用少一层对象。`defineRefresh`／`RefreshSource` 进退役词表。**顺带修掉两条既有的时序 flake**：`A05/A11 暂停页还没有读取时间时…` 与 `A06 重新成为读者不会自己补抄…` 都用固定次数的 `tick()` 等真实调度（约每 5～12 次红一次，`1 !== 2`），按仓库既有约定改成 `until(条件, …)`（强度与用例数不变）；另补一条用例：空 URL 被 `useRefresh` 直接拒绝（原来是 `defineRefresh` 的运行期检查，从未被测过）。**契约变化（破坏性）**：`defineRefresh<A, B>('/api/x')` ＋ `useRefresh(quote, options)` → `useRefresh<A, B>('/api/x', options)`。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **51/51**（core 36 ＋ vue 15；连跑 15 次无 flake）；`pnpm check:docs` 全项通过（叶子 19、测试标题 63；§2.2 镜像用 `node .tools/sync-mirror.mjs` 重新生成）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过（导出面断言改成「恰好两个函数」，固定装置里两处仍用旧写法的场景随之改掉）；`node scripts/benchmark.mjs` 重跑：取数中位 632 次、收敛比 0.99、在途峰值 4、释放后残留全 0。**度量**：`src/` 1130 → **1106** 行、函数 80 → **79**、结构分支 77（不变）、`maxCCN` 7（不变）；`dist/index.js` 11178 → **11169 字节**（11.18 → **11.17** kB）；测试文件 `tests/core.test.ts` 的行数 1059 → **1060**、`tests/vue.test.ts` 的行数 595 → **598**；包 45799 → **45099 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `9d688985eb384590547d78f0c017cc43f42a81076f35cfd96685930856d32da0`，10 个条目哈希与工作树逐个一致）。无（已关闭）。**2026-09-18「结构里不留没人读的字段」（ADR-74）交付记录**：确认人两次点名——「有些结构里面的很多字段都没有（存在的必要），比如 `useRefresh` 为啥需要 `source.name`」「先删掉你发现的吧；另外你没发现 `validate` 在 define 和 use 里面都有吗」。**落地**：① `RefreshSource` 从「对象（`name` ＋ 可选 `validate`）」收成**带类型的 URL 字符串**（`string & { __params?; __result? }`：没有字段、没有运行期结构、不再冻结对象），`defineRefresh<P,T>(url)` 只收一个参数、只做类型声明并返回那个 URL；空 URL 检查移到 `useRefresh`（它一定被每一页调用）。② **`validate` 整条删除**：业务准入归调用方在 `submit` 之前自己判；框架只做值域与编码，`rejected` 仍由它们产生，参数准备只剩三步（复制 → 值域 → 编码）、`prepareParameters(input)` 不再收 `source`——这是 ADR-64「核心零回调」的终点：**全库零调用方回调**。`tests/types.ts` 里那条把 `validate` 写进 `useRefresh` 调用、注释为「validation belongs to the fixed source definition」的 `@ts-expect-error` 探针（确认人在源码里看到的「两处 `validate`」）随能力删除。③ `snapshot()` 去掉派生字段 `declarers`（等价写法 `resources.flatMap(r => [...r.declarers])`）；**保留** `scheduled`／`flushing`——它们不是派生值：`scheduled` 观测「队列已空但唤醒 Timer 已排」、`flushing` 观测「正在一轮 flush 中」，都推不出来，而且是差分轨迹工具与演示面板的唯一窗口。**一次「删过头」当场纠正（记下来）**：我最初把这三个投影一起当成「只有测试与演示读的字段」删掉，发现 `scheduled`／`flushing` 站不住后立即还原，并把因此被删的三条断言补回（`A17` 销毁后无残留 Timer、`A06` 卸载清理后无残留 Timer、`A09` 满槽不自旋）。**契约变化（破坏性，明说）**：`defineRefresh(url, { validate })` → `defineRefresh(url)`；`RefreshSource` 不再有 `.name`（它本身就是那个 URL 字符串）；`snapshot().declarers` 消失；**导出面计数不变**（3 个函数 ＋ 6 个类型）。测试侧：validate 相关的断言换成了值域拒绝（`Date`）与复制失败（函数）两个触发点，**用例数与断言强度不降**。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **50/50**（core 36 ＋ vue 14）；`pnpm check:docs` 全项通过（叶子 19、测试标题 62；§2.2 镜像用 `node .tools/sync-mirror.mjs` 重新生成）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过（固定装置里一处旧假设按新口径改掉：它原先靠**对象身份**区分两份同 URL 定义，现在两份定义就是同一个字符串）；`node scripts/benchmark.mjs` 重跑：取数中位 632 次、收敛比 0.99、在途峰值 4、释放后残留全 0、事件循环延迟 mean 1.09ms／p99 1.70ms／max 7.27ms。**度量**：`src/` 1147 → **1130** 行、函数 81 → **80**、结构分支 82 → **77**、`maxCCN` 7（不变）；`dist/index.js` 11635 → **11178 字节**（11.63 → **11.18** kB）；测试文件 `tests/core.test.ts` 的行数 1061 → **1059**、`tests/vue.test.ts` 的行数 597 → **595**；包 46276 → **45799 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `7ec440721fd45c07efc92b4444aa070eba9db5f3a440162336c06cec91c9d339`，10 个条目哈希与工作树逐个一致）。**收尾（同一轮交付内）**：记录落盘后抓到一条 flake——本轮新增的 `A06 重新成为读者不会自己补抄…` 用例用固定次数的 `tick()` 等真实调度，偶发等不到第二版（`1 !== 2`，约 5 次里 1 次）；按仓库既有约定改成 `until(条件, …)`（真实时序只等条件成立），随后连跑 8 次 50/50 稳定；`tests/vue.test.ts` 的行数随之记为 **595**。包不受影响（tarball 只含 `dist/` 8 ＋ `package.json` ＋ README）。无（已关闭）。**2026-09-18「`eligible` 触发计数也删掉」（ADR-73）交付记录**：确认人「`eligible` 也没啥用啊」。**落地**：删掉 `const eligible = shallowRef(0)`、副作用里那行依赖读取、watcher 里那记 `+= 1`——读副作用此后只由**这一格被写入**与**换了身份**唤醒；watcher 只做「把 `lastReadAt` 清掉 ＋ `core.reconcile()`」。ADR-72 记的那条顺序约束（先清 `lastReadAt`、后 `eligible += 1`）随之消失。**语义变化（明说，一处小的）**：「重新成为读者」这一拍**不再自己把表里已有的那一版补抄进来**——它只清掉上次读取时间，因此**下一份写入**到达时立即上屏（不等窗口）。差别只在「表里已有一版更新、而此刻恰好没有新写入」这一情形：页面先显示自己冻结的那一帧，直到下一份写入（≤ 一个共享窗口；若这个身份没有别的读者，恢复资格本身就会触发取数，取数回来即上屏）。ADR-72 记的「重新成为读者那一拍会把同一份内容再发布一次」这条代价随触发点一起消失。首查与显式刷新本身就是写入触发的，不受影响。**钉住新口径的用例**：`A06 重新成为读者不会自己补抄：下一份写入到达时才上屏，而且不等窗口`（覆盖：暂停页没有读取时间时上屏第一份 → 有读取时间后冻结 → 恢复不补抄 → 下一份写入不等窗口直接上屏）。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **50/50**（core 36 ＋ vue 14）；`pnpm check:docs` 全项通过（叶子 19、测试标题 62）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；核心未动（`node scripts/trace-core.mjs` 与 ADR-70 那次的 28 行轨迹**逐字节相同**）。**度量**：`src/` 1152 → **1147** 行、函数 81（不变）、结构分支 82（不变）、`maxCCN` 7（不变）；`dist/index.js` 11668 → **11635 字节**（11.67 → **11.63** kB）；测试文件 `tests/core.test.ts` 的行数 1061（不变）、`tests/vue.test.ts` 的行数 549 → **597**；包 46350 → **46276 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `6ded01802c8743ec85d2e1229e7268edd4a57dfa664e24fbc047658ccff12699`，10 个条目哈希与工作树逐个一致）。无（已关闭）。**2026-09-18「读取面收成一个数（上次读取时间）＋ 两次比较」（ADR-72）交付记录**：确认人第三次点名读取面——「为啥要区分 效果上不是一样的吗 都需要我需要一次数据，判定是 `lastReadAt + every >= update` 啊……读取面肯定是想读数据关 error 啥关系」。**落地**：删除 `sampled`（存整格）、`pending`、`immediate`——适配层只剩**一个标志** `released` ＋ 读取面**一个数** `lastReadAt`（上一次抄进画面的那一版数据的 `updatedAt`；`null` ＝ 还没读取过）＋ 一个纯 Vue 触发计数 `eligible`。判定三行：读闸门 `core.isEligible(...) || (lastReadAt === null && core.isVisible())`；**同一版不抄第二遍**（格子的 `updatedAt` 与失败位都跟画面里那份一样就直接返回——画面本身就是「已经抄到的那一版」，因此不需要另存状态）；窗口 `lastReadAt + every > cell.updatedAt` ⇒ 不换画面；发布后 `lastReadAt = cell.updatedAt`。刷新只把 `lastReadAt` 置 `null`（数据一到自然就换）。**「同一版」那一行是承重的（真 Chrome 抓到的）**：失败那一笔**不改 `updatedAt`**、只换 `error`/`failedAt`（ADR-63），只有它能看见「只失败、没新数据」的写入；少了它会红两条——`A13`（失败要能被读取面取到）与演示页 `查询列表` 的「失败关闭」（重新开启自动刷新那一拍会把旧版抄一次、用掉置空的读取时间，随后那笔失败被窗口挡住，页面永远等不到失败而关不掉自动刷新）。**唯一变掉的口径（明说）**：暂停页**还没读到过**时会跟着第一份到达的数据上屏一次（真 Chrome 的 `shown(乙) === null` 已按新口径改写为 `shown(乙) === 111`），此后它有了上次读取时间就冻住；「点过刷新之后**才**失活」的那一帧**仍会更新**（角落原样保留）。**必须保留的一处**：失去资格那一侧（暂停、失活、隐藏）**不清** `lastReadAt`，否则读闸门第二项会把整个失活期放行、画面在后台一路跟下去。**顺序约束**：配置/生命周期 watcher 里必须**先**清 `lastReadAt`、**后** `eligible.value += 1`（后者是同步副作用的触发点）。**订正我前两轮的判断（记下来免得重提）**：我曾把「失败可见性」与「`pending` 删不掉」当成不能合并的理由，两处都错——前者的实测方法不对（按这个模型写，失败靠一条能从画面本身推出的「同一版」比较就能看见），后者只在「未上过屏的暂停页什么都不显示」这条旧口径下成立；已写入 ADR-72。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **49/49**（core 36 ＋ vue 13）；`pnpm check:docs` 全项通过（叶子 19、测试标题 61）；`pnpm test:browser` 真 Chrome **12/12**（过程中抓到 ③ 的一处回归）；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；核心未动（`node scripts/trace-core.mjs` 与 ADR-70 那次的 28 行轨迹**逐字节相同**）。**度量**：`src/` 1160 → **1152** 行、函数 81（不变）、结构分支 82（不变）、`maxCCN` 7（不变）；`dist/index.js` 11608 → **11668 字节**（11.61 → **11.67** kB）；测试文件 `tests/core.test.ts` 的行数 1061（不变）、`tests/vue.test.ts` 的行数 550 → **549**；包 46311 → **46350 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `001cea2c4fb69c3d8cefbba01e0a87663f257fc88b8bf20bb78e16d1c29e7d66`，10 个条目哈希与工作树逐个一致）。**一处既有缝隙（等点名，本轮未改）**：成功之后的**周期性失败**（不改 `updatedAt`、又没有显式刷新或重新成为读者触发）仍会被窗口挡住，页面要等下一次成功或一次 `refresh()` 才看得到它——ADR-63 起就如此；要不要让「失败位变了」不进窗口（代价是与「error 不参与判定」有一点张力），请确认人裁。无（已关闭）。**2026-09-18「读取面收成『一个读取基准 ＋ 一次比较』：删掉 `immediate`」（ADR-71）交付记录**：确认人第二次追问读取面「它不就是创建一个 ref 记录读取时间和 data、`updatedAt` 比较吗」，并给出需求原话：「读取面第一读取没有时间所以直接读取，然后记录读取时间，和 watch 的最新数据时间比较」。**落地**：`immediate` 删除。它原来只回答「这一份不等节流」，三个来源里「身份落定」与「重新成为读者」本来就是**没有读取基准**的状态（把基准清掉即等价，「没有时间就直接读」），「显式刷新」改由 `pending` 在判定里直接跳过窗口。判定因此只剩「记录读取时间，和 watch 的最新数据时间比较」：版本没变不动；有基准且 `sampled.updatedAt + every > cell.updatedAt` 不换画面；没有基准（第一次读取／换身份／重新成为读者）直接读。适配层标志 3 → **2**（`released`、`pending`）。**必须保留的一个条件（明说）**：配置／生命周期边沿只在**此刻有资格**时才清基准；失去资格那一侧（暂停、失活、隐藏）不动——否则那一拍会把当前这一版重抄一次、顺手把 `pending` 清掉，于是「失活之前点过刷新、结果随后到达」的那一帧上不了屏（ADR-67 的角落、确认人「允许它更新一帧」的裁决；本轮第一版没加这个条件时那条用例当场转红）。**代价（明说）**：① 重新成为读者那一拍即使结果表里的版本没变，也会把同一份内容再发布一次（数据不变、`display` 换新对象）；② 这一轮减的是**概念**不是行数——结构分支 81 → **82**、`src/` 1155 → **1160** 行、`dist/index.js` 11557 → **11608 字节**。**仍然删不掉的**：读闸门的第二项只认 `pending`（「这一页点过刷新、还没看到那一拍」）——ADR-70 记了两次实测：换成 `immediate` 会让未上过屏的暂停页乱显示（真 Chrome 3 条红），换成「读取基准为空」会被一次不产出的副作用运行用掉。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **49/49**（core 36 ＋ vue 13）；`pnpm check:docs` 全项通过（叶子 19、测试标题 61）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过。**度量**：`src/` 1155 → **1160** 行、函数 81（不变）、结构分支 81 → **82**、`maxCCN` 7（不变）；`dist/index.js` 11557 → **11608 字节**（11.56 → **11.61** kB）；测试文件 `tests/core.test.ts` 的行数 1061（不变）、`tests/vue.test.ts` 的行数 550（不变）；包 45894 → **46311 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `0e399a1125542bfd85aac96460e3e715db3a286680366fe668e954e0f65c6f6d`，10 个条目哈希与工作树逐个一致）。无（已关闭）。**2026-09-18「删掉 `waiters`：刷新是给身份的命令（队头插入 ＋ `produced`／`needsNext`）」（ADR-70）交付记录**：确认人裁决「这个 waiter 怎么还没干掉啊，refresh 手动不管是谁最后反应的都是 resource 需要重新刷新吧，那不是只需要给 core 里面 queue 头部插入进入一个 resource 就行了吗，resource 以及 core 不关心是谁要 refresh，调用方根据数据新鲜度 超过了就加载，哦对了 这里手动刷新可以直接把上次读取时间置 null 不就行了」。**核心侧（照裁决落地）**：`Resource.waiters: Set<Config>` 删除，换成两个位——`produced`（这一轮的结果已经产出：写表之前置起、一轮结束清掉）与 `needsNext`（产出之后又有人点过刷新，本轮结束再排一次）；`refresh` 三条分支＝**没有执行 → 插到队头**（新增私有 `enqueueAtHead`：`queue` 是 `Set`，先 `place` 再按「它最前、其余保持原序」重建）／**有执行且结果未产出 → 什么都不做**（A14：在途结果就够，不追发）／**有执行且结果已产出 → 记一笔**；`settle`／`fail` 收成只记结算时刻与置位（不再交回「这一批要求」），私有 `settleRequest` 删除，`undeclare`／`submit`／`dispose`／`releaseIfUnused` 里的要求清理全部删除，`isWanted()` 收成一句话「还有声明者吗」。**语义变化（明说，一条）**：旧口径「一个页面同时只能有一次未满足的刷新请求」这个上界没了——产出之后点的每一次刷新都会补一轮（同一轮内点几次仍合并成一次，因为 `needsNext` 是一个位）。「每写一次结果就点一次刷新」的页面会一直取下去：页面自己的循环就是页面自己的循环，框架只保证同一轮内合并。这条不是纸面顾虑：旧用例（`A12/A14 结果写入期间新登记的刷新要求由后继请求满足`）在改动后**当场死循环**（`onWrite` 不断置位、全微任务链把事件循环饿死，`pnpm test` 60 秒超时），已按新语义改写（写表期间只点一次）并新增一条钉住新口径的用例（每收一次结果点一次 → 连取 4 轮，停手就不再取）。**读取面这一侧试了两次、都被实测打回（如实记）**：① 用 `immediate` 顶替读闸门 → 真 Chrome **3 条转红**（`shown(乙) === null, 'paused page stays frozen'`：从未上过屏的暂停页在结果到达时当场显示）；② 把 `sampled` 置 `null` 当那个位 → 失活那一拍 `eligible` 变化唤醒副作用、把当前旧格抄进去，`null` 当场消费掉，「点过刷新之后**才**失活、结果随后到达」的那一帧再也抄不进来（本轮新增的失活角落用例当场红）。结论：`sampled`（已抄到的那一版：引用比较 ＋ 节流基线）与「这一页还欠一次抄写」是**两件事实**，适配层保留 `pending`／`immediate`，读闸门与 ADR-66／ADR-67 逐字相同；核心侧删掉「谁在等」不受影响（适配层只对自己的页面记账）。**没做的（明说）**：把 `queue`／`running` 并进注册表——入队次序与占槽计数是两件别的事（ADR-68 已说明）。**取证**：旧 8 幕轨迹在新旧核心下**逐字节相同**（21 行 / 1889 字节）——旧行为面一行没动；随后给轨迹工具补上**队列顺序**（`queued=n[键序]`）与两幕新语义：S9「刷新插队头」（`queued=2[{"id":3},{"id":2}]`，实测 `c=30`、`b=2`）、S10「产出之后点两次只补一轮」；新基线 `.tools/trace-after.txt`（28 行 / 2747 字节）。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **49/49**（core 36 ＋ vue 13）；`pnpm check:docs` 全项通过（叶子 19、测试标题 61）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；`node scripts/benchmark.mjs` 重跑：取数中位 637 次（实测在 633–637 间波动）、收敛比 1、在途峰值 4、释放后残留全 0、事件循环延迟 mean 1.09ms／p99 1.80ms／max 10.47ms。**度量**：`src/` 1139 → **1155** 行、函数 80 → **81**、结构分支 82 → **81**、`maxCCN` 8 → **7**；`dist/index.js` 11600 → **11557 字节**（11.60 → **11.56** kB）；测试文件 `tests/core.test.ts` 的行数 1036 → **1061**、`tests/vue.test.ts` 的行数 439 → **550**；包 45014 → **45894 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `f96760681aae4b80c6b029a484b7d6491c1a02214846fe97709fdb50ceb232fb`，10 个条目哈希与工作树逐个一致）。无（已关闭）。**2026-09-18「守卫具名化 ＋ 差分轨迹工具」（ADR-69）交付记录**：确认人裁决「需要守卫具象化，但是单词要易懂、不要专有名词」（DESIGN §9.4 那张表里原本还写着「尚未裁决」）。**落地**：`Resource` 新增 `hasExecution()`（这个身份还有没有执行）、`isCurrent(controller)`（这次执行还是不是当前执行）、`isWanted()`（还有人要它吗）三个判定；`RefreshCore` 新增 `find(url, key)`（按身份键找实例，找不到＝身份不成立）；`run` 里三处裸比较改走 `isCurrent`，`enqueueDue`／`refresh`／`refill` 改走 `hasExecution`，`releaseIfUnused` 改走 `isWanted`，`refresh`／`isEligible` 改走 `find`。名字都是常用词，没有 `stale`／`generation`／`guard` 这类行话；写表那一刻的同步重入本来就不是 `if` 而是顺序（先交这批要求 → 写表 → 逐条结算），文档如实写明。**取证工具（挂了四轮的前置）**：新增 `scripts/trace-core.mjs`——8 个脚本化场景驱动核心，打印**只看结构、不看时间戳**的可观测轨迹（资源数／声明者数／在队／在跑／唤醒 Timer／结果表格子状态／写入次数）。改动前后各跑一遍：**21 行轨迹逐字节相同**（1889 字节），这是单测之外更强的「零行为变化」证据；工具同时进仓，以后任何改名／提取都能用它对比。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **46/46**（core 35 ＋ vue 11）；`pnpm check:docs` 全项通过（叶子 19、测试标题 58）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；`node scripts/benchmark.mjs` 重跑：取数中位 634 次、收敛比 0.99、在途峰值 4、释放后残留全 0。**度量**：`src/` 1114 → **1139** 行、函数 76 → **80**（三个新判定 ＋ `find`）、结构分支 82 不变、`maxCCN` 8 不变；`dist/index.js` 11441 → **11600 字节**（11.44 → 11.60 kB）；测试文件未动（`tests/core.test.ts` 的行数 1036、`tests/vue.test.ts` 的行数 439）；包 44218 → **45014 字节**（10 个条目，两次 pack 逐字节相同，SHA-256 `a69535780573f7d40048fedc7c201dff70fa11b592df528e32f54977aaf886a2`，10 个条目哈希与工作树逐个一致）。**没做的（明说）**：把 `queue`／`running` 并进注册表（那是入队次序与占槽计数两件别的事，ADR-68 已说明）。无（已关闭）。**2026-09-18「身份注册表拍成一层」（ADR-68）交付记录**：确认人看出「核心不需要多个字段持有 resource，只需要一个容器」——注册表真正多余的只是**第二层**。**落地**：`buckets`（`Map<url, Map<key, Resource>>`）→ **`identities`（`Map<身份键, Resource>`）**，键改由新导出的 `identityOf(url, key)` 构造（`url\u0000key`）——**结果表用的就是同一个键**，于是 `src/store.ts` 里的 `keyOf` 删除、改为复用 `identityOf`（一处格式，不再两处各拼一遍）。收益：少一层嵌套；`releaseIfUnused` 少一段「桶空了就删桶」的清理（现在只删一个键）；`all()` 就是 `[...identities.values()]`；三处查找（`resourceFor`／`refresh`／`isEligible`）仍是**一次命中**，读路径不变。**没做的（明说）**：换成 `Set<Resource>`——功能等价（`Resource` 自带身份），但三处查找变成扫描，其中 `isEligible` 在**读路径**上（每个页面每次结果写入各一次）；`queue`／`running` 也不能并进去：它们回答的是「入队次序」与「占了几格」两件别的事（`Set` 只保留首次加入的顺序，FIFO 会错位；槽位要计数）。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **46/46**（core 35 ＋ vue 11）；`pnpm check:docs` 全项通过（叶子 19、测试标题 58）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；`node scripts/benchmark.mjs` 重跑：取数中位 632 次、收敛比 0.99、在途峰值 4、释放后残留全 0。**度量**：`src/` 1117 → **1114** 行、函数 76（不变）、结构分支 88 → **82**、`maxCCN` 8 不变；`dist/index.js` 11637 → **11441 字节**（11.64 → 11.44 kB）；测试文件未动（`tests/core.test.ts` 的行数 1036、`tests/vue.test.ts` 的行数 439）；包 44110 → **44218 字节**（10 个条目，两次 pack 逐字节相同，SHA-256 `17dab90cfee069a9c87492a048649c813b5d24d9f407c37a4b8bd6e132f04e38`，10 个条目哈希与工作树逐个一致）。无（已关闭）。**2026-09-18「一个身份一本账 ＋ 读取面改写入驱动节流」（ADR-66、ADR-67）交付记录**：确认人这一轮给了两句——「身份 resource 才是合并请求的标准，终止也是终止这个，排队也是排对这个」（ADR-66）；面对并行会话落下的读取面实现，「对方改动的是正确的，它做完了……认下这个新口径，这比脉搏稳妥得多」（ADR-67，本轮补上它的裁决记录与四份文档）。**落地**：① ADR-66：`Config` 变成 `{ enabled, every: number｜null, active }`，**每页一个对象、适配层原地改写**，它同时就是这一页在核心里的登记（加入实例 `declarers`）；`Demand` 类型与全局名册（`RefreshCore.demands`、`addDemand`、`removeDemand`）删除；核心公开面收敛成十个（`isVisible`、无参 `reconcile()`、`isEligible`、`undeclare` 是新的）；读闸门搬到适配层（`core.isEligible(...) 或 (pending 且 core.isVisible())`），`isReader` 删除；换身份改成扫描注册表找旧位置（仍不存反向字段）。② ADR-67：读取面**零 Timer**——结果表写入驱动，按 `sampled.updatedAt + every > cell.updatedAt` 节流（一个 `every` 窗口最多换一版），三处不等窗口（身份落定、重新成为读者、显式刷新），首帧与失败不受节流；`pulse`／`setInterval`／`MAX_PULSE_DELAY` 全部删除。**代价（明说）**：写端密于 `every` 且 `every` 不是写入网格整数倍时，实际周期量化到写入网格；失活（KeepAlive 缓存）期间点过刷新的页面只要浏览器可见会在后台更新一帧（A04/A06 的验收不受影响）。**过程中抓到并修掉三件**：① 删 `coordinate` 时漏了「卸载／换身份要撤销未完成的要求」——补回并加用例（`A06 卸载撤销这一页未完成的要求`）；② 并行会话那条 `A21 写端稀于本页 every` 用例的基线在首帧之前读（`undefined + 2 = NaN`，`until` 只会空转到超时）——改成先等首帧，断言强度不变；③ 术语残留订正（`src/vue.ts`／`src/public-types.ts` 里「身份级一个位」「某一拍」）。④ 浏览器那条「双组件共享」的慢页面断言按新口径改写：节流按结果时间戳之差算窗口，窗口内到达的 102 会被**故意跳过**，所以「乙必须显示 102」在新口径下不可能成立——改成把乙自己那次 5 秒周期请求放行，断言它跳过中间版本后仍跟到新结果（验收内容不变：暂停不取消共享 I/O、乙继续收到新结果、暂停页冻结）。**试过并否掉的更激进版本**：把 `waiters` 也收成身份级一个布尔 → `A12/A14` 的补发用例死循环（写表期间的重入会反复置位），证明「哪个页面已有未满足要求」必须按页记。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **46/46**（core 35 ＋ vue 11）；`pnpm check:docs` 全项通过（叶子 19、测试标题 58）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；`node scripts/benchmark.mjs` 重跑：取数中位 632 次、收敛比 0.99、在途峰值 4、释放后残留全 0。**度量**：`src/` 1147 → **1117** 行、函数 81 → **76**、结构分支 91 → **88**、`maxCCN` 8 不变；`dist/index.js` 12291 → **11637 字节**（12.29 → 11.64 kB）；测试文件 `tests/core.test.ts` 的行数 978 → **1036**、`tests/vue.test.ts` 的行数 370 → **439**；包 44898 → **44110 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `8012cf06c9916c38246856dd66a08cb6525ad32f99454e69cc9f7d3c542fd915`，10 个条目哈希与工作树逐个一致）。无（已关闭）。**2026-09-18「拆环 ＋ 砍框架上限与 `Task` ＋ 结果表拍平」（ADR-65）交付记录**：确认人对「数据结构太多、职责不对、互相引用」的整体分析裁决「根据分析全部执行吧 概念合并，拆环 砍收 task」，并指出挂死请求由 axios／nginx 负责。**落地**：① **拆环**——`Resource` 不再持有核心（构造只剩 `(url, parameters)`），`settle(at)`／`fail(at)` 只记结算时刻并把这一批要求交回核心，写表、回收、排队全归核心，`clearRequest`／`refill` 删除、新增私有 `settleRequest`；`releaseIfUnused`／`writeResult`／`writeFailure`／`enqueue`／`place`／`run` 一并收成私有，**核心公开面 14 → 10**，**全库再无环**（实例不调核心、核心不调页面）。② **砍上限与 `Task`**——删除 `LOAD_TIMEOUT_MS` 与 `expire`，`runTask(task)` → `run(resource)`；`Task` 类型删除，`queue`／`running` 直接装 `Resource`，「这次执行」的取消与认人收到 `Resource.controller`，位置迁移四态 → `place(resource, queued／running／abandoned／idle)`。依据（明说）：请求必然终止由注入的传输负责（axios 的 `timeout`、反向代理或宿主自己的截止）；代价是一个永不结束的请求会占着它的并发槽直到传输自己结束，ADR-20 的 `10000` 毫秒与 A10 叶子随之作废。③ **概念合并**——`ResultCell` 拍平成 `data`／`updatedAt`／`error`／`failedAt` 四个平字段，`Entry`、`ResultRow`、`RefreshFailure` 三个类型删除，**公共类型 7 → 6**。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **42/42**（core 33 ＋ vue 9；A10 随能力移除）；`pnpm check:docs` 全项通过（叶子 20 → **19**，测试标题 55 → **54**）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；`node scripts/benchmark.mjs` 重跑：取数中位 633 次、收敛比 0.99、在途峰值 4、释放后残留全 0。**顺带**：`tests/scenarios.ts` 里名义上挂着 A10 的那条受控场景改名为 `A11 换身份建立新实例…`（A10 已不存在）；A06 用例补一条断言，钉住「被弃的在途请求仍占着槽位、真实结束后交还」（新的 `abandoned` 态）。**度量**：`src/` 1179 → **1147** 行、函数 83 → **81**、结构分支 94 → **91**、`maxCCN` 8 不变；`dist/index.js` 12702 → **12291 字节**（12.70 → 12.29 kB）；测试文件 `tests/core.test.ts` 的行数 979 → **978**、`tests/vue.test.ts` 的行数 367 → **370**；包 45280 → **44898 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `7d140897a9fa076d90b988a862e545c8af47dc560775efcd5eb6d3df43de6f0e`，10 个条目哈希与工作树逐个一致）。**没有做的（等点名）**：把身份也从需求上拿掉、让实例只持有一组 `Config`（理由见 ADR-64／ADR-65）；**甲**（三组守卫具名化）。无（已关闭）。**2026-09-18「核心不再认识页面」（ADR-64）交付记录**：确认人指出「根本问题核心不再认识页面好像还没做啊」——ADR-63 之后核心还剩三根线：`Handle.source`（定义对象）、`Handle.cleanup`（释放回调）、`submit` 的 `prepare` 闭包。**落地**：① `Handle` → `Demand`（`url` / `config` / `parameters`，纯数据、零回调），核心不再 import `RefreshSource`；`handles` → `demands`、`addHandle`／`removeHandle` → `addDemand`／`removeDemand`、`snapshot().handles` → `snapshot().demands`。② `submit(demand, parameters)`：复制、值域检查、身份键编码与 `validate` 全部搬进适配层的提交边界，核心没有 `try/catch`、也不产生 `rejected`（`rejected` 由适配层的 `submit` 就地返回）。③ `setCleanup` 删除（可见性监听由适配层在 `app.onUnmount` 与返回的 `dispose()` 里自己摘），模块私有的 `isolate()` 随之删除。④ `Resource.source` → `Resource.url`（一个字符串）。**结果**：`core.ts` 拨出去的外部调用只剩传输 `http.post` 与结果表 `sink.write／fail／remove／list` 两个**注入端口**——核心零回调，不执行任何调用方代码。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **43/43**（core 34 ＋ vue 9）；`pnpm check:docs` 全项通过（6 模块、叶子 20、测试标题 55）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过；`node scripts/benchmark.mjs` 重跑：取数中位 632 次、收敛比 0.99、在途峰值 4、释放后残留全 0。**顺带修掉 benchmark 两处过时投影**（`snapshot().handles` → `demands`；残留项改用 `results.length` 数结果表条目，原来读的是 ADR-59 就删掉的 `Resource.entry`）。**度量**：`src/` 1188 → **1179** 行、函数 88 → **83**、结构分支 97 → **94**、`maxCCN` **8 不变**；`dist/index.js` 12996 → **12702 字节**（13.00 → 12.70 kB）；测试文件 `tests/core.test.ts` 的行数 969 → **979**；包 45281 → **45280 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `19f468d505b46aa70305e85f90227cdc02e942e885da305603038bd4956ae3ae`，10 个条目哈希与工作树逐个一致）。无（已关闭）。**2026-09-18「读取面按每页 every 采样 ＋ 失败进结果表」（ADR-63）交付记录**：确认人裁决「页面按自己的频率读 pinia」与「失败写在 resource 上、由读取面取、成功就删掉」。**落地**：① 结果表每格改成 `ResultCell`（`entry` 与 `failure` 各自可空）；`ResultSink` 加 `fail`、`RefreshCore` 加第四个实例入口 `writeFailure`；`Resource.fail` 改成与 `settle` 同序地写这一格（保留 `entry`，成功清 `failure` 且整格换新对象）；`Handle.onError`、`report` 与通知循环删除（框架调的页面代码只剩 `validate` 与 `cleanup`，DESIGN §6.4 改写）。② 适配层新增采样脉搏（`setInterval(本页 every)`，只在愿意开且激活时装，`handle.cleanup` 与 `onScopeDispose` 都停），`display` 一拍最多发布一次、三处不等拍（身份落定／重新成为读者／显式刷新）；`RefreshDisplay` 加 `failure`、`data` 与 `updatedAt` 可空；`RefreshOptions` 删 `onError`；导出面从 3 函数加 6 类型变成 3 函数加 **7** 类型。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **43/43**（core 34 ＋ vue 9，新增两条：A21 采样、A13 失败读面）；`pnpm check:docs` 全项通过（叶子 19 → **20**，测试标题 53 → **55**，词汇 17 → 19）；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）类型 0 错误、**7 条**运行期场景通过（新增「慢页面不跟快页面跳、显式刷新不等拍」与「失败写进那一格：旧址保留、原始异常可读、成功后清掉」）。**三处按新语义改的浏览器断言（明说）**：A13 超时场景从「超时不交付」改成「交付失败且 `data` 为 `null`」；双组件共享场景在「重新进入」之前先用乙页的显式刷新拿一份**新鲜**结果（否则甲重新挂载时那份已经到期，到期的那次周期取数会把「重新挂载不强制新请求」测糊——采样让这一步的时序长了一个周期），并给共享页加了一个「刷新本页一次」按钮；两处 5 秒页断言显式给 15 秒预算。**顺带修掉一个 HEAD 就存在的运行期缺陷**：`scripts/benchmark.mjs` 没给 `createRefreshManager` 传 `pinia`（自 ADR-59 起必需），脚本一跑就抛 `getActivePinia()`——即上一轮记录的基线数据其实早于 ADR-59；本轮补上 `pinia` 后重跑并更新 README：取数中位 640 次、收敛比 1、在途峰值 4、释放后残留全 0。**度量**：`src/` 1094 → **1188** 行、函数 84 → **88**、结构分支 93 → **97**、`maxCCN` 6 → **8**（采样那一支进了 `useRefresh`）；`dist/index.js` 12263 → **12996 字节**（12.26 → 13.00 kB）；测试文件 `tests/core.test.ts` 的行数 949 → **969**、`tests/vue.test.ts` 的行数 269 → **367**；包 41748 → **45281 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `c5d3c73c497a1319e37328d4e32cc879b6d05436a8d864ad69dd826ad0097eac`，10 个条目哈希与工作树逐个一致）。**没有做的（等点名）**：设计 B/D（核心不再认识页面）；`LOAD_TIMEOUT_MS` 与 `expire`／`detached` 的取舍；**甲**（守卫具名化）——要先按新契约更新差分轨迹工具。本轮**没有**用差分轨迹：这是一次有意的语义变更，且 `.tools/trace-core.mjs` 仍停在 `load` 与 `publish` 的旧契约上。无（已关闭）。**2026-09-17「清理：拆 flush 四步、删不可达分支、report 收私有、§6.1 订正」（ADR-62）交付记录**：确认人「先清理干净」。落地：① `flush` 拆成四个私有方法——`coordinateAll` → `enqueueDue` → `startQueued` → `scheduleWakeup`，`flush` 只剩四行调用，全库 `maxCCN` **11 → 6**；② 删掉 `startQueued` 里「队列任务不再是当前执行就丢弃」那个分支（`queue` 的唯一写入者是 `placeTask`，它只在「就是当前执行」时入队），并用一条不变量探针 `assertQueueConsistent` 把前提钉住（新增第二个入队路径就会红）；③ `report` 收成模块私有（唯一调用点在 `Resource.fail`）；④ DESIGN §6.1 的「三项」订正为「两项」（那段讲的是 `options` 两个 `Ref` 的依赖收集边界，`active` 在 `readConfig` 之外读）。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **41/41**（core 34 ＋ vue 7）；`pnpm check:docs` 全项通过；`pnpm test:browser` 真 Chrome **12/12**；`build` 与 `build:demo` 通过；干净消费方（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）五条场景通过。**度量**：`src/` 1076 → **1094** 行、函数 79 → **84**、结构分支 94 → **93**、`maxCCN` **11 → 6**；`dist/index.js` 12151 → **12263 字节**（12.26 kB）；包 41192 → **41748 字节**（10 个条目，两次 pack 逐字节相同，SHA-256 `9070ea25c85cc6c06aa9fd6c8417effa46c45f43052a6136655609e6fd53fbf6`）。**没做（等点名）**：`LOAD_TIMEOUT_MS` ＋ `expire` 与 `detached`（砍了才是「只剩一个 `setTimeout`」，代价是挂死请求永久占槽，属产品取舍）；**甲**（守卫具名化）——要先按新契约更新差分轨迹工具再动。**被 ADR-62 取代的交付记录**：**2026-09-17「声明与资格两分」（ADR-61）交付记录**：确认人裁决「先一起做吧」——P1（`active` 并进配置快照）与 P2（声明与资格两分）合并成一次交付。**落地**：`Config` 变成 `{ enabled, every, active }`，`Handle.active` 与 `RefreshCore.activate`／`deactivate` 删除，适配层把两个 `Ref` 与这一页的激活状态合成一份快照写进 `Handle.config`（唯一写入口是一个 `flush: 'sync'` 的 watcher）；`Resource.subscribers` → **`declarers`**（成员资格＝声明了该身份，挂载期间一直算），登记在 `coordinate`、撤销只发生在卸载／换身份／销毁；资格不另存集合，由 `Resource.isEligible` 现算，`eligibleEvery(visible)`／`dueAt(now, visible)` 据此取最小间隔；`releaseIfUnused` 判据换成「`declarers` 与 `waiters` 都空」——**暂停、失活、隐藏都不再释放实例、不再删结果表条目、不再 abort 在途**；abort 只发生在卸载、换身份、销毁与框架上限到期。**行为变化**：失活／暂停／隐藏后恢复**直接读回已有结果、不重复取数**（「保留期」就是页面的挂载期，不需要 TTL）；失败通知面收窄到读者（暂停页不会收到它没要求过的失败）。**过程中抓到一个真缺陷**：`coordinate` 起初把「暂停」也算成撤销刷新要求的条件，于是暂停页刚登记的刷新被下一轮 flush 抹掉，那次结果到不了画面（A05／G6）；真 Chrome 的 `查询列表` 场景与一段 node 复现同时抓到。修法是把环境与意愿拆成两个判定：`isPresent`（激活且可见）与 `isEligible`（`isPresent && enabled`），**撤销要求只看环境**。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **41/41**（core 34 ＋ vue 7；其中 5 条按 ADR-61 的语义改写——隐藏/失活/暂停从「实例释放、结果消失」改成「声明还在：实例与结果都留着，只是不再取数」，失活恢复从「重查」改成「读回」）；`pnpm check:docs` 全项通过（6 模块、叶子 19、测试标题 53、17 组词汇）；`pnpm test:browser` 真 Chrome **12/12**（`tests/scenarios.ts` 里 5 条按新语义改写：暂停不再取消在途、换身份建立新实例、隐藏/失活只失去资格）；`build` 与 `build:demo` 通过。**干净消费方**（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）：类型 0 错误，五条运行期场景通过——两份同 URL 定义合并成一次 POST、请求体即参数值、**暂停不丢结果、恢复读回同一份结果对象且不重查**、两个读者拿到同一个对象、释放后页面保留最后一帧。**度量**：`src/` 1028 → **1076** 行、函数 77 → 79、结构分支 99 → **94**、`maxCCN` **11 不变**；`dist/index.js` 12231 → **12151 字节**（12.15 kB，本轮 −80）；包 39075 → **41192 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `4b2686d87b0e03ff5dff85c2ff2bd275828247853ebadf2a8d942379210e8d0f`，10 个条目哈希与工作树逐个一致）。**顺带保留**：本轮交付里包含 `src/store.ts` 的一处独立优化（结果表从「整张表一个 `shallowRef`」改成「每个 `(url, 参数键)` 一份独立 `shallowRef`」，写一格只唤醒订阅这一格的 watcher；cell ref 不删、只把值置回 `undefined`，避免依赖对象被换掉后画面停摆）——注意它导出的是一张**原始 `Map`**，因此 `pinia.state.value['vue-refresh']` 里看不到结果表，调试要用内部的 `list()`。**被 ADR-61 取代的交付记录**：**2026-09-17「暂停／失活后画面冻结」（ADR-60）交付记录**：确认人指出 ADR-59 的行为项 (a) 不对——「暂停、失活了为啥还在刷新，画面应该冻结了」。裁决：视图不是无条件按身份读结果表，而是**读者才跟随**——读者 ＝ 本页此刻订阅着该身份，**或**它上面有未撤销的刷新要求（`RefreshCore.isReader`，即 ADR-46 之前 A12 的「接收者」集合）；不是读者就**冻结在最后一帧**且不清空。为什么不按「订阅」单条件：A05／G6 要求暂停页仍可刷新一次，那一次它只有刷新要求没有订阅，按订阅判定会把暂停页自己刷出来的结果挡在闸门外。**落地**：`src/core.ts` 新增只读判定 `RefreshCore.isReader`；`src/vue.ts` 的 `display` 副作用在写入前加这道闸门，并在适配层自己发起的资格边沿（`activate`、配置 watcher）上 +1 个计数器让它重新判定。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **41/41**（core 34 ＋ vue 7，用例数与叶子前缀未变）；`pnpm check:docs` 全项通过（6 模块、叶子 19、测试标题 53）；`pnpm test:browser` 真 Chrome **12/12**——其中三条按冻结语义改回原口径（`双组件共享…单页暂停后画面冻结`、`A02/A06 真实HTTP：共享、暂停冻结画面…`），而 `A14 暂停后显式刷新`、`A05/A14 同一同步块…`、`行情面板…失活冻结`、`A04/A06 祖先 KeepAlive 失活` 这几条专门覆盖「暂停页自己刷新仍要更新」与「失活冻结」的场景同时全绿，说明闸门把两种情形分开了；`build` 与 `build:demo` 通过。**干净消费方**（TS 4.9.5 ＋ 真 `pinia@4.0.3` ＋ 真 `axios@1.13.2` ＋ 真实 HTTP）：类型 0 错误、五条运行期场景通过。**度量**：`src/` 1005 → **1028** 行（函数与结构分支不变，新增 `isReader` 与注释）、`dist/index.js` 11936 → **12231 字节**（12.23 kB）；包 38331 → **39075 字节**（10 个条目，两次 pack 逐字节相同，SHA-256 `3305556850e8afd90919cce5cf308b601b78c7813818398e769f208760045171`）。**边界（明说）**：浏览器可见性是全局的，隐藏会让所有页面同时失去订阅、实例通常当场释放、条目删除，因此由「没有条目就保留上一帧」兜住；若隐藏期间仍有未撤销的刷新要求，回到可见后画面在下一次结果到达时更新，最多滞后一个周期。**被 ADR-60 取代的交付记录**：**2026-09-17「结果表切到 Pinia」（ADR-59）交付记录**：确认人裁决「结果随实例清」「出口使用 `shallowRef`；如果调用方要改，让用户自己复制——也就是说同一对象」「axios 返回的结果直接写入 pinia，按照 url 分组，相同 url 按照稳定后的 key 分组，pinia 提供数据出口」之后的**阶段二**落地。落地：新增 `src/store.ts`（模块级 `defineStore('vue-refresh')`，`shallowRef` 装 `URL → 参数键 → Entry`、整条替换、写入端只给内核）；`Resource.entry` 删除，`Resource.publish` → `Resource.settle`，`deliverTo`／`deliverLatest`／`Handle.publish` 一并删除（框架不再调用任何页面交付回调）；内核新开第三个实例入口 `writeResult`，`releaseIfUnused` 改为删结果表条目；`display` 改成按已声明身份读结果表的 `shallowRef` 视图（`watchEffect` ＋ `flush: 'sync'`，`args` 每次复制、`data` 共享同一对象）；`snapshot()` 多一项 `results`；`createRefreshManager({ maxConcurrent, axios, pinia })`，`pinia` 升为 peer 依赖。**行为变化（明说）**：① 暂停／失活只停「由这一页驱动取数」，**不再冻结画面**（读共享表，别人刷新的结果它也跟着显示）；② 画面保留（keep-last）：条目随实例释放即删，但已挂载页面保留最后一次读到的画面；③ 数据是共享对象，就地改它对同身份所有读者可见、且不触发响应式更新；④ 框架不再有交付回调（A16 只剩 `onError`／`cleanup`），A12 的「接收者」改成「按身份读」，A11／A16 口径同步改写。**过程中抓到并修掉三个真缺陷**：`settle` 把 `waiters` 快照放在写结果之后（同步重入登记的要求被同一轮清掉，A12/A14 用例 `1 !== 2`）；`display` 用 `computed` 且首次求值在「还没有身份」时短路（结果表里已有条目而页面永久显示「等待首查」，浏览器 10 条场景转红）；「释放即删条目 ＋ 页面无副本」会让暂停页画面变空（与 A05／G6 冲突，补丁：适配层 keep-last）。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **41/41**（core 34 ＋ vue 7，用例数与叶子前缀未变，19 处「交付」断言映射为「按身份读结果表」）；`pnpm check:docs` 全项通过（叶子 19、测试标题 53、17 组词汇、6 个模块）；`pnpm test:browser` 真 Chrome **12/12**（三条断言按新语义改写：暂停页跟随共享结果、结果是共享对象、篡改对同身份读者可见）；`build` 与 `build:demo` 通过。**干净消费方**（`typescript@4.9.5` ＋ `vue@3.5.42` ＋ **真实 `pinia@4.0.3`** ＋ **真实 `axios@1.13.2`** ＋ 本包 tarball）：类型检查 0 错误；运行期对**真实 HTTP 服务**验证——两份同 URL 定义合并成一次 POST、请求体即参数值、结果表就在传入的 `pinia` 上且按 URL → 参数键只有一条、两个读者拿到**同一个对象**且就地改它对同身份读者可见、释放后结果表条目清空而页面保留最后一次画面。**度量**：`src/` 5 → **6** 个文件、895 → **1005** 行、函数 71 → **77**、结构分支 93 → **99**、`maxCCN` **11 不变**；`dist/index.js` 10921 → **11936 字节**（12.05 kB；`pinia` 已加进 `vite.lib.config.ts` 的 external，否则会被打进产物）；测试文件 测试文件 `tests/core.test.ts` 的行数 840 → **897**、`tests/vue.test.ts` 的行数 218 → **265**；包 34416 → **38331 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `5b3cb6000d0cf4a8350ed8aacdb185864e7e088a4ae87ae8302af4ab90a5ea39`，10 个条目哈希与工作树逐个一致）。**安装提示**：`pinia@4` 自己声明可选 peer `typescript >= 5.6`，因此 TS 4.9 工程安装本包时要用 `--legacy-peer-deps`（或 `overrides`）——本包自身的 6 个声明文件仍按 TS 4.9 校验（已实测）。**证据口径说明**：本轮**仍未**使用差分轨迹——该工具还停在阶段一之前的契约上（按 `load` ＋ `publish` 驱动），本轮没有更新它；调度不变量（合并、排队、并发上限、取消、迟到结果、隐藏、卸载）由 41 条单测与 12 条真 Chrome 场景覆盖。**被 ADR-59 取代的交付记录**：**2026-09-17「取数身份与发起方式：URL ＋ 参数值、框架发 POST」（ADR-58）交付记录**：确认人给出现实形态与目标之后的**阶段一**落地（阶段二＝结果表切到 Pinia，未开始）。落地：`RefreshSource` 只剩 `name`（取数 URL，必须非空）与可选 `validate`，`defineRefresh(url, { validate? })`；身份由「Source 对象 ＋ 参数键」改成「**URL ＋ 参数键**」，**同一个 URL 在几处各写一份定义也照样合并**（原来静默分裂成两个实例且没有任何检出）；取数改由框架发起——`RefreshCore` 构造收一个 `RefreshHttp`（只要求 `post(url, data, { signal })`，真实 axios 实例结构上满足它），`createRefreshManager({ maxConcurrent, axios })` 注入，因此 `core.ts` 仍是零运行时依赖、包里也没有 HTTP 客户端；`runTask` 发 `post(URL, 参数值的框架私有克隆, { signal })` 并取 `response.data`。`validate` 仍在定义里且**跟参数走**（每次 `submit` 由提交方自己那份定义执行），不参与身份。**代价（明说）**：① 响应结构校验不再由框架承担（原来 `load`／`transform` 里那一段移到传输侧，示例放在 `demoHttp`，接入方通常是 axios 响应拦截器）——2xx 但形状不对的响应现在会被当成成功并覆盖旧结果；② `T` 从「`load` 返回类型（证明）」变成「定义点声明」；③ 库从「来源无关」变成「HTTP POST 专用」（确认人确认所有取数都是 axios REST POST、没有非 HTTP 来源）。**未采纳的相邻提案**：不加 `admit()` 式同名冲突检出（`load`／`transform` 都不在定义里，已无「谁先接入谁生效」的东西可比）；不为「未订阅也能读」新开公开读入口（导出面仍是 3 函数 ＋ 6 类型）。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **41/41**（新增「A20 同一个 URL 在两处各声明一份定义仍然合并」，core 34 ＋ vue 7）；`pnpm check:docs` 全项通过（叶子 18 → **19**、测试标题 52 → **53**、17 组词汇）；`pnpm test:browser` 真 Chrome **12/12**（GET→POST 之后八条受控场景与三条代表页面全绿，含真实 HTTP 共享、传输超时、旧响应晚到、卸载清理）；`build` 与 `build:demo` 通过。**干净消费方**（`typescript@4.9.5` ＋ `vue@3.5.42` ＋ **真实 `axios@1.13.2`** ＋ 本包 tarball）：类型检查 0 错误（真实 axios 实例满足 `RefreshHttp`；含「缺 URL」「`Date`／`Map` 值域」三条反向探针），运行期对**真实 HTTP 服务**验证——两份同 URL 定义合并成**一次 POST**、请求体就是参数值 JSON、两个页面各交付 `price=3`、值域外容器参数 `rejected`、销毁后 `submit` → `cancelled`（不带 `reason`）；装进去的 `dist/index.js` 就是本次的 10921 字节。**度量**：`src/` 863 → **895** 行、函数 71（不变）、结构分支 92 → **93**、`maxCCN` **11 不变**；`dist/index.js` 10752 → **10921 字节**（10.75 → 10.92 kB）；测试文件 `tests/core.test.ts` 的行数 825 → **840**（用例数 34）；包 33080 → **34416 字节**（两次 pack 逐字节相同，SHA-256 `d08afd877dac35742a6c277b4a04868213456dbabe4a461d01f0bde70cb394dd`，9 个条目哈希与工作树逐个一致，README 的编辑全部发生在打包之前）。**证据口径说明**：本轮**没有**用差分轨迹——可观测的取数发起方式与身份语义是**有意改变**的，逐字节轨迹不是合适的工具；受影响的调度不变量（合并、排队、并发上限、取消、迟到结果、卸载清理）由 41 条单测与真 Chrome 12 条场景覆盖；阶段二（结果表）会用差分轨迹证明调度未变。**被 ADR-58 取代的交付记录**：**2026-09-17「订阅关系只留一处事实、配置快照改字段」（ADR-57）交付记录**：确认人裁决「② → ③ 分两次走」，本轮是 ③。落地：删掉 `Handle.subscription`——订阅关系此后只有一处事实（实例侧 `subscribers` 的成员资格），`coordinate` 接入、`unsubscribe` 退出都改走按身份查桶；`handle.config` 由闭包 `() => snapshot` 改成普通字段（`vue.ts` 少一个变量，`Handle` 成为纯数据记录）。**过程中发现两件真东西**：其一，DESIGN §3.1 声称「没有第二个对象描述同一条关系，因此不存在『两侧一致』这类不变量」，而 §3.5 第 1 条列的正是那样一条不变量，两句自相矛盾——随字段一起修掉；其二，`removeHandle` 原先「先清 `parameters`、后 `unsubscribe`」，旧的反向索引让退订不必知道身份，字段一删这条顺序就会让退订查不到实例（句柄留在 `subscribers`、实例永不回收、不 abort、**已卸载的页仍收到迟到结果**），修法是把 `unsubscribe` 提到清空身份之前。**这个缺陷由两条独立探测器同时抓到**：差分轨迹的「旧顺序」变体与正确版有 4 处差异，既有用例 3 条转红（A06、A06/A11、A17）。**零行为变化的证据**：`.tools/trace-core.mjs` 的 8 个场景 **55 行**可观测轨迹（新增 S7 配置变化、S8 卸载后迟到结果）在新旧 `core.ts` 下**逐字节相同**；`pnpm typecheck` 0 错误、`pnpm test` **40/40**、`pnpm check:docs` 全项通过（叶子 18、测试标题 52、17 组词汇）、`pnpm test:browser` 真 Chrome **12/12**、`build` 与 `build:demo` 通过。**测试探针变强而非变弱**：8 处断言原先读句柄上的副本，现在读实例侧 `subscribers`（新增 4 行 `subscribed()` 探针），测试文件 `tests/core.test.ts` 的行数 817 → 825（用例数仍是 33）。度量：`src/` 869 → **863** 行、函数 72 → 71、结构分支 93 → **92**、`maxCCN` **11 不变**；README 度量行由 `pnpm complexity --write` 回写，产物尺寸行 10832 → **10752 字节**（10.83 → 10.75 kB，本轮 −80；②＋③ 合计 10577 → 10752）。包 `vue-refresh-0.0.0.tgz`（**33080 字节**，SHA-256 `23808ce698f24e28889887e697fdfa36103b813090aa3809f1503ee17e81fa85`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `pack` 逐字节相同，9 个条目的哈希与工作树逐个一致（README 的编辑全部发生在打包之前）；干净消费方在 `typescript@4.9.5` 下零错误、三条运行期场景通过（`submit` → `price=3`；值域外容器参数 → `rejected`；销毁后 `submit` → `cancelled` 且不带 `reason`），装进去的 `dist/index.js` 就是本次的 10752 字节。**门禁未动**：`check-docs.mjs` 的 `INTERNAL_FIELDS` 里 `subscription` 保留原样（那张表只禁止规范 §0 命名内部字段，删条目只会削弱它）。**被 ADR-57 取代的交付记录**：**2026-09-17「任务位置收成一个写入点」（ADR-56）交付记录**：确认人裁决「② → ③ 分两次走」，本轮是 ②。落地：`src/core.ts` 新增私有 `placeTask(task, position)`（`queued`／`running`／`detached`／`settled`），把 `Resource.task`、`queue`、`running` 三处状态由「4 个函数各写一处」收成**一个写入点**，5 个调用点（`enqueue`、`flush` 起跑、`runTask` 的 `finally`、`expire`、`releaseIfUnused`）全部改走它；`detached` 是给 `releaseIfUnused` 的新位置——实例已回收、当前执行已撤销，但在跑的那次仍占着并发槽位，直到迟到的结束自己交还。**零行为变化的证据**：`.tools/trace-core.mjs` 用同一套脚本化场景分别驱动 HEAD 与新 `core.ts`，输出 43 行可观测轨迹（load／abort／`snapshot()` 投影／交付／错误）**逐字节相同**，覆盖 5 个调用点与 `detached`／`settled` 两条丢弃分支（S3 实测「卸载在跑的页」后实例 0、在跑 1）；`pnpm typecheck` 0 错误、`pnpm test` **40/40**、`pnpm check:docs` 全项通过（叶子 18、测试标题 52、17 组词汇）、`pnpm test:browser` 真 Chrome **12/12**、`build` 与 `build:demo` 通过。度量：`src/` 856 → **869** 行、函数 71 → 72、结构分支 89 → **93**、`maxCCN` **11 不变**；README 度量行由 `pnpm complexity --write` 回写，产物尺寸行 10577 → **10832 字节**（10.58 → 10.83 kB）。包 `vue-refresh-0.0.0.tgz`（**33094 字节**，SHA-256 `d3a1eba0fc036076975e7e8b86fbfb95005c273f3ceb07f8d66ca1b6d6328c77`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `pack` 逐字节相同，9 个条目的哈希与工作树逐个一致（README 的编辑全部发生在打包之前）；干净消费方在 `typescript@4.9.5` 下零错误、三条运行期场景通过（`submit` → 交付 `price=3`；值域外的容器参数 → `rejected`；销毁后 `submit` → `cancelled` 且不带 `reason`），装进去的 `dist/index.js` 就是本次的 10832 字节。**代价（明说）**：`dist/index.js` ＋255 字节（10.58 → 10.83 kB）、`dist/core.d.ts` 11123 → 11629 字节——换来的是三处状态不再有第二个写入者。**顺手订正**：DESIGN §3.3 的 Resource 行原写「核心只在跨实例边界读写 `task` / `entry`」，与代码不符（核心还增删 `subscribers`／`waiters`），已改为核心实际越界的四个字段。**被 ADR-56 取代的交付记录**：保留 `pnpm complexity` 与基线测量作为回归证据；**2026-09-17「注释分流与读代码导读」（ADR-55）交付记录**：确认人裁决「先做 1」，即本轮只做文本面（注释与文档），不碰任何逻辑。落地：`src/` 五个文件的注释按「第一句＝做什么、理由下沉到 DESIGN／ADR」重排，注释行 289 → **232**（−57，全部减在注释上），代码行 532、空行、函数数一行未动；`DESIGN.md` 新增 §9「读这个库的顺序」（三条进入路线、读前先记住的四个词、每个符号一句话作用、「遇到 `if` 时它在防什么」的三组守卫对照表），文首顺序建议与 §3.3 原「`src/core.ts` 的接口注释是权威」改为指向 §9 与 §3.3 本表。**零行为变化的证据（两条独立）**：按 `check-docs.mjs` 的 `stripComments` 口径逐文件比对，五个源文件的**非注释行逐行相同**（5／31／44／365／87 行）；`dist/index.js` **10577 字节、SHA-256 `f1710b19e206cc25…` 与 ADR-54 逐字节相同**，函数数 71、结构分支 89、`maxCCN` 11 不变。README 度量行由 `pnpm complexity --write` 回写为 **856 行**（913 → 856）。门禁 `pnpm typecheck` 0 错误、`pnpm test` 40/40、`pnpm check:docs` 全项通过（叶子 18、测试标题 52）、`pnpm test:browser` 真 Chrome 12/12、`build` 与 `build:demo` 通过。包 `vue-refresh-0.0.0.tgz`（**32234 字节**，SHA-256 `90fe2eca8174377b2bd765ec70e0074202a71c1cec45d8b48f4ecbf28bf45c37`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `pack` 逐字节相同；9 个条目与工作树逐个哈希一致（README 的编辑全部发生在打包之前）；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → 交付 `price=3`；值域外的容器参数 → `rejected`；销毁后 `submit` → `cancelled` 且不带 `reason`；运行期导出面恰好是 `createRefreshManager,defineRefresh,useRefresh`）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 10577 字节。**代价（明说）**：`dist/*.d.ts` 随注释一起瘦身（`core.d.ts` 12737 → 11123、`source.d.ts` 3173 → 2080、`public-types.d.ts` 4730 → 4335、`index.d.ts` 939 → 682；`vue.d.ts` 807 → **927**，因为三个公开函数之一的 `useRefresh` 此前没有任何说明），消费方在编辑器里悬停看到的是「一句话作用」而不是整段理由——理由仍在 `DESIGN.md` 与 `ADR.md`（两者都不进包）。**被 ADR-55 取代的交付记录**：**2026-09-17「句柄三组角色归位、删掉 `Handle.disposed`」（ADR-54）交付记录**：包 `vue-refresh-0.0.0.tgz`（34868 字节，SHA-256 `252dcaba04c730c736e8daf37e0bc698a436f07835f4e7ae1f517bd2238f9ea0`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `pack` 逐字节相同；`dist/index.js` 10.58 kB（10577 字节，哈希 `f1710b19e206cc259f1c35a8f95ee1a7f0e8251c8bf7f10900c667a80e729564`）；包内 `package/README.md` 与工作树哈希一致（`05aae81762b744691788d93311092d36e8928f4018c013fb5ace88593763b944`，README 的编辑全部发生在打包之前）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 40/40（core 33 ＋ vue 7）、`pnpm check:docs` 全项通过（叶子 18、测试标题 52）、`pnpm test:browser` 真 Chrome 12/12、`build` 与 `build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → 交付 `price=3`；值域外的容器参数 → `rejected`；销毁后 `submit` → `cancelled` 且不带 `reason`；运行期导出面恰好是 `createRefreshManager,defineRefresh,useRefresh`）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 10577 字节。**被 ADR-54 取代的交付记录**：ADR-53（值域声明形式与显式 `undefined` 的文档订正）34066 字节 / `05ae275d5efa0b5c82d06ac19011f881a105700d88d083bf64f2a15b138e26ad`（10552 字节）。ADR-53 那次交付的完整记录保留如下——**2026-09-17「值域声明形式与显式 `undefined` 的文档订正」（ADR-53）交付记录**：源码与 `dist/` 未改（`src/` 905 行／89 结构分支／`maxCCN` 11、`dist/index.js` 仍 10552 字节），改的是包内 README（`U15` 补「声明形式」一句；README 第 53 行订正显式 `undefined` 的说法并补同一条嵌套写法说明），按 ADR-49 纪律全部发生在打包之前。包 `vue-refresh-0.0.0.tgz`（34066 字节，SHA-256 `05ae275d5efa0b5c82d06ac19011f881a105700d88d083bf64f2a15b138e26ad`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `pack` 逐字节相同；包内 `package/README.md` 与工作树哈希一致（`51c6e47574120d…`）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 40/40（core 33 ＋ vue 7）、`pnpm check:docs` 全项通过（叶子 18、测试标题 52、17 行词汇表、0 类型回边）、`pnpm test:browser` 真 Chrome 12/12、`build` 与 `build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → 交付 `price=3`；值域外的容器参数 → `rejected`；销毁后 `submit` → `cancelled` 且不带 `reason`；运行期导出面断言通过）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 10552 字节。**被 ADR-53 取代的交付记录**：ADR-52（参数值域与参数隔离）33960 字节 / `ad1cf2df35acab4c2b358961720ac8c3fe5c2cc68d25f250652b844430937692`（10552 字节）。ADR-52 那次交付的完整记录保留如下——**2026-09-17「参数值域与参数隔离」（ADR-52）交付记录**：包 `vue-refresh-0.0.0.tgz`（33960 字节，SHA-256 `ad1cf2df35acab4c2b358961720ac8c3fe5c2cc68d25f250652b844430937692`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `pack` 逐字节相同；`dist/index.js` 10.55 kB（10552 字节）；包内 `package/README.md` 与工作树哈希一致（`a3459104527928c0…`，照 ADR-49 写下的纪律，README 的编辑全部发生在打包之前）；门禁 `pnpm typecheck` 0 错误（含新增的参数值域反向探针）、`pnpm test` 40/40（core 33 ＋ vue 7）、`pnpm check:docs` 全项通过（新增第 17 组「退役术语」；叶子 17 → 18、测试标题 51 → 52）、`pnpm test:browser` 真 Chrome 12/12、`build` 与 `build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误（新增 `Date`／`Map` 两条 `@ts-expect-error`）、客户端场景通过（`submit` → 交付 `price=3`；值域外的容器参数 → `rejected`；销毁后 `submit` → `cancelled` 且不带 `reason`；运行期导出面恰好是 `createRefreshManager,defineRefresh,useRefresh`）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 10552 字节。**被 ADR-52 取代的交付记录**：ADR-51（onError 只报取数失败、删掉 RefreshError 与最后一个常量对象）31737 字节 / `ba466e55319f763b9a11530b307598a3a688820d8c7dc7e50fef3857f6b5a090`（10211 字节）。ADR-51 那次交付的完整记录保留如下——**2026-09-17「onError 只报取数失败、删掉 RefreshError 与最后一个常量对象」（ADR-51）交付记录**：包 `vue-refresh-0.0.0.tgz`（31737 字节，SHA-256 `ba466e55319f763b9a11530b307598a3a688820d8c7dc7e50fef3857f6b5a090`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `pack` 逐字节相同；`dist/index.js` 10.21 kB（10211 字节）；包内 `package/README.md` 与工作树哈希一致（`947545af3bd8f688…`，照 ADR-49 写下的纪律，README 的编辑全部发生在打包之前）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 39/39、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12、`build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → 交付 `price=3`；销毁后 `submit` → `cancelled` 且不带 `reason`；运行期导出面恰好是 `createRefreshManager,defineRefresh,useRefresh`）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 10211 字节。**被 ADR-51 取代的交付记录**：ADR-49（导出面计数与用例数进门禁）34815 字节 / `47fbfeb0…c57b84`（10665 字节）。ADR-49 那次交付的完整记录保留如下——**2026-09-17「导出面计数与用例数进门禁」（ADR-49）交付记录**：包 `vue-refresh-0.0.0.tgz`（34815 字节，SHA-256 `47fbfeb0cb43d62ead02d49fe169266c6b77ef270963d3c692a74bbddcc57b84`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `pack` 逐字节相同；`dist/index.js` 10.66 kB（10665 字节，与 ADR-48 相同——本条只改 `scripts/check-docs.mjs`，而 `scripts/` 不进包）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 39/39、`pnpm check:docs` 全项通过（新增导出面计数与用例数两组断言；7 条反向探针逐条变红后复原）、`pnpm test:browser` 12/12、`build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → 交付 `price=3`；销毁后 `submit` → `cancelled` 且不带 `reason`），装进去的 `dist/index.js` 就是本次的 10665 字节。**本条订正 ADR-48 的交付记录**：它记的 34792 字节 / `89aefd6e…` 是**在 README 最后一处编辑之前**打出来的包（那次编辑改了消费方的 `@ts-expect-error` 清单，而 README 被 npm 强制打进包内），与提交树不符；本条按当前树重打包，逐条目比对确认 9 个条目里 **8 个与 ADR-48 的 tarball 逐字节相同、只有 `package/README.md` 不同**（`ed7b04e6981a…` → `b1d28ef58a10…`），并连跑两次验证可复现。**纪律**：README 在包内，任何 README 编辑都必须发生在打包之前。**被 ADR-49 取代的交付记录**：ADR-48（错误来源按调用方的动作分两侧）34792 字节 / `89aefd6e…c253a7b`（10665 字节；其 README 是发布前版本，已由本条订正）。ADR-48 那次交付的完整记录保留如下——**2026-09-17「错误来源按调用方的动作分两侧、删掉 `CancelReason`」（ADR-48）交付记录**：包 `vue-refresh-0.0.0.tgz`（34792 字节，SHA-256 `89aefd6ed89f3e7d12f6c6a89c2aa464c1613161ccfcd03088cb5b99ec253a7b`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `build` ＋ `pack` 逐字节相同；`dist/index.js` 10.66 kB（10665 字节）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 39/39、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12、`build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → 交付 `price=3`；销毁后 `submit` → `cancelled` 且不带 `reason`）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 10665 字节。**被 ADR-48 取代的交付记录**：ADR-47（清掉无消费者结构）34946 字节 / `ae59749a…aa5dee`（10801 字节）。ADR-47 那次交付的完整记录保留如下——**2026-09-17「清掉无消费者结构：subscription.every、parameterKey 导出、验证台死方法与声明代次轴」（ADR-47）交付记录**：包 `vue-refresh-0.0.0.tgz`（34946 字节，SHA-256 `ae59749a3f2d98f92ab4bc710ef1527b3bfc3c34ad06f0b993fe0d01eaaa5dee`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `build` ＋ `pack` 逐字节相同；`dist/index.js` 10.80 kB（10801 字节）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 39/39、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12、`build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → 交付 `price=3`）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 10801 字节。**被本条取代的交付记录**：ADR-46（删掉读取出口与刷新回执）35854 字节 / `7ba34b33…befbf`（11120 字节）。ADR-46 那次交付的完整记录保留如下——**2026-09-17「删掉读取出口与刷新回执」（ADR-46）交付记录**：包 `vue-refresh-0.0.0.tgz`（35854 字节，SHA-256 `7ba34b33c32915ebc8bbbc85ba3bded7caef4809a1458a8733665bf71f9befbf`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `build` ＋ `pack` 逐字节相同；`dist/index.js` 11.12 kB（11120 字节）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 39/39、`pnpm check:docs` 全项通过（叶子 18 → 17）、`pnpm test:browser` 12/12、`build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → 交付 `price=3`；`readSnapshot` 的断言随入口一并移除）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 11120 字节。**被 ADR-46 取代的交付记录**：ADR-45（删掉任务版本编号）37485 字节 / `d0e21f69…0e63`（12067 字节）。ADR-45 那次交付的完整记录保留如下——**2026-09-17「删掉任务版本编号」（ADR-45）交付记录**：包 `vue-refresh-0.0.0.tgz`（37485 字节，SHA-256 `d0e21f690dd6e6230c871a136c65e20db2803d3204bb8f5a6df1f67389d30e63`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `build` ＋ `pack` 逐字节相同；`dist/index.js` 12.07 kB（12067 字节）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 38/38、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12、`build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → `price=3`、`readSnapshot` 可读）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 12067 字节。**被 ADR-45 取代的交付记录**：ADR-44（删掉六处恒不触发的守卫与 `registered` 入口）37705 字节 / `439b5847…62a1`（12158 字节）。ADR-44 那次交付的完整记录保留如下——**2026-09-17「删掉六处恒不触发的守卫与 `registered` 入口」（ADR-44）交付记录**：包 `vue-refresh-0.0.0.tgz`（37705 字节，SHA-256 `439b584754698468682ad95d1ce48b8e6f906f8689602b8397783a67e0df62a1`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `build` ＋ `pack` 逐字节相同；`dist/index.js` 12.16 kB（12158 字节）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 38/38、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12、`build:demo` 通过；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → `price=3`、`readSnapshot` 可读）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 12158 字节。**被 ADR-44 取代的交付记录**：ADR-43（删掉恒真的版本门槛）37404 字节 / `26a150d8…dcc8e5`（12326 字节）。ADR-43 那次交付的完整记录保留如下——**2026-09-17「ADR-40 的收尾：删掉恒真的版本门槛」（ADR-43）交付记录**：包 `vue-refresh-0.0.0.tgz`（37404 字节，SHA-256 `26a150d8a27c8702b9f64778e200b6a5f8167f5cab54e29f1e771dd634dcc8e5`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `build` ＋ `pack` 逐字节相同；`dist/index.js` 12.33 kB（12326 字节）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 37/37、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → `price=3`、`readSnapshot` 可读）——安装前删掉 `node_modules/vue-refresh` 与 lockfile、用全新 `--cache`，并确认装进去的 `dist/index.js` 就是本次的 12326 字节（照 ADR-42 订正过的验证方法）。**被 ADR-43 取代的交付记录**：ADR-42（`Resource` 直接持有核心）37299 字节 / `b055f4df…a73cc`（12415 字节）。ADR-42 那次交付的完整记录保留如下，其中订正的验证方法仍然有效——**2026-09-17「Resource 直接持有核心、删掉 `ResourceHost`」（ADR-42）交付记录**：包 `vue-refresh-0.0.0.tgz`（37299 字节，SHA-256 `b055f4df5fdf7b3f39dd7d25074dca9c47bd4a8811da6fb719fc2e64429a73cc`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `build` ＋ `pack` 逐字节相同；`dist/index.js` 12.41 kB（12415 字节）；门禁 `pnpm typecheck` 0 错误、`pnpm test` 37/37、`pnpm check:docs` 全项通过、`pnpm test:browser` 12/12；干净消费方在 `typescript@4.9.5` 下零错误、客户端场景通过（`submit` → `price=3`、`readSnapshot` 可读）。**ADR-42 订正一项验证方法**：此前的消费方检查被固定装置的 `package-lock.json` 整定（integrity）钉在旧 tarball 上——`npm install` 报 up to date、装进去的 `dist/index.js` 仍是 12259 字节的旧产物；现在改为删掉该目录与 lockfile、用全新 `--cache` 安装，确认装进去的是 12415 字节的新产物，再跑类型与运行期检查。**被 ADR-42 取代的交付记录**：ADR-41 时 37909 字节 / `8bec8153…9e72af`（12599 字节）；ADR-40 时 36388 字节 / `a9cef8a0…d6c7da`（`dist/index.js` 12259 字节）；ADR-38 ＋ ADR-39 时 36356 字节 / `ec4ec42b…eb929`（`dist/index.js` 12370 字节）；ADR-38 单独落地时 36172 字节 / `3f595a85…`；2026-09-16 根契约重写时 35566 字节 / `54802a84…`；性能基线 load 中位 635 次、收敛比 0.99、在途峰值 4、释放后残留 0（详见工程 README「实际验证与边界」）。哈希记在本表而不写进 README：README 被 npm 强制打进包内，包自身哈希无法写在包内文件里。上一版（根契约重写前）的 63665 字节 / `826c5008…` 等记录已由 ADR-42 取代 **2026-09-18「读取面随协调者退场 ＋ 观测面搬出核心」（ADR-78、ADR-79）交付记录**：确认人先问「什么情况会销毁协调者」，核清写入点只有两处（`install` 的 `app.onUnmount` 与 `manager.dispose()`，见 `src/vue.ts`）后裁决「另一个会话已经结束了」，并对本轮问题给出「协调者理论上只有 app 整个销毁才会出现吧，所以为啥要考虑销毁但是页面还活着」这句口径校正，再裁决按 **B** 收：两个会话的改动**分两条 ADR 一起落**。**① ADR-78（读取面随协调者退场）**：缺陷是探针实测出来的——`manager.dispose()` 之后结果表条目已清空（`results=[]`），而页面读到的 `display` 仍是销毁前那一帧，且同一次重跑里 `failure` 已清成 `null`、`display` 却冻住（两个出口对同一事实给出相反答案）；只有三个真实来源：HMR（`examples/main.ts` 两处热更新）、调用方主动 `dispose()`、测试与基准脚本（纯 SPA 生产路径里 App 卸载时页面一起销毁，看不见）。修法是一处依赖：**模块级的安装槽从普通 `let` 改成 `shallowRef`**（普通 `let` 被读进副作用不登记依赖——这一点是用例先逼出来的：退回普通 `let` 时接管那条用例当场红），读取副作用第一件事读 `installed.value`，协调者退场置空时副作用重跑，两个出口一起清回 `null`（内联三行）；**另修一条同一根源的缺陷**：页面在 `setup` 里捕获的那个协调者销毁后不会更新，而新协调者也**不接旧协调者手里的配置槽**——旧槽的 `every` 还是有效值、新资源却没看见它，于是接管后这一页一次也不取数（干净消费方的运行期场景先抓到）。落法是每页记一个 `slot`（此刻报给谁）＋ `reportConfig()` 单一写入口：读取副作用发现 `installed` 换了人就把 `slot` 换掉（这一行放在**任何提前返回之前**），配置 watcher 的取值函数把 `slot` 也带进依赖，于是新协调者拿到重新读过的那份配置（`enabled`／`every`／`active`）。**语义变化（明说）**：`currentCore()`（不在包导出面）销毁后回 `null`，不再回已销毁的实例（仓内所有调用点都在协调者活着时读它）。**顺手清掉一处同源重复**：身份键的拼（`identityOf`）与拆（原在 `store.ts` 的 `list()` 里自己认 NUL）合成一对，新增 `splitIdentity`。**② ADR-79（观测面搬出核心）**：删掉 `RefreshCore.snapshot()`，只读计数投影改成 `tests/support/observe.ts` 的纯函数（四个字段不变），核心公开成员 10 → 9、`core.ts` 分段 6 → 5（README／DESIGN 的分段列表已同步）；越界读私有账本显式化并加**形状校验**（内部改名当场抛错，不静默返回空）；四个使用方（`tests/core.test.ts`、`tests/vue.test.ts`、`scripts/trace-core.mjs`、`scripts/benchmark.mjs`、`examples/main.ts`）改名接入；顺手删掉 `scheduled`／`flushing` 两个推得出来／已无人读的投影字段。**代价（明说）**：示例面板与基准脚本现在从 `tests/support/` 导入投影（示例与文档不进包，暂可接受；将来示例要独立分发就得给它一个明确的开发用出口）。**取证与门禁**：`dispose()` 前后实测（修前 `display` 有帧／`results=[]`，修后 `display=null`／`failure=null`）；新增两条用例（`A17 协调者销毁后读取面一起清空…`、`A20 身份键的拼与拆是同一处规则…`）；`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **54** 全绿（`tests/core.test.ts` 当时 37、`tests/vue.test.ts` 当时 17）；`pnpm check:docs` 全项通过（19 个叶子、66 条测试标题、核心分段与文档一致）；`pnpm test:browser` 真 Chrome **12/12**；`build`／`build:demo` 通过；`node scripts/benchmark.mjs`：取数中位 632、收敛比 0.99、在途峰值 4、释放后残留全 0、事件循环延迟 p99 1.77ms／max 7.02ms；`dist/core.d.ts` 不再有 `snapshot`（已核），多了 `splitIdentity`。**未用差分轨迹**：两条改动的可观测面都在适配层与测试侧，核心的调度行为一行未动，轨迹工具对它恒等，跑它不构成证据。**订正**：另一个会话把观测面外移记成了「随 ADR-74」（`DESIGN.md` §3.3／§4.2／§9.3 与本节 §2.4 四处），已改成 ADR-79。**度量**：`src/` 1155 → **1193** 行、函数 76 → **77**、结构分支 82 → **84**、`maxCCN` **6**（不变）；`dist/index.js` 10970 → **11259 字节**（10.97 → **11.26** kB）；测试文件 `tests/core.test.ts` 的行数 1060 → **1071**、`tests/vue.test.ts` 的行数 639 → **730**；包 **48807 字节**（10 个条目 = `dist/` 8 ＋ `package.json` ＋ README，两次 pack 逐字节相同，SHA-256 `7a51cf53db7331ec13a04694ab58bb7c0692e36c24a5b1a71a10dcda694262b3`，10 个条目哈希与工作树逐个一致）。无（已关闭）。**2026-09-18「注释只留一句做什么」（ADR-80）交付记录**：**采纳并已落地**。确认人：「检查需求是否现在都是我后来给你明确的，把注释里面大量无效解释全删了」。**改动**：`src/` 六个文件的注释按「第一句＝做什么」重排，删掉设计理由、ADR／DESIGN 引用、用例编号与举例。注释行 **516 → 157**、`src/` 总行 **1193 → 834**。**代码零改动**（按 `check-docs.mjs` 的 `stripComments` 口径比对：六个文件的非注释行逐行相同），函数与结构分支不变，`dist/index.js` 仍是 **11259 字节**（打包时注释已被剥离）；变了的是 6 个 `.d.ts`（随注释瘦身，包 48807 → **33430 字节**）。**门禁**：`pnpm typecheck` 0 错误、`pnpm test` **54** 全绿、`pnpm check:docs` 全项通过（`RegExp`／`ArrayBuffer`／`NaN` 三个被文档反引号的符号按门禁保留在 `src/source.ts` 的值域说明里）。**度量**：`src/` 1193 → **834** 行、结构分支 84（不变）、`maxCCN` 6（不变）；包 **33430 字节**（10 个条目，SHA-256 `91d7066189dd33f813f5c66c257e63c10478212c6681444b4f5615a85edcf692`）。无（已关闭）。 **2026-09-18「代码排查后的八处收敛」（ADR-81）交付记录**：**采纳并已落地**。确认人：「排查代码 里面无效变量，假设场景非真实需求场景 仅测试使用代码 多余的数据结构 错误的持有结构」，随后给出定性：「全部修复，就是spa单例，所有不是单例都是因为hmr热更新，不要假设需求」。**逐条落地（八处）**：① 删掉 `useRefresh` 里解构后零读取的 `const { store } = installed.value`（`noUnusedLocals` 未开，门禁抓不到）；② 安装检查从 `installed.value === null || installed.value.core.isDisposed()` 收成 `installed.value === null`——后者恒为 false（install 先挡已销毁实例，`uninstall()` 又在 `core.dispose()` 之前把槽置空）；③ `ResultSink` 去掉核心**从不调用**的 `list`，换成核心真正用的 `read`，核心新增 `readResult(url, key)` 供读取面用（端口形状不再由观测面决定，核心也不再有不用到的端口方法）；④ 观测面从 `tests/support/observe.ts` 搬到 `scripts/observe.ts`（`package.json` 的 `files` 只有 `dist`，scripts 不进包），示例与工具不再反向依赖测试目录，`DESIGN.md` 三处同步改成「开发侧支撑模块」；⑤ 删掉 `boundApp` 与「同一实例装到第二个 App」的冲突分支——SPA 单例、非单例只有 HMR 的原地接管，install 只按 `installed.value` 判「同一个实例重复安装」与「已有存活实例」；⑥ `identity`（`Ref`）与 `declared`（`let`）两处持有同一次提交，合成一个 `submitted`（`shallowRef<Parameters | null>`），顺带删掉 `publishData` 里那条只为类型收窄的不可达分支；⑦ `installed` 只持有协调者（原先 `{ core, store }` 与核心内部的 sink 重复持有同一个 store），页面读取改走 `core.readResult`；⑧ `Resource.isPresent`／`eligibleEvery` 收成 `private`。**没有改（明说）**：观测面仍用一次带形状校验的 `as unknown as` 越界读核心三个私有集合（ADR-79 的既定设计，未点名不动）。**口径不变**：SPA 单例；非单例只发生在 HMR 原地接管。**订正**：ADR-79 记的支撑模块位置 `tests/support/observe.ts` 由本条改为 `scripts/observe.ts`。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用**例总数 54** 全绿；`pnpm check:docs` 全项通过；`build` 与 `build:demo` 通过。**度量**：`src/` 834 → **824** 行、结构分支 84 → **83**、`maxCCN` 6（不变）；`dist/index.js` 11259 → **11024 字节**（11.02 kB）；包 33062 字节、SHA-256 `0f28e36809ad36196c0d43740de0f7f7fc3207e52919f8465ecd328ab1fd69b7`。无（已关闭）。**2026-09-18「Resource 与 RefreshCore 拆成两个文件」（ADR-82）交付记录**：**采纳并已落地**。确认人：「resource和refrescore 拆成俩文件」。**落地**：新增 `src/resource.ts`（`Config` 配置槽 ＋ `Resource` 类），`src/core.ts` 只留 `RefreshCore` 与结果表类型（`ResultCell`／`ResultSink`）、身份键（`identityOf`／`splitIdentity`）、传输接口（`RefreshHttp`）与调度私有件。**没有环**：`Resource` 不持有核心、也不 import 核心，只 import `source.ts` 的 `Parameters` 类型，因此运行期边只有 `core → resource`，严格向下。**层表与文档同步**：模块层插入 `source` 与 `core` 之间（`public-types ← source ← resource ← core ← store ← vue ← index`），`scripts/check-docs.mjs` 的 `LAYERS` 表加上 `resource.ts: 2` 并顺移其后各层，`README.md` 与 `DESIGN.md` 的 ```text 模块清单、依赖方向行、模块职责表同步；顺带订正 `DESIGN.md` 里 `RefreshCore` 的公开面计数（补 ADR-81 新增的 `readResult`，9 → 10）与 `store.ts` 的端口动作名。**导入点改指**：`src/vue.ts`、`tests/core.test.ts`、`scripts/observe.ts` 的 `Config`／`Resource` 改从 `resource.ts` 取。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用**例总数 54** 全绿；`pnpm check:docs` 全项通过（**7 modules**）；`build`／`build:demo` 通过；真 Chrome **12/12**；`node scripts/benchmark.mjs`：取数中位 632、收敛比 0.99、在途峰值 4、释放后残留全 0。**度量**：源文件 6 → **7**、`src/` 824 → **830** 行、结构分支 **83**（不变）、`maxCCN` **6**（不变）；声明文件 6 → **7**；`dist/index.js` 11024 → **11028 字节**（11.03 kB）；包 **33184 字节**（11 个条目，SHA-256 `53363e4a104fa1198629f059e1e4febbcd175f1161c61a5237b59e7b18420919`）。无（已关闭）。**2026-09-18「maxConcurrent 就是真实 HTTP 并行度」（ADR-83）交付记录**：**复核后不改结构**。确认人：「refreshcore 出现了重大设计失误，http根本没有并行，我们设计的 maxConcurrent 是 http 并行度，结果高出了个 fifo quene 和 running，串行在这调用……我们需要维护的就是一个并行 http set 执行完就从队列移动一个过去，而不是现在虽然有两个 但是根本没有并行啊」。**先取证**：写探针直接数**传输层**的在途调用（不是框架自己的投影）——8 个身份、`maxConcurrent=4`、假传输可控放行：按下后**在途 = 4、峰值 = 4**；放行 1 个之后**在途仍是 4**（队列里立刻补上一个）、峰值不变；稳定期 400ms（every=50ms、单请求真实耗时 20ms）每 5ms 采样，**在途峰值恒为 4**。**机制**：`startQueued` 用 `void this.run(resource)` 起（**不 await**），`run` 在 `await this.http.post(...)` 处挂起，循环因此继续把下一个也发出去——同一轮最多发出 `maxConcurrent` 个，是真的同时在途；`run` 的 `finally` → `flushSoon` → `flush` → `startQueued` 从队列补下一个，所以在途在稳定期一直顶满上限。**两个结构不是重复**：`running` 就是「并行 http set」（同时兼并发计数），`queue` 是**待取 FIFO**，只负责顺序（手动刷新经 `enqueueAtHead` 插到队头）。**结论：没有串行，结构不改**。**但证据面有缺口，本轮补掉**：仓里此前唯一「并行」的证据是框架自己的 `running` 投影（循环论证），因此新增用例 `A09 maxConcurrent 就是真实 HTTP 并行度：在途顶满上限，一个结束立刻从队列补上一个`——直接数传输层调用，断言 4 个同时在途、峰值恒等于上限、放行一个立刻补一个、最终 8 个身份各发起一次；用例总数 54 → **55**（core 37 → 38）。**顺带查明**：`examples/main.ts` 的演示默认并发是 **2**（`?slots=1` 时为 1），所以在演示里最多看到 2 个在途——那是示例配置，不是核心。**验证**：`pnpm typecheck` 0 错误；`pnpm test` **55/55**；`pnpm check:docs` 全项通过（67 条测试标题）；`pnpm build` 通过（**`dist/index.js` 不变，11028 字节**——本轮只动测试与文档）。**度量**：`src/` 830 行、结构分支 83、`maxCCN` 6 均不变；包 **33186 字节**（SHA-256 `ec760846cc1649cbbbbd0cf34e30c9fe74aa37da5398300405170f761609a162`）。无（已关闭）。**2026-09-18「RefreshCore 收一遍」（ADR-84）交付记录**：**采纳并已落地**。确认人：「refrescore的代码能否继续优化了 看着很多调用 实现的很奇怪」→「全部做 不要出现reconcile 这种意义不明的函数」。**① 修掉一个真缺陷（探针实测）**：`refill` 会把**已释放的身份复活重取**并把结果写回表——路径是 `settle` → `sink.write` 的同步扇出里页面 `refresh()`（此时 `produced` 为真，于是 `needsNext = true`）并当场卸载（`releaseIfUnused`），回到收尾时 `refill` 见到 `needsNext` 仍就把这个已不在注册表里的实例排进队列，`startQueued` 照样发请求、把已删掉的格子写回来（探针：`calls` 1 → 2，且写回 `{"data":999}`）。修法：`releaseIfUnused` 里把这一份账一并作废（`needsNext = false` ＋ 摘出队 ＋ 清 `controller` ＋ abort）——符合「刷新是给身份的命令，不留账」。复验：`calls` 回到 1、结果表不再被写回；新增回归用例 `A06/A17 写表时的同步重入里刷新并卸载：释放之后不再补一轮，身份不会被复活重取`。**② 删掉两处死调用**：`dispose` 里的 `queue.clear()` 与 `identities.clear()`——在队的实例必带 `controller`，而回收会把实例从注册表与队列一起摘掉，循环之后两个集合必然已空。**③ 删掉四处一行包装**：`all()` / `find()` / `refill()` / `scheduleWakeup()` 全部内联消失。**④ 消掉 `reconcile` 这个意义不明的公开入口**：配置写入与重排合成一个方法 `setConfig(config, enabled, every, active)`——适配层把三个值交给核心，核心在同一个同步块里写槽并重排；`Config` 的唯一写入口从「适配层的 `applyConfig` ＋ 另一次 `reconcile()`」收成「核心的 `setConfig`」，适配层的 `applyConfig` 随之删除。**⑤ 执行位置不再用四态字符串**：`place(resource, 'queued'|'running'|'abandoned'|'idle')` 删除，改成两个各说一件事的结构——`queue`（**数组**，只表达待取顺序：`enqueue` 排到队尾或插到队头、`dequeue` 摘出）与 `running`（**在途集合**，并发槽的唯一事实）；「这次执行还算不算数」仍由 `Resource.controller` 单独表达（回收时清空它，请求仍占着 `running` 里那一格直到真实结束）。观测面的形状视图同步改成数组校验。**口径不变**：SPA 单例；`maxConcurrent` 是真实 HTTP 并行度。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **56**（core 38 → 39）全绿；`pnpm check:docs` 全项通过（68 条测试标题）；`build` 与 `build:demo` 通过；真 Chrome **12/12**；`node scripts/benchmark.mjs`：取数中位 632、收敛比 0.99、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 正常。**度量**：`src/` 830 → **802** 行、结构分支 83 → **80**、`maxCCN` 6 → **7**（`run` 的收尾内联了「补一轮」那一支）；`dist/index.js` 11028 → **10585 字节**（10.59 kB）；包 **32842 字节**（SHA-256 `2acc774e2633fe27e2e9c669632df5502fe3bb5d5ce2ab641318ebec0a244c73`）。**2026-09-21「接管后的配置以当前 refs 为准（删 `slot`）＋ 身份键拆分认最后一个 NUL ＋ 全文档清理」（ADR-85）交付记录**：**采纳并已落地**。确认人四条：版本口径「毫秒的两个数据来源默认是一样的 足够业务使用了」（保持）；① 按推荐 A 收；「清理文档 文档腐朽度太高了 从头查看所有文档全部清理」；「`settledAt` 好像没啥用你在分析」＋「url 不会含 nul 可以换成 `lastIndexOf` 更严格」。**落地**：① 删 `slot`——它三处用途没有一处承重（普通 `let` 进 watch 取值函数不登记依赖），取值函数改读安装槽，接管后新协调者因此拿到这一页当前的配置（探针：空窗期关掉这一页，接管后取数 1 → 0），新增一条用例钉住；② `splitIdentity` 认最后一个 NUL（键里的 NUL 由编码转义、URL 里可能有），A20 用例按新前提改写；③ 全文档清理：DESIGN 里点着三个已不存在的名字（`find`／`place`／`declared`）与一处已删的死调用（`identities.clear`），另有端口 `list` → `read`、`validate` 引用、§9.4 重复词、§3.7 引错 ADR 号、§6.1 的 watch 源、`maxConcurrent` 口径；README 的「6 个运行时模块」、`A01`–`A20`、「本轮新增的」与 benchmark 行；规范两处「框架不依赖应用的状态库」、§2.1 示例缺 `pinia`、五处「成功与失败都只经 `display`」、§2.5 的读出口数与 `snapshot()` 位置、U07 的「正常结束」、「取数上限」→「单次取数的截止」、§7 的模块清单与三处计数；④ `settledAt` 复核后保留（它是「最近一次执行有结局」的唯一记录，删掉就要让调度读结果表并取 `max(updatedAt, failedAt)`），注释与 DESIGN／规范三处「正常结束」订正为「成功与失败都算」；⑤ `check-docs` 第 17 组退役词补 `place`／`find`／`declared`／`identities.clear`。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **57**（core 39 ＋ vue 18）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、69 条测试标题）；`build` 与 `build:demo` 通过；真 Chrome **12/12**；`node scripts/benchmark.mjs`：取数中位 637、收敛比 1、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 输出与 HEAD 逐字节相同（核心只改了 `splitIdentity` 一行）。**度量**：`src/` 802 → **795** 行、结构分支 80 → **79**、`maxCCN` **7**；`dist/index.js` 10585 → **10535 字节**（10.53 kB）；包 **32696 字节**（11 个条目 = `dist/` 9 ＋ `package.json` ＋ README，SHA-256 `d2226de3b460fcb10f59ebc13abe97ca6cc3d41cb9d0d20a6b6074d5884d810e`；包内 README 与工作树哈希一致）。**口径（明说）**：① 把 ADR-78 已写下的承诺从「名义」变成「事实」；② 换的是拆分前提，只影响「URL 里含裸 NUL」这类输入（原先切错、现在切对）。**2026-09-21「三处同一件事两处表达收成一处」（ADR-86）交付记录**：**采纳并已落地**。确认人：「看起来冗余那就是真冗余 说明数据结构设计的可能有问题，死调用是一定要删除的」。**落地**：① 删掉 `enqueue` 开头的死去重（三个调用点都保证实例不在队里，在队实例恒有 `controller`），前提交给 `assertQueueConsistent` 的新断言「队列里没有重复项」；② 安装槽只会有活着的协调者，`!current.isDisposed()` 恒真，收成 `if (current !== null) throw`——原地点接管靠的是槽空着；③ `refresh` 的入口闸改问 `Resource.isPresent`（该方法放开为 public），presence 规则只留一处；④ 内联只用一次的 `const store`。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **57**（core 39 ＋ vue 18）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、69 条测试标题）；`build` 与 `build:demo` 通过；真 Chrome **12/12**；`node scripts/benchmark.mjs`：取数中位 640、收敛比 1、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 与 HEAD 逐字节相同。**度量**：`src/` 795 → **792** 行、结构分支 **79**、`maxCCN` **7**；`dist/index.js` 10535 → **10479 字节**（10.48 kB）；包 **32743 字节**（11 个条目 = `dist/` 9 ＋ `package.json` ＋ README，SHA-256 `c44374ab5d9afae9fd56179fbd3f718840139af2cf88e40712d2851f1bd1156`；包内 README 与工作树哈希一致）。**口径**：结构收敛，无行为变化。**2026-09-21「可见性的持有者从核心移到适配层」（ADR-87）交付记录**：**采纳并已落地**。确认人的问题：「我们在 vue 适配层已经处理了如 visible 之类的，为啥要在调度里面还存个变量 visible，去更新调度，直接调用调度需要执行的函数不行吗」→ 裁决「改」。**落地**：① 核心删掉 `visible` 字段、`isVisible()`、`setVisible()`；「环境允许」由适配层在 `readConfig` 里把「这一页激活」与浏览器可见合成快照的一个位 `present`，核心只读快照（`refresh` 的入口闸、`isEligible`、`enqueueDue` 三个读点全部改问快照）；② 隐藏／恢复走既有的配置写入边沿（每页 watcher 重报、`flushSoon` 合并成一拍），不再需要核心的重排入口；③ 适配层的读闸门第二项从 `core.isVisible()` 改成读 DOM（读那个 ref 会把它登记成依赖，从而顶掉「重新成为读者要等下一份写入」的既有口径）；④ 夹具与文档同步：核心级「隐藏」改成「环境不允许」、规范 U04 的「全局可见性由核心持有」改写、DESIGN 的公开面 10 → 8 与状态表一并订正。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **57**（core 39 ＋ vue 18）全绿；`pnpm check:docs` 全项通过（7 模块、19 叶子、69 条测试标题）；`build` 与 `build:demo` 通过；真 Chrome **12/12**（含受控 `visibilitychange` 场景）；`node scripts/benchmark.mjs`：取数中位 640、收敛比 1、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 与 HEAD 逐字节相同（28 行）。**度量**：`src/` 792 → **785** 行、结构分支 79 → **78**、`maxCCN` **7**；`dist/index.js` 10479 → **10319 字节**（10.32 kB）；包 **32820 字节**（11 个条目 = `dist/` 9 ＋ `package.json` ＋ README，SHA-256 `41365e5a7d129001ccb940d79fdedf69db7b9c15a836d647ccc6af1bf2005dc1`；包内 README 与工作树哈希一致）。**口径（明说）**：可见性持有者从核心移到适配层，同一事实只留一处（适配层的 `visible` ref → 每页快照的 `present`）。**2026-09-21「补两条用例钉住 `run` 的两处迟到复核」（ADR-88）交付记录**：**采纳并已落地**。确认人：「可以补充用例」。**落地**：新增 `A06/A13 释放之后那次请求才失败：迟到的失败不写进已释放身份的那一格` 与 `A12/A06 复制结果期间换了执行：旧执行的结果不写进结果表（复制会读属性，取值器可能同步重入）` 两条核心用例，并把这两处故障登记进 §6.4.3 的注入表。两条都做过定向注入：去掉对应守卫即转红（前者是迟到的失败写回已释放的格子，后者是复制期间丢掉的执行写回结果）。**没写的第三处（明说）**：`await` 之后那次复核与「复制结果之后」那次在正确性上不可区分，它的作用是省掉一次复制；为它写用例会把实现细节升成契约，故不写。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **59**（core 41 ＋ vue 18）全绿；`pnpm check:docs` 的测试计数与叶子追溯两项同步（19 个叶子、71 条测试标题）；`dist/` 未变。**过程说明（明说）**：取证期间工作树里出现另一会话**未提交**的 `src/index.ts`／`src/vue.ts` 改动，它让 README 度量行的实测值变成 786 行／79 分支；本次提交的度量行配的是提交自己的 `src/`（785 行／78 分支），那份改动落定后需自带度量更新，本次交付未触碰这两份文件。**度量**：`tests/core.test.ts` 的行数 1134 → **1178**；`dist/index.js` **10319 字节**（不变）；包 **32872 字节**（11 个条目 = `dist/` 9 ＋ `package.json` ＋ README，SHA-256 `734ea476cbf62541aa97a5abad69d01615d16c0fc19064ef778afe3321544703`；包内 README 与 `dist/index.js` 都与工作树哈希一致）。**2026-09-21「读取面的身份边界」（ADR-89）交付记录**：**采纳并已落地**。外部审查（基线 `e14d0ff`）报出 F2／F4，当前 HEAD 用探针复现后按确认人「1-4 直接做」落地。**落地**：① 两个出口各记「这一版属于哪个身份键」，**同一身份同一版**才不抄第二遍（两个从未成功的格 `updatedAt` 都是 `null`，只比时间会把新身份的 args 挡在外面）；② 发布路径复核本轮快照是否仍然有效，并用一次微任务 `triggerRef(submitted)` 强制唤醒读取面——正在运行的 `watchEffect` 不会被自己触发的变更唤醒，新身份那一格的依赖必须靠这一下重订（不修就是**永久**停在旧身份）。新增 4 条用例并逐条定向注入（去掉身份划界 → F2 用例红；去掉复核与重订 → 三条 F4 用例红）。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **64**（core 41 ＋ vue 23）全绿；`pnpm check:docs` 全项通过（19 个叶子、76 条测试标题）；`build` 与 `build:demo` 通过；真 Chrome **12/12**；`node scripts/benchmark.mjs`：取数中位 636、收敛比 0.99、在途峰值 4、释放后残留全 0；`node scripts/trace-core.mjs` 与本批之前逐字节相同（28 行）。**度量**：`src/` 785 → **802** 行、结构分支 78 → **82**、`maxCCN` **7**；`dist/index.js` 10410 → **10658 字节**（10.66 kB）；包 **33652 字节**（11 个条目 = `dist/` 9 ＋ `package.json` ＋ README，SHA-256 `4ed823f30b7529fcf2a134022a945724989a7d7bdb601efa2afdfc07435e39ee`；包内 README 与工作树哈希一致）。**2026-09-21「结果表的储存随活跃身份收敛」（ADR-90）交付记录**：**采纳并已落地**。外部审查 F6 在当前 HEAD 复现（1000 轮 `read`+`remove`：内部 Map 写入 1000 个身份键、删除 0 个）。**落地**：① 释放时连格一起删（`cells.delete`）；② store 增 `size()`，基准残留行加 `cells` 并因此纳入「残留必须为 0」的契约；③ 新增用例（换身份不留空壳／同 key 重建仍唤得醒／释放后格数收敛到 0）并做定向注入（`remove` 退回置 `undefined` → 红）。**边界**：删格只影响「已释放或已换身份」的旧 ref 订阅（读取面已重订或已停止），`read` 仍会在被声明身份上建空格，所以稳态格数是活跃身份数。**验证**：`pnpm typecheck` 0 错误；`pnpm test` 用例总数 **65**（core 41 ＋ vue 24）全绿；`pnpm check:docs` 全项通过（19 个叶子、77 条测试标题）；`build` 与 `build:demo` 通过；真 Chrome **12/12**；`node scripts/benchmark.mjs`：取数中位 633、收敛比 0.99、在途峰值 4、释放后残留全 0（含 `cells: 0`）；`node scripts/trace-core.mjs` 与上一批逐字节相同。**度量**：`src/` 802 → **805** 行、结构分支 82 → **81**、`maxCCN` **7**；`dist/index.js` 10658 → **10647 字节**（10.65 kB）；包 **33891 字节**（11 个条目，SHA-256 `4dc07d1ed058db3334afdafe79b75cbaf3f76bb32edab5bf7ff029b6383c9009`；包内 README 与工作树哈希一致）。**2026-09-21「读取基准只在资格从无到有那一条边上重置」（ADR-91）交付记录**：**采纳并已落地**。外部审查 F5／C1；确认人口径「失活恢复画面本来就有值，不做主动读回；同一份配置内的变化只重排调度，读取基准要保留」。**落地**：配置/生命周期 watcher 先按旧配置问一次资格、写完再问一次，只有 `false → true` 才把 `lastReadAt` 置空；改频率、改可见性不再放行窗口，重新成为读者（暂停→开启、失活→激活、隐藏→显示）仍按 U12 放行一次。**C1 按 ADR-73 收口**：不做主动读回，把 G5／U06／A06／§2.5 改成「画面保持已有结果 + 表里已有更新时下一份写入才上屏」。新增用例并定向注入（退回「只要有资格就清基准」→ 红）。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **66**（core 41 ＋ vue 25）全绿；`pnpm check:docs` 全项通过（19 个叶子、78 条测试标题）；真 Chrome **12/12**；benchmark 中位 633、收敛比 0.99、在途峰值 4、释放后残留全 0；`trace-core` 与上一批逐字节相同。**度量**：`src/` 805 → **808** 行、结构分支 81 → **80**、`maxCCN` **7**；`dist/index.js` 10647 → **10701 字节**（10.70 kB）；包 **34175 字节**（SHA-256 `69d4f2a48e3cb8476892868dd8c6d0148b3588579928780d6a5180828af139c1`；包内 README 与工作树哈希一致）。**2026-09-21「低风险精简」（ADR-92）交付记录**：**采纳并已落地**（外部审查 §7／§8）。**落地**：① 三处死代码（`src/source.ts` 未使用的 `ReadonlySnapshot`、`tests/core.test.ts` 的 `micro`、`tests/vue.test.ts` 的 `RefreshOptions`，全部由编译器确认）；② `tsconfig` 打开 `noUnusedLocals`／`noUnusedParameters`，把这类死代码变成门禁；③ 删掉 `reportConfig` 这一层，watch 取值函数返回具名对象、回调直接用当轮新值（少一个函数、少一次重复读取）；④ `enqueueDue` 直接迭代 Map 视图（`dispose` 那趟保留快照）。**不做**：合并异常转换、内联两个谓词、换裸计时器句柄、观测函数再外迁（都只是表达风格）。**验证**：`pnpm typecheck` 0；`pnpm test` 用例总数 **66**（core 41 ＋ vue 25）全绿；`pnpm check:docs` 全项通过（19 个叶子、78 条测试标题）；真 Chrome **12/12**；benchmark 中位 640、收敛比 1、在途峰值 4、释放后残留全 0；`trace-core` 与上一批逐字节相同。**度量**：`src/` 808 → **809** 行、结构分支 **80**、函数 71 → **70**、`maxCCN` **7**；`dist/index.js` 10701 → **10705 字节**（10.71 kB）；包 **34311 字节**（SHA-256 `259efa06093cef053ff5f3fbe0b6c1ad3d4a241cdca18bba0792e43a6904c2de`；包内 README 与工作树哈希一致）。无（已关闭）。 |

角色表示未来实施职责，不暗示已指定具体人员。决策记录必须包含选择、依据、确认日期和关联验收项。
门槛数值只能由确认人给出，实现者不得静默填入或声称已达标；已关闭项记录结论、确认人与日期后不再重复讨论。
已裁决的产品边界与历史提案作为独立决策记录维护：[ADR.md](./ADR.md)。本节只保留门槛决策（G01–G04）。

---

## 6. 工程、执行与记录约束（非规范性：执行约定）

### 6.1 工程与运行

包名 `vue-refresh`，Vue 为 peer dependency；包只导出两个正式函数与 6 个公共类型（清单见 §2）；
`pinia` 也是 peer 依赖（结果表就挂在它上面），运行期只多依赖一个零依赖的小包（`fast-json-stable-stringify`，参数键的确定性编码）。本地与 CI 使用同一组命令，失败返回非零：

```sh
pnpm install --frozen-lockfile
pnpm typecheck        # tsc --noEmit
pnpm test             # node --test tests/*.test.ts
pnpm build            # vite 库构建 + 类型声明 + 说明符改写与产物形态断言
pnpm build:demo       # 演示页面
pnpm test:browser     # Playwright 集成场景
pnpm complexity       # 每文件与函数的行数、分支、圈复杂度、嵌套；加 --write 回写 README 度量行
pnpm check:docs       # 一致性门禁，清单与各项动机以 scripts/check-docs.mjs 自述注释为准；
                      # 并打印叶子→测试标题的追溯报告（只报告，不阻断）
```

- 依赖版本由锁文件固定；实际验证环境与结果以工程 README 为准，不声明其他版本支持。
- 演示页与验证页见 README；HTTP fixture 只由开发服务提供，静态演示构建不算后端服务。
- 不改预期、不吞错、不关闭 strict、不跳过失败来变绿；测试错误按契约修正并说明。
- 部分路径通过不等于整个验收 ID 通过；原生探针不等于框架通过。
- 权限或网络受限而未执行的检查记录为环境阻塞，不计作业务失败，也不伪造通过。

### 6.2 记录方式

只维护一份简短记录：当前切片与代码位置、实际环境/依赖版本、运行命令、预期与实际结果、
相关验收 ID、未验证项、下一动作。覆盖状态只用 §4.1 的三个词：`已验证` / `部分验证` / `未验证`。
首次阅读顺序：§0.1 → §1 → §2.1/§2.4 → §3 当前能力域 → 实现时按当前动作查阅契约。

执行约束：

- 用户明确指令优先。已确认选择不重复审批；普通命名、局部修复与当前切片所需的跨模块修改自行完成。
- 契约冲突须给出原文位置、最小反例和推荐，只暂停受影响部分。
- **保持两个公开入口的形状**；新增公开类型或改变取值域必须同步 §2.2 并由确认人裁决。
- 删除字段或行为必须有派生证明及对应回归，不按百分比机械删减。
- 删除行为条目或验收叶子时必须清理反向指针：grep 该条目的关键名词（含 §0 给的限定词与同义说法），
  修正正文、README 与本节引用；主题式引用（`……的行为规则见 §3（U…）`）一旦变化，
  同步更新 `check-docs.mjs` 的 `TOPIC_CITATIONS`。
- 生产模块不得导入测试代码；构建与测试脚本不得读取旧目录。

### 6.3 停止与继续条件

| 情况 | 处理 |
|---|---|
| G01 已关闭（取消） | 边界与依据见 §5，不阻断编码、测试与交付 |
| G03 已确认 | 版本范围按 §5 声明；发布前在干净消费方核验安装与 TS 4.9 类型消费 |
| G04 已确认 | 按已确认规模做一次基线测量并记录，不设独立阈值 |
| 当前行为失败、契约清楚 | 修复并回归，再扩下一行为 |
| 需要改变契约或新增公开类型 | 给出最小反例和建议，等待对应决定 |
| 环境或权限阻止运行 | 记录实际原因，不把模拟通过当真实贯通 |

出现语义冲突、取消后写入、未完成取数超 `maxConcurrent`、退订后画面变化、
旧错误关闭新操作、无限校验重试或零间隔 Timer 时，先修正当前问题，不继续扩功能。

**正式交付**按 §7 的当前覆盖状态逐项标注，并核对 §4 全部叶子与 G01/G03/G04，
保留源码、测试、锁文件、实际环境、运行结果与最终包 SHA-256；源码/测试/依赖变化后补受影响回归。
不授权自动发布、部署或发送消息。

### 6.4 验证方法（非规范性：执行约定）

本节只说明怎么取证，不改任何判据；判据在 §4，两者冲突时以 §4 为准。

#### 6.4.1 先核对业务依据
把场景依据分成「已确认需求」「工程设计决定」「待验证假设」，各自关联规则/验收 ID、来源与确认依据。
以需求与已有明确决定编写正例、反例和预期，不能根据当前代码输出倒推预期。
没有获授权的真实素材时可用明确标注的合成例继续验证；合成例通过只证明所列契约场景。

#### 6.4.2 验证连续事件与进展
用真实资源（URL）、真实协调者与已实现的核心路径运行，只替代时间、外部请求与 Vue 输入。
单元测试直接驱动核心（配置槽由测试提供），集成测试使用真实 Vue、KeepAlive 与可延迟响应的 HTTP 服务。
分别观测请求、取消、排队与尚未结束的执行数、结果集、页面快照、外部开关与结果表那一格的失败。

| 固定序列骨架 | 主要检查与验收 |
|---|---|
| 刷新 A → 声明 B → B 成功 → 旧身份的 A 晚到 | 只接受当前身份的结果，旧执行无晚到副作用；A12、A14 |
| 最后退订删除 A → 同 key 新建并成功 → 旧 A 晚到及 finally | 新结果与执行不被旧身份写入或删除；A06 |
| 槽满且有排队项 → 取消运行项但不结束 → 推进时间 → 旧执行真实结束 | 未结束执行数不超限、不提前开槽；真实结束后按 FIFO 继续；A09 |
| 写表时同步重入替换执行 → 旧链继续返回 → 新执行成功 | 旧链不再写结果，新执行正常写结果；A16 |
| 刷新得到新画面 → 下一次请求失败 → 本页隐藏再恢复且他人维持旧结果 → 新成功 | 恢复不回退旧结果；A13、A06 |

除「没有错误写入」等安全性，还必须验证实际进展：有效请求确实启动、当前成功确实交付、
后台失败后按间隔重试、真实空槽出现后队列继续运行。始终不发请求却满足全部安全性断言的实现不能通过。

#### 6.4.3 检查测试能否发现关键错误
对下列有限故障族做定向代码故障注入；不建设新验证平台，不要求全项目变异分数。

| 故意引入的代码错误 | 应检出的现有场景 |
|---|---|
| 把刷新命令落到已经不是当前身份的实例上，或放行旧实例的晚到结果/清理 | A14、A06；分别验证命令只落在当前身份与当前执行身份的保护 |
| 去掉 `produced`：有执行时刷新一律不排队 | A14（结果还没产出：这一轮就够）与 A12（结果已写进表：要下一轮）必坏其一（ADR-70） |
| 把读闸门第二项从「还没有读取时间」改回「点过一次刷新、还没看到那一拍」这一个位 | A05／A11；还没读取过的暂停页不再跟着第一份数据上屏（浏览器断言 `shown(乙) === 111` 会红，ADR-72） |
| 去掉「同一版不抄第二遍」这一步 | A13；失败那一笔不动 `updatedAt`、只动失败位，少了这一步它就进不了画面（真 Chrome 的「失败关闭」用例抓到过，ADR-72） |
| abort 时提前释放并发槽位 | A09 |
| 把 `args` 每次抄写的复制去掉 | A19／A11；检查页面改 `display.args` 是否污染身份键与下一轮请求 |
| 让释放／销毁路径在没有任何页面回调配合的情况下跑完（声明、队列、结果表条目都清空） | A16；检查其余读者与已定结果不受影响 |
| 去掉失败分支里的身份复核（`catch` 里那次 `isCurrent`） | `A06/A13 释放之后那次请求才失败`；迟到的失败会把已经随实例删掉的那一格写回来 |
| 去掉复制结果之后的身份复核 | `A12/A06 复制结果期间换了执行`；复制要读属性，取值器里同步释放身份时旧结果会被写回 |

执行顺序固定为：未改动基线通过 → 在隔离临时副本中只注入一个故障 → 目标断言失败 →
恢复改动 → 同一目标测试再次通过。保留基线版本、注入差异、实际命令/退出码与失败断言证据。
编译、导入、设施或无关断言失败不算检出。

---

## 7. 覆盖状态（非规范性：追踪）

| 工作单元 | 状态与证据 |
|---|---|
| 根契约与实现 | `已验证`：`src/` 七个源文件（`public-types` / `source` / `resource` / `core` / `store` / `vue` / `index`），`pnpm typecheck` 0 错误 |
| 单元验收 | `已验证`：`pnpm test` 66 个用例全绿（`tests/core.test.ts` 41、`tests/vue.test.ts` 25；A01–A09、A11–A14、A16–A21；A10 随框架上限一并移除，A15 随只读入口一并移除） |
| 真实浏览器场景 | `已验证`：`pnpm test:browser` 12 条场景全绿（四条代表页面 ＋ 八条受控场景） |
| 文档一致性 | `已验证`：`pnpm check:docs` 全项通过（19 个叶子、78 条测试标题）；叶子→测试标题追溯见 `scripts/trace-leaves.mjs` |
| 性能基线 | `已验证`：`node scripts/benchmark.mjs`，数字与复现命令见工程 README |
| 包交付 | `已验证`：11 个条目（`dist/` 9 ＋ `package.json` ＋ README），包 SHA-256 见 §5 G04；干净消费方的包级导入与 TS 4.9 类型消费已实测（README「实际验证与边界」） |

**三条代表页面必须展示：**

| 页面 | 必须展示 |
|---|---|
| 查询列表 | 完整参数提交、独立启停、分页排序复用已提交参数、失败关闭、暂停仍可单查 |
| 行情面板 | 无查询按钮、一次提交已准备参数对象、响应式频率、显示数据多旧、后台首查失败继续、失活冻结 |
| 双组件共享 | 同参共享、切换品种、单页暂停、全部退订、重新进入及共享快照隔离 |

另需展示无启停按钮的刷新组合：在同一个同步块里开启意愿并声明新身份。

---

## 8. 维护约定

### 8.1 唯一维护入口

| 要确认什么 | 位置 | 性质 |
|---|---|---|
| 术语定义与词汇表 | §0 | 非规范性 |
| 目标、范围与决策记录 | §1、§5 | 规范性（不含行为细节） |
| 公共 API 与返回值 | §2 | 规范性 |
| 行为规则 | §3 | 规范性；行为的唯一规范表述 |
| 触发锚点（`U01`–`U09`、`U11`–`U18`） | §3 | 索引；锚点所在的 R1–R6 条文是行为本体，锚点不重不漏且每个引用都有落点，由 `pnpm check:docs` 断言 |
| 验收预期与判定标准 | §4 | 判定依据 |
| 覆盖范围、完成条件与执行约束 | §6、§7 | 非规范性：追踪与执行 |
| 模块划分、数据模型、参数与结果边界、调度与生命周期 | [DESIGN.md](./DESIGN.md) | 设计；**内部字段与所有权只在 DESIGN §3 维护**，本文档不复述 |
| 已裁决的产品边界、被否提案与重启前提 | [ADR.md](./ADR.md) | 非规范性：决策记录（每条含结论、依据与关联） |

只有「规范性」各节构成对外承诺；「判定依据」用于验收，「追踪与执行」只约束本项目自身的推进方式。
**行为陈述的规范效力只在 §3。** §1 写目标与授权边界，§4 提供判据与证据，§5 写决策与理由：
这三处涉及具体行为时只写边界、结论与指针，不复述 §3 的规则细节。任何一处与 §3 冲突时以 §3 为准。
公共类型变更同步 §2.2；行为变更只修改 §3 并更新相关测试；字段所有权与实现细节属设计文档。
取舍理由只记 [ADR.md](./ADR.md)，规范正文只写行为与取值。
