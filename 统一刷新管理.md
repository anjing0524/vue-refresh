# Vue SPA 统一刷新管理

**统一文档** · 2026-09-16 根契约重写版 · 状态：实现完成；全量验收、业务接入与发布未完成

本文是唯一维护入口：目标、公共契约、行为规则、验收与门槛都在这里；设计与实现细节见 [DESIGN.md](./DESIGN.md)。
文档修订版本不代表软件成熟度。

**阅读路径：** §0 名词解释 → §1 目标与范围 → §2 公共契约 → §3 行为规则 → §4 验收目录 →
§5 门槛 → §6 工程与记录约束 → §7 覆盖状态 → §8 维护约定。
本版是「根契约重写」（依据见 [ADR.md](./ADR.md) ADR-27）：目标与规则按**页面要什么**重新推导，
不再是上一版按实现事实逐条累加的 25 条触发锚点与 129 个验收叶子。旧编号整体退役，不复用。

---

## 0. 名词解释

### 0.1 核心概念

这八个词是产品的抽象层，不依赖任何具体类型或字段；先读它们，再看后面的条目。

| 名称 | 含义 |
|---|---|
| 声明 | 一个组件对「要哪份数据、多久一次」的声明。声明本身不是请求；相同身份重复声明幂等（U03）。 |
| 身份 | Source 身份 ＋ 完整参数值。身份相同即共享；字段顺序不影响身份，数组顺序影响身份（U02）。 |
| 共享实例 | 一个身份在框架内的一次共享生存期：第一个需求创建它，最后一个需求退出即销毁。 |
| 需求 | 一个句柄对某个共享实例的要求的总称：周期订阅与一次性刷新要求（U06）。 |
| 资格 | 一个订阅当前是否应该驱动自动刷新的唯一判据：存活、已声明身份、环境允许、配置开启且有周期（U04）。它是推导值，不单独保存。 |
| 暂停 | 调用方通过 `enabled` 表达的「不要自动刷新」。框架只读、从不改写；暂停页仍可刷新一次（U05）。 |
| 刷新 | 明确要求重新获取当前身份的数据。显式刷新与自动刷新走同一条取数、复核与交付路径（U14）。 |
| 刷新要求 | 一次显式刷新尚未满足时的临时要求：必须由一个「在本次动作之后启动」的任务提供结果；只结算一次（U14）。 |

### 0.2 领域名词

本节只保留**对外含义**；内部字段与所有权由 [DESIGN.md](./DESIGN.md) §3 维护。

| 名称 | 含义 |
|---|---|
| Source / 来源（`RefreshSource`） | 一种稳定的资源定义：固定参数 interface、结果类型 `T`、`load` 与可选同步 `validate`。应用级常量，定义时冻结。 |
| 已准备参数 | 提交边界复制并深冻结的参数快照，以及完整参数值的稳定编码；编码表示数据范围，不是共享实例的生存期。 |
| 协调者（`RefreshManager`） | 应用级对象：一个 App 一个实例，负责共享、调度、取消与交付（U17）。 |
| 句柄（`RefreshHandle`） | 一个组件的一份需求：`display` / `submit` / `refresh`。 |
| 已声明身份 | 句柄当前声明的参数身份；临时退出保留，新身份整体替换，**校验失败不改动它**（U03）。 |
| 订阅 | 一个句柄对一个共享实例的周期连接；退出即失效。它的间隔是这条连接当前的刷新间隔。 |
| 任务 | 一次共享后台执行；`load` 是 Source 提供的请求函数。任务版本在所属共享实例内分配。 |
| 页面快照（`RefreshDisplay`） | 页面独立的参数、数据、来源与结果产生时间快照，由 Vue `shallowRef` 唯一持有（U11）。 |
| RefreshError / SubmitResult / RefreshResult | 错误通知值与两个入口的判别返回值；取值与形状见 §2.4。 |

---

## 1. 目标与范围

页面已有查询函数与组件数据，但自行编写定时刷新要重复处理定时器、频率变更、页面失活、浏览器隐藏、
手动暂停、取消请求与旧响应覆盖；多个组件需要相同数据时还会重复请求。

**目标：** 页面表达刷新需求，公共工具负责订阅、共享请求、调度、取消与有效结果交付。
框架按身份保存共享结果的内部副本，只通过 `display` 与 `readSnapshot` 对外交付。

**十条根承诺**（每条都由 §4 的现有叶子判定，不新增行为）：

| # | 承诺 | 可观察事实 | 判定 |
|---|---|---|---|
| G1 | 写一次需求 | `submit(参数)` 之后框架负责取数；表单草稿变化无效果 | A01、A03 |
| G2 | 同参数只取一次 | 两个组件同参 → 一次 `load`，各自拿到独立副本 | A02、A11 |
| G3 | 到期才取 | 未到期不请求；轮询不重叠；不补跑漏掉的周期；不超过给定并发上限 | A07–A10 |
| G4 | 该停就停 | 暂停／失活／隐藏／卸载后不再发新请求；最后需求退出即 abort 在途并清干净 | A04–A06 |
| G5 | 回来就有 | 恢复时先交付已有结果再按频率继续；不重放、不补跑 | A06、A07 |
| G6 | 手动刷一次 | 不吃掉在途请求；等一个「本次动作之后启动」的结果；与自动刷新同一条路径 | A14 |
| G7 | 失败不毁画面 | 保留旧画面、通知页面、下个周期继续；框架不改写调用方的开关 | A13 |
| G8 | 交付面自洽 | 参数/数据/来源/时间整体发布；页面之间、页面与共享副本之间无可变别名 | A11、A12 |
| G9 | 非组件也能读 | `readSnapshot` 返回独立副本，不创建实例、不保活 | A15 |
| G10 | 应用之间不串 | 每个 App 独立的协调者与结果集 | A17 |

**职责划分：** 调用方负责完整参数、业务校验、页面交互与展示；框架负责订阅、共享、调度、取消与交付。
框架不是查询库、缓存层或事务层。

**不纳入：** 产品边界以 [ADR.md](./ADR.md) 的裁决表为唯一清单。最常被问的三例——本地时钟与动画、
跨标签页共享、通用保存封装——都不在范围内。保存后需要重读时，由页面提交一次新的主动刷新，不增加保存事务 API。

**工程边界：** 包只导出三个函数、三个状态常量对象（`RequestOrigin` / `ErrorOrigin` / `CancelReason`，既是值也是类型）与 8 个公共类型（`RefreshSource` / `RefreshOptions` / `RefreshHandle` / `RefreshDisplay` / `RefreshError` / `SubmitResult` / `RefreshResult` / `RefreshManager`）；其余类型只是上面这些类型的组成部分，不单独导出——调用方按推断使用即可，不必再记一个名字；公共入口形态、模块划分与依赖方向由 `pnpm check:docs` 断言。
框架不依赖应用的状态库：结果集由协调者自己持有。

---

## 2. 公共 API 契约

正式导出只有三个函数、三个状态常量对象与 8 个公共类型。

| 类别 | 名单 |
|---|---|
| 函数 | `defineRefresh`、`createRefreshManager`、`useRefresh` |
| 状态常量（既是值也是类型） | `RequestOrigin`、`ErrorOrigin`、`CancelReason`——状态取值的唯一来源，作为值导出，消费方写 `error.origin === ErrorOrigin.Request` 而不是裸字面量 |
| 类型 | `RefreshSource` / `RefreshOptions` / `RefreshHandle` / `RefreshDisplay` / `RefreshError` / `SubmitResult` / `RefreshResult` / `RefreshManager` |

**不导出的类型**（`ReadonlySnapshot`、`RefreshInput`、`RefreshLoadContext`、`RefreshManagerOptions`）只是上表类型的组成部分，调用方从函数签名与 `display` 推断即可；少一个名字，调用方就少一处「该用哪个」的判断。
公共类型的唯一代码入口是工程内 `src/public-types.ts`。

### 2.1 三个入口

```ts
interface QuoteParams { account: string; symbol: string }
interface Quote { price: number }

const quote = defineRefresh<QuoteParams, Quote>({
  validate: p => p.account.length > 0 && p.symbol.length > 0,
  load: (p, context) => quoteService.read(p, context.signal),
})

const refresh = createRefreshManager({ maxConcurrent: applicationConfig.refreshConcurrency })
app.use(refresh)

const task = useRefresh(quote, { enabled, every: 2000 })
task.submit({ account: 'demo', symbol: 'DEMO' })  // 声明订阅；同参重复声明是幂等的
await task.refresh()                              // 显式刷新：与自动刷新同一条获取与交付路径
refresh.readSnapshot(quote, { account: 'demo', symbol: 'DEMO' }) // 只读快照
```

`useRefresh` 的第二个参数形状是 `RefreshInput<RefreshOptions>`：既可以是对象本身，也可以是它的 Ref / getter。
对象本身是 setup 期参数——框架持有它，运行期只重新读取它的字段（`enabled` / `every` / `visible` 由 watch 跟踪，
`onError` 在每次通知时读取），替换调用方手里的那个变量不会传进来；把整个对象（或它的 Ref / getter）交给框架时，
替换 `options.value` 会按新对象重新协调。`every` 只在 `enabled` 为真时必需：**只手动刷新的页面可以整个省略它**。

| 接口 | 输入 | 返回 |
|---|---|---|
| `defineRefresh<P,T>` | `{ load, validate? }`，在组件外定义一次 | `RefreshSource<P,T>` |
| `createRefreshManager` | `{ maxConcurrent }` | `install` / `readSnapshot` / `dispose` |
| `useRefresh` | `(source, RefreshInput<{ enabled, every?, visible?, onError? }>)` | `RefreshHandle<P,T>` |
| `handle.submit` | `(params: P)` | 同步 `SubmitResult`；`accepted` 表示身份已记录，不代表请求成功；同身份重复声明幂等 |
| `handle.refresh` | 无 | `Promise<RefreshResult>`；无 DTO，取消立即结算；不恢复自动刷新 |
| `manager.readSnapshot` | `(source, params)` | 独立 `T` 或 `undefined`；参数非法抛给调用者 |
| `manager.dispose` | 无 | 幂等释放 |

### 2.2 完整类型与状态取值

每个封闭状态域在代码中枚举为一个常量对象（成员名 + 字面量），公开类型由对象推导：
新增、删除或收敛一个取值只需改常量对象。不使用 TS enum —— 工程的 `erasableSyntaxOnly`
与 Node 的类型擦除都不接受该语法。

```ts
import type { App, Ref, ShallowRef } from 'vue'
export type ReadonlySnapshot<T> =
  T extends (...args: never[]) => unknown ? T :
  T extends object ? { readonly [K in keyof T]: ReadonlySnapshot<T[K]> } : T
export type RefreshInput<T> = T | Readonly<Ref<T>> | (() => T)
export const RequestOrigin = {
  Refresh: 'refresh',
  Background: 'background',
} as const
export type RequestOrigin = (typeof RequestOrigin)[keyof typeof RequestOrigin]
export const ErrorOrigin = {
  Request: 'request',
  Validation: 'validation',
  Configuration: 'configuration',
} as const
export type ErrorOrigin = (typeof ErrorOrigin)[keyof typeof ErrorOrigin]
export const CancelReason = {
  Superseded: 'superseded',
  Unavailable: 'unavailable',
  Disposed: 'disposed',
} as const
export type CancelReason = (typeof CancelReason)[keyof typeof CancelReason]
declare const sourceBrand: unique symbol
export interface RefreshSource<P extends object, T> {
  readonly [sourceBrand]: { readonly args: (value: P) => P; readonly data: (value: T) => T }
}
export interface RefreshLoadContext { readonly signal: AbortSignal }
export interface RefreshError {
  readonly origin: ErrorOrigin
  readonly error: unknown
  readonly operationId?: number
}
export type SubmitResult =
  | { readonly status: 'accepted' }
  | { readonly status: 'rejected'; readonly error: unknown }
  | { readonly status: 'cancelled'; readonly reason: typeof CancelReason.Superseded | typeof CancelReason.Disposed }
export type RefreshResult =
  | { readonly status: 'success' }
  | { readonly status: 'error'; readonly origin: typeof ErrorOrigin.Request | typeof ErrorOrigin.Configuration; readonly error: unknown }
  | { readonly status: 'cancelled'; readonly reason: CancelReason }
export interface RefreshDisplay<P extends object, T> {
  readonly args: ReadonlySnapshot<P>
  readonly data: ReadonlySnapshot<T>
  readonly origin: RequestOrigin
  readonly updatedAt: number
}
export interface RefreshOptions {
  readonly enabled: RefreshInput<boolean>
  readonly every?: RefreshInput<number>
  readonly visible?: RefreshInput<boolean>
  readonly onError?: (error: RefreshError) => void | Promise<void>
}
export interface RefreshHandle<P extends object, T> {
  readonly display: Readonly<ShallowRef<RefreshDisplay<P, T> | null>>
  submit(args: P): SubmitResult
  refresh(): Promise<RefreshResult>
}
export interface RefreshManagerOptions {
  readonly maxConcurrent: number
}
export interface RefreshManager {
  install(app: App): void
  readSnapshot<P extends object, T>(source: RefreshSource<P, T>, args: P): ReadonlySnapshot<T> | undefined
  dispose(): void
}
```

`ReadonlySnapshot` 是**编译期**视图：框架交付的是独立副本，运行期并不冻结，
因此拿到只读视图不等于共享对象不可写——别名安全由「每次交付各复制一份」保证（U11）。

### 2.3 安装与销毁

`createRefreshManager` 要求正安全整数 `maxConcurrent`。`app.use` 安装；同实例同 App 重复安装无副作用，
同一 App 上的另一个活跃实例被拒，被拒的安装不改动已有绑定；上一个实例已销毁时原地接管注入槽位
（HMR、会话切换），因此不重复 `provide`，也不留下指向旧实例的第二份记录。
应用卸载或显式 `dispose` 释放自身监听、句柄、实例、Timer 与唤醒调度。
SSR（没有 `document`）不注册浏览器监听、不设置 Timer、不发请求。已销毁实例不复用（U17）。

### 2.4 返回值与错误契约

| 操作 | 结果与副作用 |
|---|---|
| `submit` | 同步 `accepted` 表示身份已记录，**不是**请求成功；相同身份重复声明幂等（不写状态、不产生新请求）；参数被拒返回 `rejected` 且**不改动任何状态**（无效输入不产生副作用），保留画面；已销毁返回 `cancelled` / `disposed` |
| `refresh` 入口 | 未声明身份、失活或隐藏返回 `cancelled` / `unavailable`；配置非法返回 `error` / `configuration`；已销毁返回 `cancelled` / `disposed`。入口拒绝不产生请求，也不改动已声明身份 |
| `refresh` 合并 | 要求一个「在本次动作之后启动」的任务：排队未启动的任务可直接满足它；已在执行的任务不算，等它结束后补一次后继请求 |
| `refresh` 结算 | 任务成功返回 `success`，结果按共享交付路径同次发布；任务失败返回 `error` / `request`（含框架上限到期），先结算再通知，保留旧画面与开启意愿，下个周期继续；**框架不改写调用方 `enabled`**；取消立即结算，不等底层结束 |
| 共享请求失败 | 通知仍有效的订阅者与刷新要求，保留需求与开启意愿，按下个周期继续 |
| `onError` | 同步调用、异步拒绝立即观察，不等待、不递归；通知自身失败不外传 |

取消原因只有 `superseded` / `unavailable` / `disposed`。错误通知值 `RefreshError` 带 `origin`、原始 `error`，
以及产生这条错误的**声明代次**（框架生成、只用于认领：一个页面里两处 `useRefresh` 共用同一个 `onError`
时据此分辨是谁的失败；尚无有效代次时字段缺席，字段名与取值见 §2.2）。
实例与任务标识不出现在公开通知里。错误来源与请求来源是两个语义域：
`display.origin === 'background'` 说的是「这条结果是自动轮询来的」，`error.origin === 'request'` 说的是「共享请求失败了」。

### 2.5 交付面、刷新动作与只读快照

`display` 是唯一交付面：`args` / `data` / `origin` 与 `updatedAt` 同次整体发布，不出现半新半旧的画面。
`updatedAt` 是产生本条结果的墙钟 epoch 毫秒；后加入的订阅者读到已有结果时，拿到的仍是原结果时间，
因此页面可以据此显示数据多旧。它是墙钟读数，不保证单调、也不承诺 `updatedAt ≤ Date.now()`，
页面计算相对时间时应自行把差值钳制到 0。框架不交付「正在刷新」这类实时状态。

配置非法的通知行为规则见 §3（U05）。
参数非法时的抛错行为规则见 §3（U15）。

`refresh()` 的 Promise 只报这次刷新的结算（`success` / `error` / `cancelled`），**不携带 DTO**：
数据仍只经 `display` 交付。需要「取到数据之后再写业务 Store」的页面由自己的适配层承担；
**结算在交付之后**：`await refresh()` 返回 `success` 时，本页 `display` 已经是这次的结果。

`readSnapshot` 在已销毁的协调者上直接返回 `undefined`；活跃时只计算参数键，不复制或冻结参数、不调用 `validate`。
它只可能查到**仍有活跃实例**（存在订阅或未结算的刷新要求）的结果：实例随最后一个需求退出而销毁，
因此「查不到」不等于「没有这份数据」，只表示此刻没有人在用。查到返回独立副本，无结果返回 `undefined`，
`null` 是有效结果。它只交付值、不交付时间，也不承诺新鲜度——要时间就订阅。

---

## 3. 行为规则（唯一维护入口）

规则以**能力域**为阅读单位：R1–R6 是全部行为承诺，每条能力域由「机制 ＋ 触发清单 ＋ 判定」组成。
`U01`–`U18` 是**锚点**：跨文档引用一律指向锚点；本版是根契约重写，**旧版锚点与全部旧叶子编号整体退役、不复用**（ADR-27）。`pnpm check:docs` 断言锚点不重不漏，且本文档与 DESIGN、README 里出现的每个 `U` 引用都有落点。

失败只有三个产品含义，三者互不替代：**动作失败**——页面按下的那一次没有成功（`U14`）；
**条目暂不可得**——共享数据这一次没取到，需求与开启意愿都保留，下个周期继续（`U13`）；
**输入非法**——参数或配置不成立，直接回报调用方且不改动任何已定事实（`U03`、`U05`）。

### 3.1 R1 共享结果与身份

**机制：** 已准备参数相同的调用共享同一次请求与同一份结果，参数变化立即换身份。

- `U01` `submit` 接纳完整参数；接纳时复制并深冻结参数快照，稳定编码为身份键。表单草稿与结果永不进入跟踪范围。
- `U02` 身份由 Source 身份 ＋ 完整参数值决定：字段顺序不同仍共享，数组顺序影响身份；不同身份各有一次取数。
  账户与请求上下文必须纳入参数，或由独立 Source 表达；应用级隔离由每个 App 独立的协调者保证（`U17`）。
- `U03` 相同身份的 `submit` 幂等：保留现有订阅与未结算的刷新要求，不产生新请求、不重建订阅；
  身份变化立即成为当前意图并整体替换。**参数被拒时不改动任何状态**——旧身份、订阅与刷新要求原样保留，
  只回报 `rejected` 并通知一次 `validation`。

判定：A01、A02、A03

### 3.2 R2 订阅生命周期与调度

**机制：** 随挂载、配置与可见性变化自动保持结果新鲜度，退出即无残留请求与实例。

- `U04` **资格**：一个订阅是否成立由这一个合取唯一判定——存活（协调者与句柄都未销毁）、已声明身份、
  环境允许（组件激活且浏览器可见）、配置明确开启且有正安全整数周期。资格只读事实、不产生副作用，
  取消与恢复都由它推导；刷新要求不参与资格。
- `U05` `enabled` 是唯一的开启意愿，**只由调用方写入**，框架只读。关闭只按资格退订，
  **不取消**已发起的刷新要求，也不改写调用方的开关；暂停页仍可刷新一次，刷新不把暂停页变回订阅。
  配置快照读不出或值非法时：不订阅、不自动刷新，对该句柄通知一次 `configuration`
  （连续非法期间去重，修正后按当前资格恢复），刷新入口按 `error` / `configuration` 结算；读不到的开关不猜成 `false`。
- `U06` 失去存在（失活、隐藏）当场结算本页未完成的刷新要求为 `cancelled` / `unavailable` 并退订；
  最后一个需求退出时删除实例、结果与排队任务，并 abort 在途执行。`enabled` 边沿不结算刷新要求。
- `U07` 到期时刻由「最近一次正常结束时刻 ＋ 当前最短间隔」现算，因此改频率立刻生效；
  从未结算过的实例立即到期。取消不计时、不补跑挂起期间漏掉的周期；恢复后只取一次。
- `U08` 同一实例同时至多一个当前任务：上一轮没有真实结束时不再发起，普通轮询不重叠。
- `U09` 后台执行按 FIFO 排队，并发槽统计真实尚未结束的任务；显式刷新与自动刷新共用这些槽位，
  不绕过上限；满槽不自旋，任务真实结束或上限到期后继续。
- `U10` **框架上限**：从任务真正开始执行起算（排队不计入）的一次性上限（数值见 DESIGN §5），
  到期按共享请求失败结算、立即撤销在册身份并交还槽位；这次执行迟到的结束不再写任何事实（不写结果、不交付、不二次通知）。

判定：A04、A05、A06、A07、A08、A09、A10

### 3.3 R3 交付面与失败

**机制：** 交付到页面的永远是满足当前条件的有效结果，且是整体发布的；失败不改变已定结果。

- `U11` 交付面 `display` 的 `args` / `data` / `origin` / `updatedAt` 同次整体替换，不做深响应式；
  每个接收者拿到独立副本，页面之间、页面与共享结果之间没有可变别名；`null` 是有效结果，`undefined` 不是。
- `U12` 一次成功的接收者是「有效订阅 ∪ 满足本次版本门槛的刷新要求」，同一句柄只交付一次；
  来源按「它是否在等这次结果」判定：在等就是 `refresh`，否则是 `background`。
- `U13` 共享请求失败：保留需求与开启意愿、旧画面不变、向仍有效的订阅者各通知一次 `request`，
  并按同一失败结算该实例**全部**未完成的刷新要求，下个周期继续；框架不改写 `enabled`。

判定：A11、A12、A13

### 3.4 R4 显式刷新与共享取数

**机制：** 页面显式刷新与自动刷新共用同一条获取、有效性复核与交付路径；框架只有这一条取数通道。

- `U14` `refresh` 为当前身份登记一个「不低于某版本」的临时要求：实例没有当前任务时由它登记这次请求；
  已有任务在排队（未启动）时可被它直接满足；已在执行的任务不算，等它结束后补一次后继请求。
  要求成功后按共享交付路径发布并结算 `success`；失败按 `error` 结算；失去存在或销毁按 `cancelled` 结算。
  结算在交付之后，且只报结算、不携带 DTO；`refresh` 不恢复自动刷新。

判定：A14

### 3.5 R5 只读定位与参数边界

**机制：** 非组件代码可读共享副本，不建实例、不保活；参数只有值域与循环守卫。

- `U15` `readSnapshot` 只计算参数键并读取独立副本；不校验业务字段、不创建或保活实例。
  无结果或已销毁返回 `undefined`；参数非法（非 JSON 值、`-0`、非有限数、根容器不是记录、
  非普通记录/数组、循环或病态嵌套）抛给调用者，且与此刻有没有活跃实例无关。
  守卫只为阻断循环与病态嵌套，不是业务上限（G01 已关闭）。

判定：A15

### 3.6 R6 隔离与工程边界

**机制：** 页面回调的失败不影响框架状态；每个 App 独立；参数规模由应用决定。

- `U16` 框架**调用**的页面回调（`publish` / `onError` / `cleanup`）抛错或返回拒绝的 Promise 都只被吞掉，
  不改变已定结果、订阅、调度与其他接收者的交付。`load` 拿到的 `AbortSignal` 上的监听器由宿主在
  `abort()` 时同步调用，不是框架调用的回调，其抛错由宿主上报，不在隔离承诺内。
- `U17` 每个 App 独立协调者与结果集；同实例同 App 安装幂等，非法跨 App 或多活跃绑定被拒且不破坏已有绑定；
  SSR 无请求与 Timer；`dispose` 先失效再清理自身监听、句柄、实例与调度，未结束的执行到真实结束才释放槽位。
- `U18` 参数只在提交边界准备一次；序号（声明代次与任务版本）在安全整数区间内递增，到达上界后**停在原地**，
  不销毁协调者、不抛错，也不给调用方第三种结果。

判定：A16、A17、A18

---

## 4. 验收目录

### 4.1 判定记法与归属

**归属前缀。** 每个叶子项自带一个前缀，判据是**判定它是否需要调用方观察不到的内部事实**：

- `[行为]` 产品可观察行为：判定只靠公开面，逐条可回答「这条对外承诺了什么」。
- `[不变量]` 实现不变量：判定依赖内部结构与顺序纪律，用于回归与定向故障注入，不构成对外承诺。
- `[混合]` 混合条目：外层结果可由公开面断言，但「必须结果」里另含一条内部一致性要求。

前缀与叶子同格；`pnpm check:docs` 断言每个叶子恰好有一个前缀、前缀取值封闭。
不得为通过测试而删除或放宽条目；行为随契约移除而消失时，同步删除对应条目并记录理由。

**变体记法。** 需要分别判断的输入直接写在条目「必须结果」列尾：`变体：01 …；02 …`。
编号是稳定标签，不保证连续：一旦分配即不复用、不重排。

**记号：** 甲 / 乙 指两个独立组件句柄；A / B / C 指不同参数值（因而通常是不同共享实例）。

### 4.2 共享结果与身份

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A01 | 首次声明后立即取一次并按整体发布交付；已准备参数是独立冻结副本，页面改原对象不影响身份 | `G1/G8 首次订阅…`、`A11 交付面…` |
| [行为] A02 | 同参数的两个句柄共享一次 `load` 且各得独立副本；字段顺序不同仍共享，数组顺序不同不共享 | `A11/A02 同参数…`、`A02 身份是…` |
| [行为] A03 | 同身份重复声明不新增请求、不重建订阅；参数被拒返回 `rejected`、旧身份与订阅原样保留，并通知一次 `validation`（带声明代次） | `A03 相同参数重复声明幂等`、`A03 参数被拒时…` |

### 4.3 订阅生命周期与调度

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A04 | 资格四组事实缺一不可：`visible` 为假或配置非法时不订阅；激活且可见且开启且周期合法时接入 | `A04 visible 为假时不订阅`、`A04/A05 配置非法时不订阅` |
| [行为] A05 | 关闭开启意愿只退订，不结算已发起的刷新要求；暂停页仍可刷新一次且不恢复订阅；配置非法只通知一次并可恢复 | `A04/A05 关闭开启意愿后…`、`A04/A05 配置非法：只报告一次` |
| [行为] A06 | 隐藏与失活当场结算未完成的刷新要求为 `unavailable`；最后一个需求退出时实例、结果与排队任务一并消失且 abort 在途；实例仍在时恢复交付历史结果、不重复取数 | `A06 最后一个需求退出…`、`A06/A11 恢复…` |
| [混合] A07 | 到期由「结算时刻 ＋ 当前最短间隔」现算（改频率立刻生效）；长时间挂起后恢复只取一次，不补跑漏掉的周期 | `A07 有效间隔取…`、`A07 长时间挂起后恢复…` |
| [行为] A08 | 上一轮没有真实结束时不再发起新的 `load` | `A08 轮询不重叠…` |
| [不变量] A09 | 并发槽统计真实未结束的任务，满槽排队不自旋；槽位空出后按 FIFO 继续 | `A09 并发上限约束…` |
| [混合] A10 | 上限从开始执行起算（排队不计入）：到期按 `request` 结算、立即出册并交还槽位；迟到的结束不交付、不二次通知 | `A10 上限到期…` |

### 4.4 交付面与失败

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A11 | 参数/数据/来源/时间同次整体替换；每个接收者独立副本，改页面副本不影响共享结果；`null` 是有效结果 | `A11 交付面…`、`A11/A13 空结果…` |
| [行为] A12 | 接收者是有效订阅与满足门槛的刷新要求的并集，同一句柄只交付一次；来源按是否在等这次结果判定 | `A06/A11/A12 适配层…` |
| [行为] A13 | 失败保留旧画面与订阅，通知一次 `request`，结算该实例全部未完成的要求且不自动重试，下个周期继续 | `A13 共享请求失败…`、`A13/A14 失败结算该实例…` |

### 4.5 显式刷新

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A14 | 在途任务不满足本次刷新（结束后补一次后继请求），排队未启动的任务可直接满足；结算在交付之后，`refresh` 不携带 DTO、不恢复自动刷新 | `A12/A14 在途任务不满足…`、`A14 排队未启动…` |

### 4.6 只读定位与参数边界

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A15 | 无实例返回 `undefined`，有实例返回独立副本；参数非法抛给调用者且与是否存在实例无关；不创建实例、不保活 | `A15 只读定位…` |

### 4.7 隔离与工程边界

| 叶子 | 必须结果 | 主要证据 |
|---|---|---|
| [行为] A16 | 页面 `publish` / `onError` 抛错或返回拒绝的 Promise 都被吞掉，其他接收者与已定结果不受影响 | `A16 页面回调抛错或返回拒绝…` |
| [行为] A17 | 同实例同 App 安装幂等；另一个活跃实例被拒；销毁后可原地接管；未安装时 `useRefresh` 抛错；销毁后所有入口拒绝且未结束执行到真实结束才释放槽位 | `A17 安装…`、`A17 未安装协调者时…` |
| [混合] A18 | 非 JSON 值、循环与病态嵌套在提交边界被拒且不改动状态；序号到达安全整数上界后停在原地、入口照常工作 | `A18 参数守卫与序号上界…` |

---

## 5. 决策与门槛

| 决策 ID | 内容与状态 | 提案责任与依据 | 确认人 | 阻断节点 |
|---|---|---|---|---|
| G01 | **已关闭（取消）**：参数不设业务侧深度或字节上限——业务无法预知参数规模，框架不因为键大而拒绝合法查询；实现只保留循环/病态嵌套的内部守卫（`U15`） | 用户 2026-09-15 决定：不要限制参数 | 用户已确认 | 无（已关闭） |
| G02 | 参数为可信 JSON 记录，保留 `-0` 拒绝和全字段按值共享；特殊对象逐类拒绝不属于框架承诺；DTO 由业务边界校验、框架原生复制 | 用户确认固定资源定义与一次边界校验 | 用户已确认 | 只核对 A02 / A15 的现行边界测试 |
| G03 | **已确认**：**本包自身的类型自 TypeScript 4.9 起可用于消费（已实测）**；Vue 的 TypeScript 下限以它自己的 peer 声明为准，本包不替它承诺；最低运行环境需原生 `structuredClone` 与应用卸载钩子 | 用户 2026-09-15 指定「TS 4.9，其余最新」；仓库自身用 TS 7 构建 | 用户已确认 | 发布前核验：干净消费方安装、TS 4.9 类型消费与运行时能力；证据见工程 README |
| G04 | **已关闭**：目标设备性能充足；典型规模约 20+ 订阅、在途请求很少 | 用户 2026-09-15 给出的设备与规模结论 | 用户已确认 | 无（已关闭）。保留 `pnpm complexity` 与基线测量作为回归证据；**2026-09-16 根契约重写交付记录**：包 `vue-refresh-0.0.0.tgz`（37945 字节，SHA-256 `75a354cf9ffe6c215a1c666ffe54e0014cd90a566dcd2a75dc5136556aaed50a`，9 个条目 = `dist/` 7 ＋ `package.json` ＋ README）由已落地的源码构建，连跑两次 `build` ＋ `pack` 逐字节相同；干净消费方在 `typescript@4.9.5` 下零错误、SSR 与客户端场景通过；性能基线 load 中位 635 次、收敛比 0.99、在途峰值 4、释放后残留 0（详见工程 README「实际验证与边界」）。哈希记在本表而不写进 README：README 被 npm 强制打进包内，包自身哈希无法写在包内文件里。上一版（根契约重写前）的 63665 字节 / `826c5008…` 等记录已由本条取代 |

角色表示未来实施职责，不暗示已指定具体人员。决策记录必须包含选择、依据、确认日期和关联验收项。
门槛数值只能由确认人给出，实现者不得静默填入或声称已达标；已关闭项记录结论、确认人与日期后不再重复讨论。
已裁决的产品边界与历史提案作为独立决策记录维护：[ADR.md](./ADR.md)。本节只保留门槛决策（G01–G04）。

---

## 6. 工程、执行与记录约束（非规范性：执行约定）

### 6.1 工程与运行

包名 `vue-refresh`，Vue 为 peer dependency；包只导出三个正式函数、三个状态常量对象与 8 个公共类型（清单见 §2）；
运行期不依赖应用的状态库，也没有第三方运行时依赖。本地与 CI 使用同一组命令，失败返回非零：

```sh
pnpm install --frozen-lockfile
pnpm typecheck        # tsc --noEmit
pnpm test             # node --test tests/*.test.ts
pnpm build            # vite 库构建 + 类型声明 + 说明符改写与产物形态断言
pnpm build:demo       # 演示页面
pnpm test:browser     # Playwright 集成场景
pnpm complexity       # 每文件与函数的行数、分支、圈复杂度、嵌套；加 --write 回写 README 度量行
pnpm check:docs       # 一致性门禁，清单与各项动机以 scripts/check-docs.mjs 自述注释为准；
                      # 并打印叶子→测试标题的追溯报告（只报告，不阻断）
```

- 依赖版本由锁文件固定；实际验证环境与结果以工程 README 为准，不声明其他版本支持。
- 演示页与验证页见 README；HTTP fixture 只由开发服务提供，静态演示构建不算后端服务。
- 不改预期、不吞错、不关闭 strict、不跳过失败来变绿；测试错误按契约修正并说明。
- 部分路径通过不等于整个验收 ID 通过；原生探针不等于框架通过。
- 权限或网络受限而未执行的检查记录为环境阻塞，不计作业务失败，也不伪造通过。

### 6.2 记录方式

只维护一份简短记录：当前切片与代码位置、实际环境/依赖版本、运行命令、预期与实际结果、
相关验收 ID、未验证项、下一动作。覆盖状态只用 §4.1 的三个词：`已验证` / `部分验证` / `未验证`。
首次阅读顺序：§0.1 → §1 → §2.1/§2.4 → §3 当前能力域 → 实现时按当前动作查阅契约。

执行约束：

- 用户明确指令优先。已确认选择不重复审批；普通命名、局部修复与当前切片所需的跨模块修改自行完成。
- 契约冲突须给出原文位置、最小反例和推荐，只暂停受影响部分。
- **保持三个公开入口的形状**；新增公开类型或改变取值域必须同步 §2.2 并由确认人裁决。
- 删除字段或行为必须有派生证明及对应回归，不按百分比机械删减。
- 删除行为条目或验收叶子时必须清理反向指针：grep 该条目的关键名词（含 §0 给的限定词与同义说法），
  修正正文、README 与本节引用；主题式引用（`……的行为规则见 §3（U…）`）一旦变化，
  同步更新 `check-docs.mjs` 的 `TOPIC_CITATIONS`。
- 生产模块不得导入测试代码；构建与测试脚本不得读取旧目录。

### 6.3 停止与继续条件

| 情况 | 处理 |
|---|---|
| G01 已关闭（取消） | 边界与依据见 §5，不阻断编码、测试与交付 |
| G03 已确认 | 版本范围按 §5 声明；发布前在干净消费方核验安装与 TS 4.9 类型消费 |
| G04 已确认 | 按已确认规模做一次基线测量并记录，不设独立阈值 |
| 当前行为失败、契约清楚 | 修复并回归，再扩下一行为 |
| 需要改变契约或新增公开类型 | 给出最小反例和建议，等待对应决定 |
| 环境或权限阻止运行 | 记录实际原因，不把模拟通过当真实贯通 |

出现语义冲突、取消后写入、未完成 `load` 超 `maxConcurrent`、退订后画面变化、
旧错误关闭新操作、无限校验重试或零间隔 Timer 时，先修正当前问题，不继续扩功能。

**正式交付**按 §7 的当前覆盖状态逐项标注，并核对 §4 全部叶子与 G01/G03/G04，
保留源码、测试、锁文件、实际环境、运行结果与最终包 SHA-256；源码/测试/依赖变化后补受影响回归。
不授权自动发布、部署或发送消息。

### 6.4 验证方法（非规范性：执行约定）

本节只说明怎么取证，不改任何判据；判据在 §4，两者冲突时以 §4 为准。

#### 6.4.1 先核对业务依据
把场景依据分成「已确认需求」「工程设计决定」「待验证假设」，各自关联规则/验收 ID、来源与确认依据。
以需求与已有明确决定编写正例、反例和预期，不能根据当前代码输出倒推预期。
没有获授权的真实素材时可用明确标注的合成例继续验证；合成例通过只证明所列契约场景。

#### 6.4.2 验证连续事件与进展
用真实 Source、真实协调者与已实现的核心路径运行，只替代时间、外部请求与 Vue 输入。
单元测试直接驱动核心（配置快照由测试提供），集成测试使用真实 Vue、KeepAlive 与可延迟响应的 HTTP 服务。
分别观测请求、取消、排队与尚未结束的任务数、结果集、页面快照、外部开关与错误通知。

| 固定序列骨架 | 主要检查与验收 |
|---|---|
| 刷新 A → 声明 B → B 成功 → 旧身份的 A 晚到 | 只接受当前身份的结果，旧任务无晚到副作用；A12、A14 |
| 最后退订删除 A → 同 key 新建并成功 → 旧 A 晚到及 finally | 新结果与任务不被旧身份写入或删除；A06、A10 |
| 槽满且有排队项 → 取消运行项但不结束 → 推进时间 → 旧执行真实结束 | 未结束任务数不超限、不提前开槽；真实结束后按 FIFO 继续；A09、A10 |
| 成功交付回调同步替换任务 → 旧回调链继续返回 → 新任务成功 | 旧通知停止，新任务正常交付；A16 |
| 刷新得到新画面 → 下一次请求失败 → 本页隐藏再恢复且他人维持旧结果 → 新成功 | 恢复不回退旧结果；A13、A06 |

除「没有错误写入」等安全性，还必须验证实际进展：有效请求确实启动、当前成功确实交付、
后台失败后按间隔重试、真实空槽出现后队列继续运行。始终不发请求却满足全部安全性断言的实现不能通过。

#### 6.4.3 检查测试能否发现关键错误
对下列有限故障族做定向代码故障注入；不建设新验证平台，不要求全项目变异分数。

| 故意引入的代码错误 | 应检出的现有场景 |
|---|---|
| 放行已失效的刷新要求，或放行旧实例的晚到结果/清理 | A14、A06；分别验证刷新要求归属与任务身份的保护 |
| abort 时提前释放并发槽位 | A09、A10 |
| 把深层快照复制替换为浅复制 | A11；分别检查页面之间与共享结果别名 |
| 拿掉「框架上限到期」的结算与出册 | A10；上限内仍占槽、到期立即出册并交还槽位、迟到结束不写事实，三段分别验证 |
| 让页面回调的抛错穿过交付循环 | A16；检查其余接收者与已定结果不受影响 |

执行顺序固定为：未改动基线通过 → 在隔离临时副本中只注入一个故障 → 目标断言失败 →
恢复改动 → 同一目标测试再次通过。保留基线版本、注入差异、实际命令/退出码与失败断言证据。
编译、导入、设施或无关断言失败不算检出。

---

## 7. 覆盖状态（非规范性：追踪）

| 工作单元 | 状态与证据 |
|---|---|
| 根契约与实现 | `已验证`：`src/` 五个模块（`public-types` / `source` / `core` / `vue` / `index`），`pnpm typecheck` 0 错误 |
| 单元验收 | `已验证`：`pnpm test` 35 个用例全绿（A01–A18） |
| 真实浏览器场景 | `已验证`：`pnpm test:browser` 12 条场景全绿（三条代表页面 ＋ 八条受控场景） |
| 文档一致性 | `已验证`：`pnpm check:docs` 全项通过；叶子→测试标题追溯见 `scripts/trace-leaves.mjs` |
| 性能基线 | `已验证`：`node scripts/benchmark.mjs`，数字与复现命令见工程 README |
| 包交付 | `已验证`：9 个条目（`dist/` 7 ＋ `package.json` ＋ README），包 SHA-256 见 §5 G04；干净消费方的包级导入与 TS 4.9 类型消费已实测（README「实际验证与边界」） |

**三条代表页面必须展示：**

| 页面 | 必须展示 |
|---|---|
| 查询列表 | 完整参数提交、独立启停、分页排序复用已提交参数、失败关闭、暂停仍可单查 |
| 行情面板 | 无查询按钮、一次提交已准备参数对象、响应式频率、显示数据多旧、后台首查失败继续、失活冻结 |
| 双组件共享 | 同参共享、切换品种、单页暂停、全部退订、重新进入及共享快照隔离 |

另需展示无启停按钮的刷新组合：在同一个同步块里开启意愿并声明新身份。

---

## 8. 维护约定

### 8.1 唯一维护入口

| 要确认什么 | 位置 | 性质 |
|---|---|---|
| 术语定义与词汇表 | §0 | 非规范性 |
| 目标、范围与决策记录 | §1、§5 | 规范性（不含行为细节） |
| 公共 API 与返回值 | §2 | 规范性 |
| 行为规则 | §3 | 规范性；行为的唯一规范表述 |
| 触发锚点（`U01`–`U18`） | §3 | 索引；锚点所在的 R1–R6 条文是行为本体，锚点不重不漏且每个引用都有落点，由 `pnpm check:docs` 断言 |
| 验收预期与判定标准 | §4 | 判定依据 |
| 覆盖范围、完成条件与执行约束 | §6、§7 | 非规范性：追踪与执行 |
| 模块划分、数据模型、参数与结果边界、调度与生命周期 | [DESIGN.md](./DESIGN.md) | 设计；**内部字段与所有权只在 DESIGN §3 维护**，本文档不复述 |
| 已裁决的产品边界、被否提案与重启前提 | [ADR.md](./ADR.md) | 非规范性：决策记录（每条含结论、依据与关联） |

只有「规范性」各节构成对外承诺；「判定依据」用于验收，「追踪与执行」只约束本项目自身的推进方式。
**行为陈述的规范效力只在 §3。** §1 写目标与授权边界，§4 提供判据与证据，§5 写决策与理由：
这三处涉及具体行为时只写边界、结论与指针，不复述 §3 的规则细节。任何一处与 §3 冲突时以 §3 为准。
公共类型变更同步 §2.2；行为变更只修改 §3 并更新相关测试；字段所有权与实现细节属设计文档。
取舍理由只记 [ADR.md](./ADR.md)，规范正文只写行为与取值。
