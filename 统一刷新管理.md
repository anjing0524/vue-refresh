# Vue SPA 统一刷新管理

**统一文档** · 2026-09-15 固定资源定义收敛版 · 状态：实现完成；全量验收、业务接入与发布未完成

本文是唯一维护入口：业务规则、公共契约、验收与门槛都在这里；设计与实现细节见 [DESIGN.md](./DESIGN.md)。
文档修订版本不代表软件成熟度。

**阅读路径：** 先读 §0 名词解释与 §1 目标范围 → §2 公共契约与 §3 行为规则 →
验收查 §4、门槛查 §5、工程与记录约束查 §6、覆盖清单查 §7。
设计与实现（模块划分、数据模型、参数与结果边界、调度与竞态、生命周期适配）见
[DESIGN.md](./DESIGN.md)。

---

## 0. 名词解释

### 0.1 核心概念

这六个词是产品的抽象层，不依赖任何具体类型或字段；先读它们，再看后面的条目。

| 名称 | 含义 |
|---|---|
| 订阅声明 / 声明 | 一个组件对「要哪份数据、多久一次」的声明。声明本身不是请求，也不保证已经发过请求；相同身份重复声明是幂等的（U03）。 |
| 数据身份 | Source 身份 ＋ 完整参数值。身份相同即共享同一实例；字段顺序不影响身份，数组顺序影响身份（U02）。 |
| 共享 | 同一身份只保留一个运行实例与至多一份**有效**任务；普通轮询不重叠，被取消但未结束的 `load` 在真实结束前仍占并发槽，可与新任务短暂并行（P03、R06、T12）。 |
| 刷新 | 明确要求重新获取当前身份的数据。显式刷新（`refresh`）与自动刷新（订阅到期）走同一条 `load`、有效性复核与交付路径，共用同一队列与并发上限（U04、U13）。 |
| 资格 | 一个订阅当前是否应该驱动自动刷新的唯一判据，见 U25。资格是推导值，不单独保存。 |
| 刷新要求 | 一次显式刷新尚未满足时的临时要求：必须由一个「在本次动作之后启动」的任务提供结果；未满足前不交付该页（字段记法见 [DESIGN.md](./DESIGN.md) §3.6、F02/F15）。 |

### 0.2 领域名词

| 名称 | 含义与所有者 |
|---|---|
| P / T | 一种业务的固定参数 interface / 请求结果类型。由应用声明，框架不生成运行时 schema。 |
| Source / RefreshSource / SourceRuntime | 一种稳定的资源定义，绑定 `P`、`T`、`load` 和可选同步 `validate`。前两者是公开类型，`SourceRuntime` 是内部擦除类型。应用级常量，定义时冻结。 |
| Parameters | 一次已准备的请求：提交边界复制并深冻结的 `args`，以及完整参数值的稳定编码 `key`。`key` 表示数据范围，不是 Resource 生存期 id。 |
| Resource | 「同一 Source ＋ 同一参数值」的一次共享运行实例。`Manager` 拥有，首个有效需求创建，最后一个订阅与刷新要求都退出销毁。 |
| Manager | 应用协调者，拥有句柄、资源注册表、唯一后台调度与私有 Pinia 分区。 |
| Handle / RefreshHandle | 一个组件的一份需求。内部 `Handle` 记录关系与配置快照；公开 `RefreshHandle` 提供 `display` / `submit` / `refresh`。 |
| Submission | 句柄当前已声明的身份（已准备参数）；临时退出保留，新声明整体替换，校验失败清空。 |
| Subscription | 一次「页面 → Resource」的周期连接，退出即失效；其 `every` 是这条连接当前的间隔。 |
| RefreshWaiter / refresh | 一次显式刷新尚未满足时的临时要求；`minVersion` 是它要求的版本下限，`settle` 只接受第一次结算。 |
| Task / load | 一次共享后台执行及 Source 的请求函数。Task 拥有取消控制器，版本在所属 Resource 内分配。 |
| StoreEntry / ResultStore | 某 Resource 最近一次有效后台结果，及其存储端口（Pinia 适配实现）。 |
| Display / RefreshDisplay | 页面独立的 `args` / `data` / `origin` / `updatedAt` 快照，由 Vue `shallowRef` 唯一持有。 |
| SubmitResult / RefreshResult | 声明与刷新两个入口的判别返回值，见 §2.4。 |
| RefreshError / observer | 错误通知值（含 `origin` 与原始 `error`） / 通知自身失败的诊断来源；诊断不读取原异常内容。 |

### 0.3 实现结构

这些词只在核心内部或设计文档中出现，不理解它们不影响使用产品，但读 §3、§4 与 [DESIGN.md](./DESIGN.md) 会用到。

| 名称 | 含义与所有者 |
|---|---|
| subscription / refreshes | 句柄对某个 Resource 的两类需求：`subscription` 是周期订阅（资格成立时存在），`refreshes` 是未结算的刷新要求。两者互不排斥，可同时存在。 |
| 刷新要求 / RefreshWaiter | `refresh` 建立的临时要求：`{ owner, resource, minVersion, settle }`。`minVersion` 是必须由一次「在本次动作之后启动」的任务满足的版本下限；结算或取消后从 `Handle.refreshes` 与 `Resource.waiters` 两侧移除。 |
| Input / configuration | Vue 配置读取结果。读不到的开关或可见性保留为 `null`，绝不推断为 `false`。核心只读这个快照，不重新调用 getter。 |
| enabled / lifecycleActive / browserVisible | 调用方开启意愿（只读输入，框架从不写入） / 组件是否挂载或激活 / 浏览器是否可见。资格（U25）的三项输入。 |
| operationId / nextResourceId / nextVersion | 声明代次、资源生存期序号、Resource 内任务版本分配计数。三个计数器按安全整数上界假设，耗尽不是产品行为（见 [DESIGN.md](./DESIGN.md) §3.2）。 |
| queue / running | FIFO 待执行 Task / 真实尚未结束的 Task。并发位置与 `running.size` 的唯一事实；由 `scheduler.ts` 独家持有。 |
| Clock / cancelTimer / flushPending | 时间端口（单调调度时间、墙钟时间戳与平台 Timer ID） / 唯一 Timer 的取消函数 / 已安排 flush 微任务的标记。后两项由 `scheduler.ts` 独家持有。 |
| Cleanup / disposed | 释放回调 / 已销毁标记。销毁后同一实例不可复用。 |
| 后台请求 | 框架按订阅或显式刷新发起的 `load`。只有这一条取数通道；页面自己的业务副作用由页面承担，不经过框架。 |

---

## 1. 目标与范围

页面已有查询函数、组件数据和 Pinia Store，但自行编写定时刷新会重复处理定时器、频率变更、
页面失活、浏览器隐藏、手动暂停、取消请求和旧响应覆盖；多个组件需要相同数据时还可能重复请求。

**目标：** 页面表达刷新需求，公共工具负责订阅、共享请求、调度、取消和有效结果交付。
框架按 Resource 保存共享结果的内部副本，只通过入口返回值与本页快照对外交付；页面不直接读写这份副本。

**可检验目标**（每条都由 §4 的现有条目判定，不新增行为）：

| 目标 | 观测指标（框架侧可测代理） | 验收依据 |
|---|---|---|
| 同一身份在任意多个句柄下只有一个实例，且至多一份**有效**后台 `load`；普通轮询不重叠，显式刷新与自动刷新共用同一条队列 | 同 key 的有效任务数 ≤ 1，不随句柄数增长；被取消但未结束的 `load` 在真实结束前仍计入并发上限 | P03/P04、S01、R06、T12 |
| 未到期不发起请求；已有合格结果直接交付，不重复取 | 未到期的重复进入不产生新 `load`；命中已有条目时不产生新 `load` | F01、L03、T03 |
| 关闭、失活或隐藏后不再产生新的后台请求；已取消的排队任务不占并发槽，已开始的执行占槽直到真实结束 | 状态改变后新增 `load` 数为 0；尚未真实结束的 `load` 数不超 `maxConcurrent` | B04/F08、L05/L08、T11、T12 |
| 最后一个需求退出时，实例、分区与排队任务一并消失，在途请求被 abort | 退出后注册表、私有分区与队列都为空；在途 `signal.aborted` 为真 | L02、T13、M07 |
| 轮询不重叠；长时间挂起后恢复不补跑 | 同一实例同时至多一个未结束 `load`；恢复不产生补偿请求 | T06、T09 |
| 显式刷新与自动刷新走同一条获取与交付路径：一次动作最多产生一次新的共享请求，且不吃掉在途请求 | 动作之后启动的任务数 ≤ 1；动作之前发起的多次刷新合并到同一次后继请求 | Q02、Q06、Q13 |
| 页面快照与共享副本之间没有可变别名，页面之间互不影响 | 改页面快照不改分区与 `readSnapshot` 结果，反之亦然 | L09、B12b |
| 交付面同时给出结果的产生时间，页面可以据此显示数据多旧 | `display.updatedAt` 与本次结果同次发布，后加入者读到的仍是原时间 | A03、U16 |

表内指标全部是框架侧可测代理量，由「验收依据」列的现有条目判定，不新增行为。
业务侧收益口径（接口调用量下降百分比、后端容量节省）由接入页面在真实流量下实测，本文档不虚构数值。

**职责划分：** 调用方负责完整参数、业务校验、页面交互与展示；框架负责订阅、共享、调度、取消与交付。
框架不是查询库、缓存层或事务层。

**不纳入：** 产品边界以 §5.1 的裁决表为唯一清单。最常被问的三例——本地时钟与动画、跨标签页共享、
通用保存封装——都不在范围内；其余边界条目与依据同样见该表。
保存后需要重读时，由页面提交一次新的主动查询，不增加保存事务 API。

**实现授权：** 每个 Source 固定业务参数 interface，`submit` 直接传普通对象，`refresh` 不带参数；
不使用通用参数字典或元组 API；参数只有**值域与循环守卫**，不引入第二身份维度。
守卫的完整规则、取值与全部变体见 §3（U24）与 §4（C14）；业务侧规模结论与依据见 §5 G01。
本节只声明授权边界，不复述规则细节。

---

## 2. 公共 API 契约

正式导出只有三个函数与公共类型：`defineRefresh`、`createRefreshManager`、`useRefresh`。
公共类型的唯一代码入口是工程内 `src/public-types.ts`。

### 2.1 三个入口

```ts
interface QuoteParams { account: string; symbol: string }
interface Quote { price: number }

const quote = defineRefresh<QuoteParams, Quote>({
  validate: p => p.account.length > 0 && p.symbol.length > 0,
  load: (p, context) => quoteService.read(p, context.signal),
})

const refresh = createRefreshManager({
  pinia,
  maxConcurrent: applicationConfig.refreshConcurrency,
})
app.use(pinia)
app.use(refresh)

const task = useRefresh(quote, { enabled, every: 2000 })
task.submit({ account: 'demo', symbol: 'DEMO' })  // 声明订阅；同参重复声明是幂等的
await task.refresh()                              // 显式刷新：与自动刷新同一条获取与交付路径
refresh.readSnapshot(quote, { account: 'demo', symbol: 'DEMO' }) // 只读快照
```

`useRefresh` 的第二个参数是 setup 期参数：框架持有这个对象本身，运行期只重新读取它的字段
（`enabled` / `every` / `visible` 由 watch 跟踪，`onError` 在每次通知时读取）。整体替换调用方手里的对象不会生效；
需要动态切换某项输入时，让该字段本身是 Ref / getter，而不是换掉整个对象。

| 接口 | 输入 | 返回 |
|---|---|---|
| `defineRefresh<P,T>` | `{ load, validate? }`，在组件外定义一次 | `RefreshSource<P,T>` |
| `createRefreshManager` | `{ pinia, maxConcurrent }` | `install` / `readSnapshot` / `dispose` |
| `useRefresh` | `(source, { enabled, every, visible?, onError? })` | `RefreshHandle<P,T>` |
| `handle.submit` | `(params: P)` | 同步 `SubmitResult`；`accepted` 表示身份已记录，不代表请求成功；同身份重复声明幂等 |
| `handle.refresh` | 无 | `Promise<RefreshResult>`；无 DTO，取消立即结算；不恢复自动刷新 |
| `manager.readSnapshot` | `(source, params)` | 独立 `T` 或 `undefined`；读取错误抛给调用者 |
| `manager.dispose` | 无 | 幂等释放 |

### 2.2 完整类型与状态取值

每个封闭状态域在代码中枚举为一个常量对象（成员名 + 字面量），公开类型由对象推导：
新增、删除或收敛一个状态只需改常量对象。句柄的订阅与刷新要求只在核心内部（`model.ts`），不在公开契约内。
不使用 TS enum —— 工程的 `erasableSyntaxOnly`
与 Node 的类型擦除都不接受 enum 语法。

```ts
import type { App, Ref, ShallowRef } from 'vue'
import type { Pinia } from 'pinia'
export type DeepReadonly<T> =
  T extends (...args: never[]) => unknown ? T :
  T extends object ? { readonly [K in keyof T]: DeepReadonly<T[K]> } : T
export type RefreshInput<T> = T | Readonly<Ref<T>> | (() => T)
export const RequestOrigin = {
  Refresh: 'refresh',
  Background: 'background',
} as const
export type RequestOrigin = (typeof RequestOrigin)[keyof typeof RequestOrigin]
export const ErrorOrigin = {
  Background: RequestOrigin.Background,
  Validation: 'validation',
  Configuration: 'configuration',
} as const
export type ErrorOrigin = (typeof ErrorOrigin)[keyof typeof ErrorOrigin]
export type RefreshErrorOrigin = Exclude<ErrorOrigin, 'validation'>
export const CancelReason = {
  Superseded: 'superseded',
  Unavailable: 'unavailable',
  Disposed: 'disposed',
} as const
export type CancelReason = (typeof CancelReason)[keyof typeof CancelReason]
export type SubmitCancelReason = Exclude<CancelReason, typeof CancelReason.Unavailable>
declare const sourceBrand: unique symbol
export interface RefreshSource<P extends object, T> {
  readonly [sourceBrand]: { readonly args: (value: P) => P; readonly data: (value: T) => T }
}
export interface RefreshLoadContext { readonly signal: AbortSignal }
export interface RefreshError {
  readonly origin: ErrorOrigin
  readonly error: unknown
}
export type SubmitResult =
  | { readonly status: 'accepted' }
  | { readonly status: 'rejected'; readonly error: unknown }
  | { readonly status: 'cancelled'; readonly reason: SubmitCancelReason }
export type RefreshResult =
  | { readonly status: 'success' }
  | { readonly status: 'error'; readonly origin: RefreshErrorOrigin; readonly error: unknown }
  | { readonly status: 'cancelled'; readonly reason: CancelReason }
export interface RefreshDisplay<P extends object, T> {
  readonly args: DeepReadonly<P>
  readonly data: DeepReadonly<T>
  readonly origin: RequestOrigin
  readonly updatedAt: number
}
export interface RefreshOptions {
  readonly enabled: RefreshInput<boolean>
  readonly every: RefreshInput<number>
  readonly visible?: RefreshInput<boolean>
  readonly onError?: (error: RefreshError) => void | Promise<void>
}
export interface RefreshHandle<P extends object, T> {
  readonly display: Readonly<ShallowRef<RefreshDisplay<P, T> | null>>
  submit(args: P): SubmitResult
  refresh(): Promise<RefreshResult>
}
export interface RefreshManagerOptions {
  readonly pinia: Pinia
  readonly maxConcurrent: number
}
export interface RefreshManager {
  install(app: App): void
  readSnapshot<P extends object, T>(source: RefreshSource<P, T>, args: P): DeepReadonly<T> | undefined
  dispose(): void
}
```

### 2.3 安装与销毁

`createRefreshManager` 要求应用 Pinia 与正安全整数 `maxConcurrent`。
`app.use` 安装；同实例同 App 幂等，跨 App 或同 App 多活跃实例拒绝且不破坏已有合法绑定。

Vue 适配层为每个 App 只 `provide` 一次绑定槽位（内容是当前 Manager），同 App 重建 Manager 时原地替换；核心不保存适配层状态。
应用卸载或显式 `dispose` 释放自身监听、句柄、资源、Timer 与私有 Store。已销毁实例不复用。
SSR 不注册浏览器监听、不设置 Timer、不发请求；每个应用独立创建。

### 2.4 返回值与错误契约

| 操作 | 结果与副作用 |
|---|---|
| `submit` | 同步 `accepted` 表示身份已记录，**不是**请求成功；相同身份重复声明幂等（不产生新请求、不重建订阅）；参数被拒返回 `rejected` 并清空本次声明、保留画面；`disposed` / `superseded` 返回 `cancelled` |
| `refresh` 入口 | 未声明身份、不可见或失活返回 `cancelled` / `unavailable`；配置非法返回 `error` / `configuration`；已销毁返回 `cancelled` / `disposed`。入口拒绝不产生请求，也不改动已声明身份 |
| `refresh` 合并 | 要求一个「在本次动作之后启动」的任务：排队未启动的任务可直接满足它；在执行的任务不算，等它结束后补一次后继请求；动作之前发起的多次刷新合并到同一次后继请求 |
| `refresh` 结算 | 该任务成功返回 `success`，结果按共享交付路径同次发布给本页与其他有效接收者；任务失败返回 `error` / `background`，先结算再通知，保留旧画面与开启意愿，下个周期继续；**框架不改写调用方 `enabled`**；取消及时结算，不等底层结束 |
| `background` | 当前失败通知仍有效的捕获订阅，保留开启意愿与需求，按下个周期继续 |
| `onError` | 不等待 Promise；同步异常与异步拒绝只进入固定 observer 诊断，不递归 onError、不改已结算结果 |

取消原因只有 `superseded` / `unavailable` / `disposed`。
配置无效通知与诊断内容的行为规则见 §3（U18、U19）；本节只规定取值与返回值形状。
错误通知只带 `origin` 与 `error`；框架身份（操作号、实例 id、任务版本）不出现在公开通知里，只用于 observer 诊断事件的日志关联（见 [DESIGN.md](./DESIGN.md) §3.8）。

表中各行的失败含义（动作失败 / 条目暂不可得 / 诊断）按 §3 的三分法归类，同一含义不因入口不同而改变处理方式。
其中 `configuration` 是**诊断类通知在 `onError` 通道上的一处例外**：错误来源是调用方自己的输入，
且可以发生在任何页面操作之前，因此和 `submit` 参数被拒一样直接回报调用方；处理方式仍是诊断——
只报一次错误阶段，不改变任何已定结果、快照、意愿与调度（U18、U19）。

### 2.5 交付面、刷新动作与只读快照

`display` 是唯一交付面：`args` / `data` / `origin` 与 `updatedAt` 同次整体发布，不出现半新半旧的画面。
`updatedAt` 是产生本条结果的墙钟 epoch 毫秒；后加入的订阅者读到已有结果时，拿到的仍是原结果时间，
因此页面可以据此显示数据多旧。它是墙钟读数，不保证单调、也不承诺 `updatedAt ≤ Date.now()`（系统校时可能回拨），
页面计算相对时间时应自行把差值钳制到 0。
框架不交付「正在刷新」这类实时状态；「数据多旧」与「失败」已分别由 `updatedAt` 与 `onError` 交付（U15、U19）。依据见 §5.1。

`refresh()` 的 Promise 只报这次刷新的结算（`success` / `error` / `cancelled`），**不携带 DTO**：
数据仍只经 `display` 交付，因此页面不会因为多一条通道而持有第二份结果副本。
需要「取到数据之后再写业务 Store」的页面由自己的业务层承担：在页面的请求适配器里处理，
或等待本次刷新结算后再读 `display`；框架不代管业务副作用，也不再提供受保护的同步提交入口。
`refresh` 不恢复自动刷新，也不改写调用方 `enabled`。

`readSnapshot` 先检查 `disposed`；活跃时只计算参数键，不复制或冻结参数、不调用 `validate`。
查到分区返回独立副本，无分区返回 `undefined`，`null` 是有效结果。
它只交付值、不交付时间，也不承诺新鲜度——要时间就订阅（依据见 §5.1）。
编码或复制错误抛给读取者；不通知页面、不创建 Resource、不保活后台任务。

---

## 3. 行为规则（唯一维护入口）

规则以**能力域**为阅读单位：R1–R8 是全部行为承诺，每条能力域由「机制 ＋ 触发清单 ＋ 判定」组成。
触发清单里的 `U01`–`U25` 是**锚点**：跨文档引用一律指向锚点，编号一经发布不复用、不重排，新增行为加锚点而不是改号。
一个触发可由多个验收场景验证，一个场景也可支撑多个触发；验收叶子项见 §4。
`pnpm check:docs` 断言锚点不重不漏，且本文件与 DESIGN、README 里出现的每个 `U` 引用都有落点。

失败只有三个产品含义，三者互不替代：**动作失败**——页面按下的那一次没有成功；框架只结算并通知，是否停止自动刷新由调用方在 `onError` 里决定（`U06`）；
**条目暂不可得**——共享数据这一次没取到，需求与开启意愿都保留，下个周期继续（`U15`）；
**诊断**——通知或清理自身失败，只记录固定字段，不改变任何已定结果、快照、意愿与调度（`U19`）。

**附条 A（刷新要求，`U04`、`U05`、`U06`、`U10` 共用）。** 显式刷新为当前身份登记一个「不低于某版本」的临时要求：
没有当前任务时由它登记这次请求；已有任务（排队或执行中）则等它，或等它结束后补一次后继请求。
要求成功即结算 `success` 并按共享交付路径发布，失败按 `error` 结算并保留旧画面，页面退出、暂停边沿或卸载则结算 `cancelled`。
取消、结算与清理都只作用于这一个要求对象，因此旧路径不会覆盖新的声明或新的刷新。

### 3.1 R1 共享结果与身份

**机制：** 已准备参数相同的调用共享同一次请求与同一份结果，参数变化立即换轮次。

- `U01` 显式 `submit` 接纳完整参数，表单草稿变化无效果。接纳后快照与原对象隔离，自动刷新、显式刷新与恢复复用同一份已准备参数。
- `U02` 同 Manager 内由 Source 身份＋全部参数值决定共享；字段顺序不同仍共享，数组顺序影响身份。账户与请求上下文必须纳入参数，或由独立 Source 表达；应用级隔离由每个 App 独立的 Manager 与私有分区保证（`U21`）。
- `U03` 相同身份的 `submit` 是幂等的：保留现有订阅与未结算的刷新要求，不产生新请求、不重建订阅；身份变化立即成为当前意图并整体替换。加入他人已有的 Resource 不取消其首查。
- `U05` 显式刷新按附条 A 登记刷新要求并结算；成功结果与自动刷新同次交付给全部有效接收者。

判定：P01/P02/P08–P10、B07、C01–C03、C13/C14、P03–P07、B15、S01/S06、C12、Q06、R06/R07、B01、M05、Q04、B02/B03/B14、M03、F02/F15

### 3.2 R2 订阅生命周期与调度

**机制：** 随挂载、更新、卸载自动保持结果新鲜度，退出即无残留请求与资源。

- `U08` 退订立即停止页面交付并保留画面；最后一个订阅与刷新要求都退出时删除 Resource、Store 分区与排队任务，并 abort 在途执行。
- `U09` 资格成立（`U25`）时普通恢复：Resource 存在则交付合格历史结果，否则创建并首查。mounted/activated 重复不重复订阅；卸载永久释放。
- `U11` Resource 取有效订阅的最短 `every`；主动改频率只更新该订阅的间隔并保留在途请求，由下一次调度按新间隔重算到期（缩短到已到期时正常入队）；其他订阅退出导致的最短间隔变化只重新调度。
- `U12` 成功或失败的正常结束后开始计算下一间隔；取消不计时、不补跑漏掉的周期、普通轮询不重叠。一个 Timer 按最近到期唤醒，超平台范围时分段。
- `U13` 后台 FIFO 并发槽统计真实未结束的 `load`；取消不提前释放槽。显式刷新与自动刷新共用这些槽位，不绕过并发上限；满槽不自旋，finally 释放后继续。

判定：L01/L02/L09/L10、S04、T13、M01/M07、L03–L08、B06、Q07/Q10、F01、T01–T05、B04、M02、F08/F13、T06–T09/T11/T13、C09、F12/F14、T10–T12、R10、M04、F11/F12

### 3.3 R3 资格条件

**机制：** 一个合取判据决定后台订阅是否成立，失败原因不外露。

- `U07` `enabled` 是唯一的开启意愿，**只由调用方写入**，框架只读；它是 `U25` 的一个输入，不参与刷新入口。`true→false` 的关闭边沿结算当时未完成的刷新要求；暂停之后仍可新发一次刷新。
- `U10` 刷新要求随页面退出（失活、隐藏、卸载）或暂停边沿结算为 `cancelled`；恢复按订阅规则处理：Resource 存在则交付合格历史结果，否则创建并首查，**不重放刷新动作、不恢复旧参数**。
- `U25` **资格**：一个订阅是否成立由这一个合取唯一判定——存活（Manager 与句柄都未销毁）、已声明身份（存在有效 Submission）、环境允许（组件生命周期激活且浏览器可见）、配置开启（快照有效且 `enabled` 与 `visible` 均为真）。刷新要求不参与资格：它由 `refresh` 的入口闸与 `waiters` 集合自身的归属表达，因此暂停页仍可刷新，而刷新不会把暂停页变回订阅。`U07` / `U09` / `U18` 只描述其中输入自身的来源与边沿，不构成第二套判据；判定只读事实、不产生副作用，取消与恢复都由它推导。

判定：Q01/Q10/Q11、B05/B09、M02、F10、D01–D04、M05、L07/L08、B05/B06、F09/F10

### 3.4 R4 交付门槛与交付面

**机制：** 交付到页面的永远是满足当前条件的有效结果，且是整体发布的。

- `U14` 新声明、新任务先建立身份再通知旧取消。await、外部回调、复制原始结果、写 Store 与发布 Display 之后都要复核当前身份；旧 finally 只清自身。
- `U16` DTO 由请求适配器校验；框架拒绝 `undefined` 并原生复制。Store、各页面与 `readSnapshot` 之间没有可变别名；Display 的参数、数据与结果产生时间同次发布；`null` 是有效结果。

判定：R05–R08、B10/B11、B16a、M03/M08、F03/F04/F06/F07、L09、B03/B12a/B12b、C04/C11

### 3.5 R5 失败与通知

**机制：** 三类失败可区分、可读、互不替代；失败不改变已定结果。

- `U06` 当前共享请求失败先结算 `error` 再通知，**框架不修改调用方的 `enabled`**。已声明身份保留：`enabled` 仍为真时下个周期继续；调用方要停止自动刷新就在 `onError` 里自行关闭 `enabled`。校验失败同样只结算；取消与过期结果不报告失败。
- `U15` 后台失败保留需求与 `enabled`，旧画面不变，下个周期继续；对捕获且轮到时仍有效的订阅各通知一次，本次未结算的刷新要求按同一失败结算。
- `U18` Vue 同步 watch 只读 `enabled` / `every` / `visible`，先更新配置快照再协调；这三项输入的有效性由 `U25` 统一判定。配置快照无效时对该句柄通知一次 `configuration` 并在连续无效期间去重；配置非法拒绝新刷新，订阅按资格退出（在途共享请求随最后一个需求退出被取消）；未知开关不猜为 `false`。
- `U19` `onError` 同步调用、异步拒绝立即观察，不等待、不递归。失败只记录固定 observer 说明，并在身份存在时一并记录框架身份（validate 违约发生在声明代次产生之前，因此那一条不带身份）；不读取原异常属性，不影响已提交结果与必要清理。
- `U20` HTTP 适配负责业务 schema、超时与认证；`load` 的 Promise 覆盖真实执行和响应体完成。abort 不承诺服务端停止；不得先写业务 Store 再交框架检查；需要按结果写业务 Store 的页面在自己的适配层完成，不经过框架。

判定：Q03/Q05/Q07/Q08、R11、B10、M06/M08、F05/F06、R01–R03、T07、D03、F14、T14、B04/B07/B08、C10、A07、F09/F10、B10/B11、C05、R09–R11、S08、T12

### 3.6 R6 显式刷新与共享取数

**机制：** 页面显式刷新与自动刷新共用同一条获取、有效性复核与交付路径；框架只有这一条取数通道。

- `U04` `refresh` 为当前身份登记一次刷新要求：与自动刷新同一条 `load`、同一队列、同一交付；不携带 DTO，不恢复自动刷新。需要独立副作用或绕过共享限流的业务查询由页面自己的业务层承担。

判定：Q01/Q02/Q13、B16a

### 3.7 R7 共享结果直读

**机制：** 非组件代码可读共享副本，不建实例、不保活。

- `U17` `readSnapshot` 只计算参数键并读取独立副本；不校验业务字段、不创建或保活 Resource。无结果或已销毁返回 `undefined`，读取错误抛给调用者。

判定：S03、B13a/B13b、A06

### 3.8 R8 规模、隔离与工程边界

**机制：** 参数规模由应用决定；应用之间不共享任何运行时状态。

- `U21` 每个 App 独立 Manager 与 Pinia 绑定；同实例同 App 安装幂等，非法跨 App 或多活跃绑定被拒。SSR 无请求与 Timer；`dispose` 清自身绑定、监听、资源与私有 Store，保留页面画面与开启意愿。
- `U22` 公共入口只有三个函数；参数与结果类型固定关联；`useRefresh` 必须在同步 `setup` 调用；`display` 只读，`enabled` / `every` / `visible` 都是只读输入；`refresh` 不接受参数，其结算不携带 DTO。
- `U23` 工程只从 `src` 构建包；库导出、类型检查、单元/集成与浏览器测试验证当前实现。未运行的分支不记通过。
- `U24` 参数只有值域与循环守卫：完整参数值稳定编码为身份键；`-0`、`undefined`、非有限数等歧义值拒绝；根容器记 1，只有循环或病态嵌套命中内部深度守卫（1000 层）并按非法参数拒绝。版本范围与规模结论见 §5。

判定：S05–S07、L10、M09、C08、A05、S09、A01–A03、A05–A07、C06、F07、M10、C03、C13、C14

---

## 4. 验收目录

### 4.1 判定记法与归属（取证方法见 §6.4）

**归属前缀。** 每个叶子项自带一个前缀，判据是**判定它是否需要调用方观察不到的内部事实**：

- `[行为]` 产品可观察行为：判定只靠公开面，逐条可回答「这条对外承诺了什么」，可作为 README、合同与业务接入的依据。
- `[不变量]` 实现不变量：判定依赖内部结构与顺序纪律，用于回归与定向故障注入，不构成对外承诺。
- `[混合]` 混合条目：外层结果可由公开面断言，但「必须结果」里另含一条内部一致性要求。

前缀与叶子同格，新增或删除条目只改一处；`pnpm check:docs` 断言每个叶子恰好有一个前缀、前缀取值封闭。
不得为通过测试而删除或放宽条目；行为随契约移除而消失时，同步删除对应条目并记录理由。
带内部一致性子句的条目（如 T11 的「不留下空转 Timer」、R05 的「旧 finally 不能删除新实例」、
B10 的「旧 finally 不关闭新操作」、B11 的「有界遍历」）与 T12 同规则：那类子句按 §6.4 的定向故障注入取证，
不改变条目前缀。§3 规则的确定性由规则文本承载，§4 只提供证据与「谁可以依赖它」。

**变体记法。** 需要分别判断的输入不另设索引表，直接写在条目「必须结果」列尾：`变体：01 …；02 …`。
编号是稳定标签，不保证连续：一旦分配即不复用、不重排，移出与移回都保留原号。
未取得证据的变体在尾部注明，或在工程 README 的「尚未取得的证据」中列出。

**按需入口：** 当前按 §7 推进既有实现优化，实际证据见工程 README；逐 ID 证据账见
`scripts/trace-leaves.mjs` 顶部的 `EVIDENCE` / `STATIC` / `PARTIAL` / `UNVERIFIED` 表，状态词同 §6.2。
只把已运行断言对应的分支记为已验证；某条目只覆盖部分分支时记「部分验证」，其余维持未验证。
§4 保留为回归目录，不要求开工前逐项展开；怎么取证的执行方法见 §6.4（非规范性）。

一个验收叶子项对应一个可独立判定的行为；`B12` / `B13` / `B16` 是历史父编号，以带字母子项记录结果。
`Q09`、`R04` 在框架不再改写 `enabled` 之后整体空转，已分别并入 `Q01` 与 `A02`；`Q12`、`S02` 与 `A04` 随独立查询通道与 `commit` 的删除整体移出。这些编号都按变体/条目编号通则留空，不复用。
一个测试可关联多个验收 ID 并复用证据（如 Q02/Q13、Q01/Q05、S03/B13a），
但条目通过必须覆盖其全部必需分支。执行记录见 §6.2；未执行要写原因。

**记号：** 甲 / 乙 指两个独立组件句柄；A / B / C 指不同参数值（因而通常是不同 Resource）。

### 4.2 参数、身份和共享（P）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] P01 | 修改查询表单但不提交 | 不换参数、不发请求、不影响当前订阅。 |
| [行为] P02 | 提交完整参数对象后修改原参数对象/嵌套对象 | 框架参数快照与 Resource 身份不变；变体：01 修改根参数对象；02 修改嵌套对象。 |
| [行为] P03 | 两个新句柄首次 submit，同来源同参数值；输入为不同对象且字段顺序不同 | 同 Manager 下同 key 命中一个 Resource、两个独立订阅；乙加入不取消甲的在途首查；变体：01 新参数对象但同值；02 不同对象且字段顺序不同；03 嵌套对象顺序不同。 |
| [行为] P04 | 同来源、不同品种 | 两个 Resource，数据分区和调度互不覆盖。 |
| [行为] P05 | 同 URL、不同来源对象 | 不自动合并。 |
| [行为] P06 | 甲乙共享 A，甲提交 B | 甲退出 A 并进入 B；乙的 A 不取消。 |
| [行为] P07 | B 最后一个订阅切到 C 再回 B；或唯一句柄同参重提 | B 原实例删除；相同 key 重新 attach 也新建 Resource/id，不复活旧结果；变体：01 B→C→B；02 唯一句柄同参重提后同 key 新 id。 |
| [行为] P08 | 无效新提交 | 退出旧订阅，不请求、不恢复旧参数，保留画面。 |
| [行为] P09 | 空值业务是否合法 | 仅由校验回调/调用方决定，不擅自将 0、空串判为无效；变体：01 0；02 空字符串。 |
| [行为] P10 | 非对象根、非 JSON 值、循环对象及超限参数 | 不建立需求，按 validation 报告；不扩展为任意特殊 JS 对象探测承诺；变体：01 非对象根拒绝；02 循环不属于 JSON 参数、不建立需求；03 超过内部深度守卫（1000 层）拒绝；04 大参数（1000 个客户）不被拒绝。 |

### 4.3 页面主动查询（Q）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] Q01 | 暂停页显式刷新 | 走共享路径取一次并更新本页；不恢复自动刷新、不改变 `enabled`。 |
| [行为] Q02 | 已声明身份时显式刷新 | 与自动刷新同一条 `load`、同一队列、同一交付；不复用在途结果（见 Q06），已有合格结果时按规则交付。 |
| [行为] Q03 | 刷新失败 | 本次等待按 `error` / `background` 结算并通知，保留旧画面；框架不改动开关，需要停止自动刷新的页面在 `onError` 里关闭。 |
| [行为] Q04 | 刷新成功 | 结果与自动刷新同次交付：本页与其他有效接收者一起更新；不产生第二条取数通道，也不回退到旧分区。 |
| [行为] Q05 | 刷新期间身份切到 B，旧生存期的任务晚返回 | 只有当前身份的结果可交付、可报错；旧生存期的成功、失败与 finally 都不影响新实例；变体：01 旧任务晚到成功；02 旧任务晚到失败。 |
| [行为] Q06 | 同一身份连续两次刷新 | 两次都要求「动作之后启动」的任务：在途任务不算，它结束后只补一次后继请求，两次刷新合并到同一次。 |
| [行为] Q07 | 刷新中用户关闭或失活 | 关闭边沿与失去存在都结算该刷新要求为 `cancelled`；晚到的成功不能重开订阅。 |
| [行为] Q08 | 刷新失败后再刷新（调用方已关闭开关） | 刷新不要求开启意愿：暂停页仍可再刷新一次；没有已声明身份则结算 `unavailable`，不产生请求；变体：01 存在有效声明；02 没有有效声明。 |
| [行为] Q10 | 页面默认开启但尚未声明身份 | 尚未声明时不请求；声明后的正常成功、失败后手动开启及取消恢复分别按 Q04、Q08、D01 判断。 |
| [行为] Q11 | 无启停按钮，刷新失败后再次刷新成功 | 可按页面业务重新声明并刷新，不要求手动开启按钮。 |
| [行为] Q13 | 所有共享并发槽占满后显式刷新 | 刷新登记同一队列并排队等待真实结束；不绕过并发上限、不提前 abort 在途请求。 |

### 4.4 生命周期、退订和数据冻结（L）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] L01 | 甲乙共享，甲失活/隐藏/关闭 | 甲退订并冻结快照，乙继续，不能取消乙的共享请求；变体：01 组件失活；02 浏览器隐藏；03 手动关闭。 |
| [行为] L02 | 最后订阅退出 | 取消请求、清队列项、删 Resource 与 Pinia 分区；页面快照保留。 |
| [行为] L03 | 已生效订阅恢复且 Resource 仍存在 | 交付符合交付版本要求的已有快照；到期才调度，不无条件重发。 |
| [行为] L04 | 恢复且 Resource 已删除 | 新建首查，无历史缓存或旧刷新时间。 |
| [行为] L05 | 用户关闭后反复失活/激活 | 开关不变，不订阅、不请求。 |
| [行为] L06 | 框架首查中失活、请求取消 | 不报首查失败；恢复可重新首查。 |
| [行为] L07 | mounted/activated 重复及祖先失活 | 无重复订阅；祖先失活时不运行；变体：01 mounted/activated 重复；02 祖先失活。 |
| [行为] L08 | 自定义页签隐藏；初始 visible=false 或浏览器隐藏时挂载 | 已运行时立即退订；初始隐藏时不发请求、不先发再取消；恢复规则一致；变体：01 运行后 visible=false；02 初始 visible=false 挂载；03 初始浏览器隐藏挂载。 |
| [行为] L09 | 甲退订后乙更新深层 Store 对象 | 甲嵌套快照不变，不仅顶层引用不同。 |
| [行为] L10 | 卸载后所有异步回调返回 | 不交付、不写 Store、不重新创建任务；变体：01 旧成功返回；02 旧失败返回。 |

### 4.5 频率、计时和并发（T）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] T01 | 甲 1s、乙 5s 共享 | 一个 Resource，按 1s 调度。 |
| [行为] T02 | 甲退出，剩乙 5s | 改为 5s，按上次结束计时，不额外立即请求。 |
| [行为] T03 | 乙以更短间隔新加入 | 先交付已有数据，按新间隔判断到期，不无条件替换在途。 |
| [行为] T04 | 用户主动改频率且有效订阅 | 只更新该订阅的间隔并保留在途请求；按「上次正常结束＋新间隔」重算到期，缩短到已到期时正常入队。 |
| [行为] T05 | 关闭/失活/主动查询中改频率 | 仅更新配置，不提前发请求或建立订阅；变体：01 关闭时改频率；02 失活时改频率；03 刷新期间改频率。 |
| [行为] T06 | 请求耗时大于间隔 | 普通轮询不重叠，结束后再等完整间隔。 |
| [行为] T07 | 请求失败 | 正常记录结束时间，下一间隔重试。 |
| [行为] T08 | 请求取消 | 不更新刷新时间，不交付失败。 |
| [行为] T09 | 浏览器长时间挂起后恢复 | 不追补次数，不爆发补跑。 |
| [行为] T10 | 多 Resource 同时到期 | FIFO 排队，尚未结束的框架 load 执行数不超过上限；不推断服务端连接数。 |
| [行为] T11 | 排队任务取消 | 永不调用 load，不留下空转 Timer。 |
| [行为] T12 | 在途任务忽略 abort | 直至执行真实结束才释放槽；截止仅包装返回、底层未结束的适配器不能冒充通过。 |
| [行为] T13 | 所有 Resource 清空 | 无周期 Timer、排队项及 Store 分区。 |
| [行为] T14 | every 完整数值域及边界 | 仅接受正安全整数毫秒；不转换或取整；非法值明确拒绝配置且不形成忙循环，不伪装网络失败；合法长间隔按平台能力分段，不宣称毫秒级实际精度；变体：01 NaN；02 0；03 负数；04 Infinity；05 -Infinity；06 -0；07 正小数（如 0.5、500.5）；08 超过 Number.MAX_SAFE_INTEGER；09 非 number（数值字符串、null、undefined、boolean、bigint、装箱 Number）；10 合法 1 及 Number.MAX_SAFE_INTEGER；11 合法 500 及 Ref/getter 解包后使用同一判定。 |

### 4.6 错误与竞态（R）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] R01 | Resource 首查或周期请求失败；多个订阅提供 onError | 失败本身保留订阅；按 [DESIGN.md](./DESIGN.md) §6.4 对捕获且仍有效的各订阅最多通知一次 background；新加入者不补收，无回调者无须通知；下周期继续；变体：01 首查失败、两有效订阅各一次；02 周期失败、两有效订阅各一次；03 首个 onError 抛错，报告 observer 且其余有效订阅继续；04 后续订阅已退订则跳过（含同 Handle 退出再加入）；05 首个回调只退自身、Task 仍有效，其他订阅继续；06 回调中新增订阅不补收；07 无 onError 的订阅保留且不影响其他通知；08 DTO 准备失败使用相同通知规则。 |
| [行为] R02 | 甲刷新页与乙订阅页共享同一实例，后台失败 | 按同一规则分别通知所有仍有效订阅，不按页面类型关闭订阅；本次未结算的刷新要求按同一失败结算；Store 与各页快照不变。 |
| [行为] R03 | 后续轮询失败 | 旧 DTO/页面快照保留，无错误 DTO 覆盖。 |
| [行为] R05 | A 删除后同 key 新建，旧 A 才返回 | 旧结果不能写新分区；旧 finally 不能删除新实例。 |
| [行为] R06 | 参数切到已有 B，B 有在途任务 | 不替换、不 abort 在途任务：声明只换身份，B 的既有订阅与新身份各自按当前任务继续；刷新要求等它结束或补一次后继请求。 |
| [行为] R07 | A→B→A 或同参重复提交 | 靠声明代次与任务身份防旧写；同参重复提交是幂等的，复用实例与订阅、不替换任务；变体：01 A→B→A；02 同参重复 submit 且其他订阅者维持实例。 |
| [行为] R08 | 成功回调同步关闭或提交新参数 | 后续交付重新检查身份，不交付过期内容；变体：01 回调关闭；02 回调提交新参数。 |
| [行为] R09 | 拦截器吞错、返回 undefined | 请求接入测试拒绝伪成功，不误更新 DTO；变体：01 拦截器吞错产生 undefined；02 直接返回 undefined。 |
| [行为] R10 | HTTP 头已返回、响应体仍挂起 | 取消与截止覆盖 body，不永久占并发槽。 |
| [行为] R11 | 被取消旧请求抛出普通 Error | 因身份失效而静默，不显示过期错误。 |

### 4.7 Store、应用隔离和生产验证（S）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] S01 | 同 Source 不同参数结果返回顺序颠倒 | 各写自己的分区，无相互覆盖。 |
| [行为] S03 | 无订阅组件显式读取 Store 快照 | 不创建订阅、不延长生命周期；分区缺失可明确处理。 |
| [行为] S04 | 最后退订删除分区后重订阅 | 没有旧数据直接交付，等待新有效结果。 |
| [行为] S05 | 两个应用或两个 SSR 请求 | 无共享 Manager/Store/Timer/用户数据；变体：01 两个客户端应用；02 两个 SSR 请求。 |
| [行为] S06 | 会话切换时旧请求晚到 | 不写入新会话 Store，不交付新页面。 |
| [行为] S07 | Manager 所在应用模块 HMR/应用销毁再启动 | 调用 dispose；无重复监听、Timer 或旧异步写入；旧私有 Store 与其 state 释放，其他业务 Store 不受影响；变体：01 HMR 重建；02 应用销毁后重建；03 同一 Pinia 连续创建/销毁 Manager 后私有 Store/state 不累积。 |
| [行为] S08 | 真实 HTTP 取消与超时 | signal 到达传输层，失败可识别，清理完成；变体：01 真实传输取消；02 真实传输超时。 |
| [行为] S09 | 类型检查 | 必填字段、字段类型和返回 DTO 错误在编译期被拒绝；不能通过扩大 Source 泛型绕开约束；变体：01 缺少必填字段；02 字段类型错误；03 load 返回 DTO 类型错误；04 向 `refresh` 传参或用其结果里的数据字段被类型拒绝；05 扩大 Source 参数泛型后调用；06 扩大 Source DTO 泛型后调用。 |

### 4.8 错误通知与刷新要求（B、D）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] B01 | 句柄曾声明，当前因暂停已无实例，再同参 submit | 幂等：只确认同一身份，不因为「没有实例」而变成强制刷新；需要新取数就调用 refresh。 |
| [行为] B02 | 刷新得到新画面之后，下一次请求失败 | 页面保留已交付的新画面，不回退到更旧的分区结果；失败只通知，不改已交付的快照。 |
| [行为] B03 | 保留 A 画面时提交 B，B 失败 | 展示参数仍标注 A，不拿 B 的分页/标题标注 A 数据。 |
| [行为] B04 | 同一同步栈内多次修改 every 后关闭 | 改频率不产生请求；关闭边沿让在途任务失效且不留下排队任务，微任务执行前发现关闭就不再请求。 |
| [行为] B05 | 关闭边沿之后再刷新，并再次赋同值 false | 刷新允许执行；`false→false` 不构成关闭边沿，不取消已发起的刷新要求。 |
| [行为] B06 | 失活期间调用 submit 与 refresh | submit 只记录身份（不建立订阅）；refresh 返回 unavailable 且不产生请求、不替换已声明身份；变体：01 失活时 submit；02 失活时 refresh。 |
| [行为] B07 | 新参数校验失败后改频率/隐藏恢复 | 不恢复旧参数，不建立订阅；只有新的有效声明解除无身份事实；变体：01 校验失败后改频率；02 校验失败后隐藏再恢复。 |
| [行为] B08 | 配置 getter 抛错，随后其他 getter 变化并修正 | 依赖仍被跟踪，错误不忙循环；未结算的刷新要求不因其它 getter 抛错被取消；修正后按已声明身份与当前资格协调，遵守 Q08/D01。 |
| [行为] B09 | 无启停按钮，前次失败后在回调里开启意愿并声明新身份 | 开启意愿与声明在同一个同步块内完成：框架先退出旧订阅、按新身份请求，绝不先按旧参数发起共享请求。 |
| [行为] B10 | onError 同步重入/抛错或返回 Promise 拒绝，最终诊断出口也可能抛错 | 旧 finally 不关闭新操作；立即观察异步拒绝但不等待；每个同步/异步通知异常仅尝试一次 console.error，按 observer 诊断通道报告，日志只含固定说明与框架身份标识，不传原始异常；晚到拒绝只诊断，不改既定结果；不回调 onError；最终报告失败吞掉，不递归、不阻断其他有效通知与清理；变体：01 onError 同步开始新刷新；02 onError 同步抛错后 console.error 仅报告一次 observer；03 console.error 不可用或抛错且不递归、不影响既定结果/清理；04 返回 Promise 异步拒绝，按 refresh/background/validation/configuration 来源分别取代表用例，均一次 observer 且无未处理拒绝；05 首个通知一直 pending，其他有效订阅同步继续，刷新结算和清理不等待；06 通知返回之后任务被替换或 Manager 销毁，此时才发生的 Promise 拒绝仍只写诊断，不改新操作、画面、开启意愿与调度时间；07 async 回调在首个 await 前同步替换 Task，旧通知仍立即停止；08 onError 返回已拒绝的 Promise 与返回成功值分别处理——前者观察拒绝，后者只忽略完成值；09 来自另一 realm 的 Promise 与运行期 thenable 的拒绝同样被观察，不依赖 `instanceof Promise`；10 同步异常/异步拒绝含合成凭证、参数、message/stack/cause 或嵌套请求配置，全部日志实参不含敏感标记或原对象；11 异常的 getter/toString/序列化方法抛错或产生副作用，诊断不读取也不调用，必要清理继续。 |
| [行为] B11 | 一个交付回调退订又新增订阅 | 有界遍历，旧版本不交付给新操作，不重复/无限通知。 |
| [行为] B12a | 新后台结果在提交前 DTO 校验/复制失败 | 本次不写分区或任何页面；按后台失败计时继续。提交后同步重入导致停止后续交付见 F04，不要求跨页面原子更新。 |
| [行为] B12b | Service 返回后修改原对象 | 返回对象的嵌套别名不能改变 Store 或页面快照。 |
| [行为] B13a | 读取不存在的分区 | 返回 undefined，不创建资源、订阅或后台请求。 |
| [行为] B13b | 多个订阅频率不同 | 所有订阅随每次共享成功更新，不各设交付 Timer。 |
| [行为] B14 | 刷新要求登记后实例被删除并同键重建 | 等待者绑定实例身份，旧实例的版本不影响新实例；新实例有结果即交付，没有则首查。 |
| [行为] B15 | 同一 URL 隐含租户/权限条件不同 | Source/参数边界或应用级隔离明确区分，不错误共享。 |
| [行为] B16a | 刷新未结算时同步声明新身份 | 旧刷新要求结算为 `superseded`，其任务结果不再发布；新身份立即成为当前意图。 |
| [行为] D01 | 刷新 B 尚未结算时失活，恢复且仍开启 | 该刷新要求结算 `cancelled` / `unavailable`，不报告为失败；恢复按订阅规则：实例仍在则交付合格结果，已删则新建首查；不重放刷新、不恢复 A。 |
| [行为] D02 | D01 恢复前用户关闭或声明 C | 关闭则不恢复；新声明覆盖 B，只按最新 C 身份处理；变体：01 恢复前手动关闭；02 恢复前声明 C。 |
| [行为] D03 | D01 恢复的共享请求失败 | 按新实例间隔继续；不回退到刷新动作的错误策略，也不改写开启意愿。 |
| [行为] D04 | D01 恢复成功后再次失活/恢复 | 与普通订阅完全一致：实例仍在则按到期时间，已删则新建首查；不携带任何一次性恢复标记；变体：01 恢复时实例仍在；02 恢复时实例已删除。 |

### 4.9 内部结构与实现不变量（M）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [不变量] M01 | attach/detach/重复生命周期 | Handle.subscription 与 Resource.subscribers 双向完全一致，退出再加入必须是新的 Subscription 对象；变体：01 正常 attach/detach；02 重复生命周期后 attach/detach。 |
| [混合] M02 | 同一 Handle 刷新进行中改变开关/频率 | 刷新要求与订阅互不排斥、可同时存在；同一句柄只交付一次；关闭后新刷新仍能执行；变体：01 刷新期间改变开关；02 刷新期间改变频率。 |
| [混合] M03 | Resource 重建 | 新生存期身份与旧实例隔离；旧刷新要求、分区结果和任务不能混入新实例，不以计数器具体初值判断业务通过。 |
| [不变量] M04 | 排队、出队、忽略取消、完成 | queue/running 集合归属正确，running.size 等于尚未结束 load 数，没有计数漂移；变体：01 排队取消；02 出队后正常结束；03 实例销毁且旧任务先结束；04 实例销毁且旧任务晚结束；05 忽略取消期间槽位不释放。 |
| [混合] M05 | 首次声明/同参重声明/刷新合并 | 首次声明创建实例并首查；同参重声明幂等；刷新要求「动作之后启动」的任务，多次刷新合并到同一次后继请求；变体：01 首次 submit；02 同参重声明；03 在途任务之后的刷新补一次后继请求；04 校验失败后再次有效声明仍属新声明。 |
| [混合] M06 | 校验失败/后台失败/刷新失败 | 校验失败清 submission 并退出订阅；后台失败保留订阅与开启意愿；刷新失败只结算该要求；三者都不改写 `enabled`；变体：01 参数校验失败；02 后台失败；03 刷新失败（框架不改写 enabled）。 |
| [混合] M07 | 暂时退订与最后退订 | 页面 Display 独立保留；刷新要求以版本下限表达、不延长无关实例的生存期；空 Source bucket 清理；变体：01 暂时退订；02 最后退订。 |
| [混合] M08 | 刷新失败与关闭 Ref 的同步 watcher 重入 | 失败结果不被关闭产生的取消改写；没有失败参数的中间订阅，旧回调不清除新的刷新要求。 |
| [混合] M09 | Manager dispose，底层仍忽略 abort | 清注册/句柄/队列/分区/Timer/监听并释放私有 Store 和其 state；running 隔离至真实结束，不伪造清零；应用 Pinia 和其他 Store 保留。 |
| [不变量] M10 | 新项目构建与包级导入 | 不读取/import 旧项目源码、fixture、node_modules、锁文件或构建产物；独立安装验证；变体：01 不访问旧目录的独立安装构建；02 包级导入与包内清单核对。 |

### 4.10 参数、边界与规则覆盖（C）

这些条目把散落在设计、模型与计划中的要求变成可执行验收，不扩展业务范围。
参数值域与循环守卫由 C03/C13/C14 覆盖；G04 已按已确认规模关闭（§5）。

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] C01 | 外层或嵌套普通对象键顺序不同；显式 null 与缺失字段 | 顺序变化递归生成同 key；null 与缺失生成不同 key；不自动删除字段或补默认值；变体：01 外层对象顺序不同；02 嵌套对象顺序不同；03 null 与缺失；04 只改变一个嵌套字段值；05 同一对象重复引用与独立同值对象；06 整数形式字段名按明确排序编码；07 Unicode/转义字符及 `__proto__`、`constructor` 数据字段。 |
| [行为] C02 | 数组顺序改变；字符串与数字不同 | 分别作为输入变体执行，不能错误合并身份；变体：01 数组顺序不同；02 字符串与数字不同。 |
| [行为] C03 | 参数值域与循环守卫；validate 返回 Promise 被拒 | 大参数（如 1000 个客户）正常建立需求；循环与超深嵌套按非法参数拒绝；Promise 校验明确拒绝，无任务泄漏；变体：01 大参数正常建立需求；02 循环引用拒绝；03 超过内部深度守卫拒绝；04 validate 返回 Promise 拒绝；05 合法重复引用按值生成同 key。不要求小图指数展开提前拒绝或通用图算法。 |
| [行为] C04 | 合法 DTO 为 null | 视为成功数据，与缺失分区和未展示状态区分。 |
| [行为] C05 | 一个页面交付通知抛错 | 已提交 Store 不回滚，其他仍有效订阅继续交付；异常按 [DESIGN.md](./DESIGN.md) §6.4 报告 observer，报告出口失败也不阻断必要清理和后续有效交付。 |
| [行为] C06 | useRefresh 在不支持的异步 setup 外部时机调用 | 明确拒绝，不产生无生命周期清理的 Handle。 |
| [行为] C08 | SSR 渲染 | 不注册浏览器监听，不启动 Timer 或 load，各请求隔离。 |
| [行为] C09 | 间隔超过平台单 Timer 最大延迟 | 正确安排长等待，不截断为高频循环。 |
| [行为] C10 | 配置非法后修正 | 错误按次隔离报告，意愿与已声明身份保留；仅在当前资格允许时恢复订阅，不重放此前被拒绝的刷新。 |
| [行为] C11 | 请求适配器校验业务 DTO，框架建立独立副本 | undefined 或原生不可复制结果走共享请求失败（background）路径；原生可复制结果保留其值与独立所有权。不要求按原型、描述符、跨 realm 或循环逐类拒绝；不声称框架校验业务结构；变体：01 undefined 结果拒绝；02 函数/Proxy 等原生不可复制结果失败；03 原生可复制值（含 Date/Map/循环）保持独立所有权；04 null 有效；05 业务适配器拒绝错误 HTTP 响应；06 刷新失败与后台失败继续分别验证。特殊对象逐类防御不属于框架承诺范围。 |
| [行为] C12 | 同一来源对象在 Vue 接入前后使用 | 保持来源身份，不因代理包装制造重复 Resource。 |
| [行为] C13 | validate、load 尝试修改冻结参数 | 框架副本与稳定键不变，调用方原对象未被冻结；抛错按各自校验/执行路径处理；变体：01 validate 修改嵌套参数；02 load 修改嵌套参数；03 只读快照的键计算不修改原对象；04 根/嵌套均冻结且调用方原始对象未冻结；05 多处重复引用只冻结副本且键与 load 一致。 |
| [行为] C14 | 可信 JSON 参数与固定 Source 边界 | 值域与守卫规则按 U24。应用提供普通 JSON 记录；有限数不含 -0，undefined 值等编码歧义拒绝。特殊对象属于调用方违约，不要求框架枚举拒绝。Source.validate 每次接纳提交执行一次，轮询/恢复/读取不执行；变体：01 -0 拒绝，0/有限数合法；02 undefined/非有限数不能形成共享键；03 Source 固定 validate 对合法/非法业务参数各执行一次；04 恢复/轮询/readSnapshot 不重复校验；05 参数原对象不冻结、快照不随原对象修改。不要求描述符/跨 realm/构造历史检查。 |

### 4.11 公共契约验收（A）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] A01 | 合法 submit、参数拒绝、销毁后 submit、同步重入被替代、配置非法时 submit | 分别同步返回 accepted、rejected、cancelled/disposed、cancelled/superseded；配置非法仍可 accepted 记录新参数，修正后按当前资格恢复；accepted 不等待后台请求；变体：01 有效 submit；02 参数拒绝；03 销毁后 submit；04 同步重入被替代（可复用 F07）；05 配置非法记录新参数并在修正后恢复。 |
| [行为] A02 | refresh 成功、失败及各取消原因 | 按正式判别类型结算；共享请求失败不额外 reject，取消不等待底层结束；框架不因失败改写调用方 `enabled`。无论 `onError` 是否同步重入，当前失败都结算并通知一次；变体：01 成功；02 入口配置非法（拒绝且不产生请求）；03 共享请求失败（结算并通知一次，不改写 enabled）；04 superseded 取消（被新声明或新刷新替代）；05 unavailable 取消（关闭边沿、失去激活、隐藏）；06 disposed 取消。 |
| [行为] A03 | display 的编译期写入及运行时发布 | 调用方直接写入被类型拒绝；框架整体发布 args/data/origin/updatedAt，消费方不持有共享 Store 可变别名；变体：01 编译期拒绝回写 display；02 运行时完整快照发布与引用隔离；03 后加入的订阅者读到已有结果时，updatedAt 仍是原结果时间，不随交付时刻前进。 |
| [行为] A05 | Manager 重复/非法安装、非法构造参数、只读输入形态、应用卸载与 dispose | 同实例同应用安装幂等；跨应用/多活跃 Manager、非法 maxConcurrent 在创建边界拒绝且不留资源；`enabled` / `every` / `visible` 接受值、Ref 或 getter（含只读 Ref 与可写 computed），框架不再要求可写性；非法安装不破坏已有合法绑定；dispose 幂等不可复用，并释放自身安装绑定和私有 Store；同 App 的 app.use 去重路径无副作用且不复活；变体：01 同应用重复安装同实例；02 跨应用复用同实例；03 同应用安装不同活跃实例；04 应用卸载；05 显式重复 dispose；06 disposed 实例直接 install 或换 App 安装被拒，同 App 再次 app.use 可能被 Vue 去重但无监听/请求复活；07 非法 maxConcurrent；08 enabled 传值/getter/只读 Ref/可写 computed 均合法；09 非法跨 App/多活跃安装后原合法 Manager 仍正常工作，被拒实例不改目标 App 的绑定/注入，不新增监听或请求；10 旧实例 dispose 后同 App 安装新实例，单独触发旧清理/旧卸载回调不解除新绑定，实际应用卸载仍销毁新实例；同一 App 重复重建不累积活跃绑定。 |
| [行为] A06 | readSnapshot 的存在、缺失、null DTO、非法输入及复制失败 | 分别返回独立 DTO、undefined、null 或向读取调用方抛错；不调用 Source.validate/onError，不创建或保活 Resource；变体：01 现存 DTO；02 缺失分区；03 null DTO；04 非法读取参数；05 读取复制失败；06 disposed Manager 读取（包括同时传入非法参数，均不校验而直接返回 undefined）。 |
| [行为] A07 | 配置非法时新刷新；随后配置修正 | 返回 error/configuration，不产生请求、不递增声明代次、不替换已声明身份；订阅按资格退出并在修正后恢复；连续非法状态不重复通知；变体：01 新刷新因 every 非法被拒绝；02 因 visible 配置非法被拒绝；03 因 enabled 配置非法被拒绝；04 配置修正后恢复资格；05 连续非法状态通知去重；06 配置非法期间未结算的刷新要求不因 every 非法被取消；07 配置非法但仍读到 visible=false；08 配置非法但仍读到 enabled true→false；09 getter 不可读时不猜测 false。 |

**A07 必需补充变体：** 未结算的刷新要求在仅 `every` 非法时继续，不因此被取消；
同时读到 `visible=false` 或 `enabled` `true→false` 时，它随订阅退出一起结算（`unavailable`）；
getter 不可读的值不猜成 `false`。没有已知取消事件时刷新按共享路径正常结算并交付，
不能静默丢弃成功或使 Promise 悬挂。入口拒绝与这些在途变体分别记录，不能互相代替。

### 4.12 竞态、重入与故障注入（F）

| ID | 场景/操作 | 必须结果 |
|---|---|---|
| [行为] F01 | 普通加入时分区已有成功结果，产生它的 Task 已结束且当前没有 Task | 立即交付独立快照；未到期不请求。读取已提交结果不要求原 Task 仍存活。 |
| [行为] F02 | 刷新得到新结果；随后请求失败；甲隐藏后恢复，乙一直维持同实例 | 甲保留刷新得到的新画面，不因恢复回退到旧分区；未来有效成功才更新。 |
| [不变量] F03 | 声明替换触发 abort 时，回调立即隐藏本页或声明新身份 | 新身份先在就位；旧路径尾部不覆盖新声明或新刷新要求；恢复遵从当前声明；变体：01 旧 abort 回调隐藏；02 旧 abort 回调声明新身份。 |
| [不变量] F04 | Store 分区替换触发同步 watcher，watcher 改频率或退订 | 后续逐页交付重查资格、当前任务与刷新要求；退订者不接收，其他接收者不受影响；改频率不改变本次交付。已提交分区不因通知异常回滚；变体：01 Store watcher 改频率；02 Store watcher 退订。 |
| [行为] F05 | 刷新 A 失败，`onError` 同步关闭开关并声明 B | A 仍结算 `error` / `background` 并已发出该次通知；B 的声明保留并可完成；A 的清理或取消不覆盖 B。 |
| [行为] F06 | 声明 A 的准备阶段中，旧 abort 回调关闭、失活或声明 B | A 分别及时结算 unavailable / superseded，不建立订阅、不产生请求；未通过校验的 A 参数不成为恢复身份；变体：01 准备阶段关闭；02 准备阶段失活；03 准备阶段被新声明替代。 |
| [行为] F07 | 外层 submit A 的旧 abort 回调同步 submit B | A 返回 cancelled/superseded，保留 B；外层校验或清理不再写回 A。 |
| [行为] F08 | 同一同步调用栈内多次改变 every，随后关闭 | 旧任务即时失效；中间排队任务均不执行 load；唯一后续 flush 复核关闭后也不请求。 |
| [行为] F09 | 刷新期间 every 非法或可见 getter 暂不可读，但没有已知取消事件 | 未结算的刷新要求继续，其任务成功即结算 success 并交付；配置非法期间不建立订阅，修正后按已声明身份恢复；变体：01 仅 every 非法且 visible=true；02 可见 getter 暂不可读且无已知取消。 |
| [行为] F10 | enabled 成功读值之间夹有一次读取异常 | true→未知→false：关闭边沿结算未完成的刷新要求并退订；false→未知→false：暂停页的刷新继续执行；未知不覆盖边沿历史，不用历史值恢复订阅；变体：01 true→未知→false；02 false→未知→false。 |
| [不变量] F11 | 并发槽满，多个实例排队；其中一个退订后作废排队项 | 队列按 FIFO，退订者的排队项被移除而其他项顺序不变；不因满槽排队自排 flush 或用 Timer 查队列；真实 finally 后继续启动；变体：01 退订作废排队项；02 满槽无自旋；03 忽略 abort 后真实结束唤醒。 |
| [不变量] F12 | load 的同步前缀改变其他资源频率/可见性，或本次同步抛错 | 后续队列快照项重查身份；重算最新到期时间；新追加任务下一轮处理；抛错释放自身 running。空队列/满槽无忙循环；变体：01 load 同步改另一 Resource 的 every；02 load 同步撤销后续 Task；03 load 同步抛错；04 同步追加 Task 留待后续 flush；05 自然时间前进后发现刚到期。 |
| [不变量] F13 | m 个同参句柄同轮加入或退出；混合 every | 调度使用当前最小 every，结构操作不逐条扫描 min；每次资源扫描只聚合一次，计数随总订阅线性增长；不以固定耗时阈值代替算法断言。 |
| [不变量] F14 | 后台成功写 Store 或失败通知时，同步回调改频率、退订或声明新身份 | 正常结束时间在通知前已记录；每次外部效果后复核接收者身份，退订者与已被替代的刷新要求不再接收；finally 不覆盖新任务或新实例；通知异常不把成功改成失败，取消/过期响应仍不记录结束；变体：01 成功 Store 回调改频率；02 首个失败 onError 里退订，其余捕获订阅仍各收到一次；03 onError 抛错只报告 observer、不重新进入请求失败；04 过期成功/失败均不记时间。 |
| [行为] F15 | 甲持有旧实例的刷新要求时该实例全部退出，乙同键重建并成功，甲恢复 | 旧要求随实例销毁结算，新实例的刷新要求按自己的版本域判断；甲恢复后交付新实例现有快照，未到期不额外请求，本次才新建则首查；变体：01 新实例已有 entry；02 恢复时才新建实例；03 未结算的刷新要求在新实例上仍要求一次新任务。 |

---

## 5. 决策与门槛

| 决策 ID | 内容与状态 | 提案责任与依据 | 确认人 | 阻断节点 |
|---|---|---|---|---|
| G01 | **已关闭（取消）**：参数不设业务侧深度或字节上限——业务无法预知参数规模（例如一次查询上百个客户），框架不因为键大而拒绝合法查询；实现只保留循环/病态嵌套的内部守卫（§1） | 用户 2026-09-15 决定：不要限制参数 | 用户已确认 | 无（已关闭）。`parameterLimits` 已从公共契约删除 |
| G02 | 参数为可信 JSON 记录，保留 -0 拒绝和全字段按值共享；特殊对象逐类拒绝不属于框架承诺；DTO 由业务边界校验、框架原生复制 | 用户确认固定资源定义与一次边界校验 | 用户已确认 | 只核对 C11/C14 的现行边界测试 → **2026-09-16 已核对**：C11/C14 与全部 42 条单测通过，边界与 §2.2 镜像一致 |
| G03 | **已确认**：**本包自身的类型自 TypeScript 4.9 起可用于消费（已实测）**；Vue / Pinia / Node 取最新稳定版（当前实测 Vue 3.5.42、Pinia 4.0.3、Node 24.20），它们各自的 TypeScript 下限以它们自己的 peer 声明为准（`pinia@4.0.3` 声明 `typescript>=5.6.0`，`vue@3.5.42` 的类型需要 TS ≥5.4 或 `skipLibCheck`），本包不替它们承诺；最低运行环境需原生 `structuredClone` 与应用卸载钩子 | 用户 2026-09-15 指定「TS 4.9，其余最新」；仓库自身用 TS 7 构建，与消费方版本无关 | 用户已确认 | 发布前核验：目标环境安装、TS 4.9 类型消费与运行时能力。产物形态由 `pnpm build` 末段的构建后处理断言（无 TS 5+ 语法、无 `.ts` 说明符），公共入口形态由 `pnpm check:docs` 断言（无 `export type *`），**2026-09-16 本仓库已实测消费**：`typescript@4.9.5` + `module Node16` 下本包 12 个声明文件与 Pinia 4.0.3 类型均零错误；不加 `skipLibCheck` 时的 6 处错误全部来自 `vue@3.5.42` 自身 `.d.ts`（需 TS ≥5.4 的 `NoInfer`/`ToggleEvent`）。安装侧 `pinia@4.0.3` 声明 `peerOptional typescript>=5.6.0`，TS 4.9 消费方必须 `--legacy-peer-deps`，且 Pinia 的非可选 peer `@vue/devtools-api` 需由消费方提供。**2026-09-16 确认人裁决**：声明只限定到本包自身的类型（自 TS 4.9 起可用，已实测），Vue / Pinia 的 TS 下限以它们自己的 peer 声明为准，本包不替它们承诺。证据见工程 README「实际验证与边界」 |
| G04 | **已关闭**：目标设备性能充足；典型规模约 20+ 订阅、在途请求很少（页签失活即不在后台查询） | 用户 2026-09-15 给出的设备与规模结论 | 用户已确认 | 无（已关闭）。不设独立性能阈值；保留 `pnpm complexity` 与基线测量作为回归证据，基线数字与复现命令见工程 README「实际验证与边界」；**2026-09-16 交付记录（需求重写后重新生成）**：包 `vue-refresh-0.0.0.tgz`（58827 字节，SHA-256 `c951454b55b92e190734f32223bf8504307a9ce972245d63edb9efbee05095ab`，15 个条目 = `dist/` 13 + `package.json` + README）由已推送源码构建，连跑两次 `build`+`pack` 逐字节相同；干净消费方在 `typescript@4.9.5` 下零错误、SSR 与客户端场景通过（见工程 README「实际验证与边界」）。上一版 64637 字节 / `90ce71d6…` 是需求重写前的产物，已被本条取代。哈希记在本表而不写进 README：README 被 npm 强制打进包内（`files` 只列 `dist` 也会带上），包自身哈希无法写在包内文件里 |

角色表示未来实施职责，不暗示已指定具体人员。决策记录必须包含选择、依据、确认日期和关联验收项；
关闭门槛后同步更新本表。门槛数值只能由确认人给出，实现者不得静默填入或声称已达标；
已关闭项记录结论、确认人与日期后不再重复讨论。

### 5.1 已裁决的产品边界（已关闭）

下表是本轮对评审提出的产品与结构变更给出的裁决，全部已关闭：不再作为未决门槛，也不新增 §7 覆盖子项；其验收落在既有条目（如 A03、U16、§2.5）。
每行的依据即该行的结论说明；被否的提案不给第二入口，需要重启议题时先回到本节。

| 议题 | 结论与依据 | 关联 |
|---|---|---|
| 交付面是否给出结果新旧 | **给出**：这是产品取舍——页面要显示「数据多旧」，而结果时间是框架手上唯一可靠的依据。字段记法、发布时机与后加入者读到的取值以 §3（U16）为准，验收落 A03。 | U16、A03、§2.5 |
| 交付面是否给出实时「正在刷新」 | **不给出**：这是产品取舍——框架的在途事实是「某个实例正在取数」，共享下它并不等于「这一页的数据正在更新」，做成公开布尔值会让页面显示的状态与它手里的事实不同步。页面若确实要显示「刷新中」，只能自管一个本地标记：在发起 `refresh` 或 `submit` 后置位，按 `refresh` 返回的 Promise 结算或 `submit` 的同步 `SubmitResult` 清除，`onError` 到来时也清除。**不能只等 `display.updatedAt` 前进**：失败或取消的刷新不会产生新结果，此时时间不会前进。这个标记不代表真实在途，框架不代管、也不保证一致。 | U15、U16、U19 |
| 多租户/会话隔离是否新增 `scope` / `namespace` | **不新增**：身份必须只有一个函数（Source 身份＋完整参数值，取值规则见 §3（U02）），否则 Store 键、`readSnapshot`、交付门槛与实例重建都要引入第二个维度；边界由独立 Source 或参数表达，框架不做认证语义猜测。 | U02、§1 不纳入 |
| `readSnapshot` 是否保留 | **保留**，按「不保证新鲜度」交付（用户已确认不交付时间）：典型场景是另一个组件或刚挂载的页面在没有订阅时直读共享结果做首屏预填，不创建 Resource、不延长生存期。它是不经交付门槛的读取入口，取值规则见 §3（U17），验收落 A06。 | U17、A06 |
| 资格（U25）失败原因是否对外暴露 | **不暴露**：这是产品取舍——资格是内部推导事实，暴露它就要为「部分成立」定义对外语义并长期维护，而它不改变调用方能做的任何事。排查靠调用方自己手里的事实：`enabled` 由它写入、失活与隐藏是页面事实、是否已提交它自己清楚，框架不再回报一遍。若将来确实需要只读诊断，应作为独立开发者特性单独裁决。 | U25 |
| 覆盖清单与文首状态行是否移出规范文件 | **不移出**：§8.1 已把 §7 标为非规范性，对外承诺只由 §1–§3 承载；移出要跨文件查覆盖项，并因 §8 顺移改写全部 §8 引用。文首状态行改为与实现事实一致，不再重复进度明细。§7 在原位置压缩为条目式（只留工作单元与判定范围），压缩不等于移出，本裁决不受影响。 | §8.1、README |
| 不纳入哪些能力（§1 的边界清单） | **不纳入**：本地时钟与动画、历史回放推进、下载副作用、通用保存封装、自动保存重试、跨标签页共享、通用缓存失效、分页封装、业务权限判断、认证语义猜测、服务端强一致承诺。本行是这份清单的唯一维护入口，§1 只留指针与三例。 | §1 |
| 私有 Pinia 分区是否改为内存注册表 | **不改为**：U21/S07 要求每个 App 独立的私有 Store；且分区表必须浅持有——被 Pinia 深响应式化成 Proxy 后，交付副本的原生复制会抛错，「Store、各页面与 `readSnapshot` 之间没有可变别名」随之失效。收益（少一个 peer 依赖、少一个适配模块）小于删除该验证面与依赖契约变更的代价。 | U21、S07、L09 |
| 配置 getter 读不到时是否降级为「配置无效」 | **不降级**：读不到保留 `null`、未知开关不猜为 `false`、未知不覆盖开启边沿是已声明承诺，验收落 B08/F10/A07.09；降级会把「getter 抛错」变成静默停刷，或误杀暂停期间的单次查询。 | U18、B08、F10 |
| 安装矩阵是否删除「原地接管」 | **不删除**：同 App 重建（HMR、会话切换）既要只 `provide` 一次，又不能留下指向旧实例的第二份归属，靠的就是已 `disposed` 的原实例让位；删除要求应用侧重写重建路径，并反转既有变体。 | U21、A05（10）、S07 |
| 内部窄端口与调度职责切分是否重构（删 `ConfigurationHost`／把到期计算搬回 `Manager`） | **不重构**：窄端口是各适配面对外的最小事实集；把到期计算搬回编排层会让 flush 的批处理与交付时机重新暴露在同步栈上，端口也缩不到 3 项，净收益为 0 或负。 | F08、T11、M04 |
| 第十二版评审（元评审）提出的结构级提案 | **维持（不新增改动）**：S-A／S-B／R-A／R-B／R-C 五项已在本表关闭，按「被否的提案不给第二入口」不重开；两项新提案实测后亦不采纳——`nextIdentity` 改 `throw` 实际只减 5 行（不是 −12），且会把 §2.4 的 `submit` 返回契约改成同步抛错；`createNamespace` 收敛两级会把非安全上下文里唯一的 Crypto 随机源降级为时间戳／计数器（`getRandomValues` 是 `Crypto` 中唯一可用于非安全上下文的成员）。第十二版对「少一跳」（应表述为「去掉一个回调往返」）与 Pinia 响应性消费者的措辞纠正成立，不改变结论。 | 本次复核；`manager.ts:564-571`、`app.ts:34-42`、§2.4 |
| 代码审查 v5（`CODE_REVIEW.md`）提出的结构级提案 | **不采纳（不改 src）**：该报告未指出任何行为或契约缺陷，20 项均为结构偏好。ARCH-11／ARCH-12／API-01／API-02 属本表已关闭的窄端口与调度职责议题；ARCH-10 与 §1.3 六协作者蓝图、MOD-02／MOD-03／ARCH-13／MOD-04 属 §6.2 禁止的新增模块与架构层，需先裁决再议；STATE-01／FLOW-05 与 DESIGN §3.6「两种要求＋没有要求、不建立额外枚举副本」冲突（外层 `null` 与内层 `null` 不在同一层级）；STATE-02／API-04 会新增 `Handle` 持久字段。按原样实现会失败的四处硬反例：`requestFlush` 改 private 会破坏 `QueryHost`（`query.ts:50/99`）；`instanceof QueryRun` 对 `interface` 不可实现；删循环内 `currentTask` 复核会丢掉 F14／C05 的同步重入防线；`setBrowserVisible` 只同步独立查询会让隐藏页继续后台刷新。其度量基线另有 9 处与代码不符（方法数 12／16／34、分支 56／94、测试行数 510／298、`Handle` 8 字段、闭包 42 行等），实测 maxCCN 为 8 而非 ~12。 | 本次复核；§6.2、DESIGN §3.6、F14、C05、U25。其中 `QueryHost`／`QueryRun`／`commit` 已随本表下一行的需求重写删除，该行的否决理由按当时的契约成立 |
| 第十三版评审（根本简化问题清单）提出的结构级提案 | **整体不采纳，只收下 5 项行为中性小改**：A1／A2+A3／C1／C2(1) 都是本表已关闭议题的重开（窄端口见"内部窄端口与调度职责切分"行、`Delivery` 三态见"代码审查 v5"行、配置降级见"配置 getter 读不到时是否降级"行），本版给的新证据（测试零引用、补出第 4 个消费点）不触及各行的依据。D1（`nextIdentity` 改 `throw`）另有实测反例：`tests/core.test.ts` 的"sequence exhaustion"用例已覆盖三个计数器并断言观察者日志 3 条，改 `throw` 后 `submit` 变成同步抛错（违反 §2.4「`disposed` 返回 `cancelled`」，与本表"第十二版评审"行已否决的是同一修法），该用例直接转红；其"耗尽分支无测试（按日志字符串 grep 零命中）"是取证方法错误。第一档声称合计约 −48 行，逐项实测 −23（含 D1）／−18（不含 D1）；A4／A5 按纯减法不可实施（要让编排层直接持有 `queue`／`running`，或读调度器私有字段）。C3 与「提取所有状态为提前枚举的常量」的既有指令冲突（且所举 5 处"判别读取点"有 2 处是构造）；C4 把"整体替换 `Submission`"误读成"要求也整体替换"——`setRequirement` 是 `delivery` 的唯一写入点。C2(2) 的 `errors` 单元素数组成立，但改后为 0 行而非 −4。**已收下**（src 1920 → 1916 行，测试 52 条全绿）：删 `Scheduler.disposed`，存活以 `ScheduleHost.isDisposed()` 为唯一事实（`flush` 已在用，派生证明即此；回归为新增用例"disposed manager: a flush request from any port is inert and schedules nothing"）、删 `removeHandle` 中被 `forgetActivity` 身份守卫保证为 no-op 的置空、删 `dispose()` 末尾防御性 `resources.clear()`（零订阅者实例不存续，U08）、`readConfiguration` 的 `errors` 数组改单变量、`reportObserverError` 直写三个已知字段。其备选"删 `removeHandle` 里的 `releaseActivity` 调用"不可采纳：订阅将永不释放，页面卸载后实例仍按 `every` 永久后台空转。该报告另有 11 处基线与环境陈述与代码不符，其中四例：`node -v` 实测 v24.20.0 且三项闸门当场全绿（不是"无可用 Node、闸门无法执行"）、验收叶子 136（实测 132）、本文件 630 行（实测 1015）、`requestFlush` 调用点 6 处（实测 5 处，`626` 不是调用行）。 | 本次复核；§6.2、§2.4、"第十二版评审"行、U08 |
| 第十五版评审（过度设计专项评估与需求归因）提出的两项提案 | **整体不采纳（不改 src）**：B1「`query` 的 runner 可选化」是公共契约**增量**而非减法——复核时按缺省复用 `source.load` 实现，实测 **0 行、结构分支 134 → 135**，并强制同步 §2.2 契约镜像、§2.1 契约表、README「参数怎样定义」示例与 README 度量行，另需 1–2 条测试覆盖缺省分支（`pnpm check:docs` 在镜像与度量行两处当场转红）；缺省还会同时落在 `vue.ts` 与 `manager.ts` 两处，需指定唯一归属。其新依据成立（统一文档 §2.1 与 README 的正式示例里 runner 与 `load` 是同一调用体），但按「不新增第二入口、裁剪不必要的分支」不采纳：纯增量改动等接入方提出再做，成本不变。B2「把复杂度归因账作为 §5.1 附录」不采纳：本表每行已含结论、依据与关联，附录的「重启前提」列等于给被否提案第二入口，与本节「被否的提案不给第二入口」及 §8.1「取舍理由只记 §5.1」的收敛方向相反。C 档是记账，不构成提案。其 §5.3 的唯一合并候选「`delivery.ts`(49)+`diagnostics.ts`(58)，合并不改变依赖方向」不成立：保留结果边界的形状会让 `source.ts → delivery.ts` 成为同层运行期边，门禁 9 直接失败，DESIGN §1 第 28–29 行「`diagnostics.ts` 是零依赖叶子」正是这条理由；只有并进 L0 的 `diagnostics.ts` 才方向中立，但要同时改 `LAYERS`、两份模块清单与职责表。另有 10 处基线与表述与代码或公开仓库不符：逐文件行数把 `model.ts`/`public-types.ts` 写成 257/190（实测 256/189，其表合计 1918 而非 1916，「与 `complexity.mjs` 的差异是末行空行计数」不成立——`complexity.mjs` 用 `trimEnd()`，12 个文件都没有末行空行）、规范侧 812+401（实测 811+400）、身份复核谓词「27 处调用点、manager 18」（实测 28 处标识符、20 处真实调用点，manager 19）、「状态表 18 个域中 2 个标注推导」（实测 19 行、3 处 `**推导**`）、「Manager 约 39 个方法」（实测 41 个方法 + 构造器）、「§7 顺序约束第 3 条声明的六个边界」（该条只声明五个，abort 之后来自第 1/2 条）、「本环境 Node v16.18.0」（宿主确有 `/opt/证联讯/node/node` v16.18.0，但仓库环境 `node -v` = v24.20.0、`pnpm -v` = 11.24.0，三项闸门当场全绿，未运行测试是自选限制而非环境阻塞）、「285 年不可达」（照抄 DESIGN §3.2，毫秒口径应为约 28.5 万年，本轮已修）、「`vue-query` 核心约 3-4 个模块」（`@tanstack/query-core@5.102.8` 的 `src/` 有 23 个模块，3–4 个文件是 `@tanstack/vue-query` 适配包的形状）、版本号（本版把上一轮 5 项删除记在「第十四版」名下，本表记为「第十三版评审」）。其机器读数与本次复测全部一致：12 文件 / 1916 行 / 134 分支 / maxCCN 8、132 叶子、25 锚点、52 条测试、observer 包装 17 处（manager 7／delivery 4／diagnostics 3／query 2／source 1）、34 处 `disposed` 检查、B10 十一个变体、A07 九个变体、四个窄端口 4/3/5/4 项、`Scheduler` 6 个下行方法 + 5 个 host 回调、`Handle` 10 字段＝4 固定端口＋6 可变，且每处身份复核都落在 DESIGN §7 的边界上。 | 本次复核；§6.2、§2.2、§8.1、DESIGN §1/§3.2/§3.8 |
| 外部专家评审（`统一刷新-评审.md`）提出的需求重审 | **采纳方向，进入需求重写**：主线是「订阅数据＋主动刷新」——首次/后续提交差异改为幂等声明＋显式刷新动作（`refresh`，命名按 §6.2 的普通命名授权）；删除 `query` 独立通道（`QueryRun`／`QueryHost`／`commit`／只写本页）与「query 成功后后台接管」（附条 A）；主动改频率只重算下次到期、保留在途请求。落地按该评审 §8 的顺序：先改正式需求与验收（§0–§4、§7），再改 DESIGN，最后改实现与测试，每阶段保持 `pnpm check:docs`／`typecheck`／`test` 全绿。其 §0.3 的边界（暂停刷新走同一执行路径、页面退出取消临时等待、刷新等待「动作之后启动」的请求、主动与自动共用队列与并发上限、身份变化立即成为当前意图、无需求后仍即清理）按其自述的「推荐默认值」落入规范，实现方不另填。同时登记两件事：(1) 其 §2.1 的契约口径冲突已按本行修正 §0.1／§1——原文「至多一份在途请求」「同 key `load` 并发数 ≤ 1」与实现不符，实测同身份可有两个物理在途 `load`（一个已取消未结束、一个有效），`Resource.task` 只指向有效的那个，这与 T12／F11「abort 不释放物理槽」一致；(2) 其 §6.1 触及本表"代码审查 v5"行关闭的 `Delivery` 三态——该行依据含「query 后不回退旧数据」这一产品承诺，本次需求变更移除了该承诺的前提，因此属**前提变更后的重新裁决**，不是第二入口。本次不顺带变更 Pinia 分区、安装接管、三态配置、参数身份、副本独立与真实并发计数。**已落地**：规范、DESIGN、实现与测试已按本行改写（`src` 12 → 11 个模块，`query.ts` 删除；公共契约改为 `submit` 幂等声明＋`refresh`；验收 132 → 129 叶子）。该评审自身声明：新方向尚未实现验证，§0.3 的边界是推荐默认值而非逐项确认；其 §2.2 的五处文档失准经复核四处属实（`DESIGN` §3.3 配置字段名、§5.1 时钟措辞、`model.ts` 两条过时注释，以及上一行已修的 §3.2 序号口径），已随本行一并订正。 | 本次复核；§0.1、§1、§2.1/§2.2/§2.4、§3（U03–U06、U10、U11、附条 A）、§4（Q 组、S02、T04、D 组）、"代码审查 v5"行 |
| 第十七版评审（对齐新方向：订阅＋主动刷新的契约收敛） | **主线已落地，只收下记账与四处勘误**：该版 §二／§六／§七 主张的三项取舍（删首次差异、删 query 独立通道与接管、改频率不重取）与 §十 第 0–4 步，已由 `0fabb00`／`6e6a144`／`ef216a3`／`e927cb2` 全部完成——它的基线停在重写前的 `e61e7d8`（12 文件／51 测试／132 叶子），因此其「待写入正式需求、非已实现」的表述对本仓库已过期。本次据其 §十一 的待裁项裁决：**接口命名维持 `submit`＋`refresh()`**（不采纳 `declare`，也不采纳 `refresh(args?)` 可选参——那等于把「声明新身份并立刻取一次」变成一条捷径，重新模糊声明与刷新的边界）；**错误 origin 维持 `background`／`validation`／`configuration`**（它主张把 `background` 并入 `execution`，实际相反：`execution` 已随独立查询路径删除，保留的是描述失败步骤的那个名字）；**归因账不入 §5.1 附录**（第十五版 B2 已裁决不采纳，「被否的提案不给第二入口」继续适用）；**验收叶子保留 129 条**（不按其估算 85–90 再删一轮——Q／D 组叶子是改写而非删除，新语义的合并、结算、退出取消与「恢复不重放」需要自己的验收）。同时登记它与实测不符或需更正的三处：(1)「Q 组约 10 条删除、D 组 4 条全删」实测为 Q 组保留 11 条（仅 Q12 移出）＋ D 组 4 条全部改写保留；(2)「运行时状态域 10 → 7」未发生——`Delivery` 三态是被「等待者集合＋版本下限」等价替换，DESIGN §3.8 仍为 19 行、3 处推导；(3)「删 `closeQuery` 关闭边沿」不成立——关闭边沿仍须结算当时未完成的刷新要求（B05／F10／Q07），该命名操作按新语义改名为 `cancelRefresh`，`ConfigurationHost` 仍是 3 项事实。它的 §五（P1 口径）已按其三句话改写（`f9c8032`），§六「标准化」列的五处说明纠错已修四处（第五处「分层描述」经复核判定为措辞澄清而非失准），§十一问题 3（等待者载体）由实现回答：**版本下限＝代次数字**，配 `Scheduler.isRunning(task)` 区分「排队」与「在执行」，合并／替换／重建三场景分别由 Q06／B16a／B14 断言，无需再裁。版本号链条：该版自称承接「第十五、十六版」，但工作区仅存在第十五版与外部评审 `统一刷新-评审.md`（已归档），第十六版仅见于该版转述，故本行按转述记账、不声称其原文存在。 | 本次复核；§0.1、§1、§2.1／§2.4、§3（U04–U06、U11）、§4（Q 组、D 组、B14、M05）、DESIGN §3.6／§3.8 |

---

## 6. 工程、执行与记录约束（非规范性：执行约定）

### 6.1 工程与运行

包名 `vue-refresh`，Vue/Pinia 为 peer dependency，包只导出三个正式函数与公共类型。
本地与 CI 使用同一组命令，失败返回非零：

```sh
pnpm install --frozen-lockfile
pnpm typecheck        # tsc --noEmit
pnpm test             # node --test tests/*.test.ts
pnpm build            # vite 库构建 + 类型声明 + 说明符改写与产物形态断言
pnpm build:demo       # 演示页面
pnpm test:browser     # Playwright 集成场景
pnpm complexity       # 每文件与函数的行数、分支、圈复杂度、嵌套；加 --write 回写 README 度量行
pnpm check:docs       # 12 项一致性门禁，清单与各项动机以 scripts/check-docs.mjs 自述注释为准；
                      # 并打印叶子→测试标题的追溯报告（只报告，不阻断）
```

- 依赖版本由锁文件固定；实际验证环境与结果以工程 README 为准，不声明其他版本支持。
- `node scripts/trace-leaves.mjs --map` 按能力域打印「`U` 锚点 → 判定叶子 → 是否被测试标题点名」的映射；
  未被标题点名的叶子在脚本顶部的 `EVIDENCE` / `STATIC` / `PARTIAL` / `UNVERIFIED` 表中逐条落定（§7 13.1），
  四项都不登记或重复登记的叶子会被报成账目问题；
  `pnpm check:docs` 只打印该报告的汇总行（它是事实报告，不参与判定）。
- 演示页与验证页见 README；HTTP fixture 只由开发服务提供，静态演示构建不算后端服务。
- 不改预期、不吞错、不关闭 strict、不跳过失败来变绿；测试错误按契约修正并说明。
- 部分路径通过不等于整个验收 ID 通过；原生探针不等于框架通过。
- 权限或网络受限而未执行的检查记录为环境阻塞，不计作业务失败，也不伪造通过。
- 工程目录若由 macOS 归档解压得到，需先清掉 `._*` AppleDouble 文件、`.DS_Store` 与平台专属缓存，
  再删除 `node_modules` 重新 `pnpm install`；否则残留的跨系统缓存会让类型检查、构建与浏览器测试无法运行。
  完整命令序列与本次实测结果见工程 README。

### 6.2 记录方式

只维护一份简短记录：当前切片与代码位置、实际环境/依赖版本、运行命令、预期与实际结果、
相关验收 ID、未验证项、下一动作。有 Git 时记录提交与未提交变更；无 Git 时保留可重跑源码与测试。
覆盖状态只用 §4.1 的三个词：`已验证` / `部分验证` / `未验证`；模块阶段用端口替身验证局部行为时
记 `部分验证（模块端口替身）`，不另设「已覆盖」「模块部分通过」等同义词。

首次阅读顺序：§0.1 → §1 → §2.1/§2.4 总体流程 → §7 当前工作单元（§0.2/§0.3 按需查阅）→ 实现时按当前动作查阅契约，
不先通读全部字段和 47 个子项。日常不为每个小批次生成全量摘要，也不逐子项审批。

执行约束：

- 用户明确指令优先。已确认选择不重复审批；普通命名、局部修复和当前切片所需的跨模块修改自行完成。
- 契约冲突须给出原文位置、最小反例和推荐，只暂停受影响部分。
- 保持既有运行时模块划分与公共 API；不新增持久字段或架构层，不用成功占位伪造未实现能力。
- 删除字段必须有派生证明及对应回归，不按百分比机械删减。
- 删除行为条目或验收叶子时必须清理反向指针：grep 该条目的关键名词，修正正文、README 与本节引用；
  主题式引用（`……的行为规则见 §3（U…）`）一旦变化，同步更新 `check-docs.mjs` 的 `TOPIC_CITATIONS`。
- 生产模块不得导入测试代码；构建与测试脚本不得读取旧目录。

### 6.3 停止与继续条件

| 情况 | 处理 |
|---|---|
| G01 已关闭（取消） | 边界与依据见 §5，不阻断编码、测试与交付 |
| G03 已确认 | 版本范围按 §5 声明；发布前在目标环境核验安装与 TS 4.9 类型消费 |
| G04 已确认 | 按已确认规模做一次基线测量并记录，不设独立阈值，不宣称超出该规模的性能 |
| 当前行为失败、契约清楚 | 修复并回归，再扩下一行为 |
| 需要改变契约或新增字段/架构层 | 给出最小反例和建议，等待对应决定 |
| 环境或权限阻止运行 | 记录实际原因，不把模拟通过当真实贯通 |

出现语义冲突、新参数显示旧结果、取消后写入、未完成 `load` 超 `maxConcurrent`、退订后画面变化、
旧错误关闭新操作、无限校验重试或零间隔 Timer、隔离无法证明时，先修正当前问题，不继续扩功能。

**正式交付**按 §7 的当前覆盖状态逐项标注（`已验证` / `未验证` + 原因，不构成硬阻断），并核对 §4 全部验收叶子及变体与 G01/G03/G04，
保留源码、测试、锁文件、实际环境、运行结果与最终包 SHA-256；源码/测试/依赖变化后补受影响回归。
恢复时读同一记录并核对实际文件；历史结果与现状不一致则重验受影响项，不重新审计整套文档。
不授权自动发布、部署或发送消息。

### 6.4 验证方法（非规范性：执行约定）

本节只说明怎么取证，不改任何判据；判据（叶子与变体）在 §4，两者冲突时以 §4 为准。

#### 6.4.1 先核对业务依据
将场景依据分成「已确认需求」「工程设计决定」「待验证假设」，各自关联规则/验收 ID、来源与确认依据。
以需求与已有明确决定编写正例、反例和预期，不能根据当前代码输出倒推预期。
没有获授权的真实素材时可用明确标注的合成例继续验证；合成例通过只证明所列契约场景，
不证明真实页面适用性、业务规模或阈值合理。G01–G04 的现行边界见 §5。

#### 6.4.2 验证连续事件与进展
在已有 Clock/deferred 辅助工具上运行真实 source/store/manager 与已实现的核心路径，
只替代时间、外部请求和暂未集成的 Vue 输入，不另写一套假 Manager 作为被测对象。
固定序列必须可重放，再对同类动作做有界枚举或带 seed 的生成。
使用可控时钟与可手动完成的请求验证事件顺序；集成测试使用真实 Vue、KeepAlive、Pinia 和可延迟响应的 HTTP 服务。
分别观测请求、取消、排队与尚未结束的框架 `load` 数、Store 分区、页面快照、外部开关和错误通知；
模型测试另检查对象关系与清理不变量。

| 固定序列骨架 | 主要检查与验收 |
|---|---|
| 刷新 A → 声明 B → B 成功 → 旧生存期的 A 晚到成功/失败 | 只接受当前身份的结果，旧任务无晚到副作用或错误通知；Q05、R11、B16a |
| 最后退订删除 A → 同 key 新建并成功 → 旧 A 晚到及 finally | 新分区、快照和任务不被旧生存期写入或删除；P07、R05、M03 |
| 槽满且有排队项 → 取消运行项但不结束 → 推进时间 → 旧执行真实结束 | 未结束 load 数不超限、不提前开槽；真实结束后队列按 FIFO 继续；T10–T12、M04、F11 |
| 成功 Store 回调或首个失败 onError 同步替换 Task → 旧回调链继续返回 → 新 Task 成功 | 旧通知停止，旧 finally 不清新 Task，新任务正常交付；F04/F14、B10 |
| 刷新得到新画面 → 下一次请求失败 → 本页隐藏再恢复且他人维持旧分区 → 新成功 | 恢复不回退旧 entry，合格新结果可更新；B02、F02 |

除「没有错误写入」等安全性，还必须验证实际进展：有效请求确实启动、当前成功确实交付、
后台失败后按间隔重试、真实空槽出现后有效队列继续运行。
始终不发请求却满足全部安全性断言的实现不能通过。
记录 seed（枚举则注明）、完整动作与完成顺序、时钟/微任务推进、断言与可重放的缩减反例。
有限探索不报告为穷尽证明。

#### 6.4.3 检查测试能否发现关键错误
对下列有限故障族做定向代码故障注入，§7 的「13–14 核账与性能」组核账；不建设新验证平台，不要求全项目变异分数。

| 故意引入的代码错误 | 应检出的现有场景 |
|---|---|
| 放行已失效的刷新要求，或放行同 key 旧 Resource 的晚到结果/清理 | Q05、R05、M03；分别验证刷新要求与任务身份的保护 |
| abort 时提前释放 running 槽 | T12、M04、F11 |
| 把必要的深层快照复制替换为浅复制 | L09、B12b；分别检查页面间与 Service 返回对象别名 |
| 丢失或提前清除新查询快照的交付门槛 | B02、F02 |
| 首个失败 onError 替换 Task 后仍继续通知其余旧订阅 | F14.02、R01/R02 |

执行顺序固定为：未改动基线通过 → 在隔离临时副本中只注入一个故障 → 目标业务断言失败 →
恢复改动 → 同一目标测试再次通过。保留基线版本、注入差异、实际命令/退出码和失败断言证据。
编译、导入、设施或无关断言失败不算检出。未检出要区分测试缺口、注入未触达、仍被其他有效保护拦截等原因。

---

## 7. 覆盖清单：15 阶段 / 47 子项（非规范性：追踪）

15 阶段与 47 子项的编号只用于追踪：不要求按序机械完成，也不作为开工前前置流程，按真实进度回填。
判定标准一律查 §4，本节只记工作单元与判定范围，不复制判据；模块阶段允许用明确的端口替身验证
局部行为，记为 `部分验证（模块端口替身）`（状态词汇见 §6.2，不等于对应验收 ID 已通过）；
完整阶段必须等待最后一个依赖模块具备后执行。括号内为该组覆盖的子项编号。

- **01–03 基线**（01.1–01.4、02.1–02.3、03.1–03.3）：契约整理与冻结结果核对、工程与工具链、最小测试基础（虚拟 Clock、deferred、请求/取消/通知记录）→ §2 全量、A07、M01、C14；核心导入无浏览器副作用
- **04–06 身份与生命周期**（04.1–04.3、05.1–05.3、06.1–06.3）：参数与 Source、Store 与独立快照、Resource 与订阅 → P 组、S01–S03、L01–L05、M03/M05
- **07–09 执行路径**（07.1–07.3、08.1–08.4、09.1–09.4）：Task/FIFO/真实并发、唯一 flush 与 Timer、submit/refresh 完整路径 → T 组、Q 组、A01/A02/A07、D01–D04、F02/F04/F05/F08、F11–F15、M04
- **10–11 接入与真实 I/O**（10.1–10.4、11.1–11.3）：Vue 适配与安装销毁、HTTP/SSR/会话边界 → A03/A05/A06 全部变体、B 组、L10、R09–R11、S05–S08；受限项按真实状态记录
- **12 代表页面**（12.1–12.3）：三个可编译运行的真实 Vue 页面 → `已验证`：实现于 `examples/pages/`（另含 B09 组合视图），下表三条页面的真实交互与 `tests/pages.spec.ts` 的断言一致；页面不手写 Timer，也不保存旧参数或操作版本
- **13–14 核账与性能**（13.1–13.2、14.1–14.3）：全量回归与逐 ID 证据、定向故障注入、按已确认规模做一次可复现基线 → §4 全量、§6.4.3；不设独立阈值，无未解释失败或资源泄漏。状态：14 性能基线 `已验证`（`node scripts/benchmark.mjs`，数字与复现命令见工程 README）；13.1 逐 ID 证据 `已验证`（132 个叶子逐条落定：`已验证` 120、`部分验证` 12、`未验证` 0；原先 9 项未验证已由八条定向用例点名（B04/F08、B16a、C05、C06、L06、L08、P05、S06，均做了撤除实现即失败的反证），并补 Q06 收紧「相同参数再次查询」；证据账在 `scripts/trace-leaves.mjs` 的 `EVIDENCE`/`STATIC`/`PARTIAL`/`UNVERIFIED` 表，未登记的叶子会被报成账目缺项）；13.2 定向故障注入 `已验证`（三条加固场景已按 §4.1 通则移回 §4 `B10` 变体 06/08/09，撤除实现即失败的反证已做）
- **15 本地交付**（15.1–15.2）：ESM + 声明、包级导入、验收报告 → 源码与包一致、可复现安装/验证、G01/G03/G04 均有结论记录（依据、确认人、日期）；只交付本地产物，目标环境与真实业务接入由接入方核验。状态：15.1 `已验证`（包 SHA-256 见 §5 G04 行；干净消费方的包级导入与 TS 4.9 消费实测见工程 README）；15.2 验收报告 `部分验证`（G01/G02/G04 的阻断节点已核销，G03 的 TS 4.9 实测已完成但**声明下限是否上调到 5.6 待确认人裁决**）

**三条代表页面必须展示：**

| 页面 | 必须展示 |
|---|---|
| 查询列表 | 完整参数提交、独立启停、分页排序复用已提交参数、失败关闭、暂停仍可单查 |
| 行情面板 | 无查询按钮、一次提交已准备参数对象、响应式频率、显示数据多旧、后台首查失败继续、失活冻结 |
| 双组件共享 | 1s/5s 同参共享、切换品种、单页暂停、全部退订、重新进入及 Store 快照隔离 |

另需展示无启停按钮的刷新组合：在同一个同步块里开启意愿并声明新身份（B09）。

---

## 8. 维护约定

### 8.1 唯一维护入口

| 要确认什么 | 位置 | 性质 |
|---|---|---|
| 术语定义与词汇表 | §0 | 非规范性 |
| 目标、范围与决策记录 | §1、§5 | 规范性（不含行为细节） |
| 公共 API 与返回值 | §2 | 规范性 |
| 行为规则 | §3 | 规范性；行为的唯一规范表述 |
| 触发锚点（`U01`–`U25`） | §3 | 索引；锚点所在的 R1–R8 条文是行为本体，锚点不重不漏且每个引用都有落点，由 `pnpm check:docs` 断言 |
| 验收预期与判定标准 | §4 | 判定依据 |
| 覆盖范围、完成条件与执行约束 | §6、§7 | 非规范性：追踪与执行 |
| 模块划分、数据模型、参数与结果边界、调度与生命周期 | [DESIGN.md](./DESIGN.md) | 设计 |

只有「规范性」各节构成对外承诺；「判定依据」用于验收，「追踪与执行」只约束本项目自身的推进方式。
**行为陈述的规范效力只在 §3。** §1 写目标与授权边界，§4 提供判据与证据，§5 写决策与理由：
这三处涉及具体行为时只写边界、结论与指针，不复述 §3 的规则细节（守卫、计数、字段来源这类）。
§0.1 作为入口词表可以点出概念的判定要点，规范表述仍在 §3。任何一处与 §3 冲突时以 §3 为准；
被删掉的行为细节按 §6.2 的反向指针约束清理引用。
公共类型变更同步 §2.2；行为变更只修改 §3 并更新相关测试；字段所有权与实现细节属设计文档。
取舍理由（为什么不做另一种设计）只记 §5.1，规范正文只写行为与取值，不重复写否决理由；
新增正文段落若含「为什么不做」「不可能」「若需要……就用错了入口」这类句式，说明理由该移入 §5.1
（这句判定不了语义，`pnpm check:docs` 只保证结构与引用，人工核对）。
运行证据只来自实际执行：历史报告不替代真实运行，未验证的实现验收继续保留。
